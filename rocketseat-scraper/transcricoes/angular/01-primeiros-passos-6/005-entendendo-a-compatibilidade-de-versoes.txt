WEBVTT Nesse vídeo, nós vamos comentar sobre as regras de compatibilidade entre as versões de Node, do Angular CLI e do NPM. Também é um ponto importantíssimo e você precisa compreender essas regras. Vamos dar uma olhadinha aqui. Basicamente, nós vamos ter aqui regras de compatibilidade entre o Node, o NPM e o Angular. O Angular aqui seria o Angular CLI. a versão do Angular CLI que vai estar instalado na sua máquina, ou que você vai utilizar por meio do comando npx, que nós vamos falar mais pra frente. Mas basicamente algo que você tem que ter em mente aqui é o seguinte, você vai digitar version compatibility angular ele vai entrar aqui nesse site do angular que é o angular.dev barra reference barra versions você vai entrar nessa tela aqui version compatibility aqui ele vai ter uma relação para gente de versão que nós temos que utilizar do angular CLI e do Node.js e aqui ele já fala também do TypeScript e do SXJS. Qual que é o ponto aqui? Você inicialmente vai instalar o Node na sua máquina em uma certa versão e vai criar projetos Angular em uma outra versão então elas precisam estar compatíveis porque por debaixo dos panos o Angular CLI vai utilizar o Node para executar as suas funções. Por exemplo, fazer o build do projeto, fazer o serve do projeto. Para isso, o Angular CLI utiliza o Node. Então, eu tenho que garantir que o Node instalado na minha máquina disponibilize e tenha todas essas funcionalidades necessárias pelo Angular CLI. Por isso que nós temos que compreender essas regras de compatibilidade. Por exemplo, nós vamos estar aqui criando um projeto na versão 19, nesse módulo em específico. Quando eu for criar um projeto na versão 19, eu vou estar utilizando o Angular CLI na versão 19. Só que para ele executar certinho, sem nenhum problema, para fazer o build do projeto, o serve do projeto, enfim, eu preciso ter a versão 18.19.1 do Node, ou a versão 20.11.1 do Node, ou a versão 22.0.0 do Node. Se eu tiver, por exemplo, a versão 1 do Node, não vai funcionar, porque a versão 1 do Node provavelmente não tem funcionalidades necessárias pela versão 19 do Angular CLI. Por isso que tem que tomar bastante cuidado com isso. Eu quero que você salve nos seus favoritos essa telinha aqui da documentação do Angular, tá? Version Compatibility. aqui também já falando da parte do npm é a mesma coisa quando nós instalamos o node o npm já vem junto e aí já vem uma versão compatível com ele então não tem nenhum problema nesse caso aqui você tem que tomar cuidado mesmo é na versão do node que você vai instalar bom então aqui não tem nenhum segredo é um vídeo simples mostrando essa regra que você tem que compreender aqui e sempre memorizar vou criar uma versão x do angular um projeto na versão x deixa eu ver quais as versões disponíveis do node eu consigo utilizar aqui caso você não tem essa versão disponível você tem uma outra você vai ter que desinstalar essa versão do node e instalar uma nova versão que seja compatível Fechou? Bom, então para esse vídeo foi basicamente mostrando essas regrinhas aqui de compatibilidade de versões que é algo muito importante que acaba dando uns erros bem chatinho na hora de executar os comandos ali do Angular, sei lá, ng-serve, ng-build, dá uns erros de compatibilidade lá e é difícil de encontrar a solução. Então, sempre preste atenção nisso vou criar uma versão x de um projeto angular qual versão compatível com essa versão que eu vou estar criando aí você instala o node de acordo com a versão compatível show espero que você tenha gostado desse vídeo e eu te vejo no próximo