WEBVTT Nesse vídeo nós vamos fazer a implementação dos dois endpoints relacionados a parte de favoritos do filme, que é adicionar ele aos favoritos e remover ele dos favoritos também. E esses métodos eles vão ficar no nosso service de Favorites, então aqui o nosso Favorites API, vamos entrar dentro dele, temos já os dois métodos aqui, vamos criar as implementações. No caso, para adicionar um filme aos favoritos, o que nós precisamos? Bom, dando uma olhada aqui no nosso Insomnia, na parte de Favorites, Add Movie to Favorite, nós precisamos passar o ID do filme que eu quero adicionar lá na URL, E aí ele vai ter esse retorno aqui pra gente, que é esse objeto com a propriedade message, que é do tipo string. Beleza, vamos fazer isso então. Aqui no caso no EdgeMove, eu vou colocar moveId, que é do tipo number no caso, colocar aqui do tipo number mesmo. E aqui eu vou colocar um return $this.httpclient.post, se eu não me engano é um post, isso mesmo. Então, .post. E aí vamos pegar aqui a base da nossa URL, que é esse http localhost 3000. Barra, deixa eu dar uma olhada, barra favorites, barra id. Então vamos copiar tudo aqui, colocar no formatinho de string, barra favorites, barra, e aí eu concateno aqui com o movie id que nós estamos enviando, né? Que nós estamos recebendo na verdade. Agora, o que está acontecendo aqui? Ele está reclamando que eu não estou passando o segundo argumento. No caso, eu vou estar passando um objeto vazio mesmo no body dessa requisição. Qual é o retorno dele? O retorno dele nós vamos tipar aqui, e eu vou dar o nome de iMovieToFavorite, Success Response, bem grande né, mas fica fácil de identificar. I move to favorite success response, um response de sucesso de adicionar o filme aos favoritos. Agora vamos pegar isso e colocar aqui na nossa, criar um arquivo né, com essa interface aqui na Shared. Então na Shared Models, cadê ela? Aqui na Shared Models, New File, que eu vou chamar de Move, então vamos colocar aqui, move-toFavorite Aí aqui, traço success, traço response.ts. Deixa eu ver se ficou certinho. Move to favorite success response, beleza. Export interface, imove, enfim, não vou falar toda hora isso, porque senão até eu vou cansar. Aí vamos colocar aqui message, opa, message. que é do tipo string. Agora é só voltar aqui no nosso service e fazer o import dessa interface. Beleza, finalizamos aqui a implementação de adicionar o filme aos favoritos. Agora vamos fazer aqui a implementação do endpoint de remover o filme dos favoritos. Para remover ele dos favoritos, vai ser bem simples também. Nós vamos ter que enviar um movie.id, que é do tipo number, e aí no caso nós vamos estar colocando aqui um return this.httpclient.delete nesse delete nós vamos passar também a base da url que é esse http localhost 3000 barra favorites então vamos passar ele aqui e é basicamente isso só que eu tenho que colocar um barra no final e fazer a concatenação com o moveId esse é o nosso endpoint ele é do método delete no caso aqui o retorno dele se a gente der uma olhada no insomnia olha só o moveMove ele tem aqui um 204 no content então ele não vai retornar para gente um response ali que tem um json no body desse response Nós vamos colocar aqui, para simular isso, que é um non-content, que o retorno dele vai ser do tipo void. Só para ficar identificado aqui de que esse endpoint não retorna um objeto para a gente no formato de JSON, tá bom? Bom, para esse vídeo foi basicamente a implementação desses dois endpoints. Eu te vejo no próximo vídeo para a gente começar a implementar eles lá do lado do componente e chamar esses endpoints. Espero que você tenha gostado do vídeo e eu te vejo no próximo.