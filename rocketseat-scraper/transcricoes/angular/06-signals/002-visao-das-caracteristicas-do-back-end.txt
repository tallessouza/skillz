WEBVTT Fala Devs, seja bem-vindo a mais um vídeo e nesse vídeo aqui eu quero te mostrar, dar uma visão geral bem rápida de como está construído o nosso back-end. Eu vou mostrar ele posteriormente em funcionamento, mas aqui é só para você já ter uma noçãozinha. Beleza? Vamos lá. As características do nosso back-end. Porque o nosso back-end é o cara que vai salvar os nossos filmes, vai logar o meu usuário, ter o meu usuário registrado, salvar os filmes favoritos dele e tudo isso. Beleza? O front apenas consome essas informações do back-end a partir de requisições HTTP. Então vamos lá para as características dele. Resumo técnico da API. que é o nosso MovieManager, ele no caso aqui vai estar utilizando o Node.js, beleza? O framework para criar a API é o Express e a linguagem é em TypeScript. A arquitetura nós separamos ela em FeatureBased. que no caso seria ali o Domain Driven Design, separando as responsabilidades por contexto. Users, Movies, Favorites. Lembrando que no nível anterior nós vimos sobre Feature Based. Então, basicamente aqui... Ah, e só para lembrar, tá? O back-end já está construído, não vamos criar ele do zero. Vamos criar o front, ele já tem alguns componentes lá no front também, mas o back-end já está prontinho só para a gente consumir. como que é feita a parte de persistência de dados e armazenamentos no nosso back-end. O sistema utiliza uma abordagem serverless-like, local. Então, a gente vai rodar o nosso back-end local, no nosso local host, simulando o banco de dados e o storage. Então, nós temos aqui o JSON database, que é basicamente um banco de dados ali no formato de JSON, são arquivos em JSON que vão salvar ali os nossos usuários, os filmes e etc. Os dados são persistidos em arquivos locais, server barra data e o arquivo .json, para users, movies, favorites, então tudo isso é salvo em um arquivo .json. File System Storage, para fazer o upload das imagens, gerenciado localmente na pasta pública. servida estaticamente pelo express. Então nós utilizamos esse sisteminha aqui para armazenar as imagens. Nós não vamos salvar as imagens em base 64. Por que eu estou fazendo isso? Porque quando a gente salva em base 64 o tamanho dessa imagem que é convertida para texto ela acaba ficando 33% maior do que o tamanho original dela então eu não quero isso eu estou salvando diretamente o binário no meu computador e esse binário ele vai ser acessado ali pelo nosso backend ele vai acessar a pasta que tem aquela imagem e dessa forma também quando a gente retorna essa imagem para o front fica mais fácil para o navegador fazer o cache dessas imagens caso seja necessário com base 64 fica mais difícil e às vezes o navegador nem faz o cache dessas imagens a separação de camadas. A pasta de dados data e ativos public está isolada no código-fonte source, depois eu vou mostrar isso certinho, garantindo segurança e organização. Então nós temos tudo certinho, nosso código-fonte, a nossa pastinha deira, que vai guardar ali o nosso, entre aspas, banco de dados em JSON, e a pastinha public que tem as imagens dos nossos filmes. Além disso, na parte de segurança do nosso back-end, nós estamos utilizando um Alf-Middleware, que é basicamente um middleware, um interceptador que vai validar os tokens, um JWT que nós vamos criar, quando a gente faz o login do nosso usuário, nós vamos gerar um JSON Web Token para ele, que vai falar que o nosso usuário está de fato logado na nossa aplicação. ele decodifica esse token e injeta os dados do usuário no objeto da request de forma tipada. Então, nas requisições que são privadas, digamos assim, nós vamos precisar enviar esse JWT, porque apenas usuários logados podem acessar esses endpoints. Eu não posso deixar um endpoint livre para ser acessado por qualquer pessoa. Não, apenas usuários logados. Nós vamos ter também um middleware de upload que utiliza a biblioteca muter para gerenciar o multi-part form data validando se o arquivo é uma imagem, gerando nomes únicos e salvando no disco. Essa daqui é na parte que a gente vai fazer o upload do nosso... Vai criar o nosso filme, que a gente tem que mandar essa imagem e as informações, no caso a imagem do filme, e as informações do filme. Então a gente trata isso como multi-part form data, também não é nada de outro mundo, o Angular facilita bastante essa parte pra gente. E também temos a parte de cores configurado para permitir requisições externas, essencial para a comunicação com front-end e Angular, para a gente não tomar aqueles errinhos de cores, por estar em domínios diferentes. E aí, na parte dos nossos endpoints e funcionalidades, eu listei eles aqui, mas nós vamos revisar posteriormente também, verificando ali como que ele está funcionando na prática, tá? Olha só, a API está dividida em três recursos principais. Um, usuários que seria o nosso barra users nós vamos ter dois endpoints um post que seria apenas barra então seria por exemplo localhost 2.3000 barra users apenas isso e aí ele vai criar um novo usuário a lógica verifica duplicidade de e-mail e salva no json usuário retorna o usuário sanitizado sem a senha Outro endpoint é o //users//login, que autentica o usuário, valida credenciais e retorna um token JWT, com validade de uma hora, junto com os usuários, junto com os dados desse usuário logado. Bom, o próximo seria o nosso //movies, que ele tem alguns endpoints, 1, 2, 3, 4. O primeiro seria a barra movies apenas, que é esse barra aqui, que vai listar todos os filmes cadastrados. Ele vai precisar de um token sendo enviado no header dessa requisição. Temos também o get, que é esse barra dois pontos id. Eu vou colocar barra movies barra o id do filme, que vai retornar os detalhes de um filme específico pelo id numérico. Também precisa de token. Temos um outro endpoint, que é um post, que apenas barra, então, barra movies. Quando ele é, no caso, post, ele vai cadastrar um novo filme com imagem, requer o token. A feature processa o upload, processa o upload de imagem e salva o caminho relativo. no banco de dados, evitando base64 pesado no JSON. Então a gente não vai converter aquela imagem em base64 porque senão ficaria muito pesado, vai aumentar bastante o tamanho da imagem. Vamos salvar diretamente o binário da imagem em uma pasta. Já o próximo endpoint, que é um post também, seria //movies, //id do filme, //rate, para avaliar um filme com nota de 1 a 5. O algoritmo implementa a lógica de média ponderada incremental, atualizando a nota média e o total de votos em tempo real. Então, sempre quando a gente faz um rate, ele vai gerar essa média, vai devolver ela pra gente no response da requisição, dessa requisição aqui, e nós atualizamos o front-end com essas novas informações. E por último temos os favoritos, que seria o barra favorites. Temos três endpoints. Um que é o get, que é apenas barra. Então, por exemplo, barra favorites, que vai listar os filmes favoritos do usuário logado. Realiza um join lógico, buscando os IDs no favorites.json, que seria o nosso arquivo de JSON ali no nosso, entre aspas, banco de dados. e cruzando com os dados completos do movies.json, que é outro arquivo de JSON do nosso banco de dados, que salva ali a lista de filmes cadastrados. O próximo endpoint seria um post, barra favorites, barra movie ID, que seria o ID do filme. Adiciona um filme aos favoritos do usuário. Bem simples. E por fim o delete, que seria barra favorites, barra o ID do filme, que remove esse filme da lista de favoritos do usuário. Essas são as funcionalidades do nosso back-end. Nós vamos ver na prática, mas espero que já tenha te dado aqui uma ideia geral de como ele está funcionando, as tecnologias utilizadas nele. Nós não vamos construir ele do zero, apenas rodar ele local e consumir os endpoints. Espero que você tenha gostado desse vídeo e eu te vejo no próximo para a gente continuar configurando aqui todo o nosso ambiente de desenvolvimento. Então, até o próximo vídeo.