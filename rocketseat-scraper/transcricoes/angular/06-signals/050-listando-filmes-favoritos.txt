WEBVTT Nesse vídeo nós vamos fazer a lógica para carregar a lista de filmes favoritos. Por enquanto não está carregando nada aqui, mas nós vamos nesse vídeo criar a implementação ali do Endpoint, chamar esse Endpoint no nosso componente e passar a lista retornada do Backend para o nosso componente MoviesList. Então vamos fazer isso, vai ser algo bem tranquilo. Bom, voltando aqui para o projeto, deixa eu só fechar aqui. nós vamos precisar criar um service que vai ter essa implementação né para retornar os filmes favoritos só que tem um ponto aqui inicialmente a gente criaria ele aqui no nosso favoritos no nosso na nossa feature de favoritos porque é um serviço específico de filmes favoritos porém ele vai ter outros dois métodos esse serviço que é o método de adicionar um filme aos favoritos e remover o filme da lista de favoritos só que esses métodos eles acabam sendo chamados em outras features que vai ser a feature de movies e não é legal uma feature depender de outra feature certo bom então nesse caso aqui eu vou estar colocando esse novo service na nossa pasta shared então nós vamos fazer o seguinte olha só na pastinha shared nós vamos criar uma pasta services que eu vou chamar de colocar aqui na verdade um new file eu vou chamar de favorites traço api.ts export class favorites api agora dentro desse serviço na verdade nós temos que colocar aqui o injectable e vamos colocar o provided in root Show. Agora, dentro dele, nós vamos vir aqui e colocar um private read-only underline HTTP client. Opa, coloquei tudo errado aqui. HTTP client. Inject HTTP client. Fechou. Já estamos fazendo aqui a injeção de dependência do HTTP client para realizar as requisições. Agora é o seguinte, esse serviço aqui ele vai ter alguns métodos. GetFavorites. Ele também vai ter futuramente, que nós vamos implementar, um addMovieToFavorites e também vai ter, que vamos implementar posteriormente, um removeMovieFromFavorites. Só que qual é o ponto? É o que eu tinha falado. Esses dois métodos aqui, de add e remove, eles vão ser chamados a partir de um componente de uma outra feature. Logo, não é legal esse serviço aqui estar dentro da Feature de Favorites. É melhor a gente extrair ele para Shared para ser algo compartilhado, porque vai ser um serviço que vai ser injetado tanto na Feature de Favorites quanto na Feature de Movies. Espero que você tenha conseguido compreender essa ideia. Bom, vamos fazer aqui a implementação do getFavorites. Vai ser muito simples também. return this.httpClient.get e qual que é o URL? Bom, http://localhost://3000.favorites E agora, só fazer a tipagem genérica aqui do response desse endpoint. Se a gente der uma olhadinha aqui no Insomnia, clicar aqui na pastinha Favorites, List User Favorites, ele vai retornar pra gente. Vamos ver se eu consigo chamar aqui. Token inválido, vou fazer um login. Vou fazer um login aqui, agora tentar chamar novamente, ele retornou uma lista vazia, vou adicionar um filme e agora retornar a lista novamente, e agora sim ele trouxe. Olha só, o retorno dele é uma lista de filmes e cada filme é esse objeto aqui. Bom, nesse caso, esse retorno aqui ele é bem parecido com o que a gente tem na lista de filmes então é a mesma tipagem então vou reutilizar ela basicamente aqui eu vou estar colocando na tipagem genérica movies list response que é uma lista aqui de iMovieResponse são as mesmas propriedades se fossem propriedades diferentes aí eu criaria uma outra interface beleza Bom, então aqui nós já temos o nosso endpoint, agora vamos chamar ele aqui dentro do nosso componente, da nossa feature de Favorites. Então aqui em Favorites, Favorites Movies, vamos abrir o HTML e o TS também, e vamos injetar esse nosso novo serviço. Private Read Only, underline Favorites API, Inject, Favorites API. Beleza digitei tudo certinho favorites API beleza agora como que a gente pode chamar esse endpoint assim que o nosso componente carrega nós já fizemos isso pausa o vídeo e tenta fazer aí Bom, vai ser muito simples. Nós vamos criar aqui um Favorite Resource, que vai receber o RxResource. Esse RxResource vai receber um objeto, e esse objeto tem duas propriedades que nós usamos. Params, que recebe um callback, e também, opa, coloquei o callback errado aqui, e também, String, que recebe também um callback, retornando aqui o nosso Observable, que ele vai se inscrever automaticamente, this.favorites.api.getFavorites, basicamente isso. Só que aqui eu quero que esse IndiePoint, esse Observable, seja executado assim que esse componente carrega. Nesse caso aqui no params, eu vou colocar um true. Então, dessa forma, vai ser chamado automaticamente para mim. Se fosse undefined, aí ele não executaria. Então, voltando aqui para true, ele vai executar assim que essa tela carregar. Vamos dar uma olhadinha aqui ó, recarregou aqui a nossa tela, vou fazer o login. teste.com 1, 2, 3, 4, 5, 6, 7, 8. E se eu clicar aqui no inspecionar no nosso network, meus favoritos, olha só o que vai acontecer, nós vamos ter aqui o endpoint sendo chamado automaticamente e esse é o response dele. Bem bacana né? Bom, agora nós vamos continuar aqui com a nossa lógica. A nossa lógica vai ser o seguinte, eu quero criar um signal que vai guardar a lista retornada aqui do nosso resource e que vai tratar também caso ocorra algum erro nessa requisição, nesse observable. Então, para isso, eu vou utilizar um computed, um signal de computed. Por quê? Eu não preciso de um linked signal, porque eu não vou setar o valor desse signal posteriormente, apenas quando precisar recalcular ele aqui no nosso callback. Então, aqui, vou colocar um favorites list, que vai receber um computed, Esse signal de computed recebe um callback e nesse callback nós vamos fazer a tratativa que nós já estamos acostumados const error onResponse que vai receber aqui duas exclamações this.favoritesResource.error então se for um erro aqui se ocorre tiver um erro dentro desse signo ele vai retornar true caso contrário ele retorna falso agora vamos fazer o if error onResponse E aí nós vamos retornar aqui uma lista vazia, caso ocorra algum erro. Caso contrário, a gente vai retornar quem? O this.favoritesResource.value. Se por algum motivo ele tiver vazio, ele vai retornar um array vazio. Caso contrário, retorna a lista que está alocada aqui nesse signal value. basicamente isso então é o que eu falei eu não preciso setar o valor dessa lista desse signo posteriormente apenas no momento em que esse signo mudar ou esse signo aqui velho mudar aí ele faz a computação dessa expressão aqui né dessa lógica e adiciona o valor nesse signo basicamente isso é assim que eu quero Beleza, já temos a nossa lista de favoritos. Como que nós fazemos agora? Muito simples. Nós vamos pegar aqui o nosso MoviesList, no template do FavoriteMovies, e vamos passar via input. Qual que é o input dele? É o input Movies, que recebe quem? O FavoritesList, o valor. salvei e agora vamos dar uma olhadinha olha só que bacana já trouxe aqui para gente interestelar já é um dos favoritos desse usuário na verdade aqui o usuário que eu loguei no insomnia é exatamente o usuário que eu estou logado ali na aplicação né deixa eu só confirmar isso exatamente é o mesmo usuário então vou fazer o seguinte eu vou adicionar um outro filme então aqui movies all movies vou pegar o id do matrix então aqui Caramba, não deu certo, pera aí... Copiei aqui o ID, vamos vir aqui no AddMoviesToFavorite, colocar esse novo ID. adicionar e me foi adicionado e agora se a gente recarregar aqui essa tela olha só que bacana o matrix já aparece aqui também porque ele foi retornado aqui do endpoint o nosso endpoint de cadê olha só vou mostrar novamente do endpoint de favorites temos aqui o matrix e temos aqui o interestelar também bom então para esse vídeo foi fazendo essa lógica para fazer a renderização aqui da nossa lista de favoritos espero que você tenha conseguido compreender aqui o uso do rx resource e também do computed e eu te vejo no próximo vídeo