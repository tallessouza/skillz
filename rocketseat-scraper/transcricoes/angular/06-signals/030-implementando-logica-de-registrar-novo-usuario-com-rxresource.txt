WEBVTT Agora que nós já criamos a implementação do nosso endpoint de registro, nós vamos chamar ele lá no nosso componente de registro, aqui no registerUserForm. Então nós vamos aqui no botão de criar, adicionar o evento de clique e chamar o método register. Vamos declarar esse método lá na classe do nosso componente, trazer ele aqui para baixo e fazer a implementação dele. Porém, eu tenho um desafio para você. No caso, nós vamos utilizar o RxResource, a mesma coisa que nós fizemos no componente de login. eu quero que você tente implementar a chamada dessa requisição de registro utilizando o RxResource. Vai ser algo muito, mas muito parecido com o que nós fizemos na parte do login. Então, pausa o vídeo, quebra um pouquinho a cabeça e tente executar aqui, fazer a criação dessa lógica, e aí você volta para ver o meu resultado e igualar aqui o que você fez com o que eu estou fazendo, colocando os mesmos nomes nas propriedades, e etc. Então, pausa o vídeo e daqui a pouquinho você volta. Bom, agora que você já tentou, espero que você tenha conseguido, ou pelo menos espero que você tenha tentado, vamos fazer a criação. Vai ser muito simples, eu tenho que fazer aqui, eu tenho que pegar, chamar esse método register, então para isso eu tenho que injetar esse service user API, primeiro aqui no meu componente. Então, aqui em cima eu vou colocar um private read-only, Underline User API, Inject User API. Beleza, agora já tem acesso àquele método. Então, agora vamos criar o nosso Resource, que é o cara que vai executar aquela requisição se inscrevendo no nosso Observable. Eu vou chamar de Register Resource, que é o RxResource, O RxResource, o que ele tem? Ele recebe aqui um objeto e nós vamos utilizar dois parâmetros dele. O params, que recebe um callback, vou colocar aqui undefined por enquanto, para ele retornar. E o segundo é o string, que também é um callback. Bom, aqui no Stream nós vamos retornar o Observable que eu quero que ele se inscreva e execute. Qual que é esse Observable? this.userapi.register Beleza, só que ele tem que passar dois parâmetros aqui, né? Dois não, né? São três. São três argumentos, o nome, o e-mail e o password. Beleza, eu poderia pegar aqui do meu register form, só que o que acontece? Eu tenho que, de alguma forma, disparar esse resource. Lembra que ele é disparado? Eu consigo disparar ele atribuindo aqui no parâmetro um signo e quando o valor desse signo mudar ele acaba disparando essa requisição? Então nós vamos criar um signal apenas que vai guardar esses valores necessários para o registro e sempre quando os valores atualizarem desse signal essa requisição acaba sendo disparada. Então vai ficar da seguinte forma eu vou chamar de register que vai ser um signal e esse signal aqui depois a gente cria a tipagem dele tá mas ele vai ser na verdade já vamos criar agora melhor depois a gente externaliza ela a tipagem dela vai ser um i register params ou Undefined. Lembra que eu já expliquei? Se o signal começa como Undefined, com o valor Undefined, essa requisição aqui ela não vai ser executada inicialmente, beleza? Então, aqui o IREGISTER PATTERNS, vou fazer o seguinte já, eu já vou criar aqui nas models mesmo, nas models do meu Authentication, já que é uma model específica dessa feature. Então, New File, eu vou chamar de register-params.ts export interface iregister-params, que vai ter as propriedades de name, opa, name, que é do tipo string, E-mail, que também é uma String. Password, que é do tipo String também. E por fim, um Confirm Password, que também é do tipo String. Bom, então aqui criamos a nossa interface. Agora, só a gente importar ela. Então, aqui eu venho e faço o import desse Redster Params. Ele já vem aqui para cima, olha só. Deixa eu tirar esse Validate aqui. E aí o que que acontece? Eu tenho aqui a tipagem dela e eu quero tipar também esse register model porque é a mesma coisa. Então já deixa eu tipar ele aqui para eles ficarem organizados. Show de bola! E agora nós já temos aqui o nosso novo signo. Quando que eu vou querer alocar novos valores nele? no momento em que eu clicar no botão de criar e disparar esse método register. Então eu vou atualizar o valor desse signo, esse signo ele vai estar referenciado aqui nos meus params, e aí quando ele for atualizado essa requisição acaba sendo executada. Bom, vamos fazer essa parte do params aqui por enquanto. Eu vou retornar aqui diretamente o valor desse signo. Então, dis. registrar params e ele retorna diretamente o valor alocado dentro desse signo e aí esse valor ele é alocado dentro de um objeto que tem a propriedade params nós já vimos isso lá na parte do login então aqui eu acesso diretamente né params.name próximo params.email e por params.password se você tiver alguma dúvida você consegue vir aqui e logar esse params tá os parâmetros que você recebe aqui pode colocar o nome teste e logar ele colocar aqui ó console.log e assim por diante tá bom mas aqui é basicamente um objeto que dentro desse objeto tem uma propriedade params e essa propriedade params vai guardar o valor retornado aqui no callback da propriedade params do nosso rxResource. Beleza, agora vamos atualizar os valores desse Register Params. Como que nós fazemos isso? Bem simples também. Basicamente aqui eu vou estar criando uma constante, que eu vou chamar de UserInfos, que vai receber o Dis.RegisterForm.Value. Opa, tem que só invocar ele aqui. Esse ponto velho, esse signo, ele vai retornar um objeto que tem essa estrutura aqui da minha model, exatamente essa estrutura aqui com os valores preenchidos baseado no que eu digitei aqui nos inputs, nos elementos de input e que eu fiz aqui a ligação com o field, claro. Então, ele vai retornar esse objeto. Esse objeto tem todas as informações que eu preciso para chamar aqui no register. Então, eu vou alocar essa referência de memória no meu register.params. Logo, dis.register.params.set E aí eu passo aqui o user infos. E fazendo dessa forma aqui a gente evita aquele problema que nós tivemos no login de chamadas duplicadas caso os valores do meu formulário não tenham mudados. Então não tenho mudado. Então no caso aqui ele só vai fazer uma nova chamada se o valor mudou baseado no valor anterior desse formulário. Espero que tenha dado para entender. Bom, finalizamos aqui. Vamos testar? Olha só. Voltando aqui no nosso projeto, recarregando, não temos nenhuma chamada inicial. E aqui eu consigo, por exemplo, fulaninho... O e-mail vai ser fulaninho... A senha vai ser o laninho 123, o laninho 123. E aqui a gente tem todas as validações caso necessário, né? As senhas devem ser iguais e assim por diante. Cliquei em criar, olha que bacana, o nosso users, nosso endpoint barra users foi chamado, temos aqui o payload, que é o que nós estamos enviando no body dessa requisição, que é exatamente esse objeto aqui, show, esse é o response que nós recebemos, e aqui tem toda a parte do header e tudo mais né, os headers de requisição da request e os headers de response. Aqui no console não temos nenhum erro, tudo funcionando certinho, e agora a gente já poderia logar esse usuário. Então, por exemplo, aqui ó, pegar o e-mail dele, alf barra login, E aqui eu coloco fulaninha, arroba teste, fulaninho, um, dois, três. Entrar e ele entrou com sucesso. Fez aqui a chamada de login, retornou os dados desse usuário, retornou o token pra gente. Então essa parte de registro e também de login já está funcionando. Bom, então para isso foi basicamente essa implementação utilizando o RxResource. Espero que você tenha conseguido compreender que esteja ficando cada vez mais fácil essa utilização dos signals também e também essa integração entre signals e RxJS, observables, que nós estamos fazendo aqui utilizando o RxResource. Bom, no próximo vídeo, nos próximos vídeos, na verdade, a gente vai continuar incrementando um pouquinho essa parte de registro, mas espero que você tenha gostado desse vídeo e eu te vejo no próximo.