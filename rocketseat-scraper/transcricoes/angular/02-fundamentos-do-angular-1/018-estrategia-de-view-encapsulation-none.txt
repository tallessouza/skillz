WEBVTT Agora vamos dar uma olhada no View Encapsulation None, que é bem tranquilo de entender também. Basicamente, quando nós colocamos ele dentro de um componente, os estilos colocados dentro desse componente serão tratados como estilos globais e podem afetar outros componentes. Então tome bastante cuidado ao utilizar ele. Eu vou ser sincero, eu nunca precisei de fato utilizar o View Encapsulation None. Nunca. Então, não te aconselho a ficar colocando ele nos componentes por aí, tá bom? Sempre você vai ter uma opção de fazer o que você precisa fazer, mantendo o View Encapsulation Emulated, tá bom? Então, vamos dar uma olhada aqui como ele funciona, mas eu te aconselho a não utilizá-lo, a não ser que seja algo muito, muito específico mesmo, que só com ele você vai conseguir resolver o problema, tá bom? Vamos dar uma olhadinha aqui então como ele funciona, bem tranquilo. Então, no encapsulation none, quando a gente fala dos estilos do componente colocados dentro do componente que tem o none, eles serão tratados como estilos globais, assim como os estilos do styles.css, pois eles não possuem aqueles atributos de escopo que nós vimos no vídeo anterior. Então os seletores não terão ali os atributos de escopo logo todos os seletores criados dentro desse componente serão tratados como seletores globais afetando todos os componentes da aplicação os estilos globais aqui eles vão ainda afetar esse componente então os estilos que você colocar dentro do styles.css ainda vão afetar esse componente que tem o View Encapsulation nome beleza até aí tranquilo bom e se tratando aqui de estilos do componente filho o componente pai com o nome estiliza elementos diretamente do seu template e também os do componente filho dos seus componentes filhos então já que os estilos criados dentro desse componente são globais vai afetar tanto o template do próprio componente como o template dos seus componentes filhos. Beleza? Então vamos dar uma olhada rápida em como ele funciona aqui. Não tem muito segredo, mas por exemplo aqui no UserDetailsComponent. Eu vou colocar nele aqui dentro do ArrobaComponent, lembrando que é um objeto aqui, que tem algumas propriedades. E por fim eu vou colocar a propriedade Encapsulation. Vamos colocar o enum aqui, viu? Encapsulation. Opa, digitei errado aqui. View encapsulation do arroba angular barra core cuidado para não colocar aqui ó arroba angular barra compiler tá se não vai dar erro é do core .none no final beleza olha só esse seletor aqui de parágrafo que estava sendo aplicado apenas para esse parágrafo desse componente agora ele não tem mais aquele atributo de escopo logo ele será um estilo o seletor global para mudar um pouquinho esse seletor eu vou colocar aqui um font-family ao invés de font-weight e aí vamos colocar aqui um cursive vamos dar uma olhada no resultado Salvei. Olha só o resultado que nós vamos ter. Mudou aqui o font-family de todos os parágrafos da nossa aplicação. Por quê? Se a gente inspecionar aqui o user-details na parte do parágrafo, nós vamos notar esse seletor, que foi o que nós criamos agora, e nele não tem nenhum atributo de escopo. Logo, ele é tratado como se fosse um seletor global, assim como é esse daqui do styles.css. Note que ele também não tem nenhum atributo de escopo. Logo, ele afeta todos os parágrafos da nossa aplicação. Bom, é bem simples de utilizar, é bem simples de entender também, apenas colocando aqui o encapsulation known. Bem tranquilo. Bom, então, para esse vídeo foi basicamente a explicação aqui simples de como utiliza o encapsulation known. Lembrando que não o utilize a não ser que seja extremamente necessário. Caso contrário, sempre mantenha ali o emulated. Beleza? Te vejo então no próximo vídeo.