WEBVTT E a partir dessa aula vamos começar a construir a nossa camada de UI, a nossa camada mais externa do nosso aplicativo Android, certo? Nos projetos Android, a camada de UI irá deter principalmente as classes de ViewModel e também de View. Os ViewModels terão, como a gente vê aqui, um vínculo direto com os UseCases da camada de domínio, certo? Será a única relação com as regras da aplicação a serem expostas dentro dos ViewModels. E além disso, como a gente percebe aqui também, as Views, que podem ser Activities, Fragments ou funções Composable dentro do contexto de Android, no nosso caso nós vamos ter vários Fragments e uma única Activity, irão enviar eventos para o ViewModel. Essa será a única forma de comunicação com o ViewModel a ser exposta. E receber os chamados UI States, os estados de UI de cada uma das telas. Certo? No caso, nos nossos fragments. Isso vai fazer com que a gente entregue para o nosso projeto o chamado fluxo unidirecional de dados, que é um dos conceitos base das recomendações de arquitetura que a equipe Android recomenda oficialmente para os seus desenvolvedores. A partir desses conceitos, nós vamos ver depois um pouco mais sobre o que consiste os padrões arquiteturais de MVVM e MVI, que são hoje os padrões mais modernos e utilizados no mercado Android como um todo. Certo, esses padrões arquiteturais vão ditar a forma como a Vue irá trocar informações com as camadas mais internas que se relacionam com a série de negócios da aplicação. mais internas que se relacionam com a série de negócios da aplicação. Então, além das activities, fragments, viewModels, os UIStates e também os events, nós também iremos ter outras classes que vão caracterizar o controle das nossas views, que será, por exemplo, os adapters que nós vamos construir para as nossas listas vinculadas à escolha de stack, tanto do histórico quanto também da seleção a ser vinculada no chat, e também alguns utilitários, caso possivelmente a gente vá trabalhar com imagens, datas, formatos de textos específicos, etc. E assim que a gente concluir o início da construção dos nossos new models nós já vamos iniciar a implementação de injeção de dependências dentro do projeto utilizando o coin, como havia sido falado anteriormente aqui no nosso módulo e passaremos também pela implementação do DAG Routes. Então vamos lá, vamos dar início à construção da nossa camada de UI.