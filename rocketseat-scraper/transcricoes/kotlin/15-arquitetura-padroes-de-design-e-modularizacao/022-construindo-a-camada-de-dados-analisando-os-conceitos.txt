WEBVTT E a partir dessa aula nós vamos dar início à construção das camadas dentro do nosso aplicativo RocketA baseado no que o Guia de Arquitetura Android nos recomenda, certo? E aqui a gente tem uma visão geral das camadas que nós vamos construir E a gente vai começar da primeira camada, que seria a mais interna, que seria a camada de Data A camada de regras de negócio mais internas da nossa aplicação. Certo? Eu vou dar um zoom aqui nesses elementos, pra gente ver o que estará contido dentro desse pacote, que nada mais é do que a representação de uma pasta dentro do projeto principal. Então, lá no pacote Data, nós vamos ter, principalmente, os nossos data sources, que seriam as nossas fontes de dados, e elas estarão divididas em implementações e interfaces, certo? E isso pra gente depois aplicar o princípio de inversão de dependência, passar as dependências apenas de classes abstratas e não concretas pra quem estiver associado a esse vínculo. E os nossos data sources, eles vão poder ser locais ou remotos, de acordo com o tipo de dado que vai estar sendo gerenciado, certo? Além disso, nós vamos ter também as implementações dos repositórios, certo? Na camada de domínio, nós vamos ter as interfaces e na camada de dados, de data, nós vamos ter as implementações. obter as implementações. Isso porque as implementações de repositórios entram em um contexto de necessitar fortemente de data sources vinculados para poder gerar o gerenciamento de um determinado contexto dentro do app. Por isso ela vai ser alocada aqui. E fora os data sources e as implementações de repositórios, nós temos também os serviços de API, que consistem da forma como obter os dados remotos para os nossos data sources, e também databases, que podem ser relacionais ou não relacionais, utilizando o RUM, por exemplo, ou as preferências de usuário vindas do shared preference ou preference data store. ou preference data store. Além disso, a gente tem o external storage, internal storage e outras formas também de estar construindo camadas de dados que são internamente vinculadas à web. Além disso, nós temos também os nossos data models, que são as estruturas de dados que vão representar a informação em si, que podem ser entities, as chamadas entidades, que têm vínculo com o banco de dados, certo? E vão representar para nós tabelas, ou modelos que são vindos do retorno de uma API, certo? Do retorno de um dado que vem da internet para nós. Além disso, a gente pode ter outros models também que vão fazer a representação de contextos vinculados à aplicação. E dentro de Data também nós temos os mappers, que são os mapeadores de diferentes modelos. Então, por exemplo, ao buscar um dado de um serviço de API, a gente pode querer convertê-lo para um dado que vai ser visto lá na nossa view. Um dado mais simples e com tratativas em cima de possíveis valores nulos ou possíveis valores que precisam ser mostrados de uma forma diferente e passar por um processo de conversão. Então, tudo isso será a responsabilidade dos nossos mappers. Esse seria um resumo de tudo que nós vamos ter no nosso pacote data e agora que nós vamos construir dentro do app.