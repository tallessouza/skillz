WEBVTT Partimos agora para a lógica de adicionar as atividades a partir das inserções que o usuário vai fazer na tela de home, nos campos de nome, data e horário. Para a gente começar, nós vamos ter um trabalho sendo executado tanto no ViewModel quanto na View. No ViewModel, no caso de PlannerActivity e na View de home fragment. O início de tudo vai partir de algumas classes que nós vamos construir para a gente trabalhar com as inserções que o usuário vai fazer dentro do ViewModel. Certo? Então aqui em PresentationUI ViewModel, nós vamos criar algumas classes que a gente vai usar para mapear tudo que o usuário está inserindo nesses campos. Certo? E assim que ele preencher todos os campos obrigatórios para construir uma nova atividade, nós vamos fazer o envio dela dentro do próprio ViewModel. Então o ViewModel irá deter toda essa lógica de saber se o item de nova atividade está pronto ou não para ser inserido no banco de dados. Mas então vamos começar por parte. Aqui primeiro eu vou criar uma nova classe Kotlin do tipo DataClass, chamado NewPlanetActivity. Nessa estrutura de dados, nós vamos mapear tudo o que o usuário vai inserir durante o momento em que ele estiver na tela de home. Então os campos que nós vamos ter serão Name, que será uma string possivelmente nula e com valor inicial nulo. No caso, o nulo representa que ele ainda não fez nenhuma inserção ali naquele campo de name. Da mesma forma, nós vamos ter date, certo? E date será de um novo tipo de dado chamado setDate, que será inicialmente igual a nulo também. setDate a gente já vai construir aqui também, será uma data class setDate, e terá os campos de ano, mês e day of month também será um inteiro beleza agora o mesmo vale para time time será do tipo set time inicialmente igual a nulo e nós vamos construir ele aqui embaixo data class set time com os campos hourOfDay inteiro e também minute, também inteiro o conjunto setDate e setTime irá compor para nós o dateTime a ser inserido na nova atividade dentro do ViewModel, certo? então vamos lá, vamos voltar agora para a nossa PlannerActivityViewModel e nós vamos começar mapeando um PrivateVol NewActivity do tipo MutableStateFlow, newPlannerActivity, igual a mutableStateFlow, newPlannerActivity, vazio. Certo? O construtor vazio de newPlannerActivity vai nos entregar todos os campos name, date e time iguais a nulo. E agora nós vamos criar uma função chamada update new activity, certo? Que irá receber name igual a uma string possivelmente nula, set, que será do tipo setDate, também possivelmente nulo, com valor inicial igual a nulo, e setTime, também possivelmente nulo, com primeiro valor igual a nulo. Nessa nossa função, nós vamos inicialmente verificar se name igual a null e setDate igual a null e ao mesmo tempo setTime também for igual a null e setTime também for igual a null, nós vamos executar um return já automaticamente, visto que não vai ter nenhuma alteração para ser feita aqui no nosso newActivity. Agora, caso contrário, nós vamos disparar newActivity.update. O update irá retornar current newActivity. E nós vamos atualizar o valor de newActivity para current newActivity.copy. E no campo name, nós vamos entregar name que vem do parâmetro de update newActivity. E caso ele seja nulo nós vamos manter currentNewActivity.name o mesmo vale para date certo? nós vamos disparar o valor de date no caso setDate ou caso seja nulo currentNewActivity.date. Beleza, e time, nós vamos executar setTime ou currentNewActivity.time. Aqui inclusive nós podemos mudar para date e time para mantermos sempre a mesma nomenclatura então ctrl c, ctrl v ctrl c ctrl v ok, agora voltando aqui no nosso new activity ctrl b, ctrl b aqui em new planner activity nós vamos criar uma função interna chamada isFilled, para a gente saber se o nosso new planner activity foi preenchido ou não. Então, ele vai entregar um booleano, e esse booleano será entregue a partir da análise de name, ponto, isNullOrEmpty na negativa, então se não for nulo ou vazio esse nome será válido, ou seja ele foi preenchido e além disso, date for diferente de null e também time for diferente de null certo? essa condição em conjunto E também time for diferente de nulo. Certo? Essa condição em conjunto de name diferente de nulo ou vazio, date diferente de nulo e time diferente de nulo, irá caracterizar uma new planner activity preenchida. Beleza. Voltando aqui no nosso planner activity view model, nós vamos criar uma outra função aqui embaixo chamada SaveNewActivity, certo? Para salvar essa NewActivity que está sendo mapeada aqui no nosso ViewModel. Então, SaveNewActivity irá receber dois parâmetros, OnSuccess, que será uma função sem nenhum parâmetro retornando o unit. E também onError, uma outra função sem nenhum parâmetro retornando o unit. OnSuccess e onError será para a nossa view saber se a newActivity foi salva ou não no banco de dados. Certo? E disparar ações de acordo com essas respostas. Então, o que a gente vai fazer aqui é verificar se newActivity.value . isFilled é verdadeiro. Se for verdadeiro, nós vamos primeiro executar insert, passando aqui no name, newActivity.value.name, e em dateTime, nós vamos criar uma instância de calendar, primeiro aqui fora, calendar, val calendar, igual a calendar.getInstance. E aqui em datetime nós vamos disparar calendar.apply, setando cada um dos campos contidos tanto em date quanto em time dos nossos objetos setTime e setDate. Então vamos começar com set no campo calendar.year será igual a newActivity .value .date .year ou 0 caso seja nulo. Então aqui a gente vai precisar verificar se date não é nulo. Consequentemente o year aqui. Beleza, depois do apply nós vamos dar um ponto time em milis. Para parar de dar esse erro aqui para nós. Certo? Então aqui em name, caso seja nulo, nós vamos entregar o valor de vazio. Certo? Or empty. Porém, o nosso sfilage aqui vai nos proteger contra essa opção. Certo? Para a gente não precisar ficar utilizando newActivity.value, aqui do lado de fora, vou executar newActivity.value.run. .value .run e aqui no run a gente já tem o escopo de newActivity.value para ser utilizado, então ctrl x aqui em todos esses códigos, ctrl v e aqui eu não preciso usar mais newActivity.value certo? vou removê-lo aqui de todos esses valores que a gente tinha utilizado beleza e continuar aqui com os preenchimentos então nós vamos ter agora o set calendar.month sendo igual a date interrogação.month ou 0 .month sendo igual a date? .month ou 0. 7 calendar.dayOfMonth com date? .dayOfMonth ou 0 em caso de nulo 7. calendar.hour Day of month, ou zero em caso de nulo. Set calendar.hourOfDay, passando time, interrogação, ponto. Como a gente quer a propriedade de newActivity, ponto, velho, nós vamos fazer o seguinte. Ao invés de run, nós vamos usar o let Certo? Nós vamos mapear it com o nome de newActivity Certo? E aqui nós vamos passar a usar newActivity Para executar .sfillet Também para o .name, ponto date, enfim, todos os demais. Ctrl-C, Ctrl-V, Ctrl-V aqui também, Ctrl-V aqui também. Interrogação, ponto, hour of day ou zero. E aqui também set calendar.minute recebendo new activity.time?minute ou 0 em caso de nulo. Esse apply, tudo eu vou jogar aqui para fora em uma outra variável. Então, vou chamar de igual a todo esse valor, certo? Vou remover todo esse código e manter apenas . em milhas. Beleza? Mantém o espaço aqui também. E depois do insert nós vamos chamar onSuccess. Beleza, agora no else aqui desse if, certo? Do if no caso de está ou não preenchido o nosso objeto de newActivity, no caso do else nós vamos disparar onError, dando então a capacidade da nossa view ver que algo de errado aconteceu. Logo após o insert nós também vamos adicionar um código para limpar o valor de newActivity. Então, newActivity receberá um update e para isso a gente vai usar o thisPlannerViewModel.newActivity.update como instância vazia de New Planner Activity, certo? Com nenhum dos campos preenchidos. Beleza, o usuário estará disponível nesse momento a fazer uma nova inserção de atividade. Beleza, esse código também nós vamos passar para uma função privada, Private Fun, Chamada create new planner activity filled calendar. Que irá retornar um calendar e irá executar todo esse código. Certo? Ao invés de filled calendar, nós vamos aplicar um return. Ok? E para a gente viabilizar o uso de newActivity dessa forma, nós vamos fazer aquele esquema de newActivity.value.let e no let nós vamos nomear como newActivity. Então, ctrl-c aqui, ctrl-x em todos esses sets, ctrl-v aqui na linha 79. Beleza! Agora, basta executar um ctrl-c no nome aqui dessa função e aplicá-la aqui no lugar de filletCalendar. Então, a gente vai chamar essa função createNewPlanetActivityFilledCalendar E executar o ponto timeMillis Dessa forma a gente exclui a necessidade de uso desse código aqui dentro A gente deixa tudo mais limpo e mais conciso Certo? Aqui eu vou tirar inclusive o nome de planner Para deixar ainda mais conciso Ctrl-C Ctrl-V Para atualizar o nome dessa função NewActivityFilledCalendar Beleza. Com isso nós concluímos o nosso ViewModel. Certo? Agora nós vamos remover esse Launch de inserção de atividades de forma automática. Certo que a gente utilizou apenas para testes. Vou remover esse Launch também, visto que agora a gente está só com um Launch sendo utilizado. Aqui do nosso ViewModel Scope. E agora nós vamos lá para a nossa View, para obtermos todos os valores que o usuário inserir manualmente, disparando ao longo dessas inserções o UpdateNewActivity, e logo depois, no final, ao clicar no botão de salvar, executar SaveNewActivity.