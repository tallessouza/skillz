WEBVTT Bom, beleza, continuando. Para a gente armazenar objetos tipados com o ProtoDataStore, a gente vai precisar, primeiramente, definir um esquema. O ProtoDataStore vai precisar desse esquema sendo pré-definido em um arquivo proto no diretório app-src-man-proto. Vai ser necessário criar esse pacote dentro do nosso diretório. E lá dentro vai ter o tipo dos objetos que vão persistir no protodataStore. Certo? Aqui existe um guia de linguagem de protobuf para você estar aprendendo. Certo? Não vou entrar em detalhes aqui nesse momento, visto que a gente só vai passar o olho nos códigos necessários para a gente ter essa implementação beleza, agora descendo aqui embaixo para a gente criar um proto-datastore vai precisar ser executadas duas etapas primeiro a gente vai definir uma classe que vai implementar serializer do tipo T certo? em que T vai ser o tipo definido no arquivo proto e essa classe é extremamente importante porque ela informa o datastore como ler e gravar os tipos de dados. Inclusive vai permitir ele incluir um valor padrão para o serializador caso ainda não haja nenhum arquivo criado. Então você consegue criar uma instância default a ser retornada pelo proto datastore. retornada pelo proto-datastore. Depois, o segundo passo é usar a delegação de propriedade criada pelo datastore para gerar a instância de datastore do tipo T. Certo? Lembrando que T é o tipo definido no arquivo proto, certo? Que a gente viu aqui em cima. A gente vai fazer a chamada uma vez no nível superior do arquivo Kotlin, certo? No nível do app, e acessar usando a delegação de propriedade, certo? A palavra by, em todo o restante do app, certo? By datastore. O parâmetro filename vai informar qual que é o arquivo usado para poder armazenar os dados, vai informar qual que é o arquivo usado para poder armazenar os dados e o parâmetro serializer vai informar ao datastore qual é a classe do serializador definido na etapa 1, certo? Nome do arquivo e tipo de objeto a ser armazenado. Beleza, continuando a gente tem a forma como ler em um proto-datastore, que é extremamente simples. É a mesma forma de se ler no preference-datastore praticamente, apenas com algumas alterações. A gente vai fazer o acesso a context.settings-datastore.data, com isso a gente acessa via map os settings, e para retornar, settings.exampleCounter, que no caso seria uma propriedade gerada a partir do esquema de proto que a gente definiu. Certo? No caso a gente está vendo aqui um example counter construído lá na linguagem proto beleza agora para a gente gravar em um proto data store a gente tem uma função suspensa visto o uso do flow context.settingsDataStore.updateData então a gente vê que é uma função um pouco diferente. Já não é mais o edit que a gente usou no preferenceDataStore, que atualiza de forma transacional o objeto armazenado. No escopo de chaves de updateData a gente tem o valor atual, a gente tem o valor atual e que vai atualizar de forma transacional a partir de uma operação atômica de leitura, gravação, modificação então a partir do momento que a gente faz esse acesso para poder gerar uma atualização caso a gente tenha execuções simultâneas em diferentes escopos de corrotinas toda a ordem de execução será respeitada. Então aqui a gente tem currentSettings.toBuilder e aqui a gente vai ter um método chamado setExampleCounter sendo gerado a partir daqueles códigos que a gente criou de proto e de schema. Como propriedade desse método, a gente vai passar a currentSettings.xapocounter mais um, nós vamos somar um ao valor atual, e depois é só dar o .build para concluir a atualização. E está pronto. Agora aqui a gente vê a forma como utilizar o datastore no código síncrono então para isso a gente vai estar utilizando o runblocking mas um dos principais benefícios do datastore é ser uma API assíncrona manter o assincronismo, te deixar de forma automática livre da utilização da main thread que renderiza as telas do seu app então caso você precise deixar esse código síncrono existe então a possibilidade do uso do runblocking, certo? pra buscar por exemplo, example data, a gente vai ter runblocking e no escopo de runblocking o acesso a context.datastore.data.first e aqui é mostrado que a execução de operações síncronas Pode causar os chamados ANRs Android Not Responding Que são falhas ali do sistema operacional Android Para o seu usuário Ou instabilidade da UI Devido ao uso da main thread Para poder executar as operações E aqui tem uma opção de você atenuar esses problemas carregando de forma antecipada os dados do datastore então aqui a gente vê o escopo de lifecycle scope certo, que respeita o ciclo de vida da aplicação, de forma automática fazendo o acesso então ao datastore certo, uma outra opção ao invés de runblock. Beleza. E aqui a gente tem mais algumas informações de como utilizar o datastore em códigos com vários processos, mas não vou entrar em muitos detalhes por ser algo um pouco mais avançado e não ser o alvo aqui do nosso módulo. Certo? Mas caso você tenha interesse, você pode aprender também. Seguindo aqui a documentação e seria a utilização do datastore vinculando a possíveis serviços que você tenha rodando em paralelo dentro do seu app certo? aqui embaixo também a gente tem mais algumas informações de como processar arquivos que estejam corrompidos. Certo? Mas também são raras as ocasiões. Caso você tenha interesse de ver mais detalhes, basta seguir aqui a documentação para poder evitar esse tipo de falha. Beleza, e é isso. Agora a gente vai voltar no nosso app planner E colocar a mão na massa Armazenando dados, utilizando tanto o shared preference Quanto também O preference datastore E inclusive o proto datastore Bora lá