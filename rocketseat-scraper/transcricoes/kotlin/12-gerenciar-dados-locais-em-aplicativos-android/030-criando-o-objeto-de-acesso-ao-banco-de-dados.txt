WEBVTT Dando sequência, nós vamos nessa aula construir o Database Access Object do nosso Room, certo? O objeto de acessos ao banco de dados que nós vamos ter para executar todas as queries SQL do nosso banco, certo? Primeiro, como nós vamos trabalhar com o retorno de busca via Flow, aqui em Libs.Versions nós vamos precisar adicionar mais uma dependência do room, que é o room ktx o group será o mesmo que a gente definiu anteriormente, androidx.room ctrl-c, ctrl-v e o name será igual a room-ktx com a mesma versão de referência do room beleza vou executar o sync now ok, agora em build.gradle vou gerar implementation implementation libs.room.ktx ok, vou separar aqui Room de DataStore Serialization Para a gente ter uma visão melhor De tudo que a gente está importando Ok Vou executar o sync now Beleza E agora Vou fechar aqui os arquivos E no nosso pacote database Eu vou criar uma nova Kotlin class do tipo interface chamada plannerActivity.dao vamos começar adicionando o arroba dao que vem de run, certo? E começar a construção dos nossos métodos. O primeiro será a função getAll, que irá retornar um flow de lista de PlannerActivityEntity, certo? Com a ajuda do alt enter, vou importar a classe flow, que vem de corrotinas, e aqui nós vamos escrever a query para essa busca, através do arroba query nós vamos ter um select asterisco para falar que a gente vai buscar todo o conteúdo da linha from planner activity entity ordenado por isCompleted primeiro e também na sequência por datetime então dessa forma todas as atividades concluídas virão primeiro e na sequência nós vamos ter uma ordenação por datetime também tanto para as completadas quanto também para aquelas que estão para se completar ok depois nós vamos ter uma outra query quanto também para aquelas que estão para se completar. Ok. Depois nós vamos ter uma outra query de busca por UUID, certo? Nosso identificador único de atividades. Então a gente vai buscar de novo, de PlannerActivityEntity, onde o UUID será igual a dois pontos UUID, que a gente vai receber da nossa função. Ela se chamará getByUUID. E nós vamos ter UUID sendo recebido aqui como string, e o retorno será PlannerActivityEntity. Beleza. Agora, a outra função que nós vamos ter também será construída via query. E será a função de atualizar, certo? Update isCompletedByUuid. Certo? Nós vamos passar UUID. E o isCompleted. Buleando. Aqui para gerar atualização. Não vamos ter nenhum retorno. Aqui eu vou remover. Um U a mais. E o nosso arroba query. Será. Update. PlannerActivity.ity set isUnderlineCompleted igual a dois pontos isCompleted where UID igual a dois pontos UID Beleza Nós vamos chamar esse método Quando houver O clique Do nosso botão de concluído Para as nossas atividades Além disso nós vamos ter um arroba update Para atualizar por completo A nossa atividade, certo? Visto que o usuário vai poder alterar todo o conteúdo de uma atividade através daquele bottom sheet que a gente vai subir. Tanto o nome, quanto a data e o horário também, através desse update. E depois nós vamos ter uma query de delete que vai permitir excluir uma atividade através do uuid uuid igual a dois pontos uuid que vamos receber via função delete by uuid sendo recebido aqui como parâmetro By UID. Sendo recebido aqui como parâmetro. Beleza. Com isso a gente conclui toda a nossa interface de Database Access Object.