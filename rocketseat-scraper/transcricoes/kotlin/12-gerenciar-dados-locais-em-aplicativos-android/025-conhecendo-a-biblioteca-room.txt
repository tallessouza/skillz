WEBVTT Antes de avançarmos com nossas lógicas para a construção do nosso banco de dados relacional usando o SQLite no Android, nós vamos passar pela documentação do RUM, aqui oficialmente nas docs do Android, certo? O RUM é uma biblioteca que vai abstrair todo o gerenciamento de bancos de dados SQLite de forma muito simples e direta para você sair usando dentro dos seus projetos. O RUM é a recomendação oficial do Android para você trabalhar com SQLite e ele faz parte do Android Jetpack, certo? E aqui a gente já vê que quantidades não triviais de dados estruturados vão fazer do RUM uma abordagem muito útil dentro dos seus aplicativos. E ele é muito utilizado para fazer o armazenamento em cache de partes importantes de dados que vem de forma remota para você e vão te garantir acesso depois como um conteúdo offline. Então caso você tenha essa necessidade de um cache local, o Room muitas das vezes vai ser a opção ideal. Então o que a biblioteca faz é uma camada de abstração sobre o SQLite para deixar o acesso mais fluente e dentro dos benefícios que ele vai te entregar será a verificação de consultas SQL durante o tempo de compilação vão existir várias anotações que vão abstrair todo aquele código repetitivo para poder fazer tanto buscas quanto inserções atualizações e deletes de dados que estão sempre propensos a erros, visto essa necessidade grande de códigos consecutivos para poder obter esses tipos de ações, e caso você necessite fazer atualizações do seu banco de dados, você suba alterações nas tabelas já existentes os caminhos de migração de banco, as chamadas migrations, são extremamente simplificadas pra você fazer então por isso o run ele é muito recomendado ao invés do uso direto das APIs do SQLite certo? e aqui a gente já vê os códigos pra gente começar a configuração aqui nós temos as dependências que a gente precisa importar Certo? Lá no arquivo build.gradle do app No caso a gente tem como principal o runRuntime que a gente precisa colocar E além disso a gente vai usar também o KSP Que precisa ser alocado também como dependência de plugin Para poder gerar o Room Compiler. Certo? Visto aqui que o nosso projeto tem toda uma fonte de Kotlin sendo utilizada. Certo? Caso você utilize Java no seu projeto, você precisa utilizar o Annotation Processor aqui, como visto abaixo. como visto abaixo. Aqui tem algumas outras dependências opcionais. Aqui tem algumas outras dependências opcionais, para você trabalhar compaginação, RxJava, parte de testes também, e o suporte a corrotinas, que inclusive nós também vamos estar fazendo uso. Beleza. Agora, como principais componentes, vão existir três, para a gente estar ciente, e entendermos o porquê e como utilizar a primeira vai ser a classe de banco de dados certo? aqui na documentação você consegue entrar em mais detalhes de cada uma delas mas a classe de banco de dados ela vai conter o banco de dados em si que pode receber várias tabelas. As tabelas são representadas como as entidades de dados. Nós vamos criar basicamente uma classe e incluir uma anotação que vai informar que aquela classe é uma entidade. Ela irá se transformar em uma tabela no banco. E além disso, a gente tem os objetos de acesso aos dados que vão fornecer todos os métodos que o seu app vai utilizar para poder fazer consultas, inserções, atualizações e exclusões dentro do banco de dados esses são os três componentes principais aqui do Room certo, aqui nessa imagem a gente vê que nós temos Esses são os três componentes principais aqui do Room. Certo? Aqui nessa imagem, a gente vê que nós temos todo o restante da aplicação interagindo com gets e sets de campos das entidades. Certo? Que no caso vão ser convertidas em tabelas. O Data Access Object, os objetos de acesso aos dados, eles vão buscar entidades Dos bancos de dados E persistir todas as mudanças De volta ali pro banco E do Room Database Nós vamos obter O Data Access Object Certo? O objeto de acesso aos dados Vai ser obtido diretamente do banco É ele quem vai nos fornecer Certo? A classe Database Vai abstrair o chamado DAO. Ok, aqui a gente vê o exemplo de implementação. Para nós construirmos uma entidade de dados, a gente vai usar a anotação arroba entity em nossas data classes, nossas classes de dados. E aí nós temos outras anotações para informar que um campo é uma primary key. outras anotações para informar que um campo é uma primary key e a gente pode, inclusive, com o column com o arroba column info informar qual será o nome da coluna na tabela certo? Vê se que a gente vai seguir um padrão de nome de snake case por lá a gente consegue estar diferenciando o nome da propriedade da classe e o nome da coluna na tabela. E aqui tem mais informações também de como definir os dados usando as entidades do Room. Já aqui embaixo a gente tem o objeto de acesso aos dados, com a anotação arrobaDAL. Nós sempre vamos criar uma interface com essa anotação. E dentro da interface, os nossos métodos de get, insert, delete vão ser definidos em funções abstratas com as anotações, que podem ser de arroba query, onde você tem um campo livre para poder informar qual é a query de busca ou de alteração na tabela que você gostaria de fazer, você inclusive consegue criar personalizações de campos, então você consegue usar o where aqui pra buscar, por exemplo usuários a partir de um determinado id, que vem de uma lista, então esse campo vai estar livre a usar a anotação arroba query, pra você construir a sua query SQL, certo? E o arroba insert, arroba delete e arroba update eles já vem prontos sem você precisar passar qual é a query que irá ser executada certo? inserts podem ser vinculados a varargs então você pode colocar quantos usuários você quiser um após o outro separados por vírgula ou você pode passar uma lista ou uma única opção, que aqui o RUM vai entender. Beleza, para saber mais você também tem essa outra documentação adicional, e aqui a gente vê a classe de banco de dados. Com isso a gente fecha os três componentes principais do RUM que a gente viu anteriormente. Então para criar a classe de banco de dados, nós vamos definir um arquivo do tipo abstract class, como abstract fun, que retorna o DAO, o Database Access Object, que a gente viu aqui em cima. Nós vamos ter a anotação arroba database, do qual nós precisamos definir as entities dentro de uma lista, e qual é a versão do banco. Se alguma entity mudar ao longo do tempo, ser adicionado um campo ou removido um campo, é necessário alterar a versão aqui do run database. Certo? E ele precisa estender de Room Database também. Beleza, aqui tem todas as informações que você precisa para gerar as configurações do seu Database Room. Beleza, e aqui a gente tem o uso. Certo? Para a gente criar o Database, nós vamos executar Room.databasebuilder, passar o contexto da aplicação, demonstrar qual é a inferência de classe que aponta para o app database, e o nome do database precisa ser passado também, certo? A partir do .build a gente tem a instância do nosso banco de dados run. Agora, já aqui embaixo, está sendo mostrada a forma como obter o DAO, o Database Access Object. Então o userDAO aqui no caso será db, que é essa instância que a gente criou, ponto userDAO. Visto que nós temos uma função aqui que entrega de forma automática esse objeto de acesso aos dados. Beleza, e aqui para buscar todos os usuários, a gente já tem o retorno de users do tipo listUser, executando apenas userDal.getAll. Simples assim. Com isso nós vimos a forma como configurar um banco de dados SQLite utilizando o ROM. nós vimos a forma como configurar um banco de dados SQLite utilizando o ROM. Agora nós vamos voltar para o nosso projeto para elaborar toda a lógica de obtenção dos campos preenchidos pelo usuário, das nossas atividades e, consequentemente, a construção e o gerenciamento desse banco, através de inserções, updates, buscas e deletes.