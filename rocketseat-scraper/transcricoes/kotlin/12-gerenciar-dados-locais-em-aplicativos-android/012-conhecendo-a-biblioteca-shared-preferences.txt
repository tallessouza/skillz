WEBVTT E chegamos finalmente no momento de darmos início com o armazenamento local do nosso aplicativo Planner. Nós vamos iniciar com a implementação oficial, tanto do Shared Preference quanto também do Data Store, para a gente já ter uma noção de quais códigos a gente vai precisar implementar para poder construir o formato de acesso, escrita e leitura desse banco de dados. E antes de começar a ler a documentação oficial do Shared Preference, logo de cara já é mostrado que o Data Store vai oferecer uma forma mais moderna de se armazenar os dados locais. De fato, essa biblioteca Datastore é uma biblioteca que chegou posteriormente ao Shared Preference. Shared Preference reinou por bastante tempo dentro do ambiente Android, porém o Datastore veio para suprir algumas lacunas de performance e desempenho, entregando um vínculo, por exemplo, com a distribuição de dados utilizando o Flow. Daqui a pouco a gente vai acessar o guia para mostrar como utilizar o Datastore. Mas inicialmente a gente vai falar do Shared Preference, caso você tenha um conjunto relativamente pequeno de chaves e valores para poder salvar. Certo? A API Shared Preference vai funcionar muito bem e ela é muito simples de se utilizar. A gente vai ver aqui na sequência. A forma como a gente vai processar essas preferências compartilhadas é primeiro criando um novo arquivo de preferências e depois fazendo o acesso. A partir do momento que a gente já tem um arquivo existente. Certo? E aí, através do método getSharedPreference, que pode ser acessado a partir de qualquer context no seu aplicativo, você consegue estar utilizando ele, passando como parâmetro o nome das preferências compartilhadas que você construiu. Certo? No caso, o nome do arquivo, que já vão ser referenciados no primeiro parâmetro desse método. Beleza, além disso a gente tem o getPreference, que já é um método que você vai utilizar dentro de uma activity, se você precisar de apenas um arquivo de preferências compartilhadas para a atividade. Certo? O getShieldPreference vai te dar a possibilidade de criar vários arquivos de preferências compartilhadas, identificados pelo nome. Já o getPreference vai aplicar essa criação e gerenciamento dentro de uma única Activity. Certo? Recuperando então um arquivo padrão que pertence àquela Activity. Certo? Não é necessário você fornecer um nome. Então a gente tem esses dois métodos aqui de acesso. Aqui a gente vê um exemplo de uso do GetSharedPreference, a partir do contexto de uma Activity, e buscando ali a partir da string com o nome do arquivo de preference. Como segundo parâmetro, o mode private. De acesso interno dentro do app. Já aqui embaixo, a gente vê a segunda forma utilizando o get preference. Que é uma única instância de arquivo de preferência. Vinculada a uma única activity. Beleza, de novo acessando ele via ModePrivate. E aqui um detalhe que o método GetDefaultSharedPreference vai aplicar o acesso ao arquivo de preferências compartilhadas padrão de todo o app. Certo? Com mais informações aqui caso você utilize esse padrão de implementação também. Agora aqui a gente tem um código para gravar informações dentro do shared preference. Então aqui ele já mostra que é necessário criar um shared preference.editor, chamando então o método edit. E aí você vai transmitir os valores e chaves através do uso de métodos como putInt, putString, putBullion, putDouble, enfim, todos os formatos de tipos de dados básicos estão disponíveis para você estar vinculando dentro desse formato de chave valor. Depois, na sequência, você precisa utilizar o método apply ou commit para poder salvar as mudanças que você acabou de fazer de escrita Certo? Então aqui a gente tem um exemplo de Primeiro busca de preferências através do getPreference Lembrando que aqui a gente vai estar pegando um arquivo padrão da activity Certo? Caso seja retornado nulo, é aplicado um return dentro dessa função, então a gente sai aqui do escopo do edit, e caso não seja nulo, o código sharedPref.edit é executado, abre-se aqui um escopo de chaves, é inserido através do putInt na chave de string contida lá no strings.xml, desse exemplo, savedHighScoreKey, o newHighScore, certo? O valor newHighScore vai ser inserido aqui nessa chave de string, o core vai ser inserido aqui nessa chave de string vinculada a strings.xml. Na sequência, logo depois do putInt, é acionado o método apply. Certo que muda imediatamente o objeto shared preference da memória, gravando as atualizações de forma assíncrona dentro do arquivo shared preference. Além disso, você pode estar utilizando ao invés do Apply o Commit, porém o Commit ele vai aplicar de forma síncrona. Então muito cuidado ao utilizar o Commit, porque ele pode estar pausando a renderização da sua UI. Ele é executado na Main Thread, na Thread principal, que a sua aplicação vai estar utilizando para poder criar todas as suas telas. Então é altamente recomendado você utilizar o Apply e fazer todos os seus armazenamentos de forma assíncrona, fugindo então do escopo da MainThread. Agora a forma como a gente vai ler de Shared Preference é através dos métodos GetInt e GetString, por exemplo. GetBoolean, getDouble getFloat fornecendo então a chave para o valor que você quer extrair e opcionalmente você pode também implementar um valor padrão caso não haja um valor contido naquela chave naquele momento então aqui embaixo a gente vê o código primeiro de acesso às preferências certo? ao arquivo único dentro da activity e depois é criado Então aqui embaixo a gente vê o código. Primeiro de acesso às preferências. Certo? Ao arquivo único dentro da activity. E depois é criado um default value. Que vai vir lá dos resources. Um valor base. Default. Para máxima nota salva. E aqui a tentativa de busca. Do high score. Então high score é igual a sharedPrev.getInt, passando o valor da key, da chave que está lá no strings.xml, savedHighScoreKey. E como segundo parâmetro aqui do getInt, o defaultValue que acabou de ser criado. Então caso ele não exista, defaultValue vai ser retornado. Muito simples, não? Então, aqui a gente tem todo o contexto de uso das preferências compartilhadas, do Shared Preference. Certo? Porém, a gente já viu aqui na documentação que o Datastore é, de fato, uma solução mais moderna que deve ser utilizado ao invés do Shared Preference. Então, por isso a gente vai acessar aqui a documentação para vermos as principais diferenças e como está utilizando o Datastore nos seus projetos. Antes disso, vale apenas uma ressalva. Nós comentamos anteriormente sobre o Encrypted Shared Preference, que é uma versão do Shared Preference para você armazenar dados encriptados no formato chave valor dentro do Android, certo? Essa é uma biblioteca oficial entregue pelo Android, porém está depreciado a biblioteca que implementa toda a parte de encriptação do Encrypted Shared Preference, certo? Security-Crypto, que é o artefato base aqui da biblioteca Encrypted Shared Preference, passou a ser depreciado. Aqui na documentação oficial nós temos a forma como implementar o Encrypted Shared Preference. Você ainda consegue estar utilizando, porém já não é mais uma recomendação devido a estar depreciado. Certo? Tudo que estiver depreciado no Android não é nada recomendado de você estar utilizando nos seus projetos porque não haverá mais nenhum suporte para aquele código dali para frente. Por isso nós não vamos implementar esses códigos de Encrypted Shared Preference no nosso projeto Planner, certo? Porém está aqui a documentação caso você queira seguir com a implementação em algum projeto pessoal seu ou até mesmo como complemento do aplicativo Planner, certo? E aqui eu deixei separado o merge do projeto Security Crypto do qual foi informado que todas as APIs de Security Crypto passaram a estar depreciadas já que dentro de Android Open Source Project, onde acontece toda a parte de atualização dos projetos open source oficiais do Google.