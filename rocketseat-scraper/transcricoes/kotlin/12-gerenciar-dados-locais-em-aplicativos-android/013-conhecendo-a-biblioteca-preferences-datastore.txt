WEBVTT Acessando agora a documentação do Datastore, nós vemos que essa é uma solução de armazenamento de dados que vai nos permitir também armazenar pares de chaves valores ou objetos tipados com os chamados buffers de protocolo. O principal diferencial do Datastore para o Shared Preference é a utilização das corrotinas e flows do Kotlin para poder fazer toda a parte de assincronismo, mantendo a consistência de forma moderna e confiável. Inclusive, a recomendação aqui embaixo é, caso você esteja utilizando o Shared Preference, considerar a migração para o Datastore. Se aqui um ponto muito importante do Datastore, vem aqui nessa observação. se você tiver que suportar conjuntos de dados que são considerados grandes ou complexos e o que seria esses grandes ou complexos seriam dados que são representados por classes ou data classes com diversos campos, que precisem de atualizações parciais, você mudar uma, duas, três ou mais propriedades, em um único momento, ou manter uma integridade referencial, ou seja, para uma determinada instância que está armazenada, você precisa ter sempre uma mesma referência sendo distribuída durante o acesso, você deve utilizar o RUM ao invés do Datastore. E o RUM, relembrando, é a biblioteca que vai fazer toda a parte de abstração dos bancos de dados relacionais utilizando o SQLite. Certo? O Datastore vai ser ideal para conjuntos de dados pequenos e simples e que não são compatíveis com atualizações parciais ou que devam manter uma integridade referencial de instâncias. Continuando então, o Datastore vai te entregar dois conjuntos de APIs, que seria o PreferenceDataStore e o ProtoDataStore. O PreferenceDataStore vai armazenar e acessar dados usando chaves, certo? Essa implementação ainda não vai requerer nenhum esquema definido, você não precisa estruturar propriedades que possam ter vínculo com um único dado. E ela não vai fornecer a chamada segurança de tipo. Você precisa informar qual é o tipo do valor que você vai estar obtendo do preference data store. Certo bem como a gente fez com o getInt, getFloat, getBullion no esquemático do shared preference. Boolean no esquemático do Shared Preference. Agora, já o ProtoDataStore vai armazenar dados como instâncias de um tipo de dados personalizado. Então você vai construir a sua classe que vai representar a estrutura de dados que você quer armazenar e um ponto importante também é que ele vai requerer a definição de um esquema utilizando os chamados buffers de protocolo. Certo que a gente vai falar sobre isso mais tarde. Mas uma grande vantagem é que existe a segurança de tipos. Então quando você informa qual é o esquema do protodata store baseado na classe, você tem a certeza de que aquele tipo de dado é o que vai estar sendo retornataStore baseado na classe, você tenha certeza de que aquele tipo de dado é o que vai estar sendo retornado para você no momento do acesso. Beleza, e aí como que a gente vai utilizar o DataStore da maneira correta, seguindo aqui a documentação? Aqui tem algumas regras, a primeira delas é nunca crie mais de uma instância do DataStore para um determinado arquivo no mesmo processo porque se houver vários datastores ativos, apontando para o mesmo arquivo ou seja, o mesmo arquivo ali referente a estrutura do banco de dados datastore criado vai acontecer a exception e legal state tanto ao ler quanto ao atualizar os dados o tipo genérico do datStore precisa ser imutável, sendo então altamente recomendável a utilização dos chamados buffers de protocolos para oferecer as garantias de imutabilidade e ao mesmo tempo uma serialização que é eficaz, que existe 100% de certeza que ela vai acontecer da maneira correta e te proteger contra possíveis falhas de serialização. E nunca misture os processos de uso do Single Process Datastore e do Multiprocess Datastore. Caso você precise acessar o Datastore em mais de um processo, sempre utilize o Multiprocess Datastore. Certo? Aqui, tudo isso é de acordo com a experiência de uso que você vai pegando a prática e que você vai, de fato, entendendo o por trás de cada uma dessas abordagens citadas. Certo? Não precisa se preocupar com isso agora. E agora, descendo para a parte de configuração, a primeira coisa que a gente vai fazer para adicionar o Datastore no seu app vai ser ir lá no build.gradle do módulo do seu app e adicionar implementation específica do preference datastore caso você queira o preference datastore certo aqui ele mostra pra gente que vem do androidx.datastore dois pontos datastore preference passando aqui na frente a versão aqui tem algumas outras opções de implementação com suporte ao RxJava 2 e RxJava 3. São bibliotecas um pouco mais antigas, referentes a toda a parte de controle de assincronismo. E aqui embaixo a gente vê as dependências necessárias para acesso ao ProtoDataStore. Certo? AndroidX.DataStore 2.DataStore Ok Agora descendo mais aqui embaixo A gente já vê a forma como armazenar Pares de chaves e valores com PreferenceDataStore Então vamos lá, para você criar um PreferenceDataStore Vai ser necessário a delegação Da propriedade, lembrando que a delegação da propriedade, lembrando que a delegação da propriedade vem através do uso da palavra reservada by, criada por preferencesDataStore, certo? O método preferencesDataStore vai fazer com que haja a criação de uma instância de dataStore do tipo preference. Então é necessário fazer a chamada uma vez no nível superior do arquivo Kotlin dessa forma criando uma variável imutável com este ponto data store dois pontos, o tipo inferido data store do tipo preference com a delegação by preference data store, e no caso name a gente vai passar o nome do arquivo, o nome do banco de dados em si que a gente vai trabalhar. Agora para a gente ler de um preferenceDataStore, como ele não utiliza um esquema definido, a gente precisa usar uma função de tipo de chave que vai ser correspondente ao tipo que a gente quer inserir dentro do valor, certo? Então a chave no momento da criação vai inferir para o datastore qual é o tipo que vai estar sendo armazenado. Certo? Então aqui nesse exemplo de inserção de valor de contador é criada uma chave do tipo int preferenceKey, certo? Com o nome exampleCounter. E na sequência é utilizada a propriedade datastore.data para poder expor o valor armazenado, utilizando o flow, certo? Então aqui a gente já vê que para ser feita a inserção, a gente precisa acessar context.datastore.data E aqui já entrando no contexto de flow é utilizado o .map Do qual retorna para a gente o acesso ao preference E na chave example counter é retornado o valor Caso não exista é retornado o valor Caso não exista É retornado 0 E aqui mostra que Não existe a segurança De tipo Desse dado retornado Beleza, agora aqui embaixo A gente tem a forma como gravar Em um preference data store Então vai ser liberada uma função Edit, que vai atualizar os dados De forma transacional. E que diz que existe também o parâmetro transform que vai aceitar um bloco de código que você pode atualizar os valores conforme necessário. Então aqui a gente vê que para ele gerar um incremento em um contador que está sendo armazenado no datastore preference, a gente executa context.datastore.edit, no escopo de chaves de edit, a gente vai ter acesso aos settings, via função lambda, para buscar o valor atual, a gente acessa settings, na chave example counter criada anteriormente do tipo int preferences key certo? então a gente sabe que é retornado inteiro, caso não exista é retornado um zero e já logo abaixo pra gente gravar basta a gente executar settings na chave example counter igual a current counter value mais 1. Beleza. Dessa forma a gente tem nosso dado sendo atualizado. Utilizando o padrão flow. Agora na próxima aula a gente vai estar falando sobre o proto data store dando continuidade aos conhecimentos dessa biblioteca.