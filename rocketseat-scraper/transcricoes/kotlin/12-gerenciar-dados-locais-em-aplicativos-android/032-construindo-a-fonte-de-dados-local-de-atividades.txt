WEBVTT Ok, agora nós vamos construir o DataSource referente às atividades que o usuário poderá cadastrar, buscar e também alterar. Então vamos lá. Aqui com direito no pacote DataSource, eu vou criar uma Kotlin Class File do tipo interface chamada PlannerActivityLocalDataSource. Activity LocalDataSource Essa interface Ela irá abstrair Todos os métodos contidos em DAO Certo? Então eu vou buscar aqui via Ctrl-C, Ctrl-V, esses métodos Aqui em Database, Activity DAO Vou copiar todos eles Ctrl-V Remover todos os arrobas Referentes aqui a classe do room ok, remover aqui os importes desnecessários e aqui nós não teremos uma função getAll, nós vamos ter uma propriedade de plannerActivities retornando o flow com a nossa lista de plannerActivity. Na verdade, nós não vamos ter um retorno das entidades. Nós vamos mapear essas entidades, convertendo elas para um outro formato de estrutura de dados, visto que PlannerActivityEntity possui, por exemplo, um ID que não será utilizado em momento algum dentro do nosso escopo de carregamento e trabalho com as atividades. Certo? Então, nós vamos mapear essa entidade para uma outra estrutura de dados sem esse campo ID. Então, vamos lá. Para isso, eu vou começar criando aqui em Model uma nova Kotlin class que se chamará PlannerActivity será uma data class essa data class terá praticamente os mesmos campos aqui que a gente tem logo abaixo de id então ctrl c ctrl v, porém a gente não vai ter esse colo info aqui vamos ver aqui esse imposte desnecessário e aqui em domain eu vou criar um outro pacote package, não será dentro de utils, será dentro de domain que se chamará mapper beleza uma outra coisa que nós vamos fazer também é alocar esse model e outra coisa que nós vamos fazer também é alocar esse model. E outra coisa que nós vamos fazer também é alocar esse pacote de model aqui para a domain. Então, eu vou fazer essa refatoração. Para seguir um pouco melhor o formato da nossa arquitetura. E aqui em mapper eu vou criar um novo arquivo. Kotlin. Chamado. Planner. Activity. Mapper. cotlin chamado plannerActivityMapper e aqui nós vamos ter uma função de extensão de plannerActivityEntity.to domain que irá retornar um plannerActivity certo? E será igual a criação de um novo PlannerActivity, passando no campo UUID o valor de this.UUID, isCompleted igual a this.isCompleted, name igual a this.name e aqui nós vamos ter também datetime igual a this.datetime agora aqui em PlannerActivity acessando através do ctrl-b nós vamos criar também uma propriedade de dateString que será do tipo string e irá retornar a chamada nós vamos criar também uma propriedade de DateString, que será do tipo String, e irá retornar a chamada daquele método que a gente criou no Utils, certo? No nosso DateTime Utils, convertendo de Calendar para um PlannerActivity Date, Time e também DateTime, certo? Então nós vamos chamar createCalendar from time em milis e depois a execução de cada uma dessas funções de extensão então nós vamos ter igual dateTime, calendar igual a createCalendar from time em milis passando o valor de dateTime certo? do tipo calendar vindo aqui de Android e seu útil será um método privado private e aqui nós teremos dateString sendo dateTime calendar.toPlanner activityDate. Além disso, nós vamos ter timeString, do tipo string, vindo de datetimeCalendar.toPlannerActivityTime, e também o nosso datetimeString, E também o nosso DateTimeString, que será DateTimeCalendar.ToPlannerActivityDateTime. Vou gerar um espaçamento aqui para deixar mais organizado. E beleza, essa será a nossa PlannerActivity do nosso pacote de domínio. do nosso pacote de domínio. O domínio da aplicação vai representar todo o escopo que tem um vínculo direto com a apresentação dos dados para o usuário. Certo? Aqui no pacote data nós vamos ter as regras de negócio sendo abstraídas da forma mais interna possível e depois a gente vai subindo primeiro para domain e depois a gente cai aqui no nosso padrão de UI. Beleza, esses são conceitos de arquitetura que a gente vai abstrair melhor no módulo específico de arquitetura para você entender tudo isso com detalhes. Ok, continuando aqui no nosso Data Source, ao invés de PlannerActivityEntity, nós vamos ter o retorno de PlannerActivity para todos os campos e aqui em Update também nós vamos ter uma instância de PlannerActivity sendo passada beleza, vou remover aqui esse imposto desnecessário e agora a gente vai criar a classe de implementação desse LocalDataSource então nós vamos ter PlannerActivityLocalDataSource e MPL, estendendo de PlannerActivityLocalDataSource. Então com a ajuda do Alt Enter eu vou implementar todos os membros. Ok. então com a ajuda do Alt Enter eu vou implementar todos os membros ok e receber como parâmetro private val planner activity dal agora aqui em planner activities nós vamos entregar planner activity dal ponto get all e nós vamos executar um map convertendo entities e vamos converter essas entities.map, no caso aqui nós vamos ter a análise de cada uma das entities, uma a uma, entity.toDomain. Certo? Assim nós convertemos a busca pelas entidades para um retorno de modelos que são referentes aqui à nossa camada de domínio. Já no getByUID, nós vamos ter um return de PlannerActivityDal.getByUID, nós vamos ter um return de plannerActivityDal.getByUUID vamos passar aqui o UUID e vamos gerar um toDomain no final. Ok. Aqui em updateIsCompletedByUUID, nós vamos executar plannerActivityDal.updateIsCompletedByUUID passando os nossos dois parâmetros UID e IsCompleted ok já em update como nós vamos receber PlannerActivity nós primeiro vamos buscar a entity através de PlannerActivity.getByUuid, passando no parâmetro Uuid, plannerActivity.Uuid. Beleza, ao buscar essa ente, nós vamos executar plannerActivityDal.update, e aqui a gente vai converter o nosso plannerannerActivity para PlannerActivityEntity. Para isso, nós vamos voltar aqui no nosso mapper. Vamos criar uma função PlannerActivity.toEntity. E aqui a gente vai precisar receber o id que será um inteiro certo? o retorno será plannerActivityEntity e aqui nós vamos criar um plannerActivityEntity do zero onde o id será igual ao id que a gente recebe de parâmetro e os demais campos que são esses aqui de, ctrl-c, ctrl-v vão ser vinculados ao this, que no caso aponta para a planner activity beleza, agora voltando, vou fechar aqui alguns arquivos, voltando aqui para a nossa implementação do datasource, no update nós vamos executar. PlannerActivity.toEntity. Passando como ID. Entity.id. Certo? E aqui o nome do nosso parâmetro será PlannerActivityEntity. Para deixar bem descrito tudo o que está acontecendo. Aqui no nosso código Ctrl Alt L Beleza E no delete by UUID Basta a gente executar PlannerActivityDal.deleteByUUID Passando o UUID aqui do parâmetro E está pronto o nosso local data source Certo? agora para construí-lo a gente volta aqui no nosso main service locator e ele será entregue através de val planner activity local data source do tipo planner activity local data source by lazy entregando a instância de do tipo PlannerActivityLocalDataSource by Lazy, entregando a instância de PlannerActivityLocalDataSourceImplementation, passando como parâmetro PlannerActivityDal, igual ao nosso PlannerActivityDal criado aqui em cima. Certo? Com isso, nós acabamos de construir toda a lógica referente à estruturação do banco de dados e também do data source local de controle das nossas atividades. Agora, na próxima aula, nós vamos avançar para a parte de lógica, construindo primeiro o nosso ViewModel e depois vinculando toda a lógica de View com as ações do usuário.