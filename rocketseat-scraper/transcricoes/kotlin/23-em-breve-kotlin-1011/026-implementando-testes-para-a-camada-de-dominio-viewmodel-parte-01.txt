WEBVTT E pra concluirmos os principais exemplos citados nos testes essenciais unitários baseados na documentação Android oficial que a gente viu o que nos resta agora é fazer um teste utilizando como exemplo um ViewModel aqui do nosso projeto, certo? Então a gente vai fazer um teste de ViewModel agora baseado no nosso ViewModel de busca por histórico do chat com a IA, certo? Então acessando ele aqui na camada de UI que representa aqui pra nós a camada de apresentação lá da arquitetura limpa, a gente vai acessar ai chat history view model e aqui nesse contexto a gente percebe que nós vamos ter o uso de dois use cases como dependência relacionando então com a entrega de três state flows, o de stack selecionada, o de ID do chip de stack selecionada e também da lista dos modelos de AI chat text que representam o nosso histórico de chat com AI baseado na stack selecionada também. Então, analisando aqui o nosso padrão de on event, do qual a gente expõe para as nossas views Aqui as chamadas do nosso ViewModel. Nós temos como possível o evento apenas a de seleção de stack, do qual a gente vai receber o nome da stack selecionada e também o ID selecionado ao chip vinculado a esse stack. Relembrando que é o ID de referência do elemento visual que a gente tem sendo mostrado no nosso XML. Então esse evento vai disparar essa função, que a partir do ViewModelScope.launch dispara a chamada desse nosso UseCase de busca de histórico de chat com o AI pela stack selecionada e faz um update tanto do nosso SelectedStackChipId quanto também de AIChatHistoryBySelectedStack. Certo, nós temos então uma mudança desses dois State Flows que a gente está vendo. Então vamos lá, o que a gente vai fazer agora é um teste que vincula o disparo exatamente desse evento que a gente está vendo, de SelectStack. Ou seja, o nosso usuário acabou de selecionar uma stack diferente da que estava selecionada anteriormente. Certo? Como base a gente vai estar utilizando os Stubs, sendo aplicados aqui para essas nossas dependências de UseCase. Então vamos lá, vamos partir para a construção dessa nossa classe de testes. Então selecionando aqui o nosso diretório de testes, eu vou criar agora o pacote de UI. Para a gente criar dentro de um outro pacote, ViewModel, a nossa classe. Certo? A nossa classe vai se chamar AIChatHistoryViewModelTest. Ok. Eu vou começar criando os nossos Stubs. PrivateVault. getSelectedStackUseCase do tipo getSelectedStackUseCase que será igual ao nosso MockKey. Beleza! Vamos ver também, private val getAiShedBySelectedStack useCase igual a getAishetchBySelectedStackUseCase igual a mockkey. Na verdade aqui não é igual, né? Seria o dois pontos. Beleza? Continuando, private lateinit var para o nosso ViewModel do tipo AIChatHistoryViewModel sendo este setado no nosso arroba before da função setup nós vamos ter a construção de uma instância de ViewModel a cada execução de função de teste que a gente vai construir ViewModel igual a AIShatHistoryViewModel passando aqui como GetSelectedStackUseCase o nosso GetSelectedStackUseCase mocado e também GetAIShatHistoryBySelectedStackUseCase nosso outro mock que a gente construiu. Beleza. Agora aqui no nosso setup a gente também vai fazer uma chamada prévia aqui a instância do ViewModel que será um every informando que toda vez que getSelectedStack useCase for invocado nós vamos retornar um flow of de kotlin ou seja, essa será a nossa stack inicial supondo que o nosso usuário já tenha uma stack que foi selecionada em algum momento de uso da aplicação inclusive eu vou até setá-la aqui em cima private val chamá-la aqui de dummy stack terá o valor de kotlin ctrl-c, ctrl-v aqui na chamada do nosso every. E aqui a gente vai chamar um every, por quê? Ao mostrar a forma como o evoke opera, ele não tem vínculo com a nossa como função suspensa, por isso a gente vai estar utilizando o every e não o co-every. Certo? Relembrando aí essa forma de configuração. Então agora a gente vai construir A nossa função, arrobaTest, será uma função baseada no givenWhenThen. A gente vai disparar um runTest. Já vamos construir aqui os nossos comentários também de given, de when. para deixar organizado o nosso código. E o nosso cenário de testes irá validar, dado um histórico vazio, empty history, quando trigar selectStackEvent Então, ai chatHistoryBySelectedStack e stackShipId deverão ser atualizados. Certo? É isso que deve acontecer. A gente vai simular um cenário onde houve uma mudança de stack e o histórico passou por uma mudança do qual antes era um contexto de lista vazia e agora passa a não ser mais. Ok? Então vamos lá. Primeiro, eu vou ter aqui o nosso SelectedStack Name que será Java SelectedStack ShipId será 123 E aqui a gente vai ter também Nosso AI chat historyBySelectedStack sendo igual a uma lista a partir da criação aqui dos nossos stubs de from igual a ai chatTextType userQuestion do qual a stack será a stack selecionada. Vou até chamar aqui de SelectedStackName. E o texto eu não vou passar por ser indiferente. Então, a gente não vai estar analisando aqui o campo de texto. Vamos criar mais um. Porém, não será uma UserQuestion, será uma AIAnswer. Esse aqui será o exemplo de lista que está armazenado localmente para a stack selecionada Java. E aqui, lembrando, executar o toDomain. certo? que eu vou chamar de ai chatHistoryBySelectedStack Stubble e além disso nós vamos precisar também setar o comportamento do nosso get ai chatBySelectedStackUseCase quando a stack recebida for a nossa SelectedStackName quando isso acontecer a gente deve retornar o nosso AI chatHistoryBySelectedStackStub, certo? Na verdade aqui o stub eu vou jogar ali pro início do nome da nossa variável pra deixar Mas, claro, qual que é o tipo de test double que a gente está utilizando aqui. Agora, o nosso quando está relacionado ao trigger de evento de SelectStack. Então, nós vamos ter ViewModel.onEvent.ai. ChatHistory.event.SelectStack. O nosso evento. E eu vou precisar passar dois parâmetros. O primeiro será SelectedStackName, certo? O nosso SelectedStackName que a gente criou ali em cima. E a gente vai precisar também passar SelectedStackChipID, que também será o nosso SelectedStackChipID. CTRL-ALT-L. Certo? Eu vou chamá-los aqui de dummy. Pra cada um deles. Visto que esses valores são completamente aleatórios e não interferem, e não iriam interferir ali no foco do nosso teste. Ctrl-V para alterar aqui primeiro o SelectedStackName, onde ele estava sendo utilizado. Agora Ctrl-C aqui no nosso ChipID para mudar na linha 58 o valor passado como parâmetro em SelectedStackChipID do nosso evento de SelectStack. Então agora o nosso ViewModel acabou de receber um evento referente a ação de usuário de seleção de nova stack. Agora no then, a gente vai setar o que a gente espera que aconteça nesse momento. Então primeiro, a gente espera que o nosso StateFlow de ai.chatHistoryBySelectedStack receba um novo valor. Então eu vou abrir aqui o ponto teste do Turbine para a gente inserir o AssetEquals. Alt-Enter para importar essa função de AssetEquals. E o nosso expected será stub AI.SharedHistoryBySelectedStack a partir do nosso result, que será o awaitItem. A gente vai esperar que um item chegue aqui nesse nosso StateFlow, e assim que o primeiro item chegar, a gente vai verificar se esse resultado será igual ao nosso stub que a gente acabou de construir. Beleza. Além disso, a gente vai verificar também viewModel.selectedStackChipId.test, através dos reticles, se dummy selectedStack.shpid seria o result que a gente vai ter aqui também, ctrl-c, ctrl-v referente ao nosso await item. A gente vai esperar um item chegar aqui nesse nosso state flow e verificar se o valor dele será o de shpid que a gente acabou de construir aqui em cima. É isso o nosso teste, que valida toda a execução aqui de chegada de um evento de seleção de nova stack. Vamos lá. Vamos colocar aqui para rodar. E aqui eu vou mudar antes o nome para dummy initial stack. Certo? É um dummy para stack inicial selecionada através do getSelectedStackUserCase. Beleza. Vou rodar aqui a nossa função de teste. E o nosso teste passou. Inclusive eu vou rodar agora também o teste com coverage pra gente ver o quanto de percentual de linhas de código que a gente conseguiu abranger nesse nosso ViewModel com essa classe de teste. Então acessando aqui UI ViewModel a gente percebe que 100% das nossas linhas de código, métodos e classes foram devidamente testados aqui apenas com esse cenário.