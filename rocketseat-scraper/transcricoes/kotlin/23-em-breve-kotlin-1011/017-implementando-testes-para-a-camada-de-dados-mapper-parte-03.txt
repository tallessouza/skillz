WEBVTT Agora, o que ficou faltando é o toDomain da nossa lista de entidades, sendo convertido para uma lista de modelos da camada de domínio. Vamos lá. Voltando aqui para o nosso teste. Vou dar um Ctrl-C, Ctrl-V aqui na nossa primeira função. Vou minimizar aqui a visão de projeto, para termos mais um campo maior para criar o nosso teste. E aqui a gente vai ter given AIChatTextList. Vamos remover aqui o from UserQuestion. When ToDomain is called, quando o ToDomain for chamado, então a gente deve converter para uma AIChatTextList. Certo? Simples assim. Pra construir a nossa lista, a gente vai chamar aqui de AIChatTextEntityList. Nós vamos ter, vou substituir aqui no nome também, Ctrl-C, Ctrl-V. Nós vamos ter um uma lista sendo construída. Aqui eu vou passar a quantidade de elementos, vamos deixar aqui 10. E aqui dentro das chaves, nós vamos ter a criação de AIChatTextEntityStub. O que vai acontecer é, dentro do escopo aqui de chaves, a construção de cada uma das entidades. E aqui a gente consegue receber na inicialização o índex. Certo? Esse index vai fazer com que haja a seleção de qual tipo aqui da nossa entidade. Então, when index, vou fechar parênteses aqui embaixo, caso ele seja Par, nós vamos ter o tipo UserQuestion. Caso ele seja igual a zero, nós vamos ter o NULL. Caso ele seja ímpar, nós vamos ter o tipo AIChatTextType.AIAnswer. o else aqui será NULL. E aqui no caso eu preciso remover os parênteses aqui de índex, que a gente vai lidar. Eu tenho que passar aqui para cima também o igual igual a zero. então index 0 a gente vai ter um tipo desconhecido caso seja um número par a gente vai ter o tipo user question e se for um número ímpar ai answer e só pra nos proteger aqui com o when a gente vai ter no else o retorno de nu beleza nosso result já está executando o toDomain pra conversão de lista e agora aqui embaixo o que a gente vai verificar é um assert equals se result ponto size é igual ao nosso ai-chat-text-list ponto size. Os dois devem ter o mesmo tamanho. Certo? Como a gente já validou as conversões aqui em cima, a gente não precisa validá-las novamente aqui nesse escopo de código. Certo? Então vamos lá. Vamos executar aqui o nosso teste. E aqui a gente percebe que houve o disparo da exceção devido ao valor de UNKNOWN aparecer por termos usado aqui o NULL. Então o que eu vou fazer é remover o INDEX igual a 0, visto que a gente já validou isso aqui em cima, para podermos fazer com que o nosso teste esteja condizente com o escopo a ser validado. Vamos executar novamente. E agora sim, o nosso teste passou. Agora, o que a gente vai fazer é analisar qual que é a cobertura de quantidade de linhas de código que foram validadas aqui dentro dessa classe. Certo? Então, para isso, basta você clicar aqui na execução e rodar o teste com coverage, com cobertura. Quando a gente roda ele dessa forma, é aberto aqui para nós. Do lado direito, o percentual de testes relacionado de cada arquivo. Então, se a gente entra aqui em Data, Mapper, a gente percebe que 100% do nosso código foi testado dessa classe dentro desse arquivo, ok? E além disso, a gente ainda validou também 100% dos nossos códigos envolvidos. nas classes de model da camada de domínio em ai-chat-text e ai-chat-text-type. Na seção do local aqui também, nós temos dentro de database que ai-chat-text-client também teve suas propriedades validadas e com isso a gente vai aumentando cada vez mais a cobertura de teste de todo o nosso projeto. E é isso, agora na próxima aula a gente avança para os testes do nosso repositório.