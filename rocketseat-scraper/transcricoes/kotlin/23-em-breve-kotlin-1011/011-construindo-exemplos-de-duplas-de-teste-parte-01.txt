WEBVTT Vamos acessar o nosso arquivo example-unit-test dentro do pacote test. Certo? Então vamos lá. A primeira coisa que eu vou fazer é remover essa função que a gente tinha de teste. E a primeira função que a gente vai estar construindo vai nos garantir exemplos das duplas de teste fake, dumb e stub. Então relembrando aqui os conceitos do que seria cada uma delas, o fake seria uma implementação real, porém simplificada de uma interface que a gente possui no nosso projeto, certo? Então a gente vai construir, através do fake, por exemplo, uma implementação de um data source local que, ao invés de utilizar o room, possui apenas uma mutable list que contempla as inserções, deletes, atualizações para serem entregues posteriormente para, por exemplo, um repositório que está sendo testado. Já o dummy consiste de um objeto criado apenas para preencher parâmetros obrigatórios. muito parecido com o que a gente faz lá nos previews dos nossos composables, que é o uso de diversas funções lambdas vazias para apenas garantir a passagem de parâmetros obrigatórios, por exemplo, de callbacks de ações. Aqui em casos mais específicos, a gente poderia estar construindo objetos com todos os valores default apenas para um teste de passagem de parâmetro aqui interno do nosso contexto de testes unitários. Além disso, a gente vai ter também, como teste dessa nossa primeira função, o stub. O stub, ele se parece muito com o mock. Já já a gente vai ver a diferença dos dois. Como definição, a gente tem que o stub é um objeto que retorna valores fixos. Porém, ele não verifica interações que aconteceram internamente nesse objeto. Então, por exemplo, a gente não consegue estar verificando se um determinado método foi chamado ou não dentro do escopo de códigos de teste. Ele não tem essa capacidade de entregar verificações. E a gente vai usar o mockk para poder construir os nossos stubs e também os nossos mocks. Então, vamos lá. Com essas definições a gente já consegue estar construindo a nossa primeira função de teste. Então primeiro a gente aloca aqui o arroba teste. Depois eu vou criar aqui a função de example fake dummy and stub. Esse vai ser o nome da nossa função. E vamos começar então com o nosso fake. Dentro do escopo do nosso app, como a gente utilizou como base a arquitetura limpa, dentro de data, a gente tem os nossos data source, que a gente vai construir um fake para o nosso data source remoto, que é o nosso data source mais simples. Certo? Então, lembrando, Aqui a gente tem, como fake, uma implementação real, porém simplificada de uma interface. A gente tem aqui a interface. Agora o que a gente vai fazer é, dentro do escopo de pacotes aqui dos nossos testes, será construir uma implementação simulada para esse data source. Então, primeiro eu vou criar aqui o pacote que referencia exatamente a árvore de passas até chegar na nossa interface, então rocket A. Vou criar aqui o pacote data, datasource, e aqui dentro nós vamos ter a chamada de fake-ai-chat-remote-datasource-implementation. Vou importar aqui. chatRemoteDataSource como extensão dessa classe alt enter implementar os membros remanescentes então aqui, o que eu vou estar fazendo será passar como parâmetro aqui dessa implementação fake uma lista private val de stacks válidas que é uma lista de string, certo? E aqui a gente vai retornar na chamada de sendPrompt return se validStacks.contains a stack que está sendo passada a gente vai retornar uma string com o texto validAnswer caso contrário, a gente vai retornar nulo ou seja, com o fake a gente já consegue ver aqui que é possível adicionar parâmetros que possam fazer com que o nosso teste crie situações para fazer com que o fake trabalhe de forma diferente por exemplo, a gente pode abrir também aqui uma função para a emissão de erro com o parâmetro should emitError booleano sendo passado e aqui a gente pode ter, por exemplo, uma variável should emitError booleano inicialmente igual a falso que atualiza o seu valor this.shouldEmitError igual ao shouldEmitError recebido a gente converte aqui esse if para um else if e aqui a gente adiciona o if como sendo shouldEmitError verdadeiro o disparo de uma exception com a mensagem, por exemplo, HTTP exception. Ctrl Alt L aqui pra formatar o nosso arquivo. Vou quebrar a linha aqui do nosso if. Certo? Então dessa forma a gente vê o ganho que os fakes podem ter, que é a capacidade de construir todo um comportamento específico de implementações a partir de interfaces utilizadas pelo nosso projeto. Certo? Então voltando aqui no nosso exemplo. de testes unitários, a gente vai criar agora o fake AISHAT Remote Data Source Implementation sendo igual ao nosso fake AISHAT Remote Data Source Implementation. A gente vai passar aqui como Valid Stacks uma lista contendo apenas Kotlin. Certo? Beleza, o nosso fake foi criado. Daqui a pouco a gente vai estar fazendo o uso dele. E para o exemplo do stub, o que a gente vai fazer é construir um stub para o nosso AIChatLocalDataSourceImplementation Certo? Eu vou acessar aqui primeiro a interface do nosso LocalDataSource E aqui a gente percebe que existem três diferentes funções e duas variáveis que devem ser contempladas na implementação caso a gente fosse utilizar a base do fake pra poder construir essa instância pro teste a gente iria ter um certo trabalho pra gerar cada uma dessas implementações pro nosso fake e é por isso que o stub vem pra poder deixar mais fácil e mais direto o trabalho com essas implementações que até então serão utilizadas na base de código de teste. Com o stub, o que a gente vai fazer é a construção de um objeto que retorna valores fixos, que não precisa fazer a verificação de interações. E aí o Mockkey entrega pra nós a possibilidade de se estar fazendo isso através da chamada do MockK, que é uma função da biblioteca Mockkey. Certo? Primeiro eu vou importar aqui essa função, e essa função precisa receber qual será o tipo do elemento a ser vinculado no stub. Então, a gente vai ter o AIChatLocalDataSource. Porém, desse jeito, o nosso stub ainda não foi construído. A gente precisa informar quais serão os valores fixos que o stub irá entregar assim que ele for executado. O mockkey retorna pra nós a possibilidade do uso da chamada every. O uso do every ou do call every, ambos tem a mesma implementação, a diferença é que no every a gente vai ter as regras do que será entregue em funções que não tem vínculo com corrotinas, funções que não são suspensas. E o Coevery será utilizado quando a função que a gente está vinculando é uma função suspensa. No caso aqui, a gente vai ter que usar o Coevery. Por quê? A função que a gente vai estar vinculando aqui no stub, informando qual será o valor fixo a ser entregue, será aqui a função de GetAIChatByStack. Certo? Todas as nossas três funções aqui dessa interface são suspensas. Todas deveriam usar o Coevery para poder ser informado qual será o retorno. Mas saiba que, caso não existisse o suspend fun, bastaria a gente estar utilizando o every. Certo? Da mesma forma. Então, voltando aqui, nós vamos informar que sempre que o nosso stub ai.chat.local.datasource.implementation.getAiChatByStack for chamado, não importa a stack que for passada como parâmetro. Então aqui, eu vou usar o n que é uma função específica aqui do Mockkey que vai informar que qualquer parâmetro ao ser vinculado aqui a chamada de getAichatByStack irá retornar, returns, uma lista específica de objetos, certo? Se a gente passa o mouse aqui em cima a gente tem uma lista de AichatTextEntity E agora a gente vai criar essa lista, porque essa lista vai usar o padrão de dummy pra receber três inserções aleatórias, não importa o valor, ou seja, quando a gente vê aqui a definição de dummy, a gente tem um objeto criado apenas para preencher parâmetros obrigatórios. É isso que a gente vai construir.