WEBVTT Fala pessoal, sejam muito bem-vindos a mais uma aula do nosso módulo de NumPy. Nessa aula nós vamos aprender sobre os atributos dos arrays. Os arrays NumPy possuem vários atributos e esses atributos vão fornecer informações sobre a estrutura e os dados de um array. Então nós vamos começar a ver os principais atributos e depois vamos aprender também como converter o tipo de dados de um array. Então, para que a gente possa iniciar, nós vamos ver o primeiro atributo, que é o atributo DTYPE. O atributo DTYPE vai indicar o tipo de dado dos elementos que estão armazenados no array. Então, eu posso ter, por exemplo, números inteiros, eu posso ter um array de ponto flutuante, eu posso ter um array de valores booleanos, de strings. Em geral, eu vou ter de números, porque o NumPy é muito focado justamente na matemática. Porém, eu posso ter essa estrutura de dados de um outro tipo de dados sem problema algum. Então para que a gente possa iniciar nós vamos importar o nosso numpy, import numpy snp, colocar aqui separado logo no início para que a gente não tenha nenhum tipo de problema e vou executar. Chamei o numpy, a gente já consegue criar aqui um array para verificar o tipo de type, para ver o atributo de type. Então a gente vai criar aqui o nosso array número 1, esse array número 1 vai receber nossa função np.array. E nós vamos enviar uma lista contendo os números 5, 17, 32, menos 50. Eu tenho 4 elementos que são números inteiros. Vou dar um print no meu array 1. Então ele apareceu para mim aqui os elementos do meu array e a gente vai verificar o print array1.dtype array1, faltou o número 1 aqui, então ele retorna para mim que o tipo desse array número 1 é do tipo int64. Ele é formado por números inteiros. Nós vamos criar aqui um segundo array, que eu vou chamar de array. Vou fazer melhor, ao invés de ser ser array1 eu vou colocar array underline int para a gente saber que é um array de inteiros e o segundo ali nós vamos colocar um array underline float ele vai receber um np.array nós vamos colocar uma sequência de números de ponto flutuante vamos colocar o número 2 vamos colocar 1.5 3.4 menos 17 menos 3.2 então nós vamos colocar aqui 5 números inteiros ou melhor, 5 números ponto flutuante e vamos dar um print no meu array de float e vamos dar um print no meu array de float e vamos dar um print também no meu array de float .detype .detype .detype sem o s então ele retorna pra mim aqui Meu conjunto de elementos do array E retorna o tipo Que é do tipo float64 Vamos criar aqui também Colocar aqui Array de float Criar aqui um array Buleando Só pra gente ver o the type Como é que ele funciona Vou colocar aqui array de float. Vou criar aqui um array booleano. Só para a gente ver o DTYPE como é que ele funciona. Array bool. Vou criar um array de valores booleanos que vai receber um npm.array. Nós vamos enviar uma lista true, false, true. Vamos dar um print no nosso array de bool e vamos dar um print no array bool.d type então ele retorna pra mim que o tipo é o tipo bool o tipo do array booleano pra finalizar vamos criar aqui um array de strings. Um array str vai receber np.array, vamos enviar uma lista de nomes, José Maria. Vou colocar dois nomes Vou dar um print No meu array str E vamos dar um print No meu array underline str.thetype Então ele retorna para mim José e Maria E no tipo string ele retorna Menor que o 5 Ou seja, aqui a gente tem um array de strings Onde a sequência de caracteres não é maior do que 5 Então, por exemplo, se eu colocar um nome maior aqui Ele vai retornar menor do que o 16 Significa que as minhas strings dentro do array São menores do que 16 caracteres É dessa forma que ele identifica um array de strings. A gente também tem outro método, outro atributo, método não, atributo que é o atributo shape. Ele vai retornar uma tupla indicando o número de elementos que eu tenho em cada dimensão do meu array. Então vamos criar aqui primeiro um array de uma dimensão, então vamos chamar de array uma dimensão vai receber np.array. Nós vamos colocar valores aleatórios. 10, 43, 22, 9. Vamos dar um print no nosso array de uma dimensão. Ele apareceu aqui. Vamos dar um print no nosso array de uma dimensão. Ele apareceu aqui. E aí nós vamos dar um print no meu array de uma dimensão.shape. Então ele vai retornar aqui 4 vírgula. Isso significa que ele tem apenas 4 elementos. Como é uma tupla, e uma tupla, quando a gente tem uma tupla de um único valor, ele aparece dessa maneira, utilizando a vírgula. Então, aqui eu tenho um array de uma única dimensão. E nessa única dimensão, nós temos 4 elementos, justamente o 10, 43, o 22 e o 9. Vamos fazer um outro exemplo aqui com duas dimensão nós temos quatro elementos justamente o 10, 43, o 22 e o 9. Vamos fazer um outro exemplo aqui com duas dimensões, uma matriz. Vamos chamar aqui de matriz e vai receber np.array. Nós vamos criar uma matriz com os números 10, 49, 37 10, 49, 37. Na primeira linha e na segunda linha nós vamos colocar menos 13, 9 e menos 45. Então aqui nós temos uma matriz com duas linhas e três colunas. Nós vamos dar um print. Na nossa matriz. Ele deu um erro aqui, vamos ver qual erro. Tenho três elementos aqui Três elementos aqui Pronto Aqui eu estou colocando como se fosse Uma única dimensão Faltou o meu segundo colchetes Ele vai ficar como se tivesse uma única dimensão E são duas dimensões Pronto, agora foi Então aqui estava o erro Utilizando uma única dimensão, linha e coluna E na verdade são duas dimensões Então aqui a primeira linha A nossa segunda linha. E aí nós vamos dar um print na nossa matriz. Pronto, ponto shape. Então, ele retorna para mim que eu tenho duas dimensões. A primeira dimensão eu tenho dois elementos, que são duas linhas. E na segunda dimensão eu vou ter três colunas então ele diz para mim que são duas linhas e três colunas então é dessa forma que eu vejo quantos elementos eu tenho em cada uma das dimensões eu tenho também a função size que retorna para mim o número total de elementos do array Diferentemente da shape que vai retornar a quantidade de elementos em cada dimensão Aqui ele retorna o número total de elementos Então a gente pode utilizar até aquela matriz ali de cima para a gente ver Então vamos dar um print na nossa matriz ali de cima para a gente ver vamos dar um print na nossa matriz .size e ele retorna aqui 6 porque nós temos 6 elementos 3 elementos na primeira linha, 3 elementos na segunda linha, então são 6 elementos eu posso utilizar aqui também o array de primeira dimensão para dar um print então o nosso array faltou aqui colocar o size ele tem quatro elementos, o array de uma dimensão, então está bem aqui, tem quatro elementos, então essa função size é bem simples, ela só vai retornar independente de quantas dimensões nós tivermos no array, ela retorna a quantidade total de elementos. Nós vamos ter a nDing que nós já vimos anteriormente, então vamos criar aqui um array de uma dimensão, vamos chamar de 1D, que é uma dimensão np.array 2.3 4.5 e o número 9 e vamos dar um print no meu array underline 1d.ngin então ele retorna aqui que esse array possui uma única dimensão vamos colocar também uma matriz que vai receber np.array e essa matriz vai ter uma duas dimensões na segunda dimensão a primeira linha vai ser formada pelos números 1 e 10 e na segunda linha vai ser formada pelos números 3 e 19 e nós vamos dar um print a nossa matriz. Ngin. Então ele retorna que tem. Duas dimensões. Linha e coluna. Uma linha e uma coluna. São duas dimensões. E por último a gente consegue fazer. Conversão de arrays. Ou seja a gente consegue converter. O tipo de dado de um array. E a gente tem esse método. S type. Então nós vamos criar aqui. array de inteiros para converter para float e um array de float para converter para inteiros para a gente ver como é que funciona. Então vamos criar primeiro um array de float. O O nosso array float vai receber o método np.array E nós vamos colocar os números 1.5, 2.7, 3.2 Eu tenho três elementos do tipo float dentro do meu array Então vamos dar um print no meu array de float E vamos verificar o tipo dele dentro do meu array. Então vamos dar um print no meu array de float e vamos verificar o tipo dele array__float.dtype com dtype a gente consegue ver qual é o tipo de dados do meu array então o meu array é um array do tipo float64. Então agora a gente vai fazer o nosso cast e a nossa conversão. Vamos criar um array underline cast,odo Stype ele que vai converter Para o tipo de dados Que eu informar Nesse caso aqui eu quero converter Para int E aí a gente vai dar um print No meu array underline cast E vamos dar um print No meu array Array underline cast .dy Então aqui ele no meu array underline cast ponto the type então aqui ele pegou o nosso array e ele converteu para inteiro int64 e o detalhe, o que ele fez com os valores quebrados ele simplesmente truncou ele removeu ignorou a casa decimal então o comando s type quando eu converto para int, um valor float, ele vai ignorar totalmente a casa decimal. E aí a gente consegue fazer também o oposto. Se a gente tem aqui o array cast, que é do tipo int, a gente consegue converter para load. Então vamos criar aqui um array chamado arrayUnderlineAuxiliar. O arrayUnderlineAuxiliar vai receber o meu arrayU array underline cast, que a gente tinha convertido aqui para inteiro e a gente vai utilizar o ponto cast type para converter para float então eu vou pegar meu array cast, que é um array de int64 e vamos converter para float vou dar um print no meu array underline auxiliar, vamos dar um print no meu array underline auxiliar. Vamos dar um print no meu array underline auxiliar ponto de type. Então, ele pegou, por último aqui, o array que tinha ficado com os números 1, 2 e 3 inteiro e converteu para float. Ele simplesmente colocou um ponto para informar que ele é do tipo float64. Então é dessa forma que eu consigo converter os meus arrays e utilizar os principais atributos de array numpy, que são os atributos the type, o atributo shape, o atributo size e o atributo ng. Então, aguardo vocês na próxima aula.