WEBVTT Fala pessoal, sejam muito bem-vindos a mais uma aula do nosso módulo pandas. Nessa aula nós vamos falar sobre a estrutura de dados séries. Então, uma série dentro do pandas vai ser uma estrutura bem parecida com uma lista do Python ou mesmo com um array unidimensional ali do numpy e a gente também pode fazer uma comparação com uma coluna dentro de uma tabela. Então a séries vai ser esse tipo de estrutura de dados e dentro de uma séries do pandas você pode ter dados de qualquer um dos tipos que nós já aprendemos, dados do tipo inteiro, do tipo string, do tipo booleano. E nós vamos ter um índice associado a cada um dos elementos. E esse índice tanto pode ser um índice numérico, começando ali do zero, como ele também pode ser um índice que contenha um label. também pode ser um índice que contenha um label. Então, você pode definir um label para aquele índice do elemento dentro de uma série do pandas. Então, nós vamos começar aqui nossa aula, primeiro fazendo a importação do pandas, e depois vamos aprender a criar uma série que é bem simples. Então, nós vamos utilizar o comando import pandas spd. E aí dessa forma a gente consegue importar a biblioteca pandas aqui para o nosso Google Colab. E agora a gente vai primeiro aprender a criar uma série do pandas. E nós vamos fazer isso a partir de uma lista. Então, vamos criar uma lista de números. Lembrando, criar uma lista a gente usa os colchetes. E vamos colocar os números 10, 20, 30, 40 e 50. Então, para a gente criar uma série do pandas, a gente vai chamar ela de série número 1 e ela vai receber o comando pd, que é para a gente acessar os métodos da biblioteca pandas, ponto series com S maiúsculo. Para a gente acessar os métodos da biblioteca pandas. Ponto. Series com S maiúsculo. pd.series E nós vamos enviar uma lista. No caso a gente vai enviar a nossa lista de números. Para essa função. E aí a gente vai dar um print. Na nossa lista série 1. ou melhor, na nossa série. Vamos executar. E aqui ele retorna para a gente o índice, no caso 0, 1, 2, 3, 4, e os valores como se fosse uma coluna mesmo. Então aqui 10, 20, 30, 40, 50. Cada elemento está associado. Ao índice. Então isso é uma série do pandas. A gente pode olhar. Aqui. Criar uma célula. Para dar um print. No type. Print type da série 1. E aqui ele retorna o tipo pandascore series.series então dessa maneira a gente consegue verificar que aqui é o tipo que é bem parecido com o array de uma dimensão ou mesmo uma lista só que ele fica no formato aqui bem parecido com uma coluna que a gente é acostumado a trabalhar. Então, como a gente tem esse índice, eu consigo acessar o elemento a partir do índice. Então, vamos colocar aqui, acessar um elemento da série. A gente consegue aqui dar um print, por exemplo, no índice. Então, a gente coloca o nome da série e coloca o índice, por exemplo, o índice 3. Executando, ele retorna aqui 40. Então, a gente olha aqui que no índice 3 a gente tem o 40. Então, a gente consegue fazer dessa maneira. Aqui a gente não definiu nenhum tipo de índice Igual eu falei, uma série pode ter um índice numérico Quando a gente não define E a gente pode definir um label, um rótulo Ou seja, a gente pode trabalhar com elementos do tipo string Às vezes é bem interessante fazer isso Então a gente vai criar aqui uma nova célula e nós vamos criar uma série com índice personalizado então vamos criar uma lista de números e essa lista vai ter os números 100, 200 e 300. E agora a gente vai criar uma variável chamada index e essa variável index ela vai receber uma lista de índices. Agora esses índices vão ser no formato string. Então nós vamos colocar para o número 100 o índice A, ou melhor, o carácter A. Nós vamos colocar para o 200 o carácter B e para o 300 o carácter C. Então agora a gente vai ter um índice personalizado. Ao invés de utilizar os números 0, 1 e 2, a gente quer utilizar uma string, no caso aqui contendo A, B e C. O A vai representar o 100, o B o 200 e o C 300. Ele vai indexar e aí a gente vai criar aqui uma série, vamos chamar de série personalizada e a nossa série vai receber o pd. pd.stereos e aí a gente vai enviar primeiro a lista que é a lista, underline, números e agora a gente vai enviar primeiro a lista, que é a lista, underline, números. E agora a gente vai ter um índice, que é a nossa variável aqui, nossa lista, index. A lista de índices. Então, no pd.service, agora eu estou enviando, além da lista, eu estou enviando o meu índice. E a gente vai dar um print. Insere personalizada. Vamos executar. Agora, ao invés de eu ter os números 0, 1, 2, 3, agora eu tenho um índice que é uma string. Então, aqui ele é o índice A, o índice B e o índice C. E aí eu consigo, por exemplo Dar um print Na minha série Personalizada No índice B Agora eu não coloco o índice numérico, agora eu coloco Esse índice aqui em cima Então eu vou executar o índice B Ele retorna que é 200, se eu colocar o índice C 300 Se eu colocar um índice inexist Se eu colocar o índice C, 300. Se eu colocar um índice inexistente, por exemplo, o índice G, vou executar, ele dá um erro aqui, onde ele não encontrou o G, o G não é um índice existente. Então, é dessa maneira aqui que eu consigo criar índices personalizados para uma série. E a gente também consegue criar a partir de um dicionário. Então, nós vimos aqui criando uma série a partir de uma lista. Eu também consigo fazer a partir de um dicionário, porque o dicion ele já tem ali uma chave né e essa chave em geral é uma chave personalizada uma chave do tipo string então a gente vai criar aqui uma série séries a partir de um dicionário então nós vamos criar aqui o nosso dicionário chamado dados e esse dicionário vai receber, vamos colocar aqui a chave primeiro, que vai ser o número 1, a chave 2, que vai representar o número 2, e a chave 3, que vai representar o número 3. Vamos dar um espaço aqui para ficar organizado. Então, aqui está nosso dicionário. Vamos dar um print no nosso dicionário para a gente visualizar. Então aqui está nosso dicionário, lembrando que o dicionário tem chave e valor. Então a chave primeiro representa o valor 1, a chave segunda representa o valor 2 e a chave terceira representa o valor 3. O terceiro representa o valor 3. E a gente vai criar agora um dicionário. Vamos chamar um dicionário chamado S-SERI-DICT que vai ser uma série a partir de um dicionário. E ele vai receber o pd.series. de um dicionário e ele vai receber o pd.séries. E a gente vai enviar o nosso dicionário dados para lá. Deixa eu comentar esse print do dados. Nós criamos aqui e vamos dar um print aqui abaixo em uma outra célula, a nossa série underline dict. Então a gente criou diretamente de um dicionário e ele retornou aqui primeiro 1, segundo 2, terceiro 3. Esse aqui é a nossa série, nossas séries a partir de um dicionário, então ele já criou com índice personalizado. Agora a gente consegue acessar, por exemplo, dando print insert underline dict, eu consigo acessar aqui, por exemplo, o elemento segundo a partir do índice. E ele retorna 2. Se eu colocar terceiro, ele retorna 3. Então, dessa forma que eu consigo utilizar as séries dentro aqui do Pandas, criando a partir de uma lista ou mesmo de um dicionário, ou utilizando índices personalizados. Então, aguardo vocês na próxima aula.