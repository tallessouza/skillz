WEBVTT Fala pessoal, sejam muito bem-vindos a mais uma aula do nosso módulo Pandas. E nessa aula nós vamos fazer uma introdução para poder entender melhor como é que funciona, o que é o Pandas. Então, para que a gente possa iniciar, vamos entender o que é afinal o pandas. O pandas ele é uma biblioteca Python que é amplamente utilizada em vários setores ali da área de dados, desde a parte de engenharia até partes mais avançadas como ciência de dados. E o pandas vai ser uma biblioteca de código aberto que vai oferecer estruturas de dados que são voltadas principalmente a parte de análise de dados então a gente consegue fazer ali manipulação de dados utilizando pandas e quais são essas principais estruturas de dados que a gente tem dentro do pandas então nós temos duas principais a primeira que a gente vai falar são as séries então o que que são as tem dentro do pandas então nós temos duas principais a primeira que a gente vai falar são as séries então o que que são as séries do pandas vão ser a rede uma única dimensão no formato ali parecido com uma coluna uma coluna de uma tabela então quando a gente tem uma tabela que a gente tem ali linhas e colunas você olhando a coluna é como se ela fosse um array de uma única dimensão. Então você tem ali como se fossem os andares. Em cada andar você consegue armazenar um certo valor. E os valores que a gente consegue armazenar dentro dessa estrutura do Pandas são os valores que a gente já viu na parte introdutória de Python. Valores do tipo int, do tipo float, booleano, a gente consegue armazenar listas, dicionários, qualquer outro tipo de dado dentro dessa série. E nós temos diversas operações para poder manipular os dados dentro das séries. Já os data frames, que também são estruturas que são amplamente utilizadas e são bem mais utilizadas inclusive do que as séries os data frames são estruturas de dados de duas dimensões, ou seja, bidimensionais e elas possuem colunas, várias colunas, uma ou mais colunas e essas colunas podem ser de tipos diferentes e quais tipos? Os mesmos tipos que a gente viu ali no Python uma ou mais colunas. E essas colunas podem ser de tipos diferentes. E quais tipos? Os mesmos tipos que a gente viu ali no Python. Você pode ter colunas do tipo int, do tipo float, do tipo data, do tipo booleano. E essas colunas, ou melhor, os data frames, eles vão ser similares a tabelas de bancos de dados relacionais. Então, quem já conhece bancos de dados, sabe que a gente tem de bancos de dados relacionais. Então quem já conhece bancos de dados sabe que a gente tem os bancos de dados relacionais e eles têm uma estrutura de tabela. Ou para quem não conhece e que conhece planilha de Excel, acredito que todo mundo conheça as planilhas, o DataFrame é bem parecido com uma planilha de Excel, porque você tem uma planilha, linhas e colunas. E o DataFrame ele também vai ser uma estrutura de dados que vai armazenar os dados nesse formato. Eu vou ter colunas que podem ser de tipos diferentes e eu vou ter linhas e dentro dessas linhas que eu vou ter os dados armazenados. E onde é que eu consigo utilizar o Pandas? Quais são as aplicações práticas dessa biblioteca? Então, eu consigo fazer leitura e escrita de dados, então eu consigo, com a biblioteca Pandas, ler dados, por exemplo, de Excel, consigo trazer dados para a biblioteca para trabalhar com esses dados em Python, consigo ler dados de CSV, JSON. Consigo fazer limpeza de dados utilizando pandas. Então, por exemplo, verificar valores que estão ausentes, remover esses valores ausentes ou qualquer outro tipo de limpeza nos dados a gente consegue fazer com pandas. Eu consigo fazer toda a etapa de preparação de dados que a etapa basicamente que que o engenheiro faz limpeza preparação para tornar aqueles dados prontos para análise que é o próximo passo aqui análise exploratória dos dados então o pandas não permite apenas a parte de limpeza. Ele também nos dá ferramentas para fazer uma análise exploratória, usando estatística para poder entender aquele conjunto de dados que a gente está trabalhando. E o Pandas também permite a integração com outras bibliotecas que trabalham aí na área de dados, como NumPy, Matplotlib para verificar algumas plotagens, entre outras bibliotecas também. E como é que eu faço para utilizar o Pandas? Primeiro, se for num computador local, você tem que instalar a biblioteca Python, você instala o gerenciador de pacotes pip e através do pip você consegue instalar o Pandas. Como nós vamos utilizar o Collab, nós simplesmente precisamos fazer um código para importação no google colab que é o import pandas e eu posso utilizar import pandas spd, que é algo que comumente a gente que utiliza o pandas faz criar uma alias ou seja um apelido, e esse apelido é o pd, e toda vez que eu for utilizar um método da biblioteca pandas, um método da biblioteca pandas método uma função basta usar o pd. E eu chamo o meu método chama minha função então dessa maneira que a gente consegue importar o pandas para o Google colab e depois a gente consegue fazer todos os códigos todas as funções bom então agora vamos para a parte prática, entendemos melhor o que é a biblioteca, como ela funciona e agora a gente vai praticar, vamos abrir o nosso Google Colab, vamos importar a biblioteca pandas e vamos aprender quais são os comandos essenciais para depois a gente conseguir se aprofundar na área de dados utilizando o Python. Então é isso pessoal, aguardo vocês na próxima aula.