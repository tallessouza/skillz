WEBVTT Olá pessoal, sejam muito bem-vindos a mais uma aula do nosso módulo de pandas e nessa aula nós vamos falar sobre o comando concat a função concat vai ser muito útil para concatenar objetos do pandas sejam eles séries ou dataframes e a gente consegue concatenar ao longo de algum dos eixos específicos. Ou concatenar por linhas ou concatenar por colunas. Então eu posso concatenar abaixo linha por linha ou ao lado coluna por coluna. Então a gente vai verificar como que a gente utiliza essa função. Então para que a gente possa iniciar a gente vai fazer o nosso import pandas-s-pd e vamos importar a nossa biblioteca e vamos criar nossos data frames, nossos dois data frames. Então nós vamos criar nosso dados1 que vai ser nosso primeiro dicionário para o primeiro data frame. Então, nós vamos colocar um A que vai receber uma lista. A0, A1, A2 e A3, o B que vai receber B0, B1, B2 e B3, o C que vai receber C0, C1, C2 e C3. e por último o D que vai receber o D0 D1 D2 e D3 e nós vamos colocar também um index1 que vai receber 0, 1, 2 e 3. Dados 2 a gente vai colocar O A também Que vai receber A4 A5 A6 e A7. B, C y D. B, C y D. B, B y B. C. e d7 e vamos colocar um índice 2 também 4, 5, 6 e 7 vamos executar vamos dar um print ou melhor, vamos criar o meu df1 e o df2 então o df1 vai ser o pd.dataFrame dados1 index1 e o df2 .dataFrame Dados1 Index1 E o DF2 Vai receber .dataFrame Dados2 Index2 E a gente vai dar um print Vamos executar logo Logo abaixo a gente cria Um print para DF1 e na outra célula um print para DF2. Então aqui estão os meus dois data frames e agora a gente vai fazer a concatenação. Então por padrão o concat faz uma concatenação por linhas, então vamos ver como é que faz uma concatenação por linhas. Então, vamos ver como é que funciona a concatenação por linhas. Então, nós vamos criar uma variável chamada concat__linhas, que vai receber um dataframe, que vai receber o pd.concat. E nós vamos concatenar o nosso df1 com o df2 e vamos dar um print em concat underline linhas então o concat original ele vai concatenar o nosso df1 com o df2 por linhas, é como se colocasse um acima do outro, um abaixo do outro aqui. O DF2 fica abaixo do DF1. Então esse é o concate original, que é o concate padrão. A gente pode também fazer por colunas. Então vamos colocar aqui. Então, vamos colocar aqui. Para que a gente possa fazer por colunas, nós vamos fazer o DF3. Vamos fazer o dados 3, que vai receber a coluna E. Vamos colocar a coluna E e a coluna E vai ter o E0, E1, E2 e E3, vamos deixar só a coluna 0. E o index 3 vai receber 0, 1, 2 e 3. 2 e 3 Nós vamos criar o df3 Que vai receber pd.dataframe Dados 3 E index 3 Vamos executar, tudo ok Dá um print no df3 Então aqui está a nossa coluna Então agora a gente consegue concatenar ok, dar um print no df3 então aqui está a nossa coluna, então agora a gente consegue concatenar para ele adicionar a coluna e também então nós vamos dar criar aqui deixa eu ver o nome que a gente colocou, concat linhas concat underline calls concat underline calls vai receber pdou concat linhas, concat underline calls, concat underline calls vai receber pd.concat, eu quero concatenar df1 com df3 e agora eu vou escolher o axis, que é o eixo 1, e vou dar um print em concat__calls. Então, aqui, dessa maneira, eu tenho o A, B, C e o D, e agora a gente adicionou uma coluna a mais, que é a coluna E do nosso DF3. Então, é dessa maneira aqui que eu utilizo o comando CONCAT. Aguardo vocês na próxima aula.