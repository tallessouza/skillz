WEBVTT Muito bem, nós já finalizamos essa página aqui de criar conta, a página de sign up. A gente já implementou aqui essas funcionalidades, deixa eu tirar aqui um pouco o zoom para você visualizar melhor. Então nós fizemos aqui a validação com o Zod, criamos aqui e utilizamos estados para obter aqui o conteúdo né de cada input aqui ó para o nome para o e-mail para senha e para confirmar senha e quando o usuário ele faz um submit nós chamamos aqui né esse método um submit que por sua vez chama essa função a gente utiliza aqui o evento que a gente consegue recuperar aqui do form event. E esse evento é passado através do próprio formulário. Então o formulário repassa para o onSubmit esse evento aqui. A gente chamou apenas de e, abreviação de event. Eu poderia escrever outro nome. Aqui a gente usa então o método preventDefault para evitar que o formulário recarregue e as informações se percam, a gente ativa o carregando aqui como verdadeiro porque aqui já bloqueia o botão com o método isLoading que a gente está passando aqui em seguida a gente valida os dados, nome, e-mail, password e password confirm, de acordo com o critério que a gente criou aqui no nosso signup scheme. E a gente faz então a requisição para a API, utilizando aqui o método post em barra users e envia os dados. A gente até poderia enviar aqui name, separado, e-mail, mas como já está tudo aqui ó dentro de dele ele já vai devolver todos os dados validados e se algum erro aqui de validação acontecer ele então gera uma exceção que vai levar a gente para parte do cat ou seja ele nem continua executando a próxima linha que seria fazer a requisição aqui caso a gente tem algum erro, a gente exibe no console qual é o erro e a gente verifica se é um erro do Zod, ou seja, se a instância do erro é de Zod ou do Axios. Se é do Zod, é um erro de validação aqui no nosso próprio front-end, a gente exibe a mensagem. Caso seja do Axios, significa que esse erro está vindo lá da API e aí a gente recupera a mensagem que a API retorna aqui de erro. Se não encaixa em nenhum desses critérios, ou seja, não é um erro do Zod, não é um erro do Axios, não é a API que está devolvendo o erro, a gente exibe um erro aqui genérico de não foi possível cadastrar. Por isso que a gente usa o return aqui, porque quando ele cai em uma dessas condições ele já para por aqui se deu tudo certo na nossa validação ou aqui também na requisição a gente exibe aqui um uma mensagem de confirmação eu coloquei ela já dentro de um if porque ela já retorna um valor booleano a verdadeiro falso verdadeiro caso a pessoa clique em ok e a gente navega pessoa para página anterior então esse daqui é um overview do que a gente fez na de sign up certo então só para gente reforçar aqui o como