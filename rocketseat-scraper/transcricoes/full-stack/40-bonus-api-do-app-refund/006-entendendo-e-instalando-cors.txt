WEBVTT Nessa aula eu vou explicar para você o que é CORS, porque a gente vai utilizar isso na nossa aplicação. A gente vai adicionar CORS na nossa API. Deixa eu te explicar o que é, tá? Essa palavrinha CORS, ela vem de Cross-Org Resource Sharing. Ou seja, é uma funcionalidade de segurança que permite que uma página web acesse recursos de outra origem. Deixa eu te explicar melhor como que isso funciona. Se prepara que eu vou trocar de slide aqui e eu vou ter que ativar agora o modo liked, tá? Vou deixar o dark um pouco de lado, olha só. Então, a gente tem um servidor A e um servidor B. Então, a gente tem um servidor A e um servidor B. Então, a gente tem dois servidores, certo? Em um servidor, a gente tem a nossa aplicação front-end. Então, o front-end vai estar em um servidor hospedado ali para ficar disponível para ser acessado. E a gente tem o nosso back-end que vai ser hospedado em um servidor diferente. Então, percebe que eles não estão hospedados no mesmo servidor, por isso eles são de origens diferentes. Seria da mesma origem se tanto o back end quanto a API estivesse no mesmo servidor, mas eles não estão. Eles não estão no mesmo servidor e tampouco rodando estão no mesmo servidor então pouco rodando na mesma porta por exemplo nesse caso veja que dentro do servidor a a gente tem por exemplo front-end ali rodando e digamos que o front-end ele precisa acessar uma pasta ali dentro do próprio servidor utilizando ali a mesma porta o mesmo canal canal de comunicação. Então veja que ali ele está se comunicando com a sua mesma origem, ele está dentro ali do seu próprio ambiente, se comunicando e usando recursos ali dentro do mesmo lugar. Isso é uma mesma origem. É por isso que a gente precisa aplicar cores no nosso projeto aqui, back-end na nossa API, projeto aqui, back-end na nossa API, para habilitar, deixar habilitado que um projeto front-end que um front-end que esteja em outra origem, em outro servidor, consiga acessar os recursos da nossa API e consumir esses recursos, então o CORS habilita essa comunicação esse cross-orange, ou seja, essa comunicação cruzada, digamos assim, de forma segura. Então é isso. Se a gente não coloca cores na nossa API, quando o front-end tentar acessar o nosso projeto, a nossa API, o nosso back-end, ele vai ser barrado. Ele não vai conseguir acessar a nossa pay e ele vai receber um bloco de cores dizendo que ele não é não tem permissão para acessar a origem por isso que adicionando cores na nossa pay a gente está autorizando os front-end, uma aplicação front-end a consumir, acessar e consumir os recursos da nossa API. Aqui tem em tópicos como funciona o CORS a título de conhecimento aqui para você. Então olha só, o CORS ele é ativado quando a página tenta acessar recursos de uma origem diferente daquela que a serviu, que é exatamente isso que eu acabei de mostrar para você na imagem anterior. Ele adiciona no cabeçalho HTTP dizendo pro navegador que ele pode acessar os dados solicitados e aí isso inclui o SSControl Alloy Orange inclusive esse é o bloco que a gente toma quando por exemplo a API não tem um CORS e também nesse cabeçalho de credenciais, entre outros. E aí o servidor verifica se o cabeçalho da origem responde com esses dados que foram solicitados e aí o navegador consegue visualizar os cabeçalhos dessa solicitação de controle de acesso e também desses dados que estão compartilhados e aí retorna para a aplicação cliente, que é o front-end ali que pode consumir esses dados tá então esse é o conceito do funcionamento de cordas você vai ver que é muito simples de habilitar isso no node