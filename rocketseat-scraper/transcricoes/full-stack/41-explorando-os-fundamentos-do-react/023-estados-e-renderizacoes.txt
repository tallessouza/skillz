WEBVTT Nessa aula vamos falar sobre estados e renderizações. Vamos compreender os conceitos, o mecanismo aqui de funcionamento pra gente colocar em prática logo em seguida, tá? Muito bem, o render, tá? O que é o render? O render nada mais é do que a renderização, é desenhar o componente literalmente na tela pro usuário ver. E antes dos componentes eles serem exibidos, antes desses componentes serem desenhados, exibidos ali na tela, eles são renderizados pelo React. Então o React monta eles para exibir eles na tela. E a gente vai entender as etapas desse processo e do comportamento do React para renderizar um componente na tela e aqui eu vou usar uma ilustração para simplificar essa explicação para ela ficar o mais didática possível tá então imagine que os seus componentes eles são cozinheiros tá então os componentes são cozinheiros que estão lá na cozinha montando os pratos que o restaurante oferece a partir dos ingredientes, tá? Então a gente tem ali o componente, o componente tem as suas propriedades as propriedades é os ingredientes que esse componente usa pra poder então ser composto pra ele ser montado tá bom então pega essa referência vai ser importante aqui para nossa explicação nesse cenário agora o react ele é o garçom tá os componentes são os cozinheiros os os componentes têm propriedades Alex que são ali os ingredientes que são utilizados ali para montar os pratos e o react é o garçom aqui na nossa ilustração e o garçom é quem anota os pedidos ali do cliente né o cliente faz o pedido para o garçom o garçom pega esse pedido e também o garçom é responsável por entregar esse esse pedido para o cliente assim que ele tá pronto então o garçom anota o pedido né pega o pedido ali do cliente entende ele beleza quando o pedido tá pronto ele volta ali para devolver o pedido para o cliente então são três etapas a etapa de acionar renderizar e a etapa de comprometer-se ou seja de voltar ali com o pedido pronto para o cliente. Vamos dar uma olhada em cada uma dessas etapas. Primeiro, a etapa de acionar. Há duas razões para que um componente seja renderizado. A primeira razão para um componente ser renderizado é quando é a renderização inicial do componente, ou seja, esse componente está sendo renderizado pela primeira vez na tela e essa primeira vez é toda vez que a sua tela abre pela primeira vez então quando seu aplicativo ele é por exemplo iniciado a renderização inicial ela é acionada esse é o primeiro motivo para um componente ser renderizado o segundo motivo é quando o estado do componente muda. Se o estado do componente muda, isso faz com que esse componente vá ali para fila para que tenha uma renderização automática. Você pode imaginar o cliente do restaurante pedindo mais coisas, por exemplo. Então, essas são as duas razões para um componente ser renderizado. Para fixar quando é a renderização inicial ou quando o estado de um componente muda. Por isso que é importante aqui essa compreensão de um estado. O estado não é uma variável comum, porque o estado tem esse poder de gerar, de ser um gatilho para uma atualização, uma renderização do componente. Olha só essa etapa aqui de re-renderização, ou seja, quando o estado é atualizado. Imagina só que o garçom chega e entrega para o cliente, está aqui o seu pedido, que foi esse componente de card. Porém, o cliente diz o seguinte, opa, não é esse daqui, eu quero incrementar alguma coisa aqui. Talvez eu queira mudar aqui um ingrediente, talvez eu queira colocar um ingrediente adicional, enfim. Então, o React, que é o garçom, ele vai pegar esse gatilho de opa, precisamos mudar esse componente aqui, esse pedido. E em seguida ele vai então renderizar novamente, ou seja, re-renderizar. Vai renderizar novamente o nosso componente. Seguindo aqui agora para a etapa 2, que o React renderiza os nossos componentes. Então depois de acionar uma renderização, o React chama os componentes para descobrir o que tem que ser exibido na tela. Esse processo de rendering, ou seja, de renderização, é o React chamando os componentes. E aí na renderização inicial, o React vai chamar o componente raiz mesmo. Para renderizações que foram com gatilhos de uma atualização de estado, o React vai chamar o componente de função cuja atualização de estado acionou a renderização, até para ele conseguir comparar o que mudou do componente. Olha só como funciona isso. Esse processo é recursivo. O componente também pode disparar um gatilho para renderizar alguma coisa em seguida e assim por diante. E aí o processo continua até que não tenha mais componentes aninhados e o React saiba exatamente o que tem que ser exibido na tela. Porque imagina só, a gente pode ter um componente dentro do outro e como o React sabe qual parte desse componente mudou, ele vai fazer esse processo de forma recursiva até ele chegar no detalhe ali que precisa ser renderizado. E claro, ele vai fazer isso de forma super performática, tá? Até ele atualizar e renderizar a tela. Por isso que tem a DOM real, que eu já mostrei por aqui, e a DOM virtual pra ele fazer isso em memória e por isso que isso é tão rápido, tá? Vamos agora olhar pra a etapa 3 que é justamente a confirmação das alterações na DOM. Após a renderização chamar os componentes, o React vai modificar a DOM, tá? Que é a Document Object Model, ou seja, a estrutura de fato ali da nossa página, da nossa aplicação. E aí para a renderização inicial, o React usa a API DOM para colocar todos os nós no DOM criado ali na tela. Agora para a re-renderização, que é quando tem uma atualização de estado do componente, o React vai aplicar as operações mínimas necessárias que são calculadas durante essa renderização. mínimas necessárias que são calculadas durante essa renderização e aí para fazer o dom corresponder a saída de renderização mais recente comparando com a virtual com a real tá o react por isso ele só altera os nós e aí cada nó é entenda como um componente ali ele só vai renderizar os nós ali se de fato houver uma diferença entre as renderizações. É por isso que tem duas Dons, uma real e uma virtual, para ele conseguir comparar e saber exatamente o que mudou. Então, recapitulando, qualquer atualização de tela em um aplicativo React acontece em três etapas. Então, eu expliquei tudo isso de forma detalhada aqui e talvez não tenha ficado tudo super claro para você. Talvez você tenha achado um pouco difícil, mas é normal, não é um conceito tão simples assim. Mas agora eu vou resumir para você. É isso que você precisa saber. Qualquer atualização de tela de um componente acontece em três etapas. Tem um gatilho que vai acionar isso e pode ser dois quando é um componente que está sendo renderizado pela primeira vez, então naturalmente ele já vai ser renderizado e o segundo motivo é quando o estado do componente muda. Então dentro de um componente a gente pode ter estado, se o estado muda ele renderiza esse componente. Então são esses dois motivos para que um componente seja renderizado. Con ele renderiza esse componente não são esses dois motivos para que um componente seja renderizado consequentemente esse componente vai ser renderizado que tá ali na etapa dois por um desses dois motivos que eu mencionei e ele compromete-se a fazer o que a de fato colocar isso na Dom ou seja exibir isso na tela então veja que até na etapa 3 ali, até na etapa 2 ele vai comparar com a virtual. E aí na 3, enfim, ele se compromete em exibir isso na tela. Então o React não toca no DOM se o resultado da renderização for o mesmo da última vez. Por isso que ele passa por essas etapas, compara antes, porque se não tem nada pra mudar, ele nem vai tocar nela. E por isso a gente tem uma aplicação tão performática. Então isso é estados e renderizações e como funciona no React.