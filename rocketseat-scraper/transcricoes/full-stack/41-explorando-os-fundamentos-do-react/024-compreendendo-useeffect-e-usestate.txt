WEBVTT Agora vamos falar então do UseEffect, um hook muito utilizado e a gente vai utilizar bastante ele. Ele é utilizado no ciclo de vida do componente, ou seja, quando o componente é renderizado, podemos colocar alguma lógica ali para acontecer, e também quando a gente tem algum efeito colateral que nós queremos trabalhar. Por exemplo, digamos que a gente tem ali um UseEffect, que toda vez que o componente é renderizado, em seguida ele tem uma lógica que lista os produtos. Mas digamos que a gente tenha um estado, digamos que nós temos um estado ali, que ele armazena a categoria selecionada. E aí eu posso conectar o estado de categoria com esse useEffect. E toda vez que eu mudar de categoria, digamos que por padrão ele filtra na categoria tênis, e se eu mudo, se ele é só na categoria, uma categoria diferente, vou lá e mudo para, por exemplo, camisetas. Se esse estado muda, eu posso disparar um efeito colateral nesse useEffect para que ele recarregue novamente essa lista usando esse filtro agora que está atualizado de categoria então a gente consegue utilizar tanto e os efeitos quanto e os state inclusive trabalhando juntos e a gente vai ver a isso acontecendo tá bom e os efeitos ele precisa estar dentro de um componente isso é importante você seguir tá e por que que um useEffect tem que estar dentro de um componente? É simples aqui a resposta. Para que o useEffect consiga acessar o estado do componente, os estados ali que você tem dentro de um componente, como por exemplo, para até utilizar ele ali como um efeito colateral. Então, o useEffect precisa estar dentro de um componente para que a gente tenha acesso aos estados do componente tá bom aqui tem um exemplo olha só temos aqui ó um e os efeito ele tem esse padrão aqui ó ele é uma função tá vendo e dentro dele a gente tem uma era um function e aqui a gente tem o corpo do e os efeitos aqui dentro a gente coloca lógica que quer. Por exemplo, aqui tem uma chamada a uma função search. Aqui faltou um b, é um search by name, seria, que eu esqueci de pôr um b. Mas a gente tem aqui, está vendo? Vírgula. E temos aqui entre colchetes, a gente pode colocar os estados de dependência. Se você não coloca nenhuma dependência o useEffect vai ser executado uma vez só se você coloca um estado como dependência esse useEffect vai ser executado uma vez quando o componente é renderizado e toda vez que esse estado mudar então se eu mudar aqui o estado de name, esse useEffect vai ser disparado de novo uma coisa muito legal aqui de você saber é que esse useEffect vai ser disparado de novo, tá? Uma coisa muito legal aqui de você saber é que no useEffect a gente não pode colocar um async aqui antes para usar o await aqui dentro, é isso que você tem que fazer dentro da própria função. E veja que aqui a gente também tem o outro hook também que a gente vai ver na prática que é o useState. O useState, ele é um hook para a gente trabalhar com estado. E nada mais é do que, olha só, uma função também, useState. Aqui a gente consegue colocar um valor inicial, veja que tá um texto vazio. E aqui ele retorna um array aqui, em que na primeira posição a gente tem o estado de fato, que a gente pode acessar, é onde ele vai guardar esse conteúdo e a função que atualiza o estado. O estado segue o princípio da imutabilidade, ele não deixa a gente alterar direto o estado. Então o estado fica guardadinho, separado, e se eu quero atualizar esse estado, eu preciso usar a função setName, e essa função sim tem o poder de atualizar esse estado. Isso porque internamente o React faz todas aquelas etapas que eu expliquei pra você na parte de renderização. Mas claro, a gente vai ver tudo isso na prática, tá?