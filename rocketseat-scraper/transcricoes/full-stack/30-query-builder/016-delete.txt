WEBVTT Já temos os métodos de inserir, de atualizar e de selecionar, falta o de deletar. Então vamos criar aqui uma rota, pra gente utilizar aqui o delete. A gente pode utilizar aqui como endereço aqui da nossa rota, barra curse, barra id, passando aqui o id como um parâmetro né. Vamos também utilizar aqui o async e aqui a gente pode passar o corpo aqui da nossa rota. Aqui eu posso retornar também um response.json. Muito bem, agora podemos então recuperar aqui o id, que a gente vai pegar ali do parâmetro. Podemos utilizar o await pra pegar aqui com o kinect, a gente passa aqui curse, aqui pra nossa tabela de cursos, e aqui a gente vai utilizar o delete, que podemos passar aqui também sem o id, porque o id a gente passa aqui com o ar. E aí a gente pode passar aqui o id. Pronto, dessa maneira aqui. Então estamos deletando aqui, dentro da tabela de cursos, o registro onde o id é igual a esse aqui. Posso salvar, vamos voltar aqui agora pro Insomnia, vamos criar aqui uma nova HTTP request. Podemos substituir aqui pra, vamos chamar aqui de remove. Deixa eu só tirar esse colchetes que eu coloquei a mais, pronto. Agora vamos mudar aqui o método pra delete. Podemos aqui pegar, eu vou até pegar aqui na nossa lista, deixa eu ver qual o curso. Eu vou deletar por exemplo esse de CSS. Então posso até copiar aqui esse endereço e adicionar aqui, local roxo, dois pontos, três mil e trezentos e três, barra curse, barra o id, que é o dois. Don't send, olha só. Ok, agora se eu venho na lista e don't send pra visualizar, não tem mais aquele registro. Se eu confirmo aqui no banco, executo esse select, olha só, também não tem mais esse registro. Ou seja, a gente implementou agora o método aqui de deletar um registro dentro do banco de dados.