WEBVTT Nessa aula, vamos criar uma nova tabela. Vamos fechar esse arquivo, podemos voltar aqui para o nosso skin.prisma, recolha aqui do lado para a gente ganhar espaço, e a gente já fez aqui a tabela de usuário, vamos criar aqui um novo modelo agora para criar a nossa tabela para as questões e abrimos e fechamos chaves para aqui dentro definir o conteúdo. Eu vou subir aqui para a gente focar na nossa tabela de questões agora e vamos criar uma coluna id, o tipo vai ser uma string, a gente define como chave primária, que o valor padrão vai ser o id gerado automaticamente. Uma questão também vai ter um título que vai ser uma string, vai ter um conteúdo que também vai ser uma string, e eu vou mostrar para você agora, olha só que legal e como é simples a gente trabalhar com as colunas createdAt e updatedAt com Prisma. No caso aqui, eu não posso criar dessa forma o nome aqui dessa propriedade dentro do modelo, a gente tem que utilizar o padrão aqui assim, porque senão Prisma vai reclamar se a gente não utilizar esse padrão. Descrever tudo junto, e aí a gente pode diferenciar, por exemplo, começando com uma letra maiúscula aqui no caso do h3, então primeira minúscula, que é o padrão aqui, camel case, então se a gente não seguir esse padrão, o Prisma vai reclamar. Então, para o modelo, a gente segue esse padrão, mas, olha só, a gente pode passar então o tipo dateTime, a gente pode passar aqui um valor default, que você já sabe, e a gente pode utilizar aqui o método de now para pegar data e hora atual. E aqui por fim, a gente coloca um arroba map, e aqui sim a gente renomeia para createdAt, então a forma como vai ser salvo dentro da tabela vai ser como a gente está mapeando aqui, com o createdUnderlineAt, então é assim que vai ficar dentro da tabela no banco, mas para usar aqui o modelo e manipular dentro da nossa aplicação, vai ser createdAt. Bom, olha só a simplicidade para a gente utilizar o updatedAt, a gente pode passar também um dateTime, podemos passar aqui agora simplesmente um arroba, e a gente passa aqui o método updatedAt, e ele identifica automaticamente, quando a gente modifica o registro, ele já atualiza para a gente a data e a hora atual, então fica bem legal também, então automaticamente ele já identifica, opa, o registro mudou, então ele atualiza a data de atualização, e a gente pode mapear também aqui, arroba map, e a gente pode mapear para updatedUnderlineAt, e aqui, por fim, a gente pode também adicionar um userId, colocando como uma string, e aí a gente pode também mapear para userUnderlineId, lembrando que aqui a gente usa esse padrão no modelo, mas aqui a gente usa o map para dizer como vai ficar dentro do banco de fato. Por fim, a gente define como é o nome da tabela, que é questions, e aí eu coloco no plural. Muito bem, podemos executar agora aqui, deixa eu abrir aqui um novo terminal, porque aqui está rodando o servidor, aqui está o Prisma Studio, então eu clico aqui para abrir um novo terminal, e a gente pode executar aqui npx prisma migrate dev enter, vai perguntar o nome aqui da nossa migration, eu vou colocar um create, table, questions, enter, e pronto, gerou, a gente pode vir aqui, fecha o user, e veja, aqui só está o usuário, mas se eu recarrego, recarreguei, está vendo, ele ainda não está mostrando aqui, ele está mostrando os usuários, vamos ver aqui um porquê, create table, deixa eu fazer o seguinte, eu vou parar aqui o Prisma Studio, opa, parei o servidor, npm run dev, deixa eu parar o Prisma aqui, e executar de novo, npx prisma studio, pronto, agora sim, então foi só parar e executar o Prisma Studio novamente, aí eu fecho esse, aqui já está a nossa tabela de questions, está vendo, o id, o título, o conteúdo, quando criado e quando atualizado, e o id do usuário, muito bom né, então a gente já tem aqui a nossa nova tabela.