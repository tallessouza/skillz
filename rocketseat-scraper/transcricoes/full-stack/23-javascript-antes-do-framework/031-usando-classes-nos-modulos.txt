WEBVTT A gente usou bastante funções aqui no nosso módulo, estou até aqui com o código de exemplo da aula anterior, que a gente vem utilizando aqui, mas a gente também consegue utilizar classes dentro dos nossos módulos. Então, vamos refatorar aqui, e se você não sabe o que é essa palavra refatorar, nada mais é do que modificar o nosso código. Então, vamos lá. Eu vou vir aqui no calque e eu vou fazer o seguinte, no início aqui, eu vou fazer o seguinte, bem aqui no início, export class, eu vou criar uma classe, e já exportando essa classe, eu vou chamá-la de calque, é muito comum usar essa convenção de sempre começar nomes de classe com letra maiúscula, abro e fecho chaves, então temos aqui uma classe chamada calque, e eu quero agora que a classe tenha os métodos, então vou pegar todos eles aqui, ctrl x, e vou adicionar eles aqui, quando o método está dentro da classe ela não usa function, fica assim, então tirei o function, salvei, e aí a gente não precisa dessa exportação aqui, ele vai dar esse erro, porque não é mais só importar dentro ali do próprio arquivo como a gente estava fazendo, a gente já vai ajustar, mas vamos só para reforçar aqui, então a gente criou uma classe chamada calque, e está exportando a nossa classe, e dentro da nossa classe, a gente tem o método de sum, e tem o de multiply, aqui a gente não coloca function, porque quando a gente coloca o método dentro da classe, a própria sintaxe já diz para a classe que isso é um método, uma função, agora como que a gente usa aqui, a nossa classe aqui, que está dentro do comum módulo, então agora eu vou ao invés de importar dessa forma tudo assim, eu vou só importar aqui a própria classe calque, e agora eu preciso instanciar ela, então como ela é uma classe, então eu vou usar calque igual a new, e aí a gente usa o calque, então a gente fez uma instância, e agora dentro de calque eu tenho o ponto sum, e dentro de calque eu tenho aqui o ponto multiply, então sempre a gente usa o ponto para acessar o método, que é a função que está dentro da classe, então a gente também consegue utilizar classes para também trabalhar com essa especificação de módulos, e separar o nosso código em módulos, separado em um arquivo, utilizando classe, e o legal, eu gosto muito também dessa sintaxe, porque é uma forma também de você ter dentro da classe todos os métodos também que ela compartilha, e aí vamos supor que aqui a gente tivesse também um name, eu vou colocar por exemplo como Rodrigo aqui, é uma propriedade, então chamando de name, se eu vier aqui e também colocar aqui para exibir calque.name, vai aparecer meu nome, então tudo que você colocar dentro da classe você vai conseguir utilizar também separado como módulo, eu vou só tirar isso aqui, vou deixar só os métodos mesmo, que aí fica mais interessante o nosso exemplo, então essa é uma forma também de você trabalhar com os seus módulos, seja usando ali mais focado em funções mesmo, exportando elas e tudo mais como a gente já viu anteriormente, ou criando uma classe e colocando os métodos e tudo dentro da sua classe para também usar ela como um módulo.