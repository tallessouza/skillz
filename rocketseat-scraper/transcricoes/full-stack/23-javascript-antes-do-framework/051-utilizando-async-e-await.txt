WEBVTT Você já aprendeu a utilizar o FAT com o DEN para aguardar a promise e obter os dados no retorno da requisição. Só que também dá para utilizar a SYNCOWAIT ao invés de utilizar o DEN, e aí vai muito da sua escolha. Nessa aula eu vou mostrar então como eu poderia fazer esse mesmo exemplo, essa mesma ideia que é buscar os produtos, só que ao invés de utilizar o DEN eu vou utilizar o SYNCOWAIT. Só para não perder esse código e deixar ele aqui, eu vou comentar essas três linhas aqui, vou comentar assim mesmo, e aqui vou até deixar aqui, que aqui a gente está utilizando o FAT com o DEN. E agora eu quero mostrar como utilizar aqui o FAT com o SYNCOWAIT. E aí eu vou criar uma função, e aí como a gente vai utilizar a SYNCOWAIT, a função tem que ser assíncrona, por isso eu vou usar aqui o ASYNC, FUNCTION, eu vou chamar aqui de FAT PRODUCT, então a gente vai ter um método aqui, uma função para buscar os produtos. Posso até colocar aqui PRODUCTS, assim no plural, porque são vários produtos. E aí eu vou criar o RESPONSE, e basicamente agora a gente vai reproduzir essas etapas aqui usando a SYNCOWAIT. Então aqui eu vou usar o RESPONSE igual, vou usar o AWAIT para aguardar então a PROMISE ser resolvida, e aí eu vou fazer um FAT no mesmo endereço ali, então vou fazer aqui o FAT para a nossa API, então nesse endereço. O meu VS Code quebra a linha aqui, por isso que ele está desse jeito, mas se eu diminuir o zoom aqui para você ver tudo numa linha, vai estar assim. Muito bem, aqui a gente já tem a resposta, a próxima etapa é pegar os dados aqui do JSON, utilizando esse método, então CONST DEIRA, como ele também é assíncrono, então a gente usa o AWAIT também, porque senão a gente vai ter uma PROMISE, se você passar o mouse ele já mostra, tá vendo, vai ser uma PROMISE. E aí por fim a gente pode usar o CONSOLE.LOG para exibir os dados, se eu salvar aqui nada vai acontecer, porque a gente precisa chamar essa função, então se eu chamar aqui o FAT Products e salvar, olha só, mesmo resultado, estou aqui usando o ASSYNC AWAIT, e aí vai depender muito do cenário, vai depender do seu desejo de usar um ou outro, particularmente eu uso mais esse, eu uso mais essa estratégia ASSYNC AWAIT para poder fazer as requisições, mas também tem momentos que a gente vai usar essa forma, por exemplo, dentro de um UseEffect da vida, que é um hook que você vai aprender quando você vai estudar React, a gente não pode colocar ASSYNC no UseEffect, aí a gente pode usar dentro dele essa estratégia se a gente não quer criar uma nova função, então sempre que a gente não quer criar uma função nova, a gente pode usar essa estratégia. Agora, quer usar uma função e dentro da função aguardar alguma etapa que faz uma requisição para uma API, como essa daqui, aí faz mais sentido utilizar o ASSYNC AWAIT, nada impede também de você utilizar o FAT com o DEM dentro de uma função, só que geralmente quando a gente quer fazer as etapas mais amarradinhas, digamos assim, a gente acaba utilizando essa estratégia, mas como eu disse, vai depender muito, e conforme você vai desenvolvendo e codando, você vai sentindo essa necessidade, sabe, tipo, agora eu quero que na próxima etapa eu tenha que esperar primeiro essa resposta para depois utilizar isso, então aí você pode ir aqui com o ASSYNC AWAIT, você vai ver que até na maioria das vezes a gente vai usar o ASSYNC AWAIT, mas agora o importante é isso, você sabe fazer das duas maneiras.