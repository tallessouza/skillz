WEBVTT Assim como nós temos o .gitignore, que é o arquivo onde a gente coloca aqui a lista de pastas e arquivos para o Git ignorar, por exemplo, como tem aqui a pasta node-underline-modules, que como ela é facilmente recriada, então a gente coloca aqui para o Git ignorar ela, ou seja, na hora de enviar para o GitHub essa pasta não vai, a gente também tem o arquivo .docker-ignore para definir o que a gente também quer que seja ignorado na hora de levar os arquivos para dentro do nosso docker, do nosso container. Olha só, vamos criar então esse arquivo, aqui na raiz do nosso projeto eu vou criar um novo arquivo chamado .docker-ignore-enter, então .docker-ignore para definir aqui então o que a gente quer que seja ignorado, por exemplo, a gente pode ignorar a pasta node-underline-modules, a gente pode ignorar a pasta dist, já que dentro do nosso próprio docker file aqui a gente já definiu aqui um comando para gerar essa pasta lá dentro da nossa imagem, do nosso container, temos aqui o docker file, inclusive a gente coloca aqui, igualzinho está aqui, então tem que ser igualzinho, então vou colocar inclusive aqui com D maiúsculo, o .git, eu também vou pedir para ignorar, o .docker-ignore, posso pegar ele aqui também, o próprio docker-ignore, posso também passar aqui, então vou passar a pasta node-underline-modules, a pasta dist, o próprio docker file .git, o .docker-ignore e eu também vou passar o próprio aqui .git-ignore, então esses vão ser os arquivos que eu quero que seja ignorado, ou seja, que não vá ali para dentro do nosso container, por exemplo, então nessa aula você também aprendeu a utilizar o docker-ignore.