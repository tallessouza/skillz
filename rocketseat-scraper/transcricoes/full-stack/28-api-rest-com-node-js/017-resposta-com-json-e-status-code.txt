WEBVTT A gente está devolvendo aqui a nossa resposta das requisições com o send, e aí ele devolve aqui meio como um texto, até se a gente olha aqui no header, ele está devolvendo como um text-html, mas a gente consegue também devolver o conteúdo, o content-type, utilizando o próprio JSON como formato, e lembra que quando a gente foi desenvolver a nossa aplicação utilizando o Node puro, a gente sempre tinha que fazer uma conversão, pegar ali e devolver a resposta utilizando um JSON.streamify para devolver como um texto, e depois passando o content-type, que a gente definia no próprio middleware, para devolver então a resposta no formato JSON, e remontar aqui na verdade. A resposta a gente devolvia como texto, mas como a gente passava o content-type com JSON, e aí a gente via o resultado como um JSON. Aqui a gente consegue simplesmente, no nosso código, no lugar de usar o send, vou até deixar aqui como comentário, a gente pode pegar um response e utilizar simplesmente JSON para devolver essa resposta, e aqui eu posso repassar o próprio nome e o preço. Se a gente volta aqui para o Insomnia e executa, veja que agora a gente tem aqui a visualização do mesmo objeto, porque basicamente a gente está pegando o mesmo objeto e devolvendo aqui, e se a gente vem aqui no header, veja que agora o content-type mudou, agora o content-type é um application barra JSON, tá vendo? Então bem legal, a gente consegue, com muita simplicidade, devolver essa resposta no formato JSON. Outra coisa também que é bem tranquila aqui, a gente pode também adicionar um status code, por exemplo, ponto, status, simplesmente assim, antes de dizer como a gente quer fazer o retorno, aqui a gente pode colocar o status, por exemplo, 201, vou salvar, se eu venho para cá e executo, por enquanto ele está com aquele genérico 200 OK, se eu dou um send, veja que agora mudou, a gente tem ali o 201, que é o status code de criado, que é o que a gente passou aqui agora. Então, olha só a facilidade que a gente tem também para definir o status code com o Express.