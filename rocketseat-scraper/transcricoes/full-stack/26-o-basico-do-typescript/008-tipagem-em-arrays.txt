WEBVTT Nessa aula eu vou mostrar pra você como declarar tipo para suas listas, para os seus arrays. Vamos criar aqui uma nova variável que eu vou chamar de names, e aqui essa variável names eu posso deixar explícito qual é o tipo, então eu posso criar aqui como uma lista de string, ou seja, uma lista de textos. Então perceba que o que muda é que a gente adiciona aqui o colchetes, que é o símbolo ali, a representação de uma lista, de um array aqui no Javascript. Então quando eu tento colocar aqui, por exemplo, posso colocar agora atribuir igual, e aí eu posso criar aqui uma lista e passar, por exemplo, Rodrigo, posso colocar aqui Mike e assim por diante. Então eu tenho aqui uma lista de nomes. Eu posso também criar uma lista de números, por exemplo, numbers, e vou deixar explícito aqui que numbers é uma lista de números, então eu coloco aqui numbers seguido de colchetes, e aqui eu posso adicionar agora números, por exemplo, 1, 2, 3, 4, 5, e assim por diante. Então agora eu tenho uma lista de números. Também existe uma outra maneira de chamar. Então a gente chama aqui de tipagem explícita, ou seja, a gente está deixando claro aqui as regras do jogo, dizendo qual é o conteúdo aqui, qual é o tipo de conteúdo que a nossa lista aceita, é uma lista de texto, é uma lista de números, mas tem um outro nome. É muito comum também você se referir à tipagem explícita como anotação de tipo, então é bem comum, ou type annotation. Então é bem comum, são formas diferentes de se referir à mesma coisa, isso daqui pouco importa, o mais importante é você saber o uso na prática, mas são nomes aí se referindo à mesma coisa. Então aqui a gente deixou explícito, fez a anotação do tipo, tanto que se eu tentar adicionar aqui um nome, veja que ele não vai aceitar, ele vai dizer que essa lista aqui ela aceita, aqui só número, eu estou tentando adicionar um texto e ele vai dizer que string não é aceito aqui, o valor aceito é número, e a mesma coisa se eu tentar adicionar aqui um número, como por exemplo 8, aqui é a mesma coisa, ele vai falar assim, olha, aqui não vai aceitar número porque essa lista é uma lista de string, então para você saber que a gente consegue definir o tipo também para os nossos arrays. Eu vou tirar aqui, eu vou apagar para ficar certinho, não ficar com erro, e a gente também pode utilizar a própria inferência de tipo. E aqui a gente pode, por exemplo, criar uma outra lista, digamos aqui que a gente tem uma lista de produtos, e aqui eu não vou definir, está vendo, que é uma lista, um array de string, eu vou deixar o próprio TypeScript inferir para mim baseado no valor que eu vou atribuir. Eu vou atribuir aqui, por exemplo, um produto X, um produto Y e um produto Z, por exemplo. Veja que quando eu passo o mouse em cima de Product, ele já deduziu que é uma lista de texto, então ele já colocou a tipagem aqui como sendo um array de string. Dessa forma a gente faz então a tipagem das nossas listas, tá bom? Então perceba que aqui o que muda é o valor que você quer guardar dentro da lista, e aí o que representa que é um array e não uma variável comum é o quotients. E aí você pode deixar explícito usando a anotação de tipo, o type annotation, ou utilizar a própria inferência de tipo do TypeScript.