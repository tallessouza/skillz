WEBVTT Nessa aula eu vou explicar pra você a diferença entre os valores undefined e NULL, ambos são valores primitivos, tá bom? Vamos começar falando aqui do undefined, o undefined ele é um valor primitivo utilizado quando uma variável não teve um valor atribuído, por exemplo, digamos que a gente tem uma variável chamada value, e eu vou criar essa variável definindo aqui que o tipo dela é um number, olha só se eu executar um console.log pra exibir o conteúdo de value, tá vendo que quando eu passo o mouse aqui, ele vai dizer que o valor de value não foi definido ainda, então se eu executar, veja só, quando eu clicar aqui pra executar, eu posso abrir aqui e vindo aqui na opção de logs, tá vendo que aqui ele vai mostrar que o valor é undefined, inclusive aqui em errors ele mostra, dizendo o mesmo erro aqui basicamente que ele tá me mostrando, dizendo que a variável foi declarada mas ela não tem nenhum valor atribuído, eu posso na hora que eu declaro a variável, definir o valor, então se eu colocar aqui por exemplo 12, veja que agora se eu executo, vou abrir aqui o log novamente, se eu executo, veja que agora o próximo resultado foi 12, ou eu posso exibir aqui também na sequência, ou melhor dizendo, definir depois, uma vez que é uma variável, então eu posso definir um valor após, depois de ter declarado ela, aqui executando a gente vai ter o mesmo resultado, lembrando, eu posso clicar aqui pra recolher esse painel e expandir, agora a gente tá utilizando aqui o recurso de visualizar a saída do console.log, os logs, então por isso eu vou expandir aqui esse painel, então é pra isso que serve o undefined, ele é um valor primitivo utilizado quando a variável não tem um valor atribuído, o outro cenário também do undefined é quando a gente tem um objeto e a propriedade não existe dentro do objeto, então por exemplo, deixa eu deixar tudo juntinho aqui, aqui é o exemplo com variável, digamos que a gente tem aqui um objeto, que eu vou criar uma variável, vou chamar aqui de user, e digamos que user tem a propriedade name, vou definir aqui o meu nome, agora perceba que se eu executar console.log e tentar usar user.email, o próprio TypeScript já vai reclamar aqui pra mim, opa, email não existe, se eu passo o mouse aqui, veja que o email não existe aqui dentro de user, aqui eu não defini o tipo, não fiz a notação, mas ele inferiu pra mim, baseado no conteúdo que eu declarei, que user tem, é um objeto mas que tem uma propriedade só, chamada name, e o tipo é string, bom, embora aqui o próprio TypeScript já tá me dizendo que tem alguma coisa errada aqui, no caso o email não existe, se eu passo o mouse aqui ele me diz que não existe essa propriedade ali, ele não vai impedir de executar o código, se eu executo, veja só, executei, ele vai executar o código e perceba que aqui a gente tem o undefined como resultado, então esses são os cenários aí que você pode encontrar o undefined, agora eu vou comentar essa linha aqui agora, só pra não ficar aparecendo esse erro, e agora vamos falar do NULL, o NULL é utilizado quando você quer definir intencionalmente a ausência de valor, o NULL ele é também um valor primitivo que representa a ausência intencional de um valor, é quando de forma intencional você quer dizer que não tem nada ali dentro, por exemplo, eu vou criar uma outra variável, eu vou chamar essa variável de email, por exemplo, e aí eu vou definir que o valor dela é NULL, perceba que agora, se eu executo aqui o email, executo aqui pra ele exibir o console.log que tem dentro de email, eu vou executar, então perceba que agora o que a gente tem aqui é NULL, então basicamente essa é a ideia de NULL, então quando a gente quer definir de forma explícita pra indicar que não tem nenhum valor ali, a gente pode inclusive depois usar numa condição, eu quero verificar se tem um email, aqui como o valor pode ser NULL, o email, então aqui ele já vai usar pra fazer a verificação do verdadeiro ou falso, poderia fazer assim também, eu quero verificar se é NULL ou eu quero fazer o contrário, verificar se não tem um email, então eu posso executar aqui, veja só, informe o email, por exemplo, se eu executo, aí a gente vai cair nessa mensagem, tá vendo, porque aqui como eu fiz uma negação, eu quero saber se não tem email, aí então ele vai cair dentro dessa condição pedindo pra informar o email, então em resumo é isso, então a gente pode utilizar o NULL de forma intencional pra dizer que não tem nada ali dentro daquela variável, tá bom, e o undefined a gente tem esses cenários que é também um valor primitivo utilizado quando uma variável não tem nenhum valor atribuído ainda ou quando, por exemplo, uma propriedade não existe dentro de um objeto.