WEBVTT Nesta aula eu vou mostrar pra você como salvar os dados de forma permanente em um arquivo e a gente vai criar esse arquivo dinamicamente utilizando o próprio Node. Vamos voltar então aqui para o nosso código. Dentro aqui do nosso database eu vou criar aqui um método construtor dentro da nossa classe. O método construtor ele é executado automaticamente quando a classe é instanciada. Quando e como a gente faz a instância dessa classe aqui no nosso projeto. Dentro aqui do nosso middleware a gente tem o nosso route handler. Aqui a gente criou uma instância do nosso banco de dados utilizando o new. É aqui que a gente está instanciando essa classe e é nesse momento aqui que o construtor é chamado de forma automática quando a gente utiliza o new para instanciar a nossa classe. Muito bem, até para te mostrar eu vou colocar aqui um console.log, olha só, construtor e agora se eu abro aqui o terminal veja que ele está aparecendo, se eu executar a aplicação de novo, vou até parar de executar ela, eu vou executar de novo você vai ver que aparece o construtor. Se eu salvo o construtor fica aqui porque a nossa classe está sendo instanciada então vai aparecer ali. Só que ao invés de exibir aqui o construtor a gente vai utilizar aqui uma função, um método que a gente pode criar. Eu vou chamar ele de persist e esse persist aqui é o que a gente vai chamar aqui no lugar desse console.log, vai ser esse persist, esse método aqui, perceba que a gente já usou o dist porque a gente está executando esse persist aqui dentro dessa classe na própria classe, então a gente utiliza o dist para se referenciar que ele pertence e está aqui dentro da própria classe. Dentro do nosso persist a gente pode então criar o nosso arquivo, só que para criar o arquivo eu preciso saber em que lugar eu quero criar o arquivo. Eu posso inclusive deixar o arquivo do banco de dados dentro da pasta src, mas para deixar dentro da pasta src eu preciso também saber onde está a pasta src, então eu preciso pegar esse endereço e para pegar esse endereço dinamicamente eu posso usar como referência o nome ou melhor dizendo o endereço do próprio arquivo aqui que é o arquivo que a gente está aqui que é o database.js e para pegar o endereço desse arquivo a gente pode, vou até utilizar um console.log e aqui a gente pode utilizar o import.meta.url do próprio node. Se eu salvo, olha só, ele está mostrando para a gente o endereço completo da onde o nosso arquivo está, o nosso database.js, só que eu vou fazer o seguinte, posso fechar aqui em cima até fora do nosso banco, da nossa classe, eu vou criar aqui em caixa alta um database underline path, então aqui a gente vai colocar qual é o endereço, o lugar onde a gente quer salvar o nosso banco de dados, só que eu vou utilizar aqui o new url, isso daqui é para criar uma nova url e ela é composta por nome do banco e aonde a gente quer salvar, aqui eu falei nome do banco mas é nome do arquivo, mas é o nome do arquivo que a gente quer dar para o nosso banco de dados também, então vou pegar esse import aqui, ctrl x, posso até apagar esse console.log e vou colocar ele aqui e o nome do banco eu vou chamar por exemplo de db.json, então db abreviação de database e o arquivo é json. Então a gente está criando uma url com o nome desse arquivo aqui nesse endereço aqui. Agora a gente pode dentro do persist utilizar fs, o fs é abreviação de file system, esse daqui para a gente utilizar a gente precisa importar, ele é do próprio node, por isso a gente pode importar o file system utilizando o node 2.fs só que a gente vai utilizar a barra promise para utilizar o async await, então o fs file system ele serve para a gente manipular arquivos aqui utilizando o node, então aqui eu coloco o ponto, eu posso escrever um novo arquivo e eu passo aqui o endereço desse arquivo que eu quero escrever, vírgula e aqui o conteúdo dele, eu posso passar aqui o próprio banco de dados e aqui eu já utilizo o próprio json.stringify para fazer essa conversão. Perceba que ele já criou aqui na raiz do nosso projeto esse arquivo database.js, melhor dizendo db.json, o database é o arquivo que a gente está trabalhando, o db.json já criou ele como um arquivo sem conteúdo nenhum, muito legal, então já criamos aqui o nosso arquivo.