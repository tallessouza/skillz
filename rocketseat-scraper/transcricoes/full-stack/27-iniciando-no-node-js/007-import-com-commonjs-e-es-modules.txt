WEBVTT Continuando com a nossa aula, eu vou clicar aqui pra fechar, tá, encerrar aqui esse terminal, e eu vou apagar o conteúdo aqui do nosso server.js. E agora a gente vai falar sobre importação. E o primeiro pacote que a gente vai utilizar aqui é o HTTP, a gente vai fazer a importação desse pacote e vai utilizar ele pra construir o nosso primeiro servidor. O HTTP, ele é um pacote que já vem disponível com o próprio Node, ou seja, você não precisa instalar ele pra utilizar, ele já faz parte do core do próprio Node, tá. Então eu vou criar aqui uma const que eu vou chamar de HTTP, e dentro dessa constante eu vou fazer a importação do pacote HTTP. E aqui eu vou utilizar igual, e vou utilizar essa palavrinha aqui, required, pra poder fazer a importação do HTTP. Só que perceba que aqui, ele aparece esses três pontinhos. Se eu passo o mouse aqui, ele tá dizendo o seguinte, olha, que esse arquivo, ele é uma importação utilizando o CommonJS, tá. Esse tipo de importação aqui, ele é uma importação padrão do Node, só que ela é um modo de importação mais antiga, digamos assim, mas é o padrão. Eu prefiro trabalhar com o ES Module, que é a nova maneira de utilizar, uma maneira mais nova, né, de utilizar importação de pacotes, utilizando import pra importar, export pra exportar, então eu gosto de utilizar a estratégia de import com o ES Module, tá. Então pra gente conseguir utilizar esse tipo de importação aqui, primeiro a gente teria que trocar aqui por import HTTP, e aí ficaria assim, no lugar de igual required, a gente pode trocar por from, sem parênteses, e coloca aqui dessa maneira aqui. Só que se a gente tentar executar o nosso projeto, eu vou abrir o terminal, eu vou tentar executar o nosso projeto, e dou enter, perceba que ele vai dar esse erro, dizendo aqui, que ele não consegue lidar, tá vendo, com o import, porque a gente precisa fazer um ajuste aqui no nosso arquivo de configuração, que é o pacote.json, então aqui no nosso pacote.json, a gente precisa vir aqui, e adicionar aqui uma propriedade, que é a propriedade type, e aqui no type, tá vendo que vai aparecer pra gente, o common.js, que é o padrão, e o module, a gente vai usar essa opção aqui, que é a opção de module, que é a opção que deixa a gente utilizar o es-modules, pra usar o import, e não o required, tá, clico aqui no module, salvo, e agora se eu abro o terminal aqui, eu vou até limpar ele, tá, e se eu executo aqui com o node src-server.js, que é o endereço do nosso arquivo, dou enter, tá vendo que ele executou e não retornou nada, porque agora ele executou tudo com sucesso, não tem nenhum problema, e não apareceu nada, porque o nosso arquivo não tem conteúdo nenhum, não tem nenhum retorno, melhor dizendo. Conteúdo tem, tem essa importação, mas pode ver que ela tá até meio apagadinha, porque não tem conteúdo nenhum sendo, que utiliza essa importação, então a gente não tá retornando nada, e não tá utilizando HTTP pra fazer nada, por isso ele fica meio apagadinho, só tá importando mesmo, tá. Uma outra coisa que é importante você saber, é que as versões mais recentes do Node permite diferenciar quando uma importação, ela importa uma dependência, um pacote que é do próprio Node, que faz parte ali do core do Node, ou seja, você não precisou instalar ela, ela já tá disponível, ela é do próprio Node, ou quando a importação, ela traz um pacote, uma dependência ali que você tá usando, de terceiros, por exemplo, o HTTP, a gente não precisou instalar, ele é do próprio Node, então a gente pode utilizar aqui, Node, dois pontos, e aí fica o HTTP, desta maneira, a gente tá deixando explícito, que a gente tá utilizando o HTTP, do próprio Node, legal, e aqui eu vou salvar. Existe também outras dependências, né, de terceiros, que a gente pode utilizar no nosso projeto, você vai ver mais à frente, por exemplo, existe um pacote famoso, que é o DayJS, que é pra gente manipular e trabalhar com datas, tá, então a gente vai fazer a instalação dele, no nosso projeto, pra utilizar esse pacote, pra trabalhar com a manipulação, né, de datas, por exemplo, então esse seria um pacote de terceiro, não tá disponível já, no próprio Node, a gente teria que adicionar ele no nosso projeto, tá, mas aqui, com o Node, dois pontos, HTTP, a gente tá fazendo a importação desse pacote, né, do HTTP, mas dizendo que ele é do próprio Node, legal, né, então deixa eu sacar aqui o terminal, também posso fechar ele aqui, e muito bem, então nessa aula você viu como fazer a importação, e no lugar de utilizar o comum JS, a gente mudou aqui, dentro do nosso pacote.json, pra fazer a importação, utilizando aqui como tipo o ESModule, né, e aí a gente define aqui, pra type o valor module, tá, então a gente continua na próxima aula.