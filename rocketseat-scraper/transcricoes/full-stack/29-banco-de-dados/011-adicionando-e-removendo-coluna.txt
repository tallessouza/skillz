WEBVTT Nós já criamos a nossa tabela aqui utilizando esse código SQL, mas agora eu vou apagar esse código aqui e digamos que a gente precisa adicionar uma nova coluna nessa tabela que já existe. Então aqui no nosso código SQL eu vou mostrar pra você como fazer. Podemos aqui utilizar o ALTER TABLE para alterar uma tabela. E qual tabela é essa? É a tabela de produtos, então vamos passar aqui como PRODUCTS, que é o nome da tabela que a gente está utilizando. E aqui a gente pode utilizar o ADD para passar qual coluna a gente quer adicionar, que no caso é a quantidade, vou chamar de quantity, e vou passar por exemplo aqui o INTEGER, que é o tipo de dado que a gente vai utilizar pra quantidade, número inteiro. Inclusive podemos passar aqui um NOT NULL para dizer que essa coluna não vai permitir NULL, ou seja, é uma coluna obrigatória. Se eu clico aqui em RUN, veja que a gente tem aqui a nossa coluna adicionada. Se você quiser remover uma coluna, veja que a gente também pode usar o ALTER TABLE, podemos também chamar que a mesma tabela, que é a mesma tabela que a gente quer, só que no lugar aqui de utilizar o ADD, a gente vai utilizar o DROP COLUMN. Então a gente está dizendo que a gente quer remover ali, excluir uma coluna. Qual é a coluna? É a coluna de quantity, que é a coluna quantidade. Aqui eu já vou aproveitar também pra explicar pra você uma coisa muito importante. Se você clica aqui no RUN, o que ele vai fazer quando você clica no RUN? Se não tem nada selecionado, ele executa tudo que está aqui dentro desse documento. No entanto, eu não quero executar tudo que está aqui, eu não quero criar uma nova coluna com esse nome, não quero. Eu quero executar apenas essa linha de código. Perceba que quando eu seleciono ela, veja que aqui mudou, ele mudou o botão para RUN SELECTION, ou seja, executar somente o que está selecionado. Por isso agora, quando eu clico aqui, você vai ver que removeu a coluna e veja que aqui executou somente essa linha que está selecionada. Então é importante você saber disso. Você clicando em RUN, ele executa tudo que está aqui dentro. Agora, se você quiser executar apenas algum trecho, como por exemplo, não quero executar o código inteiro aqui, quero só essa linha executar agora, esse código apenas, então eu posso clicar aqui em RUN SELECTION e ele vai executar somente esse código selecionado.