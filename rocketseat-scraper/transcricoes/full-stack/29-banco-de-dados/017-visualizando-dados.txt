WEBVTT Você já teve um spoiler aqui do SELECT, que é o comando que a gente utiliza para selecionar dados. Vamos agora começar a estudar com mais detalhes esse comando aqui SELECT. Então eu vou apagar tudo aqui pra gente focar e agora vou fazer um SELECT asterisco FROM na tabela de produtos. Se eu clico aqui em RUN, então vai continuar mostrando o que estava, deixa eu só ocultar aqui do lado. Ou não, posso deixar aberto aqui até pra gente olhar aqui as colunas da nossa tabela. Então quando a gente utiliza aqui o asterisco, significa que a gente está querendo exibir todas as colunas aqui da tabela. Então vamos colocar aqui como um comentário, até pra você lembrar, seleciona todas as colunas. Então lembrando que o comentário no banco de dados de uma linha é traço-traço. Agora a gente também consegue selecionar colunas específicas. Então olha só, seleciona colunas específicas. A gente pode utilizar o SELECT e ao invés de utilizar o asterisco, a gente pode colocar o nome da coluna. Por exemplo, digamos que eu queira selecionar NAME, então eu coloco NAME FROM PRODUCTS. Então eu estou selecionando a coluna NAME da tabela de produtos. Vou selecionar essa linha pra executar apenas ela, se eu clico em RUN, olha só, veja que agora só apareceu a coluna de nome. Você pode selecionar mais colunas separando por vírgula. Então NAME, vírgula, por exemplo, eu quero selecionar o preço. Então eu posso vir aqui, olha só, e seleciono aqui só essa linha. Se eu selecionar aqui também é indiferente, porque a linha 4 é comentário, então ele vai ignorar, mas vamos selecionar essa linha, executamos, então veja que agora tem NOME e tem PRICE. Veja que a categoria não apareceu aqui porque a gente nem quis listar. Inclusive a ordem que a gente coloca aqui também influencia na forma como vai ser exibido. Por exemplo, eu posso selecionar primeiro o preço e depois o nome da tabela de produtos. Veja que agora selecionando inverteu a ordem, então temos o preço e temos o nome. Então não muda aqui, esse comando aqui não muda a ordem em que os dados são salvos na tabela, muda apenas o como eles são visualizados aqui quando a gente executa um SELECT. Então aqui inclusive a gente consegue inverter, vamos colocar aqui como INVERTENDO A ORDEM. Padrão, por exemplo, ou definindo a ordem de exibição das colunas. Então a gente consegue também definir a ordem de exibição.