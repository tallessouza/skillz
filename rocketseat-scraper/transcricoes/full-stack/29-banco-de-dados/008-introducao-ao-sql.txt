WEBVTT Nessa aula vamos falar de SQL porque vamos utilizar SQL na prática para manipular dados com superdados dentro do nosso banco, tá? Bom, como manipular os dados de um banco de dados? Eu já te dei o spoiler, a gente utiliza o SQL. O SQL é a linguagem padrão dos bancos de dados relacionais, então tudo que você vai aprender aqui de SQL você consegue utilizar em qualquer banco de dados relacional. Pode ter alguma pequena diferença entre um banco e outro, né, ali pela empresa que mantém o banco, né, por exemplo, no SQLite o ponto e vírgula no final da linha é opcional, mas no Postgres da vida é obrigatório, por exemplo. Enfim, então esses pequenos detalhes, por exemplo, lá no SQL Server pode usar espas duplas para texto no SQLite ou no Postgres, espas simples, enfim, então pequenos detalhes, mas em essência bancos relacionais utilizam SQL como linguagem padrão, tá? Por isso tudo que você aprender de SQL aqui você vai conseguir utilizar em qualquer banco de dados relacional. SQL significa linguagem de consulta estruturada e como eu disse é uma linguagem padrão para gerenciar e manipular bancos de dados relacionais, então a gente utiliza o SQL para criar, para modificar, para consultar dados dentro do banco, tá? Então agora que você conhece o conceito e o que é o SQL, vamos agora utilizá-lo na prática.