WEBVTT Antes de criar a nossa primeira tabela, é importante compreender quais são os tipos de dados. Os tipos de dados são importantes porque eles determinam qual será o conteúdo de cada dado, ou seja, que tipo de dado cada coluna vai armazenar em uma tabela. Por exemplo, aqui a gente tem essa tabela e na primeira coluna, veja que temos aqui números inteiros, portanto, a gente pode utilizar o tipo Integer. Aqui nessa segunda tabela, temos conteúdos do tipo Texto, portanto, podemos utilizar aqui o Text. Aqui nessa tabela, a gente também tem essa terceira coluna que está guardando aqui números com casa decimal, com ponto flutuante, uma nota, 9.8 por exemplo, então, a gente pode utilizar o Real. E aqui, a gente está guardando, quando o registro foi criado, guardando por exemplo um conjunto numérico, números inteiros, que é um timestamp, um formato de data e hora que guarda ali a representação da data e da hora por um número inteiro. Então, aqui a gente também está usando o número inteiro, então, podemos utilizar o Integer. Então, veja que cada coluna guarda um tipo de dado, por isso que o tipo de dado é importante, para a gente deixar claro e definir qual é o tipo de dado que cada coluna vai armazenar. O SQLite, que é o banco de dados que nós vamos utilizar, tem esses tipos de dados aqui. Cada banco de dados tem os seus próprios tipos, então, tem banco de dados que tem mais opções, menos opções, enfim, mas aqui no SQLite, a gente tem esses tipos de dados aqui, que também vai nos atender tranquilamente. Então, temos o NULL, que representa um valor nulo ou inexistente, que é quando, por exemplo, um campo ali na tabela, ele não tem nenhum dado ou ele armazena o valor nulo mesmo, ou seja, nada foi informado ali. O Integer é um valor numérico inteiro, pode ser positivo ou negativo, a gente utiliza para armazenar números inteiros sem casos decimais, diferente do Real, que já é um valor numérico de ponto flutuante, a famosa casa decimal, usamos então para armazenar, por exemplo, valores que exigem, que precisam dessa precisão decimal, como medição, cálculo financeiro ou, por exemplo, um valor monetário, em reais, por exemplo. Aqui também temos o Texto, que é uma sequência de carácter de texto e a gente utiliza para armazenar textos como nome, descrições ou qualquer outro tipo de dados baseado em texto. E por fim, tem esse BLOB aqui, que são dados binários grandes, esse é o significado aqui, e ele é utilizado para armazenar os dados binários mesmo, por exemplo, armazenar um arquivo, imagem ou qualquer tipo de dado que não seja convertido automaticamente, então ali guarda o binário mesmo do arquivo. Então esses são os tipos de dados que a gente utiliza para definir qual é o conteúdo de cada coluna dentro do nosso banco de dados, tá bom? Bom, agora que você já conhece os tipos de dados do SQLite, vamos continuar avançando nas próximas aulas.