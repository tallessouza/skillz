WEBVTT Agora eu vou dar uma passada, uma visão geral dos principais métodos de ensemble que existem. Isso aqui até já foi comentado naquele módulo de fundamentos de machine learning, de aprendizado de máquina, mas eu quero dar um reforço aqui porque o foco desses módulos que eu vou trazer a partir de agora é justamente nos ensembles e a gente vai dar um detalhamento um pouco maior e também fazer alguns projetos para explorar melhor isso. Então o primeiro método de ensemble que eu gostaria de trazer é o bagging, que na verdade é um acrônimo, vamos pensar que é uma abreviação do que a gente chama de bootstrapping aggregation. Nesse método de ensemble o que a gente faz é criar múltiplas versões de um modelo de aprendizado de máquina, cada uma treinada em um subconjunto aleatório dos dados de treinamento. Ou seja, eu estou pegando o mesmo modelo só que ele está sendo treinado em várias partes dos dados. E aí o que o modelo de bagging faz, os modelos de bagging fazem? Eles pegam essas previsões desses modelos e agregam e combinam para produzir o que seria a previsão, a previsão final. Então essa é a lógica por trás do bagging. Um exemplo bastante conhecido de um algoritmo de bagging é o random forest, que a gente vai explorar inclusive num capítulo, num módulo totalmente à parte, um pouco mais adiante aqui na nossa trilha, porque ele é um algoritmo de referência para quem está começando com algoritmos de ensemble. Falando agora do boosting, ele já tem uma outra abordagem, ele constrói um conjunto de modelos sequencialmente, onde cada modelo tenta corrigir os erros cometidos pelos modelos anteriores. Então como se eu aplicasse um modelo, por exemplo, eu aplico o modelo A, aquilo que saiu de errado do modelo A, eu aplico o modelo B para tentar corrigir essa saída, esse resíduo. Aquilo que saiu do modelo B, eu tento aplicar o modelo C e tento corrigir o resíduo do que saiu do B. E assim por diante até que eu chegue num modelo que chega num nível de erro bem baixo e que eu consiga gerar a previsão final. Exemplos de algoritmos que a gente pode chamar de boosting, a gente tem o Cat Boost, a gente tem o XGBoost, que é um algoritmo bem famoso, os dois, o LightGBM, que é uma versão, vamos dizer, Lightweight, uma versão leve do XGBoost e o Adaboost, alguns dos exemplos de boosting. Terceiro método de ensemble é o Stacking, nesse caso a gente treina diferentes modelos e as previsões são usadas como entrada para um modelo de nível superior, o Stacking na verdade é empilhar, então na verdade você pega um modelo e vai empilhando vários modelos até que ele chegue num metamodelo superior, que ele vai aprender a melhor forma de combinar as previsões desses modelos base para poder fazer a previsão final. Aqui não tem um exemplo específico de algoritmo, mas a gente vai trabalhar isso com alguns algoritmos padrões que temos dentro do Site to Learn. Por último eu tenho o método, que até em alguns casos, algumas literaturas não consideram como método, mas eu quis trazer aqui, que é o Voting. Na verdade o Voting é usado, por exemplo, no bagging, no final do bagging, eu vou explicar aqui, ele vai fazer, no caso, um Voting para definir como ele produz a previsão final. Mas o que é o Voting? O Site to Learn tem diversos modelos de aprendizado treinados em todo o conjunto de dados, e é o que eles fazem, eles combinam essas previsões por meio de votação, para classificação ou média. Eu vou explicar isso na hora que eu vou falar do bagging, mas existem alguns algoritmos do Site to Learn específicos para Voting, então por isso que eu quis trazer aqui, como é um módulo de introdução com o Site to Learn, a gente vai usar o método do Site to Learn de Voting para ver como é que ele realiza o processo aqui, como é que ele performa. Então, é isso. Obrigado.