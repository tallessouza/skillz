WEBVTT Preciso completar aqui esse projeto prático com mais duas coisas que a gente precisa fazer para que a gente possa reutilizar esse dataset e o pré-processo desse dataset para outros projetos práticos que faremos usando esses dados. Como a gente teve uma fase de EDA e a gente teve uma fase até antes do EDA de poder preparar esses dados, de fazer o data cleaning nos dados, a gente não precisa repetir esse passo inteiro nos próximos projetos. Então a gente vai poder pegar o dataset que já está limpo, já está tratado e vamos partir dele. Como é que a gente vai fazer isso? Então a gente vai salvar esse DataFrame que está no nosso notebook como um arquivo CSV. E aí a gente na hora que for usar esse arquivo como entrada, quando a gente for usar a entrada do arquivo para o próximo projeto, a gente vai usar esse arquivo que está limpo, que já está Cleaned, vamos pensar assim, e não o arquivo original que estava cheio de colunas, as colunas lá com valores vazios e tudo mais, aquilo que a gente fez durante esse projeto. Mesma coisa o pré-processor, a gente não precisa criar um pré-processor diferente do que a gente criou agora, porque a gente está usando exatamente as mesmas colunas e vamos aplicar exatamente as mesmas transformações nessas colunas. Então a gente também não tem necessidade de recriar isso. Então a gente também vai pegar um pré-processor que já está pronto, vamos salvar esse pré-processor e vamos utilizá-lo nos próximos projetos com esse DataFrame. Então eu vou criar uma coluna aqui em Markdown, vou colocar aqui, salvar dados e pré-processador do modelo. Aí a gente vai fazer a primeira coisa aqui, que vai ser salvar o DataFrame como CSV. Para fazer isso, nosso DataFrame se chama DFLeads, que é o DataFrame já, vamos dizer, totalmente trabalhado lá, com as colunas todas já removidas, todo aquele tratamento que a gente fez para entrar com esses dados no modelo. Então é esses dados que a gente vai salvar. Então para isso a gente usa uma função do Pandas chamada toCSV, a gente define o primeiro argumento que é o lugar onde a gente quer salvar esse arquivo, então eu vou colocar aqui datasets, que é a pasta onde está o dataset original, inclusive o datasets, está o Leads aqui, e vou dar o nome, Leads, e vou colocar aqui Cleaneds, ou seja, os dados já foram limpos, ponto CSV. Um outro argumento aqui, o parâmetro que a gente deve colocar é o índex, eu vou usar o índex igual a falso, por quê? Para garantir que ele não crie uma coluna mais com um índice, um ID de 1 até a quantidade de linhas, então a gente não precisa dessa coluna a mais, então eu coloco o índice igual a falso, executo, e aí se eu venho aqui, aparece aqui o Leads Cleaned com o arquivo CSV, veja que ele não cria uma coluna adicional aqui, e aqui estão somente aquelas colunas que de fato a gente usou para o nosso modelo desse projeto prático. Uma coisa importante, a gente não fez o Unhot Encoding, ou seja, a gente não transformou essas colunas categóricas em colunas numéricas, esse processo foi feito no próprio Cessor, lá usando o Column Transformer, então a gente está aqui de fato com o DataFrame limpo, íntegro e sem a transformação de um Unhot Encode, então já fizemos essa primeira parte. Na segunda parte, a gente vai salvar o pré-processo, aí a gente faz o que? Importa o módulo que a gente geralmente usa para salvar o JobLib, e a gente vai fazer o seguinte, JobLib.dump, a gente vai pegar o objeto que a gente quer salvar, no caso a gente vai pegar o pré-processo, e vamos dizer onde a gente quer salvar esse pré-processo, então a gente quer salvar esse pré-processo para onde? Eu não vou salvar em Datasets, eu vou salvar na própria pasta do projeto, então eu vou colocar aqui, pré-processor datasetleads.pkl, o arquivo formato PICOM, se eu mandar executar esse comando, ele salva aqui um arquivo PICOM com o pré-processor, então pra gente poder usar esse pré-processor depois, basta a gente fazer o JobLib, download nesse pré-processor, e usar esse pré-processor em cima dos dados que a gente for carregar. Então com isso, agora sim, a gente fecha esse projeto prático, salvando esses dados aqui que vão ser importantes para os outros projetos que a gente vai utilizar com esse mesmo Dataset, e vejo vocês nas próximas aulas.