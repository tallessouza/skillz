WEBVTT Dando sequência agora ao processo, a gente pode fazer o trabalho de preparação de dados. E da mesma forma, como a gente fez, inclusive no módulo anterior, de adaboosting, classificação, a gente vai aproveitar um preprocessor, que a gente já tem, para fazer aquele trabalho de converter as variáveis que são objetos em variáveis numéricas, com unhot encode, e fazer toda a parte de padronização das variáveis que são contínuas para uma escala normal, standard scalar. Então, vou colocar aqui uma outra parte, preparação dos dados. Vou colocar aqui, preparar os dados para o modelo. Isso significa que a gente vai ter uma variável x, onde a gente vai pegar o dfCosts, nós vamos dropar a coluna, que é a nossa coluna target, que é justamente a medical charges, que é o valor que a gente quer prever, qual o gasto daquela pessoa. E y será o dfCosts, e aí apenas o medical charges. Feito isso, podemos agora importar o preprocessor, que a gente já salvou anteriormente, para aplicar esse preprocessor dentro dos dados de treinamento e de teste. Então, importar preprocessor, já salvo anteriormente. Para isso, a gente usa a biblioteca Joblib, preprocessor igual a Joblib.load, e aí a gente vai importar o seguinte arquivo, que também já está na nossa pasta do projeto, que é preprocessor.dataset.healthcosts.pkl. Importei o preprocessor. Próximo passo, a gente vai dividir o nosso conjunto de dados entre treino e teste. Dados entre treinamento e teste. Para isso, a gente vai usar aquela função de trainTestSplit, xTest, yTrain, yTest, trainTestSplit, x e y. Nós vamos usar um conjunto de teste de 20%, vou colocar uma chave aqui, 51. Fizemos a divisão, e agora a gente pode aplicar o Transformer nos dois conjuntos, para a gente poder treinar o nosso modelo. Então aqui, aplicar preprocessor nos dados de treinamento e teste. Recapitulando, a gente vai colocar isso no próprio xTrain, vamos usar o preprocessor.fitTransformer, ou seja, ele treina o meu preprocessor e aplica no próprio xTrain, gravando isso aqui, sobrepondo o próprio xTrain. No caso do xTest, a gente só vai fazer o Transformer, porque a gente vai pegar o treinamento que ele fez nesse preprocessor nos dados de treinamento e aplicar nos dados de teste. Então a gente vai usar somente o Transformer. Temos aqui o nosso xTrain e xTest. E podemos aqui mostrar as dimensões dos conjuntos, só para ter uma noção de como que ficou. Então print dados de treinamento, é o nosso xTrain.shape, mostra as dimensões daquele array. E fdados de teste, xTest.shape também. Ele mostra o seguinte, aquelas 1.338 ocorrências na base, 1.070 ficaram para treinamento, 268 para teste com 10 variáveis. Originalmente são 6 variáveis, 7 variáveis na verdade, como ele tem aqui uma variável objeto do sexo e uma variável objeto do region, então ele explodiu essas variáveis e chegou em uma dimensão maior. Então com isso a gente concluiu mais uma parte do nosso projeto, fazendo agora a preparação dos dados usando o preprocessor que a gente já tinha salvo anteriormente. Agora os dados já estão prontos para que a gente possa fazer o treinamento no nosso Adaboost. Então vejo vocês no próximo vídeo.