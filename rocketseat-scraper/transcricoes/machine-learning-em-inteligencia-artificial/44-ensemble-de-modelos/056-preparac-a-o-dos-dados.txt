WEBVTT Agora que nós já carregamos os dados, podemos já prepará-los para poder fazer o treinamento do modelo. Então, vou abrir uma nova célula aqui. Markdown. Preparação dos dados. Para a gente preparar esses dados, a gente vai usar a abordagem de fazer a separação entre treino e teste. Então, preparar os dados para o modelo. Nosso x dflids.drop, pegar todas as colunas menos a coluna target, que no caso é a coluna converted. E o nosso y igual a dfl leads, só a coluna converted. Para fins de interpretabilidade, eu vou criar duas listas. Lista de colunas. Uma lista com as features numéricas. E outra com as features categóricas com o select the types seleciono pelo tipo específico da coluna que eu quero no caso aqui a coluna categórica é o tipo object e seleciono as colunas e ele coloca isso numa lista. E lembrando que a gente, da mesma forma que salvou o dataset já pronto para trabalhar, a gente também já salvou um pré-processor pronto para trabalhar e fazer toda a parte de standard e scaler nas variáveis numéricas e fazer a parte de one hot encode nas variáveis categóricas. Então a gente vai usar esse pré-processor. Então usar pré-processor resistente. Para isso a gente vai importar joblib a gente vai importar joblib e vamos jogar numa variável chamada preprocessor preprocessor igual a joblib.load de carregar, nós vamos carregar o arquivo que se chama está até aqui preprocessor datasets pré-processor datasets leads .pky pkl e pico com o pré-processor agora a gente já pode fazer a divisão de treinamento e teste, dividir o dataset entre treinamento em teste, dividir o dataset entre treinamento e teste. Então a gente vai ter aqui o nosso XTrain, XTest, YTrain, YTest, e vamos chamar o Train test split passando x e y vamos usar um test size de 0.2 ou seja, 20% e um handle state aqui de 51 e agora a gente vai aplicar o preprocessor então o nosso x train é igual ao pré-processor. E aí eu vou fazer fit e transform, ou seja, eu treino nos dados de treinamento e aplico nos próprios dados de treinamento. E aí tem uma questão aqui, nesses modelos, que neste caso específico por conta do tipo de modelo que a gente está usando a gente tem que converter esse X-Train para um Array do NumPy a gente não fez isso das outras vezes, mas neste caso específico a gente vai precisar converter ele para um Array, então a gente vai colocar o comando toArray no final desse pré-processador e vamos? E vamos fazer a mesma coisa com o Xtrain, só que no Xtrain a gente não precisa treinar o pré-processador ele já foi treinado com os dados de treinamento, e eu apenas uso o transform e aplico no conjunto de testes e também converto ele para um array do numpy. E aí se eu quiser mostrar os conjuntos, print conjunto de treinamento. É o meu Xtrain.shape. E o meu conjunto de testes. É o meu Xtext. meu X text e ele me mostra aqui 7259, 1815 e já com a explosão das colunas do Unhot Encode 68 features, colunas que vão entrar no modelo então concluímos aqui a preparação para os dados do modelo já bem facilitada porque já temos o pré-processo pronto já estamos para os dados do modelo, já bem facilitada porque já temos o pré-processo pronto. Já estamos com os dados ok para poder fazer o treinamento do nosso modelo. Então vejo vocês no próximo vídeo.