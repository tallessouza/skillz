WEBVTT A gente vai dar início agora a preparação dos dados para que a gente possa fazer o treinamento do nosso modelo de ensemble com regressão. Então, primeira coisa aqui, vou criar aqui uma sessão, como geralmente eu faço, preparação dos dados. E aí a gente vai preparar dados para o modelo. E o que a gente vai fazer aqui? Vai separar o X e o Y. Então, o nosso X vai ser o DEF COS, e a gente vai dropar a coluna columns igual a medical charges. Então, a gente vai arrancar a coluna medical charges do X, e o nosso Y vai ser apenas a coluna medical charges. Você vai copiar aqui, medical charges. Pronto, já tenho o meu X e Y. A próxima coisa que a gente precisa fazer é criar os columns transformers, para que a gente possa transformar as variáveis numéricas e colocá-las todas em uma mesma faixa, numa mesma escala, usando o standard scaler, e a gente fazer um hot encoder nas variáveis categóricas. Por exemplo, a variável de gênero, que chama sex, a variável também de região, que é uma variável que também tem lá as categorias, as regiões de onde a pessoa mora. Então, também podemos automatizar, em vez de a gente ficar relacionando as variáveis todas, a gente poder fazer uma forma mais automatizada de capturar todas as variáveis numéricas e categóricas, e já jogar dentro do column transformer. Então, a gente vai colocar aqui, column transformer, para normalizar variáveis numéricas, e one hot encode para categoricas. Então, a gente vai criar uma variável chamada numeric features, que basicamente é o meu x, e eu vou fazer um select the types, onde eu vou incluir aqui apenas as colunas do tipo number, e eu vou trazer uma lista de colunas. Mesma coisa para features categóricas. Então, aqui categorical features, e o tipo é o tipo object. Então, aqui já selecionei as features que eu quero, os grupos de features numéricas e categóricas. Agora o que eu posso fazer é criar um pré-processor, e chamar o column transformer para fazer essas transformações. Então, eu vou declarar quais são esses transformers, então aqui o meu primeiro transformer, ele chama-se num, é de numérica, vou aplicar um standard scaler, e vou dar aqui um numeric features. Ou seja, eu escolho o nome, qual é a transformação que eu vou fazer, e quais são as features que eu vou aplicar essa transformação. Segunda transformer vai ser um transformer categórico, onde eu vou usar um unhot encoder, porém com aquela questão do handle unknow, por quê? Porque como a gente pode ter, na hora de fazer o split entre dados de treino e teste, eu posso ter um dado que está numa porção dos dados, e não foi encontrado na outra, no caso de testes, eu tenho que lidar com isso, uma das formas de lidar com isso, de gerenciar esses desconhecidos que possam aparecer na hora da transformação, na hora do split, eu coloco o handle unknow ignore. Fiz isso, criei esse pré-processor, então, criei o pré-processor. Agora eu posso fazer o quê? Dividir os dados entre treino e teste, e aplicar o pré-processor nas porções dos dados. Corretas, tanto no Xtrain quanto no Xtest. Então, a gente vai dividir os dados em treinamento e teste. Então, a gente vai fazer uso daquele train testsplit xy, a gente vai usar um testsize de 20%, e vamos colocar aqui uma variável, um random state, para conseguir reproduzir esse experimento. E aí ele vai alimentar as seguintes variáveis, Xtrain, Xtest, Ytrain, Ytest. Próxima coisa que eu posso fazer, eu já fiz a divisão, próxima coisa que eu posso fazer é, aplicar column transformer, nos dados de treinamento e teste. Então, eu coloco aqui o meu Xtrain, ele é igual ao pré-processor, e eu vou dar um bit transform, ou seja, ele treina o meu transformer, o meu column transformer, e já aplica a transformação nesse mesmo conjunto. Porém, para o conjunto de teste, eu tenho que usar o pré-processor que foi treinado no passo anterior, então eu não uso o bit transformer, eu uso só o transformer. E eu coloco aqui o meu Xtest, ou seja, ele aprende a transformação, ele treina a transformação e transforma os dados de treinamento, e só aplica esse treinamento que ele usou aqui, transformando o conjunto de testes. Lembrar que essa é a forma correta de se fazer isso. Criei, e a gente pode aqui mostrar as dimensões dos conjuntos. A gente pode olhar aqui que, bom, a gente tem aqui dados de treinamento, é o nosso Xtrain, a gente pode pegar aqui o shape, ele vai pegar a quantidade de linhas e colunas, e o nosso print, vamos fazer um print aqui dos dados de testes, que é o meu Xtest. Então ele mostra aqui que ele salvou 1070 linhas e 10 colunas para a dada de treinamento, e as mesmas 10 colunas mais 268 para fazer o teste no nosso modelo. Então, preparamos agora os dados para o nosso modelo, já podemos ir nos próximos passos, Então vejo vocês no próximo vídeo.