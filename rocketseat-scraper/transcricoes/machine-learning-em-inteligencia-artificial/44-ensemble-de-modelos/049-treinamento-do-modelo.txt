WEBVTT Agora que já temos os nossos dados preparados, podemos já fazer o treinamento do nosso modelo. Então, vou colocar aqui uma outra seção, notebook, treinamento do modelo. E aí, para treinar o modelo, a gente faz isso em duas partes. Primeiro, a gente cria o modelo e depois a gente faz o treinamento efetivamente. Então, criar um modelo de Adaboost, o regressor. Então, vou chamar esse modelo de BoostingModel e aí eu vou criar um objeto do tipo AdaboostRegressor. Vou usar como estimador o LinearRegression. Vou trabalhar inicialmente com 10 estimators. Podemos trabalhar com 50, enfim. Vou até colocar 50 aqui, que é o padrão. Do mesmo jeito que no caso do AdaboostClassifier, a gente tem um parâmetro aqui de LearningRate. Lembrando que o LearningRate, quanto maior o salto de aprendizado que ele tem entre uma instância e outra do algoritmo, em cada uma das 50 interações, quanto menor esse aprendizado acaba sendo. Então, é um hiperparâmetro que você pode calibrar. Depende muito do quão ruidosos estão os dados, o quanto que há um padrão de treinamento entre cada uma das instâncias que estão sendo geradas, das interações que estão sendo geradas no modelo. Eu sempre começo com um e aí eu vou regulando isso e avaliando as métricas. Isso pode ser feito também através de automação de hiperparâmetros, como a gente já viu em alguns módulos com Optuna. Ou outros módulos também, mas o Optuna é o que a gente usou em alguns módulos anteriores. Então, criamos aqui o modelo e agora podemos treinar o modelo. Aqui tem o BoostingModel.fit xTrain, yTrain. Então, já posso treinar o modelo. E aí ele mostra aqui o resultado. Estimador que você usou foi um LinearRegression. Esse LinearRegression chama o objeto LinearRegression. Aí tem a documentação aqui. Ele dá mais informação que o modelo foi fitado, foi treinado. Então, a partir daqui a gente já consegue fazer predições com esse modelo. Então, bem simples. Vamos pensar comparando o que a gente fez anteriormente no Bagging ou até no Boosting de Classifier. Tem uma estrutura bem parecida com essa diferença no caso do Adaboost. E você tem essa variável de Learning Rate, que é um hiperparâmetro que você pode controlar o nível de aprendizado que ele vai ter em cada uma das interações. Então, com esse treinamento feito, a gente já está pronto para começar a fazer a avaliação dos resultados, tanto do ponto de vista das métricas, quanto do ponto de vista do que o modelo nos dá, da importância das variáveis que contribuíram mais para o modelo, e mais algumas características que a gente consegue extrair em algumas propriedades desse modelo. Então, vejo vocês no próximo vídeo.