WEBVTT Com base nos dados já que a gente carregou, que já estão tratados, a gente já pode partir, já vista que a gente não vai fazer o EDA pra cá, a gente já fez isso no projeto anterior, a gente já pode partir pra preparar esses dados pro algoritmo. Então, vou colocar aqui, preparação dos dados. E pra preparar esses dados, também a gente vai utilizar um pré-processor que a gente já tinha, salvo no modelo anterior, pra facilitar nossa vida. Então, preparar os dados para o modelo. A gente vai fazer o que? x é igual a def leads, lembrando que a nossa coluna target é a coluna converted, ou seja, se o lead foi convertido ou não. Então, a gente vai dropar essa coluna do nosso x. E o nosso y vai ser o def leads e somente a coluna converted. Pronto. Eu vou criar uma lista pra usar mais pra frente na parte de interpretabilidade. Então, vou criar uma lista de colunas para a parte de interpretabilidade, com as colunas numéricas e as colunas categóricas. Como é que eu crio isso? A gente pode, na verdade, usar o recurso de pegar o x, que no caso são as colunas que a gente vai trabalhar no modelo, e faz uma seleção por tipo de dado. Lembrando que a gente fez a outra vez. Então, eu digo, quero selecionar todas as colunas do tipo number e eu quero que traga as colunas. E da mesma forma, eu quero fazer com as categóricas. Então, categorical features. Eu vou também selecionar por tipo de dado, incluindo aquelas que são do tipo object.cols. E aí, eu criei duas listas. Uma das features numéricas e a outra das features categóricas. Agora, eu posso já dividir os dados de training test e depois aplicar o pré-processador pra fazer a parte de estandardizar ou colocar os valores numéricos numa escala, na escala normal da distribuição normal usando o standard scaler. E também, a gente pode fazer o unhot encoding nas colunas que forem colunas do tipo object. Só que tudo isso já está feito no pré-processor. A gente precisa criar um pré-processor de novo. Então, o que a gente faz? A gente pode usar o pré-processor. Já salvo anteriormente. Inclusive, ele está aqui na minha pasta. pré-processor.dataset.leads Então, como a gente faz isso? A gente importa o joblib e faz pré-processor é igual a joblib.load e aí a gente carrega o arquivo que a gente quer. pré-processor.dataset.leads.pkl o arquivo do tipo pico e importei o pré-processador. Agora, eu posso fazer o que? Dividir os dados entre treino, treinamento e teste. Então, eu posso pôr aqui xtrain, xtest ytrain, ytest igual a aquela função treintestsplit coloco x e y informo quanto eu quero reservar para teste nesse caso eu vou reservar 20% para teste e vou colocar aqui uma semente randômica para depois poder reproduzir esse experimento usando a mesma semente randômica se eu precisar. Dividi os dados entre treino e teste vou até rodar essa célula aqui separada e agora eu posso aplicar o pré-processor como xtrain é igual a pré-processor que é uma variável que já recebeu o pré-processador com todos os column transformers eu vou dar um fit transform no próprio xtrain e na hora de aplicar no xtest eu não preciso fazer o fit eu já treinei o meu pré-processor dos dados de treinamento e vou apenas transformar os dados de teste então eu venho aqui, pré-processor, transforme, xtest e agora eu tenho o meu xtrain e o meu xtest já transformados com os pré-processadores e agora eu posso só mostrar a estrutura desses conjuntos de treino e teste então print xtrain.shape e print xtest.shape ele mostra aqui que ele reservou 7259 registros instâncias para a parte de treinamento com 68 colunas porque ele já está fazendo toda a parte do unhot encoding das colunas categóricas e reservou 1815 registros para a parte de testes reservou para testar depois o modelo e também com as mesmas 68 colunas então com isso a gente concluiu agora mais uma parte desse projeto que foi a preparação dos dados do algoritmo vejo vocês no próximo vídeo Legendas pela comunidade Amara.org