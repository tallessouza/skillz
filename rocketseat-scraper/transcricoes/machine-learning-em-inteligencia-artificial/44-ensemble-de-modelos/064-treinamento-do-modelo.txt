WEBVTT Agora que nós já estamos com os nossos dados preparados, a gente já pode começar a preparar o nosso modelo de stacking para ser treinado. É isso que nós vamos fazer agora. Então, vou criar aqui uma célula Markdown só para gente separar aqui o notebook, e aí nós vamos criar o modelo de stack regressor em duas partes. Primeiro a gente vai criar os objetos referentes aos algoritmos base, ou os weak learners que a gente vai usar e depois o algoritmo que vai, o modelo, o estimador que vai consolidar esses algoritmos base para fazer a predição final. Então, algoritmos base. O primeiro deles a gente vai usar uma regressão linear simples. Então, a gente está criando um objeto chamado lr model do tipo linear regression o segundo modelo que a gente vai usar como um modelo é um algoritmo base vai ser um modelo de elasticnet que a gente vai chamar de elastic model então vou colocar aqui elasticnet e vou passar para ele dois parâmetros. Eu vou passar os parâmetros padrões, mas deixar documentado, que é o parâmetro alfa, que é um dos regularizadores que existe no ElasticNet, como 1.0, e o L1 ratio, que é um outro regularizador, que eu vou usar meio, que é o padrão. Eu vou colocar aqui um random state de 51 e como terceiro modelo e a gente vai usar um modelo de árvore usando três modelos bem diferentes um modelo de um modelo linear sem nenhuma regularização o modelo linear com regularização e um modelo de árvore. Então, a gente vai chamar de TreeModel e a gente vai usar o DecisionTreeRegressor, também com o RundleState de 51, para a gente conseguir depois fazer a... reproduzir o experimento com os mesmos parâmetros. Depois disso, a gente vai criar agora o meta-modelo, ou o que a gente chama de meta-learner, que é aquele modelo final que vai consolidar as predições anteriores. Então, a gente vai criar esse modelo usando o Huber Regressor, que é um algoritmo de regressão mais robusto. Então, a gente vai chamar isso de Huber Model e vamos instanciar o objeto Huber Regressor. Feito isso, a gente agora vai criar, de fato, o objeto HuberRegressor. Feito isso, a gente agora vai criar de fato o objeto do stacking, que vai combinar essas duas coisas. Então aqui a gente vai falar stackingModel. Então a gente vai chamar ele do que? stackingModel é igual, a gente vai usar o objeto stackRegressor. Primeira parte que a gente vai usar o objeto stack regressor a primeira parte que a gente vai passar do stack regressor são os estimators ou seja quais são os estimadores que eu vou usar como base e ele passa né eu vou passar isso através de uma de uma lista de tuplas onde eu passo o nome pelo qual quero que o modelo seja chamado e o objeto do modelo em si. Então, o parâmetro aqui chama estimators, que é uma lista. Então, o primeiro modelo vai ser o modelo linear regression, que eu vou chamar ele dessa forma. E ele vai acionar o LMLRModel. Então, eu criei aqui o meu primeiro algoritmo base. O segundo, ElasticModel, eu vou chamar de só de Elastic, e eu vou chamar o ElasticModel. E o terceiro, que vai ser uma. Decision. Tree. Que vai ser o meu objeto. Tree. Model. Fechei os três aqui. Agora. O que eu faço? Eu vou passar o próximo parâmetro. Então. Primeiro eu fecho a lista. Fechei a lista. Vou colocar uma vírgula aqui opa, aqui então estou aqui com o stack regresso fechei a lista, agora vou passar qual o meu final estimator, ou seja, o meu metamodelo então final estimator igual a urban model e aí já tem uma sacada aqui também. Em relação a questão. Do que eu vou usar para treinar o modelo. E aí eu vou usar para isso. Aquele parâmetro. Pass true. O pass true como padrão. Ele vem como falso. Eu já vou deixar aqui como falso. Mas já vou deixar documentado aqui em cima. Então. Pass true. Igual a true. como falso mas eu vou deixar documentado aqui em cima então peço igual a tru Usa apenas as predições dos estimadores mais o conjunto de treinamento. Então, se eu quiser que aqui no caso é o nosso dataset a gente vai começar isso com o pass-through igual a falso que é o padrão então executo aqui essa célula, já criei meu stack regressor, agora a gente já pode de fato treinar o modelo então a gente vem aqui e fala treinar o modelo e para isso a gente vai fazer stack model.fit x train e y train executamos e aí a gente vai train e y train. Executamos. E aí a gente vai mostrar aqui o resultado. Então ele pegou, treinou linear regression, treinou elastic net, treinou decision tree e combinou essas três predições e passou para um Huber regressor, que é o nosso meta learner, para fazer a predição final. E aí ele mostra aqui no i que esse modelo foi treinado. Então a gente já pode depois fazer predições com ele. Então concluímos aqui mais uma parte desse projeto, fazendo o treinamento do nosso Stacking Regressor. A partir de agora a gente pode começar a fazer a análise dos resultados desse modelo. Vejo vocês no próximo vídeo.