WEBVTT Fala Dev! Vamos começar mais um projeto prático desse módulo, dessa vez fazendo o nosso projeto de classificação da IDTech usando a abordagem de stacking. Para começar, eu vou criar um arquivo novo chamado stacking-classification.ipynb. classification.ipynb Eu vou usar aqui uma abordagem parecida do que eu fiz no módulo anterior de Adaboost, aproveitando aqui algumas classes para serem importadas aqui no nosso projeto. Haja vista que a gente não vai precisar fazer a parte do EDA, a gente já fez isso antes, não vamos precisar fazer toda a parte de limpeza dos dados. Então vou aproveitar aqui, então não preciso do mat match plotlib, a gente não precisa do cborne, a gente não precisa do graph objects, do plotlib a gente não precisa do column transformer porque a gente já tem um preprocessor pronto e a gente não precisa do preprocessing pelo mesmo motivo. Em relação ao ensemble a gente vai usar uma outra classe chamada stacking classifier e no nosso caso aqui a gente vai usar um modelo de stacking considerando que é uma classificação a gente vai usar três modelos de classificação tendo um quarto modelo como sendo o nosso meta learner tá e eu escolhi aqui como meta learner o próprio logistic regression que a gente já vem usando nos outros módulos. Porém, eu vou agregar aqui os modelos que vão ser os weak learners, que vão gerar as predições para que o logistic regression concentre essas predições e dê a predição final. Então eu vou trazer um modelo chamado SGD classifier, que é um modelo baseado em gradientes, em gradiente descendente, não é o foco aqui é a gente explicar no detalhe do detalhe o que é esse modelo, mas ele é um classificador bastante interessante ser trabalhado, também trabalha com conceito, é um conceito linear vou trazer também aqui para esse projeto um modelo de árvore no caso aqui eu vou usar um modelo de árvore de decisão que a gente também já viu anteriormente, então aqui Decision Tree Classifier. E vou trazer um outro modelo que também é uma classe de modelos de classificação que é o modelo do Support Vector Machines e mais especificamente o SVC que é o Support Vector Classifier que é muito interessante para classificações binárias, como é o nosso caso aqui. A gente vai classificar se o lead vai ser convertido ou não, o 0 ou 1. Então, é bastante interessante aqui o SVC. Então, o que a gente vai ter é o SGD, o Decision Tree na árvore de decisão SVC, fazendo as suas predições e tendo o Logistic Regression como sendo o meta-modelo, o meta-learner que vai finalizar o processo. Então, eu vou atualizar aqui. Ah, o SNS aqui, o White Grid. Então, eu preciso importar. import seborn as sls fiz aqui a o import das bibliotecas e aí a gente já pode começar a carregar os dados então eu vou colocar aqui carregar os dados e aí para carregar os dados a gente vai usar os dados já limpos então a gente vai vir aqui carregar os dados, a gente vai usar os dados já limpos. Então a gente vai vir aqui, carregar os dados já tratados. .barra datasets leads cleaned, da mesma forma que a gente fez no adaboost.csv carreguei os dados mostrar as linhas as linhas iniciais def leads .red 20 iniciais, dflids.red de 20, e aqui só para recapitular os dados do CRM, mostrar as últimas linhas, dflids.tail de 20, também vou mostrar as últimas 20 linhas, também os dados aqui do CRM, e recapitulando a estrutura do dataset, do que sobrou daquele dataset inicial que tinha muitas colunas. Então aqui, estrutura do dataset, a estrutura do dataset dfleads.info e a074 entradas algumas colunas do tipo o objeto ainda que vão ser convertidas pelo pré-processo através do método de one hot encode muitas colunas que a gente já transformou inteira 01 né e algumas ainda umas colunas do tipo float, como visitas, page views. Então, fizemos essa primeira parte, carregamos as bibliotecas que a gente precisa, carregamos os dados, daqui para frente agora é preparar os dados e treinar o nosso classificador com o modelo stack. Então, vejo vocês no próximo vídeo.