WEBVTT Fala Devs, vamos começar então agora o nosso próximo projeto prático, agora fazendo, aplicando o ensamble do tipo Bagging, porém para um problema de regressão. Voltando aí, fazendo uma retrospectiva, recapitulando, para um problema de uma health tech, que quer fazer uma previsão de valor de custos de saúde de funcionários de uma possível empresa cliente. Então, a gente vai começar fazendo o seguinte, criando, eu estou usando a mesma pasta aqui para todos os projetos, a gente vai colocar todos em uma mesma pasta, os datasets, e aqui já está o modelo do Bagging, o classifiquejo que a gente fez anteriormente, aqui tem o preprocessor, aqui tem os dados do outro experimento, que a gente vai criar o novo para regressão. Então, vou criar um arquivo novo aqui, chamar ele de BaggingRegression.ipynb Já abriu aqui, aí eu vou selecionar o kernel, para que eu possa executar esse notebook, e vamos começar aqui importando as bibliotecas. Então, importar bibliotecas. O que a gente vai usar de biblioteca? Basicamente, as mesmas bibliotecas que a gente usou no anterior, diferenciando aí que tem algumas coisas que a gente, por ser um problema de regressão, a gente vai usar algoritmos de regressão. A gente vai fazer o import pandas, spd, a gente vai fazer o import numpy, snp, a gente vai usar o plotly, então plotly.express.spx A gente vai usar o plotly.graphObjectsAsGO, então aqui é a parte do EDA, aí agora a gente vai começar a importar as coisas do SectLearn. Então, primeira coisa, from sklearn.compose, import ColumnTransformer, transformar as colunas. Aqui a gente também vai usar o ModelSelection, que vai fazer a separação do conjunto de treino e teste. A gente vai usar algumas funções do módulo de pré-processamento, nesse caso aqui a gente vai usar o Standard Scalar, para colocar todas as colunas numéricas, as variáveis numéricas, quantitativas, na mesma escala, na escala normal, e o One-HotEncoder para transformar variáveis que sejam categóricas. E aqui a gente vai usar o que? A gente vai usar, no sklearn, a gente vai usar o Ensemble, só que a gente vai importar o BeggingRegressor. E a gente vai usar um modelo linear, uma regressão linear, então a gente vai importar aqui um LinearRegressor. Para avaliar as métricas, também aqui do sklearn, a gente vai trazer o RootMeanSquareError, ou seja, a raiz quadrada do erro médio quadrático, que faz com que esse erro venha na mesma ordem de grandeza dos dados que você está referenciando, no caso da variável Target, e o R2Score também, que já é uma medida, uma métrica já conhecida para avaliar a performance de modelos de regressão. Então, fazemos esse import, ele já fez o import, podemos começar agora a carregar os dados. Então, carregar dados. Então, a primeira coisa que a gente vai fazer é carregar os dados. Eu vou chamar de Def Costs, porque é custos, e a gente vai importar esses dados. Costs.read.csv, está em Markdown, então tem que trocar aqui, .read.csv, ponto, barra, está na pasta Datasets, Health, HealthCosts.csv. Já posso importar esses dados. Vou mostrar as primeiras linhas do DataFrame. Def Costs.head 10. Então, um Dataset menor, eu já vou explicar as colunas, mas quase tudo aqui é bem auto-explicativo, e mostrar as últimas linhas do DataFrame. Def Costs.tail 10. Também a mesma estrutura aqui. Vou mostrar a estrutura do DataFrame, e aí vou explicar as colunas. Def Costs.info. E aí ele tem sete colunas. Uma coluna que diz a idade, na verdade, da pessoa que está respondendo a pesquisa. Qual sexo, no caso gênero da pessoa, que é um campo texto. BMI, que é o índice de massa basal, que é um cálculo que é feito com base em altura e peso. Se essa pessoa tem filhos, então é um inteiro, pode ter 0, pode ter 1, 2, 3 filhos. Um campo que identifica se a pessoa é ou não fumante, Smoker. Então está lá com os valores, depois a gente vai olhar os valores como estão. Região, qual a região que essa pessoa mora. Foi feita a pesquisa em várias regiões. E o Medical Charges, que é a nossa variável Target, que é quanto a pessoa gastou de saúde no último ano, que é uma variável Float. Então olhando aqui, vamos dizer, de bate e pronto, não parece ter nenhuma discrepância de tipo de dado. Mas a gente provavelmente vai ter que fazer algumas transformações, porque a gente está tratando de um algoritmo de regressão, que precisa ter como input apenas variáveis numéricas, variáveis quantitativas. E é isso que a gente vai fazer. Um outro ponto que a gente vai explorar aqui, o que a gente chama de Feature Engineering, que a gente fez também no projeto prático anterior, é avaliar se existem valores ausentes, mas aparentemente aqui a gente fala 1.338 entradas, todas as colunas aqui estão com NOT NULL, ou seja, aparentemente não tem colunas nulas, valores nulos, mas a gente vai checar isso no Feature Engineering. Iniciamos aqui esse novo projeto, fizemos a importação das bibliotecas e carregamos os dados, e já demos uma boa olhada aqui na estrutura desses dados, e a gente já pode começar a trabalhar com ele. Então vejo vocês no próximo vídeo.