WEBVTT Começando agora o nosso projeto de classificação, a gente vai trabalhar no mesmo dataset, que é aquele dataset da edtech, dos leads da edtech, só que agora com a abordagem de Adaptative Boosting. Para a gente começar aqui, vamos criar um novo arquivo, vou chamar esse arquivo aqui classification.ipynb Já vou selecionar aqui o kernel, eu estou com as bibliotecas, eu estou na mesma pasta, na verdade aqui eu estou usando a mesma pasta para todos os projetos que nós vamos fazer aqui, então não há necessidade, nesse caso, de instalar as bibliotecas de novo, a não ser que você queira separar o diretório, mas essa aqui eu estou usando porque eu estou usando os mesmos datasets e vai ficar tudo dentro do mesmo módulo, então aqui eu já tenho o meu ambiente setado e já criei o notebook. Uma coisa que eu já vou fazer aqui para ganhar um pouco de tempo, porque não tem necessidade, haja vista que a maioria das bibliotecas que nós vamos usar aqui são as mesmas do módulo anterior, então vou pegar aqui o bagging de classificação, que foi o projeto anterior que a gente fez, e vou usar essa parte aqui já das bibliotecas. Algumas bibliotecas a gente não vai usar, então já vou mexer nisso, então já vou colocar aqui, copiar, colar. Então a gente não, como a gente não vai fazer DA, a gente não precisa aqui do status, para fazer o quadrado. Em termos de bibliotecas, o que que a gente vai ter que fazer? A gente está trabalhando agora com um Boost, e mais especificamente com o Adaboost. Então para a gente trabalhar com o Adaboost, a gente vai ter que importar ele. Então a gente vai fazer o seguinte, a gente vai usar o mesmo módulo dentro do sklearn, que se chama ensemble, mas em vez de chamar o bagging classifier, a gente vai chamar um outro módulo chamado Adaboost classifier. E nós vamos usar como weak learner, ou algoritmo base, a regressão logística, que foi aquela que a gente usou também no exemplo do bagging, para a gente ter uma mesma base de parâmetro em termos de uso de modelo. Vamos fazer isso, importar, carregar as bibliotecas, e aí agora a gente vai começar a carregar os dados. Então vamos colocar aqui, carregar os dados. A primeira coisa é, os dados que nós vamos carregar, não serão mais os dados brutos, que a gente carregou no experimento, no projeto anterior. A gente salvou aquele data set, já com os dados limpos, com os dados tratados, e a gente já vai carregar essa versão. Então vou colocar aqui, defleads, o nome do data frame, pd.read.csv, e aí eu vou chamar o .datasets, que está dentro do meu diretório, e aí eu salvei o arquivo, como leadscleaneds.csv. Vou olhar aqui na pasta, leadscleaneds, a versão que já foi tratada para a gente trabalhar. Carrego, aí eu posso vir aqui, mostrar as primeiras linhas, defleads.read10, e ele mostra aqui, então, algumas variáveis que estavam como 0 e 1, já foram convertidas, que estavam como true ou false, uma série de colunas foram removidas, por conta de terem valores ausentes, muitos valores ausentes. Já fizemos um tratamento também de substituir algumas colunas, os valores de algumas colunas. Então a gente já está com essa base tratada. Podemos também mostrar as últimas linhas, defleads.tail10, é a mesma coisa. Podemos dar uma olhada novamente, para recapitular, na estrutura do dataframe, como está agora, na estrutura do dataset, defleads.info. Então temos aqui, 17 colunas, a gente tinha uma quantidade bem maior de colunas no início, e 9.074 instâncias ou registros dentro dessa base. Então já carregamos aqui a base de dados, já temos ela já pronta, para a gente começar a preparar e colocar dentro do algoritmo. Então, vejo vocês no próximo vídeo.