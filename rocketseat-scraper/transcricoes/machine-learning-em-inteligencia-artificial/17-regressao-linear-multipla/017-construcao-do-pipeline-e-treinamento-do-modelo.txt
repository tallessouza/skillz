WEBVTT Com o nosso pré-processador criado, com todas as transformações já definidas, a gente vai criar agora o nosso Pipeline principal. Esse Pipeline principal vai ser composto do quê? De um pré-processamento, que é transformar todas as colunas no formato que a gente precisa, transformar no sentido de inputar dados ausentes, fazer transformações numéricas ou de hot encode, original encode, e depois, e treinar propriamente dito um modelo de regressão linear. Então como é que a gente faz isso dentro do pipeline? Então aqui, criando o pipeline principal, que ele é formado do pré-processamento mais treinamento do modelo. Então aqui a gente vai chamar de modelo, de model, a gente pode chamar de model de regressão, E a gente de novo vai chamar o objeto Pipeline. Então, esse objeto Pipeline vai ter dois steps. Step número um. A gente vai dar um nome de pré-processo, só um apelido. E qual vai ser... O que a gente vai executar? A gente vai executar esse Column Transformer aqui. Então, a gente referência aqui, pré-processo. E qual é o segundo passo que a gente vai fazer? Bom, o segundo passo é fazer o treinamento do modelo. Então, a gente vai chamar isso de regressor, né, poderia até ser modelo. E aqui a gente vai chamar diretamente o nosso modelo, nosso modelo de regressão linear, linear regression. Então, a gente cria esse pipeline, E a partir daqui, desse modelo, a gente já pode direto treinar o modelo. Então aqui, treinar modelo. É muito legal, porque quando você treina o modelo, ele já vai fazer todos os passos de uma vez só. Então, como é que a gente treina o modelo? Da mesma forma que a gente treinou no módulo anterior. A gente dá um model, regressão, ponto fit, treinar, informa pra ele o conjunto de treinamento, x e y e ele treinou, ou seja, ele rodou a pipeline completa. E aí como é que ele roda a pipeline? Eu mostro, eu tenho uma pipeline, vou até abrir aqui tá, essa pipeline tem dois steps, um pré-processor Esse preprocessor tem um column transform numérico que mudou, que fez um standard scale input nessas três colunas, um categorical que fez nessas duas colunas e um original que trabalhou nessa coluna. E depois de tudo isso ele fez a regressão linear, e isso ele mostra em um formato de código, basicamente como foi programado. E aí você pode ir quebrando essa estrutura, então o preprocessor, as variáveis numéricas, o que ele fez aqui, ele fez um simple inputer com mediana, então você consegue ver a estrutura do pipeline que ele executou. Então, com isso, a gente já tem o nosso modelo treinado. E a partir daqui a gente pode começar a fazer análise desse modelo. Então, temos o modelo treinado. Podemos começar agora a visualizar as métricas e validar se o nosso modelo está ok. Vejo vocês no próximo vídeo.