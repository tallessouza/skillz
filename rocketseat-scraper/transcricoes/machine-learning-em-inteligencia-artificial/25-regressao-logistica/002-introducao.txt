WEBVTT Olá, Dave. Nesse módulo, a gente vai falar de mais um algoritmo supervisionado, dessa vez, na regressão logística. O objetivo do módulo é apresentar o algoritmo da regressão logística, que ele é usado amplamente para classificação binária e multiclasse, e que entrega, além da classificação, a probabilidade da instância ou do registro pertencer à nossa classe de interesse. Como de costume, a gente vai fazer um projeto prático, desde o EDA, fazer otimização de hiperparâmetros, e dessa vez a gente vai entregar o modelo através de uma API num container Docker. Para a gente dar uma revisada na agenda, a gente vai mostrar o que é o algoritmo de regressão logística. Eu vou falar de mais métricas de classificação e quais a gente vai usar dessa vez nesse projeto e também comentar sobre o projeto prático de regressão logística.