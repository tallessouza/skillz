WEBVTT Fala Dev! Dando continuidade a nossa trilha de inteligência artificial, a gente vai falar de mais um algoritmo não supervisionado, dessa vez um algoritmo da classe das regras de associação, que é o algoritmo Apriori. O objetivo desse modo é apresentar conceitualmente os principais algoritmos de regras de associação, para que possamos descobrir relações ocultas entre itens em grandes bases de dados, e faremos um projeto explorando o algoritmo Apriori, onde faremos o processo completo desde o EDA até a visualização dos resultados. Em termos de agenda, a gente vai primeiro contextualizar o conceitual que são regras de associação, depois nós vamos fazer um passeio pelos principais algoritmos de regras de associação, quais são os casos de uso e aplicações dos algoritmos de regras de associação, o que é o algoritmo Apriori, que nós vamos explorar com mais detalhes nesse módulo, como funciona esse algoritmo, quais métricas são geralmente usadas em algoritmos de regras de associação, e vamos falar do projeto que a gente vai executar, o projeto prático desse módulo, para explorar o algoritmo do Apriori. Obrigado.