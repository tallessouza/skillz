WEBVTT Fala, Neve. Agora que a gente já concluiu o nosso modelo, a gente já está preparado para poder entregar esse modelo através de uma aplicação. A gente já fez uma abordagem no módulo anterior de usar o Gradle para entregar, ele interagir com o nosso modelo e assim o usuário poder testar. E a gente também testou, no módulo de regressão linear simples, de entregar simplesmente uma API que poderia ser conectada numa aplicação. Nesse módulo, a gente vai fazer as duas coisas, só que não usando o Gradle. A gente vai usar uma outra biblioteca bastante difundida no mercado para esse fim, que é o Streamlit. Então, a gente vai construir a parte do front-end no Streamlit, e o Streamlit vai acionar a API, que vai estar sendo publicada, que vai estar ativa, e a gente vai construir ela usando o FastAPI, da mesma forma que a gente fez anteriormente no módulo de regressão linear simples, tá? Então, vamos lá. Primeira parte que a gente vai fazer é criar a API. Então, vou criar um novo arquivo aqui chamado API. modelo-salário.py. Aqui, no caso, a gente não vai executar no modo interativo, porque a gente precisa rodar um servidorzinho interno, que é o uvcorn, para instanciar esse código. Então, o que a gente vai fazer aqui? Primeira coisa, a gente vai importar o FastAPI. Vamos importar o uvcorn, para que ele possa subir esse servidorzinho para a gente e a disponibilizar a API. A gente vai usar o Pydantic, que é aquela biblioteca que faz validação de dados. Biblioteca muito boa. a gente vai importar o JobLib para que a gente possa carregar o modelo, e a gente vai importar o Pandas para que a gente possa construir o nosso DataFrame de predição, da mesma forma que a gente fez aqui no modelo salário. Então, a primeira coisa que a gente precisa fazer é criar uma instância do FastAPI. A gente vai chamar essa instância de App e vamos chamar aqui de FastAPI. Vamos instanciar o objeto FastAPI. Depois, a gente vai criar uma classe para que a gente possa fazer a validação dos dados de entrada. Então, a gente vai criar uma classe com os dados de entrada que virão no request da nossa API, no request body. com os tipos esperados. Então, a gente vai criar uma classe aqui chamada RequestBody, que ela é do tipo da classe BaseModel do Pydantic. E a gente vai colocar o quê? Duas variáveis. A gente tem duas variáveis de entrada tempo na empresa, que é uma variável do tipo inteira, e temos uma segunda variável, que é nível na empresa, que também é do tipo inteira. Agora a gente precisa carregar o modelo para realizar a predição. Então, quem vai fazer a predição é a API. O Streamlit só vai chamar essa API. Então, a gente carrega o modelo para realizar a predição. Então, toda a inteligência, vamos dizer assim, está na API. vamos chamar de modelo poli, de polinomial, e a gente vai carregar com job.lib nosso arquivo ./.modelo-salário.pkl Agora, a gente precisa criar uma função que faça, de fato, a predição. Ela vai receber os dados de entrada no request, vai construir um data frame, vai fazer a predição e vai devolver esses valores. Então, def predict. A gente vai receber o request body. A gente vai receber o RequestBody, que é o tipo do dado. Esse tipo de notação, data dois pontos RequestBody, significa que a gente está colocando aqui o que chamamos de um type hint. A gente está definindo que a nossa variável data é do tipo RequestBody. Então, se a gente mandar uma informação que não esteja nesse formato, na hora de chamar a função, ela vai dar erro, porque o formato do dado de entrada está diferente. Então, por isso que é importante o Pydante, que é uma biblioteca bastante interessante, para você garantir, forçar que os dados venham no formato que você precisa. Aí, o que a gente vai fazer? A gente pode até copiar o código que está aqui, que é esse código aqui. A gente vai fazer basicamente a mesma coisa. A gente vai criar um DataFrame. E vamos dar um predict nele. Vamos fazer a predição. Só temos que garantir que a variável é a mesma. Então, aqui a gente vai substituir. Então, é data.tempoNaEmpresa. data.nivel na empresa. Por que? Porque eu tô recebendo uma estrutura de dados do tipo request body que tem dois componentes, tempo na empresa e nível na empresa. Então eu pego data.tempo na empresa, data.nivel na empresa. Preddf, Input Futures, tá correto. E eu faço a predição. Só que a predição tem que retornar em algum lugar. E aqui não é ModelPoly. Nosso modelo chama-se ModeloPoly. Então, a gente coloca aqui ModeloPoly. Então, ele vai retornar pra gente numa variável. A gente vai chamar de yPred, tá bom? Só que lembra que ele vai retornar isso como uma lista, né? Ele vai retornar isso aqui dentro de um array. Eu não quero um array, eu quero esse valor aqui dentro. Então, pra trazer esse valor aqui dentro, eu trago o índice 0 e eu quero trazer esse valor como um float. Eu quero forçar o que o resultado, né? seja um float. Então, para garantir que o retorno disso seja um float, a gente coloca stype float. E eu preciso retornar isso. Então, eu venho aqui e coloco return. Essa função tem que retornar alguma coisa. E eu vou retornar o que? Eu vou retornar uma chave e valor. Então, eu vou retornar. A chave vai chamar salário em reais. E para retornar corretamente, eu preciso converter essa predição numa lista, para que eu consiga capturar isso corretamente dentro do Streamlit. Então, eu coloco yPred to List e ele vai converter esse valor. Falta apenas uma coisa aqui. Para que eu possa expor essa função do Python como uma API, eu preciso pôr o que a gente chama de um decorador no Python. Então, eu preciso decorar essa função. Então, eu vou decorar essa função da seguinte forma. Eu vou chamar app, que é o nome daqui da aplicação, que eu instanciei do FastAPI, a instância. Qual é o tipo de API que eu vou criar? Uma API REST. A gente vai criar uma API do tipo POST. Então, eu coloco qual o verbo do REST, que é POST, e qual vai ser a URL que vai ser chamada. Então, vai ter lá o endereço, dois pontos, a porta, barra, um verbo. Qual é este verbo? De que forma eu vou chamar essa função? Através do método barra predict. Então, com esse decorator, eu consigo fazer a chamada dessa API. Então, salvei esse código. E aí, como é que a gente vai testar esse código? A gente vai abrir aqui o terminal.