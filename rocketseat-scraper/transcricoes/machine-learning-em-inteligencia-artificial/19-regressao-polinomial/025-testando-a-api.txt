WEBVTT Abrindo aqui o meu terminal, para que a gente possa agora fazer o teste da API que a gente construiu usando o FastAPI. Então, ele está aqui carregando o meu virtual environment. E a gente vai subir o servidorzinho, o servidor do vCore, para responder às chamadas de API que a gente está expondo com esse código que a gente construiu. Então, agora a gente pode fazer uvcorn api-modelo-salário.py Na verdade não precisa por .py, é só o nome do arquivo. 2.zap, que é a instância do Vast API que a gente está usando. E reload, caso a gente quiser alterar alguma coisa no código, quando a gente usa o reload, ele, cada alteração que a gente salva, ele recarrega o nosso servidorzinho. Então, vou dar um enter aqui. e ele já está aqui com o servidor rodando nessa porta. Então, 127.0.0.1 que é o IP local, porta 8000. Então, se eu vier agora aqui no meu browser e colocar a barra docs, eu entro na página do Swagger, que é aquele ambiente de testes que a gente consegue usar a API que foi exposta e treinar, testar a nossa API. Então, eu tenho aqui a API chamada Predict, a URL que a gente definiu, do tipo post. E esse a gente vê aqui. A gente tem a estrutura da nossa API, então ela tem esses dados de entrada, do tipo JSON, e eu posso testar a minha API. Então, eu posso vir aqui e dar um tryout, Eu posso colocar aqui os valores, por exemplo, 72, 10. E dou um execute. Se tudo estiver ok, ele vai retornar o valor. 4, 808, 03. Deveria ser parecido, deve ser na verdade a mesma coisa que o modelo aqui de salário que está aqui. Então, a gente agora já testou a nossa API. Usamos aqui o Swagger para testar, a gente expôs a API no FastAPI, subimos o servidor do vCore, colocamos os parâmetros dentro de um JSON e mandamos executar, ele retornou para a gente. Se a gente quiser mudar, quiser colocar um outro valor aqui, 80, execute, ele vai chamar a API de novo e vai retornar pra gente um novo valor. Então, a gente conseguiu construir nossa API e nesse tópico a gente testou nossa API. Agora falta construir o front-end que vai interagir com a nossa API. Vejo vocês no próximo vídeo.