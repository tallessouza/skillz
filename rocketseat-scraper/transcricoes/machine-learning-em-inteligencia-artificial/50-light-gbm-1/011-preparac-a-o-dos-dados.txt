WEBVTT Dando sequência agora ao nosso projeto, tendo vista que já terminamos a análise exploratória de dados e não encontramos nenhum padrão muito forte que possa correlacionar as variáveis explicativas ou preditivas com a nossa variável target, a gente vai preparar agora o nosso dataset para rodar o modelo do DLite GBM usando o modelo, o formato de otimização Bayesiana para encontrar os melhores hiperparâmetros. É isso que nós vamos fazer agora. Então, vou colocar aqui o Markdown, preparação dos dados. Então, aqui eu vou separar os Y, então eu vou ter aqui o X, que é fazer duas divisões, na verdade. Uma divisão entre treino, validação e teste. Por que a gente vai fazer isso? Porque no método que a gente vai usar do Light GBM para treinar o modelo, a gente vai usar um modelo que vai interar, um formato de treinamento que vai interar no conjunto de treino, e ele já faz a validação num outro subconjunto, que no nosso caso a gente vai usar um subconjunto de validação. E ainda vamos deixar reservado para o final um conjunto de testes, onde a gente vai pegar o melhor modelo, um modelo com os melhores hiperparâmetros, e vamos fazer a predição em cima desse modelo. Como é que a gente vai fazer esse processo de dividir entre treino, validação e teste? A gente precisa fazer isso em duas partes, o que a gente vai fazer aqui é preparar, entre treino, validação e teste. Então, a gente vai fazer o seguinte. A primeira coisa é o Xtrain, Xtest, Ytrain, X-test, Y-test. Só que em vez de eu colocar o X-test, eu vou fazer o quê? Vou colocar X-val-test e Y-val-test. Então, essa primeira split que eu estou fazendo é um split que vai dividir a parte do treinamento entre treinamento mais uma outra parte, que seria para validação em teste. Então, eu estou deixando 50 parte do treinamento entre treinamento mais uma outra parte, que seria para validação em teste. Então, eu estou deixando 50% para treinamento e 50% para validação em teste. Então, eu estou fazendo o primeiro split. O segundo split vai ser, eu vou pegar esse X-Val e teste e vou separar em duas partes. Uma que vai ser de validação e a outra propriamente dita de teste. Então, vou ficar com 50% para treinamento, 25% para validação e mais 25% para teste. Então, vou colocar aqui xval. Então, xval, xtest, yval, ytest. E aí, eu estou dividindo eles de novo em 50%. Então, pegando os 50 que sobraram daqui, dividindo de novo, vai ficar 25 para a validação, 25 para o teste, e mais os 50% para o treinamento. fizemos aqui a divisão dos dados. Então, criamos aqui o x e y e aí fizemos aqui uma separação entre treino, validação e teste. Temos que fazer isso em duas interações. Primeiro separando o treino e validação teste e depois fazendo a separação desse validação teste em duas partes novamente. Vejo vocês no próximo vídeo.