WEBVTT Dando sequência aqui na construção da nossa API, a gente vai começar a preparar os dados. Então, a primeira coisa é a gente vai receber esses dados e jogar esses dados num DataFrame, tá? Então, aqui é transformar o body que a gente recebeu em um DataFrame. Então, eu vou chamar esse DataFrame de predict, DF, pdDataFrame, vou pegar o body e vou converter ele para um dicionário. Eu uso para isso o ModelDump. Na verdade, eu posso usar o dict, mas o Python está substituindo a função dict, que é dicionário, para ModelDump. Index igual, eu preciso ter uma das colunas, pelo menos como índice, para ele fazer isso. Agora, eu vou ter que incluir a faixa etária. Então, como é que eu vou fazer isso? Eu posso até aproveitar o código, para a gente ganhar tempo, aproveitar o código que está lá, que é o código do bucketing que a gente usou. Quando a gente foi fazer aquele estudo de se a faixa etária tem correlação com a variável target. Então a gente vai fazer o que aqui? A gente só vai adaptar esse código que já está pronto. Aqui a gente vai fazer assim. Os bins e os bins originais a gente vai usar. Esse aqui a gente não precisa usar, porque a gente não está interessado na string, e nem esse. Eu troco isso aqui para cá, e esse aqui para cá. e pronto, ou seja, eu tenho os bins que são como eu vou quebrar os valores, eu tenho os bins ordinais que é o que ele tá esperando receber na feature, tô criando a feature faixa etária nesse predictdf que já recebeu todos os valores do formulário, e tô usando o bins original com o label e o bins para separar aqui em cima da variabilidade. Então tá pronto aqui. E agora eu preciso, de fato, colocar dentro do DataFrame as variáveis, as features que foram selecionadas. Então, aqui eu vou deixar as K melhores features. Então, vou falar aqui, predictDef igual a predictDef Abre, dois colchetes aqui e aqui eu vou pôr a lista de features. Quais são as features selecionadas? Foram essas aqui. Eu abri o notebook e ele falou que as features foram essas. Então essas aqui, todas elas vão constar lá na nossa lista. Então vou colocar aqui, uma por uma. Histórico familiar sobrepeso, Consuma alta caloria com frequência. Consuma alimentos entre refeições. Monitora calorias ingeridas. Nível de atividade física. nível uso tela, transporte caminhada, e por último o faixa etária. Pronto, temos aqui a lista das features. Agora a gente vai fazer o que? A predição. Então, predizer, a classificação. Se é obesidade, não é obesidade. Então, meu yPred é igual a modeloobesidade.predict. No meu predictDF, eu retorno o primeiro valor que eu receber como tipo inteiro, 0,1. E eu vou retornar esse valor, tá? Retornar valor na API. Então, eu vou dar um return JSONify, que ele retorna esse cara como um JSON. Coloco aqui que o valor que ele vai retornar vai ser a variável, né? Obesidade. dois pontos, y pred, toList, ele precisa retornar isso aqui como uma lista de valores. E agora eu preciso executar o flask, executar a aplicação flask para que ela fique pronta para receber requisições. Então eu venho aqui e faço if, o name, for main, que é o código interno principal do Python que é executado. Eu dou um app run port 5000 debug igual a true, para eu poder ver o código sendo executado. Vou salvar esse código, deixa eu ver se tem alguma coisa aqui. Eu vou salvar esse código e a gente vai, no próximo vídeo, testar esse código. E testar esse código não é só rodar ele, é rodar e a gente ter uma forma de chamar, de fazer uma chamada de API. para testar isso. Então vai ser legal porque eu vou mostrar uma forma de fazer isso dentro do próprio VS Code, como testar a API dentro do VS Code, para que a gente consiga ver dentro do próprio VS Code a resposta acionando a nossa API. Então vejo vocês no próximo vídeo.