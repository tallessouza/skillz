WEBVTT Agora, para fechar a parte do teste dessa API, eu vou abrir aqui uma sessão do terminal. Vou ativar meu ambiente virtual para garantir que as bibliotecas estejam funcionando, e vou executar python, vou executar aqui python api modelo obesidade pai, ou seja, vou executar ele vai subir um servidorzinho na porta 5000 como deixa que coloquei aqui a porta 5000 né e vai ficar esperando requisições então não entra aqui se não tiver nada nenhum problema no código não teve nenhum problema no código ele tá aqui já esperando para responder na porta 5000 tá Como é que agora eu testo essa API? E é para isso que a gente usa esse JSON. Esse JSON tem o quê? Uma estrutura com todas as variáveis que a gente precisa. Essas variáveis vão ser colocadas dentro do... Vão ser validadas pelo Pydantic, por essa classe. Vamos transformar esses dados num DataFrame, fazer aqui o bucketing, separar as oito melhores features e fazer a predição e ele vai retornar nessa chave aqui, obesidade, se é zero ou se é um. Então, existe uma extensão do VS Code, até a minha não está instalada aqui, que é o Thunderclient. Vou instalar ele aqui. Bem simples de instalar. E ele é um REST API Client para o VS Code. Existem outros REST API Client, mas esse foi o que eu gostei mais de usar. Então, ele já abre aqui uma janela, uma abi aqui, na verdade, e eu posso dar aqui um new request. O que o new request faz? Você cria um request que você quer executar para testar a sua variável, testar a sua API. Então, o que eu vou fazer? A minha API que eu quero testar é uma API do tipo verbo post e ela está em localhost 5000 endpoint chamado predict. Se eu fizer isso aqui, ele já dá um 415 porque, ou seja, ele não conseguiu ler um JSON porque eu não mandei nada de JSON pra ele, né? Mandei um payload vazio. Onde que eu coloco o payload? Eu coloco aqui em body. E aí eu posso pôr o bar em vários formatos. JSON, XML, texto, form, GraphQL, binário. Eu vou usar JSON. E aí que eu vou usar esse arquivo. Para não ter que digitar ele, tá? Então, eu vou pegar esse JSON, que estão todas as variáveis do formulário, preencher aqui com alguns dados. Vou colocar aqui. Ao colocar ele aqui, dá um send, veja que ele retornou 200. Significa que ele chamou a API e retornou. E ele traz para nós aqui a resposta. Então, nesse caso aqui, obesidade igual a zero. Então, para essa pessoa, com essas características, deu zero. Vamos tentar brincar um pouco aqui? Vamos. Vamos imaginar que a pessoa que colocou que consome alimentos entre refeições, que a gente viu que é uma coisa que Lembrando que aqui estão todas as variáveis, não estão só as oito, que é uma coisa que influencia bastante, foi uma das oito variáveis que ele selecionou. Se eu coloco essa variável como 1, será que muda o resultado? Opa, já mudou. Então o modelo já percebeu que para essa característica aqui dessa pessoa, uma pessoa de 48 anos, masculino, com esses comportamentos, Quando ela coloca que ela consome alimentos entre refeições, ela tem obesidade 1. Aí se tirar esse 0 e colocar uma outra variável, por exemplo, aqui já está com nível de uso de tela como 2. Mas vamos imaginar que ela histórico familiar já está com sobrepeso. Então já tem várias coisas aqui, monitora calorias ingeridas, se ela coloca que é 1 aqui, continua como zero. Então, ou seja, aqui o que fez mudar é quando ela informa que ela consome alimentos entre refeições. Ela já está com os outros no nível alto, nível de uso de tela. Se ela colocar nível de uso de tela zero, por exemplo, não muda nada. Então, você pode ficar brincando aqui com os parâmetros para ver qual o resultado que vai dar de obesidade. Já está chamando a API, toda vez que ele chama e dá certo, ele dá o 200 e ele faz da mesma forma que o Fetch API. Toda vez que você faz uma alteração no código do Python aqui, ele dá um refresh no servidor, para que ele pegue o código novo e disponibilize a API com esse código novo. Então, com isso, a gente fecha essa trilha, conclui aqui disponibilizando o modelo como API, e vejo vocês no próximo módulo.