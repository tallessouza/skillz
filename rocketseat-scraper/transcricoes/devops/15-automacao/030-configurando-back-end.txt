WEBVTT Show demais! Agora que nós já temos ali o S3 bucket, nós precisamos falar para o terraform que a gente quer usar o S3 como o storage ali para o nosso estado. Importante ressaltar que quando a gente vai trabalhar com terraform, se não tiver nenhuma inferência, ele vai procurar o estado no seu local, ok? Que inclusive é um back-end type, a gente vai aqui configurar o back-end, ok? Olhando para o S3. Se você não faz nada, ele vai olhar para o local, que é basicamente a raiz do projeto. Por isso que lá no git actions ele está bugado, porque ele não tem nenhuma referência. E o terraform funciona assim, ele vai olhar para o tf, para o hcl e vai comparar com o estado. Se houver alguma diferença, alguma dissonância, ele vai tentar criar. Caso tenha uma consonância, ele não vai fazer nada, é o famoso no change, uma consonância ali em sua totalidade. Como ali ele não encontra o estado, ele olha para o código, não tem estado, então ele entre aspas cria e tem a politify pipeline, só que quando ele vai tentar criar o recurso, o recurso na verdade já existe, ok? E é justamente isso, porque nós não estamos inferindo de fato o que nós vamos trabalhar como back-end. Então ele entende que é o local, ele vai tentar procurar ali no próprio path. E ele não encontra e acaba dando problema. Então nós vamos falar para o terraform que nós queremos usar o S3. Como é um aspecto configurativo, ele vai ficar aqui na tag de abertura do próprio terraform, onde tem o required providers, que nós já passamos aqui como aws, e aí abaixo, fora do required providers, nós podemos passar o back-end. E aí o back-end ele basicamente tem os types, ok? Eu até deixei aqui na documentação, para a gente ter uma ideia de quais types, de quais back-end types são comportados, ok? Inclusive aqui, by default terraform uses a back-end carry local, que vai olhar ali para o arquivo em disco. Mas nós podemos aqui ter várias questões, o Azure, GCS, o Cloud, o S3, até mesmo o Kubernetes, caso você trabalhe ali com o gerenciamento do Kubernetes dentro do terraform, que inclusive é um tópico do próximo módulo, para armazenamento, por exemplo, de kube-config, daria para fazer aqui. E o próprio S3, que está aqui, bonitinho. Então tem o bucket, key e a region. Então o nome do seu bucket, o caminho, o path dentro do bucket para conseguir chegar no tal do estado e a região ali que está o tal do bucket. Então o nosso caso aqui é na US East 2, por exemplo, ok? Então vamos configurar uma configuração relativamente tranquila, ok? Então back-end aqui vamos passar como S3, vamos passar aqui o nome do bucket, ok? Que é o que nós acabamos de criar, Rocketseat IAC, e aqui dentro do bucket, que basicamente é o caminho. E que será como? Podemos criar uma pasta, ok? State barra terraform.tfstate, ok? Basicamente o mesmo nome do arquivo. Qual que é a estrutura aqui? Teremos uma pasta, ok? Um diretório, state, e lá dentro teremos o tfstate, perfeito? E a nossa região, que no caso aqui é a US East 2, perfeito? Então basicamente com isso tudo aqui nós já temos ali o back-end configurado. Qual que seria a próxima etapa? A próxima etapa é pegar o tfstate e jogar lá na AWS. Como é que nós vamos fazer isso? A gente vai basicamente criar uma pasta, state, e jogar lá dentro do tfstate? Não, ok? Isso funcionaria? Até que sim, mas a boa prática mesmo é o seguinte, como nós já fizemos aqui o back-end, a configuração, a gente vai voltar no nosso terminal ainda, na nossa máquina, e vamos colocar terraform init. Toda vez que a gente mexe na tag do terraform, nós precisamos reinicializar o terraform, ok? Definição de módulo, por exemplo, e tudo mais. Como nós o fizemos, nós vamos rodar aqui, terraform init. E olha só que interessante, ele vai inicializar o back-end e olha, ele já detectou, olha isso. Você gostaria de copiar o estado existente para um novo back-end? Então nós já podemos resolver aqui o problema, porque ele está falando que a gente está no local e ele identificou o S3 aqui, então podemos dar um yes, que já resolve a nossa vida, olha só que fantástico. Então ele já configurou para a gente o estado, ok? E aí se nós voltarmos aqui no nosso S3, olha só como é que vai acontecer, ele já tem aqui o estado, então state e aqui o tfstate, que é o mesmo arquivo do nosso ambiente local aqui, beleza? Então, basicamente é isso. Um outro ponto que nós também ainda vamos precisar fazer localmente, é aqui no IAM, liberar as permissões do S3, tá? Então aqui, no nosso tfrole, nós vamos criar um outro statement, só para garantir, e a gente passa como S3 aqui, muda o SID aqui, o statement ID, e perfeito, porque agora nós também estamos criando ali o recurso S3, e em tempo de pipeline eu preciso recuperar o estado, ok? Então eu preciso ter um read ali no nosso banco, perdão, no nosso banco é ótimo, no nosso storage, nosso bucket, para conseguir ler esse estado e comparar com o código ali que nós estamos fazendo. Show? Então, basicamente é isso, só para não estender muito, próxima aula a gente faz aqui o commit e roda ali a nossa pipeline novamente, show? É isso, um abraço.