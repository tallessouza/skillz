WEBVTT Nessa aula, o que a gente vai fazer é realmente salvar os usuários, conforme a gente estiver listando e criando novos usuários, dentro da memória da nossa aplicação. O que eu quero dizer com memória é que, no Node, como o nosso projeto fica executando até que alguém venha aqui e pare o projeto usando um Ctrl-C, Tudo o que eu declarar de variável dentro desse projeto vai ficar salvo em memória. A gente chama isso de um conceito de aplicação chamada stateful, diferente de uma aplicação stateless. A diferença de uma aplicação stateful para uma aplicação stateless é que a aplicação Stateful sempre vai ter algum tipo de informação sendo guardada em memória, ou seja, a aplicação depende da memória, depende de informações que são salvas em memória para que ela continue funcionando. E, a partir do momento que essa aplicação for derrubada e perdeu seus dados em memória, ela pode funcionar de uma maneira diferente do que ela estava funcionando antes. Enquanto uma aplicação stateless, onde ela não salva nada em memória, geralmente ela vai salvar essas informações em dispositivos externos, como banco de dados, arquivos de texto ou qualquer coisa assim, independente se a gente parar a aplicação e rodá-la de novo, os dados, arquivos ou qualquer tipo de funcionamento vai se manter igual, não vai ter qualquer tipo de problema. Então, por enquanto, a gente vai criar uma aplicação Stateful, onde os dados da aplicação são armazenados localmente em memória. Eu vou criar uma constante aqui dentro chamada users, que vai ser simplesmente um array vazio. Deixa eu executar aqui o meu projeto Node de novo. Pronto, vou deixar ele executando. e agora a gente vai fazer o seguinte aqui dentro da minha rota de criação de usuário eu vou adicionar um novo usuário dentro desse array aqui então eu vou fazer um users.push e aqui a gente vai colocar o nosso nosso usuário digamos assim e esse usuário deixa eu pensar quais dados que eu vou só botar para ele aqui né claro que vou botar dados fictícios ainda né mas eu vou salvar ele no formato de objeto com um nome, então eu vou botar aqui John Doe, que seria o fulano, e um e-mail johndoe.example.com e aqui eu posso colocar um id para ele também, como por exemplo id 1, mais pra frente a gente pode ir incrementando esse id aos poucos E aí, agora, aqui na rota de listagem, ao invés de eu devolver um texto, listagem de usuários, eu vou tentar devolver daqui de dentro os meus usuários. Aqui, simplesmente, o meu array de usuários. Vamos ver se isso aqui funciona. Vou salvar. A gente vai executar aqui de novo http post localhost 3333 barra users. Vou dar um enter aqui agora. a gente pode ver que ele mostrou criação de usuário porque a gente continuou mostrando isso aqui aqui dentro e agora se eu executar http get localhost 33 33 barra users a gente vai ver que opa deu erro connection aborted vamos dar uma lida the chunk argument must be of type string or instance of buffer tá o que acontece né Quando a gente cria um servidor em Node e devolve uma resposta aqui, essa resposta que vai para o front-end, ela não pode ser um array. Ela precisa ter especificamente três formatos no máximo. Está vendo aqui que ele falou que a gente mandou um array, mas ele pode ser uma string, um buffer, que logo mais a gente vai falar o que é esse buffer aqui, ou um uint array, 8 array, que também tem a ver com o buffer, que é para a parte de streaming dentro do Node. Mas, então, como a gente precisa devolver especificamente agora uma string, e isso aqui é um array, para converter um array para string, a gente pode utilizar uma estrutura de dados muito conhecida para quem já trabalha com front-end e back-end, que é o JSON. O JSON é uma sigla para JavaScript Object Notation, e ele é uma estrutura de dados muito comum de ser utilizada na transição de dados entre front-end e back-end, e também entre back-end e outro back-end. O JSON é uma estrutura de dados que, como o próprio nome já diz, se assemelha muito a como as estruturas de dados são dentro do JavaScript. Então, com o JSON, a gente consegue representar arrays, objetos, tipos primitivos de dados como strings, booleanos, inteiros, floats e por aí vai e tudo isso dentro de uma string, ou seja, a gente consegue transitar dados em mais diferentes tipos entre várias aplicações e aqui nesse caso então a gente vai simplesmente aqui dentro do res.end escrever json. string fi, veja que json é uma variável global aqui do javascript, eu posso usar ela em qualquer lugar, tanto no front-end, lá no browser, quanto aqui dentro do node, no back-end, e dentro para essa função json string fi, nós vamos enviar users. Vou salvar aqui agora, a gente pode ver que o server já restartou, se eu tentar executar um get nessa rota users, a gente vai ver que retornou vazio. Por quê? Porque como eu estou salvando os usuários em memória, toda vez que o meu servidor restartar, a memória é jogada fora. Por isso, a minha aplicação stateful é problemática caso eu a coloque em produção, porque a gente não pode perder os dados dos usuários de jeito nenhum. Por isso que a gente usa mecanismos como banco de dados. Mais pra frente a gente vai falar sobre isso. mas agora então eu vou criar novamente um usuário executando uma requisição post dentro de localhost 3333 barra users pronto criou o usuário e agora eu vou executar a requisição get e agora sim veja que ele devolveu pra gente aqui agora um texto contendo a nossa lista de usuário Porém, como que o nosso front-end vai saber que o nosso back-end devolveu uma resposta no formato de JSON? E aí que vem os cabeçalhos. Cabeçalhos tanto da requisição quanto da resposta são metadados e metadados são quase que informações para que tanto o back-end quanto o front-end saiba lidar com aquela requisição da melhor forma então cabeçalhos nada mais são do que informações adicionais que não tem a ver com o dado retornado do back-end para o front-end mas sim de como aquele dado pode ser interpretado pelo front-end. Nesse caso, antes do res.end eu vou escrever .setheader, ou seja, eu quero setar um cabeçalho, e aqui ele recebe dois parâmetros. Primeiro, o nome do cabeçalho e aqui existem vários cabeçalhos comuns para a gente ter em requisições. Então, se a gente procurar aqui headers na documentação da Mozilla, a gente vai ver que existem vários... deixa eu... aqui, Specifications... vários cabeçalhos... cadê, cadê, cadê? Não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, não, Eu nem vou entrar aqui porque são muitos tipos de cabeçalho, na verdade a gente pode dar o nome que a gente quiser, mas existem alguns cabeçalhos que são padrões da gente utilizar em APIs. O que a gente quer utilizar aqui é o content-type, que eu falo basicamente qual é o tipo de conteúdo que eu estou retornando daqui de dentro. Como a gente está usando o tipo JSON, esse tipo especificamente a gente chama de Application barra JSON. Eu vou salvar isso aqui agora, a minha aplicação vai restartar, eu vou criar novamente um usuário executando http post, E agora quando eu fazer o HTTP GET, veja que aqui ó, tá vendo que ele já mostrou no meu terminal a informação de uma maneira diferente? Antes ele tinha mostrado, olha só, como é que ele tinha mostrado? Um texto, literalmente um texto, né? Tudo jogado um do lado do outro. Agora... O HTTP aqui, o HTTPI, ele já entendeu automaticamente que isso aqui é um JSON e converteu isso pra gente numa estrutura de dados, ou seja, num array, mostrou o objeto certinho aqui dentro, já mostrou aqui dentro o nosso content-type, application, barra JSON. E os headers, como eu falei, esses metadados, eles podem ser enviados tanto do front-end para o back-end, quando eu estou fazendo uma requisição. Então, por exemplo, aqui da requisição eu posso obter dados dos headers simplesmente acessando rec.headers e assim eu vou obter todos os metadados da requisição. Mais pra frente a gente vai entender pra que que eu posso utilizar esses metadados. E eu posso também, então, enviar headers de volta. na minha resposta, ou seja, os headers que eu estou setando aqui com o has eles são enviados no caminho inverso do back-end para o front-end na hora que eu estou devolvendo uma resposta ao meu front-end que fez a requisição para o back-end.