WEBVTT Nessa aula, o que a gente vai fazer é persistir o nosso banco de dados aqui nos arquivos físicos. Assim, toda vez que eu fizer qualquer modificação aqui nessa nossa classe, nessa nossa propriedade de banco de dados, eu salvo isso para quando eu reiniciar minha aplicação, eu ter esses dados disponíveis. Para trabalhar com arquivos físicos aqui dentro do Node, a gente precisa trabalhar com o módulo interno do Node de File System. Então, escrevendo aqui import nodefs, eu vou ver que eu tenho dois módulos, fs e fs-promises. A diferença deles é simples, né? O fsPromises eu posso trabalhar com o novo formato de assincronismo do JavaScript, que são promises. Então, consigo usar aquela sintaxe de .then, .cat, .async, .await. Enquanto o NodeFS, ele tem os mesmos métodos, porém com o padrão de assincronismo um pouquinho mais antigo, mais feinho, digamos assim, que é o callback, que é quando a gente passa aquela função por parâmetro para conseguir saber quando alguma função terminou de ser executada. Claro, eu particularmente prefiro usar promises sempre que possível e você também confia em mim. A única diferença é que no NodeFS Promises eu não tenho os métodos de streaming. Então, se você for lidar com stream em arquivos, ou seja, ler ou escrever arquivos por partes, você não pode utilizar FS Promises, porque não tem createReadStream, nem nada disso. Esses métodos estão disponíveis apenas aqui dentro do NodeFS tradicional nativo. Então agora, fsPromises, a gente vai importar o módulo por completo aqui dentro, e eu vou começar criando aqui um método chamado persist, e esse método aqui é o que vai escrever o nosso banco de dados num arquivo físico. Esse método aqui vai ser chamado toda vez que eu inserir uma nova informação no banco de dados, após inserir, claro, para que toda vez que eu insira um novo registro, então eu salve isso dentro aqui dos meus arquivos físicos. Aqui dentro do método, eu vou usar o fs.writeFile, que o próprio nome já diz o que é, passo o nome do arquivo, aqui como eu vou estar salvando um objeto, uma informação que é primitiva do JavaScript, um valor que é facilmente tratado pelo JavaScript, eu vou salvar isso aqui como um JSON, vai ser mais fácil eu ler e escrever nesses dados. e aqui eu vou usar o json.stringfy para converter esse nosso banco de dados aqui em uma estrutura JSON, porque o writefile só aceita que eu envie para ele uma string, então eu não posso enviar um objeto, um array ou qualquer coisa assim, eu preciso converter esse objeto e array em uma string, por isso o json.stringfy vai ser o melhor método para isso. Salvando isso aqui agora, executando o nosso servidor de novo, a gente vem aqui no Insomnia, cria o usuário e eu vou ver que o db.json foi criado aqui certinho com os dados do nosso banco de dados ali dentro. O único ponto aqui que a gente pode perceber é que o db.json foi criado aqui na raiz do nosso projeto. Por mais que o database.js esteja dentro da pasta source, e eu referenciei aqui db.json, o db foi criado na raiz, né? Por que isso? Porque o Node, por padrão, quando eu passo caminhos aqui, seja para o writefile ou qualquer outra função, ele sempre vai levar em conta o local onde eu estou executando a minha aplicação como sendo o local onde ele vai criar os arquivos, onde ele vai se referenciar aqui nos caminhos que eu passo. Então, se eu tivesse, por exemplo, dentro da pasta source e executasse o nosso servidor, veja que agora, quando eu criar o usuário, ele vai criar o db.json dentro da pasta source. Por quê? Porque agora o local que eu estou executando a minha aplicação é diferente do local que eu estava executando a minha aplicação anteriormente. Deixa eu parar de executar a aplicação, voltar para a pasta original aqui, vou deletar o db.json, deletar aqui esse db.json também, E o que a gente vai fazer? Tem várias formas de eu lidar com caminhos aqui dentro do Node e a forma mais atual da gente lidar com caminhos dentro do Node é a seguinte forma. Antigamente, até se você já trabalhou com o Node anteriormente, você sabe que existiam, existem ainda, dependendo do caso, funções com o nome, por exemplo, de your name, que ela retorna o diretório atual do arquivo que eu chamo ela. Só que, a partir do momento que você começa a utilizar aqui no Node o type module, os ES modules, essa função your name e tanto a file name, elas não existem mais. Então, mudou um pouquinho a forma da gente referenciar qual que é o local que o nosso arquivo está executando, beleza? Quando eu uso o type module aqui no nosso projeto, perfeito? Se eu não estou utilizando, aí a gente usa o your name mesmo, local é a forma de eu referenciar porém aqui agora a gente vai usar uma outra uma outra variável global que existe aqui dentro do node que é o import.meta se eu uso o import.meta.url veja que se eu salvo isso aqui agora ele me retorna exatamente o caminho para o meu arquivo database olha só retornou exatamente o caminho inteiro do meu computador até chegar no arquivo database e agora o que eu posso utilizar também é um construtor né e aí vamos tirar esse console log eu vou chamar aqui de database path eu posso usar uma classe interna do node chamada url e para essa classe aqui eu posso enviar dois parâmetros primeiro o nome do arquivo que eu quero no final db.json e no final o caminho relativo aonde eu quero criar esse arquivo. Então eu posso passar o nosso import.meta.url como um segundo parâmetro. Deixa eu dar um console.log aqui pra gente visualizar o que está sendo retornado aqui dentro. Então olha só, database.path, eu salvo e veja que agora ele retornou uma classe url e dentro dela eu tenho esse pathName. Veja que agora eu tenho todo o caminho da minha aplicação, inclusive a pasta source que é onde está o meu arquivo database.js e lá no final eu tenho o db.json agora que foi o que eu passei aqui no primeiro parâmetro só que o mais interessante é que aqui eu posso pensar que isso aqui é como se fosse um comando ali no meu shell como se fosse um cd tudo que eu passo aqui é meio que ele vai fazer se eu botar um ponto ponto barra olha que interessante se eu boto aqui agora ponto ponto barra db.json e eu salvo isso aqui agora, veja que não existe mais a pasta source aqui. É como se ele tivesse voltado uma pasta e aí colocado o db.json que é o nome do nosso arquivo. Interessante, né? O mais interessante é que agora eu posso passar esse databasePath aqui como primeiro parâmetro do fs.writeFile. Eu salvo isso aqui agora e pronto. Agora, quando eu crio o meu usuário, ele criou o db.json lá na raiz, e agora, mesmo que eu acesse a pasta source e execute o meu servidor daqui de dentro, e eu crio um usuário, veja que o resultado é o mesmo. Ele vai aqui, eu posso até deletar o arquivo, e ele vai criar de novo no mesmo local. Ou seja, agora, a localização do nosso arquivo de banco de dados, ela está relativa ao arquivo database.js, e não mais um valor relativo aonde o script do Node está sendo executado. Então, vamos voltar aqui na pasta original. E agora, para finalizar nossa aula, a gente já está persistindo os dados do banco de dados, porém, a gente não está recuperando esses dados quando a nossa aplicação inicializa. Então, vou criar aqui um método construtor, que vai ser executado assim que esse banco de dados aqui for instanciado. E aqui dentro, a gente vai usar o fs.readfile, passando o database databasePath agora para ele ler esse arquivo. Aqui como segundo parâmetro eu passo qual encoding eu estou utilizando, por padrão geralmente a gente usa o tf8. E aqui eu vou dar um ponto then, porque isso aqui é uma promise, eu estou utilizando fsPromises, ou seja, quando ele terminar de ler o arquivo por completo, eu vou pegar os dados desse arquivo aqui e eu vou salvar dentro do meu banco de dados, list.database é igual a json.parse agora né, porque antes eu escrevi, eu usei o Stringify para escrever os dados e agora para parciar os dados eu vou usar o json.parse o que pode acontecer aqui é esse arquivo não existir e aí tudo bem né, eu teria que fazer aqui um .cat para fazer a tratativa No caso desse arquivo aqui não existir, o que eu vou fazer aqui agora é chamar o método persist só pra ele persistir, criar o arquivo mesmo que vazio. Então, olha só o que vai acontecer. Deixa eu parar de executar minha aplicação, já não está executando. Vou apagar o arquivo JSON, vou salvar esse arquivo aqui. Quando eu executar o npm rundev, ele vai verificar que o arquivo não existe ainda, vai chamar o persist e pronto, já criou o arquivo vazio. Então, a gente não corre o risco desse arquivo estar vazio depois, quando a gente for trabalhar com ele. E agora, olha que interessante, eu vou criar um novo usuário. Se eu for ver no meu db.json, tá lá, usuário criado. Vou restartar minha aplicação, então, olha só, fecho a aplicação, rodo ela de novo, e agora, quando eu venho na listagem de usuários, e listo, pronto, meu usuário continua lá. Ou seja, eu estou utilizando o arquivo físico aqui, um banco de dados em JSON, a simulação de um banco de dados, um banco de dados fictício, para persistir os meus usuários aqui dentro.