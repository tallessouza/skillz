WEBVTT Bora lá, a primeira coisa que a gente vai fazer aqui na nossa aplicação para já deixar ela pronta para as próximas aulas, para os próximos passos que a gente vai ter aqui, é instalar o SLint para já deixar o nosso projeto com a padronização. Então, eu vou começar instalando tanto o SLint quanto a configuração da Rocketseat, como dependência de desenvolvimento aqui dentro. Como eu estou utilizando o npm, só npm install mesmo. E aqui eu já posso ir criando o arquivo enquanto ele instala, slintrc.json, e a gente vai apenas estender a configuração da Rocketseat para um projeto Node. Então eu boto o Node aqui, se fosse projeto com React, por exemplo, era só trocar Node por React. Salvo aqui agora. Enquanto ele vai terminando de fazer a instalação, a gente pode esperar um pouquinho. E agora aqui no package.json, a gente vai criar dois scripts. um que se chama Lint, que vai ser utilizado para fazer o processo de Linting dentro do GitHub Actions, dentro do CI, que é para validar se novos commits estão chegando com a padronização correta. Então, a gente vai utilizar esse Lint. source que é a pasta onde vai estar o nosso código e aqui eu passo ext que são as extensões .ts e aqui eu vou criar um outro comando que é o lint .fix que ele vai executar o mesmo comando só que com a flag fix aqui no final esse é mais para ser utilizado em desenvolvimento Então, a gente pode executar ele com o npm run lint fix. Ele vai tentar fixar todos os problemas e os problemas que ele não conseguir resolver, ele vai avisar aqui para a gente. Então, a primeira coisa que ele já começou a avisar é que a gente tem aqui o test e o expect, que são funções globais do VTest, porém ele não está conseguindo identificar. Para isso, a gente pode vir aqui no slintrc.json e a gente pode passar a opção env E aqui dentro de env eu posso passar vtest-globals-env 2.true e ele já vai automaticamente entender essas... ou era pra entender, vamos aguardar um pouquinho. Ah, na verdade, uma das coisas que eu esqueci é que para eu conseguir setar essas configurações do VTest, aqui dentro do Extends eu tenho que estender também o plugin .vtest-globals-recommended e aqui agora eu salvo e agora, teoricamente, Deixa eu dar um reload aqui na janela para ver se ele vai parar de dar os erros do linting. Beleza, ele já entendeu certinho. Posso até rodar um npm run lint fix novamente. E aqui ele deu erro porque um dos scripts, um não está instalado, na verdade que é o plugin, então eu tenho que instalar eslint-plugin-vtest-globals como dependência de desenvolvimento. Acabei esquecendo. Beleza, agora sim. E aqui dentro do slintrc.json, eu já vou fazer mais uma configuração. Deixa eu só rodar o lentfix de novo pra ver se não tá mais dando nenhum erro. O único erro é esse no-useless constructor, que ele tá acontecendo no nosso use case, porque ele entende que essa forma de instanciar uma propriedade AnswersRepository, que isso aqui não está sendo utilizado, que o construtor está sendo inútil, porque ele não tem nenhum conteúdo, mas isso aqui no TypeScript é totalmente válido. A gente só tem que fazer o seguinte, rules no UselessConstructor, como off. Salvo aqui agora e pronto. A gente já não tem mais nenhum erro no projeto. Podemos rodar run lentfix novamente e a gente vai ver que já está tudo corrigido.