WEBVTT Outra coisa que a gente vai fazer aqui na nossa aplicação para deixar ela ainda mais a prova do futuro, digamos assim, é separar essa lógica de inicialização de um id para uma classe separada. A gente pode entender como um value object que vai ser compartilhado entre todas as entidades da minha aplicação. Então, continua sendo um value object, assim como o slug, porém, eu vou criar ele separado. Eu vou chamar isso aqui de UniqueEntityId. E como o próprio nome já diz, a ideia... dessa classe aqui é referenciar um id único que a gente vai ter nas nossas entidades que hoje a gente está usando um random id mas mais pra frente pode ser qualquer outra coisa e a ideia que eu coloque isto numa classe é que todo lugar da minha aplicação que eu precisar gerar um id ou que eu precisar referenciar um id específico de uma classe da nossa aplicação eu tenha um ValueObject, uma classe, que me deixe trabalhar com essa informação. Aqui a gente vai seguir a mesma estrutura de um ValueObject, então eu vou ter o value, vou ter um construtor, vez.value igual value, que eu vou receber aqui em cima, basicamente é dessa forma a única diferença que eu vou fazer aqui é que no construtor esse value aqui eu vou deixar ele opcional se ele não for informado eu vou usar o random id que vem aqui de dentro do node que é mais ou menos o que a gente está fazendo nas outras classes é basicamente isso né a gente vai criar aqui também um um accessor pra isso aqui então ao invés de criar um getValue uma das outras coisas que eu posso fazer também é fornecer um método aqui chamado toString retorna o velho e 2 velho que retorna o velho também show agora o que a gente vai fazer é esse id que eu tenho aqui na minha classe entity eu vou falar que o tipo dele é unique entity id e aqui no id eu vou dar um new unique entity id passando o id que eu tô recebendo aqui O resto vai ficar tudo igual. Veja que agora eu não tenho mais na minha classe Entity uma referência direta ao Random UUID. Eu tenho uma referência à classe Unique Entity ID. Se em qualquer lugar, em qualquer momento da minha aplicação eu precisar mudar a rotina de geração de IDs, que não seja para Random UUID, eu só vou precisar trocar em um lugar que é nesse Unique Entity ID. Todos os outros lugares vão refletir de forma automatizada. Então, beleza... Como eu falei, a minha ideia é a gente pensar na aplicação, em todos esses detalhes da aplicação desde um primeiro momento, porque eu não quero criar muito código e depois voltar refatorando muita coisa, eu quero que a gente já aprenda. E lembrando que todas essas coisas, como por exemplo, criar uma classe base de entidade, criar um ValueObject do UniqueEntityId para a gente compartilhar entre todas as... as entidades da minha aplicação. Isso são coisas que eu, com o tempo, adquiri como padrão e vi que faz sentido nas minhas aplicações. Mas, como eu falei, não necessariamente isto aqui está escrito no DDD. DDD não explica como você vai implementar o seu código. Isso é muito importante. Existem conceitos que a gente está traduzindo do DDD para código. São entidades, value objects, os próprios casos de uso, que foram os conceitos que a gente viu até agora em formato de código, que são conceitos que a gente vê. na parte teórica do DDD, digamos assim, mas jamais você vai ver, por exemplo, dentro do livro do DDD falando... Olha, crie uma classe UniqueEntityId, isso é algo específico da nossa implementação, é algo que eu estou optando por implementar dessa forma, não obrigatoriamente todos os projetos vão implementar dessa forma, mas é uma coisa que eu curto e vejo que dá muito certo. Então, bora continuar.