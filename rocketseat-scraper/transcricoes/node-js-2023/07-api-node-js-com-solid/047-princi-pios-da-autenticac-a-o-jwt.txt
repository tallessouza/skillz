WEBVTT Eu vou começar criando aqui nos controllers da minha aplicação um controller para parte de perfil do usuário. Se a gente for lembrar, uma das rotas que a gente quer permitir ter aqui na nossa aplicação é uma rota que retorne os dados do usuário logado na aplicação. Então, eu vou começar criando aqui o profile. Eu vou copiar um outro caso de uso aqui, um outro controller qualquer. Só trocar o nome dele para profile. O profile não vai receber nenhuma informação, por enquanto. aqui eu vou simplesmente fazer o seguinte eu não vou colocar nada vou comentar tudo que tem aqui dentro a gente vai criar tudo isso aqui do zero daqui a pouco e eu vou deletar todas as importações que eu tenho aqui em cima eu quero que isso aqui fique o mais simples possível só retornando um status 200 mesmo tá posso até apagar isso aqui depois eu escrevo de novo aqui dentro das minhas rotas o que eu vou fazer é eu vou criar uma rota app.get E essa rota aqui, ela pode ser um barra me, por exemplo. Isso aqui é legal, gosto de utilizar nas minhas aplicações. Vou deixá-la até por último aqui, porque diferente dessas duas rotas que eu tenho aqui em cima, que elas são para criar um novo usuário e fazer login na aplicação, Essa rota aqui e as demais rotas daqui para baixo são rotas que só poderão ser chamadas quando o usuário estiver autenticado. Perfeito? Então, o usuário só vai conseguir retornar os dados do seu perfil se ele já fez login previamente. Então, a rota me vai chamar o controller chamado profile. Cuidado para importar certinho de dentro de controllers barra profile. Se eu chamar essa rota aqui agora, na minha aplicação, vai funcionar. Só que ela ainda não está fazendo nada, então agora a gente vai começar a conversar um pouquinho sobre estratégias de autenticação dentro das aplicações. Existem três estratégias mais comuns de autenticação que a gente pode falar aqui. Perfeito? Vamos lá. Primeira estratégia mais comum. Basic Auth. Basic Auth, como o próprio nome já diz, autenticação básica, ela basicamente faz com que todas as requisições, o usuário precise enviar as credenciais dele como um cabeçalho na requisição. Cabeçalho são aquelas informações, como a gente já falou previamente, que são metadados da requisição e da resposta. Eles não são dados que a pessoa vai inserir, eles são muitas vezes dados que eles são de contexto daquela requisição. Então, existe um header, um cabeçalho especial chamado authorization, que ele é utilizado para a parte de autenticação. Quando a gente passa para esse cabeçalho o tipo de autorização básica, a gente precisa enviar as credenciais do usuário, ou seja, usuário e senha, ou email e senha, se é o que a gente está utilizando, porém converter isso para um formato de Base64. Base64, caso você não conheça, é basicamente um algoritmo que permite a gente fazer o encoding e fazer o decoding de alguns dados. Ou seja, se eu criar aqui, por exemplo, meu e-mail é diegoxelf.com, dois pontos, a minha é 10123456. Ele enviaria exatamente isto aqui para o... para o... Ah não, aqui eu estou fazendo um decode, não é isso, eu quero encode. Agora sim. Ele enviaria basicamente isto aqui para o meu back-end, que seria a representação do meu e-mail e da minha senha. O meu back-end, por sua vez, pode pegar esses dados, fazer uma decodificação e saber o e-mail e senha do usuário. Por que isso aqui acaba não sendo muito utilizado na maioria dos casos? Porque não é muito seguro a gente ficar transitando o e-mail, e principalmente a senha do usuário, toda vez que a gente faz uma requisição para o back-end. Até porque, por mais que a gente use um protocolo HTTPS, ele até fala, né? Só pode ser, só deve ser utilizado com conexão segura HTTPS. Por mais que a gente use esse protocolo HTTPS, algum outro tipo de malware, fazer uma interceptação ali na nossa requisição, alguma coisa que está instalada na máquina da pessoa e pegar as credenciais da pessoa, é muito mais fácil caso do que se a gente não precisasse enviar o e-mail e senha da pessoa. Perfeito? E aí, tem um outro tipo de autenticação que é a mais famosa, mais comum utilizado em APIs, em projetos web, que é o JWT. JWT é uma sigla para JSON Web Token. E eu vou te explicar exatamente o que é um JWT e como é que ele funciona. O que que acontece? Deixa eu até pegar aqui uma... Abrir o nosso Vast Code e a gente vai... Deixa eu ver certinho como é que... Deixa eu abrir aqui o arquivo de volta... Show! No JWT, diferente da Basic Auth, quando o usuário faz login somente na rota de login, ele envia o e-mail e senha dele para o back-end, O back-end valida se esse e-mail e senha existem lá dentro do banco de dados, exatamente o processo que a gente fez lá no nosso caso de uso de autenticação. Então, validar que o e-mail e a senha batem. Depois que ele verificou que o e-mail e a senha batem, o nosso back-end gera um token único e Principalmente aqui, o token não pode ser modificado. Esse token único é um token que a gente chama de stateless token. E calma, que eu sei que são muitas informações, mas deixa eu resumir algumas delas aqui no VSCode para a gente entender. JWT, JSON, WEB, token. O fluxo, basicamente, é o usuário faz login, envia email e senha, o backend cria um token único e não modificável, logo vamos falar sobre o que é eles não ser modificável, e stateless. Stateless é basicamente, se a gente for traduzir, sem estado. Mas, se a gente for ainda trazer para uma linguagem mais popular, não armazenado em nenhuma estrutura de persistência de dados. Banco de dados. Olha só, a gente tá caminhando. O usuário faz login, ele envia e-mail e senha. O back-end cria um token único pra ele, pra esse usuário. Esse token é stateless, vamos começar por aqui. Ou seja, o token não é salvo em qualquer estrutura de banco de dados. Tá, Diego, calma. Vamos lá. Se o usuário faz login na minha aplicação, eu crio um token pra ele, esse token não é salvo em nenhum lugar, como que eu sei que depois, quando esse token for enviado para o meu back-end, como que eu sei que esse token não é um token malicioso, um token de uma pessoa que criou essa informação a partir de qualquer lugar? Porque o nosso back-end, na hora que ele vai criar o token, ele usa uma palavra-chave. Essa palavra-chave pode ter vários formatos, mas o formato mais simples é uma string. Então, dentro do meu back-end, por exemplo, eu vou configurar uma palavra-chave. Essa palavra-chave pode ser qualquer coisa. O quão mais difícil for, melhor, porque mais difícil vai ser uma outra pessoa conseguir criar um token com a minha mesma palavra-chave. Então, você bate a cabeça no teclado literalmente e isso aqui poderia ser a minha palavra-chave. Ou seja, o que acontece? O back-end, quando ele receber o e-mail e senha do usuário, e ele validar que esse e-mail e senha são reais, ele vai criar uma informação, um hash, assim como a gente viu em vários outros locais, só que a diferença é que essa informação que o back-end criar, ela vai poder ser descriptografada, o usuário vai conseguir ver, a gente vai poder fazer o decode dela, a gente vai criar o token e a gente pode descriptografar o token também, ver os valores que geraram aquele token. E aí o que acontece? Ele vai usar essa palavra-chave aqui para criar aquela informação então ele vai criar um token ali o token jwt ele é geralmente um composto entre três coisas cabeçalho payload e assinatura tá Já vamos conversar sobre essas três coisas aqui, tá? E elas, inclusive, são separadas por pontos. Inclusive, aqui no site jwt.io, você pode ver aqui que ele tem um pontinho aqui que separa a primeira parte do token, esse pontinho aqui que separa a segunda da terceira parte do token. A primeira é o cabeçalho, a segunda é o payload e a terceira é a assinatura. Logo, a gente vai falar sobre isso. Como o back-end é o único sistema que tem acesso à chave privada, à palavra chave, só o back-end pode criar novos tokens. e só ele pode validar, através da assinatura, é só esse trechinho aqui que muda conforme a nossa palavra-chave, que o token criado, ou seja, que um token que foi criado previamente, é um token que originou desta palavra-chave. Ou seja, a partir do momento que um token JWT é criado, mesmo que ele não tenha sido armazenado em nenhum banco de dados, O token que foi criado a partir de uma palavra-chave sempre vai ser diferente de um token que foi criado a partir de outra palavra-chave. E se o nosso back-end é o único detentor da informação da nossa palavra-chave única no mundo, não é possível outras pessoas modificarem aquele token, porque só o nosso back-end vai conseguir decodificar aquele token para garantir que é um token criado por ele mesmo. Ficou claro isso? Mesmo que eu não salve o token no banco de dados, o back-end vai conseguir recuperar um token no futuro, validar que aquele token foi gerado por ele mesmo e buscar as informações que estão dentro do token. E aí, a gente olhar a anatomia aqui de um JWT, a primeira parte é o header, é o cabeçalho, que é basicamente qual o algoritmo que a gente utilizou para criar esse token. Existem vários. Por exemplo, se você quiser um pouquinho mais a fundo, o HS256 é um algoritmo que a gente pode usar uma palavra-chave string mesmo, um texto, que geralmente é o comum utilizado para aplicações mais simples. para criar o token. Existem outros algoritmos com taxas de bits maiores, por exemplo, o RSA256, que daí já é um algoritmo que ele usa não só uma palavra, mas ele usa chave privada e chave pública. Então, a chave privada para criar novos tokens, a chave pública para conseguir validar que um token foi criado a partir de uma chave privada. Isso aqui não é algo para a gente entrar tão a fundo aqui agora. O segundo ponto aqui é o Payload. Payload é qualquer informação que eu quiser. E aqui, você pode ver que qualquer modificação que eu vou mudando aqui do lado, ele muda o Payload, tá vendo? Então, se eu troco o John Doe por Diego Fernandes, ele vai adicionando cada vez mais dados aqui dentro do meu Payload. Diego, o que é o Payload exatamente? O que eu boto aqui dentro do meu Payload? O que que acontece? Quando o usuário faz login e eu envio o e-mail e senha e o back-end cria um token único, como que eu vou saber qual usuário é dono daquele token? eu boto o ID do usuário, por exemplo, dentro do token. Geralmente a gente usa, é uma convenção, usar a terminologia sub para indicar qual que é o ID ou a informação, o subject nesse caso, qual que é o ID do usuário ou da aplicação ou qualquer que seja a entidade que criou aquele token. E aí, as outras informações que eu vou colocar aqui dentro, eu posso colocar o que eu quiser. Tá, Diego, mas o que que impede de um usuário vir aqui e trocar esse ID? Então, se passar por outro usuário, aqui antes estava o ID 1, a pessoa vem aqui no Token e troca o ID 2. a nossa assinatura, que é aquilo que o back-end vai validar. Porque, olha só, se eu mudar qualquer informação dentro do payload, trocar o ID de 1 para 2, veja que a assinatura muda também, olha só, 1, 2, ó, já mudou. Então, o 1 começa com JA, o 2 com ZV. E aí o que acontece é que, se eu tentar mudar qualquer coisa no payload, quando chegar uma requisição contendo aquele token para o nosso back-end, o back-end vai olhar na assinatura e... peraí cara, tá errado essa assinatura não fui eu que gerei ele vai bater lá na palavra-chave e vai falar não fui eu que gerei essa assinatura não tá batendo legal né? então a gente consegue ir brincando com essas... Basicamente com essa geração, entendendo como é que funciona. E a assinatura aqui embaixo, ela é basicamente uma geração, né? Ela pega o Base64 do header, o Base64 do payload, mais a minha palavra-chave, e então ela gera, através do algoritmo que eu selecionei lá em cima, uma assinatura, que é um trecho de hexadecimal aqui pronto, com vários caracteres, que tem a nossa, digamos, assinatura daquele token pra ele não poder ser modificado. E essa é a estratégia que a gente vai utilizar aqui na nossa aplicação. Basicamente, o usuário faz o login, a rota de login retorna um JWT, esse JWT vai ser utilizado em todas as requisições dali pra frente, a gente vai enviar esse JWT através de um header, que é um cabeçalho, que é aquele header que a gente tinha visto antes, que é o cabeçalho authorization. A única diferença é que a gente não vai utilizar authorization basic, como eu tinha mostrado antes, nós vamos utilizar aqui um outro formato que é o bearer, que é geralmente a terminologia utilizada para quando a gente está enviando um token, um JWT, para fazer autorização, para fazer autenticação de requisições aqui na web.