WEBVTT Agora, o que eu vou fazer nessa aula é criar a estrutura do nosso projeto em Node, então vai ser uma estrutura bem simples. A gente vai começar executando npm init menos y para ele criar o package.json. Vou criar uma pasta source. Dentro dessa pasta source, eu vou começar criando simplesmente um server.ts, aqui que vai ser a instância, o nosso servidor HTTP. E logo eu já vou criar o conteúdo desse server aqui. mas a gente vai também já instalar algumas extensões aqui que a gente vai estar utilizando que é o TypeScript e o arroba types barra Node e também junto com essas duas extensões aqui a gente vai instalar o TSX que é a biblioteca que a gente vai estar usando em desenvolvimento para executar o nosso código porque ele tem código TypeScript e o Node não entende código TypeScript por padrão então ele vai converter esse código para JavaScript automaticamente e rodar o código E nós vamos também já deixar pré-instalado o ts-app aqui, que é a biblioteca que a gente vai estar utilizando para criar a versão de build da nossa aplicação, ou seja, quando a gente colocar nossa aplicação em produção, antes de colocar em produção, a gente precisa converter o nosso código de TypeScript para JavaScript. Então, o ts-app vai fazer esse papel para a gente. Show! Com essas bibliotecas instaladas, eu vou executar npx tsc -, -, init para ele criar o arquivo tsconfig.json, que eu vou também já trocar aqui no target es2020, porque o Node já suporta essa versão, digamos, do ECMAScript, da linguagem, então a gente não precisa converter o nosso código para uma versão muito antiga, como é o padrão aqui es2016, a gente pode converter para uma versão mais recente. Aqui dentro do nosso server, a gente vai ter basicamente a instância do nosso servidor com o Fastify. Então, eu já vou deixar também, inclusive pré-instalado, o Fastify aqui dentro da nossa aplicação. Vou instalar então o Fastify, aguardar um pouquinho... E aqui, o que eu vou fazer é dividir a nossa aplicação Fastify em dois arquivos. Então, eu vou ter o app.ts e o server.ts. E aí, o que eu vou fazer dentro do meu app? Eu vou importar o Fastify, de dentro de Fastify. Vou criar a nossa aplicação. Quando eu crio a aplicação, eu também já vou exportar ela daqui de dentro. perfeito e agora lá dentro do server o que eu vou fazer é basicamente importar a nossa aplicação desse outro arquivo e executar o método listen nesse método listen aqui eu vou passar a porta da nossa aplicação que eu vou estar utilizando que é 3333 E aqui um hackzinho super rápido, quando eu crio uma aplicação Fastify, usar o host como sendo 0.0.0.0. Isso aqui vai fazer com que a nossa aplicação, mais pra frente, seja acessível por front-ends que estejam consumindo essa aplicação ou com qualquer outra aplicação. Se a gente não passa esse host aqui, a gente pode ter alguns probleminhas na hora de conectar alguma aplicação front-end com a nossa aplicação back-end aqui. E aqui eu vou botar um ponto then, porque esse listen retorna uma promise. Então, eu posso aguardar o meu servidor subir, ouvir aqui a porta 3363... Quando ele terminar, a gente vai dar um console log aqui dentro... HTTP server running... E aí, eu gosto de colocar aqui no comecinho um foguetinho... No Mac é Ctrl, Command, espaço... No Windows, acho que é Windows, vírgula... No Linux, acho que tem que usar alguma extensão para obter a lista de emojis, não lembro exatamente... Perfeito. Aqui o core da nossa aplicação já está criado. Uma outra coisa que já é interessante a gente criar é um arquivo gitignore. Então, eu vou deixar esse arquivo previamente criado, ignorando a pasta NodeModules e ignorando a pasta build, que é onde a gente vai fazer a build da nossa aplicação para a produção. E já lembrando, falando em build, a gente precisa aqui no nosso package.json configurar alguns scripts aqui dentro. O primeiro script que eu vou criar aqui dentro é o script chamado startdev, ou caso você prefira, somente dev, tanto faz, tá? Vou botar startdev aqui dentro. E aqui eu vou executar o tsx, que é a biblioteca que a gente instalou para executar a nossa aplicação, watch, para que ela fique observando mudanças toda vez que a aplicação mudar alguma coisa, ele executa de novo a aplicação, source, server.ts, que é o arquivo ali que tem o nosso servidor. se eu rodo agora npm run start dev a gente vai ver que já vai mostrar http server running que é o nosso servidor fastify ali executando vou criar aqui também um script de build que ele vai executar o ts app na pasta source ou seja ele vai pegar todo o código que tem dentro da pasta source vai converter para a javascript e ele vai direcionar o resultado, o produto gerado através dessa build, para uma pasta chamada build. Então agora quando eu executo npm run build, a gente pode ver que ele gera uma pasta build aqui dentro que tem o código javascript daquela nossa aplicação que a gente criou com o TypeScript. E aí, vou também aqui em cima criar um script chamado start, simplesmente, que ao invés de ele estartar o nosso servidor para desenvolvimento, ele vai estartar ele para produção. Então, eu vou executar node build barra server.js. Então, veja que agora eu executo diretamente o node e eu estou executando o nosso servidor, porém, dentro da pasta build. Então, a gente pode testar isso aqui dando o npm run start. Veja que ele já mostrou o mesmo HTTP server running. porém agora executando com o Node e não com o TSX, que é exatamente o que a gente vai usar quando a gente estiver em produção. Aqui dentro não tenho mais muita coisa para configurar, deixa eu ver se faltou alguma coisa aqui. Não, é isso mesmo. Agora com o nosso projeto previamente aqui já configurado, a gente vai partir para as próximas aulas. Bora lá!