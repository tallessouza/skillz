WEBVTT A partir de agora, na nossa aplicação, a gente vai começar a construir testes end-to-end aqui para cada rota, cada controller da nossa aplicação. O teste end-to-end, como eu já disse anteriormente, o grande propósito dele é testar a nossa aplicação de uma maneira completa, de ponta a ponta. Ele testa exatamente o que o usuário faria dentro da nossa aplicação. Só que, como a gente está desenvolvendo uma aplicação back-end, o usuário da nossa aplicação, na verdade, é o front-end, não é o usuário final. Então, a gente vai testar exatamente como que o front-end vai utilizar a nossa API. Ou seja, o teste end-to-end, ele testa desde a rota até a chamada no banco de dados ou até a última camada necessária para que aquela funcionalidade retorne os dados ali que o front-end precisa. Só que, quando a gente vai trabalhar com teste end-to-end, como eu já falei, a gente precisa bater no banco de dados. Teste end-to-end que não bate no banco de dados ou que cria muito mock, e mock é o nome que a gente dá para coisas com efeito fictício, digamos assim, com um efeito que a gente reproduz manualmente, não é legal. Por quê? Você não está testando no final das contas. O teste end-to-end tem que testar a tua aplicação assim como o usuário utilizaria ela. Tem que ser o mais fiel possível. E como a gente vai precisar utilizar um banco de dados, uma das coisas principais para a gente se fazer e cuidar é a gente não utilizar um banco de dados que a gente está usando para desenvolvimento da nossa aplicação para rodar os testes. Isso é a primeira coisa, é a parte básica. A gente tem um banco separado para testes e um banco para desenvolvimento. Melhor ainda, para melhorar ainda mais esse fluxo, uma das coisas que a gente pode fazer é separar o nosso banco de dados também por suíte de testes. O que eu quero dizer com isso? Existem várias formas de a gente conseguir fazer com que os nossos testes end-to-end, eles rodem, executem, num ambiente totalmente isolado. O que eu quero dizer com isso? É muito importante que o nosso teste end-to-end, ele não tenha, ou não só o nosso teste end-to-end, qualquer tipo de teste, tenha mínima, pouquíssima interferência, ou até zero, em caso de testes unitários, de outros testes que executaram antes dele. No final das contas, um teste que criou um usuário, por exemplo, não pode afetar em outro teste que eu vou listar usuários. O usuário que eu criei no primeiro teste não pode aparecer no teste depois que eu vou listar usuários. O usuário que eu criei no primeiro teste não pode aparecer no teste depois que eu vou listar esses usuários, porque eu preciso ter ambientes isolados para cada teste. Isso dentro de testes unitários, a gente resolveu criando aqueles in-memory repositories e antes de cada teste, a gente zera o nosso banco de dados em memória e assim quando cada teste executa ele está rodando numa aplicação limpa, só que quando eu vou para testes end-to-end que eu preciso literalmente do banco de dados, do serviço do Postgres, MySQL, SQLite seja lá qual banco a gente estiver utilizando esse processo de eu ter um banco limpo a cada teste executado ele começa a ficar mais penoso e a gente começa a ter que colocar na balança até onde isso vai ser proveitoso para a gente ou não. desse isolamento dos testes, para ter um pouquinho mais de performance, porque as operações de criar um novo banco, deletar dados de dentro do banco, resetar as migrations executadas, todo esse processo de deixar o banco limpo antes de cada teste executar, é um processo demorado. É um processo que pode levar Um segundo, um segundo e meio E aí imagina que daqui a pouco A nossa aplicação tenha, sei lá Como aqui na Rocket, a gente tem mais de Dois mil testes end-to-end Se a gente multiplicar isso aqui por um segundo e meio E dividir por sessenta Você vai esperar cinquenta minutos Os teus testes executarem? Acho que não, né? Então, esse é o ponto. A gente começa, quando a gente vem para testes end-to-end, ter que começar a fazer escolhas, né? Olhando para a balança realmente de performance e efetividade desses testes. E quando eu falei testes suites, é o seguinte, quando a gente cria um arquivo e dentro desse arquivo a gente tem vários testes, a gente chama isso aqui pode ser uma suite de testes separada das demais. Então, cada um desses arquivos aqui a gente pode considerar como se fosse uma suite de testes separadas, um arquivo com vários testes. Se ao invés de eu criar um ambiente isolado para cada um desses testes aqui, eu criar na verdade um ambiente isolado para cada um desses testes aqui. Eu criar, na verdade, um ambiente isolado no meu banco de dados para cada switch de testes já começa a melhorar. Eu não abro mão totalmente do isolamento, de ter um ambiente limpo para os meus testes, mas também não abro totalmente mão da performance. Ou seja, eu consigo ter performance e consigo evitar com que algo que eu criei dentro aqui, por exemplo, do teste de buscar, de criar uma academia, atrapalhe lá no teste mais pra frente, sei lá, de validar um check-in. Estou só exemplificando. A gente não quer que isso aconteça. Dentro do V-Test tem várias formas de a gente criar esses isolamentos de contextos. E não necessariamente no VTest. O que a gente vai ter que fazer no final das contas é... O Prisma utiliza esta variável aqui, database URL, para definir qual vai ser o banco de dados que a gente vai utilizar na nossa aplicação para executar ela. Quando eu estiver executando os testes, eu preciso que, para cada suite de testes, end-to-end, que não são esses aqui, que eu vou criar daqui a pouco, eu consiga mudar essa URL. Ou seja, eu preciso de uma de conexão com um banco de dados diferente para cada arquivo de testes, para cada conjunto de testes que ficam dentro do mesmo arquivo. Existe uma forma só de conseguir fazer isso? Não. Existem várias. Já vou te antecipando. Só que eu, particularmente, gosto muito de utilizar um conceito que existe tanto no VTest quanto no Jest, que se chama Test Environment. O Test Environment é basicamente uma configuração de ambiente para alguns tipos de testes específicos. Ou seja, usando a configuração de Test Environment, eu consigo configurar algumas coisas, como por exemplo as variáveis ambiente, que é o que a gente quer mudar, para apenas arquivos de testes específicos. Ou seja, eu vou falar, olha, todos os testes que tiveram dentro dessa pasta, eu quero que usem este environment, que traduzindo é ambiente em português. E dentro de cada ambiente, eu posso configurar muitas coisas. Eu posso configurar variáveis ambiente, eu posso executar scripts, como por exemplo, executar migrations do banco de dados, eu posso executar o que eu quiser.