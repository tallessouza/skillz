WEBVTT Nessa aula então, o que a gente vai fazer é implementar o nosso repositório do cache com Redis, mas a gente precisa que o Redis esteja funcionando. Então, eu vou aqui no docker compose da minha aplicação, criar o container do cache. Então, aqui na mesma linha, aqui na mesma coluna do PostResolve, eu vou criar um novo, vou chamar ele de vou chamar de cache mesmo e aí eu dou um container name vou chamar de nest clean cache image vou usar a imagem do redis ela é ótima para desenvolvimento para a produção não usaria essa imagem usaria a bitnami redis uma imagem que já tem configurações de segurança melhores ele tentou acessar um site nada a ver aqui por isso deu erro mas essa imagem aqui muito melhor para a produção Mas aqui, portas da aplicação, então forward portas, eu vou usar 6379, que é a porta padrão já do Redis. E aqui volumes, vou configurar também pra ele mapear, que dentro da nossa pasta barra data, barra Redis, então a gente já tá usando essa pasta data aqui pro Postgres, eu vou criar uma nova aqui, Redis, vai ser mapeada pra barra data ali dentro do Redis. No Redis é só barra data, não é barra data, barra Postgres como tem aqui dentro. É só isso. Salve isso aqui agora. Docker compose up-d. Vai ver que ele já deu um start aqui no nosso cache, no nosso container de cache. O seu, se você nunca usou Redis, ele vai baixar a imagem, vai demorar um pouquinho mais, mas não se preocupa. e posso dar um docker ps para garantir que ele está executando aqui ó 6379 já tudo certinho e no raid se eu não me engano se a gente tentar acessar a porta 6379 no navegador ele dá uma mensagenzinha não não dá então esquece esquece que eu falei agora a gente vai criar a implementação do repositório né então eu vou criar aqui dentro da pasta deles o redis cache repository e aqui ele vai ser basicamente uma classe redes cache repository que vai implementar o cache repository e aí eu implemento aqui os métodos vou colocar aqui um injector nessa classe porque ela vai ter um construtor para utilizar o nosso serviço do radius então aqui eu vou usar um private radius radius service que é o nosso serviço então para que isso aqui ocorra essa injeção eu preciso do injector aqui em cima e agora vou criar cada um dos métodos que vai ser muito simples porque é basicamente a mesma coisa só traduzir aqui então vou pegar dez ponto radius E aí, aqui estão todos os métodos que o Redis tem. E aí, tu vai ver que tem métodos mais simples, como por exemplo, set, get, que são só métodos para salvar e pegar. E tem métodos mais completos para listar, por exemplo, para lidar com hashes, que são como se fossem formas de armazenar objetos, lidar com listas. Então, todos os métodos que começam com L no Redis, eles são para armazenar listas. Todo método que começam com H, por exemplo, HGet, HDel, são usados para tratar com hashes. E por aí vai, tem JSON, tem busca, tem muita coisa que a gente consegue trabalhar dentro do Redis. O Redis é um banco incrível e muito completo. Dá para trabalhar com JSON aqui dentro dele, então tem bastante coisa interessante. Mas aí algumas coisas, como por exemplo JSON, a gente tem que instalar um módulo adicional. mas vamos lá então a gente vai usar o método set aqui e olha só é aqui e velho e aí o método certo ele tem um terceiro parâmetro que a gente consegue aqui o seu técnica nele é o método certo ele tem várias definições tá vendo mas ele tem um terceiro parâmetro que a gente consegue é passar para ele que é o número de é a data de expiração dessa informação então aqui eu consigo passar por ele da seguinte forma e x né ele até dá essa opção que é de expire e aqui eu consigo passar o número de segundos para essa informação é expirar e por que que isso é interessante porque geralmente o cache eu não quero manter ele eternamente no meu banco de dados no meu radius geralmente é até perigoso isso até para evitar aquelas coisas de informação desatualizada por muito tempo então por exemplo 60 segundos equivale a um minuto então eu quero manter no cache por exemplo por 15 minutos então ele vai manter no cache 15 minutos Faltou o async aqui. E depois ele vai apagar. E esse ex aqui, ele tem uma explicação. Então, vou até aqui pegar a documentação do comando set do Redis. E aqui tu vai ver que, no lugar do ex, eu poderia colocar outras coisas. Por exemplo, se eu boto o ex, ele vai falar o número em segundos, px, número em milissegundos. O xat, o expireAt, eu passo pra ele o timestamp e não o número em segundos. Então, eu passo exatamente a data que eu quero que ele expire. pxat, mesma coisa, só que em milissegundos. O NX... Olha só, daí tem várias opções, né? O NX vai fazer com que, quando eu faço o método set, ele só vai salvar no banco de dados se não foi salvo ainda alguma informação com aquela chave. O XX só vai setar caso já exista. Então, tem muita coisa e eu posso até misturar elas. Então, isso é bem interessante. O Redis é bem profundo. A gente vai pro método get aqui, que eu vou dar um return, this.redis.get aqui. E aqui no delete, return this.redis.del aqui. E pronto, agora a gente tem a implementação. A única diferença é que o del retorna um número ali, que é o número de dados que foram deletados. Então, eu vou trocar aqui, como eu quero void, eu vou usar await mesmo, sem return. salvo aqui agora e agora sim a gente tem o repositório de cache implementado posso vir aqui no meu cache module e a gente vai criar aqui opa aqui dentro de providers vou dar um enter aqui agora nós vamos usar mais ou menos como a gente faz no banco de dados é provide cache repository use class radius cache repository salvo aqui agora se eu tentar executar a minha aplicação teoricamente ela deveria funcionar agora aqui a gente também vai ter que exportar o nosso cache repository porque ele é utilizado lá pelo database module que importa esse módulo então a gente tem que exportar ele daqui de dentro mais ou menos como a gente faz nos repositórios do banco e ainda tá dando erro, vamos ver aqui can't resolve dependencies of radius-cache-repository no índice 0 radius-service então aqui em providers eu botei env-service não, tem que ser radius-service por isso que tá dando erro show cannot read properties of undefined reading get isso tá acontecendo radius-service env-service Eu importei o EnvModule aqui dentro. Deixa eu pensar certinho o que está acontecendo. Faltou o Injectable. Então aqui agora boto o Injectable e aí ele tem que importar. Show! E agora sim, a aplicação já está funcionando.