WEBVTT O que a gente vai fazer nessa aula é configurar o service do Prisma para que os meus controllers aqui dentro da minha aplicação possam acessar a conexão com o Prisma. Dentro de outros frameworks, o que a gente faria aqui é criar um arquivo prisma.ts e criar o Prisma Client, mas no caso do Nest, como ele trabalha com a parte de inversão de dependência, vai ser um pouquinho diferente. Eu vou criar aqui uma pastinha chamada Prisma. Dentro dela, eu vou criar aqui um prisma.service.ts. e lembra que eu falei que no nest tudo que não é controller vai acabar virando um service um provider aqui o nome que você quer dar é meio que você pode escolher mas é então nesse caso a conexão com o banco de dados né do prisma ela também vai ser um serviço né então o que a gente vai fazer aqui export class Prisma Service, antes de mais nada. E aí lembra que eu falei que todo serviço que ele vai precisar ser injetado nos controllers ou em outros serviços da minha aplicação, ele precisa ter o decorator injectable. Isso aqui vai permitir com que esse serviço possa ser utilizado por outras partes da minha aplicação. Além disso, eu preciso, aqui no meu AppModule, vir aqui dentro dos meus providers e declarar o meu Prisma Service. Pronto. A partir desse momento, todo método que eu tenho aqui dentro do meu Prisma Service, como por exemplo, sei lá, se eu criar um método aqui dentro chamado test e ele retorna... Oi. Se eu vier aqui no meu controller, aqui dentro, falar que eu tenho uma dependência é que ela pode ser read only ou não tanto faz tá eu acho que fica até melhor aqui vou chamar ela de prisma por exemplo falar que ela é do tipo prisma service automaticamente o nest vai injetar o meu serviço do prisma aqui dentro e aí eu posso usar o serviço aqui ó desde ponto prisma ponto teste que eu acabei de criar aquele método lá dentro salvo aqui agora a aplicação vai reiniciar vem aqui http post chama aqui e a gente vai ver que ele retorna oi que é exatamente o que eu declarei aqui dentro desse return ou seja prontinho a gente entendeu como é que funciona a parte de injeção de dependências aqui do nest é super simples só que eu preciso fazer conexão com banco de dados e aí aqui eu tenho duas alternativas digamos assim né eu posso criar um construtor aqui dentro certo criar um public client aqui por exemplo que é do tipo prisma client E aí, não tá pegando a tipagem automaticamente, então a gente faz na mão, né? Prisma Client, Prisma Client... E faço a conexão, né? Então, this.client é igual a new Prisma Client. Passo aqui as dependências. E aí, feito isso, salvo lá no meu controller. Agora eu consigo usar this.prisma.client e veja que eu consigo acessar user, então dou user, por exemplo, .findmany para pegar todos os usuários que eu tenho no banco de dados. Esse aqui é uma promise, então eu vou fazer o seguinte, async e vou usar um await aqui e vou tirar o tipo do retorno aqui pra não ficar me incomodando. Aqui o async e await não precisava né, não tá fazendo nada, mas não tem problema. do http post agora a gente vai ver que ele retorna um array vazio porque não tem nenhum usuário no banco de dados se eu for lá é pnpm prisma estúdio criar um usuário no banco então vamos lá é record diego fernandes diego arroba rocketcity.com.br senha 123456 salvo isso aqui no banco volto rodo de novo o post a gente vai ver que agora ele retorna o usuário ali do banco de dados ou seja isso aqui é uma forma da gente configurar só que tem uma forma ainda mais simples digamos assim é mais simples é uma reduzir um pouquinho do código né digamos assim né porque aqui eu tenho que fazer o desponto prisma.client né isso aqui meio que acaba ficando repetitivo né então como o prisma client aqui ele é uma classe né até pra instanciar ele eu tenho que fazer nil prisma client uma tática que eu posso fazer o seguinte eu posso pegar essa classe prisma service e fazer ela estender a prisma client e aqui dentro do meu constructor eu só chamo o super né o super que ele vai fazer é chamar o construtor da classe prisma client agora eu não preciso mais essa variável client aqui ó agora eu posso dar direto desse ponto prisma como essa classe como esse prisma que ele está estendendo prisma client ele já é o cliente em si então não precisa mais do client salvo aqui agora rodo e tudo vai funcionar normalmente O que eu posso fazer aqui agora também é... Esse super aqui está chamando o construtor da classe Prisma Client. Ou seja, tudo que eu passo para ele são os parâmetros que eu posso enviar direto para o Prisma Client. Por exemplo, o log. Eu posso falar para ele fazer um log nas queries SQL. Então, agora, quando eu rodo aqui o HTTP, veja que ele dá o log exatamente da query que ele fez aqui. Geralmente, a gente não usa o log query aqui. A gente usa o log WARN e ERROR para ele fazer log somente quando dá algum tipo de erro, algum aviso. mas se você quiser mostrar as queries aqui SQL também para fazer algum tipo de debug você pode colocar aqui dentro feito isso pronto a nossa conexão está finalizada outra coisa que eu gosto de fazer aqui dentro dos services do nest a gente pode implementar algumas interfaces e aqui tem uma interface que se chama on module init e tem uma interface que se chama on module destroy e aqui dentro essas interfaces até se eu for aqui no prisma service e dar um control ponto e vai ter uma opção chamada Implement All Unimplemented Interfaces. Ele vai trazer dois métodos aqui para dentro, OneModuleInit e OneModuleDestroy. O que isso aqui faz? Esses dois métodos aqui são métodos que o Nest chama automaticamente quando o módulo que usa esse serviço, que é o Prisma Service, for instanciado e for destruído. Isso pode acontecer? Pode. Por exemplo, se a minha aplicação crashar por qualquer motivo, o que eu quero que aconteça? Eu quero que a conexão com o banco de dados feche. Então, o que a gente vai fazer? Dentro desses módulos, eu vou dar um return this, que no caso é o cliente do Prisma, e aqui ele tem a função connect e a função disconnect. Então, aqui uma, claro, conecta com o banco e a outra desconecta. Então, aqui no onModuleInit, eu vou dar um connect, ou seja, Quando o meu módulo estiver inicializando, eu quero que ele conecte com o Prisma. Quando ele estiver sendo destruído, eu quero que ele desconecte. É importante implementar os dois, por quê? Porque pode ser que o meu módulo seja destruído por algum erro que aconteceu na aplicação, a aplicação crashou ou qualquer coisa assim. Só que depois que eu consertar esse crash, esse erro na aplicação, A minha aplicação vai subir de novo aqui com o auto restart, o auto reload, e daí nesse caso, esse método onModuleInit vai ser chamado de novo e ele precisa conectar com o Prisma. Então, é sempre legal implementar os dois quando eu estiver criando algum serviço que ele conecta com um meio externo, um banco de dados, um Redis, qualquer coisa assim que precisa manter uma conexão ativa, a gente vai implementar esses dois métodos aí. Show! Conexão com o banco de dados feita, já está conseguindo fazer query aqui no banco, a gente já pode prosseguir.