WEBVTT o que a gente vai fazer agora então é criar uma chave privada e uma chave pública aqui para dentro da nossa aplicação para usar no JWT só que a chave privada e a chave pública não podem ser qualquer coisa assim elas precisam seguir um padrão e aqui quando a gente seleciona até o algoritmo rs 256 ele até tem aqui o formato mais ou menos que uma chave privada e uma chave pública precisam ser só que a gente precisa gerar né então uma das coisas que tu pode fazer é procurar aqui por exemplo no gpt da vida a generate rsa 256 private and public keys no meu caso eu tô no mac então botar mac os aqui ele vai usar provavelmente o openssl show de bola Legal, então aqui ele mostra algoritmo, RSA, ALT, Private Key, PEN, UPKEY, OPT, não sei exatamente o que é isso, mas vamos torcer para que dê certo. Vamos lá. No VS Code, eu vou girar ela dentro aqui do meu projeto mesmo, então aqui na raiz mesmo, sobe aqui, ENTER. Pronto, já girou. Ela tem que ter esse formato, começar com esse BEGIN PRIVATE KEY e terminar com END PRIVATE KEY. Perfeito. E agora a gente vai gerar uma chave pública a partir da chave privada. A gente pode gerar várias chaves públicas a partir de uma chave privada. E aqui eu dou um Enter e pronto. E agora ele gerou aqui a public key, tá vendo? Que ela tem que ter este formato aqui. E agora, o que a gente vai fazer? Eu vou converter isso aqui porque, olha só, isso aqui é uma chave que tem várias quebras de linha, tá vendo? Vai ser difícil eu pegar isso aqui e jogar dentro do env da minha aplicação. Como é que eu posso fazer, né? Eu posso converter... o conteúdo do arquivo para base64 base64 ele vai criar um hash daquilo ali então aqui ele fala o base64 input e o output né então vou começar aqui ó no caso no mac né no windows deve ter outros formatos outro pode até usar ferramentas online eu sei que tem tipo aqui ó rsa aqui generator online né você vai achar aqui online rsa aqui generator pode ser, mas aqui é uma opção mais genérica aqui agora então, base64, input, vou passar aqui minha private key e output, private key e vou botar aqui um traço base64 vou botar um txt mesmo, tá? Vamos ver se ele girou aqui. Legal, já está aqui. Vamos fazer o mesmo processo com a nossa public key. E beleza, temos a public key e a private key em vc64. Agora eu venho aqui no env, a gente vai colocar aqui jwt private key. Vamos colar aqui, cuidado só com o enter no final. e jwt public key e vamos jogar aqui o conteúdo da public key show, com isso aqui em mãos eu vou já deletar os arquivos do Base64 pelo menos os outros eu ainda vou deixar ali um pouquinho só pra gente validar que deu tudo certo mas eu não quero mandar eles pro git então logo eu quero remover eles também até porque base64 não é um processo de hashing então eu vou conseguir fazer o processo inverso então eu consigo pegar a chave de volta a partir do base64 mas agora aqui dentro do meu env.ts eu vou botar aqui jwt private key salvo volta aqui e agora a gente vai ter aqui a nossa private key e a nossa public key Como eu fiz o encoding para a base64, agora aqui dentro na hora de utilizar elas, eu vou usar a private key, eu não posso passar diretamente a string, porque ela está em base64. Eu tenho que fazer o processo de decode. Então eu vou fazer um buffer. .from private key e passo para ela aqui b64 então o buffer from é basicamente o que ele faz ele vai pegar um texto a partir de um encode e converter isso para buffer no node o buffer caso você não saiba é uma forma de carregar dados em memória no node mas pode ver a representação em texto caso queira dar um console log usando um toString aqui no final Eu não vou usar o TwiString, porque o PrivateKey aceita um buffer também. Ele aceita buffer também, então não tem tanta necessidade. PublicKey, mesma coisa, buffer from publicKey, base64. E aqui, no signOptions, preciso passar o algoritmo rs256. Falar que a gente está usando o rs256 e não o hs, normalzinho. Feito isso, já estamos prontos para fazer a nossa autenticação. Então, o que a gente vai fazer aqui agora? Aqui dentro, E tem essa opção global. Vamos ver se ela funciona igual o do config. Eu vou deixá-la como true aqui por enquanto. Porque o que acontece, né? Como eu estou usando esse JWT module aqui dentro do auth module, e eu quero usar agora a geração de token lá por dentro de um controller, que eu vou até criar aqui o authenticatecontroller.ts. vou até copiar o create account controller aqui e vou tirar todo o conteúdo dele aqui ó vou comentar isso aqui comentar isso aqui comentar isso aqui por enquanto só pra ver se a gente consegue gerar um token tá comentar isso aqui aqui aqui eu vou trocar pra sessions authenticate controller tira as importações aqui ó posso dar remove unused imports aqui do vascode e agora aqui dentro eu quero gerar um token como é que a gente faz né aqui ao invés de usar o prisma então eu vou usar o jwt posso usar só jwt? jwt service que vem lá do nest jwt e agora aqui dentro const token igual this.jwt.sign e aqui eu posso passar um payload né o payload seria o as informações que eu quero conter dentro do token mandar aqui por exemplo um sub user id né por enquanto fictício e aqui eu vou dar um return token legal bora testar aqui então nossa rota de autenticação aqui no meu client vou trocar aqui já tá né sessions e mail password legal então send request Não encontrou, porque aqui no meu AppModule faltou eu passar o meu controller aqui, né? O AuthenticateController, junto com os outros controllers. Agora sim, clico em SendRequest e tá lá. Meu token já foi gerado. Se eu copio esse token aqui agora, trago aqui pra dentro do JWT.IO, Veja que eu boto aqui rs256 e trago aqui, e eu vou ver que o meu sub está ali, user id, certo? E aí, para eu validar que a assinatura dele está ok, eu posso pegar aqui a chave privada, botar aqui dentro, chave pública, botar aqui dentro, e aí já deu a assinatura verificada. Só com a chave pública, ele já conseguiria verificar a assinatura do Token, tá? Show de bola. Agora que temos nosso token em mão, nosso token de autenticação, a gente pode continuar, porque a rota de autenticação não vai ficar só assim não. Tem que ainda receber os dados, validar que o usuário existe no banco e por aí vai.