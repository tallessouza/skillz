WEBVTT Bora lá então, agora a gente vai refatorar o nosso controller de criação de conta, o Create Account, aqui hoje. Ele vai parar de usar o Prisma Service Direto e vai usar o nosso Register Student, que poderia até trocar o nome dele ao invés de Create Account também, mas não é um problema os nomes serem diferentes aqui. Beleza, vou fazer aqui todo o processo. então const result igual await this.registerStudent.execute e passamos nome, email e senha e agora if result.isLeft vou por enquanto apenas retornar um erro e se não não tem nem na verdade é sucesso aqui né não tem mensagem de sucesso nem nada disso remover aqui as importações que não estou usando fechou e é isso eu acho que é isso vamos lá a gente vai criar uma conta aqui botar o diego 5 né que eu já tenho vários outros não sendo request e pronto conta criada uma das formas também de garantir que está tudo funcionando é nós virmos aqui nos testes e rodar os testes end-to-end. Test end-to-end é o teste que realmente garante que tudo está funcionando. E aí o pnpm run test, que é para rodar os testes unitários. Beleza, tudo passando bonitinho. Feito isso agora, falta a gente ainda lidar com erros, porque a gente ainda tem esses throwErrors vazios, mas a gente vai resolver isso já já.