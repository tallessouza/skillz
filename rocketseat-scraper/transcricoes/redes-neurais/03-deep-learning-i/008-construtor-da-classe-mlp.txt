WEBVTT Fala, Dev. Eu vou apresentar agora o Laboratório 7, que é o Laboratório de Deep Learning 1, onde a gente vai fazer a implementação de uma rede neural multicamada, uma MLP, para resolver o problema do Shor, que é um problema que não é linearmente separável e que, portanto, a gente não conseguiria resolver ele usando apenas um perceptron. Lembrando que a gente fez essa implementação de MLP no módulo anterior, só que sem a fase de treinamento. A gente só fez o Feed Forward na rede e a rede não aprendeu os pesos e retro-propagou esses erros para a rede fazendo esse processo de treinamento, que é o que a gente vai fazer aqui. Então, eu vou mostrando aqui os bloquinhos, tá? Eu vou separar isso em vários tópicos, vários vídeos aqui dentro dessa parte, para ficar bastante claro o que a gente está fazendo. Estou usando aqui novamente, como os outros tópicos, o Google Colab, para facilitar essa parte de treinamento de rede, não ter que fazer nenhum tipo de configuração local na máquina. Bom, que módulos que a gente vai precisar fazer? A gente vai usar o NumPy, porque a gente vai usar todos os módulos de álgebra linear para fazer os cálculos das matrizes, e o Matplotlib, basicamente para imprimir um gráfico que vai mostrar é o quanto que o erro foi caindo ao longo das épocas. Essa que é a ideia de mostrar esse gráfico mais pra frente. Nessa primeira parte que eu vou mostrar, mostrar como que a gente vai montar essa classe de MLP. Então, para montar essa classe MLP, a gente primeiro precisa definir o construtor dessa classe, que a gente faz essa construção através desse método, que é um nome de método já reservado do Python, chamado init, com esses dois underscores aqui, antes e depois. Então, toda vez que eu inicializo uma classe, Ele busca esse método init para saber que variáveis ele precisa inicializar quando essa classe é instanciada. Um outro ponto importante. Qual é a arquitetura dessa rede que a gente vai trabalhar? A gente vai trabalhar numa arquitetura que tenha uma camada de entrada, que essa camada de entrada na verdade tem dois valores. Vamos imaginar que nós temos lá o 0 e 1, 1 e 0, 1 e 1 e 0 e 0. Então eu vou entrar com dois valores. Depois eu vou ter duas camadas ocultas, cada uma dessas camadas ocultas com três neurônios. Então eu tenho três neurônios na primeira camada, três neurônios na segunda camada. E aí eu vou ter uma saída. Qual é a minha saída? Ou vai ser o 0 ou vai ser o 1. Como eu tenho uma saída binária, a gente já pode antecipar que a gente vai usar como função de ativação a sigmoide, que vai nos retornar um número entre 0 e 1. E aí, dependendo desse limiar, se ele for acima de 50, a gente define que ele é 1. Abaixo de 50, a gente define que ele é 0. Então, começando aqui a construção. A gente tem o primeiro método, que é o método init. onde eu tenho já um hiperparâmetro que eu passo para essa classe que é o learning rate, a taxa de aprendizado. Então a taxa de aprendizado ela vem pré-configurada como 0.1. Eu poderia colocar aqui 0.01, poderia colocar qualquer valor, mas o fato é, se você não informa o learning rate na hora que você está instanciando a classe, ele vai assumir como padrão o 0.1. E aí, eu pego esse valor que veio da inicialização da classe e atribuo ele a um parâmetro, uma propriedade dessa classe. Toda vez que eu estou falando de self, significa a instância dessa classe, que na verdade é um objeto que está recebendo esse valor. Então, eu estou armazenando esse valor que veio como parâmetro dentro de uma propriedade da classe. E aí, falando de orientações a objetos, a orientação a objetos, um objeto tem propriedades e métodos. Então, o MethodInit é um método especial construtor de uma classe que gera um objeto. O LearningRate é uma propriedade dessa classe, e toda vez que eu instancio essa classe, eu também posso manipular essas propriedades. Aí o que eu tô fazendo aqui? Depois de fazer a inicialização, atribuir aqui o learning rate, eu tô fazendo a inicialização dos pesos e dos bias para cada uma das camadas. Basicamente eu tenho uma primeira camada, que é a camada 1, que é sair da entrada para a primeira camada oculta, então eu saio de 2 para 3. E aqui eu estou inicializando com valores randômicos, entre 0.5 e 0.5, nos dois casos, tanto o peso, que é o W1, que é o Weight, e o Bias, o B1. E eu defino aqui qual é o shape desse vetor. Então, eu estou falando aqui que é um vetor 2 por 3, ou seja, eu estou entrando com 2 e saindo com 3. No caso do BIAS, eu tenho 1, porque eu tenho só um BIAS, eu não tenho um BIAS para cada valor, e indo para 3, porque eu tenho a camada oculta com 3 neurônios. Então, eu inicializei esses valores randomizados para essa primeira camada. Na segunda camada, eu saio de uma camada oculta de 3 e vou para uma outra camada oculta com 3 neurônios. Então o meu W2, que é o meu peso 2, também vai receber um valor randômico nesse range. Mas aqui eu estou falando de 3 para 3. Eu estou saindo de 3 neurônios, que é da primeira camada oculta, para 3, que é da segunda camada oculta. Mesma coisa em relação ao BIAS. O BIAS também não é por neurônio. Eu vou ter um BIAS para cada um dos três neurônios. Na camada 3, eu estou falando que eu tenho, nessa terceira inicialização, eu tenho que pegar o que está saindo da segunda camada oculta, que tem 3 neurônios, e ir para a camada de saída, que tem apenas 1 neurônio, que é o final que vai trazer o 01. Então, eu saio de 3 aqui dos pesos para 1, e no caso do BIAS, eu saio de 1 para 1. E aí o que eu quero fazer também é armazenar dentro dessa classe, no caso quando eu instanciar essa classe ela vai virar um objeto, os erros que eu tive no treinamento para que eu possa fazer uma análise posterior desses erros. Então essa aqui é a primeira parte inicialização dos pesos. é inicialização, aqui eu já estou dando uma visão de qual está sendo a arquitetura da rede, na hora que eu estou criando essas matrizes de pesos, eu já estou dando essa visão da arquitetura da rede, e com isso a gente já consegue inicializar o nosso MLP. Então, fecho aqui nesse vídeo, vejo vocês no próximo vídeo.