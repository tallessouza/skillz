WEBVTT Para que a gente possa agora testar a nossa API, a gente precisa, aqui dentro do Visual Code, abrir duas janelas. Eu vou fazer isso aqui. Eu vim aqui no Terminal, New Terminal, E a primeira coisa que eu vou fazer é subir um servidor web com o uvcorn para deixar minha API no ar. E como eu vou fazer isso? Eu vou colocar aqui uvcorn, já até deixei aqui pronto, tá? src.api. Por que eu tô fazendo src.api? Porque eu estou na pasta raiz, Deep Learning, a pasta src.api.py e aí eu tô dizendo para ele acione né deixe no ar esse app que que é o app aquela variável que eu nomeei para receber o objeto do do FastAPI. Então eu falo para ele, chama esse OVCore nessa aplicação, suba essa aplicação no host 0.0.0, no localhost, na porta 8000, e esse menos menos reload significa o seguinte, toda vez que eu alterar o código de API.py, de Python, ele dá um reload automático, ele dá o hot reload, ou seja, não preciso derrubar o OVCore e subir de novo, ele faz isso sozinho. Então eu vou executar esse comando, e ele já colocou aqui, a aplicação estartou, o processo tá aqui, o vCore subiu nesse aqui, nesse servidor, então ele tá pronto para receber requisições. Se eu vier aqui, eu vou abrir uma segunda sessão, e aí na segunda sessão eu vou fazer o que? Para testar eu vou pegar e dar um curl. O curl é aquele comando de linha de comando onde eu posso testar uma API. Então eu tô falando para ele o seguinte, curl-x post, ou seja, qual o verbo que eu vou usar, qual é o endereço que eu vou chamar. Então é meu localhost, 127.0.0.1, porta 8000 e o verbo, aqui no caso, o método que eu vou chamar, barra predict. estou dizendo para ele que o header para ele tem um content-type do tipo json, ou seja, vou mandar para ele um dado do tipo json, e estou dizendo para ele que o dado que eu vou mandar, que eu uso esse esse parâmetro "-data", está contido em é o payload.json, na verdade o arroba é só para referenciar que é um arquivo que tá aqui, que é o payload.json. Então com esse comando curl, e aí tem várias formas, você pode usar o Postman para fazer isso, pode usar até o próprio, tem o Thunderclient que faz isso, tem várias outras ferramentas do próprio Visual Code, eu trouxe a ferramenta mais simples que é o curl, que é mais simples assim de você executar. Com isso, se a gente der um enter aqui ó, O que ele vai fazer? Ele vai trazer a predição. Ou seja, ele pegou, rodou o comando, e se eu vier aqui no Python 3.12, que é a sessão do vCorne, veja que só para título de curiosidade, eu trouxe aqui, mandei ele imprimir o tensor. Esse tensor é esse tensor aqui, print outputs, então aqui eu tô mandando imprimir o número que foi a predição antes dele ser transformado de forma inversa, tá? Ou seja, esse 0.0381 ele foi, quando eu fiz a transformação inversa usando o MaxMainScaler, ele se transformou em 388.840.88, ou seja, ele está dizendo que para aquele carro, que é esse payload que está aqui, o valor preço de venda estimado, que ele está estimando, é de 388.840.88. Então, com isso a gente conseguiu concluir todo o processo. salvou os modelos, os artefatos, que é o que a gente precisa, os pesos dos modelos e os pré-processos que a gente precisa usar, a gente codificou a API usando o FastAPI. Lembrando que a gente precisa validar o modelo de dados que a gente recebe com o Pydantic trazer a rede neural, a arquitetura da rede neural para cá e fazer exatamente o mesmo processo que a gente fez no Jupyter notebook para transformar o dado de entrada que a gente recebe bruto no dado que o nosso modelo precisa receber que é um tensor e para isso passando pelos pré-processos e garantindo que na hora que eu saio com o valor neste caso a gente inverta esse valor usando o mesmo preprocessor que a gente usou lá, que foi o MinMaxScaler, porque a gente quis deixar o Y também numa escala de MinMax para que ele não ficasse dançando. Poderia ter deixado ele do jeito que estava? Poderia, mas eu preferi deixar ele no MinMax no mesmo range junto com todos os valores e depois inverter essa ordem por aqui. Então, com isso, a gente conclui esse módulo do Deep Learning 2, esse Laboratório 8, que a gente explorou durante todo o módulo, onde a gente focou principalmente no uso do PyTorch para um problema de regressão. A gente trouxe o dataset, trouxe o problema de regressão, a gente desenvolveu esse problema de regressão durante todo o módulo, finalizando aqui com a entrega desse modelo para uma API. Então, vejo vocês no próximo módulo.