WEBVTT Dando sequência, agora, na parte da API, eu vou começar realmente a mostrar aqui a construção dessa API. Então, a primeira coisa que a gente faz é instanciar a API, e a gente faz isso através de um objeto, geralmente eu chamo essa variável de app, e eu instancio ela com a classe, no caso do FastAPI, e eu posso pôr um título para essa API, tá? Title igual preço de veículos, traço API inferência. Fiz esse estanciamento, garanto que na hora que eu for chamar a aplicação, ele vai deixar essa API no ar. Depois eu vou criar uma classe que valida os dados de entrada. Eu sempre, quando eu vou criar essa classe, eu sempre herdo ela de BaseModel, que é a classe principal do Pydantic. E aqui eu estou dizendo o seguinte, que eu vou receber uma estrutura chamada records, que é uma lista com um dicionário que tem uma chave string e aqui tem um N, ou seja, qualquer tipo de dado, que é exatamente o que tem aqui. eu tenho records, eu tenho uma lista, uma lista de dicionários, onde eu tenho a chave texto e o outro conteúdo que eu tenho aqui é n, ou seja, pode ser um texto, pode ser um número, enfim, então eu não estou validando cada um dos tipos de data poderia poderia validar que tem um recordes que o primeiro dado ele ele é uma categoria que tem um valor numérico e aí se eu tiver mandando essa informação errada num tipo errado o pai dante que vai acusar isso e eu não consigo vamos dizer continuar o código porque na hora que ele foi validar o modelo vai dar erro tá então aqui eu tô deixando sendo menos restrito e deixando ele ele correr Agora tem um aspecto importante. Quando a gente salva o modelo daquela forma, esse best model, a gente não salva a arquitetura do modelo. A gente salva apenas os pesos do modelo. Então, eu preciso aqui, na hora de fazer a inferência, trazer a classe da minha rede neural para esse código. Como que eu poderia fazer isso de outra forma? Eu poderia, no meu notebook Jupyter, ter criado uma classe fora, e tem importado essa classe dessa e dessa forma essa classe seria usada tanto pela API quanto pelo Jupyter Notebook. Poderia ter feito isso para não ter que fazer o código, duplicar o código, tá bom? Nesse caso aqui por uma questão mais didática, então o mesmo código que estava lá no neural network, exatamente o mesmo código eu trouxe para cá como uma classe, com a inicialização e com o forward. Aí eu criei uma função chamada carregar pré-processor, que basicamente ela faz o que? Ela dá um joblib load e carrega esses dois arquivos. Ela carrega um arquivo chamado pré-processor, carrega um arquivo minmaxscaler e retorna essas duas variáveis para quem for chamar. A gente vai chamar isso dentro do método predict. Aqui começa a parte de definição de APIs. Então, tem uma API aqui, que geralmente é feita, que é uma API de Health Check. Então, imagina que é uma API que eu poderia colocar aqui um comportamento que validasse se a minha API está up ou não. Aqui ele simplesmente retorna um status OK. Mas eu poderia colocar algum outro comportamento que realmente garantisse que a minha API está pronta para ser chamada. E aí como é que eu faço? Eu defino a função, no caso aqui eu criei o health, em cima eu coloco um Annotator do Python, que nesse caso para o FastAPI eu coloco sempre arroba app, que foi a variável app lá em cima que eu criei, tá vendo que ele até referenciou .get, que é o verbo do REST que eu quero, e qual vai ser o texto, no caso, como que eu vou chamar essa API. Então, eu vou colocar lá, por exemplo, 127.0.0.1, que é o localhost, a porta barra health. O que ele vai fazer? Ele vai executar esse método aqui e vai me dar um return status igual aqui. É isso que ele vai fazer. Agora, qual que é a API mais importante para nós? A API do predict. Então, eu faço o que? Eu defino aqui um predict, um método predict, que vai receber um request, que é o nome de uma variável, do tipo payload. Payload, a gente já declarou aqui em cima. Payload é uma classe do Pydantic. Então, ele está esperando uma estrutura da forma como eu declarei aqui em cima, que é uma lista de dicionários com a chave string e outro tipo, que é exatamente esse arquivo aqui. Então, é isso que ele está esperando como entrada desse método. Esse formato do Python, só para dar um reforço, quando a gente coloca o nome da variável, dois pontos, e aqui o tipo, a gente está fazendo um enforcing de tipo. Eu estou definindo o tipo que eu vou receber. Se eu não receber esse tipo, vai dar o erro aqui na hora de fazer a chamada. E da mesma forma, eu coloco aqui qual é o verbo que eu vou usar. Então, de novo, eu referencio aqui o annotator arroba app, que é o FastAPI, ponto post, então aqui eu tô usando o verbo post, e tô dizendo que eu vou chamar esse método com a extensão, com a URL, barra predict. Dentro do predict, o que eu vou fazer? E aí eu vou dizer que eu acho particularmente muito simples, porque o que a gente vai fazer é basicamente o caminho que a gente fez no Jupyter Notebook. Vou até abrir aqui para mostrar a similaridade. Então a primeira coisa, a gente precisa carregar os artefatos, no caso o pré-processor e o MinMaxScaler. Então eu vou chamar aquela função CarregarPréProcessos e ele vai trazer aqui qual o pré-processor que eu preciso aplicar nos dados de entrada e qual o pré-processor que eu preciso usar para inverter o valor da minha predição, para transformar de forma inversa o valor da predição por um valor na escala que eu preciso. aí eu carrego os dados do payload, então digo que requests.records é igual a records, simplesmente para tirar dessa estrutura e colocar em uma estrutura de dicionários, e aí eu carrego esse dado de records para um DataFrame do pandas. Então, até aqui é como se eu estivesse fazendo, eu estou abrindo aqui o Jupyter Notebook, é como se eu estivesse fazendo essa parte aqui, deixa eu subir aqui, É como se eu estivesse fazendo isso, eu carreguei esse dado para o DAF Veículos, só que eu não carreguei o Y, só carreguei o X. Eu não vou precisar separar entre NTest, só que eu vou precisar fazer o que? Aplicar essas transformações que eu aplico aqui, pré-processo, fit e transform. Só que como eu já apliquei o fit aqui, eu só vou aplicar o transform. Então, o que eu vou fazer aqui? Eu pego esse DataFrame Veículos, pego o pré-processo, dou um transform e transformo ele no XPROC, que é o XPROCESSADO. Com isso, é como se eu estivesse gerando esse XVAL, vamos pensar assim. Ou seja, eu estou transformando um dado de entrada num dado já pré-processado com todas as transformações de colunas que eu preciso. Feito isso, transformei os dados, eu faço o que? Eu preciso instanciar agora o modelo para poder fazer a estimativa, a predição do modelo. Então, eu tenho que fazer isso em alguns passos. Primeiro, qual é o tamanho do meu input size do modelo? Então, meu input size é o xproc.shape de 1. Exatamente o que eu coloco como input aqui. só que aqui em vez de ser o xstrain eu tô falando que é o xproc que tá lá então eu tô pegando shape de um que o que são as colunas a quantidade de colunas que eu tenho no meu dataframe depois eu ter feito a transformação e esse número tem que bater porque se eu tô fazendo a mesma transformação com o mesmo input a minha saída vai ser a mesma aí eu coloco, crio o modelo primeiro usando a minha mesma classe que eu usei lá no notebook, onde eu coloco o input size, quais são as camadas que eu defini para o meu modelo, que eu treinei, e qual o meu output size. E depois de eu ter instanciado esse modelo com essa classe, eu carrego o dicionário de pesos, que está aqui em BestModel, para dentro desse modelo. Então, na verdade, eu encadeio duas funções, uma que carrega o modelo e outra que pega esse modelo e carrega os pesos dela para cá. com isso esse modelo ele vai ter dentro dele a arquitetura que ele precisa trabalhar e os pesos aos quais a gente treinou e que geraram um best model que a gente definiu lá no jupiter notebook na verdade quando treinou ele falou aqui é o melhor peso que você tem é considerando o early stopping tá a gente usou o early stopping então ele vai trazer exatamente esses melhores pesos Carreguei o modelo, o que eu preciso fazer agora? Carregar, converter os dados para o tensor, que na verdade é isso aqui. Eu tenho aqui os sensores, deixa eu dar uma olhada aqui em cima. que na verdade é isso aqui né na verdade quando eu crio um data loader eu chamo ele para um data loader ele transforma no tensor só que eu tô fazendo essa transformação do tensor vamos dizer na mão então eu tô dizendo torte tensor pega o XPROC e transforma ele num float32 e joga em XTensor Aí eu faço o que? Modelo. Coloco ele no modo de avaliação, porque eu não vou treinar o modelo. Dentro do loop do torch no grade, que é sem gradientes, eu faço o que? Os outputs, que é a minha predição, é igual a modelo x tensor, ou seja, esse aqui é o meu input, aplica esse input no meu modelo, ele aplica nesse modelo meu input e eu vou gerar a saída. Dada a minha saída, eu até imprimo ela, vou mostrar onde que eu posso ver isso na console do uvcorn, eu inverto a escala do resultado. Então eu digo o seguinte, a minha predição é igual a minmaxscaler que eu treinei lá no meu notebook e eu uso o inverse transform na saída. aí eu só apenas coloco ele para um reshape de menos um para ele ficar com apenas uma uma dimensão e eu transformo ele numa lista Feito isso, eu retorno um dicionário, já que eu estou falando de um REST, né? Então, eu estou devolvendo um dicionário, um JSON, onde eu tenho uma chave que é predictions e o valor da predição que está vindo daqui. Então, esse aqui é o código do API.py que a gente precisa para expor o nosso modelo como API. Agora eu vou mostrar como que a gente coloca isso no ar e testar essa API. Vejo vocês no próximo vídeo.