WEBVTT Fala Dev, dando sequência ao que a gente fez no módulo anterior, eu tô trazendo agora um programa, um código em Python, que faz a chamada do nosso modelo, que está lá sendo servido pelo MLflow, através da porta 5001, como a gente fez no passo anterior. Então, esse modelo aqui, só para reforçar, ele está aqui, sendo servido aqui na porta 5001, depois que a gente rodou esse comando Esse aqui, na verdade. Esse. Pronto. Model Passages, Production, porta 5001. Então, o nosso modelo está no ar. Como que é o código para que a gente possa consumir esse modelo dentro de um programa? Imagina que você tem uma outra aplicação que vai mandar os dados de entrada, que são aqueles dados de uma passagem, e ele vai receber uma predição com a recomendação. não comprou, comprou ou esperou e comprou depois. Então, a estrutura do código se parece um pouco com a estrutura do código que a gente faz da API, com a diferença que a gente não vai precisar construir a API aqui, a gente só vai construir a chamada da API que já está servida, já está disponível dentro do endpoint do MLflow. Então, o que eu faço? Eu vou chamar aqui, aqui são os imports que eu vou fazer, aqui esses imports são apenas para que eu garanta que o dado esteja dentro de uma estrutura padrão, tá? E aí eu usando o paidante, que para garantir que a entrada que eu vou receber esteja dentro de uma estrutura. Nesse momento eu não estou validando valor por valor, eu estou garantindo só que o que eu vou receber é uma lista, onde eu tenho uma chave string e um valor de qualquer tipo, tá? Vou usar o Pandas para transformar o dado que eu recebo num DataFrame para depois fazer as transformações. Uso o JobLib para carregar o meu pré-processor. O Torch porque eu vou aplicar o Softmax nele. Eu recebo predições que são logits e eu preciso fazer a transformação desses logits em probabilidades e pegar a maior probabilidade. Toda essa mecânica é feita aqui dentro desse programa. o Pydantic, como eu expliquei, para a gente validar o modelo, e o Requests, que na verdade faz o papel do Curl, que seria uma forma de eu fazer uma requisição post de forma programática dentro do Python. Aí eu criei algumas variáveis para armazenar as informações. Então, por exemplo, uma variável aqui que armazena onde está o meu arquivo de preprocessor, que ele foi salvo durante o processo aqui no model, Como meus códigos estão sempre em source, então eu coloquei um . .model para acessar esse arquivo aqui. e qual que é a URL da minha API do meu modelo que é 5001 e esse invocations na verdade é um é o nome do endpoint que o MLflow disponibiliza para que a gente possa fazer as predições tá então esse nome ele vem fixo e eu só usei esse nome para referenciar aqui dentro Crio aqui um modelo de base de entrada, como eu falei é um modelo que não faz a validação campo a campo, ele só garante que o que está vindo é uma lista de dicionários. Carrego o preprocessor, aqui no caso eu só vou carregar o preprocessor, não vou carregar aqui o modelo scaler, eu vou carregar apenas o preprocessor. O modelo já está carregado ali e o scaler a gente não vai usar porque não é uma regressão. Então eu vou carregar esse modelo dentro de uma variável e retorno para ela. E aí eu tenho aqui o meu método, que é o meu método predict. Então, o que eu faço no meu predict? Eu recebo um payload, que no caso é um dicionário, uma lista de dicionários com os valores de entrada, que eu vou mandar para o meu modelo. Carrego os artefatos, então aqui no caso eu vou carregar o pré-processor para pegar os dados de entrada, transformar esses dados, fazendo um hot encoding e o scaler que a gente usou no nosso notebook para colocar os dados no formato que o nosso modelo precisa. Carrego os dados do payload que estão nessa estrutura de records para uma variável chamada records, e pego essa estrutura records e carrego ela dentro de um DataFrame. Seguindo exatamente a mesma lógica do nosso notebook, eu pego esse DataFrame e aplico um transform usando o preprocessor. Eu não preciso treinar em cima dos dados. Os dados já foram treinados durante o treinamento do modelo, eu apenas aplico o resultado desse pré-processor dentro do DataFramePassagens, e eu gero o que a gente chama aqui de um XPROC, um X que foi processado. Como é que eu faço agora para chamar o modelo? Então, eu crio uma variável chamada response, que recebe o resultado do modelo, e aí eu chamo a biblioteca requests, que é uma biblioteca bem famosa do Python, que faz requisições HTTP, do tipo REST. Então, eu digo requests, qual o verbo, no caso aqui pode ser cat, pode ser post, pode ser put, pode ser delete, aqui eu estou usando o método post, porque esse post é o verbo a qual esse endpoint responde a minha requisição. Ele responde essa requisição com POST e não com GET. Coloco qual é a URL que eu vou chamar e quais são os parâmetros de entrada. Como padrão, o MLflow recebe como parâmetros de entrada um parâmetro chamado INPUTS. Esse nome também é fixo, assim como esse INVOCATIONS. E aí eu mando para ele o que? O XPROC que eu tenho aqui, que ainda é um DataFrame, e eu converto ele para uma lista. Ao fazer esse POST, ele vai acionar o modelo aqui embaixo, eu capturo a resposta desse modelo aqui em response e converto essa resposta para um json e aí eu jogo esse json para o que eu chamei dos logits porque no final o que o modelo traz é um logit ele não aplicou softmax ainda lembra que na arquitetura de rede a gente não aplica softmax ele retorna só os logits E aí eu até imprimo esse logit para mostrar aqui na tela, fazer um trace de passo a passo, os logits que vieram da rede, depois como eu converto em probabilidades e por último como que a maior probabilidade vira a minha predição. Então, o que eu faço? Transformo os logits de volta para as classes originais. Então, eu faço o quê? Pego... Quando eu trago aqui o response do JSON, também, por padrão, o MLflow retorna isso dentro de uma estrutura chamada prediction. Então, ele retorna uma lista, um dicionário, na verdade, onde eu tenho como a chave desse dicionário o nome Predictions. Então, eu estou referenciando esse dicionário, chamado Predictions, e vou pegar os valores que vieram dentro desse Predictions. Eu transformo esses valores de Predictions num tensor do PyTorch, do tipo float32, porque eu estou recebendo logits com muitas casas decimais, e aí eu pego esses logits, esse tensor de predições, que na verdade são os logits, e converto eles em probabilidades usando o Softmax. Feito isso, eu imprimo as probabilidades. Eu vou mostrar aqui as probabilidades também na tela, no console da linha de comando. E aí eu falo o seguinte, qual foi a classe predita? Eu pego o argmax, da mesma forma que eu fiz lá. Eu pego qual é o maior argumento em termos de probabilidade e converto ele para uma lista. Com isso, eu vou ter só a maior probabilidade. Qual é a classe com a maior probabilidade? É a 0 ou a 1 ou a 2? Aí o que eu fiz? Trouxe para cá a mesma lógica que eu tinha feito no notebook. Eu trouxe. Quais são as classes possíveis? Comprou, não comprou e preferiu aguardar e comprou depois. E aí eu criei uma lista que indexa esses textos. Para quê? Para que ele traga o quê? Olha, se a classe predita foi a 1, Então, a minha resposta é não comprou. Se a classe predita que veio desse argmax é 0, então é comprou. E se o argmax for 2, então é o preferiu aguardar. Eu só fiz uma conversão entre este valor e o predict class. Mas por que eu não acionei direto? Porque eu poderia ter trazido aqui no xproc vários valores, eu poderia ter vindo com uma lista de entrada e ele me retornaria uma lista de saída. Então, aqui eu já estou prevendo que eu não tenha só um payload com um dado só. Posso ter um payload com múltiplos registros e ele vai me retornar múltiplas predições. E aí eu dou um return nessa função com o nome de predictions, eu crio um dicionário, eu sempre retorno em JSON, então essa chave vai se chamar prediction e eu retorno a predição que é o nome da classe predita para aquele dado de entrada. Para fechar aqui, eu chamo... Aqui é um conceito padrão do Python, ele executa se o método é o main, e aí eu passo o payload para ele. Então, eu coloco sample payload é igual a esse payload, records, e aqui eu tenho os nomes das colunas exatamente com a mesma estrutura que está no CSV, na mesma ordem e com os mesmos tipos de valores e tipos de dados que estão lá. E aí eu faço o quê? Eu falo que resultado é igual a predict, que na verdade é esse método aqui, passando esse payload. E aí ele faz o quê? Retorna para resultado e eu imprimo esse resultado. O que eu vou fazer aqui? Eu vou executar isso, tá? Então, eu vou dar um clear, vou vir aqui, vou dar um python, predict.py. E é o que ele vai trazer. Então, a primeira coisa que ele trouxe foi como vem os logits do MLflow. Então, ele vem também dentro de um dicionário chamado Predictions, e aí vem dentro de uma lista os logits. depois eu trouxe quais foram as probabilidades então transformei num tensor apliquei softmax ele já aplicou aqui o percentual quatro por cento né é que ele não multiplicou por cem setenta e cinco por cento e dezenove por cento bom qual foi a classe predominante foi a classe de setenta e cinco a classe setenta e cinco é a classe um considerando que aqui é o zero aqui é um aqui é o dois o nosso código aqui zero 1 e 2, ou seja, para esse dado de entrada, considerando o modelo que foi treinado e que está disponível via API no MLflow, a minha predição é não comprou. Então, com isso, a gente encerra esse Laboratório 9, onde fizemos todo o ciclo de desenvolvimento do modelo, passando pela RDA, passando pela transformação dos dados, criação da arquitetura do modelo, treinamento do modelo, visualização das métricas, tanto internamente no Jupyter Notebook quanto pelo MLflow, Já trabalhamos um pouco com o MLOps, fazendo todo esse trabalho de gestão de experimentos, do tracking de experimentos, e também do deployment do modelo, tanto do registro quanto do deployment do modelo, usando o MLflow. E aí terminamos fazendo uma predição usando esse modelo que foi, vamos dizer, deployado, disponibilizado dentro do MLflow. Então, espero que tenham gostado deste módulo, consigo aproveitar também os conceitos que estão aqui, inclusive para fazer algum tipo de ajuste, algum tipo de melhoria naquilo que foi o módulo anterior, o Deep Learning 2. Tudo que foi visto aqui poderia ser aplicado no Deep Learning 2, então poderia usar o ML Ops lá, poderia fazer o intercâmbio de conceitos entre esses dois módulos para ter uma robustez ainda maior de conhecimento em cima de Deep Learning. Vejo vocês no próximo módulo.