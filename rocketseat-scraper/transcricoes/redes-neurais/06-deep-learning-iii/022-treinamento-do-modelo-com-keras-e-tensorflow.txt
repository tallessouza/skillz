WEBVTT Fala Dev, esse aqui é um módulo extra referente ao módulo de Deep Learning 3, onde a gente trouxe um problema de treinamento de uma rede neural para um problema de classificação. Aqui eu tô deixando só um refresh do qual era o escopo do trabalho, onde a gente tinha que fazer um modelo que pudesse melhorar o trabalho de um buscador inteligente de passagens. E aqui no caso desse modelo, é um modelo de classificação, a gente deveria entregar uma classificação que fosse compra uma passagem e não compra ou espera para comprar depois, baseado no histórico de aquisições de passagens que essa empresa tinha em mãos para poder fazer o treinamento dessa rede neural. A gente já desenvolveu esse trabalho usando o PyTorch. E a minha ideia aqui é trazer uma analogia do PyTorch para o TensorFlow usando o Keras como API. Fazendo só um reforço, o Keras é uma API que simplifica ainda mais o trabalho dos frameworks de Deep Learning. Ele foi criado separadamente do TensorFlow lá atrás, mas com uma forte conexão com o TensorFlow. E aí na versão 2 do TensorFlow, ele foi incorporado como parte do framework. Mesmo assim, o Keras, você pode usar o Keras tendo o PyTorch, por exemplo, como o que a gente chama de back-end, já que o Keras é considerado como se fosse uma API de front-end de Deep Learning, ok? Então, o que eu fiz aqui? Coloquei os dois códigos em paralelo para comparar o código e eu vou executar o código passo a passo. Então, a primeira coisa que a gente vai ver aqui de diferença é o import das classes. Eu vou importar o TensorFlow, vou importar o Keras, dentro do Keras Layers e Models, que é como eu construo a minha arquitetura de rede, e eu vou importar especificamente esse utils, onde eu uso esse comando aqui, esse método toCategorical, que é para converter a minha saída do modelo em uma saída categórica. o resto fica tudo igual, com exceção do MLflow, que no caso aqui, como eu vou continuar logando o modelo, eu não preciso importar o submódulo, vamos pensar assim, do PyTorch, do MLflow, com tudo que eu tenho aqui, eu já consigo fazer o login dos experimentos. Então, vou executar aqui a primeira célula, ele vai importar os modelos, os módulos aqui, E aí, vocês vão perceber que grande parte das outras coisas que eu preciso fazer, elas permaneceram, na verdade, inalteradas. Eu só começo a ter alguma grande alteração quando eu começo a trabalhar com o dataset, que tem uma forma bem mais simplificada do que era de você poder lidar com os dados de entrada para o modelo. Então, eu vou avançando aqui desse lado esquerdo, enquanto ele está importando as bibliotecas. É a primeira vez que ele importa, então ele demora um pouquinho mais. Eu vou colocar aqui, preparar dados para treinamento da rede neural. Vou deixar aqui, vamos ver se ele vai terminar. Está demorando bastante, mas porque é a primeira vez e principalmente por causa do TensorFlow. Como ele usa uma versão específica dessa máquina, que é o Mac, então ele tem uma versão específica, que é o do Mac OS, e uma versão específica do Metal, que é o componente, como se fosse uma GPU dessa máquina, para permitir que eu consiga treinar esses modelos numa GPU. Por isso que ele demora um pouquinho mais. Eu vou posicionar aqui. Lá em cima está girando, então quando ele terminar de girar, a gente sabe que ele parou. Vou voltar aqui, preparar dados para treinamento da rede neural. Enquanto ele roda ali, eu vou mostrando aqui as nuances. Então, a gente veja que a parte do dividir o dataset, ele continua da mesma forma, o mapeamento de classes, o divisão entre treino e teste, a mesma coisa, dividir em validação e teste final, a mesma coisa, a criação do pipeline de pré-processamento dos dados fica exatamente a mesma coisa, aplicar a transformação e aí aplicar a transformação veja que nesse notebook eu só aplico a transformação na verdade na parte do x tá que é o pré-processor que eu faço o fit transform na entrada e depois faço transform no val teste aqui no y ao contrário por exemplo é eu fiz no outro notebook do de plano em dois eu não preciso fazer uma transformação do dado de saída, porque a gente já recebe as probabilidades do modelo. Então, eu vou fazer um encoder para descobrir essas probabilidades, tá? Por isso até que isso aqui tá comentado, que seria análogo que foi feito no notebook do Deep Learning 2, onde eu converto esse valor usando o MaxMin como encoder, né? Como Scaler. só que aqui no caso do tensorflow eu preciso fazer um processo que é de pegar essa variável que é uma saída categórica, meu ytrain, e passar por essa função chamada toCategorical para informar aqui no meu dataset que essa saída é categórica. Essa aqui é a única diferença nesse caso aqui na hora da transformação dos dados que eu preciso fazer. terminou ali de rodar que eu vou fazer então eu vou clicar aqui eu vou falar para ele executar as células acima ele vai começar a executar lá em cima já chegou aqui aí eu vou executar essa célula ele vai converter aqui vai aplicar o próprio processo Eu vou salvar o Scaler para o processamento futuro, que é esse Scaler aqui, que faz o Fit Transform, que converte as variáveis numéricas com MinMax e as variáveis categóricas com HotEncoder. Aí eu vou subir aqui, vou mostrar o shape dos dados. O shape dos dados tá aqui. Opa, rodei de novo, por isso que deu erro. Vou rodar aqui, aqui, aqui, aqui. Aqui e aqui. Pronto. E aí eu vou ver, vou rodar aqui o preprocessor de novo para salvar e eu vou visualizar o shape dos dados. Então tem aqui 70% dos dados, 30% dos dados dividido em duas partes. 124 features, já considerando um hot encoder, e aqui três saídas, que são o que? Basicamente os três valores possíveis que eu tenho dentro deste dataset. Quando eu aplico o categórico, ele já verifica qual das classes tem e já transforma aqui na saída da forma correta. Aí, de novo, começa a diferença de como eu trabalho com o dataset no PyTorch, de forma pura, e como que eu faço isso dentro do Keras. No Keras, eu simplesmente crio um objeto chamado dataset, onde esse objeto dataset vem dessa função fromTensorSlices, onde eu informo o xString e o yString, na uma tupla com a camada de entrada, com os dados de entrada e o dado de saída, ponto batch e aí o tamanho do batch size, que no caso aqui eu tô usando 32. Ao clicar aqui, ele gera, por isso aqui ele usou o MetalPlugin, que é justamente esse componente do Mac que simula como se fosse uma GPU, então ele já criou esse dataset e aí eu posso agora começar a criar a arquitetura da rede. Na arquitetura da rede, ao invés de a gente criar uma classe herdando de NNModel do PyTorch com os dois métodos, um construtor e o método forward pass, na verdade é só desenho a arquitetura realmente da rede. Então eu crio um objeto chamado model, aqui ao invés de criar uma classe eu já criei só um método, pego aqui o objeto model, digo que esse modelo é um modelo sequencial, onde eu vou informando as layers na sequência, recebo exatamente os mesmos parâmetros que o meu outro método, então coloco inputSize, hiddenLayerSize, outputSize como padrão 1, mas depois eu vou informar o número certo ali, e o dropout 0.2. e eu vou criando as camadas de rede na mesma estrutura que eu fiz aqui. Tem uma camada de input com o tamanho, aí tem a primeira camada oculta, onde eu coloco uma camada densa, que tem esse tamanho aqui, um batch normalization, um relu e um dropout, tendo como parâmetro dropout rate. Faço a mesma coisa para a segunda layer, para a terceira layer e para a quarta layer. Só que aqui tem uma diferença. Aqui no PyTorch, quando eu crio a arquitetura da rede, eu não faço o Softmax, porque o Softmax é aplicado no loop, quando eu defino que o meu Loss é o Cross Entropy Loss, ele automaticamente aplica o Softmax. Aqui não. Aqui, depois de fazer o Dropout, na última camada de saída, essa camada densa, eu pego essa camada de saída e aplico o Softmax. E aí o que o Softmax vai me entregar? Vai me entregar as probabilidades de cada uma dessas classes. E aí, com essas probabilidades, eu consigo pegar a probabilidade máxima, que vai ser a minha predição, a minha classificação que eu vou fazer. Terminada essa parte, eu dou um return nesse modelo. e eu instancio o modelo. E aí eu vou instanciar da mesma forma, só muda que aqui eu estou instanciando uma classe, estou apenas chamando um método, estou passando exatamente os mesmos parâmetros, só que com o output size de 3, porque eu tenho 3 classes, e o dropout rate de 0.2. Executo esse método, opa, esqueci de executar esse. Executo esse aqui, vou instanciar o modelo, E aí, antes de começar a fazer o treinamento do modelo, propriamente dito, o que eu vou fazer é compilar esse modelo no Keras. Então, eu vou escolher qual otimizador que eu vou usar. Então, eu vou usar o Adam, que foi o mesmo que eu usei aqui nessa rede neural, com o mesmo parâmetro de learning rate. Vou dizer que o meu loss é o categorical cross entropy, que é referente a esse aqui. e eu já posso definir qual métrica que eu também quero avaliar. Lá na frente, não sei se vocês lembram, que eu calculei com a Aruacuracy uma métrica principal e depois até loguei essa métrica principal dentro do MLflow. Aqui eu já posso automaticamente já colocar a métrica na compilação e na hora que eu vou rodando o modelo, além de calcular o loss, ele também vai calculando a métrica, tanto no conjunto de treinamento quanto no conjunto de validação, isso ajuda bastante, acelera muito o desenvolvimento. Eu vou rodar aqui a execução e vou dar um Model Summary, então o Summary também já mostra a estrutura que está no modelo, da mesma forma que aqui no PyTorch eu tenho esse Summary do Torch Summary, aqui eu tenho o Summary do Keras que mostra a estrutura da minha rede. Veja que tem uma diferença pequena aqui, não sei se tem todas as camadas, é porque aqui eu tenho uma camada densa que sai em três, né? Mas dá uma diferença aqui, 11.011, ó, Trainable Parameters, 11.011, exatamente a mesma quantidade do que esse. Eu tenho 240 parâmetros que não são treinados. E aí eu vou fazer o treinamento da rede neural. Para eu fazer o treinamento dessa rede neural, Eu não preciso criar os loops todos, mas eu preciso fazer uma coisa que é importante, que é eu ter uma forma de, ao longo do treinamento, eu logar lá no MLflow, através de um callback, tudo o que está acontecendo no treinamento. Então, o que eu vou fazer? Primeiro, eu vou definir o callback por early stopping. onde eu defino isso numa variável, já pego um callback pronto que disse no Keras, através do keras.callbacks.earlystop, e já defino o que eu quero monitorar, que no caso aqui é o ValLoss, que é uma palavra reservada, dizendo que eu quero capturar a perda do conjunto de validação, qual Patience que eu quero usar, que está aqui nessa variável, qual MinDelta que eu estou usando, e se eu quero que ele restaure os melhores pesos no final do treinamento do EarlyStop. Então, essa composição aqui permite que eu use esse objeto como callback durante o ciclo de treinamento. Só que eu vou criar agora uma classe que também vai ser chamada. Dentro dessa classe chamada MLflowCallback, eu vou herdar de KerasCallbacksCallback. Por quê? Porque, na verdade, não existe um callback específico para MLflow. Então, estou criando uma classe que herda o método de callback do Keras. dentro desse método, eu vou criar um método chamado onAppend, ou seja, quando essa época terminar, mande a época e os logs, que eles começam como none. E aí, aqui dentro, o que eu vou fazer? Eu vou pegar e acionar o MLflow, que seria o que eu faria aqui, embaixo, mlflow, logmetric, trainloss, epoch, trainloss, step, epoch. Eu teria que acumular todos os valores e fazer o log dessa métrica por aqui. Aqui eu já estou fazendo direto logmetrics no modelo de um dicionário. Então, eu estou mandando qual é a chave, o nome da chave que eu quero armazenar e qual que é o log. Esse log vem justamente na hora desse callback. Eu tô mandando pra ele essa informação do log, então ele vai armazenar o que? O train loss, que fica no objeto loss, aqui na chave loss, do log do treinamento, o ValLoss, o Accuracy, que seria a acurácia desse modelo no treinamento, e o ValAccuracy, que é a acurácia do modelo no conjunto de validação. E aí eu uso a mesma coisa no StepEpoch. Ele vai capturar essa época daqui, desse treinamento. Então, feita essa classe, eu agora começo a montar o loop de treinamento e vejam a diferença. Enquanto eu tenho aqui um StartRun, que faço da mesma forma, StartRun, capturo e aí vou logando os parâmetros, e depois eu tenho todo, por época, aquele loop de treinamento, onde eu tenho que colocar o modelo em modo de treinamento, fazer um loop no data loader, calcular, pegar os dados, calcular o logit, aplicar o loss, fazer o backpropagation, ver o clipping, atualizar o peso, e aí depois calcular todas as métricas de train loss e de epoch loss para depois logar. Depois de tudo isso, eu ainda faço toda a fase de validação. Coloco o modelo em eval e faço a mesma coisa, só que sem o backward. Depois eu tenho que ainda controlar o modelo para saber qual foi o melhor modelo. Se o meu modelo parar por learning stopping, eu tenho o meu último modelo salvo aqui. e vou apresentando essas informações todas. No final, ainda logo, o finalTrainLoss e o finalValLoss capturados por todo esse loop. Isso aqui é tudo que eu fiz, que nós fizemos no PyTorch. No Keras é muito mais simples, porque no Keras, na verdade, eu estou armazenando essas variáveis iniciais aqui, na hora que eu entro no loop, e o loop de treinamento, todo esse loop de treinamento se resume a isso, model.fit, trainDataset, epochs igual a numEpochs, que é uma variável que eu armazenei aqui, validationUnderlineData que é valDataset, ou seja, ele já faz o treinamento e já valida esse treinamento num outro conjunto de dados, E aí eu defino os callbacks, ou seja, que callbacks que podem acontecer que eu tenho que acionar durante o ciclo de treinamento. Um deles é o Early Stopping, que vai validar aqui com o Patience e o MinDelta se o Valor está tendo mudança ou não, se não ele para o treinamento. e o MLflowCallback, que vai ser chamado toda vez que a época terminar, ele vai chamar esse método e já vai logar o TrainLoss, o ValLoss da época, o TrainAccuracy e o ValAccuracy, sem eu precisar calcular nada disso, tudo isso aqui já tá sendo calculado dentro do fluxo de treinamento. Então, eu vou rodar esse comando e ele vai começar a rodar. Aqui como o bloco é muito grande, ou seja, na verdade eu tenho milhares de registros, cada batch de dados aqui vai levar bastante tempo. Então, para não ter que ficar acompanhando, são 100 épocas, é muito tempo, eu vou acelerar aqui, depois a gente vai pegar o resultado, e vai dar sequência nisso aqui, mostrando qual que é o History Loss, qual que é o Loss de treinamento, o Val Loss, e a gente vai visualizar os dados do treinamento e ver como que isso funciona em comparação ao que a gente fez no PyTorch. Então, vejo vocês no próximo vídeo.