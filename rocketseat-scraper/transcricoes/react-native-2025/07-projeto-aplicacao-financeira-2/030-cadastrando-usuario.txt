WEBVTT Fala, Dev! Na aula de hoje, nós iremos dar início ao fluxo de registro de usuário aqui no nosso aplicativo. Na aula passada, nós já começamos a consumir os dados lá diretamente do nosso estado global, ou seja, as nossas implementações até aqui estão bem interessantes. Nosso aplicativo já está indo para um outro patamar. E agora, para começarmos a nossa integração, vamos aqui em SRC, Shared Services, aqui em Auth Service, vamos criar uma função nova, basicamente a função responsável por fazermos o cadastro. Vamos colocar aqui um Export Const Register User Register User vai ser uma função assíncrona e ela vai receber como parâmetro basicamente o Form Register Params ou seja, User Data Form Register Params Uma coisa interessante que vale ressaltar é a reutilização que ou seja, userDataFormRegisterParams. Uma coisa interessante que vale ressaltar é a reutilização que nós estamos fazendo de todas as interfaces e tipos que nós criamos anteriormente. Essa noção de reutilização é bem interessante. A interface que nós criamos lá no início da construção do nosso formulário ainda está sendo útil hoje em dia, basicamente com os métodos de registro. Próximo passo, vamos criar uma const desestruturando aqui o nosso dtmoneyapi.post vamos colocar aqui também um await já que o .post vai retornar pra gente uma promise, ou seja, uma promessa, um código assíncrono, e o primeiro parâmetro assim como temos na nossa função de authenticate, é o endpoint no caso aqui o endpoint será barra alph barra register. E o segundo parâmetro, o nosso body, ou seja, os dados que nós iremos enviar lá para o nosso backend, no caso, userData. Vamos vir aqui na nossa desestruturação, vamos pegar o data e vamos dar um return em data novamente. E aqui um detalhe bem interessante, se nós abrirmos a nossa documentação, podemos ver que tanto o alf quanto o register possuem o mesmo retorno, ou seja, o usuário e o token. Isso é interessante porque vamos estar podendo reaproveitar a nossa IAuthenticateResponse. Vamos estar voltando aqui no nosso dtmoney.post. Vamos abrir o menor key e maior key e vamos estar tipando com IAuthenticateResponse. E aqui na nossa função, vamos colocar também uma PROMPT. AuthenticateResponse. Show de bola. Agora vamos pegar o nosso RegisterUser. Vamos vir aqui no nosso contexto novamente, na função de HandleRegister. Vamos fazer a mesma coisa que nós fizemos na função de Authenticate. Ou seja, vamos vir aqui e colocar um const. Vamos estar desestruturando o nosso await offService.registerUser Lembrando que como nós importamos todos os métodos que estão dentro do nosso arquivo e atribuímos um alias a ele, então ele já vai recomendar pra gente todos os métodos que estão disponíveis dentro do nosso arquivo.ts. Vamos estar invocando a função e passando o formData como parâmetro Aqui de dentro vamos desestruturar tanto o token quanto o usuário e vamos colocar o setUser e por fim o setToken, passando o nosso token de autenticação. Por fim agora vamos pegar o nosso handleRegister e vir no nosso registerForm, vamos vir aqui na nossa função de onSubmit, vamos estar passando para dentro dela, mas para isso nós precisamos estar acessando as funções que estão dentro do nosso contexto, vamos estar colocando aqui const, desestruturar basicamente o useOfContext dentro do useOfContext vamos pegar o handle register e esse handle register da mesma forma que nós fizemos aqui no nosso login form, criando o try catch para nós capturarmos todas as exceções retornadas pela nossa API ou até mesmo a nível do nosso código vamos fazer a mesma coisa aqui try e um catch possuindo um error a princípio vamos colocar aqui também uma validação if error instance of ou seja, se ele for uma instância da classe de AxiosError vamos estar dando um console.log em error .response .data lembrando que error.response.data. Lembrando que esse .response e .data é basicamente todos os dados retornados pelo próprio Axios quando a nossa API retorna alguma exceção. Por isso que nós validamos se ela é uma instância de AxiosError, para a gente ter certeza que tanto o response quanto o data estarão presentes dentro do objeto de erro. E aqui dentro do try, vamos colocar um await, handleRegister, passando aqui os dados do formulário que nós iremos receber como parâmetro do nosso unsubmit, então userData, vamos estar tipando aqui com o formRegisterParams, e vamos passar ele para dentro do nosso handleRegister. Dessa forma, quando nós tentarmos cadastrar um usuário novo, nós já seremos autenticados. Vamos fazer o teste, vamos colocar aqui test teste dois arroba gmail.com a senha um dois três dois três e a confirmação de senha um dois três dois três quando clicar vamos em cadastrar Olha só que maravilha a nossa API já registra para gente usuário e pelo fato de nós já estamos consumindo todos os dados do nosso contexto aqui no nosso navigation routes o nosso isso Use Callback já remonta as rotas para a gente exibindo apenas as rotas privadas. Um detalhe interessante que podemos fazer é vir aqui no nosso BKipper e olhar o nosso banco de dados, assim como eu ensinei lá no módulo anterior. Podemos ver que os dados já estão sendo cadastrados aqui no nosso banco de dados. Então, maravilha, já temos certeza que tudo o que nós fizemos até agora está dando frutos, está funcionando devidamente isso daqui é muito bom, a partir da próxima aula nós iremos persistir os dados do usuário na nossa aplicação, para que quando a gente saia do aplicativo, ou quando a gente rode um reload aqui no nosso app, a gente continue nas nossas rotas privadas, então é isso, espero vocês lá