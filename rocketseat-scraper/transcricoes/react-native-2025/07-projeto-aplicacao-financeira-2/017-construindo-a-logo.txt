WEBVTT Hoje iremos desenvolver o componente de imagem que irá ser exibido tanto na tela de login quanto na tela de registro, porém iremos colocar um comportamento diferente nele. Vamos fazer com que quando o nosso teclado estiver aberto, a nossa imagem seja escondida. Dessa forma a gente maximiza a utilização do espaço da nossa aplicação, escondendo o nosso ícone toda vez que o nosso teclado for exibido. Isso para dispositivos menores é muito bom, é muito interessante. Primeira coisa que nós precisamos fazer é vindo aqui em component, já que é um component compartilhado, vamos estar criando um novo chamado off header. Dentro de off header vamos estar colocando o index.tsx e aqui vamos estar criando um functional component padrão. off header e maravilha. Aqui vamos estar simplesmente retornando uma view a princípio, e aqui dentro da nossa view vamos estar colocando o componente de image do próprio React Native, e show de bola. Vamos pegar agora o nosso componente de off-header e passar aqui para dentro do nosso login, acima do nosso login form, dessa forma aqui, e show de bola. Agora um detalhe muito importante, eu vou estar disponibilizando para vocês o download da nossa logo, então assim que vocês baixarem, vocês vão vir aqui em src, vão criar assets e vamos jogar aqui a nossa logo dentro de assets, show de bola vamos passar aqui para dentro Das propriedades de image Um source, e dentro de source require No require vamos aqui passar Uma string Arroba barra assets Barra logo.png No caso o nome da nossa imagem E olha só que maravilha Nossa logo já está sendo renderizada Agora vamos estar aplicando aqui os estilos, um class name, com item center, um justify center, garantir que a nossa logo sempre fique centralizada no nosso componente, um beautiful human height 40. Show de bola. Agora aqui no nosso componente de image Vamos estar passando class name Um height de 48 pixels, lembrando que pra passar pixels Tem que ser dentro de colchetes E um width de 255 pixels Dessa forma a gente já ajusta O tamanho da nossa logo Agora vamos fazer o comportamento que eu disse no início da aula Que é fazer a nossa logo se esconder Toda vez que o nosso teclado estiver ativo Para isso nós vamos vir aqui em Shared. Vamos estar criando uma pasta nova chamada Hooks. E dentro de Hooks. E dentro de Hooks vamos estar criando um Use. Keyboard. Visible. Onto TSX. Vamos estar criando aqui um Custom Hook. Um Hook customizado cujo objetivo dele é retornar para a gente uma variável booleana. Dizendo se o nosso nosso keyboard, no caso o nosso teclado, tá sendo exibido ou não. Então vamos criar aqui um Functional Component padrão Export Const Use Keyboard Visible Vamos simplesmente retornar o fragment por hora pra ele não ficar apontando erro pra gente, mas depois a gente vai remover ele. A primeira coisa que nós vamos fazer dentro do nosso Custom Hook é criar um estado. esse estado basicamente vai controlar quando o nosso teclado vai estar sendo exibido ou não ou seja vai ser um use State do tipo booleano vai começar como falsa o nome dele vai ser um isso keyboard visible set is keyboard visible show de de bola. Próximo passo, vamos estar colocando aqui um useEffect pra que ele seja executado toda vez que o nosso custom hook, toda vez que o nosso componente for renderizado em tela e dentro do nosso useEffect vamos criar uma constante chamada keyboard showListener e dentro dessa constante vamos basicamente chamar aqui o keyboard do próprio React Native. Dentro deste é possível nós basicamente chamar aqui o Keyboard do próprio React Native. Dentro deste Keyboard, é possível nós chamarmos um método chamado .EdListener. Esse .EdListener basicamente adiciona um ouvinte. O que seria um ouvinte no React Native? Ouvinte são funções que basicamente escutam eventos do sistema, como por exemplo, a abertura do teclado, a rotação da tela, o foco em o input, etc. E o primeiro parâmetro que o AdListener espera é basicamente o nome do evento que irá ser executado. Será o KeyboardDidShow. E como segundo parâmetro do AdListener, vamos adicionar aqui uma callback. A callback, no caso a função, que será executada toda vez que o nosso evento for executado também, como neste contexto aqui. Toda vez que o nosso teclado aparecer nós vamos executar tudo que está dentro dessa função e o que nós vamos fazer aqui dentro é basicamente dar um set is keyboard visible para true e da mesma forma que nós criamos aqui uma variável para o nosso listener de abertura de teclado, vamos criar um para quando ele sumir, para quando ele for finalizado, vamos colocar aqui um keyboard hit hide sumir para quando ele for finalizado vamos colocar aqui um keyboard did hide listener aqui vai seguir o mesmo padrão keyboard ponto add listener só que no caso o nome do nosso evento será o keyboard did hide ou seja, toda vez que o nosso teclado for escondido vamos executar esta callback e aqui é basicamente um set KeyboardVisible para false. Dessa forma a gente garante que o nosso estado esteja sempre atualizado com base no status do nosso keyboard, do nosso teclado. E agora um detalhe muito importante dentro do nosso useEffect, vamos estar colocando aqui um return, vamos passar aqui uma callback. Dentro dessa callback, vamos chamar as duas variáveis que nós criamos aqui, a keyboardHideLister, vamos pegar aqui um .remove, e a mesma coisa com a outra, no caso, a keyboardShowLister.remove. Bom, e para explicar o que está acontecendo aqui, o useEffect executa uma função toda vez que o nosso componente é criado, porém dentro do ciclo de vida de um componente React, nós também temos um momento em que o componente é desmontado de tela e basicamente esse return executa uma função toda vez que o nosso componente for removido, dessa forma a gente garante limpar todos os listeners que nós criamos anteriormente e show de bola. Agora o que vamos fazer é basicamente pegar esse isKeyboardVisible e colocar aqui no return. Agora vamos pegar aqui o nosso useKeyboardVisible, vamos voltar no nosso offHeater e vamos fazer o seguinte, vamos colocar aqui um const KeyboardIsVisible igual a use KeyboardVisible. Se nós repararmos, esse componente retorna pra gente um booleano e aqui vamos simplesmente colocar um if, uma validação, se o nosso KeyboardIsVisible, E aqui vamos simplesmente colocar um if, uma validação, se o nosso keyboard is visible. Nós vamos simplesmente retornar um fragment. E olha só que interessante, a nossa implementação já está funcionando. Se nós fecharmos aqui o nosso keyboard, o nosso teclado, a nossa logo vai voltar a ser exibida. Na próxima aula nós iremos criar os botões aqui da nossa tela de login e de registro. Então eu aguardo vocês lá.