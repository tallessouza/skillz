WEBVTT Fala, Dev! Beleza? Na aula de hoje nós iremos dar seguimento com o fluxo de autenticação do nosso app. Nós já estamos realizando tanto o registro quanto o login do nosso aplicativo, isso daqui é bem interessante. Porém, faltou uma coisa de suma importância, que é persistir os dados do usuário na nossa aplicação. Mesmo se nós sairmos do app e entrarmos novamente, ou se nós rodarmos um reload aqui no nosso aplicativo, os dados vão se persistir. Então vamos estar removendo aqui esse useState que não vamos estar utilizando mais aqui no Navigation Houts. Vamos vir aqui no nosso offContext e é aqui que a mágica vai acontecer. Vamos primeiramente parar a execução do nosso aplicativo. Vamos instalar uma biblioteca chamada SyncStorage. Vamos colocar aqui o yarn add React Native a SyncStorage. Eu vou estar disponibilizando o link da documentação dela na descrição da aula. Vamos estar rodando novamente o nosso aplicativo Yarn Android. O próximo passo que iremos fazer na integração do nosso async storage é importar ele aqui no nosso auth context import async storage from react native async storage. O conceito da persistência dos dados basicamente é no momento do registro ou do login do usuário, nós iremos salvar na memória do nosso dispositivo um JSON contendo tanto o dado do usuário, tanto o token de autenticação. Para isso aqui, na função de Random Authenticate, vamos colocar um await asyncstorage.setitem. Esse setitem é basicamente um método que vai setar os dados na memória do nosso dispositivo. Ele espera receber dois parâmetros. O primeiro parâmetro sendo aqui, ou seja, a chave dos itens que nós iremos salvar na nossa memória, ou seja, nós podemos salvar múltiplos itens diferentes com a Sync Storage, porém é importante que a gente saiba qual chave que nós salvamos eles pra gente tá podendo consumir depois, vamos tá colocando aqui simplesmente um dt, money, user, e o segundo parâmetro é basicamente uma string que a gente vai querer salvar No caso um JSON Ou seja, vamos colocar aqui um JSON.stringify Ou seja, vamos transformar um JavaScript em uma string JSON Vamos colocar um objeto aqui dentro com o User E o Token Maravilha Da mesma forma que nós fizemos isso em Handle Authenticate, nós iremos fazer em handleRegister, ou seja, o fluxo é basicamente o mesmo. Vamos pegar tanto o user quanto o token e vamos estar salvando aqui dentro. A próxima coisa que nós iremos fazer dentro do nosso context de authenticate é criar uma função de restauração de dados, ou seja, toda vez que nós abrimos o nosso aplicativo, nós iremos executar esta função que irá pegar os dados do nosso syncStorage, ou seja, toda a string que nós salvamos, irá converter ela para o objeto JavaScript e irá setar todos os estados do nosso context novamente com os dados que nós salvamos anteriormente. Para isso, vamos iniciar tipando aqui o nosso authfa conta que se tá aí vamos criar uma função nova chamada restore user session ou seja vamos restaurar a sessão do usuário onde será basicamente uma uma função promise irá retornar uma string ou nulo bonzinho aqui dentro do nosso contexto agora vamos criar a nossa função abaixo aqui de renda logo out cost restore user session vai ser uma função assíncrona onde nós vamos criar uma constante const userData igual a asyncStorage ponto getItem ou seja, diferente das outras vezes que nós utilizamos o asyncStorage que era pra setar o item, agora vamos estar buscando ele e ele espera basicamente receber aqui, o caso aqui que nós passamos como parâmetro no nosso set item, vamos estar copiando e colando ele aqui e se nós passarmos o mouse em cima dele, podemos ver que ele irá retornar o string ou o nulo, ou seja, o JSON ou o nulo, caso não exista nada dentro desse dt money user desta forma vamos simplesmente fazer uma validação, se o userata existir, nós iremos primeiramente transformar o nosso JSON em um objeto JavaScript novamente, ou seja JSON.parse userData vamos estar alterando aqui userData para deixar o D maiúsculo userData, estou de bola faltou a gente colocar aqui o nosso await, porque o getItem é uma função assíncrona, então a gente colocando await aqui e validando se o user data existe ou não, o nosso JSON ponto passe já para de retornar um erro. Vamos colocar aqui agora um as iAuthenticate response, ou seja, quando nós convertermos a nossa string JSON em objeto JavaScript, basicamente ele terá os dados de autenticação, ou seja, o user e o nosso token e aqui dentro nós simplesmente desestruturamos E o próximo passo nós damos um set user no usuário E um set token no nosso token Show de bola Agora por fim nós simplesmente damos um return user data Pelo simples fato de que Na próxima aula nós iremos criar uma tela de loading no nosso aplicativo Em que dentro deste loading A depender do tipo de dado retornado pelo nosso RestoreSession, nós iremos deslogar o usuário ou não. Então, por isso, nós estamos simplesmente retornando aqui o UserData. Por fim, nós simplesmente passamos aqui o nosso RestoreUserSession dentro do nosso Value e a nossa função de restauração de usuário já está feita com sucesso. Espero vocês na próxima