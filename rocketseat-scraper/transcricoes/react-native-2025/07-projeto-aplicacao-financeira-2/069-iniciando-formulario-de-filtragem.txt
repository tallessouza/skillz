WEBVTT Hoje nós criaremos o componente de Botton Sheet aqui do nosso aplicativo contendo todos os filtros disponíveis aqui na nossa listagem de transações assim como nós já fizemos anteriormente na hora de editar uma transação ou até mesmo na hora de criar uma nova nós vamos reaproveitar o mesmo componente que criamos lá no nosso Botton Sheet Context. Vamos aqui dentro do nosso Filter Input e logo abaixo do nosso UseTransactionContext, vamos criar uma nova constante, vamos desestruturar o nosso useButtonSheetContext, e daqui de dentro nós vamos pegar a função de openButtonSheet, e vamos simplesmente passar ela pra dentro do nosso ToastableOpacity aqui, contendo o nosso ícone de filtro, vamos simplesmente colocar aqui no evento de onPress, chamar uma callback e passar o nosso openButtonSheet sheet mandando como primeiro parâmetro simplesmente um fragment por hora e como segundo parâmetro vamos passar aqui o index ele exige lembrando que esse index é basicamente definido lá no nosso use button sheet context aqui nessa variável onde nós temos duas porcentagens pré-definidas onde o índice zero ou seja o primeiro item quando ele for utilizado o nosso button sheet irá abrir ocupando 70% da tela. Já com o segundo, no caso, o Index de número 1, nós iremos abrir o nosso Botton Sheet ocupando 90% da tela. Como a nossa filtragem será um pouquinho grande, vamos colocar logo passando 90%. Agora quando nós clicarmos aqui no nosso botão de filtro, podemos ver o nosso Botton Sheet sendo exibido. Olha só que massa! Vamos ver aqui dentro do nosso filter input, vamos criar um arquivo novo, vamos criar um componente interno aqui dentro dele, transaction filters, onde aqui dentro vamos criar um index.tsx, e aquiurada o nosso formulário de filtragem, teremos basicamente aqui um contendo o nosso botão para fechar um título de filtrar transações, teremos aqui no topo de tudo a filtragem através de data, ou seja, teremos um empate na frente do outro, onde quando nós clicarmos, vamos abrir o seletor de data aqui no nosso app. Logo abaixo, temos a sessão de categoria, onde será basicamente uma listagem contendo todas as categorias que nós temos aqui no nosso app. E por fim, o tipo, se vamos querer filtrar pelas transações do tipo de entrada ou de saída. E aqui no final teremos basicamente dois botões, o de limpar filtro e o de filtrar, onde no caso de nós clicarmos em qualquer um desses dois, nós vamos executar alguma funcionalidade e fechar o nosso bottom sheet. Então, com tudo isso em mente, vamos pôr a mão no código. Primeiramente, vamos construir aquele nosso reader que temos no nosso formulário. Vamos colocar aqui uma outra view. E aqui na nossa view, pai, vamos colocar um class name contendo um flex1 pra ele ocupar 100% do espaço disponível. Vamos colocar um bg, grey, na tonalidade 1000, e um padding de 6 pra criar um espaçamento ali internamente. Vamos já aproveitar, pegar o nosso TransactionsFutures e vamos passar aqui para dentro do nosso OpenButtonSheet, aqui no nosso componente de FutureInput, no lugar do Fragment que nós tínhamos colocado. Vamos só importar ele aqui. E maravilha, agora podemos ter um vislumbre aqui do nosso formulário conforme nós formos integrando ele. Próximo passo aqui dentro da nossa View, filha, onde um espaço aqui dentro da nossa viu filha onde nós colocaremos o nosso rio vamos simplesmente colocar aqui um teste onde vamos importar do react-nate e vamos colocar dentro desse texto filtrar transações logo abaixo desse texto e vamos ter de colocar um ícone então vamos importar aqui importe arroba Expo barra vector icons vamos importar o nosso material icons como é de costume vamos colocar ele logo aqui abaixo do nosso text e vamos passar um name simplesmente de close. Vamos passar aqui também uma prop de size de 20 pra ele ficar um pouco maior e por fim um color. Vamos buscar a nossa variável de cores aqui. Ponto gray na tonalidade de 600. Maravilha. Vamos abrir aqui o nosso button sheet novamente e olha só que massa. Já temos aqui estruturado o nosso header. Vamos agora colocar uma estilização na nossa ViewPie aqui do nosso Healer. Primeiramente, vamos colocar um FlexRow para jogar todos os itens filhos um na frente do outro. Um JustifyBetween para jogar todos os itens na extremidade. E aqui dentro do nosso Text, vamos colocar simplesmente um TextXL, que é o tamanho da nossa fonte. Um FontBold, um MarginBottom de 5 e um text-white que é basicamente a cor do nosso texto. Maravilha! Já podemos aproveitar, vamos pegar aqui dentro do nosso transactions features, vamos desestruturar o nosso use-bottom-sheet-context, o nosso close, vamos simplesmente englobar o nosso material-icon dentro de um touchable opacity, vamos importar do React Native, vamos mover o nosso ícone aqui para dentro e já vamos passar para dentro do evento de OnPress, o nosso CloseButtonSheet, para já deixar esse botão funcional. E olha só, perfeito. O próximo passo que nós vamos fazer aqui no nosso button sheet é simplesmente criar o nosso componente de filtragem por data. Para isso, vamos vir aqui dentro do nosso TransactionFilters, vamos criar uma pasta nova chamada DateFilter. E dentro de DateFilter, vamos criar um index.tsx e aqui um FunctionalComponent padrão, ExportConst, DateFilter, onde aqui a princípio vamos simplesmente dar um return em um fragment, e dentro deste fragment vamos colocar aqui um text, vamos importar do React Native e colocar aqui dentro data. Perfeito. Vamos pegar já aqui o nosso DateFilter e vamos passar para dentro do nosso TransactionsFutures. Já para podermos termos um feedback do que nós estamos desenvolvendo, vamos simplesmente passar ele dessa forma. Voltar aqui no nosso componente de DateFilter, vamos criar uma view logo abaixo do nosso text, importando aqui do React Native, onde aqui vamos colocar um className contendo um flexRow, para jogar os itens um na frente do outro, lembrando que nesse filtro de data teremos dois inputs, um na frente do outro. Vamos colocar aqui também um justify between para jogar os itens na extremidade e um margin bottom de 6. Dentro desta nossa view, vamos simplesmente colocar uma outra view, onde nós iremos encapsular o input onde nós iremos clicar para abrir o nosso date picker. Vamos vir aqui no nosso Google Chrome e vamos pesquisar sobre essa biblioteca React Native Motel Date Picker. Vamos utilizar ela dentro da nossa aplicação para podermos selecionar a data que o usuário vai poder filtrar. Para isso, vamos vir aqui embaixo, vamos copiar todo esse script com Yarn, para nós instalarmos tanto o React Native Model Data Picker, quanto o React Native Community Data Picker, que é a biblioteca que o React Native Model Data Picker utiliza por trás dos panos. Então, vamos copiar aqui, voltar lá na nossa aplicação, vamos parar a execução do nosso app, vamos colar o script que copiamos lá do site do NPM. Show de bola, vamos rodar o Yarn Android novamente. Lembrando que muito provavelmente essa biblioteca utiliza códigos nativos, então é normal que demore um pouco mais para estartar, o que nós já estávamos habituados. Sempre normal quando nós instalamos uma biblioteca que não possui apenas alterações e adições JavaScript dentro da nossa aplicação, que ele tenha também algum código nativo internamente, que ele demora um pouquinho mais para estartar do que nós estamos acostumados. Nosso próximo passo será vir aqui dentro do nosso componente colocar aqui um import dateTimePicker diretamente do nosso React Native modal DatePicker e aqui dentro basicamente vamos criar dois estados onde nós iremos controlar quando o nosso modal será exibido ou não. Então, será um useState, começando com o false. Teremos aqui o showStartDatePicker e o setShowStartDatePicker. Vamos copiar ele e simplesmente alterar o nome para em vez de start, end. E a mesma coisa aqui na nossa função de update, end. Agora aqui dentro dessa nossa view, vamos simplesmente colocar um class name com width de 48%. E aqui dentro vamos colocar um touchable opacity. Nesse touchable opacity já vamos colocar um estilização contendo simplesmente um rounded MD, um padding de 2, um border B e um border gray 800. Já temos aqui o nosso primeiro componente. Agora aqui dentro vamos simplesmente colocar por hora um text. Onde dentro desse text nós simplesmente vamos colocar aqui um new date. .toString. Agora quando nós clicarmos lá podemos ver a a data Porém ela tá bem mal formatada Vamos simplesmente colocar aqui então Da seguinte forma, vamos englobar o format Do date.fns aqui em volta Dessa nossa new date, vamos colocar aqui Uma string contendo simplesmente um Dia, mês E ano Vamos ver como ficou Olha só, show de bola, vamos colocar aqui agora Uma estilização, um class name, contendo um text white por hora e um text LG para o tamanho da nossa fonte. Perfeito, vamos simplesmente copiar toda essa view que nós criamos aqui com 48% e vamos colocar aqui abaixo. Vamos simplesmente deixar o format date por hora. Já vamos aproveitar aqui dentro do nosso primeiro Touchable Passage. Vamos colocar um evento de onPress, uma beck aqui dentro com os set show start date picker para true vamos simplesmente copiar esse nosso evento e passar aqui para dentro do nosso segundo touch o pessoal porém dessa vez alterando o set show em de date picker e agora para podemos visualizar o nosso date time picker de fato vamos aqui logo abaixo das nossas vias vamos simplesmente colocar ele da seguinte forma, vamos abrir o menor que, date time picker e vamos colocar na propriedade de isVisible, o estado que nós criamos, então no caso o show, start date picker, aqui em date vamos simplesmente colocar o new date por hora agora ele tem algumas outras propriedades que são obrigatórias, como o on cancel e o on confirm então vamos criar aqui uma função const on start cancel onde vai ser simplesmente uma função que vamos setar o set show start date picker para a falsa vamos pegar aqui agora a nossa função e passar aqui para dentro do on cancel do nosso date time picker e da mesma forma nós vamos criar também um on confirm, então aqui vamos colocar um const on start confirm vai ser simplesmente uma função onde dentro dessa função vamos receber um parâmetro no caso selected date, que será uma date e aqui dentro assim como nós fizemos no on cancel, vamos simplesmente passar o set show date picker para false, até porque quando nós efetivamente selecionarmos a nossa data vamos querer fechar o nosso modal, vamos pegar aqui a nossa função e passar pra dentro do on confirm, e agora se nós clicarmos aqui dentro do nosso botão, podemos ver o nosso modal de seleção de data aparecendo, olha só que massa, uma propriedade nova aqui, vamos colocar aqui, mode vamos simplesmente colocar date pra que ele não solicite também as horas da criação, vamos simplesmente estar buscando por data, também vamos colocar aqui algumas customizações para o iOS por exemplo, como o confirm text iOS, vamos colocar aqui o confirmar e o cancel text iOS, cancelar vamos colocar aqui também um locale como pt underline br maiúsculo, agora vamos simplesmente copiar esse nosso componente de DateTimePicker e colocar aqui abaixo. Porém, alterando as propriedades de IsVisible, vamos colocar o show and DatePicker. E aqui, assim como nós fizemos com o start, vamos copiar essas duas funções e vamos criar especificamente aqui para o end também. Vamos simplesmente colocar aqui um onEndCancel e um onEndConfirm. Vamos pegar essas nossas duas funções e passar aqui no lugar do onEndCancel e do onConfirm. Dessa forma temos os nossos dois DateTimePickers, cada um com um estado responsável por gerenciar quando nós vamos exibi-los de fato ou não. cada um com um estado responsável por gerenciar quando nós vamos exibi-los de fato ou não. Na próxima aula nós vamos implementar essas funcionalidades e salvar os nossos filtros dentro do nosso context. Então é isso, espero vocês lá.