WEBVTT Fala, Dev! Hoje finalmente vamos dar início ao nosso scroll infinito aqui no nosso aplicativo. Aquela funcionalidade de sempre que nós vamos chegando no final da nossa lista, o nosso app automaticamente requisitar novas transações para ir concatenando, dando a sensação de, como o próprio nome diz, um scroll que nunca acaba. Vale ressaltar a importância de termos o nosso back-end rodando para podermos buscar todas as nossas transações. Também um fator de suma importância é nós entendermos como será feita essa paginação. Basicamente aqui no get de barra transaction, nós podemos enviar para a nossa API algumas propriedades, como por exemplo, page per page e alguns filtros, no caso search text e type ID, que no caso não nos vai ser útil no momento, vamos utilizar apenas o page e o perpage o page é basicamente o grupo de itens que nós vamos buscar no momento e o perpage é a quantidade de itens que nós vamos retornar por request dessa forma o nosso back-end fica responsável por calcular quantas páginas nós vamos ter um exemplo rápido vou utilizar um array de números para simplificar aqui para vocês basicamente quando nós vamos ter um exemplo rápido vou utilizar uma rede números para simplificar aqui para vocês basicamente quando nós vamos fazer um request lá para nossa API buscando apenas dois itens por vez nós teremos aqui no total de cinco páginas agora se nós alterarmos o parâmetro por exemplo se a em vez de perpédio nós passamos simplesmente dois nós passamos cinco nós teremos apenas duas páginas já que é levado em consideração o total de transações que nós temos. Então, bora pôr isso na prática! Primeiramente, vamos criar aqui dentro do nosso contexto de transações um estado novo const pagination set pagination. Vamos colocar aqui o useState, que por enquanto vai simplesmente ser um objeto. que por enquanto vai simplesmente ser um objeto. Dentro da nossa interface de GetTransactionRequest, vamos criar aqui uma interface nova, ExportInterface. O nome da nossa interface vai ser basicamente pagination. E aqui dentro vamos ter page, vai ser um number, um perPage, que também vai ser um number, e por fim um totalRows, que vai ser muito útil também, onde vai ser um valor op por fim um totalRows, que vai ser muito útil também, onde vai ser um valor opcional, um number. Agora vamos pegar a nossa interface de pagination e vamos voltar no nosso transaction context e vamos passar aqui para dentro do nosso useState entre menor que e maior que, vamos importar ela e agora vamos simplesmente passar os valores iniciais, onde page nós vamos pegar a página 1, o pair page vamos pegar 15 transações por request e o total rows vai iniciar como valor 0, show de bola próximo passo que nós precisamos fazer aqui, toda vez que a nossa função de fetch transaction for executada, nós vamos ter de atualizar o nosso estado até porque se nós realizamos o nosso request, logicamente nós já vamos ter aí algumas transações então nós vamos buscar as próximas transações depois daquela que nós já buscamos anteriormente então vamos adaptar a nossa função para irmos atualizando o nosso estado toda vez que ela for executada primeira coisa que nós vamos fazer é receber como parâmetro aqui dentro da nossa função a página que nós vamos estar requisitando no momento. Para isso, vamos ter de alterar aqui no nosso transaction context type, até porque quando nós criamos aqui a tipagem dela, nós falamos que ela não vai receber nenhum parâmetro, mas agora ela vai começar a receber uma interface, fetch transactions params. Basicamente, aqui vamos ter uma page, que é um number. Vamos pegar o nosso fetch transaction params basicamente aqui vamos ter uma page e é um número vamos pegar o nosso fetch transaction params vamos passar aqui para dentro do nosso fetch transactions vamos colocar aqui params e passar a nossa interface agora aqui dentro nós vamos simplesmente abrir e fechar chaves e vamos tipar com o nosso fetch transaction params dessa forma vamos pegar page e vamos passar um default value para ele a próxima coisa que nós vamos fazer aqui no nosso fetchTransaction é colocar aqui o nosso setLoadingComoTrue, ou seja, toda vez que iniciarmos a busca das nossas transações, vamos setar o nosso estado de carregamento para aparecer aquele feedback visual que nós criamos na aula passada. Agora nós vamos fazer o seguinte, aqui dentro do nosso array de dependência do nosso fetchTransaction, vamos ter de adicionar algumas variáveis a mais, até porque nós vamos passar a utilizar o nosso estado de pagination, ao invés de simplesmente mandar mocado dessa forma. Então vamos adicionar aqui o nosso pagination. E agora vamos simplesmente apagar os parâmetros que estamos passando mocados aqui para a nossa requisição. E vamos fazer o seguinte. O page é basicamente o que estamos recebendo aqui como parâmetro da nossa função perpage vamos pegar lá do nosso perpage do nosso pagination. Perpage o próximo passo agora após a execução aqui do nosso request vamos colocar um if aqui abaixo ou simplesmente colocar um e se a página for igual a um nós simplesmente vamos colocar aqui o nosso set transactions passando assim o nosso transactionResponse.data. Agora, caso contrário, ou seja, essa daqui não é a primeira requisição que nós estamos fazendo, ou seja, não é o primeiro request com a page1, nós vamos simplesmente fazer o seguinte. setTransactions, vamos abrir parênteses e abrir uma callback aqui dentro do nosso setState e vamos colocar aqui o nosso prepState, ou seja, o valor anterior que nós tínhamos no nosso estado de transações antes de nós atualizarmos ele. Vamos colocar aqui para a nossa callback retornar um array, dando um spread operator em prevState e também um spread operator no nosso transactionResponse.date. Dessa forma, nós garantimos que toda vez que nós realizarmos um request na nossa fetch transaction em uma página que não seja a primeira, nós ao invés de setarmos todos os valores dentro do nosso estado, nós vamos simplesmente concatenar, dessa forma persistindo as transações que já existiam e concatenando com as novas transações que o back-end retornou para a gente. Olha só que bacana! Agora, aqui embaixo, nós vamos simplesmente remover esses set transactions. Que temos aqui fora dos nossos ifs. Já que nós não vamos mais setar desta forma. Vamos aqui abaixo do nosso set total transaction. Colocar um set pagination. Dentro de set pagination. Vamos dar um spread operator aqui. Na própria pagination. E vamos adicionar aqui a page. Que nós estamos buscando no momento Que estamos recebendo por props E por fim, vamos colocar aqui O nosso total rows Que vai estar vindo do nosso TransactionsResponse.totalRows Show de bola Agora vamos vir aqui dentro do nosso Componente de home Vamos passar um objeto aqui para o nosso fetch transaction Já que ele está reclamando de erro Vamos passar aqui o fetch de 1 Vamos dar um reload aqui na nossa aplicação para o nosso fetchTransaction, já que ele está reclamando de erro. Vamos passar aqui o fetch de 1. Vamos dar um reload aqui na nossa aplicação. E por fim, vamos voltar lá na nossa fetchTransaction e colocar aqui o nosso thatLoading como falso. Dessa forma, a gente removeu o feedback visual de carregamento aqui da nossa aplicação. Na próxima aula, nós vamos efetivamente realizar a nossa requisição quando nós chegarmos aqui no final da nossa lista. Então