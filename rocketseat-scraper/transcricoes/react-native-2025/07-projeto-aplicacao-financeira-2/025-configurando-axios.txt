WEBVTT Na aula de hoje daremos início à configuração do Axios na nossa aplicação. Na aula passada nós configuramos todo o context referente à autenticação, criamos todos os métodos que iremos utilizar a princípio e todos os dados que serão compartilhados dentro de toda a nossa aplicação. Porém, para nós implementarmos a funcionalidade de fato, implementarmos a lógica, vamos ter a necessidade de comunicar com o nosso banco de dados, com a nossa API, para a gente, assim, estar podendo chamar as nossas rodas, efetuar o login, o registro e buscando os dados que são necessários. Para isso, nós vamos estar utilizando o Axios. Então, vamos instalar aqui no nosso terminal yarn add Axios. Lembrando que se vocês estiverem rodando o aplicativo de vocês, é importante estar parando o processo. Vamos estar rodando agora o Yarn Android para estartarmos novamente o nosso projeto. Lembrando que em aulas passadas nós alteramos o script de Android para rodar o Expo Run Android. Dessa forma a gente garante que o código nativo aqui esteja sempre atualizado. Com o nosso app rodando, vamos vir aqui em SRC. Dentro de Shared vamos estar criando uma pasta nova chamada API. Dentro de API vamos criar aqui dentro dtmoney.ts. Aqui dentro nós vamos criar as configurações do nosso Axios. A primeira coisa que iremos fazer é colocar um const dt money api igual a axios.create axios.create espera receber um objeto como parâmetro. Dentro desse objeto ele espera receber, por exemplo, o base URL que é o URL de conexão aqui da nossa aplicação, um detalhe é que o nosso back-end tem que estar rodando, então é importante você já ter inclonado o back-end e seguido todas as etapas para estarmos inicializando ele, vamos estar rodando aqui um yarn dev, ele vai estar rodando aqui na porta 3001, e após vocês configurarem rodarem as migrations e estartarem o back-end vamos voltar aqui no nosso aplicativo um detalhe interessante aqui na definição da nossa base URL vamos estar fazendo basicamente uma validação vamos criar aqui uma constante const baseURL e baseURL vai ser igual a platform.select dentro de platform.select vamos colocar o iOS vamos por uma string e Android, vamos pôr uma string. Dessa forma, a gente garante que teremos duas URLs diferentes durante o desenvolvimento. O que isso significa e o porquê disso? No do iOS, para nós comunicarmos com a nossa API, podemos simplesmente colocar o http://localhost 3001. Dessa forma, quando passarmos esse parâmetro para a nossa base URL do Axios, nosso aplicativo irá conseguir se comunicar sem grandes problemas. Porém, o emulador de Android não consegue acessar o localhost que está rodando na nossa máquina. Para isso, nós precisamos utilizar um IP especial, como se fosse um atalho especial criado pelo próprio emulador de Android, para que assim a gente consiga comunicar o local host da nossa máquina com o emulador. Para isso, nós basicamente vamos colocar aqui http:// 10.0.2.2 10.0.2.2 E agora sim, a porta que o nosso backend está rodando, 3001. Por fim, nós vemos aqui na nossa DTMoney API Apagamos aqui a string e os dois pontos E vamos estar mudando o nome também aqui do base URL Para ficar igual o do parâmetro do create Dessa forma a gente garante que Sempre que estivermos rodando em modo de desenvolvimento No iOS vamos estar acessando o localhost E quando estivermos rodando No desenvolvimento no nosso Android, vamos estar acessando esse IP, garantindo que não teremos dor de cabeça durante o nosso desenvolvimento, que não vai ser necessário a gente ficar trocando o tempo todo a nossa URL. Isso daqui vai ajudar bastante a gente. Vamos estar agora exportando a nossa constante de DT Money API para nós estarmos conseguindo utilizar ele em outros locais da nossa aplicação. Com isso, garantimos que vamos conseguir acessar a nossa DT Money API em outros locais aqui no nosso projeto. Com isso nós finalizamos a configuração inicial do Axios. Na próxima aula iremos criar os métodos que irão utilizar esta DT Money API. Então é isso, espero vocês na próxima aula.