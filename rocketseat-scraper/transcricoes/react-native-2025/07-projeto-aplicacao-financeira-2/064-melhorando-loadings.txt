WEBVTT Fala, Dev! Na aula de hoje, melhoraremos os loadings aqui da nossa aplicação. Atualmente, temos um único estado de controle de loading que estamos utilizando tanto no carregamento de novas transações, o refresh, ou até mesmo no fetch inicial das transações aqui do nosso app. E para isso, vamos criar um estado novo, const, loadings no plural, setLoadings, onde aqui teremos um useState, onde a princípio teremos simplesmente um objeto contendo aqui initial, vai começar como false, teremos aqui também refresh, que também será false, e por fim teremos aqui também o loadMore, que também será false. Basicamente o que estamos fazendo aqui, ao invés de nós criarmos vários estados aqui, cada um para um loading diferente, nós simplesmente teremos aqui um objeto contendo todos os loadings dentro das nossas transações, que no caso seria o refresh, o inicial, e o de carregar mais. Então dessa forma nós vamos simplesmente remover nosso atual estado de loading. Próximo passo, vamos criar aqui acima do nosso transaction context type uma interface, loadings no plural. Vamos vir aqui dentro do nosso estado de loadings e vamos passar aqui para dentro da nossa interface, simplesmente substituindo o false por boolean, agora vamos pegar o nosso loadings e passar aqui pra dentro do nosso estado, já aproveitando aqui dentro do nosso transaction context type vamos passar aqui loadings, a interface que nós criamos, e show de bola agora vamos passar aqui também dentro do nosso transaction context type uma função, que é basicamente a função que será responsável por atualizar os nossos carregamentos basicamente aqui nós vamos fazer o seguinte handleLoadings vai ser uma função que vai retornar void receberemos aqui basicamente um objeto como parâmetro onde dentro desse objeto nós teremos key para ativar nós utilizaremos um KeyOfLoadings, que basicamente será os valores aqui dentro da nossa interface, no caso Initial, Refresh e LoadMore. E como segundo parâmetro teremos aqui Very, que será basicamente um boolean, onde nós iremos falar se o nosso loading ficará como True ou como False. Agora nós vamos simplesmente criar a nossa função handleLoadings, onde aqui dentro nós vamos receber os nossos parâmetros e para evitar repetição aqui na escrita do nosso código, vamos voltar aqui no nosso handleLoadings dentro do nosso transactionContext e vamos criar uma interface para ele, interface handleLoadingParams, onde vamos simplesmente dar um Ctrl V e no lugar aqui deste, vamos simplesmente passar aqui a nossa interface, onde nós vamos pegar agora e passar aqui para dentro também da nossa função de handle loadings, onde aqui dentro teremos a nossa key e o nosso value, e aqui nós vamos simplesmente colocar um set loadings, retornando uma callback, e um object literal, ou seja, nós vamos simplesmente retornar um objeto aqui com a nossa função, ObjectLiteral, ou seja, nós vamos simplesmente retornar um objeto aqui com a nossa função breathValues vamos recuperar aqui os valores que nós temos no nosso estado antes dele ser atualizado, vamos colocar vírgula vamos abrir aqui, colchetes e vamos passar a nossa key, novamente aqui vindo com o conceito de computedKey, onde nós atualizamos dinamicamente um valor dentro de objeto com base na key que nós estamos recebendo e aqui dois pontos, value. Perfeito, dessa forma vamos simplesmente pegar o nosso handle loadings e passar aqui pra dentro do nosso values, vamos remover aqui loading, já que nós não temos mais este estado. Vamos passar aqui o loadings. Agora o nosso value está reclamando de erro simplesmente porque aqui dentro do nosso transaction context type nós ainda estamos enviando o nosso estado antigo de loading, vamos simplesmente remover ele daqui olha só, maravilha, e vamos simplesmente fazer uma atualização, aqui no caso, vamos remover aqui o setLoading do nosso RefreshTransactions, até porque nós não iremos realizar a atualização de loadings dentro do nosso contexto vamos fazer tudo isso dentro dos nossos componentes, vamos remover aqui também do nosso fetchTrans aqui do load more transactions nós vamos simplesmente substituir loading por loadings ponto load more transactions, da mesma forma nós vamos simplesmente pegar loadings aqui e passar pra dentro do nosso use callback da seguinte forma atualizando assim a nossa função apenas quando o load more for atualizada de fato, agora vamos voltar aqui na nossa home, vamos simplesmente remover o nosso loading que temos aqui, também aqui no nosso refresh control, e vamos sair implementando todas as funções que nós criamos na aula passada, onde nós englobamos todos os nossos requests dentro de trycats. Basicamente, vamos recuperar aqui do nosso useTransactionContext o nosso handleLoadings e também o nosso loadings. Agora, basicamente, acima da execução do request aqui das nossas funções, vamos colocar aqui o nosso handleLoadings. Por se tratar da função de handleFetchCategories, a nossa aqui será basicamente initial e o nosso very como true. Vamos simplesmente copiar essa função e vamos colocar no final aqui do nosso tryCatch para que assim que finalizar a execução da promise, nós vamos voltar o nosso loading para a falsa. Da mesma forma vamos fazer aqui no nosso handleFetchInitialTransactions, vamos pegar aqui no nosso finally e novamente passar como false. Show de bola. Agora aqui no handleLoadMoreTransactions dentro do nosso try, acima da execução do código vamos mudar aqui a nossa key, vamos colocar aqui o load more passando para true já que aqui é o início do nosso request e aqui dentro do finally vamos mudar para false mesma forma aqui embaixo da nossa handle refresh vamos só mudar aqui a nossa aqui para refresh vamos copiar vamos ver aqui dentro do nosso finally e alterar o vale para false agora aqui dentro do nosso refresh control, dentro da propriedade de refreshing, nós vamos simplesmente fazer da seguinte forma. Vamos pegar o nosso loadings.refresh Refresh Perfeito. Um último detalhe que vamos fazer aqui é colocar um await no nosso handle refresh transactions. Dessa forma garantido que o nosso finally só seja executado assim que a nossa requisição for finalizada. Caso contrário, tanto o handle loading setando o valor como o true, quanto setando aqui para a false no nosso finally serão de forma síncrona. Então, basicamente, nós não teremos um feedback visual. Dessa forma, temos a separação dos loadings referente às requisições das nossas transações. Espero vocês na próxima aula.