WEBVTT Hoje criaremos um debounce aqui no nosso aplicativo. O debounce nada mais é do que uma funcionalidade muito interessante e crucial para nós utilizarmos no momento de realizarmos requisições HTTP. Basicamente, pensem comigo. Imaginem que a cada carácter que eu digitasse aqui dentro do nosso input fosse disparado uma requisição para a nossa API. Isso seria nem um pouco performático, até porque não faria tanto sentido se nós realizarmos um request para cada vez que digitarmos dentro aqui do nosso input, então um debounce serve para que um request seja disparado apenas um pequeno tempo depois que nós pararmos de digitar aqui no nosso input, ou seja, supondo que nós escrevemos aqui test, nenhum request foi feito no momento da digitação, porém no momento em que nós paramos de digitar, vai ter um contador que vai validar quantos segundos o input parou de ser atualizado e o debounce será responsável por realizar o request apenas quando nós finalizarmos a digitação aqui no nosso input ou simplesmente nós ficarmos um certo tempo sem digitarmos aqui no nosso input. Existem várias bibliotecas que fazem isso automaticamente. Porém nós iremos criar um na mão. Para nós entendermos como funciona o debauch debauch debauch dos fãs. Primeira coisa que vamos fazer aqui dentro do nosso componente de filter input. É criar um estado novo. Vamos criar aqui um const text. Set text. E aqui vamos colocar um use state contendo uma string vazia como valor inicial. Show de bola! Agora paralela esse estado que temos aqui dentro do nosso filter input. Vamos criar também um outro estado aqui dentro do nosso transaction context. Vamos criar aqui um const search text set search text igual a um useState, começando também como uma string vazia. Agora vamos passar aqui para dentro do nosso transaction context type, um setStushText, que vai ser basicamente uma função que vai retornar um void, isso para receber um text, um valor de string como parâmetro, e vamos passar aqui para dentro do nosso value. Agora temos dois estados, um estado aqui sendo armazenado localmente, onde ele será utilizado para o feedback visual aqui no nosso input, e teremos aqui também um estado que será armazenado dentro do nosso contexto, onde este valor será atualizado apenas quando o debounce executar, ou seja, assim quando nós pararmos de digitar aqui no nosso input, nós efetivamente atualizaremos esse estado. Voltando aqui dentro do nosso filter input, nós vamos fazer o seguinte. Vamos colocar aqui o useEffect. Vamos colocar aqui a callback e um array de dependência. Onde dentro desse array de dependência nós iremos colocar aqui o text, que é o nosso estado local. O que nós faremos aqui é basicamente o seguinte. Criaremos uma constante nova, const, que é um handler, onde este handler nada mais será do que um setTimeout, onde no segundo parâmetro do nosso setTimeout vamos colocar aqui 500 milissegundos depois. Agora vamos vir aqui na desestruturação do nosso useTransactionContext e vamos pegar aqui o nosso set searchText. Vamos passar o nosso set searchText para dentro do nosso set timeout e vamos passar aqui o nosso text. Vamos vir aqui no return, ou seja, quando o nosso componente for desmontado da tela, nós vamos limpar a memória, basicamente do nosso handler, vamos pegar aqui o nosso clearTimeout do próprio JavaScript e vamos passar o nosso handler como parâmetro, agora vamos voltar lá no nosso useTransactionContext, vamos passar também como parâmetro aqui no nosso transactionContext type o nosso search text, simplesmente uma string, vamos pegar aqui o nosso estado e passar dentro da nossa propriedade de value pra que a gente consiga utilizar ele em outros locais aqui na nossa aplicação vamos desestruturar aqui no nosso filter input o nosso search text e vamos criar um outro useEffect onde esse useEffect será executado toda vez que o nosso estado de search text for atualizado, ou seja, quando o nosso setTimeout atualizar o nosso searchText, nós iremos executar esse useEffect, onde basicamente nós vamos buscar aqui o nosso fetchTransactions, vamos passar aqui para dentro uma função que se autofecha, onde essa callback aqui é assíncrona, e aqui dentro vamos colocar um try, um catch, e aqui dentro vamos passar o fetch transactions, passando como parâmetro o page, por padrão 1. E agora para finalizar, vamos vir aqui dentro do nosso text input, vamos passar aqui o nosso vary, o nosso estado de text, o onChangeText, o nosso setText. E vamos colocar um console.log aqui dentro do nosso useEffect, que está olhando para dentro do searchText. Vamos simplesmente colocar aqui um console.log dentro do searchText. E vamos comentar aqui o nosso fetchTransaction por enquanto, já que nós não estamos enviando o nosso filtro para lá por enquanto. Agora, repare no seguinte. Quando nós digitarmos aqui no nosso input, nada irá acontecer. Porém, quando nós pararmos de digitar, durante meio segundo o nosso set time out será executado atualizando o nosso sete seus textos dessa forma executando assim esse outro e os efeitos que seria responsável por efetivamente buscar as nossas transações agora da forma que está aqui é bem fácil para nós efetivamente realizarmos o nosso request vamos descomentar aqui o nosso pet transactions vamos vir aqui dentro do nosso contexto e vamos passar como parâmetro do nosso FetchTransactions. Está dentro do nosso service o nosso SearchText, que é justamente o parâmetro que ele espera receber. Olha só que maravilha. Agora, detalhe importante também, vamos pegar aqui o nosso estado e passar para dentro da rede de dependência do nosso UseCallback para que toda vez que for atualizado o nosso estado, a nossa função se remonte com ele já atualizado. Agora vamos dar um reload no nosso app para garantirmos que está tudo ok. Se nós tentarmos buscar aqui, por exemplo, esta transação, 33, e olha só que maravilha, o nosso request acontece apenas um tempo após nós pararmos de digitar. Isso daqui é muito massa. Vamos tentar buscar aqui um específico, este com teste com dois S no final. Teste e E. E olha só que massa o nosso debounce funcionando corretamente. Isso daqui é muito da hora. Normalmente se nós colocarmos aqui 200 caractere, só será disparada uma única requisição, nos dando assim bastante performance no nosso aplicativo, evitando requests redundantes e muitas vezes até mesmo desnecessários lá na nossa API também é possível nós filtrarmos aqui pelo tipo da nossa transação então vamos colocar aqui trabalho e olha só apenas as transações do tipo de trabalho estão aparecendo muito massa na próxima aula daremos início ao nosso formulário de onde nós teremos várias outras opções então é