WEBVTT Hoje criaremos um componente que será renderizado quando a nossa função de Handle Load More Transactions for executada, ou seja, quando chegarmos aqui no final da nossa lista, traremos um feedback para o usuário, para que ele saiba que as novas transações estão sendo buscadas. Para isso, nós vamos utilizar uma propriedade aqui da nossa FlatList chamada ListFooterComponent, que é basicamente um componente que será renderizado no footer aqui da nossa lista. A priori, nós simplesmente vamos colocar aqui um loadings.loadMore ou seja quando o nosso loadMore for true nós vamos renderizar um activity indicator importando do próprio react-native aqui na color dele vamos importar nossa variável de cores, vamos colocar aqui um ascent, brand-light e um size-large, agora caso contrário simplesmente retornamos aqui nulo. Agora para podermos validar. Se de fato este nosso componente está funcionando. Vamos ter de voltar aqui. Dentro do nosso use transaction context. Vamos alterar o nosso per page. Vamos pegar apenas 3 transações por request. Dessa forma vamos ter uma quantidade maior de páginas. Sem a necessidade de ficar criando várias e várias transações. E olha só que interessante aparentemente o nosso request é tão rápido que não tá dando tempo nem mesmo de ver o nosso loading mas para resolvermos esse problema é muito simples eu vou ensinar para vocês um truque basicamente aqui dentro na função de handle load more transaction vamos colocar aqui um await vamos iniciar aqui o new promise essa new promise espera como primeiro parâmetro uma callback e essa callback espera dentro dela uma função chamada resolve dentro dessa promise vamos colocar aqui um set timeout vamos passar uma callback aqui dentro e como segundo parâmetro vamos passar aqui 2 mil milissegundos dessa forma nós executamos o nosso load more transactions dois segundos após a execução do nosso handle loadMoreTransactions, vamos colocar aqui um async dentro da nossa callback pra ele deixar nós executarmos aqui uma função assíncrona. E por fim, aqui dentro da nossa callback do nosso setTimeout, vamos colocar aqui o nosso resolve, passando um parâmetro de simplesmente um nulo. Dessa forma, quando nós recarregarmos a nossa aplicação e vamos para o final da nossa flatlist, vamos poder ver por alguns segundos o carregamento de novas transações. Isso daqui é um detalhe bem interessante para caso a nossa requisição assíncrona esteja acontecendo de forma muito rápida, para a gente poder validar algum tipo de comportamento específico. Vamos remover aqui agora toda essa new promising que nós colocamos, vamos deixar apenas aqui o nosso await load more transactions e vamos recarregar aqui o nosso app. E um detalhe bem interessante é que dentro da nossa função de Load More Transactions, nós já colocamos uma validação de que basicamente se nós estivermos na última página que existe lá no nosso backend, nós simplesmente não executamos novamente a nossa função de Fetch Transactions, dessa forma impedindo que o nosso loading apareça aqui sempre quando nós chegarmos no final da nossa lista. Isso daqui é bem bacana. Na próxima aula daremos início à filtragem das transações aqui na nossa aplicação. Então é isso, espero vocês lá.