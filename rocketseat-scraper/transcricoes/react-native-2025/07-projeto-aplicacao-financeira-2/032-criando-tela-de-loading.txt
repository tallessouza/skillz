WEBVTT Fala Levi, beleza? Na aula de hoje nós iremos criar uma tela de loading para o nosso aplicativo. Essa tela de loading será a primeira tela a ser exibida quando nós abrimos o nosso app. Ela será responsável por fazer a validação se existe de fato uma sessão de usuário logada ou não. Ou seja, vamos validar se dentro da nossa Sync Storage possui uma sessão, possui os dados do usuário. Caso não exista, nós simplesmente vamos deslogar ele. Primeiro de tudo vamos vir aqui em Scrums, vamos criar uma tela nova chamada Loading e dentro de Loading vamos criar o index.tsx, aqui vamos criar um Functional Component padrão show de bola! E aqui dentro vamos colocar um return em um safeAreaView, portando do react-native safeAreaContext. Aqui dentro vamos passar um fragment, passando uma image, portada do próprio react-native, e junto da image vamos passar um activity indicator, também do próprio react-native. Esse activity indicator é basicamente o componente de loading padrão que nós temos no nosso aplicativo. Com esta estrutura, vamos dar início à configuração da nossa tela. A primeira coisa que nós precisamos entender é em que momento nós vamos querer que ela seja exibida. Como eu disse anteriormente, a primeira tela que aparecerá no nosso app será a tela de login. Então, para isso, vamos ter de fazer um ajuste pequeno aqui no nosso Navigation Routes. Vamos estar criando um estado local aqui dentro. Const loading set loading. Que vai ser basicamente um useState. Começando como true. Vamos pegar este load e vamos passar para dentro do nosso Routes. Para dentro da rede de dependência do nosso useCallback. Para que toda vez que ele atualize o nosso useCalbec, remonte a nossa função, e vamos simplesmente fazer uma validação. If loading, ou seja, se estiver carregando, vamos estar retornando aqui o nosso loading, importando aqui diretamente das nossas pages. Como vocês podem ver, a nossa tela já está sendo renderizada agora que o pulo do gato vem. Lembram que na aula passada, dentro do nosso ALF context, nós criamos a função de restoreUserSession. Nós vamos chamar ela dentro da nossa tela de loading, ou seja, const. Vamos desestruturar o nosso useAlf context. E aqui dentro nós vamos pegar o restoreUserSession. Aqui dentro nós vamos colocar um useEffect, que no caso é o hook que gerencia o ciclo de vida, e é basicamente aquele hook que executa algo quando o nosso componente é criado. Voltando aqui ao ciclo de vida dos componentes, vamos aqui dentro criar uma callback, criar uma function que se auto chama, e muito importante, essa função tem de ser uma função assíncrona, já que nós vamos chamar o nosso RestoreSession aqui dentro, e ele é uma promise. Aqui dentro vamos simplesmente colocar um try catch, lem é uma promise. Aqui dentro vamos simplesmente colocar um try catch. Lembrando que sempre que nós lidamos com código assíncrono existe a possibilidade de acontecer algum tipo de erro, algum tipo de imprevisto, alguma exceção. E para que nosso aplicativo não quebre, nós englobamos aqui um catch e maravilha. Agora o que nós precisamos fazer é o seguinte. Vamos colocar aqui um const user igual a await restore user session e vamos colocar aqui uma validação. Basicamente se user for nulo, ou seja, se o usuário não existir, lembrando que a nossa função de restoreSession retorna para a gente um stream ou nulo. Lembrando que nós pegamos os dados do nosso asyncStorage e ele pode tanto existir quanto não. Se o nosso usuário existir, ele irá retornar para a gente um JSON contendo todos os dados do usuário e o nosso token. Porém, caso o usuário não exista, nós vamos estar querendo realizar o logout do nosso usuário. Basicamente, se o usuário não existir, vamos estar rodando a função de handleLogout. E da mesma forma, se acontecer algum erro no momento em que nós formos restaurar a sessão do nosso usuário, também vamos estar rodando a função de handleLogout. No momento momento a função de handleLogout não está fazendo nada, então vamos abrir aqui o nosso context. Vamos vir aqui no nosso handleLogout, vamos transformar ela em uma função assíncrona e vamos simplesmente fazer o seguinte. await asyncStorage.clear ou seja, vamos limpar tudo que está salvo na memória do nosso asyncStorage e por fim setToken. Vamos setar como nulo e o setUser como nulo. Dessa forma a gente garante que não tenha nenhum dado tanto no usuário, tanto no nosso token e também não tenha nenhum dado aqui no nosso asyncStorage. A última coisa que nós precisamos fazer na nossa tela de loading é atualizar o dado que está aqui dentro do nosso Navigation Routes. Para isso, nós vamos ter de passar o setLoading como parâmetro para a nossa tela de loading. Então vamos fazer o seguinte, vamos criar aqui uma interface. Vão ser basicamente as propriedades que vamos ter aqui no nosso loading. set loading Vai ser uma callback que não vai retornar nada. Espera receber um valor booleano como parâmetro. Boolean. Maravilha. Vamos pegar aqui as nossas props. Vamos chupar aqui como um FunctionalComponent. Recebendo as seguintes props. Vamos desestruturar as propriedades aqui do nosso componente. E vamos passar o setLoading aqui para dentro da nossa função. E basicamente aqui, além do tryCatch, também existe o finally. Ou seja, toda vez que acabar a execução de todo o código assíncrono que nós temos aqui no nosso try-catch, nós vamos simplesmente colocar o setLoading para falso e vamos colocar aqui também o await no nosso handleLoading, já que ele é uma função assíncrona, agora aqui no nosso navigationContainer ele já está acusando o erro porque agora é obrigatório nós passarmos o setLoading como parâmetro, e maravilha, como podemos ver, como não tem nenhum usuário logado, o nosso loading já executa o nosso useEffect, assim que o nosso componente é montado em tela. Nós executamos o restoreUserSession, validamos que não possui nenhum usuário lá, e por fim finalizamos o loading da nossa tela. Agora vamos colocar aqui na nossa tela de loading uma estilização. Vamos pôr aqui um className com um bg. background-primary, um background-primary um item-center um justify-center também, e um flex1 pra ele ocupar 100% da tela aqui em image vamos colocar um class name de height contendo 48 pixels e de width contendo 255 pixels basicamente para o nosso source vamos estar colocando require passando como parâmetro arroba-roba barra assets Barra logo ponto png Maravilha, aqui no activity indicator Vamos simplesmente estar passando o color Vamos estar pegando a nossa variável De colors Ponto white E um class name contendo o margin Top de 20 Maravilha, agora se nós alterarmos Aqui o nosso set loading dentro do finally para true e dermos um reload no nosso aplicativo, podemos ver que a nossa tela de loading já está funcionando devidamente. Vamos só estar mudando aqui novamente o set loading para falso. Para testar a nossa persistência de dados, vamos estar colocando aqui teste arroba teste.com.br e a senha 123.123. Vamos realizar o login, vamos recarregar o nosso aplicativo e olha só, maravilha, vocês viram que piscou a nossa tela de loading, ou seja, todo esse fluxo que nós criamos foi executado com sucesso, a nossa função de restoreSession foi chamada com êxito, ela achou os dados do nosso usuário aqui na nossa Sync Storage, setou tanto o usuário quanto o token e por fim aqui dentro routes, foi montada as nossas telas privadas. Agora com os nossos dados sendo persistidos, podemos melhorar o fluxo de autenticação, podemos melhorar as tratativas de erro do nosso aplicativo antes de darmos início ao desenvolvimento da nossa tela de home. Então eu espero você na próxima aula!