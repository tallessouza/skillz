WEBVTT Fala, Neve! Na aula de hoje, nós iremos criar um snackbar aqui para o nosso aplicativo. Existem diversas bibliotecas com snackbar já prontas, porém, aproveitando para praticarmos um pouco mais o nosso gerenciamento de estado global com o Context API, nós iremos criar um na mão. Na aula passada, nós melhoramos as tratativas de erro das requisições HTTP aqui do nosso aplicativo. Vocês verão que isso será bem útil na hora de nós exibirmos um feedback visual para o nosso usuário. Então, vamos por a mão na massa. A primeira coisa que nós temos de ter em mente é que absolutamente todos os componentes que nós teremos no nosso aplicativo poderão ter acesso à nossa snackbar, ou seja, poderão emitir uma exceção e assim exibir uma mensagem de erro para o nosso usuário. É interessante nós termos essa visão, pois a partir disso chegamos na conclusão de que precisaremos obrigatoriamente utilizar os estados globais do nosso aplicativo para criar a nossa então para isso dentro de vamos tá criando um um de novo totalmente isolado do responsável pela autenticação que nós criamos até então isso é bem interessante pois nós podemos criar múltiplos contextos dentro do nosso aplicativo e é muito importante nós criarmos essa separação para dessa forma nós organizamos melhor nosso aplicativo vamos delegando as responsabilidades isoladas para cada um dos contas isso no fim do dia é bem interessante então vamos lá. Aqui dentro de context, vamos criar um arquivo novo chamado snackbar.context.tsx. E aqui, assim como nós fizemos no nosso alph context, vamos começar criando uma variável com o nosso create context. A princípio, vamos criar aqui const snackbar context, lembrando que com um S maiúsculo, já que nós utilizaremos ele para criar o nosso provider, igual a create context e maravilha. O próximo passo que nós vamos fazer aqui no nosso context é basicamente mapear todos os dados iniciais que teremos no nosso contexto, como por exemplo, vocês não concordam comigo que o fato de ser um contexto responsável por exibir mensagem de erro nós iremos ter de receber dinamicamente uma mensagem como parâmetro no nosso contexto então vamos começar tipo anda aqui export o hype o nec bar o contexto type basicamente aqui vamos colocando uma message string e vamos estar criando também aqui um tipo novo, um tipo responsável por definir qual tipo de mensagem nós queremos exibir no nosso snackbar no momento, se é uma mensagem de erro ou uma mensagem de sucesso. Vamos criar aqui um export type snackbar message type basicamente será um erro ou um success vamos pegar aqui o nosso snackbar message type e passar para dentro do nosso snackbar context, vamos passar a propriedade aqui de type snackbar type ou nulo aqui na nossa message também vamos estar passando um ou nulo e por fim aqui vamos estar passando um notify, que basicamente será uma função que vai retornar um void ou seja, não vai retornar nada. E essa notify irá receber alguns parâmetros. Vamos criar aqui uma outra interface para nós mapearmos os parâmetros que o nosso notify irá receber. No caso vamos colocar aqui uma interface notify message params. Notify message params terá basicamente message, que é uma string, e o message type, que é basicamente o tipo que nós criamos agora há pouco. Vamos pegar o notify message params e passar para dentro dos parâmetros de notify e maravilha. Dessa forma, temos tipado os primeiros dados iniciais aqui do nosso contexto. Eu gosto muito de, antes de dar início à configuração de algum contexto, pensar muito bem quais funcionalidades e features ele irá abrangir. Dessa forma, nós conseguimos criar um mapeamento bem interessante aqui com as nossas interfaces e tipos, para, dessa forma, facilitar bastante a nossa vida no momento do desenvolvimento de fato. E, basicamente, vamos pegar aqui o nosso snackbar context type vamos passar um objeto vazio pra dentro do nosso create context vamos colocar um as snackbar context type show de bola o próximo passo que nós vamos fazer aqui no nosso contexto de snackbar é criar o nosso componente de provider que vamos utilizar na raiz do nosso aplicativo então vamos colocar aqui o export const snackbar context provider snackbar context provider vamos estar tipando ele da seguinte forma pode se tratar de um provider ou seja como nós iremos colocar ele no nosso app.tsx assim como nosso alph context ele obrigatoriamente espera receber uma children, que basicamente é toda a nossa aplicação, e ele irá englobar. Então, vamos chipar aqui um FunctionalComponents. Vamos colocar aqui o propsWithChildren. Dessa forma, vamos recuperar aqui o children do nosso componente, das propriedades do próprio. E vamos simplesmente estar colocando aqui um return snackbar context.provider. Vamos estar fechando ele e dentro do nosso provider vamos passar aqui os children. Basicamente serão todos os outros componentes dentro do nosso aplicativo. No nosso caso a navigation routes. Próximo passo que nós vamos fazer dentro do nosso contexto é basicamente criar os estados onde nós iremos armazenar tanto a nossa mensagem quanto o nosso type. Para isso vamos criar aqui um const message setMessage igual a useState começando com o nulo e aqui nós vamos simplesmente estar tipando que será uma string ou um nulo assim como a tipagem que nós fizemos aqui em cima no nosso snackbar context type, outro estado que nós iremos criar aqui também é o tipo da mensagem que nós iremos exibir, então vamos colocar aqui type, set type igual a use state, vai começar como nulo, e na tipagem vamos colocar que ela será uma snackbar message type ou nulo maravilha, dessa forma nós podemos simplesmente chegar aqui no nosso provider, vir aqui na propriedade de value, abrir um objeto e passar as propriedades que nós já criamos, no caso message e type. Por fim, vamos criar aqui um método, no caso nosso método de notify, onde nós iremos de fato emitir a nossa mensagem, ou seja, exibir a nossa snackbar. Então vamos colocar aqui const notify igual a... Vamos simplesmente abrir uma arrow function aqui. E dentro dos nossos parâmetros nós iremos chipar passando notify message params. Aqui vamos desestruturar os parâmetros que a nossa função de notify irá receber. E aqui dentro vamos colocar um set message passando a message que a nossa função está recebendo como parâmetros que a nossa função de notify irá receber, e aqui dentro vamos colocar um set message, passando a message que a nossa função está recebendo como parâmetro, logo depois um set type, passando o message type. Show de bola. Só que um detalhe interessante é que o comportamento esperado de uma snackbar é que ela nos exiba uma mensagem, seja de erro ou de sucesso, por alguns segundos e logo depois ela desapareça, até porque não faz sentido deixar uma snackbar aí infinitamente, então nós vamos fazer o seguinte. Aqui logo após do nosso set type, vamos criar aqui um set timeout. Set timeout é uma função que espera receber uma callback como primeiro parâmetro e como segundo parâmetro ela espera receber um número. São basicamente milissegundos, que aqui a princípio vamos colocar 3 mil milissegundos que equivalente a 3 segundos basicamente o set timeout irá executar a função que está dentro aqui dessa callback logo após que finalizarem os três segundos e aqui nós simplesmente vamos estar setando a mensagem ou seja o set message como nulo e o set type como nulo também. Dessa forma nós garantimos que quando o nosso notify for chamado nós vamos exibir a nossa mensagem e logo após 3 segundos nós vamos simplesmente estar removendo a nossa snackbar do nosso aplicativo. Vamos pegar aqui agora a nossa função e vamos passar para dentro do nosso value. Próximo passo nós vamos estar pegando o nosso contexto snackbar context provider e vamos passar pra dentro do nosso app.tsx. Um detalhe importante é que o nosso provider de snackbar irá englobar também o nosso auth context. Então vamos colocar ele em volta de toda a nossa aplicação, inclusive o nosso auth context provider. E show de bola, dessa forma nós já garantimos que todo o nosso aplicativo terá acesso a nossa snackbar. Na próxima aula nós iremos criar ela de fato. Então espero você lá!