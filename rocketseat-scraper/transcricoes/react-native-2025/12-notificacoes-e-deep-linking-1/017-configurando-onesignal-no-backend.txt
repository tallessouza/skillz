WEBVTT Nesta aula nós vamos aprender a configurar a nossa API aqui com o OneSignal. Nas duas aulas passadas nós configuramos tanto o nosso app iOS quanto o nosso app Android para realizarmos o disparo de Push Notification através do OneSignal, configurando tanto aqui no developers.apple quanto no próprio Firebase. as plataformas respectivas de cada um, no caso do Android sendo o Firebase e o Apple, o da própria Apple. Agora nós precisamos configurar o nosso Backend e para isso é mais simples do que vocês pensam. Primeiramente vamos abrir aqui o nosso Back, vamos voltar aqui no VS Code. Deixa eu só arrastar pra cá e perfeito. Se vocês clonarem aí o back-end, vocês verão que possuem um .env .example e aqui nós possuímos o nosso AppSecretKey, que é a chave de autenticação secreta do nosso app aqui para realizar o login, para gerar o token JWT. E aqui abaixo nós possuímos outras duas variáveis de ambiente, que é o OneSignalAppID que é o ID da aplicação que nós criamos lá no OneSignal, que é o ID que nós já vimos nas aulas anteriores e possuímos também aqui o OneSignal API, que é a chave de API do OneSignal. E para nós conseguirmos estas duas variáveis é extremamente simples. Primeiro de tudo, precisamos vir aqui... no antsignal vamos descer aqui em conf settings basicamente o app id nós já possuímos aqui na nossa url se vocês repararem nós possuímos app barra e uma hash essa hash aqui nada mais é do que o id do nosso app e a mesma forma nós já tínhamos colocado lá no nosso no nosso app na .env mas é só reforçando um outro local onde nós conseguimos achar o id aqui da nossa aplicação Então vamos aqui na .env, no caso eu já tinha criado um .env aqui na raiz do nosso back-end, se vocês não possuem, então criem aí um .env, porque o .env.example, como o próprio nome diz, é apenas um exemplo que nós utilizamos para saber quais variáveis precisamos montar aqui a nossa .env. E vamos só fazer o seguinte, vamos copiar essas duas variáveis e colar. Agora vamos voltar aqui no navegador, pegar o ID da nossa aplicação e colocar aqui, desta forma. Agora, o OneSignal, API Key, nós precisamos ir aqui em Settings, Keys e IDs. Vamos clicar aqui em Add Key, o name vai ser o default, podem colocar qualquer nome. Create. Agora, é muito importante nós copiarmos aqui esta chave, esta daqui é uma chave secreta, então não compartilhem com ninguém. E como aqui já está dizendo, nós só vamos conseguir copiar essa chave apenas uma única vez por motivos de segurança, tanto que após de gravar esta aula eu vou acabar deletando ela. Para vocês terem uma noção de quão importante é nós mantermos esses dados seguros, então vamos simplesmente clicar aqui em Copy to Clipboard. voltar aqui no nosso back-end e dar um Ctrl-V desta maneira. Agora a gente já possui tanto o nosso OneSignal API no back quanto o nosso OneSignal API aqui. Agora, só para verificar, vamos ver se a chave do nosso OneSignal API bate com a do nosso aplicativo. E olha só, bate perfeitamente. Começa com 52 e finaliza com 79. Maravilha. Agora, com isso em mãos, basta nós rodarmos o Yarn Dev Vamos aguardar e, olha só, aparentemente a conexão foi feita com sucesso, já que não houve nenhum erro. Então, com isso agora, a partir da próxima aula, nós vamos testar as funcionalidades de fato e começar a implementação no nosso aplicativo.