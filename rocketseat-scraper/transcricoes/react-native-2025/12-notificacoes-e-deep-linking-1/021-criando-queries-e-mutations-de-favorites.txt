WEBVTT Nesta aula, vamos criar as queries e mutations responsáveis por chamar a nossa camada de service. Lembrando que é muito importante nós criarmos essa camada de cacheamento, essa camada de queries, justamente para, além de seguir os padrões aqui do nosso projeto, nós conseguimos também utilizar todo o aparato que o React Query, que no caso é uma das ferramentas do Tanztech, oferece para a gente, que é toda a questão de cacheamento e de organização de requisições HTTP, que no fim do dia é um recurso fenomenal e essencial ali para nós utilizarmos no dia a dia. Então eu reforço novamente para todos os alunos que pularam o módulo anterior e simplesmente vieram direto para o módulo de Push Notification e simplesmente clonaram o projeto. Eu sugiro que vocês voltem no modo anterior e façam todo o projeto de Marketplace, para que dessa forma vocês tenham uma compreensão melhor do que está acontecendo. Agora, para nós seguirmos, é muito simples, nós vamos ir aqui dentro de src, shared, queries, nós vamos criar um novo grupo de queries chamado favorites, onde aqui dentro nós iremos criar um arquivo de para o nosso getfavorites, vamos só olhar aqui o padrão de escrita dos outros arquivos e perfeito, vamos criar aqui um usegetfavorites.query.ts aqui vamos criar um custom hook padrão export const usegetfavorites onde aqui dentro nós vamos criar uma const query, hoje nós vamos colocar um useQuery do ReactQuery, onde aqui ele espera receber alguns parâmetros, como por exemplo, um objeto contendo a query key, onde aqui vamos colocar simplesmente uma string, ou melhor, um array contendo uma string chamada de favorites Para quem não está habituado com este padrão, no caso com o React Query, quando nós criamos uma query, nós realizamos a requisição HTTP e nós salvamos a resposta dessa requisição HTTP em um cache, onde se em outro momento nós realizarmos a mesma requisição HTTP, ao invés de nós batermos lá no nosso back-end, nós vamos buscar do cache local, o que reduz muito, por exemplo, as telas de loading no nosso aplicativo. E para gerenciar qual o cache que nós estamos lidando no momento, nós temos esta query aqui. É nela que nós sabemos que este cacheamento pertence exclusivamente à requisição HTTP do... da busca de itens de produtos favoritos do usuário. O próximo nós vamos colocar aqui é a query fn, que é a função que será executada quando nós dispararmos aqui a nossa query, que é basicamente a função que criamos na aula passada, que no caso é o getFavorites. Vamos só passar aqui desta maneira. Próximo parâmetro que nós vamos colocar aqui é o STALE TIME, que é o tempo que este cache irá ser válido, ou seja, nós podemos setar que este cache seja válido por 10 minutos. Então, todas as requisições HTTP que nós realizarmos referente ao GET FAVORITES num prazo de 10 minutos, nós iríamos bater aqui no nosso cache, em vez de bater na nossa API. De novo, todos esses conceitos nós aplicamos de forma prática no módulo passado, então não vou me aprofundar muito nesse módulo aqui. Recomendo que vocês voltem lá e assistam as aulas. Agora, no stayotime, deixa eu ver quanto tempo que nós vamos colocar aqui. Vamos pôr 1000 milissegundos vezes 60, ou seja, 1 minuto. vezes 5, ou seja, aqui nós temos o total de 5 minutos, no caso o stay time espera receber o tempo em milissegundos, então nós simplesmente multiplicamos para ficar mais fácil o mil, que é 1 segundo, vezes 60, aqui nós temos 1 minuto, vezes 5, totalizando aí 5 minutos, e agora simplesmente vamos dar um return em query desta maneira. A próxima que nós iremos criar aqui é uma mutation, ainda uma query, pelo simples fato de nós estarmos executando uma ação método HTTP, um verbo post e um método delete. Então, para isso, vamos criar aqui um useAddFavorite.mutation.ts, desta forma, export const, vamos criar um custom hook, useAddFavorite.mutation, Onde aqui dentro vamos criar uma const mutation igual a um useMutation, importando do React Query. Onde aqui, muito parecido com o nosso useQuery, ele espera receber alguns dados, como por exemplo o mutationFunctions. Aqui nós vamos receber como parâmetro o addFavorite, desta maneira aqui. Vamos ter uma outra também, que é o onSuccess, que é quando a nossa requisição HTTP for executada com sucesso. Nós vamos executar esta callback específica e o onError, Vamos executar esta outra KB aqui, onde vamos só colocar um toast, importador do, toast foi react-native.error, passando o error.message ou uma mensagem default de Falha ao remover, ou melhor, falha ao adicionar produto nos favoritos. Aqui ele tá reclamando de erro, porque a variável de erro não foi declarada. No caso, basicamente, a callback de erro retorna pra gente o retorno, o que aconteceu de erro lá no axios, ou na execução do nosso código. E aqui no onSuccess ele retorna pra gente a resposta da nossa API, da nossa função. Como a gente não tá retornando nada, então a gente simplesmente não vai tá retornando. Então, simplesmente eles retornariam ao Defined. Vamos só dar uma olhada aqui na nossa API, vamos ver se os métodos de post e de delete retornam algo pra gente. E olha só, eles retornam. Então, é interessante a gente fazer o seguinte, a gente voltar aqui na nossa FavoriteService. Vamos realizar uma desestruturação. da chamada do Axios, vamos recuperar aqui o nosso data e vamos tipar basicamente aqui no post, se a gente reparar é basicamente o id, o ProductId, o UserId e o CreatedAt, vamos ver se bate aqui com o favorite, que bate aqui no caso muda vamos criar uma interface específica vamos criar aqui um export interface vamos colocar aqui o handle favorite response onde a gente vai simplesmente copiar esses dados que nós temos na documentação e adicionar aqui dentro agora é só substituir o que tem o zero por number e aqui string E aqui na handleFavoriteResponse, vamos voltar lá na nossa camada de service, e vamos atribuir ao retorno, tanto aqui do addFavorite, quanto aqui do removeFavorite, que eu acredito que a resposta seja a mesma, vamos até abrir aqui para averiguar. E na verdade ele tem noContent aqui, então a gente simplesmente remove. Precisa retornar nada daqui de dentro. Vamos pegar esse return e colocar aqui, dessa maneira. Agora, se a gente passar o mouse em cima dessa função, a gente já sabe que ele retorna uma promessa de handle favorite response. Agora, a gente pode voltar aqui na nossa use get, ou melhor, use add favorite mutation. Vamos pegar aqui, adicionar um toast, toast.success. Vamos colocar o produto adicionado aos favoritos. Desta maneira. Aqui a gente vai colocar o seguinte também. Vamos criar uma constante logo acima da nossa mutation cost, queryClient igual ao useQueryClient, dessa forma. Vamos pegar essa nossa constante e vamos limpar o cache referente à nossa query de buscar os produtos favoritados do usuário. Então, no caso vai ser o queryClient.invalidQueries, onde aqui vamos passar a queryKey, sendo igual ao array que nós temos aqui. que é basicamente isso daqui, favorites. Só passar uma string, na verdade é para receber um array, dessa forma. Agora, para que é isso? Vamos supor que nós adicionamos um produto novo aqui aos favoritos. Partindo do pressuposto em que anteriormente nós tínhamos buscado uma lista de produtos favoritados pelo usuário e nós armazenamos essa resposta do cache, nos próximos cinco minutos nós não vamos realizar uma nova requisição HTTP. Nós vamos buscar do cache. o que acabaria gerando um bug, porque se nós buscássemos do cache e nós fôssemos listar aqui o produto, nós simplesmente teríamos uma lista desatualizada. Agora, a partir do momento em que nós realizamos a requisição, no caso quando nós executamos aqui a nossa mutation, adicionando o produto aos favoritos, nós limpamos o cache referente aos produtos favoritos, para que dessa forma seja feito um refresh automaticamente, antes mesmo do stale time aqui da nossa query ser expirada. O que força, e pelo fato de nós termos forçado essa nova requisa HTTP, agora nós temos os dados atualizados aqui, tanto no cache, quanto aqui na interface do usuário, mostrando aqui o coraçãozinho que nós vamos adicionar na próxima aula. Por isso a importância de nós termos noção desse micro gerenciamento de cache aqui com React Query, para que dessa forma nós possamos criar as funcionalidades sem dores de cabeça, sem bugs gerados posteriormente por culpa desse tipo de situação. Agora vamos simplesmente pegar essa nossa constante de mutation e colocar um return aqui debaixo dessa forma. E por fim, vamos adicionar aqui um useRemove() favorite.mutation.ts Aqui vamos colocar um export const useRemoveMutation Desta maneira. Vamos criar aqui um const mutation igual ao useMutation, importando do DirectQuery. Onde aqui vamos ter o mutation fn, sendo o nosso removeFavorite. Desta forma. Vamos ter aqui o onSuccess. Onde dentro do onSuccess, nós vamos fazer literalmente a mesma funcionalidade que nós fizemos no de adicionar, ou seja, vamos limpar o cache. Para isso, vamos ter de criar aqui um queryClient. buscando do useQueryClient do React Query, pegando aqui o nosso cliente, e realizando um queryClient.validateQueries, onde aqui nós passamos o objeto como parâmetro, a query key, um array contendo favorites, que é a mesma chave que nós temos aqui na nossa query. Vamos colocar aqui também um toast, no caso, toast, importando do React Native, ponto, success, produto removido dos favoritos. Agora aqui vamos adicionar também um onError, onde nós vamos simplesmente colocar um toast, ponto, error, passando como parâmetro uma string de falha ao remover o produto dos favoritos. Agora falta simplesmente nós darmos um return na nossa mutation. E com isso finalizamos todas as nossas três mutations responsáveis por adicionar e remover dos produtos aqui dos favoritos. Na próxima aula nós vamos implementar na UI aqui do nosso app na tela de detalhes do produto um botão para nós favoritarmos e removemos o favorito aqui do produto específico. Então espero que tenham gostado da aula e espero vocês na próxima.