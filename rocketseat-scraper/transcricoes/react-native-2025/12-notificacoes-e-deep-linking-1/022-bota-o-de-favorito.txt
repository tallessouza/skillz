WEBVTT Fala Dev! Na aula de hoje nós vamos adicionar um botão para adicionar aqui o nosso produto na lista de favoritos. Nas aulas passadas nós criamos a camada de query emutation aqui do nosso app. Nós viemos em src, shared e queries. Podemos ver aqui em favorites. todas as funcionalidades que nós temos aqui para nós adicionarmos e removermos o nosso produto dos favoritos e também aqui a de listagem. Agora, o que nós precisamos fazer é vir aqui nesta tela específica na frente do botão de voltar e adicionar um botão onde nós vamos executar aqui as nossas mutations, no caso de add favorite e de remove favorite. Para isso, vamos vir aqui dentro de src, view models, product, aqui, Dentro do nosso ProductView, temos aqui o nosso Header dentro da propriedade de ListHeaderComponent da nossa FlatList. Vamos abrir ele. E olha só, já identificamos aqui onde está a View do nosso Header. O que precisamos fazer é adicionar um TouchableOpacity logo aqui à frente. TouchableOpacity. Onde aqui nós vamos colocar um IonIcons com o Name. de Her, no caso de coração em inglês, onde aqui nós vamos colocar, por enquanto, simplesmente um Size de 28, dessa forma. Vamos colocar aqui um Justify Between para jogar todos os itens aqui na extremidade do componente. Perfeito, ele só não jogou ali na extremidade porque a gente colocou o botão dentro do Toshiba OPS e te deu o botão de voltar. Então se a gente colocar fora agora, perfeito, só falta colocar aqui um FlexRow pra jogar os itens feliz um na frente do outro. E olha só, perfeito. Acredito que dá até pra gente tirar esse Justify Between aqui, dessa forma. E aqui, no caso, ele tá cortado um pouco aqui do lado direito, porque a gente tem essa propriedade de WitherFull no TouchableOpacity de cima. No caso, é só a gente remover ele. Vim aqui de novo na ViewPie. A gente pode manter aquele JustifyBetween pra jogar todos os itens na extremidade do componente. E, perfeito, agora o que nós precisamos fazer é simplesmente colocar uma condicional no nosso Ion Icons. Basicamente, se o nosso produto estiver marcado, a gente vai deixar ele preenchido. Se não, a gente vai deixar ele com este ícone de Herb Alt Line. Que é basicamente quando o nosso produto não estiver em favorito. No caso, a gente só vai fazer o seguinte aqui por enquanto. Vamos colocar aqui um color. pegar nossa variável de colors.danger, que vai ficar vermelho. Maravilha. Agora o que precisamos fazer é adicionar uma lógica para buscar os itens favoritos aqui no nosso componente. Para isso, vamos fazer o seguinte, vamos abstrair este botão aqui para um componente específico, já que nós vamos ter de fazer uma requisição HTTP, vamos isolar a lógica aqui dentro Pra evitar re-renders. Então, pra isso, a gente vai criar aqui dentro de Header Components, que dentro de Headers, vamos simplesmente criar um FavoriteButton. E aqui dentro, vamos criar um FavoriteButton.view.tsx, onde aqui vamos só colocar um Export Cost FavoriteButtonView. Vamos simplesmente pegar o componente que nós temos aqui, dar um ctrl x e passar pra cá dentro do return. Agora é só importar aqui as coisas do react-native e o nosso icons e também a nossa variável de colors. Vamos pegar aqui o nosso favoriteButton, viu? E não, a gente não vai colocar aqui por enquanto, vamos criar aqui também um useFavoriteButton.ViewModel.ts Aqui no caso vai ser a camada onde nós vamos chamar a parte lógica e a parte funcional do nosso app, da nossa estrutura, que no caso são as queries que nós criamos. Vamos colocar aqui um export const useFavoriteButtonViewModel onde aqui vai simplesmente retornar um objeto vazio por enquanto agora vamos simplesmente criar um index.tsx aqui dentro do nosso favorite onde aqui dentro vamos colocar um export const FavoriteButton, onde aqui será o nosso componente raiz, onde nós vamos simplesmente dar um return do nosso FavoriteButtonView, onde aqui dentro nós vamos simplesmente criar uma constante para armazenarmos o retorno da nossa camada de ViewModel, ou seja, nós vamos pegar tudo que nós possuímos aqui e injetar dentro da nossa camada de View, para isso vamos ter que criar aqui const ViewModel, Vai ser igual ao nosso useFavoriteButtonViewModel. Agora precisamos pegar este nosso ViewModel e rodar um Spread Operator dentro do nosso componente, para que dessa forma nós simplesmente espalhemos todas as propriedades que possuímos aqui dentro no nosso componente. Agora, um detalhe que nós precisamos fazer é adicionar a tipagem aqui do nosso ViewModel lá dentro. Até porque, no momento, nós estamos retornando um objeto vazio por conta disso. aqui não vai retornar nenhum erro, mas agora supondo que nós adicionemos aqui, por exemplo, favorite products, que é a lista de produtos favoritos, agora nós estamos retornando algo aqui da nossa ViewModel, e como o nosso componente de View não espera receber nada, ele vai retornar um erro. Para resolver esse problema é muito simples, é só nós irmos aqui na nossa camada de View, adicionarmos na tipagem do nosso componente, falar que ele é um Functional Component, que espera receber como parâmetro o return type, do type of, de useFavoriteButtonViewModel. Desta maneira. No caso aqui, faltou só a gente colocar entre menor que e maior que o typeof. Dessa forma agora todas as propriedades que nós adicionarmos aqui no retorno da nossa camada de ViewModel já vai ser gerado automaticamente uma tipagem aqui na nossa camada de View, ou seja, se nós desestruturarmos aqui as tipagens do nosso componente, agora vamos conseguir retornar os nossos FavoriteProducts. Agora o que precisamos fazer é, aqui dentro da nossa camada de ViewModel, chamar a rota específica para retornar todos os nossos produtos favoritos. Ou melhor que isso, vamos retornar se o produto que nós abrimos no momento é favoritado ou não. Para isso vamos ter que criar uma const. Vamos ter de desestruturar o nosso useGetFavorites, desta maneira. Se nós virmos aqui na desestruturação, ele vai retornar pra gente aqui um data, que é onde possui todas as propriedades do retorno da nossa query, onde aqui vamos colocar um alias, vai ser favorites. onde nós vamos colocar um valor default pra ele de um array vazio. Se nós tirarmos este valor default aqui da nossa variável, ele pode ser undefined. Então, pra simplesmente melhorar a tipagem aqui, a gente coloca um array. Vamos colocar também aqui um isLoading, onde vamos colocar um alias de isLoadingFavorites. Agora vamos criar aqui uma variável. Esta variável nós vamos basicamente verificar se o produto que estamos recebendo no momento é ou não é favoritado. Para isso precisamos receber como parâmetro da nossa ViewModel o ProductId. ProductId, vamos receber aqui um number. Agora, se nós voltarmos aqui na nossa camada de index, ele vai reclamar de erro, porque agora o ProductId é obrigatório. Mas para resolver isso é bem simples, a gente adiciona aqui em cima, no caso, vamos criar aqui uma interface, FavoriteButtonProps, no caso vai ser o ProductId, que será um number. Vamos chipar aqui o nosso componente. FunctionalComponent.configureBottomProps. Vamos desestruturar e recuperar e passar essa propriedade para dentro da nossa ViewModel. Agora, se nós viemos aqui na View, ele está reclamando de erro, mas é porque a variável não existe mais, certo? Agora, se nós voltarmos lá no índex do nosso Header, se nós colocarmos aqui o nosso FavoriteButton, no caso, o do nosso índex, não a camada de View, mas sim a raiz dele, obrigatoriamente vamos ter de passar como parâmetro o ProductId, mas isso também não é problema, porque nós possuímos aqui dentro de ProductDetails.id desta forma. Lembrando que este ProductDetails.id está vindo lá da camada de ViewModel do nosso UseProducts, Enfim, para vocês que pularam o módulo passado, é bom vocês darem uma olhada com calma aqui na estrutura do projeto para entender, mas agora o pessoal, os alunos que fizeram, que acompanharam o desenvolvimento do zero aqui do projeto, com certeza vão estar bem mais familiarizados com essas integrações no geral. Agora aqui dentro da nossa camada de ViewModel, vamos fazer a seguinte verificação. Vamos criar aqui uma const is favorite, já podemos chipar, que ele vai retornar pra gente um boolean. Ele vai ser igual a um useMemo, onde aqui dentro desse useMemo, lembrando que o useMemo, a estrutura dele é igual a do useEffect, ou seja, ele espera receber dois parâmetros, o primeiro sendo uma callback, uma função de callback que será executada, e um segundo, uma rate de dependência, onde aqui dentro, vamos simplesmente colocar um return, nosso favorites.sum, ou seja, vamos rodar um looping em cima do nosso array de favoritos e vamos verificar se algum dos itens que está aqui dentro possui o id igual, ou seja, vamos recuperar aqui dentro o id do produto, se o id é igual ao product id que nós estamos selecionando no momento, ou seja, quando nós abrimos aqui a piscina inflável, vamos supor que o id dele é 33, vamos procurar se possui algum item com id 33 dentro do nosso favorite, o som, por si só retorna para a gente o valor booleano ou seja se uma das condicionais que está dentro aqui do looping for true ele vai retornar o true para a gente então aqui já mais um aprendizado de uma high order function de uma função de ordem superior que vai servir perfeitamente aqui para o nosso caso, caso eu só mudar o nome aqui da variável de isFavorite. Vamos só fazer o seguinte, vamos colocar aqui um console.log no isFavorite. Agora um detalhe interessante também é que nós precisamos adicionar o favorites, ou seja, dentro da nossa rede de dependências e o ProductId. Agora, um outro detalhe interessante do porquê nós utilizarmos o UseMem é por questões de performance. Basicamente, supondo que nós tenhamos aqui diversas outras variáveis e diversos outros estados dentro deste CustomHook, supondo que qualquer um destes outros states sejam atualizados, consequentemente todo o nosso componente seria recalculado, ou seja, todas as variáveis, tudo seria remontado, então teria um certo Então, teria um certo nível de gargalo de performance aqui no nosso código. Agora, com o useMemo, supondo que tenha outras variáveis, outros estados aqui dentro do nosso custom hook e esses estados sejam atualizados, esta função não será recalculada, ela não será remontada pelo simples fato de que nós temos uma rede de dependências determinando quais são as variáveis que devem ser levadas em consideração quando elas forem atualizadas. Em suma, basicamente, nós só iremos recalcular esta nossa variável. Se o favorite ou se o product id sofrer alguma alteração, se qualquer outra variável ou qualquer outro estado aqui dentro do nosso custom hook for atualizado, nós não teremos esse problema, esse gargalo de memória. Então, isso é um detalhe que, neste caso aqui, pode ser um pouco de overengineer, talvez, mas acho que vale a pena o conhecimento. momentos específicos, vocês podem lidar com uma quantidade grande de dados, então o isMemo pode ser útil pra vocês. Agora vamos simplesmente pegar aqui o nosso isFavorite e passar pra dentro do return. Dessa forma, se nós voltarmos aqui na nossa camada de index, ou melhor, aqui na nossa ViewModel, nós conseguimos desestruturar o isFavorite dessa maneira. Agora a gente consegue fazer as condicionais corretas aqui no nosso botão. Basicamente, nós vamos vir aqui no name, vamos substituir, apagar a string e colocar uma template string a princípio. Ou melhor, nem precisa de uma template string. Se isFavorite for true, nós vamos renderizar o href. Caso contrário, vai ser o href. Outline it desta maneira. Opa, coloquei headset. É href desta maneira aqui. Como é true, então se a gente inverter aqui a condicional, Supondo que nosso produto esteja favoritado. E perfeito! Agora, para essa aula não se estender muito, vamos finalizar por aqui. Na próxima aula, nós implementamos as mutations que nós criamos aqui na nossa camada de ViewModel e atualizamos também aqui o nosso estado. No caso, atualizamos aqui também a questão da renderização do ícone dentro do nosso botão. Então, é isso. Espero vocês na próxima aula.