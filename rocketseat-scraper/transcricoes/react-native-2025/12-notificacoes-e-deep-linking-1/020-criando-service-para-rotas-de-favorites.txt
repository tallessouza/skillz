WEBVTT Nesta aula nós vamos criar a camada de service dos favoritos aqui no nosso aplicativo. Se nós abrirmos aqui a nossa API, nós podemos ver que possuímos uma rota de GET, uma rota de POST e uma rota de DELETE, todas utilizadas para atualizar o status do produto, dos favoritos aqui do usuário. Pra isso é bem simples, novamente dando uma explicação rápida aqui, da arquitetura das pastas do nosso projeto, nós possuímos dentro de src, shared, uma camada de service, uma camada de serviço, onde nós criamos arquivos, onde nós configuramos aqui as requisições HTTP com a nossa API aqui, com o nosso back-end. E pra isso, vamos criar aqui um novo arquivo chamado favorites. .service.ts onde aqui vamos colocar um exportCostGetFavorites vai ser simplesmente uma função vamos criar aqui também um exportCostAddFavorite e vamos criar uma outra que vai ser o exportCostRemoveFavorite Desta maneira, seguindo uma função para cada rota. Agora, o próximo passo que nós precisamos fazer é entender os parâmetros que as rotas esperam receber lá no BEC. No caso, o GET não espera receber nenhum parâmetro, mas tem um retorno, no caso é um array de objeto. Vamos já aproveitar e copiar este array de objeto aqui. Voltar no nosso app, vir aqui dentro de interface, HTTP, vamos criar aqui, favorite.ts, colocar aqui um export interface, favorite response vai ser opa no caso aqui vai ser o seguinte vamos remover aqui os colchetes vamos melhorar vamos simplesmente colocar aqui favorite desta forma caso aqui onde está zero vamos colocar number e onde está aqui a string vamos só remover as aspas duplas agora vamos colocar aqui um export interface get favorite response onde aqui nós vamos receber um array de objeto, no caso a gente pode simplesmente pegar essa interface que nós criamos, voltar lá no nosso favorite service, só fechar os outros arquivos aqui para não confundir vocês, aqui no getFavorites, vamos só colocar aqui um promise, enviando, passando aqui para dentro um array de favorite, desta maneira. Agora aqui, simplesmente vamos fazer uma requisição, uma HTTP, buscando aqui o nosso Marketplace Client API, podemos vir em qualquer outra dessas camadas de service aqui, copiar, e colocar aqui, fazer uma desestruturação, await MarketplaceClient.get, desta forma, só transformar essa função em uma função assíncrona. Aqui falta só colocar o endpoint, que é //favorite, caso favorites no plural, vamos desestruturar aqui o data, abrir o menor que maior que, atribuir o retorno, falar que essa nossa rota retorna este dado aqui específico e dar um return em data, dessa forma. O add favorite espera receber como parâmetro apenas o id do produto, então aqui vai ser bem simples, vamos só transformar aqui em uma função assíncrona, ela não retorna nada pra gente, então podemos simplesmente colocar aqui um await, marketplaceclient.post no caso igual a de cima favorites no plural onde no segundo parâmetro ele espera receber como parâmetro o ProductId, que é um number. Desta maneira. Vamos simplesmente passar aqui como parâmetro. E por fim, o RemoveFavorites é muito parecido, a gente pode até copiar toda essa estrutura que nós possuímos aqui. Só que no caso o verbo HTTP dele é o delete, salvo engano, é isso mesmo. é o delete e aqui fica no caminho ou seja fica na própria url então a gente simplesmente remove aqui dessa forma coloca uma template string e concatena o parâmetro que estamos recebendo aqui e removemos também o segundo parâmetro que do nosso verbo http além de alterar ele no caso delete desta forma Com isso, criamos os três métodos responsáveis por chamar as rotas aqui do nosso back-end, cada uma responsável por buscar os favoritos, adicionar e remover. Agora, na próxima aula, nós vamos criar a camada de queries e de mutation, onde nós vamos consumir, de fato, estes dados aqui, estes métodos que nós criamos para, sim, utilizarmos também aqui na nossa tela de detalhes de produtos. Espero que tenham gostado da aula e espero vocês na próxima.