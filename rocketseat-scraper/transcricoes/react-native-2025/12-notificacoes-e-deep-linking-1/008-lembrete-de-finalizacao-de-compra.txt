WEBVTT Nesta aula, nós vamos iniciar a implementação da notificação de call to action do usuário para quando ele adicionar algum item no carrinho de compras e fechar o aplicativo sem finalizar essa compra de fato. Para isso, vai ser mais simples do que vocês podem imaginar. Primeiro de tudo, temos de identificar onde que nós temos aqui a tela de detalhes do produto específico. Por exemplo, é aqui que nós possuímos o botão de adicionar. Então, podemos simplesmente pesquisar aqui. Adicionar. Possuímos estes três componentes, no caso é este daqui, que é o AddToCartFooter. Ele fica basicamente dentro de src, ViewModels, Product, Components, AddToCartFooter, que é este componente específico. Como nós podemos ver, ele está recebendo como propriedade Este handle-add-to-cart, ou seja, nós temos de ir até o local onde nós estamos utilizando este componente. Se nós apertarmos Ctrl, por exemplo, e clicarmos nele, já temos uma referência de onde este componente está sendo utilizado. No caso, é aqui dentro do ProductView, na camada de View da nossa tela. E nós podemos reparar que as propriedades que estamos passando para dentro dele estão vindo da nossa camada de ViewModel, ou seja, através das propriedades, lembrando que Se nós viermos aqui dentro de app, private, product, nesta rota específica, que é onde nós selecionamos e recebemos como parâmetro o ID do produto que nós estamos vendo, nós estamos basicamente pegando o nosso ProductViewModel, enviando como parâmetro o ID do produto que estamos recebendo, através da navegação aqui do ExpoRouter e enviando pra dentro da nossa camada de ViewModel, onde aqui nós repassamos essas propriedades pra dentro da nossa camada de Query, onde nós realizamos o fetch da nossa API, do nosso back-end, para retornarmos o detalhe do nosso produto. Importante dar essa... Essa explicação, por mais que vocês já tenham desenvolvido, muitos de vocês já tenham feito esta codebase no módulo anterior, mas principalmente para o pessoal que está chegando aqui e reaproveitando o aplicativo, basicamente é este o fluxo que nós realizamos. As requisições HTTP são feitas aqui dentro da camada de service, mas antes disso nós utilizamos aqui um hook, onde utilizamos o React Query para aplicarmos cacheamento no nosso request. Mas, enfim, aqui dentro da nossa camada de useProductViewModel, que é onde nós possuímos a nossa função de handleAddToCart, é aqui que nós iremos configurar a notificação para o usuário. Identificamos aqui o nosso componente, agora o próximo passo é nós criarmos lá na nossa camada de service, de local notification service, a função onde nós vamos configurar esta notificação específica. Aqui dentro nós vamos criar uma nova função, chamada de const scannlyCartReminder, onde vai ser uma função assíncrona, onde vamos simplesmente abrir chaves desta forma. Vamos receber algumas propriedades aqui nesta função. Então, vamos simplesmente colocar aqui acima dela própria um interface. CaddlyCardReminderInterface, onde aqui nós vamos receber como parâmetro o ProductName, que é o nome do produto que nós estamos olhando no momento, que é uma String. Vamos receber também o ProductID, que é o ID do produto, que é um Number. E por fim, um DelayInMinutes, que vai ser um Number. que é basicamente o tempo de delay que nós vamos querer emitir essa notificação aqui para o nosso usuário. Vamos pegar esta interface e colocar aqui dentro das propriedades da função. Vamos simplesmente já realizar um object destructuring aqui, uma desestruturação de objeto, atribuindo a tipagem do nosso ScheduleCardReminder interface, onde aqui nós já conseguimos ter acesso às propriedades que nós vamos passar lá da nossa camada de ViewModel quando nós chamarmos aqui a nossa função. Agora, próximo passo, vamos fazer o seguinte. Logo abaixo dessa nossa função, vamos colocar um export. export cost localNotificationsService onde nós vamos simplesmente exportar uma variável contendo um objeto com todas as funções que nós estamos criando aqui. No caso, vamos pegar esse schedulerCardToReminder e colocar aqui dentro. Vamos pegar este nosso local NotificationService e voltar lá no nosso UseProductViewModel, logo abaixo do nosso AddProduct. Vamos chamar esta nossa variável que nós criamos, ponto, chamar aqui a nossa função de ScheduleCartReminder. Onde aqui, vamos invocar a função, e como a nossa propriedade é obrigatória, temos de abrir chaves, e vamos passar aqui já, logo de cara, o delay em minutos. Vamos... normalmente passaríamos algo em torno de 30 minutos, vai depender muito do contexto da notificação em que vocês estão configurando. Porém, como aqui nós estamos em ambiente de desenvolvimento, não faz sentido nós colocarmos 30 minutos, até porque teríamos de ficar 30 minutos esperando pra ver se funcionou de fato, então vamos colocar aqui simplesmente 1 minuto por enquanto. Aqui o ProductId, vamos pegar o ProductDetails.id e por fim o ProductName vai ser o ProductDetails.name, dessa forma. Estamos chamando aqui a nossa função e enviando as propriedades necessárias para quando a nossa notificação for disparada ela saiba qual o nome do produto que nós configuramos. Agora, aqui dentro dela, nós vamos começar a montar a nossa função. Primeiro de tudo, nós precisamos garantir que o usuário dê o acesso, ou melhor, dê a permissão para que o nosso aplicativo utilize as notificações. Ou seja, notificação é uma coisa que requer a permissão do usuário para o aplicativo poder utilizar. Então, vamos criar aqui um const. asPermission, igual a, vamos só colocar um await, vamos pegar aqui o nosso notifications.getPermissionAsync, desta maneira, onde aqui nós simplesmente colocamos um if, se asPermission.status for diferente de vamos simplesmente colocar um return por hora depois vamos criar uma função específica para nós fazermos isso mas primeiro vamos montar aqui a nossa função de configuração de fato logo aqui abaixo do if vamos colocar um await setupNotificationChannel, ou seja, vamos invocar a função que nós criamos na aula passada para fazermos o setup inicial dos canais de notificação no Android. Lembrando que isso daqui acontece só no Android, porque o iOS tem uma forma diferente de lidar com as notificações, eles não utilizam o channel, então por isso nós colocamos este if aqui, vamos colocar aqui o await, e aqui embaixo vamos criar uma const, uma constante onde nós vamos armazenar a nossa notificação, no caso vai ser o notification. Vai ser um await notifications.scheduleNotificationAsync onde aqui vamos invocar a função e passar como parâmetro um objeto onde ele espera receber algumas propriedades como por exemplo o identifier que é o identificador da nossa notificação. Lembrando que algumas aulas passadas nós criamos aqui os IDs que seriam utilizadas em todas as notificações locais do nosso app. Então vamos pegar simplesmente a nossa variável, passar aqui atribuindo um valor à propriedade de identifier, ponto, cartReminder. A próxima propriedade é o content, onde em content nós possuímos um objeto de configuração de como será a nossa notificação, no caso eu já tenho até uma colinha pronta, que será esta daqui, title, que será uma string de você esqueceu algo no carrinho, com este emoji de carrinho de compras, o body, vamos simplesmente colocar o produto, no caso aqui tem que ser uma template string, que aqui nós vamos fazer a concatenação da variável que estamos recebendo aqui como parâmetro, no caso vai ser, abre chaves, product, opa, product name, vamos só colocar aspas duplas aqui em volta, está esperando por você. Finalize sua compra agora. Dessa maneira. Próxima propriedade que nós temos aqui é o data, onde em data nós vamos passar alguns metadados aqui pra nossa notificação, que no caso é o type dela. Vamos simplesmente colocar aqui uma string contendo cartReminder. O ProductId também vamos colocar aqui como String. O ProductId. E aqui ele espera receber também o DeepLink. Porém, nós não vamos passar o DeepLink aqui por agora, vamos deixar mais pra frente. Quando nós focarmos em DeepLink especificamente, nós voltamos aqui para configurar o DeepLink na nossa notificação. Próximo é o Sound, só que o Sound é opcional. Por padrão ele já vai deixar o Default, então a gente pode simplesmente remover dessa forma. Agora, ele tá com vários erros porque ele possui uma segunda propriedade obrigatória, que no caso é o Trigger. Esse Trigger é um objeto onde nós vamos configurar a frequência com que a nossa notificação local irá ser executada. No caso, ele espera receber como parâmetro, basicamente, o... ou seja, o tipo de recorrência que nós vamos querer que esta nossa notificação seja executada. No caso, nós já possuímos um enum específico para isso da biblioteca. Então, se nós pegarmos o notifications.schedulableTriggerInputTypes. TimeInterval, ele vai saber que vai ser um intervalo de tempo, ou seja, ele vai ser executado apenas uma única vez após o tempo que nós vamos configurar aqui embaixo na configuração de seconds, onde vamos simplesmente pegar o delayInMinutes e multiplicar por 60. No caso aqui, no nosso useProducts.ViewModel, nós tínhamos passado apenas um. Vamos voltar aqui para 30 minutos, na nossa camada de ViewModel. Na nossa NotificationService, vamos simplesmente colocar aqui 5 segundos, por enquanto. Depois nós retornamos aqui, utilizar o nosso Delay in Minutes. É mais para nós termos uma noção se está funcionando ou não. E por fim, agora, vamos simplesmente pegar este nosso Notification e dar um Return nele. Return Notification desta maneira. Agora, se nós viermos aqui em algum destes produtos, por exemplo, Este barbeador elétrico. Clicarmos em adicionar. Esperarmos 5 segundos. Vamos ver se vai executar. Na verdade, teve uma coisa estranha. Ele não solicitou a permissão pra gente, o que eu acredito que esteja relacionado aqui ao getPermissionAsync. Na verdade, vamos fazer uma modificação aqui na nossa constante de hasPermission. Ao invés de nós pegarmos o getPermissionAsync, vamos colocar o requestPermissionAsync. Dessa forma, ao invés dele simplesmente buscar a permissão da notificação, ele vai solicitar de fato. Então agora, vamos colocar um console.log aqui embaixo. console.log Notificação configurada. Dessa maneira, agora se nós clicarmos aqui em adicionar, ele vai perguntar pra gente, allow marketplace app to send your notifications, a gente clica aqui em allow. Dessa maneira, se nós fecharmos o nosso aplicativo e esperarmos 5 segundos, Olha só que legal, a nossa notificação funcionou perfeitamente, olha só que massa. Demorou exatamente os 5 segundos que nós configuramos aqui no Trigger para ele emitir. Agora, um detalhe legal é que dentro aqui dos nossos tipos nós possuímos vários outros tipos de recorrência de notificação, como por exemplo, diário através de uma data específica ou mensalmente, Time Interval, que é o que nós utilizamos uma vez por semana, uma vez por ano. E vários outros, isso daqui é bem legal, nós conseguimos explorar bastante as notificações locais no nosso app. Vamos só mudar aqui agora, remover este mock que nós colocamos no trigger do tempo de notificação. Ao invés de 5, vamos colocar aqui o delay em minutos, desta maneira. Finalizando assim a configuração da nossa notificação do carrinho de compras, a partir da próxima aula nós vamos criar uma função específica para nós requisitarmos as permissões para o usuário. No caso nós vamos executar isso daqui no momento em que o usuário abrir o aplicativo, que é uma outra abordagem que nós podemos fazer, ao invés de verificar e solicitar a permissão no momento em que nós formos configurar uma notificação, nós fazemos quando o usuário abrir o aplicativo, É isso. Para esta aula não ficar muito grande, vamos finalizar por aqui. Espero vocês na próxima!