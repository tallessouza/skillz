WEBVTT Olá Neve, na aula de hoje vamos iniciar a configuração do OneSignal aqui no nosso app e para começo de conversa nós precisamos instalar a biblioteca do OneSignal aqui no nosso app. Para isso vamos abrir a documentação, se nós viermos aqui no Google e pesquisarmos, por exemplo, Expo OneSignal, este primeiro que nós abrimos aqui já vai ser o suficiente, ele já vai mostrar aqui todos os comandos que precisamos fazer, todas as informações, no caso aqui embaixo, temos de rodar o npx expo-install-onesignal-plugin, então vamos copiar, voltar aqui no VSCode e dar um Ctrl-V no nosso app. Lembrando que se vocês estiverem rodando o aplicativo, é importante encerrarem o processo dele antes de instalarmos qualquer biblioteca. E perfeito, ele instalou aqui nas nossas dependências o OneSignalAppExpoPlugin. Agora vamos voltar lá na documentação e ele também pede pra gente instalar esse React Native OneSignal. Então vamos copiar, voltar aqui e só substituir esse npm install por yarn add react-native-one-signal. Então, são as duas bibliotecas. No caso, um é o plugin do OneSignal, do Expo, e o outro é o React Native OneSignal. Este plugin é muito importante, até porque se nós não tivéssemos ele, teríamos de configurar na mão os códigos nativos aqui do OneSignal. Então, se nós abrirmos aqui o nosso app.json, podemos ver que ele já adiciona pra gente o OneSignal Expo plugin automaticamente, o que já ajuda bastante. Quando nós rodarmos o Yarn Expo Prebuild, ele já vai... gerar os códigos nativos levando em consideração todas as levando em consideração todas as configurações que nós temos de fazer caso automaticamente isso daqui é muito legal uma outra coisa que nós podemos fazer aqui é colocar um colchetes em volta dessa maneira e aqui nós passamos um objeto onde que podemos colocar mode vamos colocar development por hora no caso aqui tem de ser com aspas A gente consegue setar se nós estamos utilizando ambiente de desenvolvimento ou ambiente de produção. No caso aqui, a gente vai deixar como modo de desenvolvimento mesmo. Dessa maneira, deixa eu só formatar aqui o JSON. E, perfeito. Agora, se nós rodarmos o yarn expo prebuild, ela já vai levar em consideração todas as configs aqui do nosso app.json automaticamente. Olha só que bacana. Agora, o próximo passo que precisamos fazer é criar um custom hook, onde nós vamos inicializar aqui o antesignal. Para isso, vamos vir aqui dentro de src, shared hooks. Vamos criar aqui um use. onSignal.ts onde aqui vamos simplesmente colocar um export const useOnSignal.ts onde vamos simplesmente colocar um return retornando um objeto vazio aqui por enquanto agora aqui dentro nós precisamos colocar um useEffect UseEffect, onde aqui nós vamos inicializar, vamos importar logo aqui em cima as variáveis aqui do React Native OneSignal. Então, vamos colocar aqui um import from React Native OneSignal. Vamos ter de importar aqui, a princípio, somente esse OneSignal. Onde aqui dentro do nosso UseEffect, vamos pegar, colocar um onSignal.initialize. Antes de nós passarmos o mouse em cima, ele espera receber um argumento, ou seja, uma propriedade, que é o appId, onde nós já possuímos o nosso appId aqui da nossa .env. Então, pra isso, nós vamos vir aqui em cima, criar uma const onSignal.appId, vai ser igual ao process.env. Vamos copiar aqui a nossa .env com o nome dela, expopublic onSignal.appId, e vamos passar aqui pra dentro. Agora a gente vai simplesmente fazer o seguinte, vamos colocar aqui um if, se o OneSignalAppId não existir, vamos só dar um return, ou seja, a gente não vai inicializar o serviço do OneSignal para evitar possíveis erros. Agora vamos estartar o nosso app, Yarn Android ou Yarn iOS. E o nosso app foi estartado corretamente, agora tudo o que nós precisamos fazer é recuperar o id do player, no caso todo o usuário aqui do OneSignal, todo o dispositivo possui um id específico. e através desse ID que nós sabemos para quem nós precisamos enviar a Punch Notification. Para isso é bem simples, vamos só fazer uma função aqui por hora. Vamos só colocar aqui, logo abaixo do nosso .initAlias, uma função que se auto chama. Essa função tem que ser uma função assíncrona, no caso, async. onde aqui dentro nós colocamos um const playerId igual ao oneSignal.user.pushSubscription.getIdAsync, basicamente uma função que retorna qual o ID desse usuário, só que ela é uma função assíncrona, então a gente precisa colocar o await aqui, e agora um console.log no nosso playerId desta maneira. Agora vamos só dar um reload e ele não mostrou nada porque nós não estamos utilizando esse nosso Custom Hook lá na raiz do nosso aplicativo. Então vamos só pegar esse nosso UseOneSignal, só alterar o nome pra deixar o S maiúsculo. Opa! OneSignal. Dessa forma. Vamos aqui dentro de srcApp. Underline layout. Abaixo aqui do nosso useNotification. Vamos só colocar aqui o nosso useOneSignal. Importar. Agora vamos dar um reload. Dessa forma aqui. e ele tá dando problema porque ele espera o unsignal com s minúsculo no caso aqui, vamos só alterar o nome manualmente ou melhor, vamos voltar aqui, o VS Code tem uma dificuldade pra fazer esse case sensitive vamos voltar aqui pra unsignal com app minúsculo então reload aqui no app e perfeito Depois eu vou fechar o aplicativo, vou corrigir aqui o OneSignal com o S minúsculo, colocar o S maiúsculo e estartar novamente, fazer a importação correta. Mas isso é um bug do VS Code, uma coisa que já está aqui há vários anos e nunca foi corrigido, então toma cuidado aqui com... Ele é muito case-sensitivo e quando a gente altera alguma coisa, ele simplesmente não pega a atualização. Então o código, ele continua, ele fica correto o nome do arquivo, porém o import dele não fica correto. Mas, enfim... Detalhes, podemos ver que nós possuímos o ID do player, no caso o ID do dispositivo do usuário, e este ID é utilizado pelo OneSignal para fazer o disparo das Push Notifications, significa que cada dispositivo terá um ID único, e este ID precisará obrigatoriamente ficar vinculado com o usuário logado lá no banco de dados. Ou seja, vamos ter de, no momento do login ou do registro do usuário, realizar o envio desse ID gerado automaticamente pelo OneSignal lá para o nosso back-end, lá para o nosso banco de dados. Porém, vamos fazer isso na próxima aula. Vamos ter de identificar os novos campos que o nosso back-end espera receber referente ao OneSignal, a chave do nosso dispositivo. Mas é isso, para esta aula não ficar muito grande, eu vou finalizar por aqui e espero vocês na próxima.