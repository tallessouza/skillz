WEBVTT Fala Dev, na aula de hoje iremos de fato executar o ato de login do nosso aplicativo e para isso nós precisamos voltar lá na nossa camada que seria a camada de model responsável pela comunicação com o nosso back-end, mas antes disso precisamos garantir que o nosso back-end está rodando, lembrando que eu ensinei como rodar o back-end no início deste módulo Caso não tenham alguma dúvida, é só vocês voltarem lá na aula e seguir o passo a passo. Vamos simplesmente rodar um Yarn Dev. Assim que recebemos a mensagem de API rodando na porta 3001, temos certeza que o nosso Backend está rodando. Perfeito. Próximo passo que vamos fazer é acessar a documentação da nossa API. Vamos acessar aqui localhost 3001, no caso, barra docs. E aqui nós podemos ver toda a documentação da nossa API, e dessa vez nós vamos chamar o endpoint de authlogin. Ele é um endpoint bem simples, onde nós vamos enviar para ele o e-mail e a senha, e ele irá retornar para a gente os dados do usuário, o token e o refresh token, não tem muito segredo. Então, para isso, vamos simplesmente copiar aqui o nosso endpoint, voltar para o nosso código, e aqui na camada de service, em auth service, vamos criar uma nova função, export const. Login vai ser uma função assíncrona e aqui nós iremos receber como parâmetro o e-mail e a senha, mas depois vamos fazer isso. Primeiramente, vamos colocar uma const, vamos desestruturar o nosso await marketplace-api-client.post. Lembrando que é importante nós olharmos o verbo HTTP da nossa rota, do nosso endpoint, no caso é o post. E aqui vamos simplesmente passar o //login. Ele irá retornar pra gente um data e vamos simplesmente dar um return em data. Como podemos ver, o data está tipado como N, e nós não estamos mandando nenhum parâmetro para o body da nossa requisição. Então, para isso, vamos ter de criar a interface da mesma forma que nós criamos para a nossa função de registro. Então, vamos vir aqui dentro de HTTP, vamos simplesmente dar um ctrl-c e ctrl-v aqui em register, e vamos mudar o nome dele de register para login. Aqui dentro, nós podemos remover esse phone, avatar, o EL e name, até porque nós vamos enviar apenas o e-mail e a senha. Vamos mudar aqui também o nome de register HTTP para Login HTTP params. E um detalhe interessante é que se nós analisarmos a interface de registro HTTP response e de... A mesma resposta que nós temos da nossa rota de registro é a mesma da nossa rota de login. Então, vamos simplesmente criar uma nova interface dentro de HTTP e colocar aqui, alf-response.ts. Aqui vamos colocar um export interface. Vamos voltar aqui dentro de register, copiar todos esses parâmetros que nós temos e jogar dentro aqui da nossa interface. Ficou faltando colocar o nome, caso aqui é o alph, response. Vamos importar também o user interface e vamos literalmente reutilizar essa mesma interface tanto no register quanto no login, até porque é literalmente a mesma interface que nós temos. Então nós podemos simplesmente vir e apagar esse register HTTP response daqui de dentro. Vamos apagar a importação de user aqui também. Login é a mesma coisa, nós não teremos uma interface específica para login. Agora aqui na nossa camada de service ele irá retornar um erro, até porque ele não existe mais esse register HTTP response. Agora existe o ALF response, vamos voltar e substituir. Aqui no nosso ALF response, aqui também ALF response, perfeito. Agora, a nossa função de login irá receber o userData, que será simplesmente o login.httpParams, a interface que nós acabamos de criar. Vamos pegar ele e vamos repassar logo depois aqui das aspas do primeiro parâmetro do método de post do nosso Marketplace API Client. Vamos colocar uma vírgula e repassar o userData. Próximo passo que nós vamos fazer é englobar essa nossa requisição HTTP dentro de uma mutation, da mesma forma que nós fizemos em register. Lembra que aqui dentro de queries, dentro de auth, nós tínhamos aqui o userRegisterMutation? Vamos fazer literalmente a mesma coisa com o de login. Vamos simplesmente dar um ctrl-c ou ctrl-v e alterar o nome dele, que a estrutura vai ser idêntica. O caso vai ser o useLoginMutation. Vamos mudar o nome dele aqui também, o useLoginMutation. Vamos mudar também os parâmetros, o login, HTTP params. E aqui no auth.register, nós não vamos mais chamar register e sim login. Dessa forma, já temos a mutation da nossa requisição HTTP responsável por realizar o login. Lembrando que, como nós estamos utilizando o React Query, é interessante nós englobarmos todas as nossas requisições em mutations e queries para que, caso haja necessidade, nós possamos utilizar, usufruir de todos os recursos do nosso React Query. Por exemplo, aqui dentro nós poderíamos simplesmente invalidar uma chave do react-query, uma key, para que dessa forma nós fizéssemos o refresh de algum outro dado específico, nós iremos ver esse tipo de detalhe mais à frente. Por enquanto, vamos simplesmente manter dessa forma com o onSuccess tendo um console.log e o onError também tendo um console.log. E essa aula vai ficando por aqui, pra ela não ficar muito longa. Na próxima aula nós vamos voltar no nosso useLogin.viewModel, criar a nossa função de handleSubmit onde nós iremos chamar de fato a nossa mutation. Então é isso, espero que vocês tenham gostado da aula e espero vocês na próxima.