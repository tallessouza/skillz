WEBVTT Vamos dar seguimento com o desenvolvimento dos nossos dois custom hooks que criamos na aula passada. Customs hooks, esse onde nós delegaremos toda a responsabilidade por acessar a câmera ou acessar a galeria de fotos do nosso dispositivo. Novamente, entrando naquela questão da arquitetura MVVM, onde nós desacoplamos completamente toda a parte lógica na camada de model, nós vamos isolar toda esta feature dentro desses dois custom hooks. E vamos começar com o de useCamera. Primeira coisa que nós temos de entender é que nós vamos receber alguns parâmetros para dentro desse nosso useCamera, parâmetros esses que nós vamos criar uma interface para mapeá-los, interface useCameraOptions, onde aqui nós vamos receber o aspect, que será um array de number, number, o quality, também será opcional, number, o allow, EDITING será um boolean. USEIF, ESIF também será um boolean. Todos eles opcionais aqui dentro do nosso useCamera. Vamos colocar OPTIONS. USE CAMERA OPTIONS. Vamos só mudar aqui de USER para USE. Para facilitar, vamos já realizar a desestruturação das nossas propriedades aqui mesmo. Próximo passo que nós vamos fazer é criar um estado onde nós iremos armazenar a propriedade de loading. O fato de nós solicitarmos para nós abrimos a nossa galeria ou abrimos a nossa câmera, é necessário nós dispararmos requisições assíncronas. Então, vamos simplesmente pegar aqui, igual a useState, começa como false, loading, isLoading, set isLoading. Perfeito. A próxima coisa que precisamos fazer dentro do nosso custom hooks de useCamera é uma função para nós solicitarmos as permissões. Para isso, vamos fazer o seguinte. Cost request camera permission igual ao useCallback. Vamos abrir aqui uma callback aqui de dentro, passar um array de dependência e transformar essa função em uma função assíncrona, onde ele irá retornar um promise Boolean. Perfeito. E aqui de dentro nós vamos fazer o seguinte. Vamos colocar um try catch. contendo o nosso erro e um return falso. Esse try catch porque aqui dentro nós vamos realizar a solicitação de permissões e caso a pessoa negue nós iremos cair dentro do catch retornando um falso desse request camera permission. Dentro desse try catch nós vamos fazer a implementação, então vamos importar aqui import asterisco, ou seja, vamos importar absolutamente tudo e colocar um alias de imagePickerFromExpoImagePicker Vamos pegar esse nosso ImagePicker, voltar dentro do TrackCatch e fazer uma desestruturação com await ImagePicker.request permission async. Daqui de dentro vamos pegar o status. Logo abaixo do nosso status vamos simplesmente colocar um return status igual a granted. Dessa forma, garantimos que se a nossa permissão for concedida, nós vamos retornar um TRUE. Caso contrário, ele vai retornar um FALSE. Ou caso a pessoa chegar a negar ou até mesmo fechar a solicitação, nós vamos cair dentro do CAT, o que vai, novamente, fazer com que a nossa função retorne um FALSE. Agora vamos pegar esse nosso REQUEST CAMERA PERMISSION e repassar aqui pra dentro do RETURN. Vamos passar também o ISLOADING. Próxima função que nós vamos criar é a função de abrir a câmera de foto. Então vamos colocar aqui CONST. Open camera igual a, novamente, uma use callback, onde aqui dentro teremos uma callback vírgula, uma rede dependência. Transformando essa callback em uma função assíncrona. E essa callback irá retornar para nós uma promise de uma string ou nulo. No caso, ele irá retornar uma string quando nós conseguirmos, de fato, tirar a foto. Ele irá retornar para nós o caminho onde essa foto ficou armazenada, onde nós utilizaremos posteriormente. Tanto para renderizar na tela, quanto para realizarmos o upload, de fato, para o nosso back-end. Ou nulo, caso a pessoa tenha simplesmente cancelado ou tenha acontecido algum erro no momento de tirar a foto. Aqui dentro, vamos fazer o setIsLoading para true. Aqui abaixo, colocar um tryCatch e um finally. Hoje no finally, colocaremos o setIsLoading para false. Perfeito. Dentro do try, vamos fazer uma const hasPermission. Vamos validar se nós temos a permissão de fato. await request cameraPermission Se nós não tivermos a permissão, vamos simplesmente dar um return em nulo. Agora, se nós tivermos a nossa permissão, vamos dar um const result igual a await imagePicker.launch() CameraAsync. Vamos abrir parênteses porque isso daqui é uma função e ele espera receber como parâmetro um objeto. E dentro desse objeto ele espera receber basicamente todas as propriedades que nós recebemos aqui em cima, no caso o allowEditing, o aspect, if e a quality que são todas configurações para quando nós abrirmos a nossa câmera. Vamos pegar aqui o nosso result e vamos fazer uma validação, if. result.canceled e, ou seja, se a nossa requisição foi cancelada, se o result.assets e result.assets.len for maior que zero, vamos simplesmente dar um return result.assets 0.URI. Caso contrário, vamos simplesmente retornar 1. Aqui nós estamos basicamente validando se o nosso resultado foi cancelado ou não. No caso, ele não foi cancelado. Os assets, no caso, as imagens do resultado existem e eles são maiores do que 0. Estamos simplesmente repassando a URI, ou seja, o local em que a foto que foi tirada está armazenada. Caso contrário, caso não caia dentro dessa condição, nós simplesmente retornamos 1. Vamos pegar agora o nosso Open Camera. Opa, ele tá retornando aqui. Um erro. Deixa eu entender porquê. que ficou faltando aqui, nós colocamos um return no dentro do catch. Uma outra coisa que nós vamos fazer aqui, já aproveitando o embalo, é nós instalarmos uma biblioteca de Toast para nós avisarmos para o usuário quando tiver algo de errado. No caso, vamos utilizar aqui o Toastify React Native. Vamos parar a execução do nosso aplicativo e rodar o yarn add Toastify React Native. Para nós configurarmos ele é bem simples. Tudo que nós precisamos fazer é vir dentro aqui de app layout, no nosso root layout, vamos dar aqui um import host-manager-from hostify-react-native, vamos pegar este componente e colocar aqui no nosso root layout, vamos passar aqui o useModal como false. Perfeito. Agora aqui dentro do nosso useCamera vamos colocar um import from Toastify React Native, importando o toast. Vamos pegar esse toast, passar pra dentro do cat toast.error, mandando a mensagem erro ao abrir câmera. Depois da string vamos colocar a vírgula top, que é o local onde vai aparecer esse nosso toast. Vamos estartar nosso app aqui novamente. Vamos já aproveitar enquanto vai estartando, vamos colocar o toast em outros locais, como por exemplo, dentro do return. no caso aqui não vai ser mais de erro e sim de success, success, post.success, mandando a mensagem foto capturada com sucesso, exibindo aqui no top. Vamos passar esse post aqui também no nosso hook, na nossa função de requestCameraPermission, vamos simplesmente fazer uma validação, vamos pegar essa condicional que nós criamos validando esse status, é igual a granted, armazenar em uma variável, cost, current status igual a status igual a granted vamos simplesmente colocar um if se current status for false vamos simplesmente exibir um toast ponto erro ou seja uma mensagem de erro se o cara negou se o usuário negou o acesso à câmera vamos exibir precisamos da permissão para acessar sua Câmera. Vamos exibir o topo. Pop. E vamos substituir aqui embaixo o CurrentStatus. Agora, aqui no catch, vamos colocar também um tolstice de erro. Vamos simplesmente copiar esse daqui. Isso daqui é a mensagem que nós vamos mudar. E erro ao solicitar permissões da câmera. E para finalizarmos, vamos simplesmente pegar o nosso OpenCamera e passar aqui pra dentro do nosso return. Vamos ir dentro da nossa camada de ViewModelDeRegister, aqui dentro do nosso CostSelection na nossa função de handleSelectAvatar, nós vamos chamar o nosso cost igual a useCamera, passado como parâmetro. Simplesmente um objeto vazio por enquanto. Vamos pegar aqui o nosso openCamera. Dentro do nosso showSelection na nossa função de handleSelect, agora é só nós pegarmos o nosso Open Camera, vir no onPress de câmera do nosso handleSelect avatar e passar a nossa função. Navegarmos até a tela de registro, clicarmos no nosso botão para selecionar a foto de perfil e clicarmos em câmera. Olha só que legal, ele já habilita, ele já faz a solicitação da permissão pra gente. Vamos clicar aqui on this time. E olha só que legal, o boneco que aparece pra gente simulando o uso da câmera do nosso dispositivo real. Quando nós tirarmos a foto e clicarmos em OK, ele já irá exibir o modal pra gente falando que a nossa foto foi capturada com sucesso. Olha só que bacana. A nossa feature funcionando lindamente. Na próxima aula daremos segmento com as integrações. Vamos acabar aqui pra que essa aula não fique mais longa. Espero vocês na próxima.