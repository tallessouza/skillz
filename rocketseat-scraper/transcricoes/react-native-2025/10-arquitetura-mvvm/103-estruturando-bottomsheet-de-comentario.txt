WEBVTT Na aula de hoje, vamos iniciar a construção do nosso componente de Boton Sheet, onde nós iremos criar o nosso comentário. Se nós voltarmos aqui no Figma, podemos ver que quando nós clicarmos aqui em Avaliar, nós vamos abrir este Boton Sheet, onde nós teremos as estrelinhas para marcarmos a nossa avaliação e um comentário logo abaixo. Para começarmos criando este componente, vamos vir aqui dentro de src, View Models, Product, Components, vamos criar aqui um componente novo chamado Review Boton Sheet. onde aqui dentro nós vamos criar um index.tsx, vamos criar review.bottomSheet.view.tsx e vamos criar também aqui um useReview.bottomSheet.viewModel.tsx Neste componente nós vamos aplicar, né, a nossa arquitetura MVVM, caso aqui ao invés de .tsx vai ser só .ts Vamos criar aqui um export cost useReviewBottomSheetViewModel Vamos simplesmente colocar um return em um objeto vazio por enquanto Vamos ir aqui na nossa camada de view export cost review bottom.sheet.view vamos só alterar o nome ali do arquivo vou colocar 2s bottom.sheet aqui um return vou só colocar uma view aqui por enquanto já vamos aproveitar e tipar o nosso componente com o funcional component com o return type menor que maior que type of nosso useReview BottomSheetViewModel, já vamos desestruturar aqui. E aqui no index, só um export, cost, review, opa, review, bottomSheet. E vamos simplesmente dar um return no nosso review, bottomSheetView, vamos pegar aqui nossa ViewModel, ViewModel, igual ao useReviewBottomSheetViewModel, vamos pegar, a nossa ViewModel, e passar através de um Spread Operator aqui pra dentro da nossa camada de View. Aqui no índex do nosso ReviewBottomSheet, vamos criar uma interface, interface, ReviewBottomSheetParams, aqui nós vamos receber o ID do produto, que nós queremos ver, se já possuímos um comentário ou não, no caso aqui o ProductID, no caso aqui vai ser um Number, vamos pegar a nossa interface, passar aqui, falando que é um FunctionalComponent, receber destes parâmetros, Desta forma, vamos pegar e repassar este product id pra dentro da camada de model, e vamos chipar aqui também, vamos falar que nós recebemos o product id number. Maravilha. Agora vamos pegar aqui o nosso review bottom sheet, no caso o index dele, voltar no nosso useProductViewModel, vamos criar uma nova função, const handleOpen, Review, aqui vai ser simplesmente uma função onde nós vamos validar se o product details for falso, no caso for nulo ou undefined, vamos dar um return. Caso contrário, vamos desestruturar aqui acima, abaixo do nosso useModalStore, o const igual a useBottomShitStore Vamos pegar aqui o método de open e colocar um alias dele para não dar um conflito aqui com o método de open do modal. O caso vai ser aqui, open bottom sheet. Vamos pegar aqui a nossa função de open bottom sheet, vi aqui embaixo no nosso handle open review. chamar ele, aqui ele espera receber o conteúdo, no caso o content, que é o componente, então vamos passar aqui o nosso createElement, igual nós fizemos aqui em cima, enviando pra lá o nosso review, bottom sheet, vírgula, Aqui vamos passar o parâmetro que ele espera receber, que no caso é o ProductId. Desta forma. A diferença é que esse daqui ele não espera receber os parâmetros soltos igual esse outro aqui do modal, então nós vamos pegar esse nosso createElement, abrir um objeto aqui dentro, onde aqui nós passamos o content, dois pontos, o nosso createElement, Perfeito, por enquanto não vamos passar nada na config, vamos pegar esta nossa função de handleOpenReview, repassar aqui pra dentro do nosso return, para recuperarmos na nossa camada de view, e por fim repassarmos aqui pra dentro do nosso componente de header, aqui no headerComponent, vamos passar o nosso handleOpenReview, vamos só criar a tipagem dele lá no componente de header, então aqui nós vamos colocar o HandleOpenReview é uma função que retorna void. Vamos recuperar ela aqui na desestruturação. Vamos remover esse console aqui também. Vamos pegar o HandleOpenReview, vamos lá embaixo no botão que nós temos de avaliar no evento de onPress. Simplesmente enviando o HandleOpenReview desta forma. Agora vamos fazer um teste. Vamos vir aqui na camada de view do nosso ReviewBottomSheet e vamos só colocar um text aqui. Text. Review do produto. Vamos importar o nosso texto. Vamos selecionar aqui qualquer produto, essa piscina inflável. Vamos clicar em avaliar. E olha só que legal, já temos aqui o nosso borrachete abrindo perfeitamente. Na próxima aula nós vamos criar as requisições HTTP que serão necessárias aqui para a nossa funcionalidade. Para alterarmos, criarmos e buscarmos os detalhes da review. Então é isso, espero vocês lá.