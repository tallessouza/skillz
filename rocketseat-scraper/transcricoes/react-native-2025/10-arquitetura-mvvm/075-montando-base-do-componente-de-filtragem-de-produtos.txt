WEBVTT Vamos dar início ao desenvolvimento da parte visual do nosso filtro. A primeira coisa que nós vamos fazer é adicionar uma outra view aqui dentro da view que nós já possuímos. Vamos colocar aqui também um text do próprio React Native com o texto de filtrar anúncios, onde nesta view nós teremos um class name contendo um flex row para jogar os itens um na frente do outro, um item center, um justify between, um padding de 4 pixels, e um px de 6. Perfeito. Nós clicarmos aqui, ele já vai ser exibido, olha só que legal. Vamos adicionar aqui também um class name no nosso texto, com um text-lg, para aumentar o tamanho do nosso texto, e um font-bold, com um text-gray, na tonalidade 900. Logo abaixo do nosso text, vamos adicionar um touchable, Opacity, portanto, do React Native, contendo um Ion, icons, com o name de close, um size de 20, um color, vamos pegar nossa variável de cores, colors, PurpleBase, perfeito, esse daqui vai ser o botão responsável por fechar o nosso bottom sheet, como podemos ver ele já funcionando aqui perfeitamente. Agora, logo abaixo da view que nós criamos, vamos colocar uma outra view, contendo um className, com padding de 4 e um px de 6. Vamos pôr um text contendo um className .semiBold, text-base e um text-grade 300. E aqui dentro vamos colocar valor, tudo em uppercase, onde aqui vamos colocar uma outra view, contendo o nosso app-input, onde aqui nessa view vamos colocar um class name com flex1, para ocupar 100% do espaço. Aqui vamos ter um placeholder de KeyboardType numeric e o container className contendo o width de 90%. Perfeito. Vamos fazer a mesma coisa aqui logo abaixo, vamos simplesmente copiar esta nossa view inteira, colocar aqui. Agora vamos pegar estas duas views que nós tínhamos, vamos englobar uma outra view em volta delas. Vamos pegar e roubar as nossas duas views contendo o nosso AppInput, contendo um ClassName com um FlexRow para jogar todos os itens um à frente do outro, um MarginBottom 4 e um Width de 100%. Maravilha, dessa forma temos os dois inputs, um na frente do outro, já temos aqui os inputs referentes do filtro de valor. Abaixo desta view, onde nós estamos englobando os nossos inputs, vamos colocar uma outra, vamos colocar um text de categoria, onde aqui vamos colocar um class name, contendo um font semibold também, semibold, com um text base, um text gray na tonalidade de 300, Perfeito. E aqui nós vamos ter de fazer uma requisição HTTP, onde nós iremos buscar as categorias dos nossos produtos. Então, para isso, nós vamos ter de criar uma requisição HTTP e uma query, onde nós iremos consumir ela e vamos renderizar aqui em tela. Vamos fazer isso na próxima aula que vamos simplesmente acabar a estrutura base, vamos simplesmente deixar o texto por aqui e pular para a próxima parte que seria simplesmente os botões de limpar filtro e de filtrar de fato. Então vamos colocar aqui abaixo uma outra view contendo um class name com flex row para jogar os itens um na frente do outro, um gap de 3, um margin top de 4 e um margin bottom de 6. Onde aqui dentro desta view vamos colocar uma outra view contendo o app-button. E aqui vamos colocar limpar filtro. Vamos simplesmente ter um variant outline. Colocar um class name com flex1 em volta desta view. Vamos simplesmente dar um ctrl-c, ctrl-v aqui. Mudar. E vamos simplesmente remover esse variant aqui pra ele ficar com o default, que é o field. o botão preenchido e perfeito com isso temos a estrutura base do nosso filtro feito na próxima aula nós iremos criar a requisição http responsável por buscar as categorias e posteriormente criar a query e renderizar elas em tela então espero vocês na próxima aula