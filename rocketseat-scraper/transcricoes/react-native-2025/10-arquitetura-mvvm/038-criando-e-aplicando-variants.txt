WEBVTT Seguindo com o desenvolvimento do nosso AppButton, hoje nós vamos configurar os nossos Button Variants, de fato. Já abrindo eles aqui, nós vamos dar seguimento com a utilização base dos nossos Slots, no caso nós já colocamos aqui o Base. Vamos colocar aqui também a utilização do nosso texto, ou seja, Text. Vai começar com o Font. semibold e o textbase para o tamanho da nossa fonte e o icon e o icon simplesmente com uma string vazia agora assim como nós fizemos no nosso app input no caso lembrando que nós precisamos declarar os valores default do nosso estado do app input variants no caso teremos aqui algumas condicionais que serão utilizadas no de input nós temos simplesmente o isDisabled, o isError e o isFoxed aqui no button variants nós teremos a seguinte teremos O rest icon, que será um booleano, que começará como false. Será a variável responsável por dizer se o nosso botão tem um ícone de fato ou não. Teremos também um isLoading. Também vai começar como false. O isDisabled, que também será false. E por fim, nós teremos uma chamada de variant. Esta variant, nós vamos criar um enum específico para ele. Então, acima do nosso button variant, da nossa constante, vamos colocar aqui um export enum. app.button.variants.enum, onde aqui dentro nós teremos dois tipos de variação. Teremos o field, será quando ele estiver preenchido, caso aqui um string field, e teremos também a variante de outline. quando o nosso botão não estiver preenchido, ou seja, com o background vazio. Outline it. Vamos pegar, tipar o nosso default variant aqui abaixo com o AppInputVariant em um ponto. Ele irá começar preenchido e através destas propriedades que nós iremos criar as condicionais dentro do nosso variants para que dessa forma ele realize a troca da estilização dos nossos botões. Para isso vamos fazer o seguinte, aqui dentro nós vamos começar com o HasIcon, dentro da condicional de HasIcon temos o true Temos o false, por ele ser um valor booleano. Caso seja true, nós iremos estilizar os nossos elotes da seguinte forma. O nosso base terá um justify between. Caso nós não tenhamos nenhum icon, o nosso base terá a estilização de Justify Center. E olha só que legal, ele já centralizou o nosso texto. Isso daqui quer dizer que quando nós não temos nenhum icon para ser renderizado do nosso app button, nós vamos simplesmente centralizar todo o texto no centro do botão. Agora, caso contrário, caso de fato nós tenhamos um ícone a ser renderizado, nós vamos jogar o texto em uma extremidade e o ícone na outra da mesma forma que está no nosso Figma. Agora, o próximo que nós vamos fazer do hasIcon será o isLoading. Dentro do isLoading, assim como o hasIcon, teremos duas condicionais, no caso o true e o false, onde caso ele seja true, o nosso base ficará com uma opacity de 60. Ou seja, quando o nosso botão estiver carregando algo, estiver carregando uma Promise, além de renderizar um Activity Indicator que nós vamos colocar futuramente, nós vamos colocar uma pequena opacidade dentro dele. E agora, parando para pensar, não vai ter a necessidade da condicional de false dentro do IsLoading, porque o estado natural dele já seria a própria variante. Então, vamos simplesmente remover. Depois do IsLoading será o IsDisabled. Novamente, teremos aqui um true. No nosso Bass, com um Opacity. de 50, ou seja, uma opacidade um pouco menor do que nós temos aqui com o loading. Aqui novamente nós não vamos colocar a condicional de false. A próxima variant que nós teremos aqui será o próprio variant. Então aqui nós vamos fazer o seguinte. Diferente desses outros onde a nossa condicional é um true ou um false, um valor booleano, aqui nós temos o nosso enum. E o enum nada mais é do que uma string que ele tá retornando pra nós, no caso o field e o outline. Nós podemos fazer da seguinte forma, simplesmente copiar as condicionais e colocar aqui dentro. Caso a nossa string de variant seja fielded, nós vamos retornar um base contendo um bg, purple base, um border, purple base. Teremos o nosso text. com um text white, caso o nosso texto ficará branco. Agora, caso não seja o fielded, seja o outlined, nós vamos reverter, nós vamos deixar o base com o bg transparent e o border purple base. Nosso text terá um text Purple Base. Então eu corrigi aqui pra Purple e Purple. Perfeito, olha só a nossa estilização já sendo aplicada. Agora voltar aqui no nosso index.tsx. Vamos simplesmente vir aqui no class name do nosso componente de text. Vamos ver essas aspas, colocar o styles.text. Abre e fecha parênteses ali. Olha só, já temos a base do nosso botão construído. Na próxima aula nós iremos renderizar o nosso ícone de fato. Espero vocês na próxima.