WEBVTT Nesta aula vamos dar segmento com a construção da nossa tela de Home e para isso vamos criar a nossa camada de ViewModels aqui dentro de src ViewModels Home. Vamos criar aqui o nosso Use Home. .viewmodel.ts onde aqui dentro vamos criar um custom hook export const useHomeViewModel vai ser simplesmente retornar um objeto vazio por enquanto onde vamos vir aqui na nossa camada de HomeView vamos falar que ela é um funcional component que espera receber como parâmetro um return type typeof useHomeViewModel E aqui dentro nós vamos simplesmente desestruturar, recuperando todos os itens que estamos repassando aqui da nossa camada de ViewModel para a nossa UI. Próximo passo, vamos pegar a nossa camada de UseHomeViewModel, vim aqui dentro de app private tabs home.tsx criar aqui const viewModel igual a useHomeViewModel pegar o nosso viewModel e passar um spread operator aqui dentro. Dessa forma garantimos que sempre que adicionarmos uma propriedade aqui dentro vai automaticamente aparecer na desestruturação da nossa camada de view. Próximo passo que precisamos fazer é criar aqui dentro uma constante const o nosso useHomeViewModel o mesmo que nós criamos na aula passada e aqui dentro ele irá retornar várias propriedades como por exemplo o data-error fetch-nextpage, has-nextpage, is-fetch-nextpage, is-loading E detalhe que todas essas propriedades, todas essas funcionalidades nós teríamos de lidar na mão caso nós não estivéssemos utilizando o React Query. Então isso daqui é algo que é fora da curva. A lib do React Query proporcionar isso tudo pra gente é algo que é fenomenal. Próxima coisa que nós vamos fazer aqui dentro é simplesmente repassar algumas dessas propriedades aqui para dentro do return, mas antes disso vamos criar métodos específicos para alguns deles como, por exemplo, nós teremos aqui uma função de handleLoadMore, então const handleLoadMore ela será chamada toda vez que a nossa flat list chegar no final dela e aqui nós iremos fazer algumas validações antes de executarmos ela de fato, vamos colocar aqui um if se has next page ou seja se a próxima página de fato existir e nós não estivemos buscando uma próxima página is fetching next page e is Ou seja, se nós não estivermos realizando nenhum tipo de busca no momento e, de fato, possuírem próximas páginas na nossa requisição HTTP, na nossa rota, nós vamos executar o fetch next page. Maravilha! Vamos pegar o nosso handle load more e passar como parâmetro aqui. Vamos dar um reload do app pra ver se tá tudo ok, perfeito. Vamos criar também um método de refresh, const handleRefresh igual a, simplesmente uma função, vamos colocar aqui que ela é async, await, refetch, perfeito. Vamos pegar o nosso handle refresh e passar aqui pra dentro também. Agora vamos pegar e dar um console.log aqui no nosso data, vamos ver se já está funcionando. Console.log em data, ele está retornando simplesmente um undefined por hora, vamos entender por quê. até de usuário aqui, porque aquele outro usuário que eu estava usando estava dando um problema. Eu tinha deletado ele do banco de dados, mas... Olha só que legal! Podemos ver aqui que a nossa requisição HTTP já está funcionando. O nosso useInfinityQuery já está retornando o objeto, contendo todos os itens desta página específica, todos os nossos produtos. Agora vamos fazer o seguinte, vamos remover parte desses console.logs que estão piscando na tela para não ficar poluindo muita coisa. console.log Vamos remover aqui o do userData. token também, eu tinha colocado até um alert aqui para ver se estava tudo funcionando nos conformes, então eu vou mover aqui todos esses console.logs, isOpen, erro, não, esse daqui deixa, os de erros eu vou deixar, handleRefreshed é mentira, user, o de userData, Vou dar o RL também, perfeito. Vamos simplesmente dar um reload aqui. Agora temos apenas o console.log que nos interessa, que é o do infinite query. Vamos fazer o seguinte pra gente poder ter um vislumbre de absolutamente tudo que está sendo retornado pra gente. Vamos voltar aqui na nossa Use Home View Model. Vamos simplesmente dar um console.log com json.stringify em products. Ou melhor, em data. E olha só que legal, uma dica, se nós colocarmos aqui no... como segundo parâmetro e 2 como terceiro parâmetro, ele vai retornar pra gente o JSON todo formatado, bonitinho aí. Olha só que legal! Temos aqui a page contendo um array com todos os nossos produtos que deveriam vir nesta paginação específica. Olha só que bacana! Porém, reparem o formato que ele está retornando. Não está muito agradável pro que nós queremos. Até porque tudo que nós vamos querer é simplesmente extrair este array que nós temos dentro do objeto de data que o próprio Infinity Query está retornando pra gente. Para resolvermos este problema, vamos abstrair aqui dentro da própria query, dentro do nosso próprio custom hook. Vamos criar aqui uma cost, products, igual a. Vamos simplesmente pegar o nosso data.pages, e vamos rodar um flat map, onde aqui dentro ele irá iterar, sob o nosso array de pages. Vamos pegar o nosso page, e retornar o nosso page.data. Isso daqui vai simplesmente pegar todos os objetos que nós possuímos dentro de pages e ele vai simplesmente planificar todos os nossos objetos em um único array. Em suma, nós possuímos arrays com diversos objetos contendo a resposta que nós queremos e essas respostas ficam abstraídas dentro da propriedade de data. Tudo que nós estamos fazendo aqui dentro é transformando todo este array de objetos em simplesmente um único array, como se nós pegássemos e rodássemos um Spread Operator em cada um desses data que estão aqui, criando um único array. Dessa forma nós teremos dados mais consistentes. Vamos simplesmente substituir este data aqui por products, senão vamos repassar o data. Vamos pegar, remover ele daqui e substituir por products. Vamos repassar o nosso products pra cá, vamos ver se vai estar tudo correto agora. Vamos repassar aqui também o products para o return do nosso useHomeViewModel, vamos dar um reload. E quando nós chegamos aqui, olha só que massa, agora nós possuímos de fato um array de objetos de produtos onde nós vamos simplesmente voltar na nossa camada de Home.View Remover esse mock feio que nós tínhamos colocado algumas aulas passadas, pegar aqui o nosso products e repassar pra dentro da property data da nossa flatlist. Quando nós dermos um reload do nosso app, olha só, podemos ver a listagem de todos os nossos produtos, olha só que incrível. Um detalhe que ficou faltando é simplesmente nós colocarmos essa flatlist contendo duas colunas. Para nós fazermos isso é bem simples. A flatlist possui uma propriedade chamada numColumns. Onde se nós colocarmos simplesmente 2 e dermos um reload E olha só que bacana, temos aqui a nossa lista sendo renderizada com duas colunas Um detalhe é que eles estão muito próximos, vamos colocar uma estilização pra dividir eles, pra separar esses itens Pra isso nós vamos simplesmente colocar aqui um column wrapper-style contendo um objeto contendo o justify-content space Between. Dessa forma ele vai jogar os itens na extremidade do componente Pie e olha só que massa já temos sendo renderizado todos os nossos itens. E para encerrarmos esta aula vamos só fazer um pequeno ajuste aqui no nosso componente de Product Card e se nós repararmos a URL aqui dos nossos produtos está vindo como local host Porém, se nós utilizarmos ela no emulador de Android, simplesmente não irá funcionar, porque o emulador de Android não consegue acessar o local host diretamente. Para resolvermos isso, vai ser bem simples. Vamos ir aqui no nosso Product Card, aqui na nossa camada de Product View. Aqui onde nós temos o Product Photo, nós vamos colocar um helper que iremos criar, caso aqui é src, shared. Vamos criar aqui uma pastinha nova chamada de helpers. Onde neste helper nós vamos criar o buildImageURL.ts, onde aqui vamos ter uma função export const buildImageURL, onde ela vai receber como parâmetro uma string, no caso a URL, ou melhor, originalURL, vai ser uma string. Onde aqui dentro nós vamos fazer uma validação, vamos colocar aqui um if. Aqui dentro nós vamos validar se estamos no modo de produção ou no modo de desenvolvimento, no caso constants. Vamos importar do próprio expo. Vamos importar as constants aqui do expo constant, import. expoconstants vai ser constants.expoconfig.extra.isProduction caso vamos pegar aqui, colocar aqui o opcional, vamos transformar isso daqui em um booleano Se isProduction for de fato true, nós vamos simplesmente retornar o nosso original URL, porque partimos do pressuposto de que se estivermos em produção, vamos estar consumindo a API, no caso o endpoint, que vai estar na web, então não há necessidade dessa validação. Agora, caso o isProduction seja falso, nós vamos dar segmento aqui com o nosso helper. A gente vai simplesmente dar um return em platform.select Aqui vamos abrir parênteses, chaves, e aqui vamos passar Android. Se estivermos no Android, vamos pegar o nosso original URN, vamos dar um ponto replace do nosso local host. Vamos substituir pelo 10.0.2.2. Caso contrário, se for iOS, Simplesmente vamos repassar o nosso original URL. Agora ao invés de nós colocarmos lá no ProductCard, como eu tinha dito, vamos fazer melhor. Vamos pegar o nosso BuildImageURL, vamos vir aqui dentro de WhereIsProduct, aqui no nosso UseProductInfinity. Vamos vir aqui dentro do nosso Product, nessa constante que temos na linha 38, vamos simplesmente colocar um map, onde nós vamos simplesmente pegar o produto que está sendo interado, Product, Vamos retornar literalmente um object literal aqui, como o product, sendo que a foto do produto, vamos pegar o nosso build.image.url, passando o product.foto. Dessa forma, quando nós consumirmos lá na nossa product card, já vai estar 100% atualizada. Quando nós pegarmos aqui o nosso emulador de Android, ele já vai estar funcionando perfeitamente olha só que legal ele já vai ter substituído a base url em vez de localhost para este número aqui que é o local onde o android studio consegue se comunicar com o nosso localhost e com isso finalizamos a nossa aula por aqui espero vocês na próxima para darmos continuidade com o desenvolvimento do nosso app