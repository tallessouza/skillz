WEBVTT Nesta aula, vamos configurar a biblioteca que utilizaremos para acessar a câmera e as fotos do nosso dispositivo. No caso, vamos utilizar o Expo Image Picker. Então vamos parar o processo do nosso app, caso o app de vocês esteja rodando. Vamos rodar o npx expo install expo image picker. Vamos aguardar a instalação. Perfeito. O próximo passo que temos de fazer é vir aqui dentro do nosso app.json. Aqui nós vamos configurar em plugins algumas configurações para que, quando sejam gerados os nossos códigos nativos, o próprio Expo fique responsável por configurá-los. Antes do Expo, nós tínhamos, manualmente, de vir na pasta de iOS e de Android, fazer as manipulações, adicionar, por exemplo, a requisição de permissões. O Expo trouxe essa facilidade para nós. Vamos colocar aqui Uma vírgula e eu vou colocar uma colinha e vou deixar disponível para vocês aí nos comentários. Vamos colocar este array contendo este objeto, no caso contendo o XForImagePicker com objeto de fotos permission e câmera permission. Nós vamos vir aqui em Android. Dentro do objeto de Android, após o packages, nós vamos adicionar o seguinte. Vou pegar uma colinha aqui também, dar um ctrl-v. Vou disponibilizar todos esses códigos na descrição da aula para vocês simplesmente utilizarem. Próximo passo, nós vamos rodar o comando npx expo-prebuild. Dessa forma, nós vamos regerar os nossos códigos nativos, ou seja, a nossa pasta de iOS e a nossa pasta de Android. E para confirmarmos se tudo está ok, nós vamos fazer o seguinte no iOS, vamos ir aqui, marketplace-app. Info.plist e vamos procurar os textos que nós adicionamos anteriormente. No caso, este app precisa acessar sua câmera e sua galeria. Olha só, no iOS funcionou perfeitamente. Agora no Android, precisamos ir em Android app, src, main, Android manifest e aqui nós teremos de procurar as permissões de câmera. E de read external storage. Se todos estes estiverem aqui, significa que funcionou perfeitamente a instalação das nossas bibliotecas. Agora é só nós rodarmos o Yarn iOS ou Yarn Android novamente. Um detalhe interessante é que no emulador de iOS não dá pra nós abrirmos a câmera, então nós vamos utilizar Um emulador de Android para validarmos essa funcionalidade. E olha só, um detalhe interessante, é nos certificarmos que esse newArchEnabled esteja como true, no caso para nós habilitarmos a nova arquitetura do React Native. Em seguida, vamos rodar o npx export prebuild para nós regerarmos os nossos códigos nativos. Então, vamos dar um start aqui, yarn android. Perfeito, com o nosso aplicativo estartado, nós vamos começar utilizando a câmera. Para isso, vamos voltar aqui dentro de src, sharedhooks, criar um novo hook chamado useCamera.ts. Aqui, o export const useCamera, vai ser uma função normal, vamos simplesmente retornar um objeto vazio, vai ser um custom hook. Já vamos aproveitar, vamos criar um outro custom hook que vamos fazer. Além do useCamera, vamos ter aqui também o useCamera, Gallery. Vamos mudar o nome aqui também. Use Gallery. Perfeito. E a partir da próxima aula vamos dar seguimento com a configuração desses nossos dois Custom Hooks. Pra que no momento em que nós vamos realizar o upload do nosso arquivo, nós abrimos ou a câmera ou a galeria. Ele retornou um erro aqui pra gente falando que os Children possuem a mesma aqui. No caso ficou faltando nós colocarmos aqui no modal de Selection. Então vamos abrir ele aqui. Selection modal. Vamos vir aqui dentro do nosso Touchable Opacity, vamos recuperar o índex aqui do nosso map, índex, colocar aqui, aqui, selection item, enviando o nosso índex, perfeito, dessa forma ele vai parar de emitir aquele erro pra gente. Bom, e nessa aula foi isso, vamos acabar por aqui, e espero vocês na próxima!