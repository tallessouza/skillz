WEBVTT Na aula passada, nós criamos a requisição HTTP responsável por buscar as nossas Product Categories. Hoje nós vamos renderizá-las em tela. Para isso, vamos vir aqui logo abaixo do text, onde nós colocamos o título da sessão de categoria. Vamos simplesmente colocar uma condicional. To is loading for true. Nós vamos adicionar aqui um componente de text. Carregando categorias. 3 pontinhos. Caso contrário, ou seja, caso não esteja carregando, nós vamos renderizar aqui uma view. Vamos fechá-la. Onde aqui nesta view, vamos colocar um className contendo um marginBottom de 6 e um gap de 3. Perfeito. Aqui dentro nós vamos abrir chaves para executarmos o Javascript. Vamos pegar as nossas productsCategories.map onde nós vamos simplesmente retornar um touchable, opacity, onde aqui dentro vamos por ora colocar simplesmente um text contendo a categoria que está sendo iterada no momento, no caso vamos pegar aqui o name, e vamos pegar também o id para nós utilizarmos como aqui do nosso map, vamos pegar aqui o template string product category id, aqui dentro vamos passar o name, Perfeito, olha só que legal, todas as categorias já sendo renderizadas. Vamos colocar um className aqui no nosso texto, alterando o tamanho dele, com um textBase e um textGray 400. Vamos colocar aqui no nosso Toshiba Opacity também, um className flexRow, que nós vamos precisar, um itemCenter e um py de 2. E, por fim, nós vamos ter de instalar uma lib específica de checkbox do Expo. Então, vamos parar a execução aqui do nosso app. Vamos dar aqui um npx expo install expo-checkbox. Perfeito. Vamos rodar o nosso app aqui novamente, yarn ios. E, enquanto ele vai rodando, nós já vamos importar aqui import from expo-checkbox. Vamos importar aqui a nossa checkbox. Na verdade, não vai precisar de chaves. Checkbox. Vamos pegar nosso componente de checkbox, voltar aqui dentro do nosso botão, acima do nosso text, vamos colocar ela, onde aqui nós vamos simplesmente colocar um color, chamando a nossa variável de colors, purple base por enquanto, e também vamos colocar um class name, de margin-right 3, e rounded-full, Perfeito. Vamos só esperar o nosso app estartar novamente. E olha só que legal, já temos aqui as checkboxes sendo renderizadas lindamente ao lado do nome das categorias que temos disponíveis. E o legal é que como nós colocamos um touchable opacity em volta de todo o nosso componente, independente de nós clicarmos no texto ou na checkbox, A funcionalidade de marcar vai funcionar perfeitamente, isso daqui é algo bem legal. Na próxima aula iremos dar seguimento com o desenvolvimento da nossa camada de ViewModel. Vamos criar uma nova Store onde nós utilizaremos a ViewModel para comunicar com ela, responsável por orquestrar o estado do nosso filtro. Então é isso, espero vocês na próxima aula.