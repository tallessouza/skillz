WEBVTT Na aula de hoje, instalaremos o Native Wind aqui na nossa aplicação, basicamente o que utilizaremos para criar as estilizações aqui das nossas telas, assim como foi no módulo passado, tanto pela praticidade, até pelo fato de não ter a necessidade de ficar escrevendo vários arquivos .ts para colocarmos a nossa estilização. Para isso, vamos abrir aqui o nosso navegador e navegar e procurar pela documentação do Native Wind. Show de bola, vamos vir aqui na parte de instalação. Vamos selecionar o Yarn. Agora aqui um ponto muito importante, nós não vamos utilizar a versão do React Native Reanimated, nem do Safe Area Context, que eles estão colocando aqui, por serem versões mais antigas. Vamos simplesmente copiar todo esse código daqui, toda essa primeira linha. Vamos voltar aqui no nosso VSCode e vamos dar um Ctrl V. Vamos realizar aqui o ajuste das versões que nós vamos utilizar, o React Native Reanimated. Vamos utilizar a versão 4.1. E do react-native-safe-area-context vamos utilizar a 5.6.0. Show de bola! O native-wind vamos deixar do jeito que está ali, agora vamos aguardar a instalação das nossas bibliotecas. Perfeito, vamos voltar lá na documentação. Vamos rodar este outro comando que ele instala o tailwind e o preacher-plugin aqui do tailwind. Vamos simplesmente dar um ctrl-v. Aqui nós não vamos fazer nenhum tipo de ajuste na versão. E show de bola, agora vamos continuar seguindo o passo a passo aqui da configuração, vamos ter de criar um arquivo de tailwind.config.js, então vamos copiar aqui ele por completo e voltar aqui no nosso VSCode, vamos criar aqui um tailwind.config.js, vamos dar um Ctrl V no código que nós copiamos, Vamos para o próximo passo, vamos criar um arquivo de CSS global na nossa aplicação, vamos copiar aqui toda essa estilização, voltar aqui, e dentro de src, vamos criar aqui styles, e dentro de styles, vamos criar um arquivinho de global.css, vamos dar um ctrl-v, vamos para o próximo passo aqui da nossa configuração, vamos adicionar o babel presets, então vamos copiar, voltar para o nosso aplicativo e criar um arquivo de babel.config.js. Vamos simplesmente dar um ctrl-v aqui nele. Próximo passo, vamos ter de criar aqui também um arquivo de configuração do metro, o metro.config.js. Então vamos copiar ele e voltar aqui pra nossa aplicação, o metro.config.js. Lembrando que aqui vamos ter de ajustar o apontamento do nosso global css, na verdade vamos colocar aqui o src barra styles barra global css. Lembrando que isso daqui é de suma importância, ele tá apontado pro local certo, no caso pro local onde nós colocamos o global css, pra que ele possa identificar. Próximo passo, nós vamos aqui dentro do nosso layout.tsx. Lembrando que esse layout é a base da nossa aplicação, todas as rotas ficarão aqui dentro do layout, então quando nós tivermos de configurar algum tipo de provider global, normalmente será feito aqui dentro. Vamos dar um import. Vamos simplesmente colocar aqui um //styles//global.css. Estamos basicamente importando o global.css e pelo fato de toda a nossa aplicação estar dentro do nosso root layout, todo o nosso aplicativo terá acesso a essa estilização. Vamos pular esta etapa 6 e vamos para a configuração do TypeScript. Vamos ter de criar um nativewind-env.d.ts para ele poder adicionar a tipagem ali do TypeScript nos nossos componentes React Native, o class name, Vamos voltar no nosso app. Na raiz, vamos criar um nativewind-env.d.ts e aqui dentro vamos colocar um Ctrl-V. Lembrando que é de suma importância todos esses arquivos de configuração que nós criamos estarem devidamente configurados. No caso, o twind.config.js. Será onde nós colocaremos as configurações, como as cores, as fontes e os tamanhos base do nosso aplicativo, onde nós utilizaremos de forma global em todos os locais. Uma outra biblioteca que nós instalaremos aqui vai ser o React Native Worklets. Então vamos colocar aqui o yarn add react-native. O React Native Worklets permite a gente executar código JavaScript em threads paralelas no React Native. Dessa forma, a gente melhora significativamente a performance em animações e que nós iremos utilizar futuramente aqui na nossa aplicação. Então, vai ser bacana a gente já deixar configurado aqui no início. E um detalhe de suma importância é que aqui no tailwindconfig.js nós vamos ter de mudar o conteúdo aqui da nossa aplicação. Se nós repararmos, ele está apontando para o app .tsx que é onde normalmente fica a raiz do nosso aplicativo como nós estamos utilizando o ExpoRouter. Lembra que nós tivemos de alterar até na nossa package.json a entrada do nosso app. Vamos ter de mudar aqui também. Em suma, vamos ter de substituir esse app.tsx por src barra 2 asterísticas, barra asterístico, ou seja, absolutamente tudo que tiver dentro de src, ponto com a extensão, vamos colocar aqui a extensão js, jsx, ts, tsx. Esse segundo parâmetro aqui, nós podemos simplesmente apagar ele, já que dessa forma nós já vamos englobar absolutamente tudo que nós temos. E perfeito! Dessa forma, se nós chegarmos aqui em qualquer um dos nossos componentes, podemos ver a propriedade de ClassName já disponível. E se nós virmos aqui dentro da String, podemos ver todas as nossas propriedades CSS já disponível para serem utilizadas. Agora, vamos estartar o nosso aplicativo. Lembrando que é importante nós rodarmos a Prebuild antes de nós inicializarmos de fato. Ou simplesmente rodarmos aqui o nosso Yarn iOS, por exemplo, ou Yarn Android. Dessa forma, vamos atualizar os nossos códigos nativos com todas as bibliotecas novas que nós colocamos. Agora vamos aguardar um pouquinho aqui, até que o nosso emulador inicialize. E olha só, parece que ele retornou um erro pra gente no momento da build. Vamos entender o que ele está querendo dizer. E parece que ficou faltando nós instalarmos o arroba expo metro runtime. Então vamos parar aqui a execução do nosso aplicativo. Vamos instalar ele, npx expo install. E após a instalação, vamos estartar o nosso app novamente. Nosso app foi buildado com sucesso, agora se nós chegarmos aqui na nossa tela de index e alterarmos a cor do nosso texto, por exemplo, colocarmos aqui um class name com um text-head na tonalidade de 300, ele já vai se repletir. Olha só que maravilha, que massa! O último passo que nós vamos fazer para finalizar a configuração do nosso Native Wind é simplesmente configurar as nossas cores, as cores bases aqui do nosso aplicativo. Para isso eu irei disponibilizar para vocês um objeto de cores. Em suma, vamos vir aqui em styles, vamos criar um arquivo novo chamado colors.ts E vocês dão um ctrl-v nesse objeto daqui, que eu irei disponibilizar na descrição da aula. Agora vamos voltar aqui no nosso tailwind.config.js, vamos importar logo aqui abaixo, com um cost igual a require, vamos passar aqui parênteses, o caminho para o nosso arquivo, ponto barra src, barra styles, barra colors. Show de bola, e aqui dentro vamos pegar o nosso colors. Vamos vir aqui dentro do nosso Tim, dentro do objeto de Extents, vamos simplesmente passar o nosso objeto de cores. Agora, só pelo fato de nós colocarmos o nosso colors dentro de Extents, toda a nossa aplicação já tem a referência de todas as cores que vamos utilizar dentro do Native Indie. Então, se chegarmos na nossa tela de index.tsx, por exemplo, e substituirmos esse red300 por purple, Base, que é uma tonalidade que temos no nosso objeto de cores, ele já vai se refletir no aplicativo. Olha só que massa! Por fim, vamos voltar no nosso tailwind.config.js e vocês vão colar aí agora o objeto de font-size e de height. que são os tamanhos das fontes, e um rate customizado que vamos utilizar dentro do nosso aplicativo, dentro de extends mesmo, ou seja, agora toda vez que nós utilizarmos o sm, base, lg, xl, ou até mesmo o rate button, vamos saber exatamente o que é, já que configuramos tudo globalmente dentro do nosso tailwind.config, isso daqui é muito massa, isso daqui é maravilhoso, porque dessa forma a gente consegue centralizar toda a parte da estilização do nosso aplicativo, E, a partir da próxima aula, vamos configurar o React Query com o Axios, essa duplinha que nós utilizaremos para realizar o caixamento das requisições HTTP, o que vai deixar a nossa aplicação muito massa. Então, é isso! Espero vocês lá!