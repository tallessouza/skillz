WEBVTT Na aula de hoje, nós criaremos a mutation responsável por encapsular o nosso método de register que criamos na aula passada. Como nós temos o React Query instalados aqui no nosso projeto, nada mais justo do que utilizarmos ao máximo os recursos dessa biblioteca maravilhosa. Então, para isso, vamos entender primeiro o que é uma mutation. O React Query possui as próprias queries que englobam as requisições HTTP Colocando o cash nelas. Nem sempre nós temos a necessidade de bater no back-end para buscar algum dado específico. Muitos desses dados não se alteram com frequência, como é no nosso caso um aplicativo de marketplace. Os itens que vão estar listados na loja não tem um nome ou uma foto de perfil ou até mesmo um valor. alterado o tempo todo, então uma ferramenta como React Crew proporciona pra gente a possibilidade de armazenarmos a resposta da nossa API em um cache interno da nossa aplicação e no próximo momento em que nós realizarmos a mesma requisição com os mesmos parâmetros, ao invés de nós realizarmos a requisição batendo no nosso Mac, nós pegamos da memória interna do dispositivo, o que diminui muito a tela de loading e melhora bastante a experiência de usuário. Porém, o React Query proporciona pra gente um recurso chamado mutation. Diferente das queries, ou seja, como o próprio nome já diz, mutations são para modificar dados. No geral, funcionalidades como login e registro, nós não podemos aplicar esse conceito de cacheamento, até porque nós, de fato, precisamos buscar o dado mais fresco possível ou até mesmo atualizar o dado que está lá no nosso back-end, seja com uma sessão ou seja para cadastrar um usuário. Então, nós não podemos fazer isso utilizando uma query. O próprio React Crew já proporciona pra gente esse recurso específico, visando esses casos. Então, vamos pôr a mão no código. Aqui dentro de shared, vamos criar uma pasta nova chamada queries. E dentro de queries, vamos criar uma pasta nova chamada alph. Todas as nossas queries e mutations nós vamos colocar aqui dentro de alph. Vamos criar aqui um arquivo novo chamado useRegister.mutation. .ts como vocês podem ver pela nomenclatura uma mutation ou até mesmo uma query nada mais vai ser do que um custom hook que nós iremos criar então aqui vamos criar um export const useRegisterMutation aqui é simplesmente uma arrow function dentro do nosso useRegisterMutation vamos criar uma constante const mutation igual a useMutation do próprio react-query aqui ele espera receber Basicamente um objeto como parâmetro e dentro desses parâmetros nós temos o mutation function. Essa mutation function é no fim do dia qual a função que será executada quando nós chamarmos a nossa mutation. Ou seja, como o nosso objetivo é realizar o cadastro do usuário quando nós a chamarmos, vamos passar como parâmetro o nosso método de register. Então aqui dá um import asterisco as alf-service-from, colocar ponto, ponto, barra, ponto, ponto, barra, services, alf-services, vamos pegar o nosso alf-service.register, E show de bola! Agora, um detalhe interessante é que se nós passarmos dessa forma, o nosso método de register vai retornar um erro pra gente, até porque ele espera receber como parâmetro um objeto contendo todos os dados do usuário. Então, o que nós precisamos fazer é abrir uma arrow function dentro da nossa mutation e aqui dentro vamos colocar a userData. passando como parâmetro, simplesmente o nosso register HTTP params. Se nós passarmos o mouse aqui em cima do nosso método de register, ele já vai retornar pra gente o que ele espera receber. Então, vamos voltar e passar o nosso register HTTP params pra dentro do nosso userData e enviar o userData pra dentro do método de register. Próximo parâmetro que a nossa mutation espera receber é o de onSuccess. É simplesmente uma função de callback que será executada caso a nossa requisição HTTP tenha acontecido com sucesso, no caso, os dados do usuário. Ou seja, caso o usuário tenha sido registrado com sucesso, essa função irá executar. No momento, vamos simplesmente colocar um console.log. Vamos recuperar aqui no nosso onSuccess. Vocês podem ver que o próprio versicode já mostra pra gente aqui que aqui de dentro ele vai retornar pra gente um data, e esse data é basicamente o registerHttpResponse, que é simplesmente o retorno dessa função que nós configuramos aqui no mutation function. Então, podemos pegar aqui o nosso response. Vamos dar um log nele. Foi de bola. Uma outra coisa interessante que o useMutation dá pra gente é a propriedade de onError, ou seja, da mesma forma que nós temos uma callback pra caso aconteça um sucesso, temos uma callback que dispara um erro, então aqui vamos colocar um erro, a gente dá um console.log no erro. Agora, aqui abaixo da nossa mutation, ainda dentro do nosso custom hook, vamos simplesmente dar um return em mutation. Dessa forma, nós criamos uma camada que irá abstrair a comunicação entre a nossa ViewModel e a nossa camada de service. Em suma, nós estamos utilizando a mutation do próprio ReactQuery, porque ele sozinho já gerencia pra gente o loading e as notativas de erro e de sucesso automaticamente, o que adianta bastante a vida quando nós formos realizar a integração lá na nossa camada de ViewModel. Quando nós utilizarmos, vocês irão ver como vai ser simples nós realizarmos a nossa requisição HTTP, ele já vai retornar pra gente um loading bonitinho. E, com isso, nós temos parte da nossa camada de model já configurada, ou seja, toda a parte responsável por realizar a comunicação com o nosso back-end, nossa camada de service. E, a partir da próxima aula, nós vamos pegar esse nosso UserRegisterMutation e vamos chamar ele dentro do nosso UserRegister.ViewModel. Vocês vão ver como vai ser simples a integração com esta abordagem. Então, é isso. Espero vocês na próxima aula.