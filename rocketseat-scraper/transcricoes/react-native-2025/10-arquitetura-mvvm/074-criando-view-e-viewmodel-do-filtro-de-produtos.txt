WEBVTT Na aula de hoje, iremos dar início à construção do formulário de filtro do nosso app. Para isso, aqui dentro de componentes, vamos simplesmente criar um novo componente chamado filter. Onde dentro aqui de filter, vamos criar um filter.view.tsx. Vamos aplicar o MVVM dentro do nosso componente de filter. Vamos colocar aqui export cost filter. Vamos simplesmente retornar uma view vazia por hora. Vamos criar aqui também o nosso useFilter.viewModel.ts, aqui vai ser um custom hook, export const useFilterViewModel, onde vamos simplesmente retornar um objeto por hora. Maravilha. Por fim, vamos só criar um index. Aqui dentro de filter também, index.tsx, export, const, filter, onde aqui vai ser, dar um return no nosso filterView, filterView, só alterar o nome aqui, do nosso componente de View para filterView, voltar aqui, importar ele, Vamos adicionar agora a tipagem dentro do nosso FilterView, falar que ele é um FunctionalComponent que espera receber um return type, menor que, maior que, type of useFilterViewModel Maravilha, dessa forma tudo que nós colocarmos aqui dentro do nosso useFilter já vai aparecer automaticamente na nossa camada de view. Agora simplesmente vamos vir aqui, dar um const no nosso index, props igual a useFilterViewModel, vamos pegar as nossas props e rodar um spread operator aqui dentro. Então vamos repassar tudo da nossa camada de ViewModel pra nossa camada de View. Dessa forma, abstraindo totalmente a parte lógica, a parte funcional e a parte do UI do nosso app. E pra finalizar, nós vamos pegar este nosso filter, voltar lá no nosso componente de Search Input. onde nós estávamos passando simplesmente esse teste, esse text anteriormente, vamos simplesmente importar ele aqui desta forma, agora quando nós clicarmos aqui, ele já vai aparecer automaticamente. Como nós não temos nada dentro do nosso componente de filtro, de fato, o nosso bottom sheet não vai ser exibido. Esta foi mais uma aula de configuração para nós separarmos melhor passo a passo da construção do nosso filtro. A partir da próxima nós vamos começar a mexer na UI. parte visual. É isso. Essa aula vai ficando por aqui. Espero vocês na próxima!