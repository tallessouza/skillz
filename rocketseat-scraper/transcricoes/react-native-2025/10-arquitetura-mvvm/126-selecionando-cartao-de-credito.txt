WEBVTT Nesta aula nós vamos criar a funcionalidade de selecionar qual cartão de crédito iremos utilizar para realizar a nossa compra, e para isso nós vamos fazer uma coisa antes. Vamos aqui dentro de SRC, View Models, aqui dentro do nosso cart em Components, vamos transformar o nosso Cart Footer, que atualmente é simplesmente um componente, padrão React, e aplicar aqui a arquitetura MVVM nele, para que dessa forma nós possamos englobar toda a parte lógica para que dessa forma a gente consiga isolar ali as questões das funcionalidades também da UI então pra isso aqui no cart-footer vamos criar o nosso cart-footer.view.tsx e aqui um funcional component padrão cart-footer.view.tsx onde aqui vamos simplesmente dar um return, voltar no nosso index e pegar todo o conteúdo que possuímos aqui dentro deste return e repassar para dentro deste return da camada de view. É interessante nós pegarmos estes casos de transformar um componente que até então era padrão, que nós não tínhamos a necessidade de aplicar essa arquitetura MVVM nele, e nós aplicarmos para nós vermos que durante o desenvolvimento algumas ideias mudam, um componente que anteriormente era simples agora requer uma carga maior de complexidade no qual para nós seguimos aqui o padrão do nosso projeto vai ser necessário nós fazemos aqui a adequação ele a arquitetura que estamos usando mas isso faz parte do processo no fim do dia desenvolver software é uma coisa muito inconstante muitas variáveis ocorrem ali no meio do desenvolvimento vamos importar aqui todas as variáveis e componentes que precisamos, aqui o FlatList, nosso AppButton, ActivityIndicator e... perfeito. Agora precisamos receber os demais aqui das propriedades. Para isso, antes de tudo, vamos criar aqui a nossa camada de ViewModel, então useCartFooter.ViewModel.ts onde aqui nós vamos criar uma constante, xPostConst useCartFooter.ViewModel.ts PutterViewModel, onde aqui vamos simplesmente dar um return em um objeto vazio, por enquanto. Vamos simplesmente pegar esta nossa camada de ViewModel, voltar na nossa camada de View, e como nós já fazemos sempre, vamos chipar aqui, falando que é um FunctionalComponent, que espera receber um return type, do type of type of useCartFooterViewModel, onde aqui nós já podemos até colocar chaves para desestruturarmos. E aqui no index vamos remover todos estes componentes que nós possuímos, colocar aqui simplesmente o CartFooterView, onde aqui nós vamos simplesmente pegar, criar uma constante nova para ViewModel, como é de costume, onde nós vamos armazenar o use cartFooterViewModel e vamos rodar um Spread Operator enviando todo o retorno da nossa ViewModel para a nossa camada de View. Agora o que nós precisamos fazer também é enviar todas essas outras propriedades que nós temos aqui recebendo no nosso cartFooter, no caso o openCartBottomSheet, o nosso creditCard e o nosso loadingCreditCard também, nós vamos ter de passar aqui pra dentro do nosso cartFooter. Pra isso vai ser bem simples, vamos voltar no nosso cart, Outro view. Fora aqui do fechamento do menor que e do maior que, aqui da tag, vamos colocar um e, vamos abrir chaves e vamos simplesmente colocar um export aqui, export, na nossa interface. Dessa forma, vamos pegar e colocar o eCardFooterParams desta maneira. Agora aqui a gente já espera receber o Credit Cards, o nosso Loading Credit Cards e também o nosso Open Card Bottom Sheet. Uma coisa também interessante é que nós poderíamos simplesmente pegar esta lógica, esta nossa query que retorna os dados que estão sendo renderizados aqui na nossa lista e repassar tudo aqui pra dentro do nosso useCardViewModel, mas vamos seguir desta maneira, como nós estamos fazendo aqui por enquanto. Agora aqui ele tá reclamando que espera que nós passemos todas essas variáveis pra lá, no caso o creditCard, o loadingCreditCard e também o openCardBottomSheet desta maneira aqui, perfeita, aqui na camada de View ele está reclamando porque ele não tem o total, no caso esse total, vamos recuperar ele aqui na nossa camada de ViewModel, const, vamos pegar o total do nosso UseCardStore, o tal dessa maneira vamos voltar aqui na view e vamos passar desta maneira aqui agora ele tá reclamando só do credit card item vamos só importar ele aqui dessa maneira e perfeito temos aqui o nosso componente funcionando perfeitamente agora aqui dentro da nossa camada de view modo nós vamos criar um estado onde nós vamos controlar qual cartão de crédito que nós selecionamos no momento Para isso, simplesmente vamos criar uma nova constante igual a useState, onde neste useState o valor inicial dele vai começar como nulo e nós vamos tipar antes da abertura dos parênteses que ele será um nulo ou um credit card. Onde aqui nós vamos colocar o nome, caso selectedCreditCard e o setSelectedCreditCard, da nossa função de dispatch. Vamos pegar estas duas variáveis e repassar aqui para dentro do return, porque a gente consiga recuperar aqui na nossa camada de eview, aqui desta maneira. Hoje nós vamos pegar estas duas variáveis também e vamos repassar para dentro aqui do nosso componente de credit card item. Vou só mudar o nome dele aqui, acabei colocando o nome dele errado. No caso, credit. CardItem, vamos mudar aqui também, perfeito. Onde dentro deste componente vamos colocar algumas propriedades que ele espera receber, como por exemplo o IsSelected, que será simplesmente um valor boleano, um true ou um falso. E vou mostrar aqui também o nosso setSelectedCreditCard, que será uma função que retornará void e ela irá enviar como parâmetro um creditCard, que será um creditCard, desta maneira. Vamos simplesmente desestruturar aqui desta forma. Agora vamos voltar aqui na nossa camada de view. Aqui ele está reclamando um erro pra gente, mas é porque há o nome do componente. Vamos remover também essas importações que não estão mais sendo utilizadas. Na nossa camada de view, vamos retornar, enviar aqui primeiramente o nosso isSelected. Aqui nós vamos fazer o seguinte, nós já estamos recebendo o cartão de crédito selecionado como parâmetro, vamos fazer uma validação. A nossa Flat List já está retornando o item que está sendo iterado no momento, ou seja, o cartão de crédito que ele está iterando, nós vamos pegá-lo. Vamos até fazer o seguinte, vamos colocar um alias aqui no nosso parâmetro, no caso o creditCard no singular, já que aqui estamos lidando com um loop, no caso a flatlist vai percorrer o loop em cima da listagem e ele irá retornar um creditCard por vez, onde aqui nós vamos validar se o creditCard.id do item que está a ser literado é igual ao selected creditcard.id com esta comparação nós já retornamos um boleano para o nosso componente onde nós iremos colocar algumas condicionais se ele for selecionado ou não e aqui por fim vamos colocar também o nosso setSelectedCreditCard é literalmente a mesma função que estamos recebendo aqui como parâmetro aqui no item é só nós mudarmos o nome da variável para o alias que nós atribuímos a ela no momento e perfeito Agora vamos voltar no nosso credit card item, vamos pegar essas nossas duas variáveis que estamos recebendo como parâmetro e vamos vir na nossa camada de credit card item view, onde fora aqui do menor que e do maior que, do nosso return type, vamos colocar um e para nós estendermos uma tipagem a mais, enviando para lá simplesmente o isSelected e o setSelectedCreditCard, vamos só importar aqui a nossa interface, Onde nós vamos simplesmente recuperar aqui as nossas propriedades, desta maneira. Onde vamos pegar o nosso setSelectedCreditCard e passar no evento de onPress do nosso componente. Desta forma, abrir uma callback, já que nós vamos ter de enviar um parâmetro pra lá. No caso, simplesmente será o nosso creditCard que estamos recebendo no momento. Maravilha, agora vamos simplesmente pegar este nosso isSelected, vamos só fazer uma condicional. Vamos remover aqui esta string comum que nós possuímos, essas aspas. Colocar aqui uma template string, já que aqui nós vamos fazer uma condicional. Vamos remover simplesmente esse border-grays na tonalidade de 100, que nós possuímos aqui mockado, e vamos colocar uma condicional. Vamos abrir aqui o cifrão chaves. Se o isSelected for true, A nossa borda será um border, opa, ficou faltando a gente abrir aqui as aspas, será um border purple base. Caso contrário, teremos aqui o nosso border na tonalidade de grey 100, igual tínhamos antes. Vamos testar pra ver se está funcionando, nós clicamos aqui agora no nosso item e ele retornou um erro pra gente, ele falou que não é possível, que setSelectedCreditCardIsNotAFunctionalIsUndefined, isso porque ficou faltando a gente passar os nossos dois parâmetros aqui no índex da nossa chamada de credit card item Viu? A gente só vem aqui e passa o ISSELECTED dessa forma. E o nosso SET SELECTED CREDIT CARD, ele já deixa, ele já muda automaticamente a estilização do nosso cartão de crédito selecionado, isso daqui é muito legal. Agora, nós já podemos, de fato, implementar a funcionalidade de confirmar compra utilizando a mutation que nós já criamos em algumas aulas anteriores. Mas vamos deixar isso para a próxima aula. Vamos sinalizar esta por aqui para não ficar muito longa. Então, espero vocês lá.