WEBVTT Bom, já que nós construímos o formulário responsável por criar o nosso cartão de crédito, nada mais justo do que construirmos a camada de services para que possamos comunicar com o nosso back-end para criarmos o nosso cartão de crédito. E para isso nós vamos fazer o seguinte, vamos ir dentro de src, shared, services, vamos criar um novo service aqui, no caso o creditcard.service.ts, onde aqui vamos ter uma função, vai ser o export, GetCreditCards vai ser uma função assíncrona. Agora o que vamos fazer é voltar lá no nosso navegador, na nossa documentação, caso local host. Podemos ver que temos um grupo específico com duas rotas aqui, uma de busca e uma de criação de cartão de crédito. Vamos simplesmente pegar essa de barra credit cards. Então, para isso, vamos vir aqui, const, vamos desestruturar, o await marketplace-api-client.get passando simplesmente como parâmetro uma string contendo o endpoint da nossa rota. Agora vamos entender o que a documentação fala que esta rota retorna pra gente, no caso é simplesmente um array contendo vários objetos de cartão de crédito, contendo o userId, o titularName, o número e todos os outros demais dados do cartão. Vamos simplesmente copiar este nosso objeto, voltar no nosso projeto aqui dentro de interfaces HTTP, vamos criar aqui o credit, card.ts onde vamos criar as interfaces de resposta export interface get credit cards response vamos simplesmente retornar vai ser simplesmente o objeto que copiamos vamos só fazer aqui a alteração dos dados de fato onde tem zero vai ser number Onde temos string, vamos só remover as aspas duplas. Desta forma aqui. E onde temos a data, vamos simplesmente colocar um objeto de date. Desta forma. Pra eu selecionar vários itens assim de uma vez, é só selecionar o que eu quero, por exemplo, o ponto e vírgula, e ir apertando o ctrl-d. Desta forma eu consigo manipular vários itens ao mesmo tempo. Agora vamos pegar o nosso get, opa, vamos só corrigir o nome aqui. Get credit cards response. Voltar aqui no nosso Credit Card Service e colocar antes da invocação da função de get, antes da abertura aqui, dos parênteses, vamos simplesmente abrir o menor que e o maior que e passar a nossa interface, onde aqui simplesmente vamos recuperar o data. Ah, um detalhe que aqui na verdade não é só um objeto, ele retorna um array de objetos, então vamos passar desta forma aqui, onde simplesmente daremos um return no data. Agora vamos criar a query para nós consumirmos esta nossa rota. Para isso vamos aqui dentro de queries, vamos criar um novo grupo de credit cards, onde aqui vamos criar simplesmente o use get credit cards.query.ts que vai ser simplesmente export const useGetCreditCards vai ser simplesmente um custom hook onde iremos criar uma função que teremos uma constante de query aqui dentro que vai ser o useQuery do ReactQuery onde aqui vamos passar um objeto como parâmetro contendo a query function que será esta função que nós acabamos de criar de getCreditCards desta forma Só importar ela. Vamos passar aqui também o QueryKey, que vai ser simplesmente Credit Cards. Opa, que ele espera receber um Array dessa forma. E vamos colocar um StayAllTime aqui de mil milissegundos, vezes a quantidade de segundos, opa, vezes 60 segundos vezes a quantidade de minutos, vamos colocar 5 minutos aqui por enquanto. E agora simplesmente vamos pegar esta nossa variável e dar um return na query. Dessa forma temos a nossa query de consulta de cartões de créditos, onde nós iremos utilizar na camada de ViewModel. Novamente, toda esta parte que nós fizemos aqui é referente à camada de model da nossa aplicação, a parte de lógica, de requisição, HTTP, onde nós iremos simplesmente consumir lá na nossa camada de ViewModel. Na próxima aula, vamos criar a requisição, o Imutation responsável por criar o nosso cartão de crédito. Então, vamos finalizar por aqui e eu espero vocês lá.