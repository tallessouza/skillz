WEBVTT Fala, Dev! Na aula de hoje daremos início à configuração das nossas rotas públicas e privadas aqui no nosso app. Seguindo o mesmo conceito do nosso último aplicativo, teremos aqui rotas que serão de público acesso, no caso, a rota de login e a rota de cadastro de usuário. E teremos as rotas privadas que, obrigatoriamente, precisaremos de um token de autenticação para podermos acessá-la. E nessa aula eu irei mostrar para vocês um método bem interessante de nós fazermos isso. Até então, nós temos apenas duas rotas criadas, no caso a index, que seria a principal, e temos aqui a de login, e temos simplesmente a stack sendo passada dentro aqui do nosso host layout. O simples fato de nós passarmos a stack dessa forma, ele já identifica todos os arquivos que já foram criados com este padrão de export default e já joga aqui dentro. Porém, no momento, nós não vamos fazer dessa forma, nós vamos abrir e fechar o stack para que dessa forma a gente consiga colocar várias rotas e ter o controle de qual nome deve ser para nós podemos navegar até ela e algumas outras propriedades e logo de cara podemos simplesmente pegar aqui novamente o nosso stack.screen Vamos auto fechar ela aqui dentro e show de bola. Aqui dentro vamos colocar a nossa primeira rota, que no caso seria a nossa rota de login. Perfeito. O próximo passo que nós iremos fazer é, aqui dentro de app, vamos criar uma rota nova, uma rota de registrar.tsx e aqui vamos criar um funcional componente padrão, um export. Function. Registrar. Vamos simplesmente retornar uma view. Portado react-native e um text como texto de registro. Agora dentro de layout vamos colocar novamente uma outra stack.screen Vamos auto-fechar ela e name igual a register. Show de bola. Um detalhe interessante que podemos fazer aqui na nossa aplicação é um ajuste aqui no nosso app, ou seja, na raiz do nosso aplicativo. Vamos simplesmente apagar todos os componentes que temos aqui dentro e vamos colocar um import. export-router. Vamos importar aqui o redirect. E esse redirect vamos colocar ele aqui dentro com o href. passando uma string barra login. Dessa forma, nós garantimos que sempre que o nosso usuário abrir o nosso aplicativo, ele vai passar pelo index, no caso, pelo app e ele vai automaticamente ser redirecionado para a tela de login. Futuramente, transformaremos essa nossa pasta de index em uma tela de loading, ou seja, sempre que o nosso usuário abrir o aplicativo, iremos fazer uma requisição HTTP para o nosso servidor, no caso, o nosso back-end Para que dessa forma possamos validar se a sessão do usuário está válida ou não, no caso o token de autorização dele. E caso de fato os dados estejam corretos, vamos redirecionar para as rotas privadas. Caso contrário, vamos seguir desta forma, mandando o usuário para a tela de login. Agora, olha só que legal como foi simples nós configurarmos as rotas aqui. Comparado com o React Navigation, onde nós tínhamos de criar várias coisas, teríamos de criar aqui a tipagem das propriedades, teríamos de configurar o nosso Provider, além disso teríamos de diversas outras configurações, aqui não, aqui simplesmente só de criar os arquivos dentro do nosso app já é o suficiente. E com isso nós podemos simplesmente vir e realizar a navegação, aqui em Logins, colocarmos um Touchable Opacity, com um texto, enviando o usuário para a tela de registro. Vamos colocar no evento de onPress uma arrow function. O router, vamos importar do próprio expo-router.push. enviando ele para a barra registro e clicarmos no botão de registro e olha só, ele não conseguiu encontrar, vamos ver o que está acontecendo aqui. Show! Ficou faltando nós colocarmos o default aqui na exportação do nosso componente. Lembrando que sempre que nós criarmos uma tela nova, precisamos colocar com o export default para o export router compreender que este arquivo se trata de fato de uma tela nova. Agora, se nós tentarmos navegar, vamos conseguir ir para a nossa tela de registro e aqui em cima ele já vai aparecer para a gente o botãozinho de volta. Para nós configurarmos a exibição desse reader aqui, podemos fazer individualmente em cada uma dessas stacks, ou podemos simplesmente fazer globalmente. No caso, dentro da propriedade de Screen Options, temos aqui o Reader Show, que vamos deixar como falso. Dessa forma, vamos esconder aqueles readers do próprio ExpoRouter que ele cria pra gente. Vamos simplesmente colocar um class name, Aqui na nossa tela de login, com um flex1 pra ele ocupar 100% da tela, um itemCenter pra centralizar e um ingestifyCenter. Podemos ver aqui o nosso componente, vamos copiar aqui a nossa classe e passar aqui também pro de registro pra gente já poder ver. E olha só, perfeito, agora não temos mais aquele header feio do próprio ExpoRouter. Dá pra customizar o... O próprio ExpoRouter tem muitos recursos que nós podemos utilizar na própria biblioteca do ExpoRouter para criarmos readers customizados, porém nós iremos criar os nossos próprios aqui no aplicativo. Nós utilizaremos também a navegação com base nas tabs, então vamos conseguir explorar bastante essa parte de configuração de interface do próprio ExpoRouter. Espero que vocês tenham gostado da aula. Na próxima aula, nós iremos configurar as rotas privadas do nosso aplicativo. Iremos utilizar a Tab Navigation, ou seja, aquela navegação com os botões internos. Vai ficar muito massa, iremos aprender bastante coisa sobre isso. Iremos aprofundar bastante referente ao uso do ExpoRouter neste módulo. Então, espero que vocês tenham gostado e espero vocês na próxima aula.