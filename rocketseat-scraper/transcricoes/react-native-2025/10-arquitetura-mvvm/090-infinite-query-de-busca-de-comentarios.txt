WEBVTT Nesta aula vamos construir a Infinity Query responsável por consumir os comentários aqui do nosso produto, para isso vamos vir aqui dentro de src, shared queries product, vamos criar aqui um use get product comments infinity.query.ts, aqui vamos colocar um export const use getComments.infinityQuery vai ser igual a, que vai ser um custom hook, então vamos simplesmente retornar um objeto vazio por ora, aqui vamos criar um constQuery igual a useInfinityQuery, só corrigir o nome aqui do nosso componente, infinityQuery, infinity com e no final, perfeito. onde aqui nós vamos passar o nosso query function, que será o getProductComments, aqui vamos ter parênteses, onde ele espera receber alguns parâmetros, logo de cara vamos desestruturar a callback aqui do nosso query function, onde nós vamos pegar o nosso pageParam, que vai ser igual a 1, onde aqui nós vamos já passar o productId também, onde nós vamos receber aqui como parâmetro, caso ProductIdNumber, vamos passar ele aqui desta forma. Aqui em pagination, vamos simplesmente passar o page20 e o page de 1 também. No caso, o pageParam. A próxima propriedade que vamos passar aqui é o nosso QueryKey, que vai ser um array contendo o ProductComments, mandando o ProductId, a próxima propriedade obrigatória é o getNextPageParam. A gente aqui vamos ter uma função, opa, vamos ter uma função aqui dentro, lastPage, a gente vai fazer uma validação if lastPage.page for menor que lastPage.totalPages vamos simplesmente dar um return em lastPage e incrementar mais um caso contrário vamos simplesmente retornar um undefined e a próxima vai ser o initialPageParam que vai ser número 1 maravilha, aqui só faltou a gente colocar o .page aqui dentro Dessa forma a gente garante, com esta função, de que caso nós ainda tenhamos algo a ser retornado lá no back-end, ou seja, supondo que nós estejamos na página 2 e no total de páginas que nós temos seja um total de 7 páginas, nós vamos realizar este request e ir incrementando o nosso page em 1 até que nós cheguemos no valor final na última página que nós temos disponível lá no nosso back-end. Agora vamos simplesmente pegar aqui essa nossa query. e retornar aqui no lugar deste objeto. Uma última coisa que vamos fazer aqui na nossa query, é simplesmente realizar o flatMap que temos aqui na nossa resposta, da mesma forma que fizemos na nossa infinity query para buscar os produtos, no caso aqui, utilizando o flatMap, vamos voltar, simplesmente criar uma constante const comments igual a Vamos pegar o nosso query.data.pages.flatMap. Vamos rodar um loop em cima dela, retornar aqui a nossa página atual. Vamos retornar o page.data ou simplesmente colocar um array aqui desta forma. Lembrando que a nossa query retorna pra gente simplesmente um array. contendo um objeto, e dentro desse objeto nós possuímos o data, que de fato é onde está os nossos comentários, ele vai retornar basicamente desta forma aqui pra gente, tudo que nós estamos fazendo é como se nós estivéssemos rodando um Spread Operator neste data no ArrayPy, então dessa forma, ao invés de nós termos um Array de objeto com data, nós temos simplesmente um Array contendo os comentários, no caso, todo o conteúdo dentro desses Arrays iriam ser espalhados dentro desta variável aqui de comments, Simplesmente retornar aqui desta forma, vamos rodar um Spread Operator em Query e retornar os comments desta maneira. Desta forma reaproveitamos todas as propriedades e todo o aparato que o próprio React Query oferece pra gente utilizando o hook de useInfinityQuery e retornamos também a listagem de comentários já formatada para ser utilizada lá no nosso front. Um outro detalhe muito interessante que nós precisamos fazer é que da mesma forma que nós fizemos aqui no nosso useGet, no nosso useProductInfinityQuery, onde nós utilizamos o build e o rl para formatar a foto dos produtos, nós também vamos fazer isso daqui com a foto de perfil dos usuários. Para isso, vamos simplesmente rodar um map aqui dentro. Vamos só retornar um object literal, que vai ser comment, comment no singular, rodando o spread operator, no comentário. Aqui onde nós temos o user, vamos simplesmente retornar, novamente, um Spread Operator em comment.user. Porém, a propriedade de avatar, nós vamos fazer uma pequena alteração, nós vamos pegar a URL, vamos chamar o nosso helper de buildImageURL e mandar como parâmetro o comment.avatar.user .avatar.url. Dessa forma nós garantimos que a URL do usuário sempre será mostrada para a gente tanto no Android quanto no iOS. Lembrando que o Android é uma máquina virtual que não possui acesso direto ao local host da nossa máquina. Para isso ele possui uma outra porta, um outro local onde é exposta a nossa local host, o nosso local host, com o local host lá da máquina virtual do Android Studio. Então a gente precisa fazer toda esta validação quando nós estivermos em modo de Se nós não estivermos em modo de desenvolvimento e utilizando um endpoint que já está aberto na web, não tem problema. Simplesmente nós retornamos à original URL desta forma, garantindo assim que vamos sempre ter a foto de perfil do usuário disponível aqui no nosso app. Com isso, nós finalizamos a nossa Infinite Query por hora. Na próxima aula, nós vamos consumi-la lá na nossa camada de View Model da tela de detalhes de produto para que, posteriormente, nós possamos criar o componente de renderização dos comentários. A aula vai ficando por aqui, espero vocês na próxima.