WEBVTT Nesta aula vamos implementar as funcionalidades de UpdateQuantity e de GetItemCount. Para isso vamos voltar lá na nossa CartStore, vamos criar aqui os métodos de UpdateProductQuantity. Aqui nós vamos ter uma função onde nós iremos receber como parâmetro a ProductList. no caso vai ser a cartProduct, um array de cartProduct o productId, que vai ser um number e por fim a quantidade de fato vai ser um number, perfeito uma coisa, como aqui nós temos três parâmetros, vamos transformar isso daqui em um objeto e vamos tipar aqui um vamos realizar a desestruturação aqui do objeto e recuperar productId, productList e quantidade Aqui dessa forma, só o fato da gente chamar essa função, ele já vai recomendar pra gente quais propriedades ele espera receber. Agora, aqui dentro, vamos fazer o seguinte, vamos colocar um if, se a quantidade do item atual for menor ou igual a zero, vamos pegar um return cardService.removeItem, opa! removeProductFromList enviando pra lá nossa ProductList e o nosso ProductId. Ou seja, se a quantidade de item que nós estamos atualizando for menor ou igual a zero, nós vamos simplesmente remover ele, porque ele não vai mais estar lá dentro da nossa lista. Agora, caso contrário, vamos simplesmente dar um return aqui na nossa ProductList. onde simplesmente vamos fazer uma atualização, vamos fazer uma verificação, vamos retornar aqui o ID do produto que está sendo iterado. Se o ID do produto que está sendo iterado for igual ao produto que nós estamos atualizando a quantidade, nós vamos executar simplesmente um return em um Spread Operator no produto, no caso vamos fazer o seguinte, ao invés da gente desestruturar o valor aqui da callback do map, vamos pegar aqui o product Vamos desestruturar aqui dessa forma, pegar o quantity, caso é só colocar aqui em inglês, acabei colocando em português o nome da propriedade, caso é quantity. Perfeito, caso aqui vai ser o product.id, que a gente não está mais desestruturando dessa forma. Caso contrário, vamos simplesmente dar um return no product. o Product que está sendo iterado do momento. Só um detalhe, aproveitando já o embalo destas outras funcionalidades que nós temos aqui, onde nós já estamos retornando o Product e o Total, nós já estamos abstraindo o cálculo aqui para dentro do próprio método, vamos fazer isto aqui também, ao invés de nós simplesmente colocarmos o Return desta forma, vamos atribuir o resultado deste Map em uma constante, constant, uma const Product igual a, vamos criar aqui, const Total, igual ao nosso cartService.calculateTotal onde nós vamos enviar o resultado de products aqui do nosso map. Aqui nós simplesmente pegamos, damos um return enviando aqui tanto o nosso products quanto o total. Próxima função que nós vamos criar é a função de getProductCount, no caso nós já temos aqui uma função responsável por calcular o total do valor que temos no nosso carrinho, agora vamos fazer uma função que busca a quantidade de itens que temos no total, vai ser bem parecido com esse daqui, vai ser um reduce também, a gente vai simplesmente vir aqui, vai ser o getItemCount onde aqui nós vamos ter uma função onde vamos simplesmente receber a ProductList desta forma no caso vai ser um array de ProductCard Onde aqui vamos simplesmente retornar direto o nosso ProductList.reduce, onde aqui, lembrando que o reduce espera receber dois parâmetros, no caso a callback de execução e o valor inicial, no caso aqui vai ser 0. Aqui vamos pegar o acumulador, que seria este 0 daqui, onde nós vamos somar ele com a quantidade do Product. Então vamos pegar o nosso product, ou melhor, acumulador, mais o product.quantity. Dessa forma vamos percorrer o looping em cima de todos os itens que temos dentro do nosso carrinho e vamos de fato realizar a soma da quantidade que temos aqui nele. Agora tudo que precisamos fazer é vir aqui dentro da nossa store, dentro da nossa cart store, aqui no update.quantity nós vamos desestruturar aqui. O ProductId é a quantidade que vamos estar atualizando. Dentro do set, vamos remover as chaves e simplesmente repassar o nosso set, chamando o nosso cartService.updateProductQuantity, onde nós vamos simplesmente enviar o ProductId. A ProductList, nós vamos pegar aqui do nosso state, que vem da callback aqui do set, no caso vai ser o state.products. E por fim, a quantidade que estamos recebendo. Agora aqui no getItemCount, vamos simplesmente vir aqui e retornar o nosso cartService.getItemCount, onde nós vamos enviar aqui pra ele o nosso cartProduct. No caso, como aqui nós não vamos fazer nenhum tipo de alteração no nosso estado, não vamos usar o set, vamos utilizar o get que temos aqui em cima na linha 28, que vem aqui da callback do nosso método aqui do create. Vamos simplesmente chamar aqui o get, parênteses, porque ele é uma função, ponto, products. Desta forma aqui. E só um comentário antes de finalizarmos a aula. Toda esta nossa cart-service eu resolvi fazer como um objeto para mostrar para vocês que daria para fazer simplesmente da forma que nós fazemos aqui com os nossos serviços HTTP. Daria até para nós desacoplarmos, criar um HTTP services e um services comum para nós separarmos as nossas requisições HTTP e os services de regra de negócio interno do nosso aplicativo. E também daria para nós utilizarmos uma classe aqui ao invés de um objeto, desta forma aqui. que nós fizemos, com objetos seria até mais proveitoso, poderíamos utilizar outros recursos ali da programação orientada a objetos, como criar métodos privados, por exemplo, e afins. Porém, vamos seguir desta forma pela simplicidade, para vocês verem que dá para nós utilizarmos um objeto como um hub de funções, de funcionalidades que temos aqui na nossa aplicação, para utilizarmos em outros locais aqui no nosso app. A aula vai finalizando por aqui, espero vocês na próxima.