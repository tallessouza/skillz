WEBVTT Hoje daremos início na configuração da camada de móvel aqui da nossa aplicação. Como mostrei para vocês na aula anterior, a camada de móvel será responsável por realizarmos as nossas requisições HTTP, por contermos as nossas regras de negócio, e com isso será necessário nós configurarmos tanto o Axios quanto a nossa camada de service e as nossas queries que iremos utilizar. E para isso, hoje, nós iremos configurar o Axios na nossa aplicação. Aqui dentro da nossa Shared, vamos criar uma pasta nova chamada API. E aqui dentro de API, vamos criar um arquivinho novo chamado de marketplace.ts. Lembrando de parar o processo aqui do nosso aplicativo e rodar o comando yarn add axios para instalarmos a biblioteca. Show! Vamos startar nosso aplicativo aqui novamente. Diferente da forma que nós fizemos no módulo anterior, no app do DTMoney, aqui nós iremos construir uma classe para o nosso API Client. Então vamos colocar aqui um export class MarketPlace. API Client. É interessante nós colocarmos sempre a nomenclatura de qual API Client nós temos. É muito comum que aplicações web e mobile façam requisições e mais de um back-end específico, por isso é importante de nós especificarmos qual API nós estamos conectando aqui. Agora, aqui acima, vamos colocar um import access from access. E aqui dentro da nossa classe, nós vamos ter uma propriedade private chamada instance, onde nós vamos tipar ela com a Axios Instance vamos importar aqui do próprio Axios e teremos aqui também um private isRefreshing onde ele vai ser um valor de false, perfeito. Próximo passo, vamos colocar aqui na nossa função construtora, essa função é simplesmente um método que é chamado sempre que nós instanciarmos a nossa classe, ou seja, toda vez que nós instanciarmos, vamos utilizar ela em algum local, ele irá rodar esta função para construir a nossa classe, como o próprio nome da função já diz, vamos atribuir em this.instance o valor de axios. Vamos abrir parênteses e passar o objeto aqui de base com o base url, simplesmente uma string vazia por ora. Agora, aqui dentro da nossa classe, vamos criar um método chamado de getInstance, onde aqui, simplesmente, nós vamos dar um return em this.instance. Esse método é simplesmente um getter pra nós podermos recuperar a nossa instância fora do contexto da nossa classe. No fim do dia nós vamos criar uma classe para fazer a gestão do nosso API Client pelo simples fato que nós iremos lidar com várias questões aqui. Diferentemente do app anterior, aqui nós iremos tratar com Refresh Token e mais algumas questões bem bacanas. Por mais que dê pra fazer tudo com funções, assim como nós fizemos no módulo passado, Vai ser super interessante, vai agregar bastante. Nós orquestrarmos um API Client do Axios utilizando uma classe. Nós esmolarmos todas as lógicas de interceptos, seja do request ou até da response aqui dentro. Vai ficar muito bacana quando nós finalizarmos. E aqui, abaixo da nossa classe, vamos colocar um export const market place API Client. No caso, aqui é basicamente o nome da nossa classe, porém com o M no singular. Vai ser igual a um new market marketplace client, no caso aqui, a nomenclatura de new nós utilizamos quando nós estamos instanciando a nossa classe, ou seja, a partir do momento em que nós instanciamos a marketplace API, nós rodamos aqui o nosso construtor e futuramente nós criaremos um método de setup interceptors, onde nós vamos colocar aqui dentro, onde nós vamos configurar os interceptos de request e de response, como eu disse anteriormente, No caso, é basicamente para nós adicionarmos token no header das requisições ou até mesmo para nós validarmos se o token é válido ou não. Ou até mesmo o interceptor na resposta de erro para validarmos se o nosso token está válido ou não, mas isso daí deixa para o futuro. O que importa é que agora temos aqui a nossa classe. Por último, vamos ir aqui na nossa classe e vamos chamar o método de getInstance. Dessa forma, nós simplesmente vamos exportar a instância do Axios que está sendo setada dentro da nossa classe e vamos expor para toda a nossa aplicação. Na próxima aula nós iremos criar a camada de serviço de registro. Nós vamos preparar o caminho, ou seja, vamos construir passo a passo a nossa camada de Model, ou seja, desde as nossas requisições HTTP até a nossa camada de Query com as nossas mutations, para que no fim nós possamos utilizar tudo dentro da nossa camada de ViewModel. Dessa forma deixando a nossa aplicação bem robusta e Basicamente, o mesmo padrão que nós vamos seguir aqui pra nossa tela de registro e na nossa tela de login vai ser o que nós vamos seguir pra toda a aplicação. Então, acredito que nós pegarmos e fazermos logo a implementação de toda essa parte de registro. Vai ser interessante pra ficar bem claro os conceitos da arquitetura MVVM antes de nós darmos seguimento com a nossa aplicação. Nós fazemos simplesmente uma integração de ponta a ponta dessa arquitetura para que não haja dúvida e que a gente possa ter um proveito maior com o seguimento das nossas aulas. E é isso, espero que vocês tenham gostado. Como eu disse na próxima aula, vamos configurar a nossa camada de service, onde nós, de fato, realizaremos a comunicação com a nossa rota, com a nossa API. Tudo isso seria englobado dentro da nossa camada de moda, da nossa arquitetura. Então, espero vocês lá!