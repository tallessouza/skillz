WEBVTT Na aula de hoje, nós iremos construir um componente bem interessante para o nosso aplicativo, que será o componente de upload da foto de perfil do nosso usuário. Quando nós clicarmos aqui, será exibido o modal que iremos criar, exibindo as opções de onde o usuário quer buscar a imagem, se é da foto, se é da câmera ou se é da galeria de imagens do dispositivo. Mas antes disso, nós vamos criar um componente de modal global no nosso app. Nós iremos utilizar modais em vários locais diferentes e para isso nós vamos criar um custom hook onde nós iremos chamá-lo. Houver a necessidade de nós utilizarmos algum tipo de modal. Teremos diversas variações, modais de seleção, modais de confirmação, modais de erro. E para isso, vamos começar voltando aqui no nosso app dentro de src.shared dentro da nossa store. Vamos criar uma store específica para o gerenciamento de modal aqui na nossa aplicação. Então, vamos criar aqui o nosso modal store.ts. Aqui dentro vamos seguir a estrutura padrão do sustained, assim como nós fizemos aqui no usuário. Vamos começar criando a interface da estrutura de como ficará a nossa store. Então, vamos colocar aqui interface modal store, onde teremos aqui dentro a propriedade de isOpen, que será um valor booleano, que será responsável por dizer se o nosso modal será exibido ou não. Teremos o content, que será um React Node, ou no, no caso, será o modal, o conteúdo que será renderizado dentro do modal. Teremos também config, que será um objeto, e aqui vamos criar uma interface modal config. Hoje aqui dentro nós teremos animation type, que será opcional. Aqui teremos none, slide, ou fade. No caso, o tipo de animação que queremos no nosso modal, teremos aqui se ele será transparent, um boolean. Também teremos o status bar translucent, também será boolean. Perfeito, vamos shipar aqui agora o nosso config com modal config. Teremos o open, será uma função que retornará um void. Esperará receber como parâmetro o content, no caso aqui o react-node. E também receberíamos a config, que será o modal-config. Também teremos o close, que simplesmente será uma função que retornará um void para nós. Agora, com esta interface, vamos criar a nossa store. export const useModalStore() E vamos pegar o create, importando do próprio sustent. Aqui, antes de nós iniciarmos, vamos atipar ele com a nossa modal store. Aqui dentro vamos colocar uma arrow function, retornando um object literal, ou seja, parênteses chaves, e aqui dentro teremos todos os valores disponíveis. Vamos colocar o initial value, no caso o isOpen, que será falso, por padrão. O content será nulo. E o config teremos aqui o animation type que vai ficar como fade, o padrão, fade. Teremos aqui também o transparent, que ficará true. E por fim teremos o status bar, translucent, que ficará como false. Maravilha. Próximo passo, vamos colocar aqui as nossas funções, nossos métodos, no caso o open, dois pontos, aqui dentro da nossa função. Retornando o set, esse set vai vir de dentro das propriedades da nossa callback que está dentro do create, no caso aqui o set. Esta função que será responsável por atualizar a nossa store de fato. Simplesmente colocando aqui. O open irá receber como parâmetro o content e o config, então vamos simplesmente copiar a estilização aqui de cima. pegar o nosso content e passar aqui dentro, vamos pegar também config e rodar um Spread Operator aqui na nossa config, agora aqui depois do set vamos colocar vírgula, get, no caso será a função responsável por retornar o nosso estado atual da nossa store, vamos fazer o seguinte, antes do Spread Operator no nosso config, vamos colocar aqui um Spread Operator no get, ponto, config, ou seja, vamos simplesmente reaproveitar o que já temos e atualizar com os novos dados que nós estamos mandando através das props de open. E por fim, para ficar parado desse monte de erro, nós vamos colocar a função de close, onde a única responsabilidade dela será simplesmente de setar set, o contents de volta para null e o isOpen para false. E perfeito! Já temos todo o state responsável por exibir ou não o nosso modal. Não vai ficar muito grande, no próximo nós iremos construir o nosso componente genérico. Então é isso, espero vocês lá!