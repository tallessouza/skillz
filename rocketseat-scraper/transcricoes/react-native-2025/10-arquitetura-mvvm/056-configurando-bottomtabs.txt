WEBVTT Na aula de hoje, nós vamos configurar a Tab Navigation aqui na nossa aplicação. Se nós analisarmos o nosso Figma, podemos ver que possuímos uma navegação com base em Modern Tabs, ou seja, que são esses botões que ficam aqui na parte de baixo do nosso aplicativo. E com o ExpoRouter, é muito simples nós criarmos esse tipo de navegação. Isto foi uma das vantagens que ele trouxe pra gente, que é a facilidade de nós configurarmos diversos tipos diferentes de navegação na nossa aplicação, de maneira muito simples. Primeira coisa que precisamos fazer é entender como que será a estrutura de pastas aqui do nosso app. Teremos aqui as nossas rotas privadas, ou seja, as nossas tabs ficarão aqui dentro de private. Tudo que precisaremos fazer é criar um grupo novo de tabs aqui dentro. Onde aqui nós teremos novamente um underline. layout.tsx, como vocês podem ver é bem padrão nós criarmos um arquivo de layout para cada grupo de navegação que nós possuímos, assim como nós possuímos um global, um root layout no nosso app, possuímos um também dentro de public, um dentro de private e aqui um também dentro das nossas tabs. Aqui dentro vamos colocar um export const tabs layout vai ser simplesmente um arrow function na verdade aqui vamos fazer diferente para seguir o padrão vamos colocar simplesmente o export default function tab layout temos de ter o export default para que o próprio export router identifique este arquivo aqui vamos simplesmente colocar um return retornando tabs onde nós vamos importar do próprio ExpoRouter, e aqui dentro, onde a mágica vai acontecer. Primeiro de tudo, vamos logar em um usuário. Eu já tenho uma conta aqui, eu criei algumas aulas passadas. Vou colocar aqui a nossa senha também. Vamos realizar o login, e perfeito! A validação que nós colocamos na aula passada já está sendo feita corretamente. Quando o nosso usuário é logado, nós já somos redirecionados aqui pra nossa tela de Home, no caso pro nosso grupo de rotas privadas. E aqui dentro das nossas tabs, Vamos começar a configuração do layout dos nossos botões, de fato. Pra isso, vamos vir aqui no root layout das rotas privadas, aqui no private layout. Vamos fazer o seguinte, a princípio, vamos simplesmente colocar aqui, abrir e fechar o nosso stack. Aqui dentro pegaremos a stack novamente, ponto screen, onde ela se auto fecha, com o name, sendo parênteses, tabs, que é o nosso novo grupo de rotas que criamos. No próximo passo, vamos simplesmente vir aqui e colocar um OPTIONS com HEADER SHOW como FALSE, por padrão. E perfeito! Primeira coisa que temos de ter em mente é que toda a configuração que teremos referente a Tabs, todas as telas, ficarão aqui dentro deste nosso novo grupo de rotas, o que novamente nós temos de fazer igual nós fizemos com a Stack anteriormente. Lembrando que Stack é um tipo de navegação, que é a navegação em pilhas, que é literalmente essa navegação de uma tela para outra sem nenhum componente explícito de navegação, como por exemplo um Drawer ou uma Tab, igual nós temos agora, é simplesmente podemos colocar em qualquer outro local e realizar o empilhamento das nossas rotas, a navegação delas. Aqui nas Tabs nós vamos abrir e fechar, onde aqui colocaremos algumas opções, Screen Options, com o Header Show A verdade é que fica até redundante, que nós já tínhamos colocado aqui no nosso private layout, mas vamos seguir nosso TabBarStyle. Vamos abrir chaves, vamos colocar aqui um height de 110 e um padding top. de 16. Estamos definindo a estilização das nossas tabs. Teremos uma altura de 110 pixels com um padding, ou seja, um espaçamento interno no topo de 16 pixels. Dentro da nossa tab, nós teremos aqui novamente a tab .screen literalmente seguindo o mesmo padrão que nós temos aqui no caso tendo a stack principal e aqui a stack.screen aqui vai ser a mesma coisa teremos a nossa tabs.screen onde aqui nós teremos name que será home options onde dentro de options colocaremos title que será produtos, ou seja, a nossa tela de home, onde será exibida a listagem de produtos, tab, bar, active, tint color, teremos aqui colors, vamos importar a nossa variável de cores, purple, base, nosso tab, bar, icon, aqui dentro nós colocaremos simplesmente uma função, renderizando um componente de ion, icons, do ExpoVectorIcons, com color, essa cor, nós vamos receber ela como parâmetro desta callback, no caso, se nós desestruturarmos, podemos ver que temos aqui algumas propriedades, no caso aqui color, vamos repassar, que é basicamente a cor que nós temos aqui acima, que nós citamos anteriormente. Próximo passo, vamos colocar name, sendo o store, front, outline, e o size de 25. A próxima propriedade que colocaremos dentro da nossa tab screen será o nosso tab bar label style, onde aqui teremos a font-size de 14 e o margin-top de 4. Perfeito. Próximo passo que nós precisamos fazer é simplesmente pegar esta nossa Home e mover aqui pra dentro das nossas Tabs. No caso, vamos simplesmente pegar a Home e mover aqui pra dentro. Perfeito. Agora ele tá reclamando de erro porque a gente precisa corrigir aqui as importações. Vamos dar um reload e olha só que legal, já temos a nossa tab sendo renderizada perfeitamente, olha só que massa. Se nós voltarmos aqui no Figma podemos ver que já está bem parecida. E seguindo esta mesma configuração da nossa tab, nós vamos criar os botões das outras telas que nós teremos, no caso a tela de pedidos e a tela de carrinho. Para isso é muito simples, é simplesmente nós chegarmos aqui dentro de tabs e criarmos as demais telas, no caso cart.tsx, vamos criar aqui também o orders.tsx, export, const, orders, opa, aqui na verdade tem que ser um export, default, function, orders, vamos simplesmente retornar aqui uma view, com um text, de ORDERS, mesma coisa aqui pra CART, EXPORT DEFAULT, PUNCTION, CART, vamos simplesmente retornar uma view, contendo um text, carrinho, Agora, como vocês podem ver, automaticamente ele já cria as bottom tabs aqui pra gente. No caso, já cria. Conforme nós vamos adicionando as telas na nossa tab, ele já vai configurando automaticamente. Porém, não é isso que a gente quer. Nós vamos querer manter essa estilização que nós criamos anteriormente. Então, vamos simplesmente dar um ctrl-c aqui e alterar o nome para orders, para fazer referência a este arquivo. Vamos mudar aqui também o nome para pedidos. E o nome do nosso icon vai mudar também para Clipboard Outline. A mesma coisa para cart, vamos simplesmente dar um Ctrl C. Alterar o name para referenciar a cart. Mudar o nome aqui da label para carrinho. Por fim, vamos mudar aqui também o name do nosso icon para Cart Outline. E olha só que massa, agora toda vez que nós clicarmos e navegarmos para dentro da nossa tela, a utilização já muda automaticamente pelo fato de nós já termos definido o TabActivityTintColor para o nosso purple base. Como podemos ver, já está até aqui em cima, a nossa navegação já está funcionando perfeitamente. Esta aula vai ficando por aqui, a partir da próxima aula daremos seguimento com a construção da nossa tela de rock. Espero vocês na próxima!