WEBVTT Olá, Devs! Na aula de hoje iremos configurar a nossa camada de service referente ao registro de usuário aqui na nossa aplicação. E vocês devem estar se perguntando por que nós estamos tentando essa parte de configuração e, sendo que se quer, nós iniciamos o desenvolvimento aqui das nossas telas. Acredito que nós fazermos esta abordagem configurando toda a nossa parte do Axios e requisições HTTP, consequentemente, configurando toda a nossa camada de model. da nossa arquitetura MVVM em um momento inicial seja mais interessante, porque desta forma nós vamos realizar a integração de ponta a ponta de todo um fluxo aqui da nossa aplicação, que no caso é o de registro e de login, aplicando a arquitetura MVVM. Então, logo de cara, vamos abrir aqui a nossa API. Lembrando que eu ensinei como clonar e como startar a nossa API lá no início do nosso módulo. Aqui nós vamos rodar o script de dev. Vamos rodar simplesmente o yarn ou o npm run dev. E com isso temos a nossa API rodando na rota 3001. Com isso, vamos voltar aqui no nosso navegador e vamos acessar o localhost 3001-docs. E estamos aqui novamente na documentação da nossa API. Sempre vou ressaltar da importância de aprendermos a ler documentações no nosso dia a dia como desenvolvedor. Uma das coisas mais comuns é nós termos de realizar a integração em APIs, sejam APIs nossas ou APIs de terceiros. Então, sempre bom a gente estar afiado. O primeiro serviço que nós vamos fazer é referente ao registro do usuário, ou seja, temos aqui o verbo HTTP de post, que é o de criação, e temos aqui também o nosso endpoint, que é o de barra alfa, barra registro. E aqui temos o nosso request body, ou seja, o formato que nós vamos enviar do nosso aplicativo para o nosso endpoint será através do body, um objeto simplesmente contendo o nome, o e-mail, avatar, o url, o número de telefone, e a senha do usuário, e logo abaixo nós temos os casos de resposta da nossa API, ou seja, caso nós realizarmos uma requisição HTTP, com todos os dados certinhos, ele vai retornar pra gente um 200. A gente pode simplesmente testar aqui na própria documentação, clicando em Tryout. Vamos utilizar aqui, vamos colocar o nome de Breno, vamos colocar o e-mail de breno, arroba, teste, ponto com, Vamos botar o URL, vamos deixar string mesmo, número de telefone eu vou deixar do jeito que tá e a senha também eu vou deixar do jeito que tá. Quando nós clicamos aqui em register, podemos ver que o meu usuário foi registrado com sucesso. Tudo que nós precisamos fazer é recuperar todos esses dados que nós temos aqui no nosso JSON lá no nosso aplicativo através de um formulário e repassar pra cá. Nós vamos ter esta resposta com os dados do usuário, o refresh token e o token. Então, agora que nós já temos uma ótima noção do que é necessário nós enviarmos para a nossa API e o que ela retorna de fato, vamos voltar lá para o nosso VS Code, para nós podermos seguir com o nosso desenvolvimento. Aqui na camada de service dentro de Shared, vamos criar um arquivo novo chamado auth. .service.ts, ou seja, aqui dentro nós teremos a nossa camada de serviço de autenticação. Em suma, todas as nossas funções perente a todo esse fluxo de autenticação nós criaremos aqui dentro. Próximo passo que nós vamos fazer é criar as interfaces necessárias para... nós realizarmos esta comunicação com o nosso back-end. Então, aqui dentro de shared, vamos criar interfaces. E aqui dentro de interfaces, vamos criar uma nova subcategoria chamada HTTP, que serão as interfaces referentes às nossas chamadas HTTP. Aqui dentro, vamos criar um register.ts e aqui dentro, vamos colocar um export interface register.http E aqui dentro nós vamos pegar todos os dados que precisamos enviar lá para o nosso Mac. No caso, vamos simplesmente copiar o JSON que temos aqui. No nosso navegador, vamos voltar e dar um Ctrl V aqui dentro. Vou só apagar essas chaves que estão excedendo. E perfeito! Aqui dentro, vamos simplesmente substituir tudo o que nós temos por String, ou seja, todos os dados que nós vamos enviar lá para o nosso back-end serão String no fim do dia. Então, podemos simplesmente vir aqui e realizar a substituição de todos esses dados. E agora, vamos criar um export interface. Register HTTP Ou seja, temos aqui agora uma interface que será a resposta da nossa rota de registro. Vamos voltar na nossa documentação. Podemos ver que simplesmente temos um objeto com três propriedades, que no caso seria o nosso token de acesso, o nosso refresh token e os dados do usuário. Um detalhe interessante é que nós podemos desacoplar a interface aqui do nosso usuário. Por exemplo, aqui nós estamos dentro de HTTP. Dentro de interfaces, vamos criar uma nova um novo arquivo chamado de user.ts aqui dentro vamos colocar um export interface user interface vamos dar um ctrl v vamos pegar todos os dados que temos aqui e vincular com tipos no caso o id seria um number e todos os demais são strings então podemos simplesmente selecionar todos e substituir por str8. Vamos pegar agora o nosso user interface, voltar aqui pra dentro do nosso arquivo de register.ts e aqui dentro vamos colocar o user tipado com a user interface. Maravilha! Temos aqui o nosso token, que é simplesmente uma string, e temos aqui também o nosso refresh token, que também é uma string. Todos os nossos dados mapeados aqui dentro da nossa camada de service em authService, vamos criar uma função, simplesmente um export, const register a gente teremos aqui uma arrow function aqui nessa arrow function ela vai ser uma arrow function assincrona até porque nós iremos realizar uma requisição HTTP e aqui dentro nós vamos receber a user data essa user data nada mais é do que a nossa register HTTP params A interface que nós criamos. Ou seja, todos os dados do usuário vão estar aqui dentro. O avatar, o url, e-mail, name, password e phone. O próximo passo que vamos fazer aqui é importar a nossa instância do Axios. Então, vamos colocar aqui import from ponto ponto barra. api-marketplace, vamos importar o nosso marketplace-client, lembrando que é o que começa com o minúsculo, que no caso é a nossa constante que estamos exportando com a instância da classe que nós criamos, vamos pegar o nosso marketplace-api-client e vamos colocar aqui um const, vamos já realizar a desestruturação, await.post Lembrando que o verbo HTTP que nós utilizamos aqui na nossa API, na nossa rota de registro, é esse daqui de POST e o endpoint é esse, barra ALF, barra REGISTER. Então, ele tá reclamando aqui, esperando que a gente passe... o parâmetro pra dentro dele, no caso, o nosso endpoint, que seria o alfa register, e o segundo sendo o nosso body, ou seja, o corpo da nossa requisição, que são basicamente os dados que nós vamos enviar pra nossa API. Vamos passar o nosso user data. Agora podemos simplesmente tipar aqui, direto, no nosso post, o que ele irá retornar pra gente caso a nossa promise, ou seja, caso o nosso código assíncrono seja resolvido com sucesso. No caso, vamos voltar aqui na nossa interface de HTTP register e vamos pegar o nosso register HTTP response e vamos colocar aqui dentro. Cortar ele. E aqui dentro da nossa desestruturação temos o nosso objeto de data que ele já vai estar retornando pra gente o register HTTP response. Dessa forma, vamos simplesmente dar um return no nosso data. Vamos só corrigir aqui o nome da nossa função, que é registrar. Temos aqui a nossa função que irá realizar a requisição HTTP com o nosso back-end, realizando assim o cadastro do usuário. Porém, diferente do que nós fizemos no nosso aplicativo passado, nós não vamos simplesmente chamar o nosso método de registrar dentro de algum componente React. Aqui nós estamos utilizando o react-query e logo, Por consequência, nós vamos englobar todas as nossas requisições HTTP, toda a nossa camada de service com uma mutation ou com uma query. O que nós vamos fazer na próxima aula é simplesmente pegar esse nosso método, essa nossa função de register, criar uma mutation para ele, para nós utilizarmos todo o aparato que o próprio React Query proporciona para a gente, para nós podermos utilizar ao máximo essa biblioteca maravilhosa. Espero que vocês tenham gostado da aula. É isso, espero vocês na próxima.