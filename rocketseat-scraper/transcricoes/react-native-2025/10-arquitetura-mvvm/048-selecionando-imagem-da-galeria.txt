WEBVTT Na aula passada nós criamos o nosso custom hook responsável por abrir a câmera do nosso dispositivo e dentro dele nós delegamos toda a responsabilidade tanto de solicitar a permissão do acesso da câmera para o usuário quanto de abrir a própria. Na aula de hoje nós daremos seguimento com as nossas implementações e iremos criar um custom hook responsável por acessar a nossa galeria. Primeiro passo, vamos só adicionar um L a mais aqui no nosso use gallery. Em seguida, vamos fazer uma estrutura muito parecida com a que nós fizemos no nosso UseCamera. Um detalhe interessante é que nós podemos utilizar uma interface que a própria ExpoImagePicker disponibiliza para nós. No caso, se nós virmos aqui no nosso LaunchCameraAsync, podemos ver que ele tem esse ImagePickerOptions. Vamos voltar lá no nosso UseGallery. Aqui dentro, vamos chipar. Os parâmetros do nosso CustomHook, no caso aqui será um objeto, já vamos fazer a desestruturação, vamos utilizar o nosso ImagePickerOptions, e aqui dentro vamos recuperar as mesmas propriedades que nós estamos utilizando no nosso UseCamera, no caso o Aspect, Quality, AllowEdit e o Asif. Perfeito. O próximo passo que nós vamos fazer é criar um loading da mesma forma que nós fizemos no nosso useCamera, um useState. Põeremos aqui o loading, o setLoading. O valor inicial será false. E aqui dentro nós teremos duas funções seguindo o mesmo princípio, a mesma estrutura do nosso useCamera. Uma função responsável por requisitar as permissões e a outra responsável por abrir a câmera. Então já vamos criar elas aqui. Um const, request, gallery, Permission igual a, vamos utilizar um useScalback. Aqui dentro vamos colocar função, uma rede dependência como segundo parâmetro e aqui dentro um async. Lembrando que é importante nesses casos nós utilizarmos o useCallback, até porque como isso daqui se trata de um custom hook, que será reutilizado em diversos outros componentes, cada vez que o componente que utilizar o custom hook for re-renderizado sem o useCallback, o React irá recalcular toda esta função, ou seja, ele não irá recriar, recalcular esta função. A mesma coisa serve aqui para o nosso useCamera. E aqui dentro vamos simplesmente colocar um try, um catch, Vou colocar aqui um error. E aqui dentro vamos importar um import imagePickerFromExpoImagePicker. Vamos só colocar aqui um asterisco, s. Ou seja, estamos importando absolutamente tudo que temos dentro da nossa biblioteca e aplicando um alias pra ele, pra que dessa forma fique mais fácil de nós acessarmos o conteúdo que tem lá dentro. Vamos pegar o nosso image picker dentro do nosso try, vamos realizar a desestruturação do nosso await image picker ponto REQUEST MEDIA LIBRARY PERMISSION ASYNC Lembrando que é importante nós utilizarmos o REQUEST MEDIA LIBRARY PERMISSION ASYNC diferente do nosso USE CAMERA, que seria o REQUEST CAMERA PERMISSION ASYNC. Ficar atento nesta parte. Vamos pegar aqui na desestruturação o status e aqui vamos seguir literalmente o mesmo passo a passo que nós temos. Nós podemos simplesmente vir aqui e copiar toda esta lógica que nós já temos do nosso USE CAMERA. Vamos dar um CTRL V. Em suma, se o nosso Current Status for diferente de Granted, nós iremos exibir este Toast portando do próprio Toastify React Native. Vamos só mudar a mensagem. Precisamos da permissão para acessar suas fotos. Aqui dentro do Catch. Vamos simplesmente colocar um toast também com um return de false. Maravilha! Próximo passo, nós vamos criar a nossa função de OpenGallery. const OpenGallery igual a, novamente, um useCallback. E aqui dentro teremos uma arrow function com uma rede de dependência vazia e transformar essa função em uma função assíncrona. Aqui dentro nós vamos fazer o seguinte, colocar um try catch. E aqui no final nós vamos colocar um finally. Aqui acima do trycat vamos colocar o set loading para true. No finally o mesmo set loading para false. E aqui dentro do try nós vamos fazer o seguinte const hasPermission igual a await crashedGalleryPermission vai retornar um boolean e aqui vamos colocar um if hasPermission vamos simplesmente dar um return nulo ou seja, quando nós executarmos a nossa função vamos chamar essa outra função que nós criamos aqui em cima e se o usuário negar a permissão nós vamos simplesmente retornar um nulo caso contrário nós vamos seguir com a execução da nossa função vamos colocar aqui um const result igual a await imagePicker.launch ImageLibreAsync. E aqui dentro nós vamos passar todas as propriedades que nós temos. Um detalhe interessante que nós podemos fazer, agora como nós temos certeza que as propriedades que iremos receber do nosso UseGallery será a nossa ImagePickerOptions. Podemos simplesmente abstrair isso daqui tudo em uma única variável, por exemplo, PickerOptions. E vamos pegar simplesmente esse PickerOptions que estamos recebendo e passar aqui pra dentro do nosso LaunchImageLibreAsync. Perfeito. Vamos simplesmente fazer a mesma coisa do nosso useCamera. Primeiro, vamos simplesmente vir aqui e remover esse nosso useCameraOption que nós tínhamos colocado aqui anteriormente. Vamos simplesmente passar todo o nosso objeto de imagePickerOptions. Vamos só fazer aqui a importação correta dele. Importar igual nós temos ali em cima. Show. Para não precisar pegar como se fosse um objeto. Vamos mudar aqui também o nome dos nossos parâmetros, abstrair tudo para dentro de um Picker options. Pegar ele e passar aqui pra dentro do nosso launch camera async. Show. Vamos voltar agora aqui no nosso use gallery. Aqui dentro do nosso result, vamos fazer o seguinte. Vamos fazer uma validação, um if. Vamos validar se a nossa requisição foi cancelada ou não. Se a requisição para abrir a biblioteca de câmeras foi cancelada. Vamos pegar aqui o nosso result.consented. Se ela não foi cancelada e nós possuímos um result.assets e o nosso result.assets.length for maior do que 0, nós simplesmente vamos dar aqui um return no nosso result.assets no índice 0.uri Ou seja, vamos pegar o resultado da foto que nós selecionamos e vamos pegar o caminho que ele está armazenado no nosso dispositivo para que dessa forma possamos retornar para o nosso OpenGallery. Vamos colocar aqui embaixo também um return num. Agora, aqui dentro do cat, vamos colocar aqui simplesmente um return null também. E é importante nós colocarmos esse return null para manter a consistência da tipagem da nossa função. Se nós passarmos o mouse em cima do nosso Open Gallery, podemos ver que agora ele irá retornar uma promessa contendo uma string ou um nulo. Agora, supondo que nós removemos o nulo que temos aqui abaixo da nossa verificação, abaixo do nosso if, ele poderá enviar três tipos diferentes, ele poderá retornar uma string, um null ou um undefined, ou seja, teríamos uma inconsistência nos nossos dados e não é isso que a gente quer. Vou até fazer o seguinte, aqui depois dos nossos parênteses nós vamos colocar os dois pontos promise, string ou null Podemos ver que ele tá retornando até um erro. O erro diz que o tipo de retorno não inclui um undefined, ou seja, ele já tá falando pra gente que tá faltando colocar algum retorno aqui dentro da nossa função. Perfeito. Próximo passo, vamos simplesmente pegar os toasts que temos aqui dentro do nosso useCamera. Vamos seguir o mesmo exemplo dentro do cat. eu ao solicitar permissão de câmera, vamos mudar aqui erro ao selecionar a foto e aqui dentro do nosso if, vamos colocar um toast de sucesso, success de foto selecionada com sucesso vamos pegar agora a nossa função de open gallery e passar pra dentro do nosso return para que nós possamos utilizar nos nossos componentes vamos pegar também aqui o nosso loading Agora vamos voltar no nosso useRegister.viewModel. Vamos fazer da mesma forma que fizemos no nosso useCamera. Vamos simplesmente colocar aqui um const igual ao useGallery. Vamos passar um objeto vazio por enquanto. Aqui dentro vamos pegar a nossa função de openGallery e substituir o alert que nós temos no onPress do nosso showSelection. No caso, temos aqui o botão que é responsável por abrir a nossa galeria, o onPress dele. Nós iremos executar a função que nós temos. E agora quando nós vemos aqui no nosso aplicativo e clicamos no nosso botão Galeria, podemos ver que a galeria é aberta com sucesso, olha só que legal! Vou abrir aqui o emulador de iOS, no emulador de iOS ele possui algumas fotos por padrão, então vai ser bem legal para mostrar para vocês. Com o emulador de iOS estartado, se nós clicarmos aqui no nosso botão de upload, clicarmos aqui em galeria, podemos ver que ele já está abrindo a galeria de fotos do dispositivo. Agora se nós selecionarmos algum desses aqui, olha só que legal, foto selecionada com sucesso, que massa! Sinal que a nossa integração já está funcionando. Vamos só fazer o seguinte, vamos colocar um console.log aqui dentro da nossa URI. No caso, na verdade, vamos colocar aqui no nosso Open Gallery. Vamos englobar ela com uma função anônima, vamos colocar aqui um const imageUri igual a await OpenGallery, só transformar essa função, é uma função assíncrona. Agora vamos pegar o nosso imageUri e dar um console.log nela. Vamos ver se vai estar tudo ok. Quando nós clicarmos aqui novamente, Gallery, selecionarmos qualquer uma dessas imagens e olha só que legal, já temos acesso ao caminho da nossa imagem dentro do nosso dispositivo. A gente na próxima aula vamos fazer mais alguns ajustes para melhorar a experiência de usuário aqui na nossa aplicação. Então é isso, espero vocês na próxima.