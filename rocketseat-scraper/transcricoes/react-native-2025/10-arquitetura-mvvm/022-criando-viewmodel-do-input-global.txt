WEBVTT Hoje nós criaremos a camada de ViewModel do nosso componente genérico de AppButton. A camada onde ficará toda a parte lógica ali do nosso componente. Onde nós colocaremos os nossos estados e repassaremos para dentro do AppInputVariance para que dessa forma o Variance possa aplicar a utilização com base no momento atual do nosso input. E para começar vamos criar aqui dentro do nosso AppInput um useAppInputViewModel.ts aqui é simplesmente um funcional component padrão export host useAppInputViewModel.ts Aqui vamos simplesmente retornar um objeto vazio por hora. Próximo passo, vamos começar criando os estados que teremos dentro do nosso componente. No caso, teremos aqui um useState, onde nós iremos armazenar o valor de showPassword e setShowPassword. Outra variável que iremos criar aqui dentro é a da referência do nosso input, cost, input, half, que será simplesmente o useHalf, onde vamos, onde vai começar com o valor NULL e vamos tipar aqui com o textInput do React Native, a tipagem dele. O próximo estado que teremos aqui é o cost isFoxed, set isFoxed, vai ser o useState, Também vai começar como false. Perfeito. O próximo passo que vamos fazer aqui é adicionar algumas propriedades que vamos ter de receber dentro da nossa useAppInputViewModel para que nós possamos processar alguns dados. Vamos criar aqui a interface App Input ViewModelProps E dentro dessas propriedades vamos ter aqui o isError Vai ser opcional, um e um boleano. Vamos ter aqui também o isDisabled, que também será opcional e um boleano. Vamos ter também o error, que será simplesmente uma string. Vamos ter aqui também o secureTextEntry, que será um valor boleano. Vamos ter aqui uma função de onFocus, Será opcional, essa função vai retornar pra gente simplesmente um void. E aqui nós vamos receber um event. Será um focus event. Vamos importar do próprio react-native. Da mesma forma que vamos ter o focus, também vamos ter o unblur. Será opcional. E assim como o outro, vamos simplesmente copiar aqui. Vai ser o blur event. Importar do próprio react-native. Vamos ter também o nosso método de mask. Será uma máscara que criaremos para o nosso texto em alguns locais da nossa aplicação. Simplesmente vai ser uma função. Vai retornar pra gente uma string. Ou um NULL. E ela vai receber como parâmetro um text. No caso é uma string. Vamos ter aqui também o onChangeText. Será opcional. E assim como ali em cima ele vai receber o text. Que é uma string. E ele retorna pra gente o String ou o NULL. O fim aqui dentro vamos receber o VARY que é o valor do nosso input, que será simplesmente uma string. Perfeito. Vamos pegar toda essa tipagem, vamos colocar aqui dentro. Vamos colocar que ele vai receber um objeto do AppInputViewModelProps. Vamos agora simplesmente importar tudo que nós temos aqui dentro do nosso componente, e a partir disso vamos começar a criar alguns métodos que vão ser bem úteis pra gente dentro do nosso input, como por exemplo o const, Handle, password, logo. Vai ser uma função que simplesmente irá alternar entre mostrar o texto ou não, entre esconder ele, o modo do input de senha. E vamos colocar aqui o setShowPassword. Vamos abrir uma callback aqui dentro. Lembrando que quando nós abrimos uma callback dentro das nossas funções de dispatch, de estado, ele retorna pra gente o valor atual, ou melhor, o valor antigo antes dele ser atualizado. Então, teremos aqui o prevValue. Vamos inverter ele. Então, quando nós clicarmos aqui e este valor estiver como FALSE, nós vamos negar o FALSE e ele vai se tornar TRUE. A próxima função que nós vamos criar aqui será a de HANDLEWRAPPERPRESS CONST HANDLEWRAPPERPRESS E aqui dentro vamos simplesmente pegar o nosso inputRef.current?focus Toda vez que essa função for chamada, o nosso input será focado e consequentemente abrirá o nosso teclado O próximo método que vamos colocar aqui é o handleFocus Este método será chamado toda vez que nós focarmos o nosso input, ou seja, quando nós clicarmos aqui, nós iremos alterar esse nosso estado com setIsFocused para true. E também nós vamos executar o nosso onFocus, que estamos recebendo como parâmetro aqui da nossa ViewModel. Como ele pode ser opcional, nós vamos simplesmente colocar aqui um ponto de interrogação, ponto. abrir parênteses e agora essa nossa função de handleFocus espera receber como parâmetro este evento assim assim como o oneFocus que está vindo dos parâmetros então vamos simplesmente pegar ele aqui e vamos configurar a nossa função para receber este event e repassar o nosso event pra dentro do nosso onFocus que estamos recebendo aqui como props. E da mesma forma que fizemos o handleFocus, vamos fazer também com o nosso handleBlur. No caso, o handleBlur vai ser chamado quando nós removermos o foco do nosso input, ou seja, quando nós estivermos digitando e clicarmos fora, por exemplo, ou clicarmos em outro local. ele irá executar esta função, então aqui não será o focusEvent, será o blurEvent, e aqui ao invés do setEfocus para true, ele vai para false, e aqui ao invés de nós chamarmos o onFocus, vamos chamar o onBlur. Uma última função que nós iremos criar dentro da nossa camada de useAppInputViewModel será a de getIconColor, const getIconColor, vai ser igual a Uma função, e perfeito. Nós não vamos colocar a estilização do nosso input, especificamente a cor dele dentro do nosso input variants, até porque ele não está preparado para receber a cor do input com base no native wind css, então vamos optar por fazer desta maneira, utilizando uma função, e aqui vai ser literalmente um if que nós vamos colocar. Vamos validar aqui, por exemplo, o if isFoxed, por exemplo, vamos retornar aqui um color, nosso objeto de colors que configuramos lá no início do nosso projeto, vamos abrir colchetes aqui e colocar o purple base, ou seja, caso esteja focado, a cor do nosso ícone será o roxo principal que nós temos aqui no nosso aplicativo. O próximo que nós teremos aqui é o if is, erro, vamos dar um return colors, abrir colchetes, colors.danger, ou seja, o vermelho que é a nossa cor de erro. O próximo é o if ou seja, se o value existir, return colorsPurpleBase, até porque quando nós digitarmos algo no nosso input e sairmos do foco dele, o nosso ícone continuará roxo, então por isso que nós temos esta variável aqui, mesmo se nós sairmos do foco, nós ainda cairemos aqui dentro dessa validação, e por fim, caso nenhum desses casos aqui sejam satisfeitos, vamos simplesmente colocar aqui um return em colorsPurpleBase ponto gray na tonalidade 200. Ou seja, caso o nosso ícone não esteja focado, não esteja com erro e nem esteja com o valor preenchido, ele simplesmente manterá na cor original dele, que deve ser de 200. Agora, tudo o que precisamos fazer é pegar todas essas nossas variáveis e repassar pra dentro do nosso return, pra que dessa forma nós possamos ter acesso aqui dentro da nossa camada de view. Então, vamos pegar aqui o nosso getIconColor, vamos repassar também o nosso handleBlue, Vamos só mudar aqui que eu coloquei handleBlue, na verdade é handleBlue, com R no final, handleBlue, show de bola, o handleFocus também, vamos repassar também o handleWrapperPress, nosso handlePasswordToggle, e por fim o nosso showPassword. Agora tudo que precisamos fazer é vir aqui dentro do nosso componente, colocarmos aqui um const, Vamos desestruturar o nosso useAppInputViewModel. E olha só, ele já espera... E olha só, ele já retorna um erro pra gente, até porque ele espera receber um objeto contendo todas aquelas propriedades que nós configuramos. Se nós abrirmos uma chave aqui, ele já vai reclamar pra gente, esperando receber essas propriedades. Então, vamos fazer o seguinte. Algumas dessas propriedades nós vamos desestruturar do próprio appInput, como, por exemplo, o próprio value que nós temos. Vamos desestruturar aqui também o isError. Esse isError, que no caso seria uma das propriedades herdadas lá do nosso appInputVariantProps, que no caso aqui nós teríamos o isDisabled, isError e o isFoxed. Outra propriedade que vamos recuperar é o secureEntryText, onde vamos colocar o valor default como false mesmo. Próxima propriedade vai ser o... OnBlue e o OnFocus. Lembrando que todas essas propriedades que nós estamos pegando aqui, algumas delas são herdadas do próprio TextInputProps, ou seja, são as propriedades nativas do React Native do nosso componente de TextInputs, como nós estamos estendendo as interfaces que já existem, nós estamos simplesmente herdando todas elas. E algumas outras são desta AppInputVariantProps que nós temos aqui, que também iremos utilizar bastante. Então, voltando aqui, vamos seguir com a nossa desestruturação. OneChangeText também precisamos passar pra lá. O nosso mask. Uma outra propriedade que vamos ter de obrigatoriamente receber dentro do nosso app input props é a de error, que será simplesmente a mensagem de erro que exibiremos no nosso helper text, que ficará abaixo do nosso input, aquela mensagenzinha de erro vermelha que temos lá no nosso Figma, se nós repararmos aqui no nosso style guide, vamos receber dinamicamente ela, para que dessa forma a gente consiga reproduzir diversos erros diferentes aqui. Então, vamos simplesmente colocar aqui o error sendo opcional, e sendo também uma string, Vamos pegar aqui e desestruturar ele também. O erro ficou de bola. Vamos pegar agora e repassar todas essas propriedades pra dentro, caso o erro que ele tá retornando aqui um errinho pra gente, porque dentro do nosso useAppViewModel ele é obrigatório, mas na verdade ele poderá ser undefined também. Todas as propriedades que temos aqui dentro poderão ser undefined, então já fica aí o adentro. Vamos voltar aqui, vamos repassar o nosso onBlue, o nosso onFocus, Vamos passar também o isError. O isError vai ser um caso interessante. Nós vamos simplesmente pegar o nosso erro aqui e fazer da seguinte forma. Então, caso exista um erro, nós saberemos que o erro, de fato, tem algo dentro dele. Vamos repassar aqui também o nosso método de mask. Ele tá retornando um erro aqui porque o mask é opcional, espera receber um value string e lá na nossa useAppViewModel ele espera vir mask, string ou null. Olha só que interessante, aqui ele vai retornar um void e o string aqui nós colocamos null. Então vamos fazer a alteração, tanto do onChangeText quanto do mask, é um string ou void que ele vai retornar pra gente. Próximo é o onChangeText que nós já estamos desestruturando aqui. O próximo é o isDisabled, faltou nós desestruturarmos aqui em cima, isDisabled, o secureEntry, textEntry e por fim o value aqui do nosso componente. Perfeito! E aqui dentro do nosso useAppInputViewModel nós podemos recuperar todos aqueles métodos que nós temos lá dentro e agora podemos pegar e repassar alguma dessas variáveis pra dentro do nosso AppInputVariants, porém pra essa aula não ficar muito grande, eu irei fazer isso a partir da próxima aula, onde nós vamos implementar as variações aqui dentro do nosso InputVariants e vamos ver de fato o nosso input alterar a estilização com base no estado que nós temos dentro da nossa ViewModel. É isso, espero que vocês tenham gostado da aula, foi uma aula um pouco mais chatinha, um pouco mais de configuração, mas a partir da próxima nós iremos de fato colocar isso aqui pra valer. É isso, espero vocês lá.