WEBVTT Nas aulas passadas, nós desenvolvemos o nosso input genérico do nosso aplicativo, onde nós iremos utilizar literalmente em todos os locais que houverem a necessidade de utilizar o input, seja em um formulário ou seja em um input isolado do nosso app, e hoje nós vamos criar uma extensão deste nosso componente. Como nós estamos utilizando o React Hook Form dentro do nosso aplicativo para criar os nossos formulários, nós temos a necessidade de englobar todo o nosso componente dentro do nosso controller. Então, se nós fôssemos atualmente montar a nossa tela de registro, nós teríamos de fazer da seguinte forma. Primeiro de tudo, teríamos de recuperar o control que nós estamos recebendo como parâmetros da nossa camada de registerView que está vindo diretamente da nossa camada de ViewModel que é literalmente o control que o próprio useForm retorna para nós e dessa forma nós teríamos de englobar o nosso AppInput, eu vou até remover eles aqui para não ficar estourando o erro pra gente, teríamos que chamar este componente de controller e aqui dentro ele espera receber algumas propriedades no caso ele espera receber por exemplo o control que seria o próprio control que estamos recebendo como parâmetro o name esta propriedade é para nós podemos referenciar qual propriedade do nosso formulário este input irá se referenciar no caso nós podemos ver todas as opções que ele já retorna pra gente aqui no caso vamos colocar simplesmente e-mail e a última propriedade obrigatória que ele espera receber é esse de render que é simplesmente uma função onde aqui dentro nós temos de renderizar qual será o nosso input de fato Então nós teríamos desta forma aqui, e com isso, dentro desta callback de renders, nós passarmos o mouse em cima, o próprio VS Code já mostra que dentro destes parênteses, dentro desta callback, ele retorna pra gente alguns parâmetros, como por exemplo o field, o field state, o form state, e aqui de dentro, se nós desestruturarmos, nós vamos pegar o field, com o onChange, por exemplo, com o onBlue, E com o value, dessa forma, nós repassaríamos pra dentro do nosso app input, no caso, o onChangeText, passaríamos o onChange, o onBlue, nós repassaríamos também o onBlue e o value da mesma forma, O valor do nosso input seria o valor de e-mail que estaria dentro do nosso formulário. No caso, o e-mail aqui seria esse teste 123 que nós criamos anteriormente. Então, se nós repassarmos ele aqui e entrarmos na nossa tela de registro, podemos ver que ele já vai estar preenchido. No caso, como o nosso campo é um Secure Entry, ele vai renderizar desta forma. Porém, isso não é nem um pouco interessante, porque imagine que nós teremos vários campos na nossa tela de registro e na nossa tela de login, e se nós tivermos de reescrever este controller toda vez que formos criar um formulário utilizando o hook form e utilizando também o nosso app input, vai ser um código repetitivo, um código chato e um código que segue um determinado padrão. Se nós analisarmos com calma, ele espera receber sempre um control, um name, um render. Então, o que nós vamos fazer é criar um AppInputControl. E a partir deste AppInputControl, como eu disse no início da aula, ele será uma extensão do nosso AppInput. Então, aqui dentro de components, nós vamos criar um AppInputControl. Vamos colocar controller, acho que vai ficar mais condizente com o controller do próprio hook form E aqui dentro vamos criar um index.tsx Perfeito! Aqui dentro vamos criar um functional component padrão export const app.input.controller Aqui, vamos simplesmente retornar o controller do próprio ReactHookForm, por enquanto. Perfeito. Aqui dentro, antes de nós começarmos a codar, de fato, teremos de entender que como ele se tornará um componente genérico, nós teremos de passar algumas propriedades para ele. Da mesma forma que nós temos o nosso AppInput, onde nós definimos nas aulas anteriores as propriedades que nós deveremos passar para ele, no caso nós estendemos dos tipos do próprio TextInput e do AppInputVariance para que dessa forma nós herdássemos todas as propriedades já pré-existentes dentro do TextInputProps nós vamos fazer algo muito parecido aqui no nosso AppInputController mas ao invés de nós herdarmos os tipos do TextInput vamos fazer o seguinte interface AppInputControllerProps Vamos abrir um menor que e um maior que, e aqui dentro nós vamos colocar um generics, ou seja, teremos uma tipagem genérica dentro do nosso componente, onde essa tipagem genérica irá extender extends, ou seja, ela irá herdar todas as propriedades de field. e esse fieldValue sendo importado diretamente do próprio ReactHookForm. Ou seja, nós teremos um tipo genérico e esse tipo genérico irá herdar todas as propriedades que temos dentro desta propriedade de fieldValues. No caso, agora logo após desse maior que, vamos colocar novamente um outro extends, ou seja, teremos dois extends nessa nossa interface e aqui nós vamos estender o omit do próprio TypeScript, ou seja, nós vamos omitir algumas propriedades, o que isso significa. Aqui dentro do menor que e do maior que, nós vamos colocar o app, app, input, props, herdados diretamente do nosso app input, ou seja, estamos reaproveitando esta interface que nós criamos nas aulas anteriores, dentro do nosso app input, e aqui dentro nós vamos omitir algumas propriedades. Nelas, nós vamos omitir o value, Vamos omitir também o onChange, também vamos omitir o error, perfeito. E após isso nós vamos abrir as nossas chaves para nós colocarmos o restante das nossas propriedades. O que isto daqui quer dizer? É muito simples, esta propriedade de omit do TypeScript faz com que nós reaproveitemos uma interface já existente, porém, removendo algumas propriedades específicas, no caso, aqui nós estamos omitindo o value, o onChangeText e o error, até porque nós não vamos utilizar o onChangeText diretamente do próprio appInputProps e sim nós vamos utilizar o onChange do próprio fieldValue. Se nós não fizéssemos esta omissão quando nós fôssemos utilizar o nosso appInputController, nós obrigatoriamente precisaríamos passar essas três propriedades, porém não vai fazer sentido quando nós montarmos o nosso componente de fato, até porque nós iremos pegar todas essas propriedades de dentro do nosso controller lá do React Hook Form da mesma forma que nós pegamos aqui, nós não vamos repassar diretamente o nosso onChange, diretamente do appInputController, nós vamos pegar diretamente da propriedade de render que nós temos aqui, Vai fazer muito mais sentido quando nós finalizarmos a montagem. Aqui dentro nós vamos colocar o control, que o nosso AppInputControllerProps vai receber, que será simplesmente um control. do próprio React Hook Form, ele já possui uma interface específica de control. Vamos abrir o menor que e o maior que e passar aqui o nosso generics. Dessa forma, nosso componente de AppInputController irá receber um controller com a tipagem vinda diretamente do nosso formulário. Então, isso daqui vai ser bem interessante, porque quando nós vamos utilizar o nosso componente de AppInputController, ele irá sugerir todas as propriedades que já temos configurada lá no nosso formulário. Próximo que nós vamos utilizar é o name. Na tipagem do name, vamos colocar o path, importando do próprio react-hook-form, passando como parâmetro também o t. No caso, novamente, isso daqui para quando nós formos utilizar a propriedade de name, para nós já termos a referência de quais propriedades nós temos dentro do nosso formulário. E aqui dentro, novamente, teremos os errors, que será opcional, os erros. Será unfilled, errors. do React Hook Form. Lembrando que é importante nós importarmos todas essas propriedades do Hook Form. Passando também o T. Perfeito. Só um detalhe, aqui na propriedade de Field Value não é Field Value no singular e sim Field Values no plural. Eu já ia cometendo um equívoco aqui, então é importante vocês realizarem essa mudança. Vocês podem ver que até o erro que tinha aqui já parou de ser emitido. Então, vamos lá. Próximo passo, nós vamos tipar o nosso componente de fato. Então, antes aqui dos nossos parênteses na nossa arrow function, vamos abrir o menor que e o maior que, e vamos falar que o nosso componente espera receber um generic como parâmetro. No caso, teremos aqui o t extends, da mesma forma que temos o extends ali em cima, vamos fazer aqui embaixo, estendendo o field values. E aqui dentro das propriedades, vamos colocar que as propriedades que vamos receber será o app input, Logo após a chamada da nossa interface, vamos colocar o menor que e o maior que, e vamos falar que a nossa interface de AppInputControllerProps recebe como parâmetro o generic t Dessa forma, nós vamos repassar todos os valores genéricos que estamos recebendo na tipagem da chamada do nosso AppInputController pra nossa interface, dessa forma garantido com que o TypeScript tem um contexto de todo o formulário que nós estamos preenchendo no momento. Isso vai ser super importante nós fazermos exatamente dessa forma pra termos a referência de tudo que nós temos. Próximo passo, só pra parar de ficar dando erro, vamos desestruturar aqui o name, o Control, o Errors. Agora vamos repassar aqui pra dentro do nosso Controller. Name, vamos repassar a propriedade que estamos recebendo como parâmetro do nosso componente. O Control, mesma coisa, Control. E por fim, só pra ele parar de ficar apitando erro pra gente, vamos colocar aqui um Render. Fazendo da mesma forma que fizemos lá na nossa RegisterView, que é abrindo uma callback dentro do Render e repassando o nosso AppInput. Ficou só faltando colocar um return aqui dentro, perfeito? Pra falar a verdade, dá até pra gente fazer o retorno direto, dá pra nós removermos as chaves aqui. E show, tirar esse ponto e vírgula, perfeito. Com isso, temos a estrutura base do nosso componente de AppInputController. Pra essa aula não se estender muito, nós iremos fazer a construção do restante das funcionalidades dele na próxima aula. Então é isso, espero vocês lá.