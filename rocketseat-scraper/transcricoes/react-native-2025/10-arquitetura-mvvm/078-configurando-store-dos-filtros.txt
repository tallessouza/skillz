WEBVTT Na aula de hoje, vamos construir a Store responsável por armazenar e controlar o estado dos filtros aqui da nossa tela de home. No caso, nós vamos interagir com esta Store neste componente de filtro e ele irá refletir consequentemente em todos os locais que estiverem buscando os nossos produtos. Para isso, primeiro de tudo, vamos ir aqui em src, shared, Store. Vamos criar uma nova Store chamada useFilterPath. filterStore.ts, aqui vamos colocar um export const useUserFilterStore, onde vamos colocar aqui um create do próprio sustent, onde aqui vamos simplesmente passar uma callback, nessa callback, essa função vai simplesmente retornar um object literal. Já vamos pegar aqui o set, que é a função que vai fazer as atualizações do nosso estado. Próximo passo, vamos criar uma interface para nós falarmos como que será a estrutura aqui da nossa... do nosso estado, vamos lá, interface, vai ser, filter, store, Nós vamos ter aqui, a princípio, AppliedFilterState, onde aqui nós vamos ter um objeto, vamos criar aqui uma interface separada, ExportInterfaceFilterState, onde nós teremos aqui o ValueMin, que vai ser o valor mínimo, que será uma String, o ValueMax, que também será uma String, vamos ter aqui também o SelectedCategories, que vai ser um array de números. E vamos ter aqui, por fim, o searchText, que vai ser uma string. Esse searchText será preenchido com aqui o nosso input. Próximo passo, vamos pegar aqui esse filterState e colocar no appliedFilters. Perfeito. Próxima propriedade que teremos aqui na nossa Store será o método para atualizar o nosso filtro, no caso teremos aqui o UpdateFilter, que será uma função que irá retornar void e irá receber como parâmetro duas propriedades, no caso vai receber somente uma, onde nós teremos aqui, se eu só mudar aqui, props, Nós vamos receber aqui, ou seja, vai ser apenas uma única função onde nós iremos atualizar todos esses valores, então vamos utilizar o conceito de chave computadorizada, onde esta aqui será basicamente o nome da propriedade do objeto, no caso da propriedade aqui do nosso FutureState, poderá ser o ValueMin, ValueMax, o SelectedCategories ou o SearchText. Então, para nós tiparmos corretamente esta nossa aqui, vamos simplesmente colocar o KeyOff do Typescript da interface FutureState, só corrigir aqui, State, colocar o E no final, State, perfeito. E o Value vai poder ser Value, uma String, ou vai poder ser um Number, Array. Maravilha. Próximo passo, nós vamos simplesmente pegar esse filter store, essa interface que nós temos e colocar aqui, antes da abertura dos parênteses do método de create dos sustent, agora ele já vai reclamar aqui, falando para nós já passarmos os valores iniciais. No caso aqui, teremos simplesmente o searchText, uma string vazia, um selectedCategories, vai ser simplesmente um array vazio, varyMax. Vamos colocar aqui como NULL a princípio. NULL. e ValueMin também como NULL, com o fato de nós colocarmos aqui em cima, que também eles vão poder ser String ou Null. Perfeito, ou NULL. Vamos fazer uma coisa aqui também que vai ajudar a gente, vamos pegar todo este objeto e separar dentro de uma constante, para quando nós criarmos uma função de limpar o filtro, nós simplesmente reutilizarmos. Colocar aqui const DefaultFilterValues. Vamos pegar simplesmente todo este objeto que nós temos aqui. Vamos ver. Desta forma. Perfeito. Próximo vamos ter de ter aqui o UpdateFilter. Este UpdateFilter vamos ter uma callback onde aqui dentro nós vamos receber como parâmetro novamente a Key e o Value. Onde aqui nós vamos simplesmente fazer o seguinte. Vamos dar um set. No caso aqui coloquei set. Show. Onde aqui dentro do set vamos passar uma callback. com um object literal. Lembrando que quando eu falo object literal, é literalmente uma função que ao invés dela simplesmente abrir as chaves, ela retorna automaticamente um objeto desta forma, que é o nosso objetivo. Agora nós vamos fazer assim, desta forma, porque basicamente nós vamos poder recuperar o nosso state atual, no caso o estado dos using que nós temos no momento. Nós poderíamos também pegar aqui do get, do método create, mas vamos seguir aqui desta forma. Vamos aqui no Applied Filters, rodar um Spread Operator no state.appliedfilters, vírgula Vamos abrir colchetes, colocar aqui a nossa key, dois pontos, value. Desta forma, nós simplesmente estamos reaproveitando todos os filtros que nós temos no momento e alterando apenas o item que nós estamos, de fato, interagindo no momento. Vai fazer mais sentido futuramente. Novamente, o conceito de chave computadorizada com esta única função, nós vamos conseguir alterar todos esses valores aqui dentro da nossa story. Isso daqui é bem massa, é bem legal isso daqui. vamos só criar uma última função aqui por enquanto de ResetFilter ela vai ser simplesmente uma função que irá retornar void e aqui nós vamos simplesmente pegar um SetResetFilter vai ser uma função tendo aqui o set direto aqui um AppliedFilterState default Dessa forma, reaproveitamos a constante que tínhamos criado anteriormente, simplesmente para resetar os valores do filtro. Só um pequeno ajuste que nós vamos fazer aqui no VALUE MIN e VALUE MAX, vamos mudar aqui de STRING para NUMBER. Agora vamos ter de alterar aqui também, no caso, teremos de adicionar NUMBER somente. Perfeito, no caso o valor poderá ser um string, caso nós estejamos atualizando o search, text, um number caso seja o value min e o value max, e um array de números caso seja o selected categories. Maravilha! Com isso temos a configuração inicial da nossa store finalizada. Nós iremos fazer mais algumas funcionalidades nela, porém para a aula não ficar muito grande. Vamos finalizar por aqui. Eu espero vocês na próxima!