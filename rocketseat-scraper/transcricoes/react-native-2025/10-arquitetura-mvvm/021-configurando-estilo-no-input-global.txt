WEBVTT Na aula de hoje nós vamos simplesmente pegar toda a estilização que está sendo retornado dos nossos variants e aplicar logo aqui no nosso componente de AppInput. A princípio, vamos simplesmente colocar um className, normalmente igual fazemos com o NativeEngine aqui na nossa view. Vamos pegar o nosso styles.container, abrir e fechar chaves. Show! Vamos abrir aqui também a nossa tela de registro pra gente ver já atualizando o nosso componente de input. Dentro do nosso pressable, nós vamos colocar novamente aqui um class name, abrir e fechar chaves, pegar o styles.wrapper, que é aquela estilização que nós criamos na aula passada do wrapper. Só um detalhe, na aula passada nós colocamos border, ficou faltando o R aqui, e olha só, só de colocar o R ele já aparece pra gente a borda no canto inferior do nosso wrapper. O próximo que nós vamos fazer é dentro do nosso text input, Aqui no class name, vamos pegar um styles.input, abrir e fechar parênteses, dessa forma, Nós já temos a estilização base do nosso componente feita, já temos aqui a questão do ícone e do input também. Só um pequeno ajuste que vamos fazer dentro do nosso input variants, vamos mudar esse padding-bottom aqui pra 2 pra ficar mais condizente com o que temos aqui no nosso Figma, já que o espaçamento que o nosso ícone tem da nossa borda inferior não é tão grande. Vamos colocar aqui também dentro do nosso IonIcons um size de 22. Mesma coisa aqui para o nosso Ion Icons com o Touchable Opacity em volta, no caso aqui, um size de 22 também. O próximo passo que vamos fazer é aqui logo acima do nosso Pressable, vamos colocar um text, que no caso será a nossa label. Vamos simplesmente colocar label, depois nós vamos colocar todas essas propriedades aqui no nosso AppInput. E aqui dentro vamos colocar a estilização do className. Vamos abrir chaves, chamar o styles.label. E show de bola, nossa lima já está aqui corretamente. Vamos só ajustar aqui o margin button da nossa lima. Vamos colocar um margin button de 3. Dessa forma fica mais condizente com o que nós temos lá no nosso figma. Agora, por esse se tratar de um input genérico de texto, nós precisamos passar algumas propriedades pra ele e, por consequência, nós precisamos herdar todas as propriedades que o nosso TextInput já recebe por padrão no React Native. Então, pra isso, vamos criar uma interface logo acima dele. Vamos colocar aqui o export interface. app-input-props, onde nós vamos colocar um extents, ou seja, vamos herdar todas as propriedades já existentes em uma outra interface, que no caso seria o text-input-props, vamos importar do próprio react-native, e aqui dentro vamos pegar o nosso app-input-props e vamos shippar o nosso componente com um funcional component, vamos importar do react, abrir e fechar parênteses, passar o AppInputProps. Perfeito. Dessa forma, quando nós desestruturamos as propriedades do nosso AppInput, podemos ver que ele possui todas as propriedades de um componente de input de texto padrão, o onChangeText, por exemplo. Então, pra ficar mais fácil, vamos simplesmente dar um Spread Operator aqui no TextInputProps. Vamos pegar ele e rodar um spread aqui dentro do TextInput novamente. . . . TextInputProps. Dessa forma, nós podemos passar as mesmas propriedades do TextInput no nosso AppInput, o que vai deixar o nosso componente ainda mais flexível. Nós vamos poder utilizar ele literalmente como um... TextInput, de fato, reaproveitando todas as propriedades já existentes. E agora, um outro detalhe que nós vamos fazer é, além de estender o TextInputProps, nós também vamos estender as propriedades do AppInputVariance. Então, dentro do nosso Variance, nós vamos colocar um export logo abaixo da nossa constante. export type AppInputVarianceProps vai ser igual a variant props que nós importamos lá na aula passada, abre o menor que, maior que, type of nosso app input variants. Dessa forma nós vamos ter o isDisabled, o isError e o isFoxed. Vamos pegar essa nossa propriedade, esse nosso tipo e vamos colocar vírgula na frente da extensão do texto input props e colocar ele. Vamos importar. Agora, dentro das chaves, vamos adicionar mais algumas propriedades que vai ser necessário nós passarmos como, por exemplo, a própria label que nós vamos exibir aqui no nosso componente. Vamos colocar aqui uma label. Ela será opcional e será uma string. Teremos aqui também o left icon, que também será opcional. E aqui ele será um key off, type off. IonIcons, ou seja, os ícones que nós vamos utilizar.glyphMap. Agora, toda vez que nós utilizarmos o leftIcon no nosso app InputProps, ele já vai recomendar pra gente todos os ícones que temos disponíveis dentro do IonIcons aqui, utilizando o TypeScript a nosso favor, pra que a gente não precise vir. Ficar abrindo a documentação do IonIcons e pesquisar qual ícone que a gente vai querer utilizar, aqui ele já vai recomendar. Mesma coisa que fizemos com o LeftIcon, vamos fazer com o RightIcon. Lembrando que todos são opcionais, então teremos a opção de não utilizarmos nenhum ícone no nosso AppInput. Vamos receber aqui também um ContainerClass. name, que será uma string, que será uma class name específica para o nosso container, poderá ser útil em alguns momentos. E uma outra propriedade que teremos aqui no nosso app input próprio será um método chamado mask, uma funçãozinha que servirá para nós mascararmos os componentes de texto aqui do nosso app. Uma funçãozinha de mascaramento de texto, em que nós iremos utilizar em alguns locais aqui no nosso aplicativo, ele será simplesmente uma função que receberá como parâmetro um value, que será uma string, e ele irá retornar um void ou uma string. Perfeito. Agora, dentro aqui da desestruturação das propriedades do nosso componente, vamos pegar todas essas aqui, no caso a label, o left icon, right icon, e um container class name, Maravilha, vamos só mover esse TextInputProperties aqui pra baixo, que como ele é um REST Operator, nós vamos estar separando todas essas propriedades dele aqui. Vamos vir aqui dentro do styles.container da nossa ViewPy, vamos abrir chaves e vamos passar um class name. com o container class name. Dessa forma nós garantimos que se em algum momento nós quisermos especificar alguma estilização para o nosso componente de input, nós iremos passar nessa propriedade de container class name e isso daqui vai ser muito útil para alguns casos específicos onde o nosso app não daria suporte a um tipo específico de estilização. Dessa forma, a gente não vai ter que ficar criando N variações aqui dentro. Nós vamos simplesmente poder especificar o tipo de estilo que nós vamos querer passar pra dentro dele. E um outro detalhe é que nós, além do container className, teremos o className padrão. E esse className nós vamos passar ele dentro aqui do nosso componente de Input. Com isso, finalizamos a estilização base aqui do nosso app Input da próxima... Na próxima aula, criaremos a camada de ViewModel do nosso app EmPunch para deixar ele dinâmico e, dessa forma, nós teremos, de fato, os variants em ação. Vamos criar uma camada de ViewModel onde nós iremos controlar quando o nosso app está sendo clicado ou não, quando o nosso app está ativo, quando tem um erro, de fato, aparecendo ali dentro dele. Então, é isso. Espero que tenham gostado e espero vocês na próxima aula.