WEBVTT Nesta aula, vamos criar a requisição HTTP responsável por atualizar os dados do usuário. Para isso, vamos voltar aqui na nossa documentação, no localhost 3001-docs, onde nós temos aqui, por exemplo, em ALF, um verbo HTTP do tipo PUT-USER, onde nós enviamos, por exemplo, o nome, o e-mail, o fone, a senha e a nova senha do usuário. Então, para isso, vamos simplesmente voltar aqui. no nosso projeto, vim aqui em src, shared services, nós possuímos aqui um auth service, porém nós vamos criar um serviço novo, vamos criar aqui um específico para profile, no caso vai ser o profile.service.ts, que é simplesmente um export cost, um update, user profile, vai ser uma função assíncrona, E aqui nós vamos enviar como parâmetro este objeto aqui simplesmente, então vamos criar a interface dele dentro de src, shared interface, http, vai ser o updateProfile.ts, que vai ser o export interface, updateProfileParams, então vamos dar um ctrl-v e substituir todos esses valores aqui por string, Vamos voltar aqui no nosso ProfileService, colocar aqui o UserData, dois pontos, vai ser o nosso UserProfileParams. Vamos ver o que a nossa API retorna pra gente? Simplesmente os novos dados do usuário, no qual nós vamos utilizar para atualizar a nossa Store. Então vamos pegar aqui este objeto, voltar lá no nosso UpdateProfile, criar aqui um export, interface, Update profile response, vou só dar um Ctrl V aqui. onde nós vamos substituir, vamos remover estas aspas de user, que vamos só remover aqui, o id, colocar number, e o restante é só remover as aspas duplas, desta forma aqui. Vamos pegar agora aqui a nossa interface, voltar aqui na nossa camada de service, e vamos shippar agora, caso vamos desestruturar, o nosso await marketplace apiclient.put em barra como é que é lá o nosso endpoint é barra user desta forma no segundo parâmetro mandamos o user data E agora vamos chipar aqui no patch, menor que maior que, o updateProfileResponse, recuperar o nosso data e dar um return aqui nele. Próxima coisa, vamos criar aqui a nossa mutation responsável por chamar a nossa função de updateProfile, vamos vir aqui em shared, queries, vamos criar aqui um novo grupo de profile, onde aqui dentro vamos criar a nossa mutation useUpdate, profile.mutation.ts onde aqui vamos criar o nosso custom hook, e no caso export const useUpdateProfileMutation vai ser simplesmente uma arrow function com uma const mutation igual ao useMutation do React Query e com o query function sendo o nosso updateUserProfile onde aqui teremos o onSuccess Nesta forma aqui, onde vamos receber a response. Aqui nós vamos atualizar os dados do usuário quando a nossa requisição for, de fato, retornada com o status code 200 de sucesso e o onError. Vamos simplesmente exibir aqui. Vamos pegar aqui a error. Colocar aqui o nosso toast. Do toast vai retnative.error. enviando para lá o error.message ou vamos retornar o nosso uma mensagem genérica falha ao atualizar os dados do usuário onde aqui no segundo parâmetro vamos colocar ele no top aqui no success aqui acima da nossa constante de mutation vamos desestruturar o cost a variável lá do nosso useUserStore, vamos pegar aqui o nosso updateUser, no caso aqui são os dados do usuário, no caso este método aqui ele espera receber alguns parâmetros do usuário, no qual nós vamos atualizá-los aqui na nossa store, no qual nós vamos executar aqui dentro do unsuccess, simplesmente fazendo o seguinte, vamos colocar aqui o updateUser, passando como parâmetro todos os dados que são retornados aqui do response.user Ele não deu erro porque literalmente aqui dentro do user ele tem simplesmente o name, e-mail, avatar URI, phone, created at, updated at e deleted at, todos esses dados que nós já possuímos lá na nossa store do Xustant. Nós viemos aqui na nossa user interface e todos esses dados já possuem lá, menos aquele de updated at, mas isso daí não tem problema, a gente simplesmente envia lá para a store do Xustant também, sem problema nenhum. Vamos pegar a nossa mutation e colocar no return do nosso custom hook para que possamos utilizá-lo na camada de ViewModel aqui da nossa tela. A partir da próxima aula, nós vamos implementar algumas validações antes de chamarmos aqui a nossa mutation.