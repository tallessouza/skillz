WEBVTT Na aula de hoje, nós vamos iniciar a construção da camada de service dos nossos pedidos. Para isso, vamos ir aqui no nosso navegador. Vamos acessar a nossa documentação aqui no localhost. Podemos ver que temos um grupo chamado de orders, onde aqui temos uma get barra orders, onde nós retornamos simplesmente uma listagem com todos os pedidos do usuário. E possuímos um método post barra orders, onde nós enviamos aqui o ID do cartão de crédito e também todos os itens que nós vamos comprar com a quantidade onde o back-end automaticamente já vai processar aqui o nosso pedido, isso daqui vai ficar bem legal. Então, vamos voltar aqui no código, vamos fechar todas as nossas pastas, vim aqui dentro de src, shared, services, vamos criar aqui um novo arquivo chamado orders.ts, opa, ficou faltando colocar o .service, .service.ts, aqui vamos colocar um export, const, SubmitOrder, onde simplesmente vamos ter aqui uma função assíncrona e vamos chipar aqui o body da nossa requisição HTTP. Vamos simplesmente ter um objeto com o credit card id e os itens, que é um array de objeto contendo o product id e o quantity. Para isso, vamos voltar aqui no nosso app dentro de shared interface HTTP. Vamos criar aqui um Orders, opa, orders.ts onde aqui vamos colocar um export interface orders request body ou request params, vou colocar só request params aqui pra seguir a forma que nós estamos fazendo aqui no projeto, só remover essas chaves extras aqui e substituir os números por number, aqui o quantity também, number, Desta forma nós podemos manter, nós não vamos utilizar este item aqui em outro local, vai ser apenas aqui nesta interface. Vou colocar aqui interface no final, vamos pegar o nosso request params interface, vim aqui dentro da nossa função e vamos tipar aqui o order com o nosso orders request params interface. onde aqui vamos simplesmente colocar um const, vamos desestruturar o nosso await marketplace API client, a nossa instância do Axios, no verbo HTTP post na rota barra orders, onde simplesmente vamos enviar como segundo parâmetro do post o orders, que aqui seria basicamente o body da nossa requisição. Vamos analisar agora o retorno da nossa API. É simplesmente um objeto contendo a mensagem, contendo o ordCount e também o orders, que de fato foi executado. Vamos simplesmente voltar aqui no nosso projeto, dentro de orders, colocar um export interface, vai ser o submit ORDER RESPONSE, onde vamos dar um CTRL C com o que nós temos aqui na nossa documentação. Desta maneira. Só removendo aqui as strings e transformando os NUMBERS em NUMBER. Desta maneira. Vamos mudar o nome aqui também da nossa interface de parâmetros. Vamos colocar aqui o SUBMIT ORDER. Request, só mudar o nome aqui também, onde nós estamos usando na nossa camada de service. Perfeito, agora vamos tipar o retorno da nossa requisição HTTP, será um SubmitOrderResponse, onde vamos desestruturar o data do nosso retorno e simplesmente dar um return data dessa maneira. Próximo passo, vamos criar a nossa mutation, responsável por chamar aqui a nossa função de submitOrder. Vamos ir aqui dentro de src, shared, dentro de queries, vamos criar uma nova categoria aqui de orders. Aqui dentro vamos criar o nosso useSubmitOrder. order, opa, submit, order.mutation.ts, onde aqui vai ser simplesmente um custom hook padrão, igual nós já criamos aqui até o momento, export const useSubmitOrderMutation, onde vamos simplesmente criar uma função, ou melhor, uma constante de mutation, igual ao nosso useMutation, invocando o custom hook lá do nosso tanstack react-query, enviando como parâmetro um objeto com o query function, no caso a mutation function, sendo aqui onde nós simplesmente podemos pegar o nosso submitOrder e passar aqui desta maneira. Aqui, teremos o onOnSuccess, que será uma função de callback executada quando a nossa promise for concluída com status code na família dos 200, ou seja, se ela de fato for realizada com sucesso. Vamos só colocar um console.log aqui na nossa response, que é retornado aqui nos parâmetros.message. E o onError, vamos também só colocar aqui um Recuperar o error e colocar um console.log no error. Vamos já aproveitar e colocar aqui um toast do toast for react-native.error mandando pra lá o error.message ou simplesmente falha ao realizar pedido. Segundo parâmetro, vamos só colocar uma string com top, pra esse toll se aparecer na parte superior do nosso app. Vamos pegar a nossa constante de mutation e simplesmente dar um return aqui embaixo. Desta forma nós garantimos que vamos estar acessando a nossa submitOrder através da nossa mutation e dessa forma aproveitando todo o aparato do React Query. Detalhe interessante é que nós podemos simplesmente repassar a nossa função aqui do mutation function desta forma. Em outros locais nós simplesmente abrimos os parênteses e enviamos desta maneira. Porém, isso daqui era só para ficar mais visível para vocês, para ficar mais explícito de que nós estamos recebendo os parâmetros da nossa requisição e repassando lá para a nossa camada de service. Agora, como você já tem uma noção melhor de todo este fluxo que nós estamos fazendo, como se fosse uma camada intermediária ali, antes de chegarmos de fato na camada de service, Só o fato de nós passarmos a nossa função dessa forma, ela já vem tipada automaticamente com o que nós temos lá na service de fato, que é simplesmente uma função que espera receber o SubmitOrderRequestParam e simplesmente retorna pra gente uma promessa de SubmitOrderResponse. Dessa forma fica até mais limpo o nosso código. Com isso, finalizamos a nossa aula por aqui para não exceder muito. Na próxima nós vamos, de fato, implementar a funcionalidade aqui no nosso botão de confirmar compra. Então, é isso. Espero vocês na próxima aula!