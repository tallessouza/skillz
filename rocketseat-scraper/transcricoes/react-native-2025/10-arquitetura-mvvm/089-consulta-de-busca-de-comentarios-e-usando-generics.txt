WEBVTT Na aula de hoje, vamos construir a requisição HTTP responsável por bater no nosso back-end, na rota, para buscarmos os comentários de um produto específico. Para isso, se nós voltarmos aqui na documentação da nossa API no nosso back-end, no local host 3001-docs, teremos esta rota aqui, do tipo post-products-comments, onde nós enviamos para ele um bole contendo o ID do produto específico e As propriedades de paginação contendo a page e per-page, da mesma forma que nós temos a nossa rota de products, onde nós também enviamos a paginação para ele, a page e o per-page, nós vamos fazer a mesma coisa aqui para nós não carregarmos os comentários todos de uma vez. Novamente, vamos supor que nós tenhamos um produto muito vendido lá no nosso app de marketplace e ele possui dezenas de milhares de comentários, imagine... Toda vez que nós fôssemos fazer uma requisição HTTP para vermos os detalhes, carregar todos de uma vez só, não ia ser nem um pouco performático, nem para o front-end, nem para o back-end carregar uma quantidade tão grande assim de dados. Então nós quebramos esta requisição gigantesca em várias requisições menores, onde nós passamos novamente o page e o per-page para nós sabermos onde nós estamos no momento atual. Assim que nós realizarmos a requisição, ele irá retornar para a gente um objeto contendo o data, que é nada mais do que o array dos comentários em si, onde nós vamos ter o id, o content, o product id, o user id, e aqui alguns dados do usuário, como, por exemplo, o nome do usuário, o e-mail dele, e o avatar, onde nós iremos utilizar para renderizar a foto de perfil do usuário de fato. Isso daqui vai ficar bem legal. E também temos aqui a avaliação, o rating, que aquele usuário deu para aquele produto específico. Dentro do retorno desta rota, nós também possuímos a página. atual, o per page, a quantidade de itens que veio, o total de páginas que nós possuímos lá na nossa API e o total de itens que nós temos também. O interessante é isso daqui para nós utilizarmos como parâmetro para sabermos se já chegamos no final da nossa lista ou não. Então, para darmos segmento aqui com o desenvolvimento da mesma forma que nós utilizamos o useInfinityQuery para buscar a listagem de produtos, nós também vamos utilizar ele aqui, no caso, em shared Services, dentro de Products, vamos criar uma nova função, export, const, getProductComments, vai ser uma função assíncrona. onde vamos ter aqui const data, vamos desestruturar o await marketplace API client, vamos pegar a instância do Axios, ponto post, lembrando que o verbo HTTP desta nossa rota é do tipo post, justamente para nós podermos enviar o body contendo os dados da paginação. Era possível também nós utilizarmos o próprio get, porém no get não é o ideal nós enviarmos um body, na verdade eu acho que nem o Axios permite a gente enviar um body para o get, Então teríamos de colocar todas as paginações na query string, mas vamos seguir aqui desta forma. Vamos simplesmente colocar aqui o //products//comments. E aqui no segundo parâmetro vamos ter aqui os apagination. Vamos pegar aqui, ele espera receber simplesmente o product id e o pagination. Vamos copiar essa interface. Vamos vir aqui dentro de interfaces, http, product comments.ts export interface get product comments interface, vamos só dar um ctrl-v substituir esses valores numéricos aqui pelo tipo de number pegar essa interface e voltar aqui ctrl-v, vamos só mudar o nome desse parâmetro, ao invés de pagination vamos colocar aqui params No segundo parâmetro do método post da instância, vamos passar pra lá os parâmetros dessa forma. Agora vamos criar a resposta, vamos copiar todo este objetão que nós temos aqui na documentação, voltar, aqui dentro do product comments mesmo, export getProductCommentsResponse, vamos dar um ctrl-v, opa, falta colocar aqui interface, Vamos criar aqui fora de HTTP, dentro de interfaces. Opa, vamos criar um arquivo novo chamado product-comment.ts, onde aqui dentro vamos colocar um export interface product-comment, onde nós vamos pegar todo este objetão que nós temos aqui dentro de data. passar aqui pra dentro, só tirar essas chaves que tem englobando tudo, substituir os valores numéricos por number, e remover as aspas aqui das strings. Vamos pegar este nosso product comment e chippar aqui dentro do data do nosso getProductCommentsResponse, vamos importar ele, e aqui, onde tem zero, nós simplesmente vamos colocar number. Um detalhe interessante que nós podemos fazer aqui é que se nós repararmos na resposta do nosso ProductToResponse, ele retorna literalmente a mesma estrutura de dados, se nós colocarmos aqui lado a lado, ele retorna um PerPage, um Total e um TotalPages. Então, uma coisa interessante que nós poderíamos fazer é criar dentro de HTTP um tipo genérico de PaginatedResponse, onde ele irá receber um generic, onde a única mudança que nós possuímos aqui nas duas interfaces de getProduct, commentResponse e de productResponse é literalmente o conteúdo de data, aqui nós passaríamos simplesmente um generic. Para fazer isso, vamos simplesmente aqui criar um paginated response.ts, aqui vamos colocar um export, paginated response, com interface aqui, perfeito. Onde aqui no paginated response nós vamos colocar após o nome dele um T, que é o generic, onde vamos copiar toda essa resposta que temos aqui do nosso product response e passar aqui pra dentro. Onde nós vamos pegar aqui o nosso data, dois pontos, vamos falar que teremos um T array. Sem o menor que e o maior que. Dessa forma, nós podemos simplesmente fazer o seguinte. Aqui hoje nós estamos exportando o ProductResponse, nós podemos simplesmente fazer desta forma. Vamos arrancar todo o objeto que nós temos. Vamos só fechar aqui do lado esquerdo. Agora, onde nós temos o nosso ProductResponse e todos os outros locais que possuem uma resposta padrão de paginação, podemos simplesmente arrancar todo este objeto daqui, colocar um Extents, E no nosso Paginated Response, com menor que e maior que, passando o tipo de item que estamos retornando da API. No caso, lembrando que a única alteração que temos nas respostas paginadas da nossa API é o data. Então, vamos passar aqui o data. o Product Interface, abrir aqui as chaves. Desta forma, nós automaticamente informamos que este generic se trata de um Product Interface. Então, se nós olharmos aqui o local onde nós estamos usando esta nossa Product Response, se nós passarmos o mouse aqui dentro, nós já vamos saber, por exemplo, se nós desestruturarmos, que teremos um data e este data aqui se tratará de um Product Interface. Dessa forma, a gente reaproveita uma tipagem genérica aqui de Paginated Response. Na verdade, daria até para melhorar, nós poderíamos pegar simplesmente, ó, voltar aqui na nossa camada de service, dentro do GetProducts, ao invés de nós passarmos o ProductResponse, na verdade, esse arquivo aqui sequer existiria agora, ao invés do ProductResponse. Aqui, nós teríamos simplesmente um PaginatedResponse, menor que e maior que, enviando o nosso product interface. Daria literalmente no mesmo, nós estaríamos economizando algumas linhas de código, alguns arquivos que não seriam necessário, mas a criação deles, justamente por causa do paginated response. Agora vamos simplesmente voltar lá onde nós estávamos aqui do Product Comments. Aqui no Get Products Comment Response, novamente, ele não vai ser mais necessário, justamente pelo fato de que nós já possuímos o nosso Paginated Response, então vamos voltar aqui dentro do Product Service, vamos pegar novamente o nosso Paginated Product Response, e vamos tipar aqui o post antes da invocação da função, antes dos parênteses, o Paginated Response, enviando pra lá o Product Comment, Dessa forma, quando desestruturarmos o data da resposta da nossa promise, vamos desestruturá-lo e pegar o data que vem de lá de dentro, e quando passarmos o mouse aqui em cima, nós já saberemos automaticamente que teremos um array de comentários e, novamente, todas as outras propriedades da paginação. Isso daqui é muito legal, muito massa. Agora, simplesmente, vamos pegar e dar um return. Aqui no data. Perfeito. Essa aula acabou se alongando um pouquinho mais por culpa da nossa tipagem que nós criamos do paginated response. Mas não tem problema, isso daqui é algo que com certeza vai agregar bastante. É bem legal nós utilizarmos os generics do TypeScript no dia a dia para nos auxiliar nessas situações específicas. Só um detalhe, antes de seguirmos, ele está retornando um erro aqui pra gente justamente por culpa dessa mudança de tipagem que nós fizemos. Pra resolver isso é bem simples, basta a gente chegar aqui e remover essa desestruturação que nós tínhamos feito aqui anteriormente e retornar apenas o data. Se nós dermos um reload, o app vai voltar a funcionar normalmente. E simplesmente removendo a desestruturação e passando o data dessa forma, ele já vai parar de reclamar aqui o erro pra gente. E com isso finalizamos a nossa função de getProductComments. Na próxima, nós iremos criar a nossa infinite query, onde nós a utilizaremos de fato. Então, é isso. Espero vocês na próxima aula.