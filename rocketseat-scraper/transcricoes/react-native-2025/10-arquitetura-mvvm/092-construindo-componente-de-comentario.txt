WEBVTT Na aula de hoje, vamos construir o componente responsável por renderizar os comentários. Se nós voltarmos aqui no nosso Figma, podemos ver que é um componente bem simples, onde nós temos basicamente a foto de perfil do usuário, o nome, uma flagzinha caso o comentário seja o nosso, a questão da avaliação e também o conteúdo de fato da avaliação. Para isso, vamos voltar aqui no nosso app dentro de Components, de Product, vamos criar um componente novo chamado comment item onde aqui nós vamos criar um index.tsx vamos fazer um funcional component padrão export cost comment item onde por hora vamos simplesmente dar um return em uma view este comment item vai esperar receber como parâmetro o product comment, então interface comment item param Vai ser o comment-product-comment. Vamos pegar essa nossa interface e falar que será um Functional Component recebendo o comment-item-params. Vamos desestruturar. Perfeito, vamos simplesmente pegar aqui por ora um texto chaves comment.content. para a gente já ver ali em tela. Vamos copiar o nome aqui deste nosso componente, voltar lá na nossa Product e ver dentro da nossa Flat List Na propriedade de renderItem, vamos substituir este fragment que tínhamos colocado aqui, temporariamente. Vamos desestruturar os parâmetros da callback retornada dentro do nosso renderItem, onde ele retorna aqui pra gente o item de fato que está sendo iterado, ou seja, o comentário que está sendo iterado no looping dentro da FlatList. E vamos repassar o nosso comment. Desta forma. Ele tá reclamando de erro justamente por conta da tipagem, como nós fizemos aquele flatmap lá na nossa Infinity Query, nós vamos ter que fazer o seguinte, vamos pegar aqui a nossa tipagem de Product Comment, voltar lá na nossa Infinity Query, e tipar ele aqui, colocando simplesmente um as array de Product Comment. Maravilha, agora ele parou de reclamar de erro, que a gente está dizendo que o array que nós temos aqui de fato é um ProductContent. Acabou que se perdeu ali a tipagem no momento do FlatMap, ele não teve a capacidade de entender que nós estávamos gerando no fim do dia um array de ProductContent. Agora, vamos selecionar aqui este copo installing, que é um que nós já sabemos que possui comentário. E olha só que legal, já temos aqui a lista de comentários sendo renderizada. Que massa! Vamos ir aqui agora e começar a aplicar a estilização. Aqui vamos colocar um class name contendo um bg, white, com um padding de 4, um margin-bottom de 3 e um rounded, lg, com um shadow, sm. Vamos voltar lá na nossa tela. Temos aqui a estrutura base. Dentro dessa view, vamos colocar uma outra view. Onde aqui nós vamos colocar um className contendo um flexRow para jogar os itens um na frente do outro, um itemCenter, e um justify-between para jogar os itens na extremidade do componente py com o margin-bottom de bottom também. Aqui dentro uma outra view, onde aqui dentro, onde vamos deixar ela assim por hora, aqui abaixo desta view, vamos simplesmente colocar uma outra view, onde aqui teremos um ion-icons com o name de star, um size de 16, e um color, vamos chamar nossa variável de colors, colchetes, color, opa, purple, base, abaixo do nosso ioicons vai ser um text contendo o comment.user.rating.value, aqui na frente barra, um outro text contendo um 5, opa, 6 não, um 5, com um class name dentro desse text sendo um text sm um font bold e um text gray 600 o text interno aqui, vamos simplesmente colocar um class name com um text de 10 pixels, um pouco menor 10 px com um text de grey, também na tonalidade de 600. No componente pie, aqui do nosso ícone do nosso text, vamos só colocar o class name com o flex-row, jogar os itens uma na frente do outro, e um itens-end. Agora aqui, no grupo de views de cima, que nós tínhamos criado anteriormente, vai ser onde vai ficar a foto de perfil de usuário. Para isso, vamos fazer o seguinte, colocar uma outra view aqui onde aqui nós vamos fazer uma validação se o comment.user.avatar.url de fato existir e o nosso comment.user.avatar.url .url for diferente de uma string vazia. Nós vamos renderizar a image. Vou só importar aqui do react-native. Caso contrário, dois pontos, nós vamos renderizar uma view. Onde dentro dessa view vamos ter um icon com o name de person, com um size de 20. um color vai ser colors.gray na tonalidade 400 agora aqui na imagem vamos colocar um class name contendo um width full width full, height full também um resize, opa, dentro da imagem vai ser um resizeModeCover e um source vai ser uma URI contendo o comment.user.avatar.url, desta forma. Assim, nós garantimos que, se de fato a foto de perfil do usuário existir, nós vamos exibir ela. Caso contrário, nós vamos simplesmente exibir o ícone de pessoa. Agora, aqui no componente py, onde nós estamos englobando esta condicional, vamos simplesmente colocar um class name contendo um width de 8, um height também de 8, um rounded de 6px, um overflow hidden, um bg gray, 201 margin write de 3. Ficou faltando nós colocarmos a estilização aqui na view do nosso ionicons, caso aqui é um class name, simplesmente com um width full também, um height full, um itemCenter e um justifyCenter, pra ficar centralizado o nosso ícone. Agora aqui, no fechamento da view onde temos a foto de perfil do usuário, vamos colocar uma outra view, que aqui vai ser onde vai ficar o nome do usuário, a gente teremos aqui o text, contendo o comment.user.name, E aqui embaixo nós vamos fazer uma validação. Se o ID do usuário do comentário for igual ao ID do usuário que está logado, nós vamos exibir aquela flagzinha lá do Figma falando que é a gente, no caso, a flagzinha de você. Para isso, vamos fazer dessa forma bem simples. const, desestruturar o useUserStore. Vamos pegar aqui o user. Caso o user.id seja igual a. Ao comment.user.id, nós vamos renderizar esta flag, que será simplesmente uma view. Vamos fechar ela. No caso aqui, ele pode ser undefined, então, colocar o ponto de interrogação. Vamos ter um text, simplesmente com o texto de você. Vamos colocar um class name aqui, vai ser um bg-blue-base. no text, um px de 2, um py de 1, vamos abrir aqui o comentário lá novamente, vamos só inverter a condição pra gente conseguir ver aqui, de fato, e show, a gente tá conseguindo ver, um py de 1, um rounded full, e um margin left de 2. Perfeito, aqui no text, vamos colocar o class name, tô tendo text white, o TextXS e o FontBold. Aqui na ViewPy, vamos colocar o ClassName contendo o FlexRow, jogar os itens na frente do outro, o ItemCenter e o Flex1. Agora pra gente jogar aqui o nome na frente da foto de perfil do usuário, a gente vai vir aqui na ViewPy, que nós não tínhamos estilizado ainda, colocar o ClassName com o FlexRow, o ItemCenter, flex1 só pra garantir que ele vai ocupar 100% do espaço. Também vamos colocar o espaçamento aqui no nome do usuário com margin-left de 2. No nome do usuário vamos colocar também um class name contendo um text-base, um font-medium e um text-gray na tonalidade 800. Maravilha! Com isso temos finalizado a base aqui da nossa componente de comentário Só uma coisinha aqui pra ficar mais semântica, aqui na condicional que nós colocamos para renderizar ou não a flagzinha de que se é a gente ou não, a gente até invertido ela pra gente conseguir enxergar. Vamos criar uma variável aqui em cima, const is currentUser, vai ser literalmente o user.id igual ao comment.user.id. Aqui só a gente colocar a interrogação, ele pode ser nulo também. e é que ao invés da gente colocar a condicional desta forma, que fica meio enigmática, a gente coloca isso que a gente usa e fica mais semântico. Só a pessoa bater o olho aqui, ele já vai saber o que se trata esta condicional. Lembrando que é importante a gente fazer um código muito semântico, porque geralmente outras pessoas que fazem a manutenção dele posteriormente, ou a implementação de novas features, alguma refatoração, dá um código bem estruturado com uma semântica bacana ajuda bastante. Os devs das futuras gerações irão agradecer todo e qualquer esforço que foi feito para garantir a semântica de um bom código. E com isso finalizamos a estruturação base aqui do nosso componente de renderização de comentários. Na próxima aula nós iremos realizar as consultas infinitas quando nós chegarmos no final da nossa lista para continuarmos consultando os comentários. e, dessa forma, aplicando o scroll infinito aqui na nossa tela. Espero que tenham gostado da aula e espero vocês na próxima!