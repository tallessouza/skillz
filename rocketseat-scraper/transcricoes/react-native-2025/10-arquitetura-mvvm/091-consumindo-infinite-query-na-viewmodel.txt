WEBVTT Chegou a hora de finalmente utilizarmos o nosso custom hook contendo a nossa useInfinityQuery lá na nossa camada de ViewModel de detalhes de produto. Para isso vamos vir aqui dentro de src, ViewModels, Product, useProduct, ViewModel. Aqui dentro nós vamos simplesmente fazer uma desestruturação, uma desestruturação aqui da nossa useGetCommentsInfinityQuery. onde nós vamos passar como parâmetro simplesmente o ProductId por enquanto. Aqui vamos recuperar os comentários, vamos recuperar aqui também o isLoading, no caso ele está dando conflito com o nome da variável aqui em cima, então vamos simplesmente colocar um alias aqui na propriedade da desestruturação do nosso objeto chamado de getCommentsLoading, vamos pegar aqui também o hasNextPage, que é uma propriedade booleana, que diz se nós temos ou não uma próxima página, temos aqui também o fetchNextPage, o refetch, ele tá reclamando aqui, show, refetch, temos a propriedade de erro, isRefetching, o erro aqui no caso, getCommentError, E show de bola. O que nós vamos fazer aqui é simplesmente criar uma variável de loading onde nós vamos englobar tanto o loading dos dados do produto quanto o loading do carregamento. Com isso nós vamos criar funções específicas para executar os métodos da nossa Infinite Query para ficar mais semântico, tipo const handleLoadMore. vai ser uma função, que vamos fazer uma validação, if, se nós possuímos uma próxima página, nós vamos cair dentro do caso do if, nós precisamos pegar aqui também o isFetchingNextPage, vamos validar aqui também, ee, isFetchingNextPage, se ele for falso, nós vamos cair aqui dentro desse if, simplesmente para evitar requisição duplicadas, nós já estamos buscando a próxima página, não faz sentido nós realizarmos o mesmo request duas vezes, onde aqui nós simplesmente vamos rodar o fetch nextPage, desta forma. Vamos criar aqui também um método de const handle refetch, onde vamos simplesmente colocar aqui um refetch. Vamos só colocar aqui um if, vamos pegar o nosso refetch aqui pra dentro e isRefetching, nós só vamos executar o refetch se o isRefetching for falso, ou seja, se nós não estivermos previamente buscando já uma requisição através do refetch. E por fim, o método que será executado para nós quando nós chegarmos no final da nossa flatlist, igual nós já possuímos aqui, onde nós vamos chamar este método de costHandleAndReshed. A gente nós vamos simplesmente chamar aqui o nosso handle loadMore. Dessa forma pra ficar mais semântico. Agora tudo que nós precisamos fazer é repassar todas essas propriedades aqui pra nossa camada de view. Colocando elas aqui dentro do objeto do return. Vamos passar aqui. GetCommentsLoading também. You get comments error. Com isso, é só passar nossas propriedades dentro do objeto. Nós já conseguimos recuperar todas elas aqui dentro da nossa camada de view. Ficou faltando nós passarmos também aqui os comentários em si. Então, vamos pegar os comments. Passar aqui pra dentro. Vamos recuperar os comentários e repassar pra dentro da propriedade de data que dá a nossa flatlist. Vamos só dar um console.log pra ver se tá tudo funcionando corretamente. console.log aqui em comments, vamos escolher aqui qualquer produto, este copo Stanley por exemplo, e ele retornou um erro, ele está dizendo que cannot replace of undefined, ah, só nós vimos aqui na nossa infinity query para pegar os comentários, no caso aqui dentro do nosso use getProduct comments aqui dentro onde nós estamos utilizando o build image url nós vamos colocar primeiramente o ponto de interrogação aqui na url, na verdade, que ela pode não existir e com ela não existindo nós vamos colocar dois pontos de interrogação string vazia dessa forma quando nós abrirmos o copystaller, olha só conseguimos acessar normalmente a nossa tela de detalhes e possuímos aqui, olha só que legal, um array contendo todos os comentários dos usuários, isso daqui é muito massa, isso daqui é muito legal. O próximo passo que nós vamos fazer é construir o componente para renderizar estes itens de fato.