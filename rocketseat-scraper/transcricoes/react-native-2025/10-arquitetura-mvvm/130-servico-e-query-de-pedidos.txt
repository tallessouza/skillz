WEBVTT Dando continuidade aqui com o nosso projeto, nesta aula vamos iniciar o desenvolvimento aqui da nossa tela de pedidos, da nossa tela de orders. Para isso vamos ter de criar a camada de service, no qual a camada de service até já existe aqui, que nós criamos algumas aulas passadas referente às nossas ordens, aos nossos pedidos, para nós de fato podermos realizá-los de fato aqui no nosso submitOrder. Vamos criar uma nova função aqui para buscar todas as ordens que nós temos. No caso, vamos abrir aqui a nossa documentação. Aqui no grupo de ordens, podemos ver que possuímos uma rota com o verbo HTTP GET, que é simplesmente bar ordens, que não espera receber nenhum parâmetro, apenas o nosso token de autenticação. Lembrando que toda vez que nós possuímos aqui no Swagger este cadeado, significa que é uma rota privada, uma rota autenticada, onde ela vai simplesmente retornar pra gente este objeto contendo esta listagem de ordens aqui pra gente. Então para isso vamos começar criando aqui o nosso método, a nossa função exportCostGetOrders onde teremos aqui uma função assíncrona onde aqui dentro nós não vamos receber absolutamente nada como parâmetro, e aqui vamos simplesmente fazer, como padrão, a desestruturação do nosso await marketplace.api.client.get em barra orders. Desta maneira, vamos simplesmente pegar lá na documentação o objeto de retorno. Vamos voltar aqui no nosso app dentro de src.shared.interface.http Vamos criar aqui uma nova... o novo arquivo no caso vai ser o getOrder.ts vamos colocar getOrders no plural e aqui só pra ficar mais semântico onde nós tínhamos nomeado de order anteriormente onde nós tínhamos os parâmetros e resposta do submit vamos só colocar aqui submit Orders, pra ficar mais semântico aqui os nossos dados. E vamos chipar aqui correto também, com o retorno do nosso objeto, dessa maneira. Por mais que a gente não tá usando esses dados de retorno. A gente irá esse objeto aqui de dentro dos colchetes. Perfeito, agora o próprio VS Code já faz o realinhamento das rotas, a atualização do nome dela. Aqui vamos colocar um export interface getOrdersResponse Vamos simplesmente vir aqui, voltar na nossa documentação, dar um CTRL-C aqui na resposta, e dar um CTRL-V aqui na interface. Vamos pegar aqui o objeto de ordem. e vamos separar ele, caso aqui, em interface, vamos só colocar order no singular, .ts, export interface, order interface, só dar um ctrl v dessa maneira, assim a gente cria uma interface específica só para as ordens, caso seja necessário nós utilizarmos em outros locais, e aqui vamos só substituir estes zeros por number, aqui embaixo também, number, String só removerá as aspas duplas, desta maneira. E aqui em createdAt, vamos só colocar date desta forma aqui. Vamos pegar a nossa order interface, voltar para dentro de HTTP, getOrders, e vamos tipar dizendo que esta propriedade se trata de um order interface, no caso um array de order interface. E o total order é simplesmente um number. Vamos pegar a nossa interface e voltar aqui na nossa camada de service e vamos simplesmente tipar aqui com o getOrdersResponse, recuperando o nosso data e dando aqui um return no data desta maneira. Agora vamos criar uma query para nós consumirmos aqui o nosso método da camada de service, como é de padrão aqui do nosso projeto. Todos os métodos dentro de service, no caso todos os métodos HTTP, a gente vai criar sempre uma query ou uma mutation para estarmos executando ele, englobando com o aparato do React Query. Vamos vir dentro de shared. Queries, aqui dentro de Orders, simplesmente vamos criar um novo Custom Hook, que vai ser o UseGetOrders.query.ts Normalmente, seguindo o padrão, export const UseGetOrders.query Vamos ter simplesmente um Custom Hook Aqui vamos criar uma constante chamada query igual ao useQuery do nosso ReactQuery. Lembrando que o ReactQuery possui duas variáveis com nome parecido, no caso é o useQueries, no plural useQuery. É importante nós pegarmos nesses casos aqui o useQuery. no singular, onde aqui nós vamos passar como parâmetro um objeto com o nosso query function sendo o nosso getOrders da nossa camada de service, o nosso query key, vamos passar um array contendo o nosso userOrders caso, a chave para o React Query armazenar no cache local a resposta da nossa requisição HTTP. E aqui no stale time, que é o tempo que o nosso cache vai ser válido, vamos colocar aqui 1000 milissegundos, vezes 60 segundos, assim nós temos 1 minuto, vezes, vamos colocar aqui, 10 minutos. Agora, um detalhe interessante, nós vimos aqui na nossa query, no caso, na nossa mutation de useSubmitOrder, aqui no onSuccess, seguindo os mesmos princípios das outras mutations que nós possuímos aqui no nosso projeto, partindo do pressuposto em que nós possuímos um cache interno no nosso app, e de que, posteriormente, nós acabamos alterando uma ordem, ou criando, ou simplesmente cancelando a ordem, Se nós não removermos ou invalidarmos o cache interno da mutation que traz, de fato, as nossas ordens, nós vamos possuir dados desatualizados aqui na nossa interface, no nosso client-side, pelo simples fato de que no servidor, de fato, a operação foi feita. No caso, a API, de fato, foi no banco de dados e cancelou a ordem, criou a ordem. Porém, o fato de nós estarmos sempre consumindo do cache local, nós não vamos ter acesso a esses dados atualizados. Então, nós vamos vir aqui no nosso useSubmitOrderMutation, no método de onSuccess, onde nós vamos invalidar a nossa query key. Para isso, vamos criar aqui uma constante, const queryClient, vai ser igual ao nosso useSubmitOrderMutation. ou no caso, useQueryClient. Vamos pegar esta nossa constante no objeto de onSuccess, vamos pegar o nosso queryClient.invalidateQueries, onde aqui vamos invocar esta função, enviando como parâmetro um objeto contendo a queryKey, sendo simplesmente um array, onde aqui nós vamos passar simplesmente também a chave que nós temos aqui no nosso useGetOrdersQuery. Para que dessa forma, toda vez que a nossa requisição HTTP de criação de ordem for de fato resolvida com o status code 200, nós entendemos que de fato houve uma alteração no server-side, lá no nosso banco de dados. E, consequentemente, vamos vir aqui invalidar a nossa query e o próprio React Queue já vai fazer um refetch automaticamente aqui da nossa listagem, trazendo sempre dados frescos e, dessa forma, garantindo uma boa experiência de usuário. Vamos simplesmente colocar um return aqui abaixo na query. para que nós possamos utilizar ela de fato aqui, invocando o nosso custom hook e com isso finalizamos a nossa aula. A partir da próxima aula vamos utilizar esta query para de fato listarmos aqui os nossos pedidos. Espero que tenham gostado e espero vocês na próxima aula.