WEBVTT Fala Devs! Na aula de hoje daremos início na construção do nosso Botton Sheet e é simplesmente aquele menu inferior que aparece quando nós clicarmos aqui nos nossos filtros e para isso nós vamos fazer algo muito parecido com o que nós temos aqui Nos nossos modais, vamos utilizar o próprio Zustand para realizarmos o gerenciamento de quando nós vamos exibir ele ou não, e também vamos colocar um componente dinâmico para nós podermos reaproveitar a mesma estrutura em literalmente todos os bottom sheets que iremos utilizar no nosso aplicativo. Para isso, primeiro de tudo, vamos vir aqui dentro de src, shared, store, vamos criar uma nova store chamada bottom sheet. store.ts onde aqui dentro nós vamos colocar um export const use bottom.sh store vamos pegar o create do próprio sustent onde aqui dentro vamos ter uma callback retornando um object literal, onde nós construiremos o estado inicial da nossa store. Próximo passo, vamos criar uma interface para nós dissermos quais serão os tipos que teremos aqui dentro. BottomsheetStore. Onde nós teremos o isOpen, que será um valor booleano. Um boolean. Que será um true ou um false. Onde nós vamos controlar quando queremos exibir ou não o nosso Bottomsheet. O próximo será o content. E seguindo o mesmo princípio que nós temos no nosso modal store, onde nós podemos utilizar qualquer componente React para renderizarmos como um modal. Aqui nós também teremos este mesmo tipo de um ReactNode ou Nulo. Teremos também aqui uma config, que será simplesmente uma configuração base que nós teremos, como por exemplo os snap points do nosso bottom sheet. Então vamos querer aqui uma interface, bottom sheet config, teremos aqui snap points, que será opcional, e teremos aqui também uma string array, ou seja, um array de strings e um enable. down to close, que é simplesmente a opção de nós arrastarmos o nosso bottom sheet para baixo, para fecharmos ele. Vamos deixar opcional, também como um boolê. Vamos pegar esta nossa tipagem e tipar o nosso config. Próxima prop que teremos aqui será um open, uma função para abrir o nosso bottom sheet, que ela irá retornar um void, irá receber como parâmetro um content, que será um react node, E a config, a gente teremos aqui o bottom. sheet.config, aqui vamos fazer o seguinte, vamos pegar aqui, content, dois pontos, vamos englobar esta tipagem que nós temos aqui dentro de um objeto, desta forma, nós garantimos a consistência dos dados, é sempre legal, a gente está englobando as propriedades da função dentro de um objeto, dessa forma, quando nós formos passar a propriedade, ele próprio já irá retornar para a gente as opções, as sugestões de nomes das propriedades que ele espera receber, no caso o content e o config. Caso contrário, se nós utilizarmos daquela forma, nós obrigatoriamente teríamos de passar a propriedade na ordem que nós definimos a função. É sempre legal nós utilizarmos esse tipo de prática aqui no nosso dia a dia. Próximo teremos um close, que será simplesmente uma função que não irá receber nenhuma própria e irá retornar a void. Vamos pegar esta nossa interface, vamos colocar aqui antes da abertura da nossa... antes da invocação do create, antes dos parênteses, vamos simplesmente passar o BottlesheetStore, agora ele tá reclamando, porque ele espera que nós passemos todas as... todos os dados aqui, no caso o isOpen, vamos começar como false, o content vamos começar como nulo, que será... o componente renderizado dentro do nosso bottom sheet, o config, nós teremos uma configuração inicial, então vamos fazer o seguinte, const default config, vamos tipar ele, falando que é um bottom sheet config, igual a um objeto, contendo aqui o snap points, vamos colocar aqui por padrão, que o nosso bottom sheet poderá abrir de 80% do rate da nossa tela até 90%, também o enable, And now to close, como true. Vamos pegar o nosso default config, passar aqui pro config, próximo as nossas funções, open, e o close. Vamos colocar a vírgula, close. E perfeito! Agora vamos implementar essas nossas duas funções. Aqui nós passamos o mouse em cima, podemos ver que ele já dá a tipagem do que essa função espera receber como parâmetro, no caso o content. Então se nós simplesmente desestruturarmos, nós podemos recuperar tanto a config quanto o content de fato. Aqui nós podemos simplesmente tirar, podemos simplesmente já colocar aqui o set, esse set nós precisamos importar da callback da invocação do create, no caso teremos o set aqui dentro, perfeito, o set espera receber como parâmetro um objeto e aqui dentro nós vamos simplesmente colocar o isOpen open como true por padrão, vamos pegar a nossa config também, vamos repassar a config que nós já temos, estamos recebendo como parâmetros, mas antes disso vamos fazer o seguinte, vamos fazer um spread operator na default configs, default config vírgula Outra experiência do perito na config que estamos recebendo. Dessa forma, nós conseguimos reaproveitar essas propriedades padrões que nós colocamos aqui em cima. Toda vez que nós formos abrir o nosso bottom sheet, nós já vamos reaproveitá-las e sobrescrever com o que nós estamos recebendo como parâmetro. E, por fim, o content. Vamos só repassar desta forma. O close não tem muito segredo, da mesma forma podemos remover as chaves e passar diretamente o set aqui dentro. Vamos dizer que nós simplesmente setamos aqui o isOpen para false, o content para null, e o config, nós citamos o default.config de volta ao estado inicial. Desta forma temos toda uma store preparada para nós utilizarmos em toda a nossa aplicação de forma global, onde nós iremos controlar a exibição do nosso bottom sheet Na próxima aula, nós iremos criar o nosso componente genérico, onde ele irá receber o content, ou seja, esse React node, onde nós iremos reutilizarmos novamente em todo o nosso app. No caso, vamos inicialmente estar criando o nosso filtro, mas posteriormente vamos utilizar também para a criação de comentários. Espero que tenham gostado da aula e espero vocês na próxima.