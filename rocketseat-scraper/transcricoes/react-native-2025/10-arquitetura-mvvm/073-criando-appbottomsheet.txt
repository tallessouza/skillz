WEBVTT Olá, Devs! Na aula de hoje, iremos construir o componente de Boton Sheet Global que iremos utilizar no nosso app. Na aula passada, nós construímos o nosso Use Boton Sheet Store, onde nele nós controlaremos todos os status referentes à exibição do Boton Sheet no nosso app. Nesta aula, nós vamos seguir com a construção do componente base em si. Para isso, vamos ir aqui dentro de src, Shared Components, vamos criar uma nova pasta chamada App, Boton Sheet, E aqui dentro vamos criar um index.tsx, onde aqui vamos fazer um Functional Components de padrão, app.bottomSheet, e perfeito. Próximo passo, vamos simplesmente pegar esse nosso app.bottomSheet, vamos aqui dentro de app, private layout, dentro do nosso root layout, das nossas rotas privadas, vamos simplesmente colocar o nosso bottom sheet aqui dentro. E o próximo passo que nós precisamos fazer é instalar a biblioteca que iremos utilizar como base para construir o nosso bottom sheet, que no caso é o gorham-bottom-sheet. Vamos parar o nosso app, rodar um yarn add gorham-bottom-sheet. Vamos rodar o nosso app aqui novamente, yarn iOS ou yarn Android. Perfeito, com o nosso app rodando, vamos fazer algumas importações aqui dentro do nosso componente. Vamos simplesmente importar. Brown. Arroba. Gorham. Bottomsheet. Vamos importar aqui o nosso Bottomsheet. Strollview. Vamos precisar também do nosso Bottomsheet. Backdrop. E por fim, o Bottomsheet. Backdrop props. Com isso, vamos colocar o nosso componente de Bottomsheet aqui. que no caso nós vamos pegar a importação base da própria biblioteca, onde nós vamos simplesmente abrir e fechar ele. Ele tá dando o erro porque ele espera algumas propriedades obrigatórias, no caso, o children que ele irá receber dentro dele, no caso, o children que nós já possuímos aqui dentro da nossa store. Então, vamos colocar aqui const igual a use, nosso use-bottom-sheet-store que criamos na aula passada. Vamos pegar aqui o nosso content. Vamos passar aqui pra dentro. Perfeito. Voltando, em volta do nosso content, vamos colocar aqui o bottom-sheet-scrollview. que é simplesmente caso haja a necessidade do nosso bottom sheet possuir uma scroll view, ou seja, se o height dele extrapolar o tamanho de fato do bottom sheet, nós vamos ter o comportamento de scroll dentro dele próprio. Vamos só dar um reload aqui no nosso app. E ele tá dando esse erro porque ele precisa do React Native Gesture Handler pra funcionar, então vamos parar aqui a nossa aplicação. Vamos simplesmente copiar aqui esse React Native Gesture Handler. Vamos dar um npx expo install React Native Gesture Handler. Perfeito, vamos rodar o nosso app novamente, YarnOS, e enquanto nosso app vai sendo rodado, vamos voltar aqui no nosso root layout, ou seja, no root layout global da nossa aplicação. Nós vamos englobar todo o nosso app com o GestureHandlerProvider. O GestureHandler é basicamente uma biblioteca que adiciona eventos aos toques no nosso app, ou seja, temos a possibilidade de implementar drag and drops, por exemplo, ou até mesmo captar movimentos específicos do toque do usuário no nosso app. E esta biblioteca de bottom sheet que nós estamos utilizando necessita dessa biblioteca por trás dos fones para ela funcionar perfeitamente. Então vamos simplesmente colocar aqui um import from react-native-gesture-handler. Vamos importar aqui a gesture-handler. Vamos pegar este componente de RootView e englobar todo o nosso app, ou seja, vamos pegar e colocar em volta do nosso QueryClientProvider. Só para garantir, vamos colocar aqui um className com flex de 1, para garantirmos que ele vai ocupar 100% do espaço disponível na tela, nosso app estartado. Vamos voltar agora lá no nosso componente de AppBottomSheet. Aqui dentro nós vamos pegar as outras propriedades que temos dentro da nossa useBottomSheetStore, no caso o método de close, o isOpen, o método de open e o config. Na verdade o de open nós não vamos usar aqui, nós vamos usar eles fora, perfeito. Aqui dentro nós vamos criar uma constante para nós armazenarmos a referência do nosso bottom sheet, então vai ser um bottom. kitHalf, vai ser igual ao useHalf hook.react, vamos tipar ele utilizando o nosso bottomSheet, o mesmo componente que nós estamos utilizando aqui embaixo no nosso return. Vamos colocar um nulo aqui como valor default. Próximo passo, nós vamos pegar e criar uma constante const snapPoints, vamos colocar um useMemo, vamos importar também do React, onde aqui dentro nós vamos ter uma callback e um array de referências, onde aqui vamos colocar simplesmente um config?snapPoints, onde aqui nós vamos simplesmente fazer uma validação, no caso, onde aqui nós vamos simplesmente retornar o nosso config?snapPoints, vamos até colocar aqui um ponto de interrogação, porque ele pode ser opcional, ou um array, Vamos simplesmente pegar aqui string 80%, vírgula, string 90%. Esse daqui é a porcentagem do rate que o nosso bottom sheet vai poder abrir na nossa tela. Próximo passo que nós vamos fazer é colocar um useEffect aqui dentro. Vamos ter uma... colocar o bank e uma rede de dependência, onde nesse rede de dependência vamos ter o isOpen e o content. E aqui dentro nós vamos fazer o seguinte, vamos colocar um if isOpen e content existirem, vamos pegar a referência bottomSheetRef.current.snapToIndex 0. Ou seja, nós vamos chamar a função de snapToIndex para que o nosso bottomSheet se abra até o índice 0, no caso 80% da nossa tela. Caso contrário, aqui dentro do else, vamos simplesmente colocar um bottomSheet half.current.close. Dessa forma garantimos que se caso o isOpen esteja como false ou o content não exista, nós vamos simplesmente fechar o nosso bottom sheet. Próximo passo que nós vamos fazer é simplesmente criar aqui um renderBackdrop, const renderBackdrop, vai ser um useCallback, E aqui teremos a rede dependência, onde vamos simplesmente deixar vazio. Aqui a gente pode apagar essas chaves e colocar direto parênteses, porque nós vamos retornar um componente, que no caso vai ser o BottomSheet. BottomSheetBackdrop, onde aqui nós vamos passar algumas propriedades, onde a maioria delas vão vir dos parâmetros da callback do nosso useCallback, no caso vamos chegar aqui para props, vamos chicar com BottomSheet. backdrop props que nós importamos anteriormente. Vamos simplesmente vir aqui e dar um spread operator nas props que iremos receber. Próxima coisa que iremos colocar é um appear on index 0, um disappear on index menos 1, um opacity de 0.7, e por fim, um press behavior, opa, um press behavior, Passou da String de Clones. Vamos pegar agora este nosso RenderBackdrop, passar pra dentro da propriedade de BackdropComponent, desta forma. Este backdrop nada mais é do que o comportamento de fechar o nosso bottom sheet quando nós clicamos fora dele e aquele comportamento também de escurecer a tela do que está embaixo para dar uma impressão de que o bottom sheet de fato está sobre toda a nossa aplicação. Próximo passo, vamos simplesmente colocar um background style aqui dentro. Hoje nós vamos colocar um background color. contendo um colors, onde nós vamos importar nossa variável de cores, ponto background, border-top-left-radius, 32, e border-top-right-radius, de 32 também. Perfeito. Próxima propriedade que vamos colocar aqui é o enablePanToClose. Vai ser simplesmente um boolean, chamando o nosso config.enablePanToClose. Vamos colocar aqui um index-1 e um animationOnMount. Somente dessa forma. Perfeito, temos o componente base de nosso bottom sheet. Ficou faltando só passar aqui os snap points. Snap points. Bora testar pra ver se está funcionando? Para nós testarmos, nós vamos simplesmente vir aqui dentro da nossa ViewModel de Home. Onde nós temos aqui dentro do nosso ListHeaderComponent, vamos vir aqui no searchInput, aqui dentro nós vamos recuperar o método de open lá do nosso useBottomSheetStore. Vamos pegar aqui o open. Onde aqui dentro do ToastableOpacityDefilter, nós vamos colocar um OnPress, um evento de OnPress, uma callback, onde vamos chamar o Open, passando como parâmetro o content, simplesmente o componente React aqui dentro, vamos simplesmente passar um text. Text de teste. e um config, ele espera receber o objeto vazio, para resolver isso é só a gente vir aqui e falar que config é opcional também, dessa forma a gente não precisa ficar passando o objeto vazio. Para finalizar vamos só fazer uma correção, aqui ao invés de nós colocarmos simplesmente um boolean com config.enableDownToClose, nós vamos simplesmente fazer desta forma, aqui, dois pontos de interrogação, show, Perfeito, vamos criar aqui também mais uma função, que é uma função de handleSheetChains const handleSheetChains, que vai ser igual ao useCallback, onde aqui nós vamos ter uma rede de dependência ou método de close, onde aqui vamos fazer uma validação, vamos receber um index aqui de dentro, que vai ser um number, se este index for igual a "-1", vamos simplesmente fechar o nosso bottom sheet, Simplesmente este é o nosso método onde nós vamos pegar ele e passar aqui pra baixo dentro do método de onChange do bottomSheet, onChange. Também ficou faltando nós adicionarmos a referência aqui no nosso componente de bottomSheet, então no caso aqui o half vai ser igual ao nosso bottomSheet.half, que nós vamos passar aqui embaixo. Dessa forma, quando nós clicarmos aqui no nosso botão de filtro, olha só o nosso Warren Sheets sendo exibido, que coisa linda! No caso, como temos aqui apenas este componente, ele vai ficar bem escondido lá embaixo. Porém, quando nós adicionarmos o nosso componente de filtro aqui, ele vai ficar melhor. Na próxima aula, nós iremos começar a construir o nosso formulário de filtro. Então é isso, espero vocês lá!