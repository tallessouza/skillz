WEBVTT Dando seguimento com o desenvolvimento do nosso app, na aula passada nós criamos a nossa camada de service, onde nós temos o nosso método de getProducts, onde nós simplesmente estamos batendo na rota post de products, onde ele retorna pra gente todos os produtos com base nos parâmetros que estamos enviando pra ele. Um detalhe interessante para deixar a nossa camada de service mais limpa é virmos aqui dentro de HTTP, vamos simplesmente criar um novo arquivo aqui chamado product.ts Onde aqui nós vamos pegar todo o nosso objeto que temos, colocar aqui pra dentro, export product request, onde aqui nós vamos simplesmente importar ele. Perfeito. Pra ficar limpa a nossa camada de service. Próximo passo, vamos vir aqui dentro de queries, vamos criar uma nova entidade de queries aqui, no caso as queries referentes a product. Onde aqui dentro nós vamos criar uma use product infinite. .query .ts Aqui vamos simplesmente criar um funcional component por padrão, que se faz const useProductInfiniteQuery Na verdade é um custom hook, onde vamos simplesmente dar um return em um objeto. O próprio ReactQuery disponibiliza para a gente um carinha chamado useInfinityQuery, nós colocarmos aqui import from ReactQuery nosso useInfinityQuery E dentro do useInfinityQuery nós possuímos vários recursos que vão nos auxiliar a realizar esta paginação, esse scroll infinito. Primeiro de tudo, vamos simplesmente realizar uma desestruturação do próprio useInfinityQuery. Ele espera receber como parâmetro um objeto, onde teremos aqui o queryFunction. Assim como nós temos nas nossas mutations, aqui no caso será o nosso getProducts. Hoje nós vamos englobar o nosso getProducts dentro de uma callback assíncrona. Vou colocar aqui async. Onde aqui nós vamos receber como parâmetro pageParam, que vai começar como 1. E aqui vamos fazer um tryCatch, tryCatch. Vamos recuperar o erro. Colocar simplesmente um troll no erro que retornou. Vamos pegar aqui agora const response igual a await getProducts onde aqui vamos passar simplesmente o pagination com o page Sendo o pageParam e o perPage vamos pegar 10 itens por requisição. Maravilha. Próximo passo que precisamos fazer é passar uma outra propriedade obrigatória aqui do nosso useInfiniteQuery aqui no final do fechamento dos colchetes do nosso query function. Vamos pegar o nosso getNextPageParam, onde será uma callback. onde aqui dentro pegaremos o nosso last page, ou seja, a última página que nós recebemos. Vamos dar um return no lastPage.page Se ele for menor que o lastPage.totalPages, ou seja, se a nossa página atual for menor do que a quantidade total de páginas que nós temos, que o nosso próprio back-end retorna, nós vamos simplesmente retornar um lastPage.page mais um, ou seja, vamos pegar a última página que nós buscamos e incrementar um valor na página. Caso contrário, vamos simplesmente mandar um undefined. Ou seja, se a última página que nós buscamos for igual ou maior do que o total pages retornado pela própria API que nós temos aqui, nós vamos simplesmente parar e não executar nada, nós não vamos buscar a próxima página. A próxima propriedade obrigatória que nós temos aqui é o initialPageParam, que vai ser o número 1. Maravilha! Agora, vemos aqui que tem vários erros expocando na tela. Vamos entender o porquê disso estar acontecendo. Acredito que seja porque nós não demos simplesmente um return response aqui no query function. E ficou faltando outra propriedade obrigatória, que no caso é o query key, que é a chave que nós vamos utilizar para criar o cache da nossa requisição HTTP. No caso, vamos colocar aqui um array products, onde vamos receber como parâmetro. alguns itens, vamos colocar futuramente, vamos deixar só isso aqui por enquanto, e perfeito, agora todos aqueles erros pararam de aparecer, temos como erro aqui por enquanto apenas os parâmetros que estamos repassando para o nosso GetProducts, mas isso aqui é o de menos, o que importa é nós entendermos as funcionalidades e o que cada um desses recursos, dessas propriedades aqui faz. anteriormente nós teríamos de controlar a paginação utilizando o próprio estado no componente e isso é muito chato nós temos muita abstração de código aqui dentro como vocês podem ver temos um local onde simplesmente buscamos fazemos a requisição http temos um local onde nós controlamos nós vamos querer buscar ou não a próxima página isso com base na última resposta que ele devolve pra gente, ou seja, supondo que nós vamos fazer a requisição HTTP e ele retorna aqui pra gente que nós possuímos duas páginas, nós anteriormente fizemos a requisição buscando na página 1, logo essa nossa condicional cairia como true, ou seja, a última página é menor do que total pages, o que consequentemente liberaria a execução do nosso getNextPageParam, incrementando o nosso lastPage mais um, ou seja, buscando a última página, e quando nós chegássemos, de fato, na página 2, tendo o parâmetro de totalPage2, esta nossa lógica indicaria que nós chegamos na última página que temos desta rota. Consequentemente, não executando mais nada, isso daqui abstrai bastante coisa, isso aqui é muito legal quando nós formos realizar o request de fato, Agora vamos resolver os erros que temos aqui quando estamos passando os parâmetros para o nosso getProducts. Para nós resolvermos isso, vamos simplesmente vir aqui em getProducts. Na nossa interface de getProductRequest, vamos transformar filters em opcional. e sort também em opcional. Desta forma, vamos simplesmente resolver o problema, até porque, por enquanto, nós não vamos aplicar filtros apenas à paginação na nossa requisição HTTP, então não tem o porquê de nós enviarmos aquelas propriedades. E outra, que elas também não são obrigatórias no nosso back-end. Então, nada mais justo do que nós deixarmos os dados completamente consistentes aqui no nosso front-end também. E por fim, vamos buscar alguns dados, dados de estruturação do nosso useInfinityQuery, como, por exemplo, o data. que será onde ficará armazenado o resultado das nossas requisições, o erro, caso algo de errado aconteça, o fetchNextPage, que é uma função onde nós de fato vamos buscar a próxima página, o is, o hasNextPage, que é literalmente um booleano que irá dizer se possui ou não uma próxima página a ser buscada, teremos aqui também um isFetchingNextPage e o isLoading um, no caso, quando nós estivermos buscando a próxima página e outro quando estivermos fazendo a requisição de fato, um refetch e por fim um isRefetching, que é literalmente uma própria boleana de loading para quando nós estivermos realizando um refetch. Vamos pegar todas essas propriedades que temos aqui e vamos repassar para dentro do return. A aula vai ficando por aqui, para não ficar muito longa. Na próxima aula daremos segmento com o desenvolvimento da nossa listagem de home. Então é isso, espero vocês lá.