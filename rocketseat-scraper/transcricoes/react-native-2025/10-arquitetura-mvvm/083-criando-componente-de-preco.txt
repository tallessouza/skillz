WEBVTT Agora vamos criar um componente global que iremos utilizar em toda a nossa aplicação para renderizarmos os valores. Para isso, vamos vir aqui dentro de src, Shared Components, vamos criar aqui um AppPriceText, onde aqui vamos criar um index.tsx, vamos criar também um useAppPriceTextViewModel.ts, também vamos criar um AppPriceTextViewModel. priceTextView.tsx Maravilha! E nós vamos aplicar a arquitetura MVVM também. Para darmos início, vamos começar criando aqui a nossa camada de ViewModel. export const useAppPriceTextViewModel Vamos simplesmente retornar um objeto vazio por enquanto. Nossa camada de View, export const appPriceTextView vai ser uma função, hoje nós vamos shippar ela, falando que é um Functional Component, que tem o return type, type of useAppPriceViewModel, hoje aqui vamos simplesmente retornar uma View por enquanto, e aqui vamos unir os dois, export cost AppPriceText, Agora aqui nós vamos dar um return, simplesmente no app.priceTextView. Vamos receber aqui também algumas propriedades, no caso dos valores, que vamos renderizar em tela, no caso app.priceTextParams. E será o class nameCurrency, será opcional, será uma string. Também teremos um class nameValue, que será opcional, e um string. E o value, de fato, que será um number. Vamos tipar, geral component, app, price, text, params. Vamos desestruturar o value, class, nameValue, class, nameCurrency. Vamos instanciar a nossa camada de ViewModel. useAppPriceViewModel. No caso, vamos simplesmente pegar aqui, ViewModel. Desta forma, a gente está reclamando de erro, que a gente importou a ViewModel errada, no caso aqui é o useAppPriceTextViewModel. E vamos simplesmente repassar tudo que nós temos via Spread Operator pra nossa camada de View, perfeito. Agora precisamos repassar o Value que nós possuímos aqui pra dentro da nossa camada de ViewModel, então aqui é bem simples, vamos pegar o Value, que será um String, ou melhor, que será um Number. E nós simplesmente vamos passar o nosso Value. Agora precisamos receber o class NameCurrency e o class NameValue aqui da nossa camada de view também, então a gente vai literalmente estender um e objeto string e string string opcional e opcional string a gente simplesmente vem aqui e repassa esses outros valores que estamos recebendo como prop caso que a gente tem que estender depois do fechamento A gente remove aqui, vem pra fora do maior que, e estende aqui desta forma, perfeito. Agora quando a gente vem aqui a gente já consegue passar essa nossa prop className currency e o className value. Agora vamos simplesmente recuperar aqui, desta forma. Agora aqui na nossa camada de ViewModel, nós vamos fazer o seguinte, nós vamos criar algumas constantes aqui dentro, como por exemplo, cost, format, price, na verdade aqui vai ser uma função, onde nós vamos pegar o valor que estamos recebendo no momento, e vamos dar um return, new, ntl, do próprio Javascript, ponto, number, format, pt, traço, br, vírgula, aqui um objeto, style, currency, Currency vai ser brl. E aqui vamos colocar um ponto format vary. Perfeito. Vamos criar aqui também const formattedPrice igual a string formattedPrice No caso, esta é a nossa função que nós criamos aqui em cima. Vamos só pegar esse nosso dentro de format, vamos colocar ele em volta de um number pra ele já fazer a formatação direitinho. Abaixo do nosso format price, vamos criar uma nova constante, currency. Symbol igual a Parts. Vamos pegar aqui const Parts igual a. Nesse Parts vamos pegar o nosso formattedPrice, essa constante que nós criamos, ponto, split e esta colinha que eu vou passar aqui para vocês. Basicamente, quando nós tentamos quebrar a String retornada pelo FormattedPrice, por algum motivo, o JavaScript não está entendendo quando nós passamos desta forma daqui, para nós quebrarmos a String com base nos espaços, porém, desta forma daqui, ele funciona perfeitamente. Então, vamos seguir desta forma. Vamos criar aqui uma outra const. Currency Symbol igual a Parts no índice 0. E outra const, vary text igual a, só corrigir aqui, const parts.slice, um ponto join, Ou seja, vamos unir novamente o array que nós tínhamos, passando novamente esta mesma propriedade aqui em cima. Agora, se simplesmente pegarmos aqui o nosso formatPrice, o recible, valueText e value, e repassarmos no return, voltarmos para a nossa camada de view, onde nós vamos desestruturar todas elas aqui para utilizarmos. Perfeito. Agora aqui na camada de view, vamos criar um text, Vamos importar aqui do React Native, onde nesse primeiro vamos colocar o corrente Symbol. E aqui embaixo um outro text, onde vamos colocar o ValueText. Vamos pegar aqui o nosso componente de AppPriceText, voltar lá no nosso ProductCard, na nossa View Model de Home, em Components, ProductCard, na nossa View, vamos remover esse componente que nós tínhamos aqui anteriormente. Vamos importar, passar aqui as propriedades, no caso o value seria o product.value. Onde ele está reclamando aqui porque o value é uma string e ele está esperando receber um number. Então a gente simplesmente vem aqui, dá um number e converte ele. Perfeito. Outra coisa que nós vamos fazer, voltando aqui na nossa camada de view do nosso appPriceText é colocarmos a estilização, no caso vamos colocar um className, um flexRow. para jogar os itens um na frente do outro, e um Items Baseline. Perfeito. Aqui no Text, vamos colocar aqui uma estilização, o Class Name Currency, ou, uma estilização base de Text SM, com Text Gray na tonalidade de 900. Mesma coisa aqui embaixo, no Text do Veritext de fato, vamos simplesmente colocar um Class Name, onde aqui vamos abrir chaves, class name value, ou um text 2xl, um font bold, e um text gray na tonalidade de 900. Perfeito. No caso, como nós vamos querer uma utilização específica no nosso product card, nós vamos simplesmente voltar aqui e passar da seguinte forma, o nosso class name currency, vamos passar um text small, E no nosso class name-value, vamos simplesmente passar um text-lg pra ele ficar um pouco menor, um font-bold e um flex1. E olha só que legal, finalizamos assim a estilização do nosso componente de valores de preço aqui do nosso app. Espero que vocês tenham gostado. A partir da próxima aula, vamos começar a construção da nossa tela de detalhes de produto. Quando nós clicarmos aqui em um dos nossos produtos, vamos navegar para uma outra tela para podermos ver os comentários e os demais detalhes deste produto específico. Então é isso, espero que vocês tenham gostado da aula e espero vocês na próxima.