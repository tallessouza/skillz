WEBVTT E hoje, finalmente, iremos começar a construção do nosso componente animado de cartão de créditos. Para isso, vamos voltar aqui no Figma para nós entendermos o que nós vamos ter. A princípio, simplesmente, um cartão de crédito com um degradê e os dados que vão ser preenchidos conforme nós colocarmos as informações aqui no nosso formulário. Porém, nós vamos fazer uma animação bem legal. Este cartão, por exemplo, quando nós clicarmos aqui para preencher o CVV, ele vai virar. Ao contrário, para mostrar os dados que nós estamos preenchendo. E para fazer essa animação, vai ser bem bacana. Mas, antes de tudo, precisamos iniciar a estrutura base do nosso componente. Vamos ir aqui dentro de src, ViewModels, Cart, aqui em Components. Aqui no nosso AddCardBottomSheet, vamos criar aqui um novo component. melhor vamos criar aqui uma pasta para componentes e aqui dentro nós vamos criar um credit card onde aqui nós vamos ter um credit card.view.tsx que vai ser a camada de view do nosso cartão de crédito export const credit cardView, hoje aqui vamos simplesmente retornar por enquanto uma View, returnView, vamos ter aqui também a nossa camada de ViewModel, no caso aqui vai ser o useCreditCard.ViewModel.ts, como nós já fizemos sempre quando nós criamos algum componente mais complexo aqui no nosso projeto, até o momento é literalmente a mesma coisa, export const useCreditCard.ViewModel, Onde aqui vamos simplesmente retornar um objeto vazio, já vamos aproveitar, pegar o nosso useCreditCardViewModel, e tipar aqui a nossa camada de view, falando que ele é um Functional Component, que espera receber um return type, typeOf, UseCreditCardViewModel. A gente já conseguiu desestruturar e recuperar os nossos itens daqui. Agora vamos pegar esse nosso Credit Card View. Na verdade, vamos criar aqui um index.tsx e aqui a gente vai simplesmente fazer a injeção dos dados da ViewModel na View Export, Cost, Credit Card. Aqui vamos simplesmente ter um return na nossa Credit Card View. Vamos criar uma constante de ViewModel, onde vamos armazenar o nosso UseCreditCardViewModel, e eu vou dar um Spread Operator aqui na ViewModel, enviando todos os parâmetros para a nossa camada de View. Agora vamos simplesmente pegar este nosso Credit Card, que temos aqui no nosso index, vamos vir aqui dentro do nosso AddCardBottomSheet, logo aqui acima da View, que está englobando todo o nosso formulário. Vamos simplesmente colocá-lo assim, desta forma. A princípio não vai aparecer nada, porque a gente não criou nenhum componente lá dentro, mas se nós colocarmos aqui, por exemplo, um class name contendo um height de 192 pixels, Já vamos criar o espaçamento aqui, porém, vamos deixar para fazer a criação dos componentes internos na próxima aula, pois nós iremos ter uma aulinha bem introdutória referente ao Reactive Learn Method, onde vai ser necessário nós entendermos alguns conceitos básicos antes de darmos seguimento com o desenvolvimento.