WEBVTT Bom, e para darmos início à configuração da nossa funcionalidade de Refresh Token, vamos ter de vir aqui dentro de shared API marketplace. Basicamente aqui dentro onde nós temos a nossa instância do Axios, onde nós exportamos ela para toda a nossa aplicação, todas as requisições HTTP nós fazemos aqui dentro, elas passam aqui por dentro deste interceptor. No caso temos aqui um interceptor para a requisição, no caso a request, onde nós simplesmente estamos pegando o token que temos salvo dentro da store do usuário e aplicamos no reader da requisição. para que dessa forma o nosso app consiga se comunicar com o back-end de uma forma segura, onde lá do lado do back é feita todas as validações. Agora nós vamos criar um outro interceptor. Diferente desse, nós vamos criar um interceptor na response ao invés do request. Então aqui fora do nosso use, vamos criar um this.instance.interceptors.response onde aqui dentro da mesma forma que fizemos aqui em cima vamos chamar o ponto use abrindo parênteses aqui dentro ele espera receber dois parâmetros no caso duas callbacks onde a primeira callback é o caso de sucesso e a segunda callback é o caso de erro então aqui vamos só pegar nossa response Dá simplesmente aqui um return, ou melhor, só repassar o response desse jeito. Aqui, na nossa primeira callback, nós não vamos fazer nada porque pressupõe-se de que se caiu aqui é porque a requisição HTTP retornou status 200, retornou um sucesso. Então, aqui nós não vamos fazer nenhuma validação. Agora, aqui por outro lado, nós temos o nosso error. onde nós iremos fazer a validação de que se essa resposta de erro é referente a um token expirado, no caso se a causa deste erro é porque o nosso token de acesso está expirado de fato ou não. E para validarmos isso é bem simples, é só nós validarmos o status HTTP retornado pelo back-end, se o status HTTP for 401 e a mensagem que o back-end retornou para a gente também for token expirado, nós temos certeza de que o token que tinha na sessão atual do usuário foi expirado de fato. Então, é de suma importância vocês entenderem a forma que o back-end que vocês estão consumindo de fato funciona. Às vezes, eles utilizam apenas o status e não usam uma mensagem, ou às vezes é um status diferente com uma mensagem diferente. Então, é muito importante nós sabermos desse tipo de detalhe para a gente saber como lidar também. Primeiro passo, vamos criar aqui uma const original request, vai ser igual a um erro.config E logo aqui abaixo vamos fazer um if, uma validação, onde aqui dentro nós vamos verificar se o error.response.status é igual a 401. Vamos colocar aqui um ponto de interrogação neste status, porque não sabemos se ele de fato vai vir dentro do objeto de response. Vamos fazer uma segunda validação, errei. Opa, dois if comerciais. caso erro.response.data.message for igual a token expirado caso aqui vão ser três iguais e aqui nós vamos também novamente colocar ponto de interrogação que caso não exista este data dentro da response ou até mesmo esta message dentro do data nossa aplicação não vai quebrar e por fim a última validação que nós vamos fazer aqui não sei se vocês lembram da propriedade que tínhamos criado de isRefreshing aqui dentro da nossa classe, nós vamos simplesmente vir aqui dentro do nosso if e fazer uma exclamação, no caso uma negação, this.isRefreshing. Faltou colocar só os dois is comerciais aqui após a segunda validação, no caso aqui do token, e perfeito. Apenas se todas essas condições forem satisfeitas, nós vamos cair dentro da execução desse if. Esse refreshing é literalmente para impedir requisições paralelas com a mesma funcionalidade. No caso, quando nós começarmos a revalidação do token, nós vamos setar esse carinha daqui para true, essa nossa variável. Então, a primeira coisa que nós vamos fazer aqui é o this.isRefreshing para true. Então, supondo que estejam acontecendo duas requisições HTTP paralelamente e as duas quebrem, apenas uma delas vai cair aqui dentro desta nossa condição do if, desta forma impedindo, assim, que novamente seja feita duas requisições paralelas de revalidação de token. Não faz tanto sentido, assim, uma das duas ia acabar falhando. Perfeito. O próximo passo que nós vamos fazer é simplesmente colocar um try catch aqui dentro. Try. Catch. Vamos recuperar o nosso erro. Vamos pôr aqui também um Finally. E aqui dentro nós vamos ter de recuperar os dados da Store do usuário da mesma forma que fizemos no nosso InterceptedRequest. No caso, nós vamos literalmente copiar este objeto que temos aqui em cima. que, no caso, ficou faltando só transformar esta callback em uma callback assíncrona, em uma função assíncrona. Perfeito, para dessa forma a gente conseguir usar o await. E lembrando que este asyncStorage é o que nós estamos utilizando no provider do Zustand para persistirmos os dados da sessão do usuário. Então, independente do local em que nós estejamos, se a gente fizer desta forma, a gente consegue recuperar Também um token do usuário que está salvo lá na nossa Store, então é interessante, nós sabemos desse tipo de informação. Próxima coisa que nós vamos fazer aqui é uma validação IF. Se UserData não existir, nós vamos simplesmente dar um Throw. New. Error. Manda uma mensagem. Usuário não autenticado. porque se de fato nós não possuímos o UserData, é porque de fato nós não temos nada da sessão do usuário salvo aqui dentro. Próxima validação que nós vamos fazer, ou melhor, o próximo passo, vamos realizar a desestruturação do json.parse do nosso UserData. Lembrando que este método de getItem retorna pra gente um json, ou seja, uma string, e esse json.parse transforma toda esta string em um objeto JavaScript, onde aqui dentro nós vamos recuperar o state, E aqui de dentro vamos recuperar o refresh token. Lembrando que aqui em cima nós tínhamos recuperado o token. JWT para nós repassarmos para a nossa requisição HTTP. Eu vou até remover esse console log daqui. Aqui, no caso, nós vamos recuperar o nosso Refresh Token para nós, de fato, batermos lá no nosso back-end, na nossa rota de revalidação. E aqui, abaixo, vamos fazer só um IF. Se o Refresh Token não existir, vamos dar um Throw. New. Error. Refresh Token não encontrado. Maravilha. Próxima coisa que vamos fazer aqui é simplesmente uma requisição HTTP lá para a nossa rota. Se nós voltarmos rapidinho lá na nossa documentação, podemos ver que é esta rota aqui de barra alf barra refresh, onde nós vamos enviar um body para ela contendo o nosso refresh token e ela irá retornar para a gente literalmente um par de tokens novos, um par de token e de refresh token, para que dessa forma o usuário possa ficar sempre nesse ciclo. Então vamos voltar lá para o nosso código. Aqui nós vamos fazer uma requisição HTTP direto, vamos simplesmente colocar um const response igual a await this.instance.post no barra alf barra refresh onde aqui vamos simplesmente passar um objeto como parâmetro enviando o nosso refresh token. Vamos só desestruturar aqui a resposta, no caso vamos pegar aqui do data Agora o data nós vamos desestruturar, vamos pegar aqui o nosso token. E o refresh token. Maravilha. Olha só, ele tá dando conflito aqui, então vamos fazer o seguinte, vamos manter desta forma daqui ó. Vamos só colocar um alias nesse data de response. Pra não ficar dando conflito com o nome da variável retornada da nossa API com a que nós já temos. Agora, aqui embaixo nós vamos simplesmente fazer o seguinte, const currentUserData, vamos só dar um json.parse no nosso userData, onde aqui embaixo nós vamos atualizar as propriedades de token e de refreshToken, no caso aqui nós vamos simplesmente colocar um currentUserData.state.token vai ser igual a response.token E aqui embaixo vai ser o CurrentUserData.State.RefreshToken igual a Response.RefreshToken Dessa forma nós temos um objeto persistindo todos os dados que temos lá na nossa Store do Zustant, porém apenas com a atualização de que agora nós possuímos o nosso Token e o nosso Refresh Token aqui dentro, no caso nós simplesmente alteramos os valores de Token e de Refresh Token aqui dentro do nosso objeto para ser igual ao que foi retornado para nós lá na nossa API. Próximo passo, nós vamos ter de setar os dados do usuário, novamente, lá no nosso SyncStorage. Então, vamos só colocar um await a SyncStorage.setItem, onde, no primeiro parâmetro, vamos passar a chave de onde nós estamos armazenando esses nossos dados. No caso, é o MarketplaceAuth. E o segundo parâmetro, vamos passar um json.stringify do nosso CurrentUserData. Novamente, vamos converter o nosso objeto JavaScript em uma String e salvar lá no nosso AsyncStorage, para que dessa forma, quando o nosso Zustand for reitratado, no caso, quando os dados do AsyncStorage forem utilizados para popular a Store do Zustand, já automaticamente vai estar tudo 100% atualizado. Agora aqui, vamos simplesmente pegar um original request. .readers.authorization igual a, vamos pegar aqui uma template string concatenando, o better, opa, better, cifrão, response, ponto, token. Dessa forma garantimos que nós já atualizamos automaticamente a instância do Axios com o token mais atual que nós possuímos e aqui embaixo vamos simplesmente dar um return this.instance onde nós vamos enviar o nosso original request. Desta forma nós garantimos que 100% do tempo nós vamos estar utilizando o nosso token atualizado, no caso o nosso token recém-gerado lá na API. Agora aqui no catch nós vamos fazer o seguinte, vamos simplesmente dar um return prompts.reject enviando um new error mandando a mensagem sessão expirada. Passa o login novamente. Perfeito, agora aqui no final vamos simplesmente pegar o this.isRefreshing e setar de volta para false. Lembrando que o finally é executado independente da nossa promessa ter sido cumprida ou não, no caso da nossa requisição e de toda essa tratativa ter funcionado ou não, ele vai cair aqui e vai resetar o isRefreshing de volta para a falsa. Agora, logo aqui abaixo do nosso if, eu vou até diminuir ele aqui para ficar mais fácil de nós conseguimos ver, ainda dentro da nossa callback de erro, nós vamos fazer uma outra tratativa, simplesmente vamos colocar se erro. Opa, aqui eu tô fora da função, isso agora eu tô dentro da callback if error.response e error.response.data se eles de fato existirem nós vamos simplesmente repassar aqui um return ramses.reject mandando um new error enviando o nosso erro.response.data.message. Vamos repassar. A mensagem retornada lá do nosso back-end para a nossa classe global de erro do JavaScript. Agora, caso contrário, ou seja, caso o erro que nós tomamos lá no nosso back-end não tenha sido tratado, ou seja, caso ele não possua uma mensagem, nós vamos fazer literalmente a mesma coisa, só que com uma mensagem genérica de erro. Vou colocar aqui, falha na requisição. Dessa forma, nós garantimos que sempre vamos ter uma tratativa de erro independente dele estar sendo tratado no back-end ou não. A partir da próxima aula, nós vamos lidar com o caso de que se toda essa nossa operação caia dentro deste catch, nós garantirmos que o usuário vai estar desautenticado de fato, ou seja, nós vamos trazer o usuário de volta aqui para a nossa tela de login. Então é isso, espero que vocês tenham gostado da aula e espero vocês na próxima.