WEBVTT Vamos dar seguimento com o desenvolvimento da ViewModel da nossa tela de login. Vamos começar criando uma função onde englobaremos o nosso handleSubmit que será disparada quando submetermos o nosso formulário. Para isso vamos criar aqui um const LoginMutation onde nós vamos chamar o nosso useLoginMutation que criamos na aula passada. Também vamos criar uma função const OnSubmit onde aqui dentro teremos um handleSubmit, ou seja, o handleSubmit que está sendo retornado de dentro do nosso useForm. Aqui dentro ele tem uma função de callback, onde dentro dessa função de callback ele retorna todos os dados do usuário que nós temos do nosso formulário. Então vamos simplesmente abstrair aqui no userData. Melhor, UseFormData, tanto que se nós passarmos o mouse em cima desse UseFormData, podemos ver que ele tem literalmente o mesmo tipo do nosso UseForm, no caso esse LoginFormData, com e-mail e com password. E aqui dentro, vamos transformar essa callback em uma callback assíncrona. E aqui vamos colocar um await loginMutation.mutateAsync. Ou seja, estamos disparando a requisição HTTP que nós configuramos na nossa aula passada dentro do nosso useLoginMutation, no caso, literalmente, o que está dentro deste mutationFn, ou seja, mutation function que está sendo disparado. E vamos repassar agora como parâmetro o nosso useFormData. Vamos pegar o onSubmit, repassar para dentro do nosso return, Vamos recuperar o onSubmit na nossa camada de View. Se nós chegarmos aqui, podemos ver que ele já irá retornar para a gente automaticamente o onSubmit, pelo fato de nós estarmos tipando a nossa camada de View com o return type do useLoginViewModel. Aqui dentro nós vamos simplesmente criar um botão aqui em cima, um touchable, opacity qualquer, com um texto. de login, aqui dentro vamos chamar o onPress, passando o onSubmit. Detalhe interessante, se nós clicarmos no onSubmit no momento, ele irá exibir as mensagens de erro do nosso formulário, até porque nós não preenchemos nenhum dado dele ainda. Agora, se nós colocarmos aqui teste arroba gmail.com E a senha 1, 2, 3, 1, 2, 3, 1, 2, 3 e clicarmos em Login, o nosso Login irá acontecer de fato, porém nós recebemos aqui um status 404, até porque nós não temos nenhum usuário com essas credenciais cadastradas de fato. Agora, vamos fazer uma melhoria. Vamos voltar aqui na nossa UseLoginViewModel. Vamos fazer o seguinte, vamos armazenar o retorno do nosso LoginMutation em uma variável, Vamos dar simplesmente um console.log aqui dentro, por enquanto, user data. Vamos vir aqui na tela de registro, eu vou até desabilitar o teclado do próprio emulador, no caso eu vou utilizar o do próprio hardware, no caso do próprio computador meu. Vamos cadastrar um usuário novo, vamos colocar aqui Breno. Telefone, qualquer um. O e-mail de Breno, 123, arroba gmail.com. A senha de 123, 123, 123. E para confirmar a senha, 1, 2, 3, 1, 2, 3, 1, 2, 3. Vamos registrar o nosso usuário. Temos aqui um console.log que o nosso registro foi feito de fato. Se nós voltarmos aqui na nossa tela de login e colocarmos o mesmo e-mail, Breno. 1, 2, 3, arroba gmail.com e a senha 1, 2, 3, 1, 2, 3, 1, 2, 3 e clicarmos em login, podemos ver que a autenticação foi feita com sucesso, o nosso console.log foi executado, ele bateu lá no nosso back-end e ele retornou pra gente os dados do nosso usuário. Olha só que coisa bacana! Com isso daqui, nós já podemos dar segmento com o desenvolvimento do nosso app. Na próxima aula, nós vamos salvar os dados do usuário na Store do Zustand. Para que dessa forma, todos os dados fiquem persistidos e os dados do usuário possam ser acessados de qualquer componente do nosso aplicativo. É isso, espero que tenham gostado da aula e espero vocês na próxima.