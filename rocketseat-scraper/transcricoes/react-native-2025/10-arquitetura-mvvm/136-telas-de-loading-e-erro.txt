WEBVTT Nesta aula vamos construir um componente para caso ocorra algum erro na busca aqui das nossas listagens de ordens. Para isso, vamos seguir literalmente o mesmo padrão dentro da nossa pasta de componentes de ordens da nossa ViewModel. Vamos simplesmente vir aqui e criar um... Error. Onde aqui, novamente, vamos criar um index.tsx, colocar aqui um export const... Error. Esse é simplesmente um Functional Component, hoje vamos simplesmente colocar aqui uma View e um Text por enquanto. Falha ao carregar pedidos. Vamos simplesmente pegar aqui o nosso error. Vamos voltar na nossa camada de ViewModel aqui de UseOddOrders. Na desestruturação do nosso Custom Hook da Query, onde estamos consultando, vamos pegar aqui o error e vamos repassar aqui pra dentro desta forma. Agora nós conseguimos recuperar o erro na nossa camada de view e aqui em cima simplesmente colocamos um if, erro, simplesmente vamos dar um return em, erro, aqui dos nossos componentes dessa maneira. Podemos fazer até tudo inline mesmo, em uma única linha, só falta colocar o return aqui. Vai ficar mais limpo. E dessa forma, vamos só inverter a condicional para que possamos ver a nossa tela de erro. Vamos colocar aqui um className na Vue.py com um flex1, um itemCenter e um justFileCenter também. Desta forma, vamos colocar aqui um className contendo um textXL. Para aumentar um pouco aqui o tamanho do nosso texto, um text. na tonalidade 600, e aqui acima vamos colocar um IonIcons com um name de alert, opa, name alert, desta forma, onde aqui a gente vai englobar esses dois views em uma única view, Já que aqui vamos colocar um className com flexRow para jogar todos os itens um na frente do outro. Um itemCenter, um justFileCenter também para centralizar tudo. Vamos só jogar esse IonIcons pra baixo. E aqui eu tinha até colocado errado. Na verdade, vamos colocar aqui o textDanger dessa forma. E aqui no color vai ser a mesma coisa. Vamos chamar nossa variável de colors.danger. E aqui vamos colocar um size de 23. desta maneira aqui embaixo vamos só colocar um appbutton onde aqui dentro nós vamos colocar simplesmente um voltar para produtos e aqui na viewpy vamos só colocar um px de 6 No nosso AppButton, um className com margem top de 6 também, desta forma. E só um ajustezinho aqui no nosso VectorIcons, vamos pegar ele e colocar aqui fora dessa view. Vamos melhorar aqui essa interface, vamos englobar uma view em volta dele. Vamos ver o nosso IonIcons pra cá, onde aqui dentro nós teremos um Width de 10, um Height também de 10, vamos ter aqui um BG, Red na tonalidade 300, dessa forma, um ItemCenter, um JustifyCenter também, um RoundedFull, desta forma, e um MarginBottom de 6. E aqui no text onde nós tínhamos colocado o text Danger, vamos só colocar um font bold. Desta maneira. Perfeito. Com isso é só uma tela informando para o usuário que ocorreu alguma falha para carregar os pedidos dele. Agora é só a gente vir aqui e inverter essa condicional, dessa forma. E já aproveitando o embalo, vamos aproveitar aqui e criar também uma tela de loading, no caso aqui vai ser o loading. Vamos criar aqui um index.tsx export cost loading. Vamos simplesmente retornar aqui um functional component. Um text. carregando produtos, ou melhor, aqui na verdade é carregando pedidos, três pontinhos, e aqui em cima um activity, opa, activity indicator, onde aqui teremos um styles large, vamos ter um color, chamar nossa variável de colors no purple base, aqui no text vamos por aqui um class name text-grey600 no margin top de 4 e aqui na view simplesmente um class name com flex 1 para ocupar 100% da tela item center justify center também E agora é só pegar o nosso loading, vir aqui no nosso useOrdersView, vamos desestruturar aqui do nosso useGetOrderSquare, o isLoading, repassar pra cá, isLoading, recuperando a nossa camada de view, e colocar aqui em cima do if de erro, if isLoadingForTrue, simplesmente vamos retornar o nosso Loading. Desta maneira aqui. Vamos só inverter a condicional pra gente ver como que tá a nossa tela. E perfeito, já está funcionando perfeitamente aqui as nossas condicionais de erro e de loading também. Com isso finalizamos a nossa tela de pedidos. Na próxima aula nós vamos iniciar a construção da nossa tela de perfil de usuário, para que possamos atualizar, por exemplo, a foto de perfil, o nome e o e-mail do usuário. Então, a gente se espera em vocês na próxima aula.