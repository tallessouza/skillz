WEBVTT Na aula passada nós criamos a requisição HTTP responsável por realizar o upload da nossa foto de perfil no momento do registro do usuário e na aula de hoje daremos seguimento com a integração desta nossa rota. Se nós vimos na documentação da nossa API, lembrando que temos que estar com o nosso backend rodando. Nós viemos aqui na nossa rota de user avatar. É necessário nós estarmos autenticados antes de enviarmos a foto de perfil do user, até porque o back-end vai identificar de qual foto de perfil pertence aquele usuário através do token que nós estamos enviando, através do nosso reader. E atualmente, quando nós realizamos o login ou realizamos o cadastro, de fato, nós não estamos setando o headers da nossa instância de Marketplace API Client com o nosso token. E para resolver isso, vai ser bem interessante, vai ser de uma forma bem legal. Partindo do princípio que nós temos uma classe que representa a nossa instância do Axios responsável por fazer a comunicação com o nosso back-end do Marketplace. nós vamos criar um método aqui dentro responsável por fazer o setup dos interceptors, ou seja, por fazer a configuração. Para isso, nós vamos criar uma função privada, um método privado, private setupInterceptors() Hoje nós vamos fazer da seguinte forma, simplesmente pegando o this.instance.interceptors.request.use O que isso significa? Estamos pegando a instância que temos dentro do nosso próprio objeto e vamos pegar um interceptador das requisições. O que isso significa? Toda vez que nós realizarmos uma requisição HTTP do nosso aplicativo, independente da rota que seja, seja ela para autenticar ou seja simplesmente para buscar os produtos que temos para simplesmente exibirmos em tela, nós vamos executar uma callback que temos aqui dentro deste use que ele espera receber como parâmetro para que desta forma nós possamos alterar algumas propriedades da nossa instância antes de que o request de fato seja feito. É um conceito muito parecido com os mirrors do back-end onde nós colocamos algumas etapas antes da nossa requisição HTTP chegar na camada de controller de fato, aqui no caso Nós vamos interceptar a nossa requisição HTTP para nós inputarmos no reader o nosso Barret token. Ele tá reclamando aqui porque esta callback precisa ser assíncrona e nós precisamos retornar pra ele a config que a própria callback retorna pra gente, no caso aqui a config. E aqui dentro nós vamos fazer o seguinte, vamos colocar aqui um const userData igual a await asyncStorage, importando lá da nossa biblioteca de asyncStorage que nós instalamos várias aulas passadas quando nós configuramos a questão do gerenciamento de estado global do nosso app, no caso o react-native-asyncStorage. Vamos pegar o .getItems onde aqui dentro nós vamos passar a chave que nós setamos lá na nossa UserStore. Então, vamos navegar aqui dentro de src, shared, store, UserStore. Aqui dentro nós possuímos a propriedade de name nas configurações da persistência dos dados. Lembrando que nós já estamos persistindo todos os dados do usuário, ou seja, toda a seção que o usuário possui dentro do provider do AsyncStorage. Dessa forma, toda vez que nós abrirmos o nosso aplicativo, irá acontecer um rehydrate dos dados, dos parâmetros da nossa Store Global do Zustage, onde ele irá pegar todos os dados que estão aqui dentro da localStorage e setar novamente, para que dessa forma não haja necessidade de a gente ficar tendo de logar o usuário toda vez que ele abre o aplicativo, isso é uma experiência de usuário péssima. O que nós precisamos fazer é pegar este name daqui, e colocar dentro do getItem para que desta forma nós também possamos ter acesso a todas as propriedades salvas e persistidas do usuário. Nós temos certeza que todos os dados vão estar lá pelo simples fato de que quando nós executamos qualquer um dos nossos métodos de login ou do nosso método de register, Nós automaticamente já setamos os dados do usuário na nossa Store, se nós voltarmos aqui na nossa ViewModel, na tela de login, por exemplo, se nós viemos aqui na nossa uselogin.viewmodel e abrimos aqui o nosso LoginMutation. o nosso useLoginMutation, podemos ver que estamos pegando a função de setSession do nosso CustomHook do UserStore, para que dessa forma nós já citarmos os dados do usuário quando nós realizarmos a autenticação de fato, e todos esses dados podem ser simplesmente buscados novamente aqui dentro do nosso Interceptors utilizando o AsyncStorage, isso daqui é algo bem legal. Nós temos esta compreensão de que nós temos acesso a todos os dados dentro da nossa Store do Zustand, que foram persistidos em outros locais, que não necessariamente são componentes React. Com isso daqui em mãos, nós temos de fazer uma validação, if, e o UserData de fato existir. Nós vamos fazer uma desestruturação, const, igual a. Vamos fazer um JSON, ponto parse, passando o UserData, lembrando que o método de GetItem retorna pra gente uma Prompt de String, ou seja, ele não retorna pra gente de fato um objeto JavaScript e um JSON em formato de String, onde nós temos que fazer a conversão para um objeto JavaScript de fato, e aqui dentro nós vamos ter acesso a todos os dados do usuário. E aqui na desestruturação nós vamos simplesmente pegar State Vamos desestruturar novamente porque state é onde está literalmente todo o nosso estado de marketplace off. Hoje nós vamos simplesmente vir aqui e recuperar o token do usuário. Perfeito, vamos colocar aqui simplesmente um console.log no token para nós podermos ter um vislumbre se de fato estamos conseguindo recuperar o token do usuário ou não. Próximo passo é se o token de fato existir, nós vamos simplesmente chegar no config o mesmo config que a própria callback do .use da própria instância retorna pra gente .readers.authorization com o A maiúsculo igual a, vamos colocar aqui uma template string pra gente concatenar o better cifrão chaves token Dessa forma, nós temos certeza que toda vez que nós realizarmos uma requisição HTTP e nós termos os dados do usuário na sessão, nós vamos simplesmente setar o cabeçalho da nossa requisição com o token de autenticação do usuário, o que consequentemente, ao realizar o cadastro do nosso usuário, nós vamos chamar a rota que faz o upload do da foto de perfil e no momento dessa chamada nós já vamos ter setado aqui o nosso better token e por fim nós vamos simplesmente vir abaixo de todos esses ifs aqui e dar um return em config para que dessa forma o use pare de reclamar um erro que ele obrigatoriamente espera que nós retornemos aqui o config como parâmetro E um outro detalhe interessante é que aqui dentro do nosso user podemos colocar uma outra callback como parâmetro aqui logo no final do fechamento das chaves. Se nós colocarmos aqui uma vírgula, podemos simplesmente abrir uma outra callback e esta callback possui um erro. Ou seja, se ocorrer algum erro no momento da execução do nosso interceptor, nós vamos simplesmente cair aqui dentro, colocar simplesmente um return promise.reject enviando o próprio erro. que ele já nos entrega aqui como parâmetro. E olha só, perfeito! Agora, tudo que precisamos fazer é pegar o método que nós criamos e chamarmos dentro da nossa função construtora. Voltando aqui para os conceitos de programação orientada a objetos, a função construtora é a função que é executada quando nós instanciamos a nossa classe, ou seja, como aqui embaixo nós estamos exportando a instância da classe de Marketplace, ele sempre irá executar o construtor, ou seja, teremos de simplesmente vir aqui em this. Setup Interceptors. E com isso, teoricamente, já seria o suficiente para nós adicionarmos o nosso Better Token na nossa requisição HTTP nos nossos readers. Vamos fazer um teste? Vamos fazer o seguinte, vamos colocar um console.log aqui no Userdata também. Para nós termos certeza que, de fato, estamos caindo aqui dentro. Próximo passo, nós vamos vir aqui em Registro. Vamos simplesmente cadastrar um novo usuário aqui, colocar qualquer senha. mynewuser arroba gmail.com telefone qualquer um new user Quando nós clicarmos em registrar, nosso usuário foi registrado. Olha só que legal! E quando nós clicamos novamente no nosso botão de registrar, podemos ver o log que colocamos aqui na nossa instância do Axios. Podemos ver que ele retorna pra gente o state contendo todos os dados do usuário, incluindo o nosso Bearer Token. Ele retornou um erro, até porque este nosso usuário já está cadastrado, então já era esperado este erro que ele retornou pra gente. Porém, olha só que legal, agora nós temos certeza que temos o nosso token já cadastrado, tanto que ele logou duas vezes dentro do objeto, que foi o primeiro console.log que nós colocamos, e o segundo console.log que foi simplesmente o token sozinho. Com isso, podemos seguir com o desenvolvimento aqui da nossa tela de registro. Na próxima aula, criaremos a nossa mutation responsável por realizar a comunicação entre a nossa camada de ViewModel e a nossa camada de Service, de fato. Então é isso, espero vocês na próxima aula.