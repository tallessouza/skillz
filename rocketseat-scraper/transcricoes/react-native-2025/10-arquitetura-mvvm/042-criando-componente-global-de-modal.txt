WEBVTT Na aula de hoje, iremos criar o nosso componente genérico de modal, que será utilizado em toda a nossa aplicação. Para isso, vamos vir aqui dentro de src.shared-component. Vamos criar aqui, da mesma forma que temos o nosso app-button e o nosso app-input, vamos criar o nosso app-modal. E aqui dentro, vamos criar um index.tsx. Aqui dentro, um funcional componente padrão, app-modal. E aqui dentro nós vamos simplesmente retornar o componente de modal do próprio react-native. Próximo passo que nós vamos fazer é simplesmente recuperar os dados que vêm lá da nossa useModalStore. Onde aqui dentro vamos pegar o nosso isOpen, content, config e o close, perfeito. Próximo passo vamos simplesmente colocar uma condicional if the isOpen for falso ou não tiver conteúdo nenhum, content, simplesmente vamos retornar aqui, return null. Vamos barrar a renderização do nosso componente de modal. Caso contrário, nós vamos renderizá-lo de fato. Primeira coisa que nós vamos fazer é nas propriedades de Visible, passar o isOpen. Na propriedade de AnimationType, vamos passar aqui o config.animation Na propriedade de transparent, vai ser o config, vou só quebrar a linha aqui, vai ser o config.transparent, na de status bar translucent, mesma coisa, vai ser literalmente o config.statusbartranslucent, e onRequestClose vai ser a nossa função de close. Agora dentro do nosso modal, vamos colocar o touchable, withAllFeedback, onde aqui dentro colocaremos dentro da propriedade de onPress ou Close. E aqui dentro vamos pôr uma View contendo um className, um flex1, um bgBlack barra 50, ou seja, vai ser um preto com uma transparência, um justifyCenter, um itemCenter, e um PX de 6. E aqui dentro desta view que nós vamos colocar o nosso content. Um próximo detalhe que nós vamos fazer é, dentro da nossa view, englobando o nosso conteúdo, vamos colocar um outro toastable width ou feedback pelo simples fato de que, se nós não colocarmos um outro toastable width ou feedback aqui em volta do nosso conteúdo, sempre que nós clicarmos dentro do modal que está sendo renderizado, ele irá fechar. Então, para evitar esse disparo dessa função do onPress, nós simplesmente vamos englobar um toastable width ou feedback aqui dentro. passando o conteúdo, e dentro do onPress, simplesmente vamos executar uma callback, onde nós não vamos fazer nada. Dessa forma, prevenindo o disparo do evento de onPress, que de fato executa o close. Agora nós vamos pegar o nosso appModal, voltar aqui dentro do nosso layout, dentro do queryClientProvider, vamos passar ele aqui dentro do nosso root, para que dessa forma ele fique disponível em toda a nossa aplicação. Agora, aqui dentro do nosso modal store, vamos só colocar o isOpen aqui como true, que ficou faltando nós colocarmos na aula passada, e aí quando nós chamarmos o open e setarmos a nossa variável, o nosso conteúdo para true, nós vamos sair dessa condicional, colocar o visible como true e de fato renderizar o conteúdo do nosso modal para a nossa aplicação. Na próxima aula nós iremos criar um custom hook onde nós vamos delegar essa responsabilidade. Voltando aqui para a arquitetura MVVM, nós vamos ter a camada de modal onde ficará toda a nossa regra de negócio e este nosso custom hook fará parte dela. Onde nós simplesmente iremos chamar em qualquer lugar da nossa aplicação e simplesmente passarmos o componente para ele ser renderizado. Então é isso, espero que tenham gostado da aula e espero vocês na próxima.