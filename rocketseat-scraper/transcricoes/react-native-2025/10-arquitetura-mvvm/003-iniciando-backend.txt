WEBVTT Fala Devi! Hoje vou mostrar para vocês como inicializarmos a API do nosso aplicativo diretamente do nosso computador. Assim como fizemos no módulo passado, eu vou disponibilizar para vocês o link do repositório onde vocês vão vir aqui em code HTTPS. Vamos colar o link que nós temos aqui, abrir o nosso terminal. Terminal. Deixa eu navegar aqui para Documents. Vamos colocar um git clone e vamos dar um ctrl-v na url que nós copiamos aqui, pra ele assim começar o processo de clonagem, basicamente baixar o repositório remoto aqui na nossa máquina. Show! Vamos navegar pra dentro do nosso diretório, cd marketplace-backend e dar um colo de ponto pra abrirmos aqui o nosso VS Code. Deixa eu só jogar ele aqui pra outra tela. E maravilha! Aqui dentro do próprio repositório, no próprio GitHub, podemos ver um arquivo de Readme onde temos um passo a passo de como podemos estartar a nossa aplicação. Então, basicamente, se nós seguirmos aqui, vai dar tudo certo. Primeiro passo nós fizemos, clonamos o nosso repositório. O segundo, utilizar o Yarn ou o npm. Nesse caso daqui, eu vou utilizar o npm mesmo, já que nós já temos um package lock.json aqui dentro do nosso repositório. Vamos rodar o npm i para baixar todas as dependências. Show demais. Próximo passo que nós vimos aqui, vamos rodar o comando yarn migration run ou npm. Então, vamos voltar aqui. Vamos dar um zoom aqui só pra vocês conseguirem enxergar melhor. Deixa eu limpar aqui o terminal. Vamos rodar o npm. run migration 2.run. Este é o script que temos aqui dentro da nossa package JSON, onde nós executamos o type ORM para inicializarmos todas as migrations aqui da nossa aplicação. Então, vamos rodar aqui o npm run migration run e show de bola! Todas as nossas migrations foram criadas com sucesso. Agora, para termos certeza que o nosso banco de dados foi de fato criado, vamos ir aqui dentro de src infra database type ORM marketplace. Temos aqui o nosso database.sqlite. É basicamente um banco de dados local que vamos rodar aqui dentro da nossa aplicação para facilitar as coisas. Não tem necessidade de ficar subindo container docker ou subir algum banco de dados no servidor. Vamos utilizar tudo local aqui mesmo. Vamos olhar aqui o próximo passo do nosso tutorial e é simplesmente rodar um npm run dev. Vamos rodar aqui o npm run dev. E show de bola! Com isso, todas as seeders que populam o banco de dados vão ser rodadas e o nosso projeto está ativo na porta 3001. Então, se nós viermos aqui no nosso navegador e acessarmos localhost 3001-docs, vamos poder ver toda a documentação da nossa API. Essa documentação vai ser de suma importância para nós no desenvolvimento do nosso aplicativo. assim como nós fizemos no módulo anterior, onde nós ficamos o tempo todo consumindo a documentação da nossa API para nós sabermos o input e o output das nossas rotas, ou seja, basicamente, quais parâmetros nós precisamos enviar, seja através de body ou através de parâmetros, mesmo na própria URL, e o que essas rotas retornam para a gente, os possíveis status de erro para nós tratarmos no nosso front-end, no caso, no nosso aplicativo. Com isso, já temos o terreno pronto para darmos início à configuração e desenvolvimento do nosso app. O link do repositório vai estar aí, abaixo na descrição da aula. E é isso, espero vocês na próxima aula.