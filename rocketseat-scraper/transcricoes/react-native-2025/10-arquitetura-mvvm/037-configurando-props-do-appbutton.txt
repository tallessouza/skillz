WEBVTT Dando seguimento com o desenvolvimento do nosso componente genérico de botão, vamos adicionar algumas propriedades customizadas, ou seja, propriedades que não existem naturalmente em um Touchable Opacity no React Native. Novamente, voltando aqui no princípio de criar componentes que podem ser reutilizados em diversos contextos diferentes, e voltando aqui no Figma, É sempre interessante nós analisarmos os elementos presentes no componente para nós sabermos o que necessariamente vamos ter de enviar para ele. Logo de cara podemos ver que ele poderá ter um input, ou seja, teremos de enviar via props qual input que deverá ser renderizado no momento. Logicamente, também teremos o texto que será renderizado. Nós vamos fazer isso aqui de uma forma bem interessante. Eu vou ensinar vocês a fazerem como se fosse literalmente um componente de button sem a necessidade de colocar um text dentro dele para renderizar um texto. E, além disso, vamos ter a variação. No caso, qual variant que ele vai estar utilizando, se é com o background preenchido ou se é com o background limpo, apenas com a borda. Então, para isso, vamos começar mapeando as propriedades logo de cara. Vamos colocar aqui um left icon. que será opcional, e nele nós vamos colocar, da mesma forma que colocamos aqui no AppInput utilizando o glyphMap do nosso IonIcons, então já vamos importar ele, import from, arroba, export, barra, vector icons, o nosso IonIcons, com o keyOf, ou seja, as chaves do tipo typeOf IonIcons.glyphMap. Dessa forma, Se nós voltarmos aqui no nosso login.vue, onde nós estamos utilizando o nosso App Input, se nós colocarmos o Left Icon, ele já vai automaticamente sugerir todos os ícones que tem disponível dentro do Ion Icons para nós utilizarmos, dessa forma, otimizando o TypeScript. Não tem a necessidade de a gente ficar perdido procurando qual ícone nós vamos querer renderizar. O próximo que nós vamos colocar são os Children, ou seja, Literalmente todos os componentes, todos os elementos que forem englobados pelo nosso App Input, no caso aqui vamos fazer algo diferente. Diferente de outros componentes que recebem Children, que no caso são React Nodes, aqui nós vamos simplesmente receber uma String como Children, pelo simples fato de que quando nós utilizamos um touchable opacity, nós obrigatoriamente precisamos passar um componente, um elemento filho pra dentro dele, nós não podemos simplesmente chegar e passar o texto desta forma, que ele vai retornar um erro pra gente falando que strings tem que estar dentro do elemento de text, então vamos simplesmente abstrair isto pra dentro do nosso componente, pra que quando nós formos utilizar, de fato, o nosso app-button não tenha necessidade de nós fazermos dessa forma, de nós englobarmos aqui dentro, colocar um outro text, dessa forma deixando menos verboso a utilização desse nosso componente. Vamos só dar um ctrl-z, reverter, e voltar pra lá. Agora vamos simplesmente desestruturar o que nós estamos recebendo, no caso o left icon e o nosso children. Lembrando sempre, vamos colocar isso antes do nosso left operator. Aqui dentro do text vamos colocar o children, no caso seria o text e vamos reservar para nós utilizarmos o left icon em outros locais agora quando nós voltarmos aqui dentro do login view ele vai reclamar de erro porque children é obrigatório porém nós não vamos simplesmente passar o children dessa forma poderia muito bem ser utilizado desta maneira por exemplo colocando aqui teste Ele já vai ser renderizado, porém nós podemos muito bem seguir uma outra sintaxe, que é simplesmente abrir e fechar e englobar um texto aqui dentro, no caso teste. Dessa forma ele também vai entender que tudo que ele está englobando é o children e como aqui nós estamos colocando literalmente um string, um texto, Nós já estamos abstraindo a renderização do texto aqui dentro, então fica a critério de vocês. Como vocês preferirem utilizar, no caso aqui, só mostrando que existe mais de uma forma de nós realizarmos a renderização do texto no nosso AppButton. Vamos encerrar por aqui pra aula não ficar muito longa, porque o próximo passo que nós vamos fazer é a configuração do nosso Button Variance. Se nós fizéssemos nessa aula, ela iria ficar grande demais. Então, espero vocês na próxima.