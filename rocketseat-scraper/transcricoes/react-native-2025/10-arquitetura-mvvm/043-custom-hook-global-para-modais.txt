WEBVTT Dando seguimento com o nosso componente genérico de modal, hoje vamos criar um custom hook específico responsável por realizar a abertura do nosso modal. E como eu disse algumas aulas atrás, nós teremos diversos tipos de modais diferentes. Teremos modais de seleção, modais de confirmação, modais de erro. E para isso, vamos vir aqui dentro de src, dentro de shared, vamos criar aqui uma nova partinha chamada hooks. E aqui dentro nós vamos criar um use. appmodal.ts e aqui possui um custom hook export const useAppModal. Vamos simplesmente retornar um objeto vazio por ora. Aqui dentro nós vamos desestruturar algumas variáveis de dentro do nosso useModalStore, no caso aqui o open, youclose, E aqui dentro do nosso useAppModal nós iremos criar funções responsáveis por abrir esses nossos tipos diferentes de modal. No caso, o que nós vamos utilizar para realizar o upload da nossa foto de perfil será o de seleção. No caso, teremos ali várias opções para o usuário. Ele irá poder escolher entre abrir a câmera ou abrir a galeria do celular. Então, para isso, vamos simplesmente colocar aqui um const showSelector. Vai ser simplesmente uma função e nessa função nós iremos executar o open. Agora aqui nós precisamos passar como parâmetro os parâmetros do open, que no caso seria o conteúdo do nosso modal de fato, e também o config, as configurações do nosso modal. Pra isso nós precisamos receber como parâmetro dentro do nosso showSelection os dados do nosso modal. Por se tratar de algo genérico, o título, o subtítulo, do modal e também os textos dentro dos nossos campos de seleção, dos nossos botões de seleção, deverão ser também genéricos e para isso nós vamos fazer o seguinte, vamos criar uma interface acima do nosso useAppModal interface, onde aqui dentro nós teremos o selection, options, Onde o selection option nós teremos um text, que será uma string, um onPress, que será uma função, que retornará simplesmente um void. Teremos também um icon, onde nós vamos importar aqui o import, from, arroba, expor, vector icons, o nosso ionicons, o keyoff, typeoff, ionicons.glyphmap, Temos também aqui o Variant. Este Variant eu iria explicar mais pra frente do que ele se trata, mas a princípio teremos aqui Primary, Secondary ou Danger. Agora, voltando no nosso Show Selection, vamos ter aqui a nossa config. Vamos chipar aqui diretamente, teremos o título geral do nosso componente de seleção, será uma String. Teremos aqui a Message. E também, que será opcional, e uma string. E por fim, teremos as nossas options, que serão as opções do nosso modal de seleção, que será o nosso selectOption.i. Com isso, agora nós precisamos fazer o seguinte, nós precisamos criar um componente específico do nosso showSelection, para nós passarmos como parâmetro para o nosso open. Aqui dentro de modals, vamos criar um selectionModal, E dentro de selectionModal vamos criar um index.tsx. Aqui vamos colocar um export const selectionModal, simplesmente retornar aqui uma view, react-native e um text de modal de seleção. Perfeito. Vamos pegar o nosso selection model, voltar para o nosso useAppModels, e dentro do primeiro parâmetro do nosso open, nós vamos pegar o nosso createElement, importando do react, onde neste createElement ele espera receber como parâmetro um elemento, um componente, Que no caso aqui seria o nosso SelectionModal, vírgula, e aqui os parâmetros que o nosso SelectionModal espera receber. No caso, como o nosso SelectionModal não espera receber nenhuma prop no momento, então aqui ele não vai recomendar nada. Então vamos simplesmente remover, depois nós colocamos aqui. Agora vocês me perguntam do porquê nós estamos utilizando o CreateElement ao invés de simplesmente chegarmos e passarmos o componente desta forma. É pelo simples fato de que nós estamos dentro de um arquivo .ts, então aqui o próprio TypeScript não reconhece a utilização dessa sintaxe, por isso nós simplesmente vemos aqui e colocamos o createElement, que no fim do dia vai ser literalmente a mesma coisa. Agora reparem que open tá retornando pra gente um erro. No caso desse erro, porque se nós olharmos, é obrigatório nós passarmos uma config pra ele. Vamos abrir lá o nosso modal, vamos colocar a config como opcional. E nem sempre nós vamos ter a nossa config aqui. Vamos simplesmente tipar aqui também na nossa função. Perfeito. Temos aqui a nossa função de showSelection, vamos exportar ela aqui dentro do nosso return. Pra que dessa forma, sempre que nós chamarmos o nosso useApp modal, nós vamos ter a disponibilidade de abrir o nosso showSelection. Agora, dentro do nosso selection modal, nós iremos montar todo o layout do modal de seleção, de fato. Mas vamos deixar isso para a próxima aula, pra esta não ficar muito longa.