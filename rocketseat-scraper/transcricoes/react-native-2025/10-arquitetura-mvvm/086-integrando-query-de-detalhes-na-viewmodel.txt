WEBVTT Dando seguimento com o desenvolvimento da tela de detalhes do produto, o que nós vamos fazer agora é simplesmente vir aqui no root layout das nossas rotas privadas, para que nós possamos simplesmente remover o reader que temos aqui do próprio ExpoRouter. Então para isso vamos simplesmente pegar o nosso stack.screen, onde vamos colocar aqui o name, onde vai ser simplesmente o product, barra, id, onde vamos colocar o options headerShowFalse. Perfeito, agora estamos sem o nosso header, nós poderemos simplesmente vir aqui e colocar um options na stack global, caso colocado aqui o headerShowFalse, dessa forma a gente poderia remover esses outros dois que temos aqui embaixo. Mas isso aí fica a critério de vocês, é interessante nós colocarmos individual caso nós queiramos personalizar. Agora, dando segmento, vamos aqui dentro de ViewModels, para nós criarmos a camada de ViewModels aqui da nossa tela. Então, vamos aqui criar, Product, onde aqui em Product vamos criar o UseProduct. ViewModel.ts, que vai ser o CustomHook. ExportCost. UseProductViewModel. Vamos simplesmente retornar o objeto vazio por hora. Vamos querer aqui também a nossa camada de View. Product. ser o Product.View.tsx, aqui vamos simplesmente colocar um FunctionalComponent, por enquanto, ProductView, onde aqui vamos simplesmente retornar uma View, a parte visual agora a gente simplesmente tipa, fala que é um FunctionalComponent com o return type do type of useProductViewModel, Agora tudo que a gente já retornar lá automaticamente já vai aparecer aqui na nossa camada de view, porque a gente só importou o custom hook errado. É o useProductViewModel. Perfeito. E agora a gente vem aqui dentro de product, dentro das nossas rotas privadas, nós instanciamos aqui const viewModel igual a useProductViewModel. Um detalhe interessante é que nós vamos repassar também o ID aqui do nosso produto que estamos recebendo aqui da nossa UseLocaleSearchParams. Vamos simplesmente repassar aqui para dentro da nossa ViewModel. Então, aqui vamos colocar o ProductIDNumber. Agora, se nós voltarmos aqui dentro da nossa página de Product, ele vai reclamar, falando que nós precisamos enviar o ID para ele, e no caso, Aqui ele é uma string, então vamos simplesmente fazer a conversão number, mandando para lá o id que estamos recebendo como parâmetro na navegação. Vamos pegar aqui a nossa ViewModel, chamar a nossa ProductView, e passar um Spread Operator na nossa ViewModel. Perfeito. Dessa forma, garantimos que tudo vai estar funcionando perfeitamente aqui, aplicando os conceitos da nossa arquitetura, separando a parte lógica na camada de Model, criando aqui a ViewModel, onde nós de fato injetamos todos esses dados na nossa camada de View, para nós possuímos fazer o isolamento. Agora vamos vir aqui na nossa camada de UseProductViewModel, vamos criar aqui, ou melhor, vamos instanciar A query que nós criamos na aula passada, no caso, const, vamos desestruturar aqui, igual a useGetProductDetailQuery, onde ele espera receber o ProductId. Maravilha, aqui dentro ele vai retornar pra gente o data, vamos renomear aqui de ProductDetail, Vamos ter aqui também o IsLoading e também o Error. Vamos pegar essas propriedades e repassar aqui, o IsLoading, ProductDetail e Error. Agora vamos aqui dentro de ProductView, vamos recuperar essas propriedades que nós repassamos lá, no caso de Error, o IsLoading e o ProductDetail. Vamos simplesmente fazer uma validação aqui em cima, se houver algum tipo de erro, vamos simplesmente retornar aqui um texto, Houve um erro ao carregar os detalhes do produto. Só importar aqui o texto vindo lá do React Native e show de bola. Um detalhe interessante é que se nós olharmos lá no nosso figure, podemos ver que teremos a listagem de comentários na nossa tela. Então, uma coisa que nós iremos fazer é construir toda esta tela dentro do contexto de uma flat list. No caso, toda esta parte aqui dos detalhes do produto, nós vamos colocar no reader da flat list para que, desta forma, possamos aproveitar todos os recursos que esse componente incrível pode oferecer para a gente. Vamos simplesmente pegar tudo isso aqui e separar em duas seções diferentes. Para isso, já vamos fazer o seguinte, vamos pegar aqui a nossa flat list, Vamos importar aqui do próprio React Native, aqui ele espera receber o data, vamos passar um array vazio por enquanto, um renderItem, também vamos simplesmente só passar a renderização de um fragment desta forma, e um listReaderComponent, aqui é onde de fato nós iremos colocar toda a parte dos detalhes do nosso produto. por hora vamos só colocar um fragment aqui para ele parar de retornar um erro pra gente e na verdade vamos colocar um text aqui dentro também um text contendo o ProductDetail.name Vamos ver se está funcionando, vamos só clicar aqui na piscina inflável E olha só que legal, já funcionou, ele já está renderizando aqui o nome do item do item que nós clicamos, no caso clicamos aqui no copo Stanley Ele já vai aparecer lá em cima também, vamos só englobar essa nossa FlatList dentro de uma SafeArea, viu? SafeArea, viu? Pegar aqui a nossa FlatList, mover lá pra dentro, e perfeito! Vemos que a nossa funcionalidade de passar o parâmetro através das propriedades da navegação aqui para a tela de detalhes do produto. Já estamos recuperando o nosso Product.ht, fazendo a requisição HTTP através da nossa query. Agora é simplesmente pegar os dados que estamos recebendo da nossa API e montar em tela. A partir da próxima aula, nós vamos dar início à construção da nossa interface de fato.