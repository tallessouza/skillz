WEBVTT Nesta aula, vamos construir o componente de exibição dos produtos aqui na nossa listagem. No caso, vamos literalmente desenvolver este card, onde teremos a foto do nosso produto, teremos o nome dele e o valor. Para isso, vamos voltar aqui dentro de components, vamos criar um componente novo chamado ProductCard. E aqui dentro, vamos criar um index. .tsx, aqui vamos criar um funcional component padrão, product card, onde simplesmente vamos retornar uma view vazia por enquanto. Uma coisa interessante de nós fazermos é voltarmos aqui no local host, docs, aqui na nossa rota de busca de produtos, no caso temos esta daqui que retorna um array contendo estes objetos aqui e entendemos como será os dados que a nossa API irá retornar para nós já criarmos a interface e temos alguns dados mocados para podermos utilizar para construir a interface então vamos simplesmente pegar todo este objeto que temos aqui na nossa rota post que buscamos os nossos produtos lembrando que ele retorna pra gente uma lista dentro de data e por ser uma rota paginada ele espera receber page per page total E ele também já retorna aqui na nossa resposta. Vamos voltar no nosso app. Vamos vir aqui dentro de shared. Só minimizar todas as pastas. shared interface htps Vamos criar aqui, ProductResponse.ts e aqui dentro de interfaces vamos colocar também Product.ts. Vou só mudar aqui do nome da nossa interface para o singular. Aqui dentro de product vamos colocar Export Interface, Product Interface. Vamos dar um CTRL-V nos dados que nós copiamos, perfeito. O ID ele sempre vai vir, então vai ser simplesmente um number. Todas as strings que nós temos vamos simplesmente vir aqui, vamos simplesmente selecionar todas, e simplesmente apagar as aspas. Perfeito. Todos que estão com valores de zero também vamos selecionar e substituir por number. E aqui vamos colocar também export interface product category. Vamos pegar esta categoria daqui e colocar o product category. Vamos remover, number, string, maravilha. Vamos pegar aqui também, vamos pegar agora o nosso product interface, voltar dentro de product response, colocar export interface, product response, onde teremos o page, number, per page, que também será um number, data, que será um array de product interface, vamos ver o que ele mais retorna pra gente, page, page, page, total e total pages, olha, isso daqui é interessante, vamos já colocar aqui também, page e total pages, vamos colocar number, e com base nisso daqui, vamos simplesmente voltar no nosso componente de home, aqui na nossa home, viu, vamos fazer o seguinte, vamos criar um objeto montado aqui por enquanto, const products, List, onde simplesmente teremos um Array, vamos chipar aqui que ele será um Array de Product Interface. Se nós colocarmos aqui, teremos o Name, Produto, Category, nós teremos aqui também o ID, vamos colocar qualquer coisa, Name também, Test, Average Rating, vamos colocar 1 aqui, só para preencher, vamos colocar valores aleatórios, Na verdade, vamos simplesmente vir aqui, copiar todo esse objeto que nós temos na nossa documentação e colar aqui dentro, é mais rápido. E perfeito, vamos pegar o nosso ProductList e colocar aqui dentro. Vamos pegar o nosso componente de ProductCard que nós criamos, vamos colocar aqui dentro do nosso RenderItem. Vamos recuperar o item que está sendo iterado no momento. Se nós passarmos o mouse em cima, podemos ver que ele já vai estar equipado com a nossa Product Interface. Agora temos de vir aqui dentro do Product Card Interface. Product Card Item. Ou melhor, Product Card Params. Tipando o Product, vamos receber um único produto como parâmetro do nosso Product Card, que vai ser o Product Interface. Vamos tipar FunctionalComponent recebendo o ProductCardParams. Se nós voltarmos na nossa camada de HomeView, ele vai obrigar que nós passemos o Product como parâmetro, no caso aqui será o Item. E outra propriedade muito importante que nós vamos ter de colocar aqui será o KeyExtractor. Onde será? Uma função, onde teremos aqui os nossos itens sendo iterados, vamos simplesmente retornar aqui uma string ProductListItem Passando o id, isso aqui é literalmente igual àquela key que nós temos quando utilizamos o método de map, basicamente para o React ter a referência do componente. É importante que nós passemos aqui esse key extractor. Como nós temos um único item, se nós chegarmos aqui e colocarmos, por exemplo, um text, Portanto, o do react-native, pegando o do nosso product.name, ele já vai ser renderizado em tela, olha só que legal! Vou parar esta aula por aqui, simplesmente a configuração das interfaces e a criação do nosso componente base. Na próxima aula daremos seguimento com a construção deste componente. Então é isso, espero vocês lá!