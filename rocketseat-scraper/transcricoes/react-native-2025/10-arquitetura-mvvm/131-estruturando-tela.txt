WEBVTT Agora no segmento com o desenvolvimento da nossa tela de pedidos, vamos vir aqui dentro de src, viewmodels, vamos criar aqui um novo arquivo chamado orders, onde aqui dentro nós vamos criar um orders.view.tsx, opa, ficou faltando colocar o x aqui no final, aqui um funcional component, export, const, orders, view, vai ser simplesmente um funcional component, onde vamos simplesmente retornar uma view por hora, Vamos ter aqui também a nossa camada de ViewModel useOrders.ViewModel.ts Aqui novamente um custom hook export cost useOrders.ViewModel onde simplesmente vamos retornar um objeto vazio por hora Voltando aqui na nossa camada de view, já vamos realizar a tipagem, falando que ele é um Functional Component que espera receber o return type, ou seja, o tipo do retorno do type of useOrder ViewModel, para que toda variável que nós retornarmos possamos ter acesso na nossa camada de View. E aqui no nosso app, dentro de Private, Tabs, aqui em Orders, vamos remover este texto que estamos renderizando no momento, e simplesmente retornar aqui o nosso OrdersView. Vamos criar uma variável de viewModel acima do return, useOrdersViewModel, onde vamos simplesmente rodar um Spread Operator aqui dentro, dessa maneira. para nós passarmos todo o retorno da ViewModel lá para dentro da nossa camada de View. Agora, aqui dentro da nossa ViewModel, já vamos fazer o seguinte, como na aula passada nós já criamos a nossa query responsável por buscar os nossos pedidos, vamos executá-la aqui dentro da nossa camada de ViewModel, logo de cara. Vamos colocar aqui o nosso useGetOrdersQuery. Aqui dentro vamos pegar o data e colocar um alias de orders, no plural. Vamos simplesmente dar um console.log pra ver se tá funcionando de fato e olha só que legal, a nossa query já tá retornando uma listagem contendo todos os pedidos que nós fizemos até então, contendo todos os itens que nós compramos, os valores e também o valor total. Vamos simplesmente pegar estes Orders. Na verdade, vamos fazer melhor ainda, porque se nós analisarmos com calma, o Backend retorna para a gente um objeto, e dentro deste objeto, de fato, temos aqui o nosso Orders. Então, vamos renomear aqui o nosso data para OrdersResponse. onde aqui dentro nós vamos realizar uma desestruturação do nosso ORDERS.RESPONSE, desta maneira. Vamos recuperar aqui o DATA, no caso aqui ele pode ser undefined, pelo que eu tô vendo. Vamos fazer o seguinte, vamos simplesmente pegar, criar aqui um CONST, ORDERS, vai ser igual ao ORDERS.RESPONSE.ORDERS ou um array vazio, desta maneira. A gente garante que esta constante sempre terá o tipo de order interface e não poderá ser um undefined. Onde a gente simplesmente pega e repassa o orders pra dentro do nosso return. Vamos ir lá no nosso figma, nós vamos utilizar também a quantidade de orders que nós possuímos aqui no figma. Vamos ir aqui em projeto, na nossa tela de pedidos de fato. Aqui... é... não é utilizado, então a gente vai simplesmente só retornar o orders desta maneira. Vamos aqui na nossa view, vamos aqui na nossa camada de view, substituir esta view aqui por uma safe area view, importando do react-native safe-area-context, onde aqui dentro vamos simplesmente chamar uma flatlist do react-native, Vamos recuperar as orders aqui na desestruturação do nosso componente, onde nós vamos simplesmente passar aqui pra dentro do data. No renderItem, vamos chamar aqui uma callback. Simplesmente renderizando um texto por enquanto. Corta aqui do React Native. Vamos pegar o item que está sendo iterado no momento. Colocar um alias nele de order no singular, pelo fato de estarmos iterando sobre uma lista, sobre um array. E aqui vamos passar o nosso order ponto ProductName, só pra gente ter um vislumbre se de fato já está chegando aqui. E olha só que legal, nós já possuímos a listagem de ordens na nossa tela. Vamos só vir aqui no nosso SafeAreaView, colocar um className com flex1 pra ele ocupar 100% da tela. Para essa aula não ficar muito longa, vamos encerrar por aqui. Na próxima nós vamos criar o card de renderização aqui da nossa FlatList.