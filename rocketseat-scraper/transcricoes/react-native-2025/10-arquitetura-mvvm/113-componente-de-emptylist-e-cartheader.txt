WEBVTT Nesta aula nós vamos criar a tela de quando nossa lista estiver vazia. No caso, vamos vim aqui simplesmente dentro de components. Vamos criar uma pasta nova chamada emptyList. Onde aqui vamos criar um index.tsx. Simplesmente com a funcional component padrão, export cost. emptyList. Onde aqui vamos simplesmente retornar uma view por hora. Vamos pegar esta nossa render EmptyList, voltar lá na nossa CartView, e aqui vamos colocar no componente de List, EmptyComponent, desta forma aqui assim. Perfeito. Agora pra nós podermos ver de fato essa nossa EmptyList, como nós já adicionamos alguns itens aqui no nosso carrinho, é bem simples, basta nós virmos aqui dentro de Data e passar um Array vazio, desta forma aqui, quando nós popularmos o nosso componente com algum texto, por exemplo, vamos colocar só um texto aqui de tela vazia. Ele já vai ser renderizado perfeitamente. Primeira coisa, vamos colocar uma SafeAreaView aqui. SafeAreaView, importando do SafeAreaContext. Novamente, nós não vamos mais utilizar o SafeAreaView que vem do React Native, pois ele será descontinuado. Aqui dentro da nossa SafeAreaView, vamos só colocar um class name aqui com flex1, para ele ocupar 100% da tela. Vamos colocar uma view aqui dentro, contendo também um className, opa, um className com flex1, itemCenter, px de 20, e um paddingTop de 16. Aqui dentro nós vamos colocar simplesmente um IonIcons, com o name de cartOutlined, um size de 80, e um color, vamos simplesmente vir aqui, pegar nossa variável de colors.gray na tonalidade de 200. Aqui abaixo do nosso IonIcons vamos por um text contendo um seu carrinho está vazio. Nesse text teremos um class name contendo um text xl para o tamanho da fonte, um font bold um text grey na tonalidade 700, um margin top de 4 e por fim um margin top de 4 e um margin bottom de 4 também. Abaixo desta view, abaixo deste text teremos outro text, onde aqui teremos explore o catálogo de produtos e faça sua primeira compra. Onde aqui abaixo da view nós simplesmente vamos colocar o nosso app button, No left icon, simplesmente chamando o StoreFrontOutline, desta forma, com o variant também de Outlined, assim. Aqui dentro do appButton nós vamos passar o ExplorarProducts, desta forma, e aqui vamos colocar um className contendo um width de 197px, desta forma. E vamos centralizar ele, um self-center, assim. Perfeito, agora aqui no texto onde nós colocamos a descrição, aqui no class name, nós vamos simplesmente colocar um text-base, com um text-grey na tonalidade 400, um text-center pra ele ficar centralizado, e um margin-bottom de 8. Agora no on-press, Vamos simplesmente chamar o nosso Macaubeck com o router.push enviando o usuário para a barra private barra tabs barra home dessa forma quando nós clicarmos vão ser direcionados para homem opa ele mandou aqui pra gente falou que a page not found vamos ver o que que tá acontecendo na verdade aqui só de colocar o barra home já vai ser o suficiente ele já vai identificar então a gente não precisa passar o caminho todo de privates tabs Só de colocar o barra home assim dessa forma, nós clicarmos e já vamos ser redirecionados aqui para a nossa tela de home. Agora, se nós repararmos lá no Figma, podemos ver que temos um header em comum, tanto na tela de lista vazia quanto na tela preenchida, então vamos criar um componentezinho aqui para o nosso header, para reaproveitarmos, simplesmente dentro de component. Vamos criar um cartHeader, onde aqui vamos criar simplesmente um index.tsx com export cost cartHeader. onde aqui simplesmente retornaremos uma view com um text de carrinho. Embaixo desse text teremos um outro text, opa, um outro text de veja seu carrinho de compras. Vamos só importar esse texto do React Native. Nesta view vamos colocar um class name contendo um py de 3, um gap de 1, o margin-bottom de 4 neste outro text um class name contendo um text de 20px um font-bold e um text-gray 800 e aqui embaixo, neste outro, teremos um class name simplesmente com um text-gray 400, desta forma. Vamos pegar o nosso cart-reader, voltar aqui no nosso cart.view E aqui dentro da propriedade de ListHeaderComponent, vamos simplesmente passar ele desta forma, nem vai precisar abrir o menor que e o maior que. Vai ficar assim já. Vamos só corrigir aqui o título, coloquei carrinho com V, na verdade é carrinho desta forma, perfeito. Desta forma finalizamos aqui o nosso componente de lista vazia e do nosso header. Na próxima aula nós daremos segmento com a parte aqui do cartão de crédito. Então é isso, espero vocês na próxima aula.