WEBVTT Na aula de hoje daremos seguimento com o desenvolvimento do input global do nosso app. Na aula passada nós configuramos diversas coisas dentro da camada de ViewModel do nosso input. Nessa nós iremos fazer ele funcionar de fato. Iremos ver o Tailwind Variance aplicando a estilização em tempo real. Então, para isso, a primeira coisa que nós iremos fazer é criar uma nova função aqui dentro do nosso AppInputViewModel. const handle.next.change() Hoje ele vai ser uma função que irá receber como parâmetro um text, que no caso seria uma string, e aqui dentro do handle text nós iremos fazer uma validação. Nós esperamos receber o método, a função de mask dentro do nosso useAppInputModel e essa função será responsável por aplicar alguns filtros no nosso texto, por exemplo, um filtro de conta bancária, em que conforme nós formos digitando, ele irá colocar, por exemplo, irá separar os dígitos, irá colocar traços, por exemplo, se for um número de telefone, irá colocar parênteses. E nós iremos utilizar este método de mask em algumas vezes aqui no nosso aplicativo. Então, vamos simplesmente validar se mask está sendo recebido no nosso input. Nós vamos executar o onChangeText, caso como ponto de interrogação, pois ele é opcional, passando como parâmetro o mask, englobando o nosso text. ou uma string vazia. Lembrando que o onChangeText pode ser undefined, até porque nós estamos seguindo o estilo do que o próprio textInput do React retorna pra gente. Se nós chegarmos aqui e passarmos o mouse em cima do onChangeText do próprio React, no caso do onChangeText que estamos herdando do textInputProps, podemos ver que ele poderá ser undefined. Então, nós vamos simplesmente repassar isso pro nosso useAppInputViewModel e perfeito. Agora, nós vamos repassar o nosso handle TextChange aqui para dentro do nosso return, vamos passar aqui também o nosso isFoxed, vamos repassar para que nós possamos recuperar esses dados e já vamos pegar eles aqui na nossa ViewModel, o handle, TextChange e também o isFoxed. Próximo passo que nós vamos fazer é vir aqui dentro do nosso AppInputVariants, abrir chaves, vamos passar logo de cara o IsFocused aqui pra dentro. O IsFocused está sendo retornado diretamente da nossa IsAppViewModel e assim que ele for true, nós iremos sobrescrever o estilo padrão que nós temos aqui dentro do Slots pela estilização que nós colocarmos dentro da condicional do IsFocused. No caso, nós não temos nenhuma estilização aqui por enquanto. Nós vamos criar já já, mas antes de criarmos a estilização dentro do Input Variance, nós vamos pegar os métodos que estamos recebendo na nossa camada de ViewModel e repassar pra dentro do Text Input, no caso, o OnBlur. Vamos passar aqui o handleBlur, lembrando que dentro do onBlur, do nosso textInput, nós vamos passar o handleBlur que está sendo retornado da nossa camada de ViewModel. É importantíssimo este detalhe. E da mesma forma que fizemos com onBlur, vamos fazer também com onFocus. Vamos repassar aqui o nosso handleFocus, que está retornando da nossa ViewModel. Perfeito. Agora, toda vez que nós clicarmos e focarmos no nosso input, nós vamos disparar o método de handleFocus na nossa camada de ViewModel. Vamos até colocar um console.log para nós fazermos um experimento. Vamos colocar no handleFocus um console.log. Focou no input. Agora aqui no handleBlue, vamos fazer a mesma coisa. desfocou do input e vamos fazer o seguinte aqui na nossa camada de register.view vamos duplicar o nosso app input agora nós temos dois quando nós trocarmos o foco do nosso input podemos ver que a nossa função de handle focus foi chamada disparando o nosso console dessa forma nós atualizamos aqui o nosso estado de setFocused para true, e quando nós focamos em outro input, podemos ver que ele desfoca do input antigo, seta o valor dele para false e já atualiza o novo input como sendo o input que está sendo focado. Isso daqui é super interessante, porque este valor que irá fazer com que o nosso input variance se atualize, ou seja, quando ele for true, nós iremos sobrescrever os estilos que nós temos, E agora vamos fazer o seguinte, vamos já criar a estilização variante dentro do nosso arquivo, dentro de true, ou seja, se o isFogZed for true, nós iremos querer alterar as propriedades que nós determinamos aqui anteriormente, tanto que se nós vemos as opções que temos aqui dentro, é nada mais nada menos do que o que nós já tínhamos pré-ajustado dentro de slots, ou seja, aqui dentro nós vamos alterar primeiramente o nosso wrapper, onde no nosso wrapper nós vamos colocar um border com a cor de purple, Base, e a nossa label, ou seja, quando nós estivermos com o nosso input focado, a nossa label terá a cor de texto, Text, Ripple, Vamos testar pra ver se tá funcionando de fato, se nós clicarmos no nosso input e olha só, a nossa borda mudou de cor e a nossa label também ficou roxa. E se nós trocarmos o input, podemos ver que ele está funcionando direitinho, ou seja, quando nós passamos a variável de isFoxed pra dentro do nosso input variants como sendo true, nós agora temos a aplicação em tempo real. da variação da estilização dos nossos componentes, no caso do nosso wrapper e da nossa label. Isso aqui é uma forma bem interessante de nós estarmos criando variações sem encher o nosso código com um monte de if. No fim do dia, acabou ficando bem mais abstraído essa responsabilidade para dentro do app input variants, diferente de como nós fizemos no app passado, que foi utilizando o CLSX, que também é uma ótima abordagem. mas deixa aquele código mais feio, aquele código mais marcarônico, com vários ternários dentro do nosso className, e isso daqui acaba deixando o código confuso e pouco legível, mas o appInputVariance abstraindo tudo isso aqui dentro dessa simples configuraçãozinha, já deixa tudo mais simples, mais visual, até mesmo na hora de nós lermos, darmos uma manutenção ou aplicar alguma variação nova, o fato de estar tudo isolado aqui dentro Acaba deixando tudo mais simples. Um último detalhe que nós vamos fazer é remover essa label que está fixa aqui na linha 66. Vamos remover ela, colocar as chaves e pegar a label que estamos recebendo como parâmetro do nosso app input. Se nós viermos aqui dentro da nossa camada de register.view e passarmos como parâmetro aqui, simplesmente a label, sendo email, E o segundo label, sendo por exemplo senha, ela já vai ser renderizada aqui pra gente. Olha só que legal, o nosso componente genérico de input já está sendo montado, cada vez mais fidedigno com o que está no Figma. Um último detalhe que nós vamos fazer aqui, dentro do nosso componente de AppInput, é virmos aqui dentro do nosso Icons, vamos colocar um className com um marginWrite de 3, para que ele se afaste um pouquinho do nosso texto, que ele tá muito junto, tava muito colado. Olha só que massa que temos aqui a nossa estilização. E o último detalhe, antes de nós finalizarmos a nossa aula, é que ficou faltando um detalhezinho importante dentro da nossa camada de ViewModel, na nossa função de HandleTextChange. Após a validação de se nós temos ou não a função de Mask, ficou faltando nós colocarmos aqui um Else. Caso nós não estejamos enviando a nossa função de Mask, nós vamos simplesmente executar o OnChangeText igual nós temos aqui em cima. Porém, sem englobar o texto com o mask e vamos remover essa condicional aqui também. Show de bola! Dessa forma nós garantimos que o onChangeDetect sempre vai ser executado quando nós digitarmos algo no nosso input. Na próxima aula nós vamos aplicar a estilização dinâmica dentro dos nossos ícones. Então é isso, espero vocês na próxima aula.