WEBVTT Bom, e na aula de hoje nós vamos de fato criar as animações aqui do nosso cartão de crédito. Para isso nós vamos vir aqui no nosso useCreditCard.viewModel. Aqui a princípio nós vamos criar uma nova constante, no caso vamos criar aqui o cost, o value. vai ser igual ao useSharedValue lá do nosso React Native Reanimated, onde ele vai começar como zero. Este useSharedValue é basicamente o estado da animação. Este valor não fica armazenado na thread do JavaScript. Em suma, nós temos algumas threads rodando aqui o nosso aplicativo, no caso a thread da UI e a thread do JavaScript. São threads completamente diferentes. Então, basicamente, toda vez que este FlipValue é atualizado, ele não gera um re-render aqui no nosso componente. Vamos supor que nós estamos fazendo uma animação que vai da altura 0 até a altura 200. Se nós estivéssemos utilizando simplesmente o UseState, não ia ser nem um pouco performático, porque a cada atualização de estado, todo o nosso componente seria remontado. Já utilizando este recurso aqui do próprio React Native Reanimated, nós não temos este problema. É como se nós estivéssemos lidando com uma variável fora do Javascript. E de fato, no fim do dia, é isso que está acontecendo. É uma função muito parecida com o próprio useMem, apesar do useMem estar rodando ali na thread do Javascript. E eu digo no âmbito de prevenir ali as renderizações. Vamos criar aqui outras duas variáveis. onde nós vamos utilizar aqui o nosso useAnimatedStyle, também lá do nosso React Native Reanimated, onde aqui ele espera receber como parâmetro uma callback, onde vamos simplesmente retornar aqui um objeto dessa forma. E da mesma forma que nós temos o estilo animado da parte dianteira do nosso cartão, vamos ter também da parte traseira, então, um const backAnimatedStyle, isso é a mesma coisa, useAnimatedStyle, onde vamos passar como parâmetro uma callback retornando um objeto por hora. Basicamente aqui dentro que nós vamos armazenar os valores que irão ser alterados com base aqui no nosso flipValue. E para isso nós vamos criar uma outra constante aqui dentro do nosso frontAnimatedStyle const rotateValue. No caso nós vamos utilizar uma animação com base em rotação aqui no nosso carrinho. Se nós olharmos a documentação aqui do reanimated Nós podemos até ver aqui, vamos abrir aqui o Diacórdion Flipkart, ele já tem um exemplo aqui pra gente de como nós podemos fazer esta animação, no caso se chama Flipkart, onde aqui nós vamos utilizar o Interpolate com o WithTiming. A biblioteca do React Native Reanimated é maravilhosa. Em um módulo futuro nós vamos focar muito mais minuciosamente os princípios conceitos por trás dela, mas caso vocês estejam interessados, podem acessar este link aqui, onde ele mostra um exemplo bem detalhado do que que acontece, de como construir este tipo de animação e várias outras animações, como por exemplo, este de Accordion, ou até mesmo construir um Bottom Sheet na mão. Mas vamos voltar aqui por ora? Vamos criar aqui o nosso Rotate Value, onde nós vamos pegar o Interpolate, Este interpolate é basicamente uma animação que vai e que volta, onde aqui nós vamos passar alguns parâmetros para ela. No caso, o nosso app chegou até sair. Vamos só remover esses erros aqui. Como primeiro parâmetro, nós vamos passar o valor que nós estamos no momento, ou seja, o valor que a nossa animação vai iniciar, onde nós vamos pegar aqui do nosso flip. Opa! O segundo parâmetro é, basicamente, os índices que nós vamos ter nas animações dentro deste interpolate. Novamente, este interpolate espera receber uma série de estados. No caso, como vai ser uma interpolação, a nossa animação terá vários estados. No nosso caso, a parte da frente do nosso cartão de crédito terá ele, de fato, sendo exibido e ele não sendo mais exibido, sendo coberto pela parte de trás do nosso cartão, onde aqui nós teremos apenas dois estados, que será o índice 0 e o índice 1. E o segundo parâmetro, que são os valores de fato, que no caso aqui vai começar como 0, que será ele sendo exibido, e aqui vai finalizar como 180. No caso aqui, nós vamos sair de 0 graus até 180 graus. para a rotação do nosso cartão de crédito. Aqui dentro do Return, vamos só retornar aqui o Transform, que é uma propriedade aqui do CSS, onde ele espera receber um array, onde aqui dentro vamos passar o rotateY, ou seja, a rotação na horizontal, passando uma template string. Normalmente aqui ele espera receber, por exemplo, 90 graus, por exemplo, caso aqui a gente vai colocar deg dessa forma, mas aqui a gente vai colocar o nosso rotateValue, que estamos armazenando, e deg aqui, que seriam os graus de rotação. Vamos só copiar este valor e colocar aqui dentro do nosso BackAnimatedStyle, onde aqui a única coisa que a gente vai mudar é que ele já vai começar como 180 e o final dele vai ser 360, dessa maneira. A gente vai simplesmente pegar e abrir o nosso app aqui novamente. Vamos só abrir ele. E vamos repassar esses nossos dois styles aqui pra dentro do return. Agora vamos recuperar aqui na câmera de view, desestruturando, dessa forma, Back Animated Style, e o nosso Front Animated Style, onde pra nós passarmos eles aqui nas nossas Animated Views, nós vamos simplesmente vir aqui, Dentro do array, e simplesmente passar o frontAnimatedStyle, agora aqui no back a mesma coisa, backAnimatedStyle, dessa maneira. Agora para nós dispararmos de fato as nossas animações, quando nós estivermos focando aqui, por exemplo, no CVV ou nos outros campos, é bem simples, nós vamos vir aqui dentro da nossa camada de ViewModel novamente, vamos criar aqui um UseEffect, no caso useEffect onde nós vamos receber na rede de dependências o isFlipped onde aqui dentro nós vamos fazer um flipValue.value vai ser igual a um if Ou seja, esse withTiming vamos importar também do React Native, onde nós conseguimos executar ações específicas aqui dentro e passar uma duração para essas ações, para essas animações que nós vamos querer executar. No caso, o que nós vamos fazer aqui, o que ele espera receber como primeiro parâmetro é o valor que nós vamos querer realizar a animação, mas para isso vamos pegar o nosso isFlipped. Se ele for true, nós vamos passar o valor 1. caso contrário vamos passar o valor 0 e como segundo parâmetro do nosso with timing nós vamos passar o nosso duration onde vamos colocar aqui 600 milissegundos Basicamente, este número aqui é o índice de dentro do nosso rotateValue. O que nós estamos dizendo é que quando o nosso isFlipped for true, de fato, nós vamos pegar o nosso flippedValue e vamos fazer uma animação, por exemplo, quando ele estiver em 0 até 1, onde nós definimos aqui dentro do nosso rotateValue que o índice 0 é o valor 0, de fato, e o índice 1 é o de 180, então ele vai percorrer todos os números de 1 até 180, gerando uma fluidez na hora da animação aqui no nosso Rotate Value. Vamos testar para ver se está funcionando. Quando nós clicarmos aqui em CVV, e olha só que legal, já está funcionando perfeitamente a nossa animação. Se nós voltarmos aqui para um outro, para o foco em um outro campo que não seja o CVV, ou seja, o IsFlipped, vamos até colocar um console.log, console.log, Ele está como true, porque nós tínhamos definido aqui no nosso useAddCardBottomSheetView que o nosso isFlipped, se ele for igual a CVV, ele vai ficar como true. Se for um outro, ele vai ficar como false. Olha só que bacana, já está funcionando perfeitamente a nossa animação. Agora, tudo que precisamos fazer é passar os dados do nosso cartão de crédito aqui para o nosso componente animado para ser exibido e também fazer algumas formatações aqui de dados. Mas vamos fazer isto na próxima aula para esta não ficar tão longa.