WEBVTT Já que chegamos até aqui, vamos dar uma melhorada aqui nas interfaces do nosso Product Card. Como podemos ver, possuem alguns produtos que possuem um nome muito grande, o que acaba estourando o preço aqui na nossa tela. Porém, para resolvermos isso, vai ser mais simples do que aparenta. Nós vamos simplesmente vir aqui na nossa Use Product Card View Model. onde nós, no momento, estamos simplesmente repassando o produto que está sendo iterado lá na nossa listagem da nossa FlatList. A primeira coisa que nós vamos fazer é criar aqui uma função const formatProductName, onde aqui nós iremos receber como parâmetro o nome do produto, caso aqui, name, que vai ser uma string, onde vamos simplesmente ter um if, se o name.length, ou seja, se a quantidade de caracteres dele for maior ou igual a 17, vamos simplesmente dar um return aqui no nosso name, vamos abrir o template string, vamos concatenar o name.slice, vamos cortar a partir do índice 17, e vamos concatenar com três pontinhos. Agora, caso contrário, vamos simplesmente dar um return no name, porque o nome é pequeno. Próximo passo, nós vamos simplesmente criar uma constante aqui, const, displayName vai ser igual a formatProductName igual a product.name. Vamos pegar o nosso displayName e repassar ele aqui. Recuperar lá na nossa camada de view do productCard. Desestruturando. displayName, vamos passar aqui no product. Perfeito. Vamos dar um reload e olha só, já melhorou muito aqui a interface dos nossos itens. Próxima coisa, vamos fazer alguns ajustes aqui no nosso CSS, aqui no FormattedRating. Vamos fazer uma outra função aqui dentro da nossa ViewModel, const FormattedRating igual a, vamos simplesmente dar um return, no product.averageRating.toFixed, estando em 2. Só um detalhe que eu acredito que seja melhor do que nós colocarmos essa função de formatRating é nós simplesmente colocarmos em uma variável isso daqui. Como é uma operação mais simples, nós simplesmente colocamos dessa forma aqui. Vamos também colocar aqui um .replace. Vamos substituir vírgula por ponto. Vamos pegar o nosso formatRating E repassar aqui no nosso Product Card, viu? Vamos recuperar. Repassar aqui. Perfeito, agora nós já temos detalhadamente aqui como que está a avaliação dos nossos produtos. Vamos só mudar aqui o toFixed para 1, na verdade, ao invés de 2. Agora vamos criar um componente específico para renderização aqui dos nossos valores. Porém, vamos fazer isso na próxima aula para esta não ficar muito longa. Esses pequenos ajustes vão ser o suficiente por enquanto. Então, é isso. Espero vocês na próxima aula.