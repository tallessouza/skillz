WEBVTT Na aula de hoje vamos dar seguimento com a construção do nosso Product Card. Primeira coisa que eu gostaria de fazer aqui do nosso componente de Product Card é aplicar a nossa arquitetura MVVM dentro dele, para vocês verem que o conceito da arquitetura MVVM não se limita apenas a telas inteiras, mas podendo ser estendidas também para subcomponentes como é o próprio Product Card. Vamos primeiramente fazer o seguinte. ProductCard.View.tsx Vamos pegar todo este componente que nós já possuímos aqui e repassar aqui pra dentro. Vamos só alterar o nome para ProductCardView. Vou alterar aqui também pra ficar semântico. ProductCardViewParams que, na verdade, nós não vamos nem repassar dessa forma, nós vamos voltar aqui no nosso índex. Aqui vai continuar dessa forma, PRODUCT CARD, só que aqui nós vamos passar o nosso PRODUCT CARD VIEW. PRODUCT CARD VIEW. Dentro do nosso PRODUCT CARD VIEW, ele tá reclamando porque nós temos literalmente a mesma tipagem onde nós esperamos receber o PRODUCT, porém nós não vamos fazer dessa forma, vamos remover toda essa tipagem que nós temos aqui. Vamos pegar aqui a nossa view, o nosso text, também, vamos importar. Vamos tirar a texta própria. Vamos criar aqui também o nosso useProductCard.viewModel.ts Vamos colocar um export const useProductCard.viewModel Onde nós vamos ter aqui um componente, onde nós vamos retornar simplesmente um objeto vazio por hora. Vamos ter aqui uma interface, vamos receber alguns parâmetros aqui dentro. interface vai ser o nosso useProductCardViewModelParams. Onde receberemos o produto, será o product interface. Vamos chipar aqui agora, opa, vamos só mudar aqui a nossa interface para começar com letra maiúscula. Vamos chipar ele dessa forma daqui, desestruturando o nosso produto. Vamos pegar agora o nosso useProductViewModel, instanciar ele aqui dentro do nosso ProductCard, pegando as props, igual a, enviando o produto que recebemos como parâmetro. Ele está retornando um erro, por quê? Ah, isso é porque ele espera receber desta forma daqui. Product. Já que é dessa forma, vamos fazer o seguinte, vamos abstrair aqui dentro para props. Vamos mover isso daqui para props, dessa forma vai ficar correto. E aqui vamos pegar ViewModel, mudar o nome dessa constante pra não ficar duplicada. Simplesmente dar um Spread Operator na ViewModel. Agora dentro da ProductCardView, vamos falar que é um FunctionalComponent. Isso vai receber o return type typeOf useProductCardViewModel Dessa forma, todas as variáveis que nós retornarmos aqui na nossa camada de ViewModel, no caso estamos retornando simplesmente o Product por enquanto, já irão simplesmente vir aqui e serem repassadas aqui pra dentro, onde nós podemos simplesmente recuperar e voltar a recuperar os valores aqui de dentro. Vamos dar um reload no nosso app e podemos ver o nosso item sendo renderizado novamente em tela. Olha só que legal, que bacana. Novamente nós vamos utilizar a arquitetura MVVM aqui, porque nós teremos algumas funcionalidades, alguns comportamentos específicos dentro deste componente, onde se nós formos utilizar tudo dentro de um único componente, iríamos unificar a parte lógica e ao UI, perdendo o sentido da nossa arquitetura. Então já é interessante, nós já temos esta visão de aplicarmos esta estrutura também dentro dos nossos subcomponentes para nós organizarmos. Fica um pouco mais verboso, mas não tem problema, ao meu ver, o código fica não necessariamente verboso, mas temos mais arquivos, contanto que eles tenham consistência, contanto que faça sentido eles estarem ali. contando que faça sentido para o ecossistema da aplicação estarem estruturadas do jeito que estão. Para a aula não ficar muito longa, nós vamos parar por aqui e na próxima aula vamos começar a estilização do nosso componente. Então é isso, espero vocês lá!