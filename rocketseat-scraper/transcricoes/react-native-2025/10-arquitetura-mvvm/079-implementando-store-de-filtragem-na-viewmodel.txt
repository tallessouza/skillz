WEBVTT Seguindo com o desenvolvimento aqui da nossa funcionalidade de filtros, vamos fazer uma leve mudança aqui na nossa Store e vamos implementar mais algumas propriedades. Nós tínhamos feito anteriormente que o UpdateFilter iria atualizar o AppliedFilter, mas na verdade nós vamos utilizar uma outra propriedade aqui da nossa Store, onde nós vamos atualizar quando estivermos interagindo aqui no nosso BottomSheet. Vamos simplesmente dar um Ctrl-C, Ctrl-V aqui no nosso AppliedFilterState. Vamos simplesmente pegar o nosso FilterState. Maravilha! Vamos fazer aqui um Ctrl-C e um Ctrl-V, o que nós já temos aqui, Filter State, certo? Agora vamos só substituir aqui embaixo, onde nós temos o Applied Filter State dentro do nosso método de Updated Filter, pelo nosso Filter State, e aqui a mesma coisa. Agora aqui também, no de Reset Filter, vamos resetar completamente, inclusive o FilterState, perfeito. E um nós iremos utilizar para preenchermos o formulário que nós temos aqui no momento, vamos utilizar para interagirmos a nível de interface e quando nós clicarmos no botão de filtrar, que inclusive até coloquei o nome errado aqui, em limpar filtro, vou até ajustar aqui no nosso FilterView, vamos mudar aqui para Aí nós vamos sim de fato vir e setar todos os valores dentro do appliedFilterState. Pra isso vamos criar uma última funçãozinha aqui, que vai ser o de applyFilters, onde nós simplesmente vamos ter uma função que não vai receber nada e vai retornar um void. Essa função vai vir aqui, applyFilters. onde teremos o set, vou até passar direto aqui o set vamos ter uma callback aqui dentro, retornando um objeto literal com o get, com o state onde vamos vir aqui no applyFilters Iremos simplesmente pegar o nosso AppliedFilterState e colocar um state.filterState. Dessa forma, vamos simplesmente jogar tudo que nós já tínhamos pré-configurados anteriormente e vamos jogar aqui dentro do nosso AppliedFilterState, onde de fato vamos utilizar esta propriedade para realizarmos a requisição HTTP. Próximo passo, vamos voltar lá na nossa camada de ViewModel. Aqui no nosso useFilterViewModel, aqui vamos criar alguns métodos, algumas funções que iremos utilizar para interagir lá com a nossa camada de Store. Então, cost, vamos primeiramente pegar o nosso userFilter... Vou só mudar o nome aqui de useUserFilterStore para use... Vou só remover esse user aqui do nome do nosso CustomHook. Perfeito. Aqui nós vamos pegar o... UpdateFilter, vamos criar uma função de Cost HandleValueMaxChange, onde aqui vamos simplesmente receber um VALUE, que será um NUMBER, onde teremos o UPDATEFILTER, onde teremos a nossa KEY, sendo o VALUE MAX, e um VALUE, sendo de fato o valor que estamos recebendo como parâmetro. E olha só que legal, esta mesma função que nós criamos lá no nosso USEFILTERSTORE vai poder ser utilizada em todas as propriedades, então aqui embaixo vamos fazer a mesma coisa com MIN, COST, HANDLE, VALUE, MIN, CHANGE, Teremos aqui simplesmente o value também, que vai ser o number, seguindo o mesmo exemplo aqui de cima, vamos até copiar, só que aqui vai ser o value min. Mesma coisa teremos para o nosso handleCategoryToggle, porém lá teremos uma diferença. Nós temos aqui o cost, handle, category, toggle, toggle, Onde nós teremos aqui o CategoryId, que vai ser um Number. Aqui no HandleCategoryToggle nós não vamos simplesmente fazer desta forma, até porque não faz tanto sentido, pelo fato de nós estarmos lidando com um Array de números. Nós vamos fazer a seguinte coisa, vamos recuperar o nosso FilterState aqui do nosso CustomHook, do lado da nossa Store. FilterState. Vamos fazer uma validação. const CategoryAlreadyInArray igual a nosso featureState.selectorCategory.includesCategoryId, ou seja, estamos verificando se dentro do array que nós possuímos já existe a categoria que nós temos aqui no momento. Se ele existir, if CategoryAlreadyInArray Vamos pegar aqui o nosso Updated Filter, vamos só dar um CTRL V e alterar aqui a chave. No Valley, vamos simplesmente pegar o FilterState.SelectedCategories.Filter, vamos simplesmente rodar um filtro, vamos simplesmente pegar o id e vamos retornar todas as ids de categorias que são diferentes do category id que nós estamos recebendo como parâmetro dessa forma nós vamos remover ele agora caso contrário else se a categoria que estamos recebendo como parâmetro não existir dentro do nosso selected categories nós vamos simplesmente dar um update filter passando aqui novamente o selected categories e o value sendo um Spread Operator em filteredState.selectedCategories, vírgula CategoryId. Desta forma garantimos que caso exista de fato o item que nós selecionamos aqui na nossa lista de categorias, nós vamos desmarcar ele. Caso ele não existir, nós vamos simplesmente colocar ele lá de volta. Vamos pegar todas essas três funções que nós criamos e repassar aqui para podermos recuperar na nossa camada de View. HandleCategoryToggle. e o handleMain, handleValue, mainChange. Perfeito? Vamos remover esse erro e esse refete que não vamos utilizar também. Voltar aqui na nossa camada de view e recuperar todos esses valores. Maravilha! Vamos pegar eles agora e colocar aqui nos nossos empates. No caso, vamos pegar este handleValueMaxChange aqui de... Vamos mudar aqui o texto também até o placeholder. Vamos colocar o onTextChange, passando simplesmente o handleValueMaxCategory. No caso, aqui nós vamos ter de fazer o seguinte, vamos ter de abrir uma callback. Hoje temos aqui o nosso text, vamos converter o nosso text para number. Vamos fazer a mesma coisa que no nosso outro input, que é o de valor mínimo, handle, value, minChange. Agora aqui no nosso toggle vai ser um pouquinho diferente, vamos voltar aqui na nossa ViewModel, vamos ter que repassar também o nosso FilterState, na verdade vamos repassar somente o nosso SelectedCategories, vamos passar aqui o nosso FilterState.SelectedCategory apenas, onde vamos voltar na nossa camada de View, recuperar aqui nas propriedades, Vi aqui dentro do nosso map onde nós temos esta nossa checkbox e aqui no nosso totable opacity também. Primeiro que nós vamos fazer é simplesmente uma validação, value igual a. Vamos pegar o nosso selectedCategories.include o id que estamos percorrendo no momento. Ou seja, estamos rodando um map em cima da nossa listagem de categorias de produto e se de fato o item que está sendo iterado no momento na renderização existir dentro da nossa lista, nós vamos falar que ele de fato vai ficar marcado, ele vai ficar com checked. Vamos colocar aqui também um on, value change, a gente teremos aqui o handle, categoria toggle mandando o id e A mesma coisa aqui no nosso TOTAL OPACITY no evento de ONPRESS. ARROW FUNCTION com o HANDLE, CATEGORY TOGLE mandando o ID. Dessa forma garantimos que sempre que nós clicarmos em qualquer parte aqui das nossas categorias, vamos selecionar de fato ou não. E olha só que legal, já está funcionando perfeitamente a nossa filtragem, todos os nossos dados conforme nós formos preenchendo já vão automaticamente populando os dados do FilterState lá na nossa Store. O próximo passo é quando nós clicarmos em Filtrar, nós vamos pegar todos esses dados que estão no FilterStates e mandar aqui para o Applied Filters, onde vamos fechar o nosso modal e aplicar o filtro na requisição HTTP. Isso daqui vai ficar muito legal. Para esta aula não vai ficar muito longa, ela vai acabando por aqui. Então, espero vocês na próxima.