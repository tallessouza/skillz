WEBVTT Um novo segmento com o desenvolvimento da nossa tela de detalhes de produto. Vamos colocar algumas outras propriedades na nossa flatlist para que a UX, ou seja, a experiência do usuário fique bem mais fluida, fique bem melhor. Primeiro de tudo, vamos pegar este método que nós criamos algumas aulas passadas, esse handleAndArrayShed, ele simplesmente vai ser executado quando nós chegarmos no final da nossa listagem, ou seja, vamos pegar aqui o onAnd, Não sei nem se é assim que se pronuncia, mas enfim, vamos colocar ele aqui. E vamos fazer um teste, vamos voltar aqui na nossa Infinity Query, na nossa camada de ViewModel. Vamos vir aqui no nosso UseGetCometsInfinityQuery. Vamos mudar o PerPage para buscar apenas um. Vamos também colocar um StaleTime, de apenas um segundo por enquanto, para que se ele fez alguma requisição anteriormente, Ele dá um busque do cache, a gente mudou o per page aqui para 1 porque os produtos possuem poucos comentários, consequentemente se a gente clicasse em um ele já iria carregar todos de uma vez só, mas não é isso que a gente quer, a gente quer exibir apenas um comentário por vez conforme a gente vai chegando no final da lista igual nós temos aqui. Na listagem de produtos, vamos tirar o nosso style time, vamos manter esse per page aqui, vamos dar um reload no nosso app. Agora vamos entrar aqui nos detalhes deste produto de kit de canetas coloridas. Se nós chegarmos aqui no final da nossa lista, olha só que legal, ele já vai aumentando o scope, que ele já vai fazer a requisição HTTP e consequentemente montando os outros comentários aqui na listagem que temos na nossa tela. isso aqui é bem legal, vamos voltar o per page aqui para 20 novamente e fazer uma paginação buscando uma de cada vez, não faz tanto sentido assim o próximo passo que nós vamos colocar é simplesmente um refresh aqui na nossa flat list, vamos pegar aqui o método de handleRefetch, aqui no onRefresh, vamos simplesmente passar ele dessa forma, enquanto nós abrirmos um qualquer item aqui na nossa lista, opa, refreshingIsBoolean, ele está reclamando que nós temos que passar aqui também o refreshing, vamos simplesmente passar aqui o isRefreshing, vamos voltar aqui na nossa camada de ViewModel, vamos pegar o nosso isRefetch e repassar para dentro do objeto do return e recuperar aqui na camada de View, isRefetching, E passar aqui pra dentro. Show! Agora quando nós abrimos os detalhes do nosso produto, nós vamos poder realizar o reload aqui da nossa tela de detalhes do produto. Outra coisa que nós vamos fazer aqui é criar um componente pro footer aqui da nossa listagem. No caso, quando tiver carregando algo, nós vamos colocar um Activity Indicator aqui no final. Pra isso, vamos ir aqui dentro de Components. Vou colocar aqui o ListFooter, onde vamos criar um index.tsx, que funciona como componente padrão. ListFooter. Aqui vamos simplesmente retornar uma view, por enquanto. Aqui nós vamos receber também alguns parâmetros, interface. ListFooterParams. vai ser o isLoadingMore, vai ser um boolean, vamos chipar o componente falando que ele espera receber esta interface com o isLoadingMore, perfeito aqui acima desse loading vamos simplesmente colocar se isLoadingMore for false, simplesmente retornaremos um NULL caso contrário vamos retornar aqui um Activity, opa, ActivityIndicator, onde a tag se auto fecha, com o color simplesmente vamos importar nossa variável de cores, pegando o purpleBase, purpleBase, o size dele vamos colocar em small, small, e aqui na view, dentro do class name, um padding y de 4. Agora vamos pegar o nosso ListFuture, voltar lá na nossa Product View, dentro aqui no ListFutureComponent, vamos simplesmente passar ele desta forma, importar e passar a própria do IsLoadingMore, onde aqui nós vamos simplesmente pegar, vamos ver se a gente já está recebendo aqui a variável de quando está carregando mais, IsLoading, FetchNextPage, o IsFetchNextPage, Vamos passar ele aqui dentro do return, pra gente conseguir recuperar na nossa camada de view. Vamos passar ele aqui embaixo. Perfeito. Dessa forma a gente vai ter um loadzinho indicando pro usuário que uma requisição HTTP está ocorrendo, dando aquele feedback visual pro usuário, que é bem importante. Agora, um componente para a lista vazia, nós vamos simplesmente criar um outro componentezinho aqui chamado EMPTY LIST onde aqui dentro vamos ter um INDEX.TSX, vamos colocar aqui um EXPORT COST EMPTY LIST vamos simplesmente retornar aqui uma VIEW e aqui dentro nós também iremos receber algumas props INTERFACE EMPTY LIST PARAMS Vai ser o isLoadingComments, que será um boolean. Agora vamos tipar aqui nosso componente. Vai ser o FunctionalComponent, que receberá o EmptyListParams, desestruturando aqui a nossa propriedade. Aqui vamos fazer uma verificação. Se isLoadingComments for true, nós vamos dar um return Vamos dar um return em uma View Contendo um ActivityIndicator Ele vai se auto-fechar E aqui vamos colocar um Text Portado do React Native Carregando avaliações Aqui nessa ViewPy Colocaremos um className contendo um ItemCenter E um py de 8 No Activity Indicator, novamente um Color, vamos importar nossa variável de Colors, acessando o Purple Base, com o Size sendo Small. Aqui no Text, vamos colocar um Class Name com um Margin Top de 2, pra ter o espaçamento ali do nosso Activity Indicator, um Text Gray na tonalidade 500. Gray 500, perfeito. No return de baixo, vamos colocar um text. ainda não há avaliações para este produto. No caso, se nós caímos aqui, significa que nós não possuímos nenhum comentário no nosso produto. Isso aqui é legal para dar o feedback para o usuário, para ele não pensar se aconteceu algum erro ou simplesmente ficar sem entender por que os comentários não estão sendo exibidos. Esse feedback aqui é bem legal. E aqui embaixo, colocar outro texto com um texto de CTA, um texto de Call to Action, seja o primeiro a avaliar. E para estimular o usuário a comentar no produto, vamos colocar um className com um py de 8, um itemCenter no text, um className de textGray na tonalidade 500, com um textBase, No outro, simplesmente um class name com o text-gray um pouco mais claro, na tonalidade 400, com o text-sm pro tamanho da fonte e o margin-top de 1. Vamos pegar aqui o nosso componente de EmptyList, vamos voltar aqui na nossa camada de view do product, vamos colocar aqui no handler EmptyList, opa, no caso é o ListEmptyComponent, desta forma. Vamos importar ele, e aqui vamos passar a propriedade de isLoadingComments, onde será simplesmente o isLoading. Isso aqui é bem simples, parte do pressuposto de que se nós estamos carregando a listagem de comentários, ela irá estar vazia, então faz todo sentido nós colocarmos este loading, este carregamento aqui neste componente de EmptyList, Agora, se o IsLoadingComments, ou seja, se o carregamento, se a promise já foi finalizada e nós não possuímos nenhum tipo de comentário na nossa listagem, significa que, de fato, o produto não possui nenhum comentário até então, o que faz todo sentido. Lógico, nós renderizamos esta mensagem de CTA, informando que não possui uma avaliação deste produto e chamando o usuário para ser o primeiro. Dessa forma, a gente tem alguns feedbacks e tratativas para o usuário. Isso aqui é bem legal a gente ter ciência desse tipo de detalhe. Agora, um outro componente que nós vamos fazer é simplesmente um loading. Só uma correção, aqui na verdade não é o isLoading, esse isLoading aqui se trata do isLoading dos detalhes do produto e não do carregamento dos comentários. O que a gente tem que fazer é pegar esse getCommentsLoading aqui e repassar lá no lugar, no caso o getComponentsLoading vai ficar aqui, e esse isLoading nós vamos colocar aqui acima, desta forma, se o if isLoading for true, nós vamos dar um return em um componente para informar que o carregamento dos produtos do usuário está acontecendo de fato. Para isso não vai ser bem tranquilo, nós vamos criar um outro componentezinho aqui de loading, onde aqui vamos criar novamente um FunctionalComponent Aqui um export, cost, loading. Nós vamos simplesmente colocar aqui um return, vai ser uma view. Aqui não vai precisar nós passarmos uma prop, pois a validação da renderização deste componente, no caso a condicional, vai ficar literalmente aqui dentro da camada de view. Então aqui nós simplesmente colocamos dentro dessa view um class name contendo um flex1 pra ocupar 100% do espaço da tela e um bg, background. Aqui vamos colocar uma outra view. Onde colocaremos um ActivityIndicator. Ah, ele se auto fecha. Com um text. Text. Carregando produto. Perfeito. Vou só colocar aqui um className com um flex1, um JustifyCenter. E um ItemsCenter. No Activity Indicator, o Color, novamente, vamos importar nossa variável de cores. Purple Base. Com o Size, aqui nós vamos colocar Small, vamos colocar Large, para ele ser um pouco maior. E aqui no Text, o Class Name, contendo um Margin Top de 4, um Text Base e um Text Purple Base. Maravilha, agora vamos pegar esse nosso componente de Loading e repassar aqui na condicional do Sloading. Como eu falei, como a condicional da renderização deste componente já está dentro da ProductView, não faz sentido nós passarmos uma propriedade para dentro dele, diferentemente dos componentes que estão aqui dentro da nossa FlatList, onde nós temos condicionais internos para renderizar. Caso contrário, nós teríamos que fazer condicionais externários aqui diretamente na propriedade e o código ficaria uma bagunça, não ficaria nem um pouco legível e bonito dessa forma. Na próxima aula nós vamos... tratar os casos de erro, no caso, no momento, caso ocorra algum erro aqui na nossa tela de detalhes do produto, nós estamos simplesmente exibindo esta mensagem de erro, e caso, por algum motivo, não exista o ProductDetail, estamos simplesmente retornando um NULL, isso aqui não é nem um pouco interessante. Então, na próxima aula, vamos criar um componente e algumas tratativas para isso. Então, espero vocês na próxima aula.