WEBVTT No seguimento, com a criação das requisições HTTP de cartão de crédito, aqui dentro nós vamos criar uma nova função, vai ser um export cost create credit card, onde aqui teremos uma função assíncrona. Agora temos de entender como que é a nossa rota de criação aqui no back-end, na nossa doc, onde teremos aqui simplesmente a rota idêntica à de GET, no caso, mudando apenas o verbo HTTP, que é de POST, onde aqui ele espera receber como parâmetro no body um objeto contendo um number, um cvv e um expiration date. Vou simplesmente pegar todo este objeto, voltar aqui no nosso projeto, lá onde nós tínhamos criado anteriormente a nossa credit card. Vamos só alterar aqui o nome do nosso arquivo, vamos colocar aqui o getCreditCard dessa forma, ele já vai atualizar aqui onde nós estamos consumindo ele. Vamos criar um novo aqui de createCreditCard.ts onde aqui vamos simplesmente colocar um export interface createCreditCard.params ou melhor, request params vamos dar um ctrl-v aqui no que se espera enviar aqui é simplesmente uma string vamos fazer o seguinte aqui o number e no expirationDate vamos simplesmente passar uma string também Maravilha! Vamos ver o que ele retorna pra gente. Retorna simplesmente o cartão de crédito criado desta forma. Vamos fazer o seguinte então, pra ficar melhor, pra não ficar redundante a questão da interface do cartão de crédito. Aqui onde nós tínhamos criado este nosso GetCreditCard, vamos simplesmente mover ele pra dentro de HTTP, vamos jogar ele dentro de interface, onde aqui nós vamos simplesmente também mudar o nome, do arquivo e o nome da interface. Essa aqui vai ser simplesmente Credit Card, no singular. E esta mudança nós vamos fazer para podermos reaproveitar a interface do cartão de crédito retornado lá do nosso Backend em outras rotas e em outros locais aqui na nossa aplicação. Por exemplo, agora é simplesmente nós pegarmos o nosso Credit Card, voltar na nossa camada de Service, onde está dando o erro, ou simplesmente apagar a importação que temos aqui, e substituir a tipagem pelo credit card, que aqui no fim do dia vai continuar sendo a mesma coisa, ele vai retornar um array de credit card. E aqui é onde nós possuímos, onde nós criamos dentro de HTTP o createCreditCard, nós vamos colocar aqui um export interface, createCreditCardResponseParams, onde ele vai retornar pra gente um data contendo o credit card, caso um data, mantendo o credit card de fato, e ele vai ter também uma message e um success, message vai ser string, e um success que vai ser um boolean. Dessa forma, vamos pegar esta nossa interface, botar lá na nossa camada de service de credit card, vamos já utilizar aqui const, igual a await marketplace-api-client.post, o verbo HTTP aqui é diferente, mas o endpoint é o mesmo, no caso, vamos só copiar o que nós temos aqui na nossa rota de GET, onde aqui na frente nós vamos passar o body contendo os parâmetros, no caso aqui vai ser creditCardData vai ser igual ao createCreditCardRequestParams. Nós vamos simplesmente pegá-lo e repassá-lo desta forma. Hoje nós vamos chipar o retorno do nosso método de POST contendo o createCreditCardRequestResponse caso response dessa forma vamos só mudar aqui assim o nome para ficar mais semântico no caso vamos tirar params vamos mudar lá no arquivo também vamos só colocar create credit card response vai ficar mais semântico dessa forma simplesmente vamos pegar o data e repassar aqui pra dentro no return agora vamos criar a nossa mutation responsável por chamar este nosso método vamos ir aqui em src shared Queries. Aqui dentro de Credit Card, vamos criar uma nova chamada useCreateCreditCard.Mutation.ts. Vamos colocar um export, const useCreateCreditCard.Mutation, onde aqui vamos ter uma função. com uma constante de mutation, igual ao useMutation do React Query, como nós já vemos fazendo aqui de padrão. Então, eu vou... Aqui vamos ter o nosso mutation function, que vai ser simplesmente o nosso createCreditCard. Então, aqui a gente vai abrir parênteses desta forma, enviar o createCreditCard, onde aqui nós vamos receber o creditCardData, onde nós vamos chipar, dizendo que é um createCreditCardRequestParams e repassando este parâmetro aqui pra dentro da nossa função de createCreditCard. Vamos ter aqui um onSuccess também. Vamos simplesmente colocar aqui uma callback pra quando a nossa requisição der certo. Vamos simplesmente colocar aqui um toast.success enviando o response.message. Ou, uma mensagem genérica aqui, cartão criado com sucesso. Um outro detalhe que vamos ter de fazer aqui dentro da nossa mutation é que, novamente, nós caímos no caso de que se nós realizarmos a criação de um cartão de crédito e a nossa query responsável por buscar os cartões de créditos ainda estiver com o stale time ativo, com o stale time válido, se nós realizarmos uma requisição HTTP, ou melhor, se nós utilizássemos esta nossa query para buscar os dados do cartão de crédito, nós simplesmente estaríamos com os dados desatualizados, pelo simples fato de estarmos buscando do cache, e não do nosso back-end, lá da nossa API. Para resolver este problema é muito simples, novamente, vamos voltar aqui dentro da nossa mutation, acima da constante que criamos agora, vamos criar uma const nova chamada query client, onde aqui dentro vamos utilizar o use, queryClient, onde vamos pegar o nosso queryClient dentro do método de onSuccess que temos aqui da nossa mutation, vamos chamar o nosso queryClient.clearQueries, onde aqui vamos passar um objeto como parâmetro com o nosso query, sendo simplesmente um array, contendo o nosso, opa, vamos só voltar aqui no nosso useGetCreditCard, pegar a chave que nós temos aqui e repassar aqui pra dentro, Desta forma, garantimos que toda vez que nós realizarmos a criação de um cartão de crédito novo, nós vamos invalidar aquela chave contendo a requisição HTTP, a última resposta que foi feita na busca dos cartões de créditos. Consequentemente, o React Query já vai automaticamente fazer uma nova requisição HTTP para buscar os dados mais frescos possíveis lá do nosso back-end. E, novamente, a maravilha do React Query delegando essa responsabilidade de realizar refets E revalidações é algo fenomenal, algo que definitivamente ajuda muito no dia a dia. Logicamente daria para fazer isso aqui tudo sem o uso do React Query, porém seria muito mais verboso. No fim do dia nem é tão prático assim de nós fazermos. Agora vamos pegar a nossa constante aqui de mutation. Vamos colocar um return aqui embaixo na nossa mutation. E perfeito, com isso finalizamos a camada de model dos cartões de créditos. Aqui nós vamos simplesmente agora pegar as nossas duas queries e utilizar lá dentro da nossa mutation, na verdade a nossa query e a nossa mutation, para utilizarmos aqui dentro de viewModels e de cart. Mas vamos começar a fazer isso a partir da próxima aula. Para essa daqui não se alongar muito. É isso, espero vocês lá.