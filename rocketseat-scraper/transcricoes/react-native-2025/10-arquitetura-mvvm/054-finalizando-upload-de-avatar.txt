WEBVTT Nesta aula, dando seguimento com o desenvolvimento da nossa requisição HTTP, onde nós vamos realizar o upload da foto de perfil do usuário, iremos criar a nossa mutation responsável por executar o nosso método de upload avatar, o intermediário entre a nossa camada de service e a nossa camada de ViewModel. Para isso, nós vamos simplesmente vir aqui em src, shared, queries, alph. Vamos criar aqui dentro de alph o use, upload avatar.mutation.ts e aqui dentro vamos simplesmente colocar um export const use upload avatar mutation vai ser uma simples arrow function onde aqui dentro nós vamos simplesmente colocar um const mutation, uma variável onde nós iremos armazenar a nossa mutation de fato, vai ser igual ao useMutation, importado do próprio tanstack React Query, e aqui dentro nós vamos passar o objeto, onde dentro deste objeto teremos a nossa mutation fn, ou seja, a função que nós iremos executar quando nós chamarmos o método de mutateAsync da nossa mutation, no caso aqui nós teremos simplesmente uma arrow function, onde nós vamos receber como parâmetro a image, o ri, que será uma string, chamando o nosso upload avatar. mandando a nossa img uri como parâmetro. Um detalhe interessante é que, na verdade, nós podemos simplesmente abstrair toda essa sintaxe passando apenas o nosso upload avatar, até porque a tipagem dela é literalmente a mesma, é uma função assincronar onde ele espera receber como parâmetro o avatar, o ri, então nós podemos simplesmente abstrair tudo isso que nós colocamos desta forma, que no fim do dia vai dar o mesmo. Próximo passo, aqui dentro, nós vamos colocar um onSuccess, que será uma callback que irá ser executada no momento em que a nossa requisição HTTP for concluída com sucesso. Vamos pegar aqui a nossa response. E essa response aqui, se nós passarmos o mouse em cima, ela já está tipada, o TypeScript já pegou, que a tipagem dela é o UploadAvatarResponse, que é o retorno aqui do nosso método de UploadAvatar, que é simplesmente o data contendo o UploadAvatarParams. Onde aqui dentro nós simplesmente vamos colocar um console.log por hora no response para nós vermos o que o back-end está retornando pra gente. E vamos colocar aqui também um onError. Diferentemente da onCircuit, esse daqui irá executar se ocorrer algum erro com a nossa requisição HTTP. Então vamos simplesmente colocar aqui um console.log error. Na verdade, vamos fazer o seguinte, vamos colocar aqui um Toast, portando do próprio Toast for React Native, ponto erro, passando a mensagem, erro, ao fazer upload da foto de perfil. Detalhe interessante é que mesmo que ocorra um erro no momento do upload da foto de perfil, o nosso usuário já foi criado, já foi cadastrado com sucesso, então ele pode posteriormente realizar o envio da foto de perfil em outro momento. Então, isso daqui não é algo que irá implicar na paralisação do nosso fluxo de registro, É interessante deixar isso bem claro. Próximo passo aqui, fora do nosso useMutation, vamos simplesmente dar um return na nossa mutation. Perfeito. Próximo passo que nós vamos fazer é simplesmente pegar esse nosso useUploadMutation, vir aqui na camada de ViewModel de register, no nosso userRegisterViewModel, Onde aqui dentro nós temos a nossa função de onSubmit do nosso formulário, que é responsável de fato por realizar o cadastro, por chamar o nosso UserRegisterMutation e consequentemente cadastrar o nosso usuário. Hoje vamos fazer uma pequena alteração aqui no nosso CustomHook de UserRegisterMutation. Vamos simplesmente passar algumas propriedades pra dentro dele. Aqui vamos colocar uma interface UserRegisterMutation. onde aqui dentro nós poderemos receber o método de onSuccess, que será uma função que irá retornar um void. Este onSuccess também irá receber como parâmetro a nossa alphResponse, que é o mesmo tipo retornado pelos nossos métodos de register e de login. E esse nosso método de onSuccess será opcional, ele não será obrigatório. Vamos simplesmente pegar aqui, fazer já a desestruturação das propriedades do nosso custom hook e recuperar o nosso método de onSuccess. E aqui embaixo nós vamos fazer o seguinte, vamos pegar o nosso onSuccess? ponto, passando a nossa response. Por que nós fizemos desta forma? Este daqui é uma função que pode ser undefined. Este ponto de interrogação previne que o nosso aplicativo quebre, caso nós não tenhamos nada aqui dentro, ou seja, caso nós não estivéssemos passando nada para dentro do nosso useRegisterMutation. Vamos só aqui, após a tipagem dos nossos parâmetros, colocar o igual a um objeto vazio. para que desta forma lá na nossa camada de useRegisterViewModel ele pare de acusar erro pra gente se nós removermos isso daqui ele irá obrigar a gente a passar pelo menos um objeto vazio na chamada da nossa mutation porém isso daqui não é nem um pouco interessante, fica muito feio o nosso código então simplesmente colocamos um objeto vazio onde agora fica completamente opcional o ato de nós enviarmos um método de onSuccess Próximo passo, vamos simplesmente pegar a chamada aqui, a declaração do nosso userRegisterMutation e levar lá pra baixo, acima do nosso unsubmit. E aqui dentro, passando o nosso objeto no método de unsuccess, vamos simplesmente passar aqui uma função, onde receberemos aqui a nossa responsa. Onde este código será executado quando a nossa requisição HTTP de cadastro de usuário de fato for feita. nós vamos simplesmente fazer o seguinte, vamos fazer uma validação, se o nosso estado de avatar URI existir, ou seja, if avatar URI existir de fato, vamos abrir colchetes e fazer o seguinte, nós vamos chamar a nossa mutation de upload avatar, pra isso vamos declarar ela aqui acima, const uploadAvatarMutation igual a useUploadAvatarMutation Vamos simplesmente fazer uma desestruturação const igual a uploadAvatarMutation.mutateAsync, onde aqui ele espera receber o avatar URI. Perfeito. Aqui vamos colocar uma wait e transformar essa callback em uma callback assíncrona. Maravilha. Aqui dentro nós temos a URL da imagem que o nosso back-end retornou pra gente. Um detalhe interessante que eu acho que ficou faltando de explicar nas aulas passadas é o do porquê nós fizemos o retorno da nossa URL dessa forma. É pelo simples motivo de que quando o nosso back-end retorna, a URL das imagens, ele armazena todas as imagens diretamente no armazenamento interno da nossa API, ou seja, nós conseguimos acessá-la através do localhost 2.3001, que seria a porta do nosso back-end, parra, e aqui seria o caminho filepath até a nossa imagem, porém nós temos uma problemática que é o emulador de Android não reconhece o local roxo e isso nós resolvemos esse tipo de problema há muito tempo atrás lá na configuração da nossa instância da API do Axios utilizando o platform.select é simplesmente por isso que nós fizemos dessa forma que nós optamos por realizar a construção do retorno da nossa URL dessa forma, porque simplesmente se nós não fizermos assim, o nosso app no emulador de Android simplesmente iria quebrar, e vice-versa, se nós fizermos a montagem da nossa URL dessa forma, o emulador de iOS também não iria reconhecer. Só um adento que eu acho que é interessante. vou esclarecer para vocês agora aqui dentro do nosso if nós vamos colocar simplesmente um console.log para nós vermos se a nossa url está chegando de fato e aqui nós vamos criar uma nova constante const updatedUser igual a vamos simplesmente rodar um spread operator em response.user Colocar aqui a nossa avatar URI, pegando a nossa URL. Perfeito. O que é isso daqui? Nós vamos reaproveitar todos os dados do usuário que já foram... Que já existem, ou seja, todos os dados do usuário que já estão salvos lá na nossa Store do Zustand e vamos atualizar a nossa avatar URI, para que dessa forma nós simplesmente vamos chegar, chamar o nosso método de setUser lá da nossa UserStore. Vamos simplesmente abrir ela aqui para ver qual que nós vamos utilizar. No caso, o setSession espera receber como parâmetro o userToken e o refreshToken. Vamos ter de criar aqui um novo método para atualizar os dados do usuário. Vamos criar aqui o updateUser, que vai ser um método que vai retornar void e espera receber como parâmetro o userData. No caso, a userInterface. Vamos criar este método aqui abaixo do nosso updateToken. onde nós vamos simplesmente pegar os dados que estamos recebendo, no caso, o UserData. Não tem a necessidade de nós tiparmos aqui, porque nós já estamos passando a tipagem da nossa interface como parâmetro do Create, então ele já identifica que esse UserData é a UserInterface. Vamos chamar o nosso método de set que o próprio Existend retorna pra gente, atualizando simplesmente o User com a nossa UserDate, no caso, UserData. Uma forma diferente que nós podemos fazer isso daqui é da seguinte forma, dentro desse método de set ele também espera receber como parâmetro uma callback, ou seja, se nós fizéssemos da seguinte forma e aqui nós passássemos simplesmente um object literal, que no caso é uma função que retorna um objeto de fato, ele retorna pra gente o nosso state atual, onde nós poderíamos simplesmente mudar a forma que nós estamos setando o usuário onde aqui dentro nós vamos simplesmente fazer uma validação se o state.user de fato existir nós vamos colocar um objeto passando um spread operator no state.user Caso contrário, ele vai simplesmente ser o valor nulo. Só mudar aqui pra dois pontos e perfeito. Agora, ao invés de nós simplesmente mandarmos o UserData, como estamos fazendo aqui, nós vamos mandar simplesmente um Partion um Partial do User Interface. O que isso daqui significa? Essa função de Partial do TypeScript tem a responsabilidade de tornar todas as propriedades obrigatórias de uma interface como opcionais, o que significa que, por ser um método de Update, nós podemos atualizar pequenas partes do usuário, ao invés de atualizar o usuário todo, nós reaproveitamos tudo que nós já temos dentro da nossa Store e simplesmente repassamos a atualização que nós recebemos, no caso o Vamos até mudar o nome aqui das propriedades que nós estamos recebendo, no caso vai ser o updatedUserData. Onde nós simplesmente vamos pegar este updatedUserData e rodar um Spread Operator após o Spread Operator do state que nós colocamos. Ou seja, vamos sobrescrever as propriedades novas que estamos atualizando. Hoje nós vamos simplesmente vir aqui e colocar uma vírgula após o Spread Operator dos dados do usuário que nós já temos no nosso updatedUserData. Maravilha, dessa forma nós podemos atualizar pequenas partes do usuário sem necessariamente ter de repassar ele por completo lá na nossa camada de ViewModel, o que consequentemente acaba perdendo o sentido desta variável que nós criamos de UpdatedUserData. Tudo que nós precisamos fazer agora é aqui no nosso UsersStore, na desestruturação dele, pegar o nosso UpdateUser, rodar logo aqui abaixo, o updateUser, mandando como parâmetro o avatar, o url, que será igual a url de fato. Olha só que legal! Acabou que perdeu até o sentido dessa response que temos aqui no nosso onSuccess, então podemos simplesmente remover daqui de dentro e também do nosso userRegisterMutation. Vamos remover aqui também do nosso método. Nós não vamos mais ter de repassar aqui para dentro. E perfeito, esse tipo de mudança é super normal no desenvolvimento de um software. Eu tinha planejado passar daquela forma, mas eu achei mais interessante mostrar para vocês esta forma diferente de nós atualizarmos os dados da história do Zustand recuperando o estado atual que nós temos e simplesmente dando sobrescrevendo com os novos dados que estamos atualizando, no caso como aqui estamos enviando apenas o avatar URL, nós vamos reaproveitar todos os dados dos usuários que já existem, mais a atualização do avatar URL que estamos enviando aqui dentro. Isso daqui é muito massa. Uma outra coisa legal que podemos delegar é dentro do nosso userMutation esta função de setSet. Vamos remover ela daqui, eu vou simplesmente dar um Ctrl X ou um um ctrl c, vamos remover aqui também essa constante onde nós estamos armazenando o valor da resposta do nosso método da mutate async, vamos abrir aqui dentro e aqui dentro da nossa própria mutation nós podemos simplesmente chamar o nosso custom hook da nossa userStore, no caso userStore use userStore, pegar aqui o nosso setSession e aqui dentro do onSuccess simplesmente repassar O que nós tínhamos copiado anteriormente, mudando aqui de Mutation Response simplesmente para Response. e irá continuar funcionando perfeitamente. Dessa forma a gente até deixa mais limpa aqui a nossa camada de ViewModel, olha só como que ficou lindo o nosso código, como que ficou legível, ainda mais aplicando os conceitos de MVVM, fica tudo melhor ainda. Agora vamos realizar um cadastro de usuário e fazer o upload de uma foto pra ver se de fato tá funcionando. Vamos fazer o seguinte, vamos voltar o console.log que nós tínhamos colocado aqui anteriormente, console.log url, Vamos colocar aqui um objeto, uma chave em volta, para que ele se destaque dos demais logins que nós já temos. Vamos cadastrar um usuário novo. Vamos colocar aqui uma senha qualquer. Uma senha. MyNewUser123 arroba gmail.com Daqui a pouco eu estou ficando até sem criatividade para inventar e-mail novo para colocar aqui. Telefone. Meu nome. User. Next. Vamos escolher aqui da nossa galeria de fotos. Vamos pegar essa cachoeira. Perfeito. Quando nós clicarmos em registrar, as senhas não condizem. Deixa eu mudar aqui. Quando nós clicamos em cadastrar e olha só que legal, a nossa foto foi salva com sucesso, a nossa requisição HTTP funcionou perfeitamente. Agora se nós simplesmente abrimos essa nossa URL que retornamos do console.log que nós colocamos aqui, podemos simplesmente ver que está sendo exibido corretamente a nossa foto de perfil. Isso daqui é muito massa, agora podemos simplesmente pegar e exibir. na nossa tela de home do usuário. Outro detalhe interessante é que se nós pegarmos o back-end do nosso projeto, nós teremos aqui um arquivo específico onde nós estamos salvando todas as fotos de perfil. No caso, aqui dentro de Images, Avatars, podemos ver aqui todas as fotos que nós já possuímos. Isso daqui é muito legal. No mundo real, normalmente, isso daqui seria salvo em um bucket S3 da vida ou na Store do S3 ou em algum outro local. Mas aqui, para ficar mais prático, nós vamos salvar diretamente na nossa máquina. Então, a aula foi isso. Espero que tenham gostado. Espero vocês na próxima!