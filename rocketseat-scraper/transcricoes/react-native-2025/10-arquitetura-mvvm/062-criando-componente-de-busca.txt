WEBVTT Hoje vamos seguir com o desenvolvimento da nossa tela de home. Em específico, vamos fazer o nosso componente de search aqui da nossa tela de home. Como podem ver, é simplesmente um input contendo um botão aqui do lado para nós abrirmos um filtro. Para isso, um detalhe que ficou faltando na aula passada é simplesmente nós adicionarmos um font-bold aqui no texto de mensagem contendo o nome do usuário, no caso... Na verdade, ficou faltando adicionarmos o class-name inteiro dele, simplesmente com o font-bold. Com o text-base. Perfeito! Agora vamos aqui dentro de components, dentro de home, na nossa viewmodel criar, search input, onde aqui dentro vamos criar um index.tsx, simplesmente criar um funcional component padrão, search input, onde aqui dentro nós iremos simplesmente retornar uma view por enquanto. Vamos criar a estrutura base aqui do nosso componente. Primeiro de tudo temos um título aqui em cima, no caso um text. Vamos importar e colocar que será ExploreProducts. Hoje teremos um class name de Text2XL, vai ser um texto um pouquinho maior, um font-bold, um margin-top de 6 e um margin-bottom de 4. Vamos pegar o nosso Search Input e colocar logo abaixo aqui do nosso componente de Header. Agora vamos voltar aqui na nossa Home.view. Aqui no nosso List Header Component, vamos fazer o seguinte, vamos pegar essa nossa Home Header, vamos abrir simplesmente aqui uma função, onde nós vamos abrir e fechar um fragment, passar o nosso Home Header, e aqui embaixo o nosso Search Input. Maravilha. Vamos fazer dessa forma porque vamos passar algumas propriedades da nossa camada de ViewModel da nossa ROM pra dentro do nosso SearchInput, então vamos simplesmente fazer dessa forma aqui pra evitar ProcDrilling. Próximo passo que nós vamos fazer, vou botar aqui no nosso SearchInput, colocar aqui logo abaixo do nosso text uma outra View, onde nesta outra View vamos colocar um className de FlexRow, onde aqui dentro colocaremos uma outra View, Contendo o nosso AppInput, onde aqui dentro teremos um className contendo o flex1, para o nosso AppInput ocupar 100% do espaço. No leftIcon teremos aqui o search. No className teremos aqui também um textLg, para a nossa fonte ficar um pouco maior do que nós estamos habituados. E aqui abaixo da view do nosso AppInput vamos colocar um touchable, opacity, onde aqui dentro teremos um IONICONS portados do EXPOVECTORICONS onde nós teremos o NAME de FILTER OUTLINE com o SIZE de 24 e o COLOR vamos chamar nossa variável de COLORS.PURPLE ou melhor, colchetes PURPLE BASE onde aqui no nosso TOUCHABLE OPACITY vamos colocar um CLASS NAME contendo um MARGIN LEFT 5 o margin-top também de 2, um item-center, um justify-center, um rounded-xl, border de 1px, com o height de 48 pixels, width também de 48 pixels, e um border purple base. Maravilha! Dessa forma temos aqui uma estrutura base para o nosso componente de pesquisa. Outra coisa que vamos fazer é colocar um flex1 no class name do nosso app-input e vamos mudar aqui o margin-top para 6 do nosso Toshibopassage. Perfeito! Vamos colocar também aqui um margin-top de 6 na viewpie. Acredito que possamos remover esse margin-bottom daqui. Perfeito! Com isso temos a estilização base do nosso componente de search-input. Na próxima aula, daremos segmento com a construção da nossa listagem de fato. Então, é isso. Espero vocês lá!