WEBVTT Fala Devio! Hoje nós iremos implementar o AsyncStorage junto do nosso Existence na história que nós configuramos na aula passada foi um local para nós armazenarmos os dados do usuário juntamente do RefreshToken e do Token do usuário para que dessa forma toda a nossa aplicação tenha acesso a esses dados que muitas vezes nós vamos acabar utilizando, renderizando o nome do usuário em algum local ou até mesmo reaproveitando o Token do usuário para fazer alguma requisição HTTP nos Providers da nossa API Porém, um detalhe interessante é que o Zustand, por padrão, não persiste os dados, ou seja, quando nós realizarmos a autenticação aqui no nosso aplicativo, os dados do usuário vão se perder se nós realizarmos um refresh ou se nós sairmos do aplicativo e entrarmos novamente nele. E com o AsyncStorage nós vamos resolver esse problema, até porque não faz sentido nós realizarmos o login e simplesmente perdermos o usuário na próxima sessão do usuário. Isso obrigaria o usuário a realizar o login diversas vezes e isso não é nem um pouco interessante. Então pra isso vamos parar aqui o processo do nosso aplicativo, vamos instalar esta biblioteca, o arroba react-native-async-storage-async-storage, vamos dar um yarn add no react-native-async-storage, eu vou deixar o comando da instalação aí pra vocês, vamos rodar aqui. Perfeito, com a nossa lib instalada, vamos rodar novamente a nossa aplicação no nosso emulador. Perfeito, com o nosso app estartado, a primeira coisa que vamos fazer é aqui no início do nosso arquivo, da nossa UserStore, vamos dar um import do async.storage.from este asyncStorage no fim do dia é muito similar ao localStorage que nós temos no nosso navegador só para exemplificar se nós vimos aqui no navegador aqui no console por exemplo uma forma de nós persistirmos dados na web simplesmente nós pegarmos um localStorage.setItem onde aqui nós simplesmente passamos a chave de onde nós vamos armazenar esse valor específico, no caso, por exemplo, teste vírgula e aqui nós passamos um json.stringify ou seja, nós transformamos um objeto javascript, uma variável javascript simplesmente em um string vamos simplesmente colocar aqui nome nome do usuário se nós agora chamarmos o localStorage.getItem passando como parâmetro a mesma chave que nós colocamos aqui no primeiro parâmetro do setitem, ele irá retornar pra gente simplesmente um dizum contendo o dado que nós armazenamos e se nós dermos um reload aqui na nossa página e buscarmos o mesmo valor que nós citamos na sessão anterior, ele irá persistir o valor e no fim do dia isso daqui é muito interessante pra gente persistir novamente dados de usuário, tokens e refresh tokens. Claro que existem muitas outras abordagens até mais interessantes do que o próprio local storage, como, por exemplo, a utilização de cooks. Porém, aqui no React Native nós vamos seguir com o asyncStorage. Para nós implementarmos, de fato, o asyncStorage na nossa aplicação, precisamos importar uma outra variável aqui, vinda diretamente do nosso zustand. Vamos colocar um import from zustand barra mirror. E daqui de dentro nós vamos pegar o persist e o createJSONStorage. Vamos pegar esse persist e vamos englobar ela sobre todo esse método, sobre toda essa função, essa calberra que temos aqui dentro do nosso método de create. Vamos pegar ele, vamos remover, chamar o nosso persist. abrir parênteses e o primeiro parâmetro é simplesmente a nossa storage fato, ou seja, aquilo que nós já tínhamos anteriormente, a função que nós acabamos de copiar e como segundo parâmetro do nosso persist ele espera receber um objeto e dentro deste objeto nós vamos colocar as propriedades de name que no caso é basicamente a chave que eu acabei de mostrar pra vocês lá no local storage vamos colocar aqui market, place, alf E como segundo parâmetro, ele espera receber o Provider do armazenamento, ou seja, é possível utilizar outras fontes de armazenamento além do AsyncStorage, como por exemplo, o próprio SecureStore do próprio Expo ou até uma lib chamada MMKV. Isso pra vocês entenderem que não se limita apenas ao asyncStorage, é apenas uma forma de estar utilizando ele. E aqui na propriedade de store vamos chamar aquela função que nós tínhamos importado ali em cima, o createJsonStore. Aqui dentro nós vamos abrir parênteses e aqui vamos passar uma callback mandando como parâmetro o nosso asyncStorage. Lembrando que a necessidade de nós utilizarmos esse createJSONStorage é pelo fato de que nós só podemos salvar JSONs, ou seja, strings dentro do nosso asyncStorage, assim como é no localStorage do navegador, como eu mostrei pra vocês no início da aula. E o próprio Zustand faz toda esta abstração. pra gente caso nós não tivéssemos utilizando o sustent seria necessário nós criarmos uma camada da nossa aplicação com vários getters e setters pra nós podemos buscar e setar os dados dentro do nosso asyncStorage por exemplo nós teríamos vários métodos parecidos com esse de getUserData que seriam métodos assíncronos, e aqui dentro nós utilizaríamos o próprio asyncStorage diretamente sem a intermediação do próprio sustained. E aqui dentro nós teríamos algo como const userData, onde teríamos aqui um await asyncStorage.getItem. E aqui dentro nós passaríamos a chave de onde nós estamos armazenando os dados do usuário, no caso seria essa chave daqui de marketplaceAuth. E se nós passarmos o mouse aqui em cima, podem ver que ele pode retornar pra gente um nulo ou até mesmo uma string, e dessa string que seria o nosso JSON, então aqui nós faríamos uma validação, se o user data existisse, nós simplesmente daríamos um return no json.parse convertendo o nosso JSON em um objeto JavaScript novamente e nós teríamos vários setters e getters desse daqui na nossa aplicação basicamente para cada dado que nós quiséssemos persistir no nosso aplicativo teríamos de criar um método desse e seria muito chato no fim do dia nós temos de replicar todo esse código além disso também um método de set para nós salvarmos para nós rodarmos o set item aqui convertendo para JSON e tudo mais o próprio Zustand faz toda essa abstração De forma muito simples, só o fato de nós colocarmos essas configurações aqui, ele já vai entender que quando nós formos salvar algo no nosso asyncStorage, ele já vai... o middleware de createJSONStorage já vai interceptar a operação, converter em um JSON e no fim do dia jogar aqui no asyncStorage na chave que nós setamos aqui. Isso daqui é muito massa. E com isso, na próxima vez que nós realizarmos o login aqui do nosso usuário, quando nós sairmos do aplicativo e entrarmos de novo, os dados do usuário vão estar persistidos. Olha só que legal, que massa que a gente acabou de fazer aqui. E, novamente, comparado com outras soluções do mercado, o Zustand oferece uma simplicidade incrível, uma coisa que configurar Abstrair essa comunicação com o sistema de persistência de armazenamento aqui da nossa aplicação é uma coisa maravilhosa. Outras soluções como, por exemplo, o próprio Redux oferecem também o Redux Persist, porém não chega nem aos fés da simplicidade que é integrar isso daqui com o Zustand. Agora vamos realizar um teste para ver se está tudo ocorrendo corretamente. Vamos ir aqui dentro da nossa camada de ViewModels de registro. Vamos alterar os dados do usuário que nós tínhamos mocado nas aulas passadas para nós realizarmos o registro do usuário de fato. Vamos colocar aqui só mais um teste. Como este usuário aqui já tinha sido criado, então a API retornaria um erro pra gente. Vamos navegar até a nossa tela de registro e realizar o registro. Olha só, perfeito! Nosso usuário foi registrado com sucesso. Agora nós saímos do nosso aplicativo. E olha só, ele retornou um erro falando que faltou nós instalarmos os CocoaPods após a instalação do AsyncStorage. Então vamos simplesmente parar a instalação do nosso aplicativo, dar um cd ios e rodar um pod install. Vamos aguardar. Agora vamos navegar de volta e rodar um yarn ios. Agora, se nós fecharmos o nosso app e abrirmos ele novamente, navegarmos até a tela de registro, podemos ver os dados do usuário persistidos da mesma forma do momento em que nós realizamos o cadastro. E isso daqui é muito bacana, até porque em duas aulas nós configuramos toda uma Store do Zustand para utilizarmos aqui na nossa aplicação. E por mais que faltem coisas aqui a serem implementadas, isso daqui já vai ser o suficiente para seguirmos com o desenvolvimento do fluxo de autenticação. e de registro aqui do nosso app. Uma última coisa que vamos fazer antes de finalizar a aula é implementar o método de logout, de fato. Nós vamos apagar essas chaves e vamos retornar simplesmente o set, que é o método retornado aqui da nossa callback. Vamos aqui dentro abrir chaves e vamos resetar os valores que nós temos no nosso store, ou seja, vamos simplesmente copiar o que temos aqui em cima e colocar aqui dentro do nosso set. Sempre que nós executarmos esse método, essa função de logout, nós vamos apagar tudo que nós temos referente ao usuário, o token e o refresh token. E com isso aqui temos o nosso método de logout finalizado. Espero que vocês tenham gostado da forma que nós configuramos o Zuster nestas duas aulas. Na próxima nós seguiremos com o fluxo de autenticação e registro do nosso app, melhorando ainda mais a nossa camada de UserRegisterViewModel. Então é isso, espero vocês na próxima aula.