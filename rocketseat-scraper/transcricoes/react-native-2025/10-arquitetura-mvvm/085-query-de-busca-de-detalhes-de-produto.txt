WEBVTT Dando seguimento com o desenvolvimento da nossa tela de detalhes, hoje nós vamos criar camada de service aqui para realizar a requisição HTTP na rota que vimos na aula passada. No caso, esta rota daqui, de barra product, barra o ID do produto específico. Para isso, nós já vamos reaproveitar o service de products que nós já possuímos, no caso, dentro de shared services products. Vamos criar aqui uma outra função, export, const, getProducts, ou melhor, getProductDetail, onde vamos ter aqui uma função, essa função vai ser assíncrona, e aqui vamos receber o id do produto, que vai ser um number. Vamos simplesmente dar aqui um const igual a await marketplace API client, a nossa instância do Axios, ponto, get, enviando pra lá, barra, product, vamos só ver se é no plural ou no singular, aqui tá no plural, então products, id, que a gente só vai abrir um cifrão e enviar como parâmetro aqui da nossa requisição o id que estamos recebendo na nossa função. Vamos desestruturar aqui o deira. E aqui é simplesmente dar um return no data. Agora vamos tipar o retorno aqui da nossa requisição HTTP. No caso, vamos copiar todo este objeto que nós temos. Voltar aqui no nosso projeto. Aqui em Product Category, vamos ir aqui dentro de Interfaces. No caso está Shared Interfaces HTTP. No caso, vamos colocar aqui Product Detail.ts. Export Interface. Vou colocar aqui getProductDetailInterface, vamos dar um ctrl-v e aqui a gente só substitui na tipa normalmente id vai ser number, todos esses com um zero aqui a gente seleciona coloca number e todas essas strings a gente seleciona e remove as aspas Perfeito, vamos pegar aqui o nosso GetProductDetailInterface e voltar aqui no ProductServices e tipar aqui antes a abertura dos parênteses no método de Get da nossa instância do Axios, vamos importar. Perfeito, agora se passarmos o mouse aqui em cima da nossa função ela já está dizendo que é uma promessa que pode retornar o GetProductDetailInterface. Próximo passo, vamos criar a query. Para isso, vamos em src, shared, queries, product, vamos colocar aqui o useGetProductDetail.ts. Aqui vamos colocar o export, const. Só corrigir aqui o nome do nosso arquivo, coloquei o igual ao invés do traço. Export, const. useGetProductDetailQuery vai ser igual a uma função, no caso aqui vai ser simplesmente um custom hook que nós vamos fazer vamos criar aqui uma constante const query igual a useQuery passando como parâmetro aqui do objeto do nosso useQuery do React Query o query fn, query function vai ser o get ProductDetail enviando como parâmetro o id. No caso aqui, vamos receber o id como parâmetro do nosso CustomHook. Vamos simplesmente pegar aqui o ProductId. Vai ser um number. Vamos enviar aqui dessa forma, vamos só passar aqui assim, transformar aqui em assíncrona. Próximo que vamos passar é o QueryKey, vai ser um array, no caso é o QueryKey, para que o React Query saiba onde armazenar o cache e caso nós realizemos a requisição HTTP deste mesmo produto com este mesmo ID, nós vamos pegar do cache ao invés de bater no back, no caso aqui vai ser o ProductDetail, vírgula, enviando o product id, dessa forma ele cria um cache para cada produto que a gente consulte. E aqui a gente simplesmente pega o nosso query e coloca no return, desta forma. Com isso temos a configuração da nossa função responsável por bater na nossa API e temos também a query onde utilizaremos na camada de ViewModel da nossa tela de detalhes. Então é isso, espero vocês na próxima aula.