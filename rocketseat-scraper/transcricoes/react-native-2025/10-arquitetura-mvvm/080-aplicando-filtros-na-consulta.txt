WEBVTT Vamos agora implementar a funcionalidade de fato de clicarmos no botão de filtrar, fechar o nosso modal e aplicarmos o filtro aqui na nossa tela de home. Para isso vamos vir aqui no nosso useFilterViewModel, vamos criar uma função nova, const handleApplyFilters() Aqui dentro do nosso handle applyFilters, vamos ter de vir aqui na nossa useFilterStore, pegar a função de applyFilters que criamos lá na nossa store, executar aqui dentro do handle applyFilters, vamos também desestruturar aqui a store do nosso bottomSheet, então useBottomSheetStore, bottomSheetStore, onde vamos pegar aqui o método de close, para nós fecharmos ele, Close. Vamos repassar esse HandleApplyFilters aqui para o Return, para que ele já fique disponível lá na nossa camada Divio, onde nós vamos simplesmente pegá-lo. voltar aqui embaixo na nossa função de filtrar. Nós vamos colocar aqui no appButton o onPress de HandleApplyFilter. Vamos simplesmente pegar os filtros que já temos aqui, clicar em filtrar e ele já está fechando automaticamente o nosso bottom sheet. E se nós dermos um console.log aqui nos nossos applied filters. Só dar um console.log. no Applied Filters, vamos abrir aqui novamente. E olha só que legal, já temos aqui o array de categorias selecionados, prontos para serem enviados para o nosso back-end, para a nossa requisição HTTP. Agora o que nós precisamos fazer é voltar aqui na use-home.viewmodel, onde nós de fato temos aqui a nossa infinite query. Vamos desestruturar aqui a const igual a use-filter-store. Vamos pegar aqui o Applied Filters. onde nós vamos vir aqui dentro da nossa useProductInfinityStore, vamos repassar ele aqui pra dentro, no caso, vamos querer aqui uma interface, products, infinity, query, forums, Vamos até ajustar esse infinity terminando com um E aqui no nome do nosso Custom Hook. Vamos só ajustar aqui e aqui também. Perfeito. Onde aqui nós vamos ter? Os Filters. Onde nós vamos tipar com o FilterState. Vamos pegar. Desestruturar e já tipar aqui o objeto contendo o Filters. Vamos colocar também que ele pode ser opcional. E esses filters nós vamos repassar eles para cá, no nosso getProducts, em filters. Vou só pegar filters, ele está reclamando aqui, provavelmente, algum detalhe de tipagem. Para a gente resolver esse problema é bem simples, é só a gente vir aqui na função de getProducts, no productRequest, vamos transformar todas essas propriedades aqui em opcionais, o mainValue, o searchText, CategoryId vamos deixar, o to e o from também vamos alterar. to e o from, perfeito. Agora vamos ir aqui, colocar dois pontos, abrir chaves, vamos pegar as propriedades que nós temos, no caso o categoryId vai ser igual ao filters.selectedCategories ou um arre vazio, temos aqui também o maxValue, nós vamos pegar aqui, do filters.valueMax ou vamos passar simplesmente um undefined assim ou minValue, mesma coisa, filters.valueMin ou undefined Por fim, o searchText, nós vamos simplesmente passar aqui o filters.searchText ou também um undefined, ou melhor, um undefined. Dessa forma a gente garante que quando nós selecionarmos, por exemplo, alguma categoria, como por exemplo de brinquedos, e clicarmos em filtrar, o search irá realizar corretamente trazendo apenas os produtos que possuam essa categoria específica, mesma coisa aqui de utensílios domésticos, se nós clicarmos e dermos um reload, ele já vai trazer aqui pra gente todos os itens que são da categoria de utensílios domésticos, agora a gente só precisa fazer com que ao fechar ele automaticamente já aconteça esse auto refresh, pra isso é bem simples, nós vamos pegar esse objeto de filters e vamos passar aqui dentro do nosso query key, vamos colocar vírgula e dar um ctrl v, Essa query key, novamente, é a chave que nós utilizamos para pegar o cache interno da... Ao invés de dispararmos a requisição HTTP, e quando nós passamos o objeto de filtro, ele cria uma chave personalizada para aquela consulta específica. Então, supondo que nós removemos aqui, utensílios, e pegamos aqui vestuário, clicamos em filtrar, ele vai fazer uma outra requisição HTTP automaticamente. pelo simples fato do query aqui ter alterado, então ele entende que nenhum dos caches que existiam anteriormente vai satisfazer este conjunto de dados específicos dessa requisição HTTP que estamos fazendo, no caso essas que estamos logando aqui no momento, então ele vai disparar uma outra requisição HTTP buscando apenas os itens que satisfaçam o filtro que nós selecionamos. No caso aqui, vestuário e brinquedos. Ele trouxe aqui um jogo de tabuleiro e um tênis esportivo. Olha só que massa! Então, se nós desmarcarmos aqui e clicarmos em filtrar, ele irá trazer novamente todos os itens que possuímos na loja. Próximo passo que precisamos fazer é adicionar a funcionalidade de limpar filtro aqui no nosso botão. Isso é bem simples, vamos simplesmente vir aqui no nosso UseFilterViewModel, vamos desestruturar aqui do nosso UseFilterStore, o Clear, o ResetFilter, vamos criar um novo método, no caso, const handleResetFilter, vai ser igual a uma função, Onde aqui dentro nós vamos executar o método de close do nosso bottom sheet e vamos executar bem o resetFilter. Vamos pegar esta função e repassar ela no return. Desta forma a gente pode simplesmente vir e recuperar este dado aqui na nossa camada de view. Desta forma. Vindo aqui no onPress de limpar filtro. chamando essa função, ou seja, agora quando nós tivermos algum filtro específico aplicado, Quando nós clicarmos em limpar filtro, ele vai fechar automaticamente, vai fazer a requisição HTTP. E vocês podem ver que tá tudo sendo muito rápido, pelo simples fato de que nós estamos cacheando todas essas respostas com o React Query. Então, basicamente, já temos tudo instantaneamente. Isso aqui estamos batendo lá no nosso back-end, apenas quando o stale time da nossa query, de fato, é expirado, que ele faz uma requisição HTTP. Ou quando nós temos um conjunto de filtros novos que ainda não tinha sido pesquisado anteriormente, por exemplo, isso aqui de R$10 até R$200. Nós clicarmos aqui, olha só que legal, ele fez uma requisição muito rápida, trouxe os itens, e a partir do momento em que nós novamente formos fazer essa mesma pesquisa, ele vai bater aqui no cache interno, em vez de bater na API. E um outro detalhe é que nós esquecemos de colocar o placeholder aqui no nosso search input. Vamos simplesmente vir aqui no search input, no placeholder, colocar aqui, pesquisar. Maravilha! Com isso, finalizamos a etapa inicial aqui dos nossos filtros. Na próxima aula, iremos construir um debauche para realizarmos a pesquisa conforme nós fomos digitando aqui no nosso input. Então, é isso. Espero vocês na próxima aula!