WEBVTT Olá pessoal, então agora vamos aprender um pouquinho sobre a camada de dados. A gente aprendeu sobre a camada view, a camada de negócios. Agora a gente vai aprender sobre a camada de dados para entender qual o papel dela aqui na nossa arquitetura MVC. Então, o que é a camada de dados primeiramente? Ela é responsável por armazenar, recuperar e atualizar as informações do sistema, intermediando a comunicação com o monte de dados e alguma outra fonte de dados. do sistema, intermediando a comunicação com um monte de dados e alguma outra fonte de dados. Então se eu quero, por exemplo, num CRUD, eu quero criar, eu quero atualizar, remover ou pegar informações de algum lugar, esse é algum lugar que define onde vai ser a camada de dados. Ele que define como ele vai guardar, onde ele vai guardar, de que forma ele vai te retornar e assim por diante. Então a camada de dados é a única que sabe qual a implementação vai ser feita. Então se ela vai utilizar uma SQL, se ela vai utilizar um NoSQL ou um SQL normal, então se é um monte de dados relacional ou não relacional, se é um monte de dados MySQL, PostgreSQL, DynamoDB, DocumentDB, MongoDB, o que for ela vai definir isso pra você, tá? Então é a camada de negócio que define e ela simplesmente te retorna a informação ou atualiza a informação que for pedida pra ser atualizada, tá? Então a responsabilidade dela, basicamente gerenciar as operações básicas de criação, leitura, atualização e exclusão de dados como eu acabei de dizer sobre o CRUD, né? Então quando a gente vai acabar criando, alterando, excluindo alguma informação, é ela que vai gerenciar. Validação, garantir que os dados estejam consistentes e corretos antes de serem armazenados. Isso tem diferença com a validação do dado em si. Então se a validação do dado, se o dado tem que ser, por exemplo, um juros, não pode ser maior do que 10. Se um desconto não pode ser maior do que 50%. Isso são validações de negócio. Aqui a validação é questão do dado em si. Então, eu não posso salvar string num campo que é inteiro. Eu não posso salvar inteiro num campo que é string. Porque isso vai quebrar no banco de dados. Se eu criar um campo varchar, eu não posso colocar um número. Se eu criar um campo timestamp, eu não posso criar uma string. Então, esse tipo de validação que seria feita, tá? Que geral uma string. Então, esse tipo de validação que seria feita, tá? Que, geralmente, a gente não faz tanto, porque como o Go é fortemente tipado, esses dados já vão chegar na tipagem certa lá na camada de dados. Mas, pensando em outras linguagens que, por exemplo, o Python, se a gente criasse uma camada de dados, seria um bom lugar pra gente validar o tipo, se realmente ele está de acordo com o tipo que a gente vai salvar no multidados. Apenas um exemplo. Otimização de carries. Então ele fica responsável por escrever as consultas eficientes. Especialmente para lidar com grande volume de dados. Então quem vai definir se vai utilizar index. Em quais colunas vai utilizar index. Se ele vai utilizar primary key. Se ele vai utilizar foreign key. Se ele vai utilizar quantas order a key id. Lá no multidados do Dynamo, por exemplo, se ele vai utilizar qualquer tipo de otimização que ele vai fazer, qualquer tipo de carry diferente que ele for fazer, criação e assim por diante, todo esse trabalho é da camada de dados, tá? Então apenas ele sabe as otimizações que ele tem que fazer, os joins que ele tem que fazer pra te retornar informação, então é apenas ele que faz esse tipo de tratamento, tá? Isolamento. Proteger a camada de negócio dos detalhes da infraestrutura de dados. Oferecer uma interface limpa e desacoplada. Então, eu não posso oferecer ao meu usuário ou à minha camada de negócio um método limpo, um método direto, por exemplo. Então, a pessoa me chamar um getUserById e ele chamar o meu método diretamente que vai no banco de dados MySQL. Não. No caso, eu implemento uma interface que vai ter lá o getUserById, createUser, updateUser, deleteUser e assim por diante, todos os métodos de crude do usuário. Eu forneço uma interface e eu implemento essa interface pra me retornar pra ele. Se acaso eu um dia mudar o meu banco de dados de MySQL para MongoDB, tudo isso muda, porque eu passo a utilizar JSON, eu passo a utilizar UID, eu não passo mais a utilizar UID sequencial, eu passo a utilizar várias diferenças ali que eu não quero mudar isso diretamente na minha camada de dados, na minha camada de negócio no caso, e não vai mudar caso eu esteja utilizando interface, que eu mudo apenas o meu objeto e assim eu mantenho a minha aplicação funcionando independente da minha mudança de banco de dados. Por isso que a gente tem sempre que isolar essa camada. Interação com as outras camadas. Então, interação com a camada de negócio, basicamente, é a camada de dados. Ela recebe solicitação da camada de negócio para buscar, armazenar, atualizar informações, que é a atualização de CRUD. Então, a camada de negócio faz as validações, faz as regras de negócio e envia para a camada de dados para ela realmente realizar as alterações ou fazer buscas para ele. O isolamento, detalhes internos como estruturas não devem ser expostos à camada de negócio como eu disse, devem ser totalmente expostos via interfaces e o método de dados ali, a camada de dados fazer a implementação, a gente vai aprender isso na prática. Evitar dependências diretas com o controller. Então o controller nunca deve interagir diretamente com o banco de dados ou executar consultas SQL. Isso nunca pode acontecer. Por quê? Senão não teria por que a gente ter uma camada de dados. Se eu vou criar uma camada de negócio que acessa o banco de dados direto, a gente está ferindo muito o MVC e não faz o menor sentido nesse caso, né? Então todo tipo de acesso a dados, seja ele o anti-dados ou seja ele acesso externo, não deve estar na camada de negócio e sim na camada de dados, beleza? Forneça resultados processados pra camada de negócio. Então ao invés de retornar o dado bruto, por exemplo, um cursor lá do SQL ou um anti-dados lá como um JSON direto do MongoDB sem tratativa e ele tratar essa informação, não, você pega essa informação transforma ela em DTO transforma ela em Entity, transforma ela sei lá, dependendo da sua arquitetura transforma ela no dado que realmente o negócio ali, a camada de negócio espera e retorna pra ele, então se eu tenho um objeto ali de domain eu vou e transformo em domain e retorno o dado já tratado pra ele, eu não vou tratar ele e retornar de qualquer forma que vai me retornar do banco de dados, porque não faria sentido, né? Porque aí o banco de dados ele tem que fazer esse papel em específico pra que a camada de negócio não saiba qual a implementação está sendo feita. Se eu retornar pra ele, por exemplo, um JSON que tenha um campo underline ID com o identificador do meu objeto, eu sei que a minha camada de controller, a minha camada de dados vai saber que eu estou utilizando o MongoDB. Se eu retornar pra ele um cursor ou uma lista de cursor, aquele list.rows lá do SQL, ele vai saber que é implementação utilizando SQL. Aí eu crio uma regra específica pra isso e um dia você muda de SQL pra não SQL. E aí você quebra toda a regra de negócio então você não pode fornecer dados brutos e sim tratar os dados antes de retornar beleza então basicamente essa camada de dados aqui que a gente pode implementar é assim que a gente consegue fazer a tratativa de dados e separar ela das outras camadas nosso projeto a gente vai aprender um pouquinho mais na próxima aula então