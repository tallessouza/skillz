WEBVTT Antes de a gente começar a criar as migrações para a nossa tabela de usuários, vamos só dar uma lidada com a estrutura da nossa API. Vamos começar definindo o pacote API. Dentro da internal, eu quero começar mantendo toda a lógica relacionada a handlers, helpers, JSON, autenticação, dentre outras coisas, como o WebSocket. E para isso eu vou usar a pasta API. Então, API, barra. Aqui, a primeira coisa que eu gosto de fazer é lidar com a injeção de dependências. Então eu vou criar um tipo, API, que vai ser uma struct onde a gente tem cada campo para as dependências necessárias do nosso projeto. Dentro dessa API.go Eu vou colocar O package API Definindo o nome do nosso pacote E definir o nosso tipo Então vai ser type API Que vai ser uma struct Por enquanto ela vai ser vazia E eu quero falar um pouquinho sobre ela O tipo API também vai ter definido como métodos Cada um dos handlers que a gente vai usar para cada uma das rotas. Por exemplo, se a gente tiver uma rota de criar usuários, a gente vai ter como método aqui uma func que vai ser API pointer para API. Create. Handle. Create. User. E aqui a gente vai ter o famoso WR e lidar com a requisição aqui dentro. Então é assim que eu gosto de fazer a nossa API. Por que eu gosto de fazer dessa forma os meus handlers? Porque desse jeito eu consigo acessar cada uma das dependências que a gente tem de dentro desse método. Então, se eu tiver aqui, por exemplo, um userService, ele vai ser acessível dentro desse handler, dentro desse método. Se eu tiver aqui um router e eu precisar lidar com esse router, eu consigo fazer isso daqui de dentro. E eu também vou ter acesso a todos os outros métodos que a gente tem. Então, é por isso que eu gosto de fazer dessa forma. Então, o que a gente precisa primeiro dentro da nossa struct? A primeira coisa que a gente precisa é um router. Então eu vou usar aqui, router.mux. Na verdade eu vou fazer disso aqui um ponteiro. Outra coisa que eu também tenho que fazer aqui só para facilitar com a nossa vida é colocar esse router como sendo um campo exportado sendo um campo público para fora desse pacote então vou deixar ele em maiúsculo a gente está tendo um problema aqui e o nosso módulo não tem o github.com.go.t dentro. Então vamos pedir para que esse módulo seja instalado. E outro problema que a gente tem aqui também é que a gente quer a versão v5. Então vamos modificar isso para v5 e rodar o gomodtidy. E vamos aproveitar também Para rodar a nossa aplicação Então vamos rodar o comando Err que a gente aprendeu lá na nossa última aula Err Hello Rocket City A gente continua na mesma coisa da última aula Mas vamos mudar isso Agora que a gente não tem mais nenhum erro Vamos adicionar aqui o arquivo Routes.go E aqui dentro vai ser o arquivo routes.go e aqui dentro vai ser o arquivo que vai definir cada uma das rotas da nossa aplicação. Então vamos lá. Package, API. Eu curto fazer aqui uma função método que tem como receiver a API uma função chamada bindRoutes. Essa bindRoutes vai dar bind em cada uma das rotas no nosso mux, no nosso tmux dentro da nossa API. Então, para isso, a gente vai usar api.router.route e a gente vai dar o nosso pattern. Eu gosto de fazer barra API, definir a função. Então, é uma função que recebe um shrouter e não retorna nada. E aqui dentro eu vou usar um r.route, e aqui a gente vai ter uma subrota. E eu curto deixar uma v1, v2 e por assim em diante. Isso é simplesmente para ajudar caso a gente vá refatorar isso aqui em outro momento. Então, barra v1, deixa eu salvar para ele importar aqui o t que está com o command defined, e vamos seguir em frente. Dentro dessa v1, o que a gente está definindo é o caminho para as rotas dentro desse r.route na função func rt.router que aqui dentro todas as requisições vão ser barra api, barra v1, barra algum outro path. Então é por isso que a gente faz dessa forma. Se uma requisição for feita aqui em cima é //api //alguma coisa, algum outro path. Então o X é muito bom para criar rotas e a gente tem uma boa visão do que está acontecendo. Por isso eu acho que criar essa bound routes, porque o X já dá essa facilidade e bound routes ajuda um pouquinho mais a gente ter tudo junto em um lugar só. É muito mais fácil de visualizar todas as rotas da nossa aplicação. Então vamos começar dentro dessa API v1, criar as nossas rotas de usuários. Então r.route barra users e vamos fazer a mesma coisa em cima func rx.router E o que a gente precisa aqui dentro? Que tal a gente começar Criando uma rota de signup Então r.post Que vai ser uma post request O usuário vai precisar enviar informações sobre ele mesmo. E vai ter aqui, signup, e a gente vai usar api.handleSignupUser. E vamos fazer um também para login. Login. API. Handle. Login. User. Esses métodos não existem, então a gente vai ter que criar eles lá como método na nossa API. Para isso eu vou criar um outro arquivo chamado userHanglers.go. E aqui dentro desse arquivo a gente vai ter todos os renders dos nossos usuários. Se a gente tiver um product, a gente vai criar um productHandlers.go. Eu também vou aproveitar para mudar aqui, porque o LSP está importando errado e ele deve estar importando o X na versão v5. Então vamos lá implementar esses métodos. Então package. Então aqui package API. Func API. API pointer de API Vou fazer aqui handle signup user wr que recebe um response writer em um HTTP request e a gente vai fazer um panic aqui só porque a gente ainda não implementou. Todo not implemented. E vamos fazer a mesma coisa pro handle login user. Vamos aproveitar que a gente está aqui também. E vamos criar um Handle Logout. Logout User. E também colocar aqui dentro. Que a gente vai ter um Handle Logout. Logout. Handle. Handle Logout User. Então aqui a gente tem definida todas as rotas de usuários. Eu posso tirar esse barra daqui. Agora que as rotas de usuários foram definidas, a gente primeiro precisa ter os usuários dentro do nosso banco de dados. Então para isso a gente precisa de uma tabela para salvar esses usuários. Então vamos criar isso agora.