WEBVTT A gente tinha falado na última aula que a gente ia falar mais um pouco sobre o CRC RF Token. Então, nessa aula eu vou demonstrar um pouco de por que isso é um problema e aonde isso pode afetar a nossa aplicação. Imagina o seguinte cenário. Ao usuário tentar logar na nossa aplicação, ele manda o username e o password dele. Ao ser autenticado, a nossa API, o nosso back-end vai retornar para ele um HTTP status OK, indicando que ele conseguiu logar e vai mandar também uma seção, um cookie para esse usuário. Isso aqui vai ficar salvo dentro do cliente e toda vez que ele fizer uma requisição para api.golbid.com, que é o local de origem desse cookie, essa seção vai ser enviada automaticamente. A gente não precisa setar, enviar numa requisição específica, colocar lá dentro que esse cookie é enviado automaticamente. Isso tem a sua vantagem e desvantagem. A parte boa disso é que você não precisa setar em nenhum momento que esse cookie precisa ser retornado para a nossa API em cada requisição que é feita. Só que tem um problema com isso também. Como isso é feito de forma automática, algumas pessoas tentam usar do fato de ser automático para se passar por você. Vamos falar um pouquinho sobre isso. Imagina agora o seguinte caso. Vamos supor que um cliente acesse um website malicioso que tem conhecimento que a API.golbit.com não tem CSRF tokens setados na aplicação. Então, o website malicioso, através do seu browser, tenta fazer uma request para a API.golbit.com, deletando um usuário. Por mais que o website malicioso não seja da mesma origem do api.golbit.com, se o cliente estiver utilizando algum browser que não tenha a feature de same site, o http cookie que foi mandado para ele vai ser enviado automaticamente para api.golbit.com e assim o próprio usuário pode ser deletado. E caso seja algum usuário com privilégios mais altos dentro da nossa aplicação, ele pode fazer várias coisas. Por exemplo, deletar outros usuários ou seja lá o que for. Você essencialmente agora foi hackeado sem perder a sua senha. E isso é um problema. Para isso, a gente utiliza uma técnica chamada CSRF Tokens. Agora, num fluxo padrão para a gente poder logar na nossa aplicação, a gente vai precisar do seguinte. A primeira coisa é pegar um token CSRF e o cookie pair dele. A segunda coisa que a gente vai precisar é mandar uma requisição post para logar com o nosso email ou username e o nosso password, com o cookie e com o token CSRF dentro do nosso header. E a terceira coisa é receber a session token da API no nosso cliente como um cookie. A primeira parte é receber o token CSRF. A rota para pegar o token CSRF é a rota que vai enviar dentro de um header ou no body o token CSRF e um cookie. Esse token geralmente é enviado dentro de uma página, um HTML. E a gente já vai mostrar como é que isso pode ser feito, mas a gente não vai se aprofundar nisso. Mas a ideia geral aqui é ter uma chave, uma fonte de verdade que não é enviada automaticamente para os outros sites. No nosso caso, é a CSRF token. Então imagina que o usuário fez uma requisição para GET API v1 user CSRF. Nesse momento a gente vai enviar para o usuário um cookie, nesse caso Gorilla CSRF, e vai mandar num header ou num body o CSRF token. é esse header quando chegar na nossa API. E a gente vai mostrar o porquê que isso é o que resolve. Então, ao usuário tentar fazer uma requisição post para logar na nossa API, uma requisição que precisa ser checada pelo CSRF, então, user barra login, ele tem que enviar também, além do seu username e do seu password, ele tem que enviar dentro do header o XCSRFToken. Então o CSRFToken só é acessível dentro de páginas que a gente queira que sejam acessíveis. Isso aí já deve soar um sino na sua cabeça dizendo CORS. Então é exatamente isso. Diferente do cookie, o token não é enviado automaticamente. A nossa API vai checar. É do método POST? É de um método que modifica alguma coisa? É um PUT? É um PAT? Um PUT? Um DELETE? Caso seja de um desses métodos, a nossa API vai testar, através do middleware, se tem o cookie e o token csrf. Caso o token csrf não seja existente, porque se for de um site malicioso ele não vai existir, ele não vai ter acesso a esse token, caso ele seja inexistente, ele vai falar que não existe e vai cancelar nossa requisição. Caso ele seja existente, mas não bata com o cookie que foi enviado, ele vai falar que arrancou um token inválido e vai cancelar nossa requisição. Caso os dois batam, eles seguem em frente, porque isso quer dizer que veio do cliente dentro de um site esperado de vir. Ou seja, não é um site malicioso, não é um site externo que está tentando atacar a nossa API ou se aproveitar de uma falha no browser do usuário. E aí sim, quando ele checa que o CSRF Token e o cookie estão corretos, eles batem, e aí sim a gente devolve para o nosso usuário a nossa sessão. Um dos pacotes mais conhecidos quando a gente está falando de CSRF Tokens é o Gorilla barra CSRF. É muito utilizado e muito bom. A gente também tem o nosurf. O nosurf também é uma biblioteca para lidar com o csrf attacks, mas a gente vai estar dessa vez utilizando o do gorila. Uma coisa muito comum quando a gente está falando de csrf tokens é enviar ele junto com forms. Então imagine o seguinte form. A gente não vai falar sobre essa linguagem de template, que é a linguagem de template padrão do Go, ao menos não agora, mas imagine que tudo isso que é abre chaves, abre chaves, ponto e alguma coisa, é algum campo de algum tipo. Nesse caso, o csrftoken sendo o token que a gente vai criar e passar para o usuário. Então, ao usuário fazer uma requisição para pegar uma página, isso imaginando um website server-side, que renderiza e manda o HTML via server-side rendering, quando o usuário for fazer a requisição para, por exemplo, user-create, ele vai ter como input também o csrftokens como o velho padrão aqui. É normal a gente colocar isso como hidden também. Essa é uma das formas de fazer. A forma que a gente vai fazer aqui é através de json body e depois devolvendo através do header. Para instalar o gorila csrf dentro da nossa aplicação, a gente pode usar o comando provido por eles aqui mesmo. Então, vou rodar aqui. E depois de ser a gente pode usar o comando provido por eles aqui mesmo. Então, vou rodar aqui. E depois de ser adicionado, vamos configurar e mandar isso para cada requisição e tentar testar para ver se o CSRF Token é recebido em todas as requisições que modificam alguma coisa no nosso banco de dados ou no nosso servidor. Vamos lá? Para isso, a primeira coisa que a gente vai fazer aqui é criar o middleware.csrf. O próprio Gorilla.csrf tem uma função chamada protect que faz isso para a gente. Então vamos lá. csrf middleware vai ser csrf.protect. A primeira coisa que o csrf.protect recebe é uma key de autenticação para esses csrf tokens. É o que vai ser checado e usado para gerar as nossas keys. Para isso, ele usa uma key de 32 bits. Para isso, a gente vai usar do nosso .env de novo. Então, eu vou fazer aqui, slicedbyte os.getenv, e vou passar golbid csrftoken. Na verdade verdade eu vou nomear isso aqui de key, né? porque é token key a segunda coisa que a gente vai receber aqui são algumas opções e principalmente pra caso a gente esteja usando algum protocolo que não seja seguro no nosso caso a gente está usando o protocolo http e não https, a gente vai usar o cssrf.secure false. É sempre bom colocar algum comentário aqui falando que isso aqui está falso só para desenvolvimento. Então, dev only. Uma outra coisa que você pode fazer aqui é caso seja uma boa ideia, caso você queira, colocar no seu getenv. No caso aqui eu não vou fazer, mas você pode colocar no env do seu projeto, se ele está em desenvolvimento ou não. Depois de ter criado o nosso SRF Midware, a gente precisa colocar isso dentro do nosso Chain de Midware. Então eu vou aqui, srf midware. Eu vou deixar isso aqui separado por um motivo. .user csrf middleware Porque a gente não vai usar isso durante toda a aplicação. Eu só estou mostrando isso aqui para vocês terem uma ideia de que isso existe e que você tem que cuidar disso. Mas durante o desenvolvimento é muito chato ter que lidar com o CSRF. Então, quando você subir a sua API, você coloca isso aqui. Valeu? Para agora, a gente não vai precisar. Eu só vou deixar aqui, vou mostrar o funcionamento e vamos ver se funciona. O CSRF middleware, ele só checa para requisições que são do tipo post, patch, put e delete. Então você não precisa se preocupar em colocar isso em apenas requisições que você quer. Todas elas podem ter. Se for uma requisição de um tipo que não precisa ter o CSRF token junto, ele simplesmente vai ignorar e vai seguir em frente. Só que até agora a gente não tem uma forma de pegar esse SRF token e mandar para o usuário. Para mandar para o usuário esse SRF token, aqui dentro da alph.go, eu vou criar um handler. Vai ser o func api.japi e vai ser um handle.get.csrf.token. Para isso, eu vou usar token, que vai ser csrf.token e a gente vai gerar isso a partir da nossa requisição. E eu vou fazer um json.utilils.encode json com nosso w e com nosso r. E o status code vai ser http.status, ok? E a nossa data vai ser um map string n. E o que a gente vai mandar aqui vai ser csrf token e o nosso token em questão. Agora que eu tenho esse handler criado, eu quero colocar ele dentro das nossas rotas. Então eu vou criar aqui, dentro da v1 r.get csrf token e o nosso handler vai ser api.handle get csrf token. Feito isso, O nosso handler vai ser api.handle.getCRFToken. Feito isso, vamos tentar criar um usuário ou então logar dentro da nossa API sem o CRFToken. Vamos tentar logar para ver o que acontece. Forbidden secure cookies hashkey is not set. Isso aconteceu porque a gente não setou o nosso .env lá. Então vamos setar isso rapidinho e reiniciar a nossa aplicação. Então dentro do nosso .env, eu quero setar aqui, golbid csrf key e a nossa aqui vai ser algo de 32 bits. Dentro do random.org, eu setei aqui e criei algumas strings de 32 bits. Eu vou pegar uma qualquer e vou colocar lá. Você pode gerar isso aqui automaticamente dentro da sua aplicação, mas no meu caso, eu estou usando uma aqui dentro de um .inf. Agora que reiniciou a nossa aplicação, vamos fazer uma requisição de login de novo. Agora deu forbidden, falando que o csrftoken não foi encontrado na nossa request. Então, para fazer o login, eu preciso pegar esse SRF token primeiro. Então, vamos fazer uma requisição para piv1-csrftoken. Pegar esse token. Ele vai setar um cookie que vai ser checado com aquele csrftoken. E agora que a gente quiser logar, a gente tem que setar esse header. Então, vou adicionar um header aqui, x-csrf-csrf-token, e eu vou setar aqui o valor sendo aquele. E agora, funcionou, a gente conseguiu logar, deu status ok. Caso o csrf-token não exista, e eu tentar mandar, ele vai falar que não foi encontrado. e eu tentar mandar, ele vai falar que não foi encontrado. Caso ele exista, csx-csrf-token, e a gente coloca aqui qualquer coisa, ele vai falar que o token é inválido. E é assim que a gente protege a nossa aplicação de csrf-attacks. Como eu disse anteriormente, isso aqui às vezes é muito chato de lidar quando a gente está desenvolvendo a nossa aplicação. Principalmente porque essa csrfF Token está em memory. Ou seja, toda vez que reinicia a nossa aplicação, o usuário precisa pegar o CSRF Token de novo. E em desenvolvimento, isso aqui é muito chato de lidar. Por mais que ele funcione, sempre que o usuário tenha um csrf token salvo é bom você mandar um csrf token novo para o usuário toda vez que ele for fazer uma requisição que mude algo dentro do nosso banco de dados ou na nossa API qualquer coisa que mude de estado beleza então eu vou aqui comentar essa parte eu também vou comentar o handle get csrf token e sim, caso você queira dar deploy disso para alguma GitHub page ou fazer o deploy dessa aplicação em algum lugar, você pode implementar e deixar o csrftoken rodando. No meu caso, para desenvolvimento, eu vou deixar desligado. Então, eu te vejo na próxima aula.