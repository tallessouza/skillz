WEBVTT Vamos começar com o Handle Signup User. Só que tem uma coisa, toda vez que a gente recebe algum JSON através de uma request http dentro de Go, a gente precisa pegar esse dado abstrato que não tem tipo e transformar em algo concreto em Go, em algum tipo concreto. Só que onde a gente vai guardar esses tipos que a gente recebe e envia para o usuário? Uma coisa comum que a gente pode fazer, dentro da internal, definir uma pasta chamada useCase, e dentro dela ter os tipos de requisições para criação de usuários, criação de produtos, atualização de usuários e por assim em diante então eu vou criar aqui a pasta user e dentro da pasta user eu vou criar aqui o create user.go e aqui eu vou definir o pacote user package user e eu vou definir o tipo que eu quero type create user rack struct E eu vou definir o tipo que eu quero. Type. Create. User. Rack. Structure. E aqui é a hora que ter usado o SQL C. Emit JSON Tags vai ajudar a gente bastante. Lembra que eu falei que o Emit JSON Tags definia as tags JSON nos models que a gente tinha? Então vamos dar uma olhada lá. Dentro do model vocês podem ver que foi definido o id, como json tag, username, email, password, tudo aqui. Então a gente pode basicamente copiar esse tipo, colocar aqui dentro e pedir por apenas aquelas coisas que a gente quer receber. Então, o ID a gente não precisa. O createdAt e o updatedAt também não. O resto a gente espera que seja enviado para a gente. Legal. A gente já tem a struct. Então quer dizer que dentro do nosso handler. A gente consegue pegar o body. E transformar naquele tipo. Certo? Certo. Só que isso vai ser um problema bem grande. Porque a gente ainda não está fazendo nenhuma validação De como a gente espera que seja esse JSON Então vamos lidar com isso agora Outro pacote que eu gosto de ter dentro da minha internal É o pacote validator O pacote validator vai ter uns helpers Para a gente lidar com o JSON E a validação de structs em Go Tem várias formas de você lidar com isso, como o Go Playground Validator. O Go Playground Validator, ele utiliza JSON tags para checar os campos assim que ele é decodificado de um JSON, de um array de bytes, para um tipo concreto em Go. Isso tem suas vantagens e desvantagens, mas na minha opinião fica meio ilegível. O que eu gosto de fazer é usar algo um pouquinho mais simples. Então pra isso, eu crio o pacote validator e lá dentro eu crio algumas funções pra lidar com o JSON. Dentro desse validator eu vou criar validator.go package validator e eu vou definir uma interface a interface vai ser validator e o que ela vai retornar é um valid que vai ser um context .context e vai retornar um evaluator, que vai ser um outro tipo que a gente vai ter. O tipo evaluator vai ser apenas um type alias para um hash map de string para string, ou string n se você preferir. Porque esse aqui vai basicamente ser retornado como json para o usuário então como é que isso aqui vai funcionar lembra do nosso use case o create user hack precisa ser validado algumas coisas, por exemplo, vamos supor que a gente não quer um password menor do que 8 caracteres vamos supor que o email que ele enviou não é necessariamente um e-mail válido, é uma string qualquer. Vamos supor que a bio ou está muito curta ou está muito grande. A gente vai lidar tudo isso e vai retornar um evaluator para checar se é válido ou não. Então vamos lá. Eu posso checar aqui, fazer um método func rec, que vai ser o createUserQuest e valid, pra gente satisfazer aquela interface que a gente acabou de criar. O valid vai receber um ctx, vai ser um context.context e vai retornar um validator.evaluator Então vamos criar aqui a nossa variável var eval, que vai ser um validator.evaluator e vamos retornar ela. Obviamente aqui a gente tem que fazer algumas validações. Então, validate stuff. Vamos retornar esse eval. Mas por enquanto esse evaluator é inútil, né? Então vamos entender como é que ele funciona. Dentro do evaluator a gente vai ter algumas coisas. A primeira é o addFieldError, que vai ser para cada campo, se a gente tiver um erro, a gente vai adicionar um erro para ele. E isso vai retornar um JSON para o usuário. Por exemplo, se o campo email não era um email verdadeiro, a gente vai colocar lá da seguinte forma. Email must be a valid e-mail. E a gente vai fazer isso para cada um dos campos que a gente precisa ter dentro do nosso Create User Brack. Caso não tenha nenhum erro, os campos não vão ser adicionados e o válido vai estar correto. os campos não vão ser adicionados e o válido vai estar correto. Então, vamos criar um método para o evaluator para adicionar erros de campo. Então, onk e vai ser um ponteiro de evaluator, add, field, error, que vai receber a chave para qual é esse campo e uma message do tipo string. A message é a mensagem de erro que a gente quer retornar para o usuário. Esse e-mail aqui tem que ser um e-mail, não retorna qualquer coisa aqui para a gente. Ah, essa bio aqui tem que ser maior do que 30 caracteres e menor do que 5 mil. ser maior do que 30 caracteres e menor do que 5.000. Então, vamos primeiro checar se o nosso evaluator foi realmente criado. A não ser que a gente queira dar make aqui dentro nesse evaluator, por igual a new. O nosso evaluator vai receber o make de map string string E se ele já foi inicializado, se ele não é um ponteiro nulo, a gente vai fazer Exist E dentro do campo daqui, então, e, na posição da nossa chave. Essa sintaxe aqui, ela pode ser um pouquinho feia, mas é a sintaxe que a gente usa para acessar maps dentro dos ponteiros. Isso aqui é a de referência em Go. Ou seja, se não existe nada com essa aqui já definida com alguma mensagem de erro, se não existe, mensagem de erro, se não existe, a gente vai colocar dentro dessa posição, dentro dessa chave no nosso HashMap, a mensagem. Só que a gente não quer acessar isso toda vez. O que a gente pode fazer é checar se o campo está correto ou não. Então, vamos criar aqui uma função CheckField. Punk e ponteiro de evaluator. CheckField. Vai receber um OK, que vai ser um bool. Uma key e uma message do tipo string então isso aqui vai ser apenas um wrapper para o addFieldError se você quiser usar o addFieldError direto, você pode usar se não, você pode usar o checkField para facilitar o que ele vai fazer é se o que foi passado aqui não está ok, se isso aqui é falso, a gente vai usar o e.addFieldError com a key e com a message. E por que a gente tem esse checkField com esse ok bool? A gente vai colocar aqui alguma checagem que retorne verdadeiro ou falso E caso dê falso, a gente adiciona um erro Beleza, só que tem algumas coisas que ficam chatas de lidar De checar se é verdadeiro ou falso na mão Que podem ficar bem longas Por exemplo, checar se algo é ou não é vazio dentro de alguma string Porque, por exemplo, se o usuário manda dentro do user name a seguinte coisa, e a gente checa, if username é igual a uma string vazia, isso aqui vai retornar falso. Ele vai considerar que isso aqui não é uma string vazia. Então, para isso, a gente precisa criar um método, uma função auxiliar, na verdade, para lidar com essas checagens. Então, a gente vai criar aqui not blank, que vai receber um valor, que é uma string, e vai retornar um booleano, natural ou falso. E aqui a gente pode apenas retornar strings.space e passar o nosso value igual, diferente, perdão, de nada. Então aqui a gente vai checar. Ah, não é vazio, essa string é uma string não vazia, e a gente retorna isso aqui. Então quando a gente for checar lá no nosso user, create user aqui, eu posso basicamente fazer eval.checkfield e colocar validator.notblank o valor que vai ser o nosso rec.username e aqui, caso ele esteja vazio e caso ele esteja vazio, a gente fala que o campo é username. E que a mensagem é this field must not be empty. E assim a gente vai fazer para cada um dos campos.