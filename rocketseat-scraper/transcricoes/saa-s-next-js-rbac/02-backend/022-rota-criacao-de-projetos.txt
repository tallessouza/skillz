WEBVTT O que a gente vai começar a fazer nessa aula são as rotas relacionadas a projetos aqui dentro, então vou até criar aqui uma sessão Projects, vou pegar aqui o createOrganization, vou copiar o código, venho aqui dentro, e createProject.ts e agora nós vamos começar a criar as rotas aqui dentro, vamos lá né, todo projeto ele vai estar dentro de uma organização, então o que eu vou fazer, aqui ó, eu vou sempre ter na url o prefixo da url qual que é a organização que eu quero fazer essa ação, no caso aqui criar um projeto, então postProjects, quer dizer que eu quero criar um projeto dentro dessa organização, aqui vou trocar pra createProject, aqui dentro vou trocar a tag pra project, create a new project, e aqui no body do projeto a gente vai ter basicamente o nome e a descrição do projeto, não lembro se a descrição eu botei, era obrigatória, então aqui boto, e aqui nós vamos retornar o projectId, ou melhor, é sim, além disso nós vamos ter o parms que é a slug da organização né, que vem aqui na url, e agora a gente pode abrir pra ver mais o código aqui, o que eu vou fazer né, slug="request.parms", aqui eu tenho o id, vou pegar aqui também a getCurrentUser, opa, getUserMembership, passando a slug, isso vai me retornar dados da organização e da membership do usuário, aqui no body eu tenho o nome e a descrição, só que antes de usar o nome e a descrição, deixa eu apagar o resto do código aqui, isso aqui eu vou deixar comentado, beleza, antes de usar aqui eu quero verificar permissões do usuário, então basicamente const="getUserPermissions", passando userId e passando membership.role, fica bem fácil, e aqui eu vou pegar o cannot e fazer, se o usuário não pode criar um projeto, throwNewUnauthorizedError, you're not allowed to create new projects, caso contrário, ele pode criar projetos, daí nós vamos pegar os dados aqui do meu body e vamos criar o projeto, então const project="await prisma .project.createData, nome, slug, eu vou usar a minha função createSlug, description, organizationId, eu vou pegar o id da organização aqui, ownerId, que é o id do usuário atual e no final retorno o id do projeto criado, removo os imports que eu não estou usando, vou no meu server, createProject e podemos testar, então deixa eu pegar meu token aqui, é bom colocar o token em algum lugar que você tenha acesso fácil, para não ter que ficar fazendo login toda vez, já que o Swagger perde essa informação, ou se você quiser usar um insomnia da vida, vou fazer o teste antes de ir direto, try it out, vamos criar um projeto aqui, botar slug aqui, que é uma organização que eu sei que eu sou dono e que eu posso criar projetos, nome project-test, testando o app, execute, já retornou o project id, então quer dizer que está tudo certo com a parte de criação de projetos na nossa aplicação.