WEBVTT O que a gente vai fazer nessa aula é criar um middleware de autenticação, que é basicamente algo que a gente vai poder aqui atrelar dentro de um arquivo, de uma rota, e esse middleware automaticamente já vai trazer pra gente alguns helpers pra gente validar que o usuário tá logado e buscar informações do usuário logado também. Então, o que que eu vou fazer? Aqui dentro de http, eu vou criar uma pastinha chamada middlewares. No Fastify, geralmente, o nome disso aqui não é middleware, ele chama, é mais um plugin. Só que eu acho que o nome plugin, ele não tem uma semântica muito bom, um significado muito bom, sabe? Então eu vou acabar chamando de middleware mesmo, e ele é basicamente uma rota, funciona igual uma rota. Então export async function, dou um nome pra ele, ele vai receber um app, que é fastify instance, e aqui dentro a gente pode adicionar algumas coisas, né? O que que eu vou fazer aqui? Eu vou dar um app.addhook pre-handler, e aqui dentro eu tenho acesso ao request, e aqui eu poderia, por exemplo, já fazer uma verificação se o usuário tá logado usando o request.jwtverify. E aí o que faria é que basicamente toda a rota que eu adicionasse esse auth, o usuário só conseguiria acessar se ele estivesse autenticado, né? Mas talvez possam existir rotas na nossa API que eu quero obter, por exemplo, os dados do usuário logado, mas se ele não tiver logado, não necessariamente eu quero invalidar o acesso dele. Então o que eu tô preferindo fazer nas minhas aplicações é, ao invés de simplesmente vir aqui e checar se o usuário tá logado e se ele não tiver, retornar um erro, é adicionar um novo método dentro do request, então fazer algo como, por exemplo, request.getCurrentUserAuth. E aí eu boto uma função nova aqui dentro, por enquanto vai dar erro de tipagem, logo a gente arruma. E aqui dentro eu faço tryConstPayload="awaitRequest.jwt.verify", falo que dentro do jwt eu vou ter aqui um sub, que é uma string, aí eu pego aqui o sub, e eu dou return nesse sub, se não, no catch eu dou throwNewUnauthorizedError, aqui eu nem preciso do erro, posso só dar o catch dessa forma aqui, invalidAuthToken, e aqui ele vai dar erro porque isso aqui não existe por padrão, então quando a gente quer adicionar novos métodos no request, eu preciso adicionar isso na tipagem do Fastify, então aqui eu vou criar uma pastinha arroba types na raiz do meu API, e vou criar aqui um Fastify.d.ts, que é um arquivo de definição de tipos, e aí eu faço o seguinte, eu importo o Fastify, e aí eu dou um declareModuleFastify, e aqui eu posso exportar uma interface chamada FastifyRequest, e aí o que acontece, no TypeScript, não sei se você sabia, quando eu tenho uma interface, por exemplo, user, e boto um id, string, embaixo eu crio uma interface com o mesmo nome, e boto o nome, string, se eu criar aqui uma constante user, e falar que ela é do tipo user, olha só, eu tenho que informar o id e o nome, por que? E no TypeScript, por padrão, as interfaces são extensíveis, eu posso declarar a mesma interface duas vezes, e o TypeScript automaticamente combina elas, é diferente de um type, o type não faz isso, e aí é por isso que aqui dentro da tipagem, só de eu criar uma interface que é o mesmo nome da interface que o Fastify usa internamente, aqui, até se eu passar o mouse aqui no middleware, nesse request, o tipo dele é FastifyRequest, só de eu criar com o mesmo nome, e adicionar alguma coisa aqui dentro, o TypeScript já vai entender que eu estou estendendo essa interface, então, por exemplo, getCurrentUserId, é um método que retorna uma promise, string, que é o id dele do usuário, então pronto, agora veja que aqui dentro, ele ainda está dando erro, porque, deixa eu ver o testconfig, ah, o testconfig aqui, ele está incluindo somente arquivos na pasta source, então vou ter que passar aqui o types também, todo o arquivo que tem aqui dentro, .d.test, e agora sim, já está dando certo, agora lá no meu getProfile, teoricamente eu poderia vir aqui, e usar request.getCurrentUserId, e aí uso dessa forma aqui, UserId, e aí uso UserId aqui embaixo, só que, para eu usar esse método getCurrentUserId, eu tenho que passar aqui em cima, da minha rota aqui, antes do get, um .registerAuth, que é o meu middleware, salvo isso aí agora, e agora, teoricamente, se eu chamar essa minha rota ali, passando o token, deu erro, deixa eu ver, request.getCurrentUserId, is not a function, um .registerAuth, eu acho que é porque eu preciso, aqui no meu middleware, eu preciso passar app.decorateRequest, getCurrentUserId, deixa eu ver se só isso já vai ser o suficiente, não, request, tá, já sei o que é o problema aqui, isso aqui na verdade nem precisa, o que acontece, tá, isso aqui é importante a gente entender que é algo que todo framework tem que é a questão de contexto, tá, o que acontece, imagina o seguinte, quando eu tô aqui na minha rota getProfile, eu tenho um app, certo, que é o que eu tô recebendo como parâmetro aqui, dentro desse app aí eu uso o método get, que eu tenho acesso ao request e ao reply, certo, quando eu dou um register nesse plugin aqui, nesse auth, aqui dentro também tem o app, que eu tô utilizando aqui dentro pra adicionar algum tipo de lógica, só que pro Fastify, quando eu adiciono algum tipo de lógica aqui no meu app, eu tô adicionando somente no contexto desse arquivo aqui, ou seja, se aqui embaixo eu viesse e usasse o request.getCurrentUserId funcionaria, porque eu tô dentro do mesmo arquivo, por padrão tudo que eu adiciono de hooks aqui no Fastify, ele adiciona somente pro arquivo atual e não pra aplicação toda, não pra todos os arquivos que usarem, por isso que o Fastify criou, isso aqui até é um módulo do próprio Fastify, que é o FastifyPlugin, que é exatamente um carinha que eu vou usar quando eu tô criando esses plugins, que eu quero que tipo, quando eu uso eles na minha rota, eles apliquem pra toda a aplicação, então a partir do momento que eu cadastro isso aqui, ele vai valer pra todas as rotas usadas, e não só aqui dentro. É bem simples de utilizar, a única coisa que a gente vai mudar é, eu vou trocar isso aqui pra uma função, alpha, e eu só tenho que escrever ela num formato diferente, aqui assim, usando o async aqui na frente, e agora eu pego todo esse conteúdo aqui e boto o FastifyPlugin por volta, então eu vou importar de dentro de FastifyPlugin, FastifyPlugin, e aí basicamente, FastifyPlugin, e passo aquele conteúdo aqui dentro, veja que agora não mudou nada, continua usando, alpha, igualzinho, só que agora, quando eu faço a requisição, ele já dá certo, ele já consegue validar e trazer o usuário, porque agora ele consegue fazer o seguinte, se eu registrei esse módulo, ele vai pegar essa alteração que eu fiz aqui dentro, que é adicionar o hook, pre-render, e ele vai adicionar isso nesta rota também, e não só dentro desse arquivo também, então isso é um pouco sobre escopo dentro do Fastify, sempre que eu quero que um plugin afete não só internamente ele, mas externamente também, eu vou usar o FastifyPlugin, e com isso, beleza, a gente já tem o nosso middleware aqui de autenticação, e agora sim, a gente já pode continuar com as demais rotas do projeto.