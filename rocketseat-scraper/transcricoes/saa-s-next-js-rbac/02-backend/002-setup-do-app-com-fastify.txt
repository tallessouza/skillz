WEBVTT O que a gente vai fazer nessa aula é criar uma estrutura aqui para a nossa API em Fastify. Então, eu vou criar uma estrutura bem simples, né? O nosso foco aqui não é tanto assim na estrutura do framework, né? Então, eu vou criar uma pasta HTTP, que é como eu gosto de fazer. Dentro eu crio um server.ts e dentro eu crio uma pastinha routes, onde vão ficar as nossas rotas, né? Aqui esse index eu posso apagar ele, por enquanto, e aqui no server é onde a gente vai criar o nosso servidor com Fastify. Eu vou entrar aqui na nossa API, vou instalar aqui o Fastify. Já vou também já deixar pré-instalado o Fastify Type Provider Zod, que é a integração do Fastify com o Zod, para usar ele como validação, a biblioteca não é Zod para validação. O Fastify Cores e o Zod também, claro, que a gente vai estar utilizando aqui dentro. Aguardar aqui ele fazer a instalação. E aí, vou começar importando o Fastify. Criar a nossa aplicação. E ouvir a porta 3333. E daí vou dar um console.log para avisar que o servidor está rodando. Além disso, o que eu já vou fazer? Vamos deixar já pré-configurado aqui o Cores, então import Fastify Cores e app.register Fastify Cores. Isso aqui já vai permitir que, basicamente, qualquer pessoa possa acessar a nossa aplicação, qualquer front-end. Por último, vamos configurar o Fastify Type Provider Zod. Aqui de dentro a gente vai importar o JSON Schema Transform, o Serializer Compiler, o Validator Compiler e, por último, o Zod Type Provider. E aí, o que a gente vai fazer? Basicamente, neste carinha aqui, neste nosso app, nós vamos jogar um .withTypeProvider passando aqui um generic ZodTypeProvider. E aí nós registramos estes nossos dois carinhas, que é o Serializer Compiler, que, na verdade, ele não vai ser dessa forma, é setSerializerCompiler e app.setValidatorCompiler passando o nosso Validator. Então, basicamente, falando como que o Fastify vai fazer a validação dos dados, e aí é usando o Validator aqui do Zod, e como que ele vai fazer a serialização dos dados. Serialização nada mais é o processo de transformação dos dados de entrada e saída das nossas rotas, dos nossos endpoints, HTTP. Beleza? Agora que a gente já tem isso pré-configurado, este nosso JSON Schema Transform aqui, a gente vai utilizar ele mais para frente, que é quando a gente criar a documentação aqui da nossa API. E aí eu vou começar criando as rotas da minha aplicação aqui dentro de Routes, e para ficar um pouquinho mais organizado, eu vou separar, mais ou menos na mesma pegada que a gente fez no Readme, aqui de separação por módulos, as minhas rotas também para a gente se encontrar. Então, vou colocar aqui, por exemplo, Auth, e vou colocar todas as rotas relacionadas à parte de autenticação, cadastro do usuário, aqui dentro, e vamos começar com a nossa primeira rota aqui, que é a rota de criação de uma nova conta. Eu vou chamá-la de CreateAccount. Essas rotas aqui, elas vão seguir a seguinte estrutura. Eu vou exportar, daqui de dentro, uma função chamada CreateAccount. Essa função aqui, obrigatoriamente, precisa ser assíncrona, senão vai dar erro. E aí ela vai receber o app, que é o Fastify Instance, que ele não conseguiu entender automaticamente. Quando acontece isso, de ele não fazer o autoimport, o que a gente pode fazer é um Ctrl-Shift-P, Restart TS Server, e aí pronto, o Ctrl-P, agora ele já entendeu o autoimport. E aí aqui, em todas as rotas, eu vou seguir essa mesma lógica, que é fazer o app.withTypeProvider, passando ZodTypeProvider, ponto, e aí eu boto a rota, por exemplo, PostUsers, que é para a criação de um novo usuário. E aqui dentro, eu vou ter o meu handler, que é quem vai lidar com essa requisição. E aí ele vai retornar, por exemplo, UserCreated. Por enquanto vai retornar só um texto mesmo, porque a gente está fazendo fictício, né? Mas, como a gente está utilizando aqui o Zod, o que eu posso fazer aqui é passar um parâmetro, um objeto de configuração aqui no meio, entre o handler da minha API e o endpoint, onde eu passo algumas configurações. Dentro dessas configurações, eu tenho o schema, que é exatamente como que eu vou fazer a validação dos dados aqui, de entrada. Então eu posso falar, por exemplo, que essa rota Users, dentro do body dela, eu espero que venha, e aí deixa eu importar aqui o Zod, que ele não importou automaticamente, Z from Zod. E aqui veja que quando eu fiz essas importações automáticas, como aqui eu estava importando apenas uma tipagem, e não uma variável mesmo do JavaScript, ele botou esse type aqui na frente, que ele pode tanto vir aqui, quanto vir aqui, olha, dá na mesma. Isso foi uma opção que eu utilizei aqui dentro do Vast Code, que é a Prefer type-only auto-import. Então quando ele percebe que eu estou fazendo uma importação automática de algo que é do TypeScript e não do JavaScript, especificamente, ele bota esse type aqui na frente, o que ajuda na hora da gente fazer o bundle do nosso projeto para produção, que os import types são simplesmente removidos do código, porque eles não trazem qualquer tipo de funcionalidade. E aí aqui no body, então, eu dou um Z object e falo, basicamente, quais são os campos necessários para criar um novo usuário. Vou botar aqui, por exemplo, nome, email e password, que vou botar um mínimo de 6 caracteres. E aí agora, o que isso aqui fez, é com que a gente tenha uma validação automatizada aí, automaticamente, quando a gente chamar essa rota, enviando no body alguma coisa que seja diferente desses valores aqui, que não bata com esses valores, o Zod já não vai deixar nem esse endpoint aqui continuar. Claro que as mensagens de erro, por enquanto, não vão ser as melhores, logo a gente vai trabalhar um pouquinho mais nisso, mas já está bem legal. E agora no nosso server, o que eu preciso fazer é vir aqui e dar um app.register e passar aqui, create account, que é a nossa rota. Aqui, o que a gente vai fazer é importar todas as rotas, cada uma das rotas da aplicação, o que vai fazer esse arquivo ficar bem grande. Caso você prefira, você pode criar aqui, por exemplo, um index.ts dentro de routes e só repassar todos os imports aqui para dentro e no server importar somente o index. Dá para fazer isso também, só que eu não acho que seja necessário, a gente não precisa ter esse medo gigantesco de ter arquivos grandes na nossa aplicação, porque uma hora ou outra a gente vai ter, desde que eles estejam bem organizados, não é um problema. Show! Com o nosso server Fastify aqui rodando, a gente pode fazer um pequeno teste. Então, eu venho aqui na minha API e para rodar agora, eu tenho que trocar aqui no meu package.json para source.http.server, rodo o dev aqui dentro, a gente vai ver o http.server running e eu posso fazer uma requisição, tanto com curl ou com post, o que você preferir. Vou fazer aqui usando o httpi, que é uma CLI bem legal. Eu vou só passar aqui o endereço, você não precisa fazer isso aqui, é só para eu mostrar realmente que está funcionando. Então, vou chamar aqui, por exemplo, a rota users, a gente vai ver que ele deu erro de validação, porque eu não enviei os dados. Vou enviar os dados agora, por exemplo, nome Diego, email diego.rocketsite.com.br e password 123456. Então, enter, ele retornou user created, ou seja, a nossa API já está funcionando, falta realmente a gente botar as funcionalidades aqui dentro.