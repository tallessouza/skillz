WEBVTT Nessa aula aqui, a gente vai criar as configurações do previewer, do slint e do typescript. E a primeira coisa que eu vou fazer aqui é remover toda essa parte de configuração do slint, previewer, typescript aqui do root, digamos assim, da raiz. Então, eu posso adotar esse arquivo slint, esse arquivo tsconfig, como eu falei, eu ia bagunçar tudo aqui, então eu nem estava dando muita atenção pra isso na última aula. E aqui, esse npmrc também é pra setar a versão do Node, eu não preciso dele aqui. E no package.json eu posso tirar aqui todas essas importações do slint, typescript, previewer, posso remover tudo. Aqui esse format também eu vou tirar. E é isso, package manager eu posso deixar, engines aqui eu posso deixar também, especifica qual versão do Node eu estou usando nesse projeto. Aqui eu estou usando a 20, mas ele bota maior ou igual a 18, que é o que o TurboRep funciona. Tranquilo, acho que aí está tudo bem. E aí eu vou começar criando aqui. Então, dentro de config agora eu vou criar um pacote chamado previewer, dentro dele eu vou criar um package.json. E esse package.json, lembrando, ele tem que ter o nome dele, começar com arroba sass, que é o prefixo que a gente escolheu. E aí eu dou qualquer nome pra ele, eu vou botar previewer, que é mais fácil. Além disso, ele tem que ter uma versão, posso botar 0.0.0, aqui a versão é irrelevante, se eu não for publicar esse pacote no npm. Porque independente da versão que eu colocar aqui, quando eu for instalar essa dependência em outro pacote aqui do meu monorepo, eu boto asterisco na versão, então ele pega qualquer versão. Vou passar aqui um main, que indica qual que vai ser o primeiro arquivo acessado quando um outro pacote importar esse pacote do previewer. Então se eu fizer import sass-previewer, qual que é o arquivo que vai ser importado? Então eu vou criar aqui basicamente um arquivo index, posso criar ele como JS, como CJS, caso eu queira seguir o padrão de CommonJS. Ou MJS, caso eu queira seguir o padrão de ECMAScriptModules. Então eu passo aqui index.mjs. E aqui eu passo private como sendo true. O que isso aqui quer dizer? Quer dizer que esse pacote aqui não vai ser publicado no npm. É legal a gente usar esse private true, porque é bem comum em monorepos, que a gente tem muitos pacotes, alguns deles a gente querer publicar, principalmente se a gente está desenvolvendo alguma lib. E o private true, ele evita isso. Ele basicamente diz que esse pacote aqui nunca vai ser publicado no npm. Vou salvar isso aqui agora. E vou acessar aqui a pasta config, barra previewer. E aqui dentro eu vou instalar o previewer em si. Então pnpm install, menos D maiúsculo, previewer. E também, como é um projeto que eu vou utilizar o Tailwind, eu vou já deixar instalado aqui. O previewer, plugin, Tailwind, CSS como dependência de desenvolvimento. Aguarda que agora já estão com as duas dependências. E agora aqui dentro do meu index.module.js, eu posso colocar configurações do previewer. Aqui como eu não estou usando um arquivo typescript, eu teria que criar aqui const config, por exemplo. Dar um export default config, por exemplo. E aqui eu teria que colocar cada uma das configurações do previewer. Então, por exemplo, print width 80. Só que como você pode ver, ele não está trazendo para a gente o autocomplete aqui. Então, algo que eu posso fazer aqui é botar um comentário aqui em cima. Escrever arroba typedef. E aí eu faço um código typescript aqui dentro. import previewer.config E aí eu dou um nome previewer.config Como se eu estivesse fazendo um import desse config de dentro do previewer e renomeando ele para previewer.config E aqui embaixo eu boto mais um comentário e falo que o type desse config aqui é previewer.config Então agora, se eu der um ctrl espaço aqui dentro, eu tenho todas as configurações que eu posso colocar no previewer em si. Então aqui eu vou botar, por exemplo, tab width 2, que é o que eu estou utilizando. use tabs false, não gosto de usar tabs. semi false, que é o ponto e vírgula no final. single quote, vou botar como true. quote props, vou botar como as needed. Esse quote props aqui, ele bota aspas nas propriedades do objeto. Porque como você pode ver as propriedades do objeto aqui, elas não precisam de aspas. Mas eu posso fazer assim, funciona também. Então aqui, as needed quer dizer, ele só vai botar quando a propriedade precisar. Ou seja, se tiver um ifem, um número, uma coisa assim. JSX single quote, vou colocar como false. Trailing comma, vou colocar como s5. Então o JSX single quote, basicamente, nos atributos do HTML, dentro do JavaScript, do JSTX, se ele deve botar os atributos com, por exemplo, se eu tiver aqui um button type, aqui se deve ser com aspas simples, eu não gosto, prefiro aspas duplas, então eu boto false. Trailing comma, que é basicamente se ele deve botar essa vírgula aqui no final. Então a opção s5 é basicamente sim, ele bota essa vírgula no final. Bracket spacing, eu vou colocar como true. Que é um espaçamento entre a chave, entre as funções e a chave, se ele coloca espaçamento aqui ou não. Arrow perms, eu vou colocar como always. Isso aqui é basicamente se funções arrow function, se eu coloco parênteses por volta dos parâmetros, se não me engano. Bom, já nem lembro mais, mas esse é o padrão que eu uso. End of line, alto, isso aqui é pra padronizar o final de linha, CLRF, se não me engano é esse formato. Entre Windows, Unix e Mac. E JSX bracket, same line, não, então é bracket, same line, vou colocar como false. E é isso, nossa configuração do preader está pronta. E agora eu vou partir pra configuração aqui do ESLint. A gente tem a configuração do preader pra fazer a relação do tailwind, mas como eu não tenho o tailwind nem instalado aqui, eu vou só jogar um plugins. E botar aqui, preader, plugin, tailwind, CSS. Não tem mais muito o que eu customizar aqui, porque eu nem tenho o tailwind funcionando ainda, então não tem muito o que mexer. Bora lá então, eu vou agora pra configuração do ESLint. O ESLint aqui eu vou só editar esse que já tem aqui, então eu tenho o preader e vou editar agora esse do ESLint aqui. Vou deletar o readme dele aqui no package.json. A gente pode mexer aqui um pouquinho, tem o library, tá bom, next. Esse react-internal aqui eu não vou ter, eu vou apagar ele. Eu vou criar um chamado node, porque eu vou ter um projeto node, então eu vou ter três configurações do ESLint. Uma pro projeto next, uma pro projeto node, que é o back-end, e uma pro library, que é qualquer biblioteca que não é nem next nem node. Então essas bibliotecas que é código puro. E aqui em dependencies, eu vou cascar tudo aqui, tirar todas as dependências, vou salvar, vou entrar aqui na pasta eslint-config, vou dar um pnpm install só pra ele. Removeu as dependências, vê que eu removi. E eu vou instalar algumas dependências aqui dentro. Eu vou usar o eslint-config da Rocketseat, então vou instalar ele aqui dentro. Também vou instalar aqui o eslint-config, eu vou instalar também um plugin, eslint-plugin-simple-import-sort. Tô usando bastante esse plugin aqui, ele arruma a ordem dos imports, fica bem legal. E, além disso, eu vou usar o printer aqui dentro desse pacote, assim esses arquivos javascript que eu tenho aqui dentro, eles vão ser melhorados ali pelo printer, vai ficar mais bonito. Então, quando eu vou instalar uma biblioteca que é interna aqui, então eu quero instalar esse pacote do printer dentro do pacote do eslint. Nesse caso, eu não faço um pnpm install, eu venho simplesmente aqui nas dev-dependencies e escrevo. sas, que é o nome do projeto, barra-printer, e aí eu boto aqui, workspace, dois pontos, asterisco. Ele basicamente diz que essa dependência aqui, o pnpm tem que buscar ela dentro do meu workspace, que é, digamos, minha área de trabalho, meu monorepo, e eu quero qualquer versão dela. Então, como eu falei, as dependências que a gente não vai publicar no npm não tem necessidade de a gente ficar gerenciando a versão delas. Salvo aqui, e roda o pnpm install. A gente vai ver que agora, dentro do node-modules aqui do eslint, já vai ter a referência aqui, pra arroba sas e o printer. O código dele não tá realmente aqui dentro. Tá vendo que tem até essa flechinha aqui do lado? Isso aqui significa que ele é um symbolic link, ou seja, ele é como se fosse uma cópia em tempo real. Os arquivos não estão realmente aqui, quer dizer que essa pasta tá só refletindo o conteúdo da pasta do printer que eu tenho aqui embaixo. Isso é legal, porque se eu altero algum arquivo aqui embaixo, agora ele já reflete automaticamente aqui em cima, não preciso fazer uma nova instalação. E aí, no eslint-config aqui, a gente vai fazer algumas alterações. Então, bem simples, tá? Eu vou começar aqui pelo next, a gente pode remover tudo aqui. E aí, como eu vou usar javascript puro aqui dentro, como um JS, module exports, eu vou botar aqui arroba type import eslint.linter.config. Basicamente, eu tô falando que o type desse objeto aqui, segue isso aqui em cima. Então, se eu dou um ctrl espaço aqui dentro, teoricamente a gente deveria ter as configurações do linter.plugins. Ah, tá aqui, já tá tudo certo. Então, primeiro, o extends, eu vou estender aqui rocketseed-eslint-config-next, que é a configuração de next que a gente usa na rocketseed já. Plugins simple-import-sort, que foi aquele que a gente instalou. E aí, eu vou passar aqui rules, simplesmente simple-import-sort, barra imports, como sendo error. O que isso aqui vai fazer, né? Ele vai permitir que se você tiver com a extensão do eslint no seu VastCode, ou no seu editor, ele faça o autofix. Então, quando você salvar um arquivo, como ele vai identificar a ordem dos imports como um erro, e não como um aviso, ele vai fazer o autofix sozinho. Se a gente deixar o padrão, que é o aviso, ou seja, ele apenas avisa, mas não indica como sendo um erro realmente, o autofix não faz o fix automático. Vamos lá, a gente vai agora fazer o de node, então eu vou até copiar. Vou criar um aqui dentro chamado node.js, e a única coisa que muda é que no lugar do next, eu vou escrever node. O último agora vai ser o meu library, que eu vou simplesmente trocar por react. Este aqui eu vou usar mais para bibliotecas react mesmo, então por isso que eu estou importando a config do react aqui. Então pronto, a gente tem três configurações do eslint aqui dentro. E é isso. A gente agora vai... deixa eu dar um reload aqui no meu VS Code. Aqui no package.json, eu esqueci de fazer o seguinte. Está vendo que o preview não está funcionando aqui, então se eu boto espaço aqui no meio, boto tab, salvo, ele não está funcionando. Porque eu não criei um arquivo de configuração do preview, ou seja, ele acha que o preview não está habilitado neste projeto. Existem duas maneiras mais comuns de a gente ativar o preview em um projeto, que é criando um arquivo de config do preview na raiz aqui. Ou no package.json, eu posso vir aqui e botar preview, veja que ele tem essa opção aqui. E eu passo o nome do pacote que tem a configuração do preview, então é sass-preview, que é o nome do pacote que a gente tem aqui. Só de salvar isso aqui agora, teoricamente o preview já vai funcionar. Se eu não me engano, quando eu uso o preview, eu tenho que dar um reload, senão ele não funciona. Deixa eu ver aqui porque ele ainda não está funcionando. Ah, claro, ele não está funcionando porque eu não tenho a extensão do preview no meu VS Code. No meu VS Code eu tenho apenas a extensão do ESLint, que integra com o preview. Então, se eu não habilitar o ESLint neste projeto aqui, ele não vai aplicar o autofix do preview em si. Então o que eu vou fazer? Aqui junto com o preview, eu vou passar ESLint config. Então veja, o próprio pacote que configura o ESLint, ele também pode usar o ESLint. Então eu passo aqui para ele extens e passo qual vai ser a config que eu quero usar neste pacote. Aqui neste caso eu posso usar qualquer uma delas, mas eu vou usar a library.js. Salvo aqui agora. Teoricamente pronto, agora já está tudo funcionando. Salvo, veja que o preview já está funcionando. Não só o preview, mas o ESLint também. Legal a gente descobrir que dá para fazer isso, isso é bem interessante. Por último a gente tem configuração do TypeScript em si. A configuração do TypeScript, o que eu vou fazer aqui? Eu vou simplesmente apagar tudo. Por quê? Porque como a gente não criou nenhum projeto ainda, eu não vou mexer nestas configurações do TypeScript ainda. Eu vou apagar todas elas, porque nenhum pacote ainda está usando TypeScript. Aqui no nome do package.js, eu vou tirar TypeScript, acho que ficou muito grande isso aqui. Eu vou chamar simplesmente TSConfig. E aqui eu vou passar, bom, não preciso mexer em nada aqui por enquanto. Eu vou salvar isso aqui agora, lá no meu package.js, eu não estou usando ela em nenhum lugar. Então é isso, a gente deixou ESLint e o preview aqui bem configurados. E agora a gente vai começar a criar realmente as nossas aplicações. E aí com elas a gente cria também a configuração do TypeScript.