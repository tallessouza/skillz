WEBVTT Antes da gente continuar aqui criando a nossa aplicação, é importante que a gente entenda uma das grandes diferenças aqui de criar uma aplicação com o Next e de criar uma aplicação React tradicional. Por quê? Uma aplicação React tradicional, como é que ela funciona, né? Vamos entender aqui. Vou começar usando aqui um quadradinho para mostrar esse aqui que é o nosso app React. Vou botar aqui React. E aí imagina que a gente tem um back-end, aqui uma API criada, sei lá, Node, Java, qualquer coisa parecida, né? Uma aplicação React tradicional, quando o usuário acessa a nossa aplicação, ele sempre vai acessar diretamente o nosso front-end. No primeiro momento que o usuário chega na aplicação, ele acessa o nosso front-end, que é a aplicação React, como eu falei, tradicional, construída com JavaScript, CSS, HTML. essa aplicação por sua vez ela tem um peso ali né então vamos botar ali que uma aplicação hoje react comum vamos por que ela tenha cinco megabytes então o usuário precisa aguardar assim que ele acessa a aplicação ele precisa aguardar esses cinco megabytes serem carregados até isso ser carregado provavelmente ele vai ver uma tela em branco né sem nenhum conteúdo porque toda a interface da nossa aplicação é construída através do javascript então todas as divs tudo que aparece em tela é construído através do javascript Pode pegar qualquer aplicação React sua que você já tenha criado, se você desabilitar o JavaScript ou acessar ela com uma internet mais lenta, você vai ver que é isso que acontece. A tela fica branca até os dados serem carregados, até o JavaScript ser carregado. A partir do momento que o JavaScript foi carregado, aí sim a nossa aplicação começa a funcionar. Então, se eu estou acessando alguma tela que precisa de dados da API, é aí, nesse momento, que essa aplicação vai lá, busca os dados da API, por exemplo, uma lista de usuários, alguma coisa parecida... Depois que esses dados aqui são carregados, ele retorna esses dados para o nosso front-end, que então, depois desses dados serem carregados, vai lá e o HTML é construído, certo? Então, isso aqui é basicamente o fluxo tradicional de uma aplicação SPA, que a gente já comumente conhece a partir do momento que a gente começa a desenvolver uma aplicação React, Vue, Angular, desde 2013 mais ou menos, a gente cria aplicações nesse formato. Só que o Next, ele trouxe uma revolução nesse formato, que é um conceito que a gente chama de SSR, ou Server Side Rendering, que é o que eu vou te mostrar agora como que funciona. Então, eu vou copiar os mesmos elementos aqui, API e essa aplicação React, e vou colocar aqui do lado, porém agora, a gente vai mudar um pouquinho aqui a estrutura. Porque no Next, além da gente ter a nossa API, como eu falei que pode ser construída em qualquer linguagem, ela não está em nenhum momento associada com o front-end da nossa aplicação, porque ela é uma API, além de ter também o nosso código client-side, que é o que vai montar a interface, a nossa aplicação React em si, o Next introduz Uma terceira aplicação que a gente chama de BFF, que não é Best Friend Forever, vem de Backend for Frontend, ou posso também chamar isso aqui de Next Server. E o que é o trabalho dessa terceira aplicação aqui? Isso aqui é uma aplicação Node.js, como qualquer outra aplicação Node.js que a gente tem. Porém, essa aplicação Node.js aqui, ela não tem o propósito de servir uma API, assim como é esta aplicação. Ou seja, isso aqui que o Next introduz, não é ou pelo menos nativamente não deveria ser. Tem gente que acaba usando para isso e tudo bem, não vou entrar aqui no mérito de ficar comparando, depois a gente vai entender isso, mas nativamente, no primeiro momento, ela não serve para criar um back-end para a minha aplicação. Então, não serve para se comunicar com banco de dados, essas coisas assim. Para que serve isso aqui? Vamos refazer o mesmo fluxo que a gente criou aqui, porém agora pensando no Next. Quando o usuário acessar, a minha aplicação agora com o Next, né? A partir do momento que a gente tá usando o Next, esse aqui é o fluxo que vai acontecer, tá? Assim que o usuário chega na aplicação, ao invés de ele acessar a aplicação front-end diretamente, ele vai acessar o endereço desta aplicação. Então, quando a gente acessa ali no navegador, ah, localhost 33000, né? Que é hoje o endereço que a gente tá usando ali pra nossa aplicação. A gente não está acessando diretamente o código client-side da aplicação, o código React. A gente está acessando este servidor Node aqui, que é o servidor que está parando em pé no momento que a gente roda o dev, o comando npm run dev aqui na nossa aplicação. Ou seja, a partir do momento que o usuário chega na aplicação, ele vai abrir este servidor Node. E qual é a responsabilidade desse servidor Node aqui? Esse servidor Node vai fazer o seguinte... Qual página o usuário está tentando acessar? Então, imagina que eu estou tentando acessar lá a página que a gente até tinha criado, né? auth barra signin, ou depois a gente tirou o auth, né? A gente está acessando somente signin. Então, o que esse servidor vai fazer? Tá, o usuário está querendo acessar a página signin. Quais dados de JavaScript, HTML e CSS que eu preciso para mostrar esta página exclusivamente e não todo o restante da aplicação? Lembra que eu falei que quando o usuário acessa a nossa aplicação SPA tradicional que não usa o Next? Ele precisa carregar todo o JavaScript, CSS, HTML, não somente da página que ele está acessando, mas de toda a aplicação. Ou seja, 5 MB aqui é um número ainda bem... Bem assim... Eu estou sendo legal aqui, porque geralmente as aplicações são muito mais pesadas do que isso. No caso do Next, ele vai fazer... Opa! O usuário está querendo acessar a página Sign In. eu vou lá, o Next faz o seguinte, ele vai lá no código front-end e busca somente os assets, no caso o javascript, css, html necessários para que apenas essa página aqui seja renderizada então ele vai trazer ali uns 300kb, uns 200kb de javascript necessários para que essa página aqui seja renderizada. Ou seja, 5 MB vs 200 KB, isso aqui vai ser muito mais rápido. Então, esse servidor Node, como eu falei, ele vai lá, busca o código JavaScript, CSS, HTML necessário para criar o layout, o visual da página sign-in, e ele monta esta interface pelo lado do servidor. O que eu quero dizer com isso? Novamente, Node é uma tecnologia que permite a gente interpretar código JavaScript fora do navegador, concorda comigo? Foi a tecnologia pioneira, hoje existem outras como Dino, BAM, não vou entrar nesse mérito, mas o Node permite que a gente compreenda código JavaScript fora do âmbito do navegador. React é Javascript. Concorda comigo? Que no final das contas, React não passa de uma biblioteca de código Javascript. Ou seja, será que o Node consegue interpretar código React? E a resposta é sim. Ou seja, o que esse servidor aqui faz? Ele busca, como eu falei, todo o código React necessário para renderizar a página sign-in, e quando eu falo renderizar quer dizer montar em tela, criar, certo? E ele cria todo o HTML desta página aqui dentro ainda do Node, ou seja, dentro desta camada de servidor. E aí, quando o usuário acessa aqui a página, este backend aqui, esse backend criado pelo Next, retorna para o usuário. o HTML pronto desta página já com toda a interface montada aqui que veio do React. E aí uma das coisas interessantes é que se esta página que o usuário está acessando, página sign-in, precisa de algum dado da API, O servidor aqui do Next consegue também ir lá na API e buscar esses dados. Então, fazer essa transição aqui de dados entre a nossa API e o servidor Node aqui. O que eu quero resumir com tudo isso aqui? O Next não é uma biblioteca que funciona apenas pelo lado do cliente. Ela é uma biblioteca que funciona também pelo lado do servidor. E se eu for aqui na nossa aplicação Next, por exemplo, nessa página de produto que a gente criou... E olha que interessante... Eu vier aqui no inspecionar elemento, vier aqui nessa engrenagem, selecionar a opção Disable JavaScript, isso aqui vai fazer com que o JavaScript, no lado do cliente, seja totalmente desabilitado. Ou seja, React, nada disso vai funcionar no lado do navegador. Só que mesmo assim, se eu vier aqui e der um F5 na página, veja que ela continua funcionando. Se eu acessar aqui uma outra página da aplicação, tudo está funcionando normal. Olha que interessante. é claro que se eu tiver alguma funcionalidade aqui na minha aplicação que seja algo que precise de javascript no lado do navegador como por exemplo uma animação feita pelo javascript usuário clica aparece alguma coisa em tela né isso são coisas dinâmicas né coisas reativas do react né que precisam de javascript no lado do navegador é claro que nesse momento vai parar de funcionar mas o usuário nunca vai estar rodando a aplicação com essa opção aqui de disable JavaScript. O que eu estou te mostrando é que o Next, mesmo com essa opção aqui desabilitada, a interface continua aparecendo em tela. Isto porque a interface não está sendo criada depois que a página é exibida em tela. Lembra que isso aqui é o formato tradicional de uma aplicação React. E se você for em qualquer aplicação React que você já tenha criado que não use o Next, você vai ver que com o JavaScript desabilitado, a sua aplicação é simplesmente uma tela em branco. Por quê? Porque quando o usuário acessa a aplicação, todo o JavaScript precisa ser carregado para que então o HTML seja montado. Ou seja, com o JavaScript desabilitado, isso não vai acontecer, porque ele não vai conseguir montar o HTML. No Next, já é diferente. Quando o usuário acessa a aplicação, ele está acessando um servidor e não diretamente uma aplicação no navegador React, uma aplicação pura em React. E este servidor, antes de mostrar para o usuário que a aplicação foi carregada, Ele busca a página que o usuário está querendo acessar, monta esta interface pelo servidor e devolve o HTML já pronto para o usuário. Ou seja, este HTML aqui não precisa de JavaScript para ser montado, porque ele já foi montado por esta camada, por este servidor que o Next implementa assim que a gente roda a nossa aplicação. E esse aqui é um dos grandes diferenciais do Next e é o que faz ser possível a gente criar coisas muito legais que a gente vai criar daqui para frente e usar algumas funcionalidades novas do React que hoje em dia só funcionam dentro de frameworks que possuem essa arquitetura. O React, por exemplo, em 2020, eles lançaram um novo conceito, uma nova arquitetura chamada de Server Components, que está aqui nesse dia 21 de dezembro de 2020, criado aqui pelo Dan Abramov e por uma galera. E esse Server Components aqui é uma nova forma de a gente trabalhar com fetch de dados dentro de componentes. Isso aqui vai estar revolucionando e vai revolucionar ainda mais o front-end. E o Next hoje é o único framework que permite a gente usar essa funcionalidade de Server Components, porque ele tem essa arquitetura aqui, onde a gente tem um servidor node rodando junto com a nossa aplicação. Isso aqui é a única forma de a gente usar a nova arquitetura do React, até por isso que quando a gente vai criar uma aplicação React hoje em dia, ela recomenda a gente usar o Next.js. E a gente vai entender o que são esses server components, como que a gente pode fazer o uso disso agora nas próximas aulas.