WEBVTT Uma coisa que você com certeza percebeu é que quando a gente veio nesse componente Product, nessa página de produto, que ela é acessada com barra Catalog, barra Product, e aqui com um ID, uma cor, um tamanho, por exemplo, você pode ter percebido que quando a gente colocou o Async nessa função de produto ele deu esse warning né não é nenhum um erro necessariamente mas é um warning porque ele vai continuar funcionando mas isso aqui pode causar bastante dor de cabeça no futuro e esse warning que veio aqui ele veio por causa do s-link então se você não tiver com s-link configurado no seu editor não vai aparecer esse warning se a gente clicar aqui ele basicamente determina que a gente não utilize o async quando eu estou usando um Client Component. Lembrando que o Client Component é qualquer componente que a gente use a diretiva useClient, ou seja, são componentes em que o JavaScript para esse componente funcionar é enviado para o navegador e ele se torna um componente reativo, ou seja, um componente que vai funcionar como um componente tradicional do React. que pode sofrer atualizações conforme a gente faz atualizações de estado, dos hooks, do React, atualizações em componentes pais. Então, por que a gente não deveria utilizar uma Async Function para componentes que são Client Components? Quando a gente estava aprendendo aqui no nosso diagrama como funciona a diferença dos Client Components para o Server Components, uma das coisas que você tem que lembrar é que um Server Component, que é o componente que não tem essa diretiva useClient, ele é um componente em que o HTML dele vai ser gerado uma única vez. O que acontece? Quando o usuário faz uma requisição para uma página que é um server component, que não tem a diretiva useClient, o HTML dessa página ou daquele componente é gerado uma única vez, ou seja, esse processo do Next buscar o HTML, CSS, JS aqui de dentro do React e gerar essa interface vai acontecer uma única vez e aquele HTML devolvido para o usuário E acabou. Aquele HTML não é um HTML reativo, ele não vai sofrer mudanças no futuro. Ou seja, é um componente que vai ser criado uma única vez. Por isso, o async, nesse caso, não vai ter nenhum tipo de problema, porque aquele componente vai ser gerado uma única vez. Aquele código que está dentro dele só vai ser executado uma única vez. Quando eu tenho um componente que é do tipo client component, e aí eu coloco o async aqui dentro, qual que é o problema? esse código que está aqui dentro eu não posso garantir que ele vai ser executado uma única vez porque se eu tô num client component do react que é um componente que eu posso utilizar qualquer funcionalidade que a gente já conhece do react nativamente como por exemplo um estado então imagina que eu tenho aqui dentro um estado simples não conta e aí eu uso e os state começa ele como zero e aí esse botão aqui é de tocar tu que eu vou fazer é aumentar o count em mais um então eu pego o estado atual dele e faço um state mais um por exemplo só que é muito comum com certeza você já deve ter feito muitas vezes e aí o que acontece aqui né se eu colocar um console log aqui dentro em account o que a gente vai perceber aqui eu vou até tirar esse outro console log que eu tenho aqui eu vou salvar esse componente volto no navegador abre o inspecionar elemento venho em console e a gente vai ver aqui várias vezes o zero sendo exibido aqui dentro, certo? Então, olha só... Se eu venho aqui em adicionar ao carrinho, olha só... Está vendo que ele vai dando vários e vários logs aqui dentro? Só que está percebendo que todo esse código que está aqui dentro de um componente React, num componente tradicional do React, ele é executado de novo, novamente, toda vez que eu faço a alteração de um estado. Não só um estado, qualquer outro hook ou quando um componente pai desse componente sofre uma renderização, Ou seja, se eu tenho um componente assíncrono aqui, com await aqui dentro, com fetch e tudo mais, esse código vai executar múltiplas vezes, todas as vezes que houver uma alteração no estado. E isso é muito ruim, porque eu não quero fazer a requisição de dados, por exemplo, toda vez que esse componente aqui sofrer uma renderização. Então, por isso que a gente não pode usar async em componentes que são client components. Nesse caso, Diego... como é que eu faria então né o fat de dados em um componente que é um client componente né e aí a gente volta às raízes do react não é porque a gente está usando next que todas as requisições dos nossos componentes todos os fat de dados dos nossos componentes precisam acontecer dessa maneira que a gente tinha mostrado aqui na nossa home, que é simplesmente colocando um await dentro do componente. Não. Isso aqui é só para quando a gente pode usar essa funcionalidade de server components. Mas nós podemos continuar, e devemos inclusive, é a recomendação da própria documentação do Next.js, fazer Fetch de dados dentro de Client Components usando estratégias de Fetch de dados que a gente já usava no React anteriormente aos Server Components. Então, por exemplo, eu posso, num Client Component, tirando o Async aqui, fazer o Fetch de dados, por exemplo, usando o UseEffect, como a gente já vinha utilizando no React. E aí, aqui dentro do UseEffect, fazer o Fetch, buscar na API, preencher o estado, Ou até usando alternativas como o React Query, que agora é o Thumbstack Query. E não é V4, acho que já tá na V5, né? Isso. O SWR também é uma solução muito famosa da própria Versal pra fazer algo muito parecido ao React Query. O próprio URQL, caso você use a GraphQL, tem várias soluções. Então, isso é muito importante a gente entender. Não é porque o Next traz novas possibilidades que a gente vai simplesmente esquecer de tudo que a gente já fazia no React e não utilizar isso. Mas tem mais uma coisa que eu gostaria de te mostrar aqui que é muito interessante. O que é? Hoje, esse componente aqui, Product, ele só é um Client Component. porque eu tenho duas coisas nele que identificam que ele precisa ser um client component. Vou te desafiar a pensar nisso. Quais duas coisas que tem aqui dentro desse componente meio que forçam esse componente a ser um client component? A primeira coisa é que nós temos um EventListener, ou seja, toda vez que eu tenho algum tipo de evento em algum elemento HTML, como onClick, onSubmit, onMouseOver, onHover, onFocus, ou qualquer coisa parecida, eu preciso que aquele componente, este elemento aqui, esteja dentro de um ClientComponent. Outra coisa é, eu tenho um estado. Estado só pode ser criado dentro de ClientComponents. e eu consigo perceber que algo precisa ser um client component quando eu tenho eu penso da seguinte forma esse componente aqui o html dele vai ser estático ou ele vai ter interatividade ou seja o usuário pode interagir com ele de alguma forma né aparecer alguma coisa em tela se ele vai ter interatividade então ele vai ser um client component beleza e aí O que acontece? Imagina que dentro dessa página, desse product aqui, eu queira fazer um fetch de dados. Então, eu queira, por exemplo, buscar os dados aqui como a gente está fazendo na home. Vou até copiar aqui da home e trazer aqui para dentro. E aí, como eu falei, eu não posso ter o await aqui dentro. Então, eu teria que transformar isso aqui em um async. Só que eu não posso transformar o produto em um async porque ele é um client component. E aí, agora, a gente cai numa das coisas mais importantes ao começar a utilizar Server Components no React. Quando a gente transforma um componente em um Client Component, eu estou assumindo que todo JavaScript necessário para este componente funcionar vai ser enviado para o navegador. Só que você concorda comigo que esse componente, apenas 10% dele precisa de JavaScript pra funcionar? O que eu quero dizer com isso? Essa div não precisa de JavaScript, ela é estática. Esse parágrafo não precisa porque ele é estático. Esse outro parágrafo não precisa. Isso aqui também não precisa. Esse botão precisa. Concorda comigo? Somente esse botão aqui precisa de JavaScript pra funcionar. Então, uma dica, algo muito importante pra gente fazer no Next é abstrair ao máximo os Client Components. O que isso quer dizer? Sempre que eu vou ter algum tipo de interatividade, se eu conseguir isolar essa interatividade em um novo componente, melhor. Porque eu vou enviar menos JavaScript pro navegador. Ou seja, se somente esse botão aqui depende de JavaScript, no lado do navegador, eu crio um novo componente, como por exemplo, Add to Cart button, que é o botão de adicionar o carrinho. Aqui eu crio um componente normal, então, addToCartButton, declaro ele como um client component, como a gente já tinha visto, e trago aqui pra dentro toda a funcionalidade desse botão. Então, trago o botão em si, trago o meu estado, trago o meu AddToCart aqui pra dentro importo aqui o UseState do React e agora vou mostrar aqui junto ao adicionar o carrinho o nosso count pra gente visualizar que isso tá funcionando, certo? salvo isso aqui agora tiro agora o UseClient, tiro o UseState tiro aqui qualquer menção ao ClientComponent e aqui dentro agora coloco o nosso componente AddToCartButton E agora, veja só, eu tenho um fetch de dados acontecendo dentro do meu product, porque agora o meu product não é mais um client component, ele não tem mais o useClient, eu posso fazer ele um componente async, usando await aqui dentro, e dentro desse server component eu estou chamando um componente que é um client component, que é o AddToCartButton. Esse componente em si tem a diretiva useClient e agora o que eu fiz é todo o JavaScript para montar este componente aqui não vai ser enviado ao navegador, mas o JavaScript para este componente aqui que é muito menor, aí sim vai ser enviado para o navegador. Ou seja, eu estou enviando menos, uma menor quantidade de JavaScript para o navegador. Olha só, vou voltar lá na aplicação, a gente vem aqui na página do produto do F5, veja que continua funcionando e o adicionar ao carrinho também continua funcionando sem problemas nenhum. Olha que interessante, né? Então, isso aqui é uma das coisas mais importantes e que a gente vai ter que exercitar bastante na nossa cabeça para entender que Quanto menos UseClient a gente colocar no nosso código, e não é menos UseClient, mas quão menor for os componentes que a gente coloca o UseClient, melhor. Porque significa que eu estou enviando menos JavaScript para o navegador, menos JavaScript para o navegador, mais rápido a nossa aplicação vai ser carregada para o usuário final.