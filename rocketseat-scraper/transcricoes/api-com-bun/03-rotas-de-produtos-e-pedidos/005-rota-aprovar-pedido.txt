WEBVTT bora criar mais uma rota na aplicação já que a gente já criou uma rota aqui que lista o pedido né vamos criar uma de mutação né que faz uma alteração uma criação seja lá o que for vou criar a rota de aprovar um pedido então aprove order que que essa rota faz né e aqui é importante o que acontece né todas essas ações de modificação do status do pedido então o pedido ele começa como pendente depois ele vai pra é processando e tudo mais né tudo isso são ações que cara a gente poderia fazer uma rota só né que o nome dela é tipo alterar status do pedido só que é mais fácil agora no começo mas mais para frente conforme a gente vai precisando adicionar regras negócios diferentes começa a tornar o negócio mais difícil porque tipo assim Para eu aprovar o pedido, a única regra de negócio é que esse pedido esteja pendente. Eu só posso aprovar pedidos que o status estão pendente. Certo? Só que isso começa a tornar cada vez mais complexo no futuro. Porque para eu cancelar um pedido, não faz sentido eu poder cancelar um pedido que o status já é entregue ou em entrega. Né? Então, tipo assim, começa... Cada uma dessas alterações de status, né? Mudar o status de um pra outro, por mais que pareça uma ação simples, porque é só a troca do status no banco de dados, elas vão ter regras de negócio completamente diferentes. Por isso que eu vou criar uma rota diferente pra cada uma. Não vou criar uma única rota de alteração de status e pronto. O front-end chama a mesma rota pra todos os status. Então, o que que eu vou fazer? Approve, order, new, leisure, use, auth, ponto. Aqui eu vou usar o patch. barra orders, order id, barra approve, async, aqui eu vou validar os parâmetros, order id, string, Aqui eu vou pegar o getCurrentUser set params mais ou menos igual ao do outro né então const orderId igual parms const restaurantId igual await get currentUser if se eu não tenho restaurantId throw new UnauthorizedError e é isso agora aqui embaixo const order igual await db.query.orders.findfirst quero encontrar um pedido onde o id dele seja igual ao order id se eu não encontrar um pedido setStatus 400 messageOrderNotFound await db.updateOrders setStatusProcessing aqui falta eq, que eu importo drizzle orders.id é igual a order.id só que aqui faltou a nossa regra de negócio que é se order.status for diferente de pending porque eu só posso aprovar pedidos que estão pendentes também vou dar um erro 400 e return message E aí a gente volta aqui no servidor a gente vai lá no banco de dados pega uma ordem que tá pending qualquer uma aqui ó verifica no teu banco uma que o status tá pending é impossível que em 200 orders você não tenha dado sorte de uma vir como pendente se você deu essa sorte roda o seed de novo vai lá no vamos aqui é criar uma nova requisição a approve order ela vai usar o método patch a barra approve E é isso. Salvo ela aqui só pra não perder. Vou na outra aqui também e salvo. No hopscotch a gente tem que salvar as requisições conforme a gente for fazendo. Send. You can only approve pending orders. É o que? Ah, eu não colei o ID, eu não tinha trocado o ID aqui em cima. Deixa eu até ver se eu peguei o ID certo. Eu tava usando o ID do outro pedido ali que a gente tava usando pra detalhe. Salvar. Beleza. Pra eu verificar que deu certo, vou aqui no Order Details, vou trocar aqui ó, pegar o ID dessa aqui e ver se ele tá retornando, ó, Processing, certinho. Legal, vamos lá continuar agora com as demais rotas da aplicação.