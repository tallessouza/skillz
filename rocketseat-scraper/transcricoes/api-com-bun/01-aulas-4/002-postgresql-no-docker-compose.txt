WEBVTT Nessa aula o que a gente vai fazer é configurar o nosso banco de dados que vai estar executando aqui dentro da aplicação e pra isso eu vou utilizar o docker e por isso eu já vou criar aqui um docker compose na raiz do nosso projeto e se você não tem instalado o docker na sua máquina é só você simplesmente procurar por docker desktop e baixar ele funciona tanto no mac no linux no windows sem problemas nenhum Aqui no Mac eu estou usando uma ferramenta diferente para o Docker, caso você esteja usando Mac, que é o Orbstack. E caso você queira testar, ela é igual o Docker, você simplesmente baixa igual o Docker Desktop, só que eu percebi que ela inicializa os containers de uma maneira um pouquinho mais rápida, mas assim, nada que vai ter uma grande diferença no seu dia a dia. e agora aqui dentro do docker compose eu vou começar especificando a versão docker compose vou usar a versão 3.7 por enquanto aqui eu vou criar meu primeiro serviço que é o Postgres eu vou estar utilizando a imagem da bitnami então bitnami barra postgres-sql na última versão, latest vou fazer o redirecionamento de porta aqui então eu uso a porta 5432 para a porta 5432 do Postgres e aqui vou colocar algumas variáveis ambiente a primeira delas que é a postgres user que é o meu usuário do postgres vai ser docker a segunda é a postgres password que é a senha que eu vou estar utilizando meu banco de dados também vou utilizar docker e a última postgres db que é o nome do banco de dados que eu vou estar criando quando esse container subir. Além disso, eu vou criar aqui um volume. Isso aqui facilita para eu conseguir salvar os arquivos do meu banco de dados entre as reinicializações do container. E aqui eu vou criar um volume chamado PostgresData. Não preciso passar nenhuma opção para ele, então só dessa forma já está bom. e aqui eu vou criar um mapeamento do meu volume, né? E aqui eu vou falar que, então, PostgresData, que é o mesmo nome que eu criei aqui embaixo, vai ser mapeado para barra Bitnami barra PostgresSQL, ou seja, todos os dados do Postgres que estão dentro do meu container vão ficar salvos nesse volume aqui, que no final das contas é só uma pasta. vou salvar isso aqui agora dentro do meu terminal com o docker instalado deixa eu ver se não tem nenhum outro contêiner rodando olha que eu tenho um contêiner rodando do outro de um outro projeto aqui eu vou fazer um stop nesse contêiner e agora sim eu posso dar um docker compose app-d e aí ele já vai subir caso você não tenha essa imagem claro que ele vai baixar a imagem antes na sua máquina para depois conseguir rodar e agora a gente já tem um banco de dados postgres rodando eu consigo visualizar os logs desse container para garantir que ele está funcionando utilizando o docker logs com id do container e o menos f para ele ficar aberto nos logs e assim caso venha algum log novo ele já mostra automaticamente Perfeito, se eu tenho essa mensagem de Database System is ready to accept connections, quer dizer que eu já estou pronto para conectar a minha aplicação no meu banco de dados.