WEBVTT Fala Dev, bora lá continuar nossa aplicação e o que a gente vai fazer nessa aula é iniciar o processo de autenticação que a gente vai criar aqui no nosso app e a gente vai utilizar uma estratégia de autenticação diferente da convencional que é via e-mail e senha porque a gente vai utilizar uma estratégia de autenticação passwordless E o nome passwordless vem do inglês, password-less, que significa sem senha. Ou seja, a gente vai utilizar uma estratégia de autenticação que não exige senha. E não necessariamente existe uma única forma de criar uma autenticação passwordless, porque hoje existem várias. Hoje, por exemplo, o usuário pode entrar ali com o e-mail, ou com o celular receber um código via SMS, e aí ele entra com esse código, o famoso OTP, One Time Password, e nesse caso, ele também é um passwordless login, ou seja, ele está fazendo login sem senho. Mas aqui a gente vai utilizar uma estratégia que foi mais conhecida como Magic Link, que é o seguinte, o usuário coloca o e-mail dele e ele recebe no e-mail um link para fazer a autenticação, quando ele clica nesse link, ele já é redirecionado para a nossa aplicação front-end e já estando autenticado. Ou seja, só dele clicar no link, a nossa aplicação já assegura que esse usuário está autenticado, então a gente usa o próprio acesso ao e-mail que esse usuário tem para definir se ele está autenticado ou não. Ou seja, uma estratégia onde facilita para a gente, para a gente não precisar ficar guardando informações credenciais sensíveis, como senha, dentro da nossa aplicação. Isso ajuda bastante no futuro, pensando em segurança. E também facilita para o usuário, porque ele não precisa ficar criando mais uma senha para mais um serviço. Mas, como eu falei, existem várias estratégias. A gente poderia utilizar aqui também login social, que resolveria o problema de forma parecida. Mas essa estratégia de senha que a gente vai utilizar via MagicLink é muito mais simples de ser implementada, não precisa que a gente faça integração com algum provider como Google, GitHub ou Twitter, Meta ou qualquer coisa semelhante. Então, o que a gente vai começar fazendo? Como eu falei, A nossa estratégia de autenticação vai consistir em o usuário fazer o input do e-mail dele e a gente vai lá ver se esse usuário existe na base de dados. Se ele existe, a gente cria um código único de acesso, que ainda não é o JWT em si, não é o token de acesso. É um código que vai ser salvo no banco de dados, diferente do JWT, que o JWT é stateless, então ele não é salvo em nenhum banco de dados ou qualquer coisa parecida. A gente vai criar um token que vai ser usado uma única vez para validar que quando aquele usuário clicou naquele link dentro do e-mail para fazer a autenticação, esse link que ele está clicando foi gerado pela nossa aplicação. Ou seja, para eu garantir que algo que o usuário vai fazer no futuro, que é clicar num link para se autenticar. Para eu garantir que isso foi gerado pela minha aplicação, eu preciso ter armazenado isso na minha aplicação de alguma forma. Então, eu vou utilizar essa estratégia de salvar esse link, esse código, na verdade, dentro do banco de dados. Para isso, aqui dentro de Schema, eu vou começar criando uma tabela para armazenar esses links de autenticação, vou chamar de AuthLinks, usar aqui pgtable, alt links e aqui eu vou começar colocando alguns campos primeiro eu vou ter o id que ele é semelhante aos outros ids que a gente tem então vou até copiar aqui da de outra tabela vou utilizar o seu id novamente pra parte de de id e aqui como eu falei a gente vai ter um código esse código a gente pode gerar ele basicamente qualquer coisa pode ser um pode ser um id pode ser um seu id ele tem que ser o único importante dele é que ele seja aleatório e digamos o mais possível único que a gente conseguir né E aí, uma das coisas importantes é que a gente precisa também ter o relacionamento aqui, né? Então, toda a autenticação, todo o link de autenticação vai pertencer a um único usuário. Então, aqui eu vou salvar o userId e ele vai referenciar a tabela users.id E aqui ele não pode ser nulo. E por último, eu vou salvar um createdAt Caso a gente queira fazer uma validação no futuro, por exemplo, aquele link só é válido por uma semana, alguma coisa assim, né? Então, é bom salvar a data que aquele link foi criado para a gente poder fazer esse tipo de validação no futuro. E eu venho aqui no index novamente, exporto dentro de alphalinks. E aqui deixa eu fazer o seguinte, eu estou com o meu banco aqui parado, então eu vou aqui dar um docker compose app-d e aí a gente já vai rodar os scripts aqui então eu vou rodar aqui o bun generate para ele gerar minha migration como a gente pode ver já tá aqui né então ela cria a tabela alphalinks e cria a constraint foreign key com a tabela users e agora eu já posso rodar o comando migrate que vai fazer o processo de rodar as migrations e pronto o banco de dados já está sincronizado com o nosso esquema aqui e agora o que a gente vai fazer é criar nossa rota para o usuário fazer login as nossas rotas não vai ser na verdade uma única rota Porque a gente vai ter uma primeira rota, que é a rota de login em si. Essa rota aqui vai enviar o e-mail com o link de login. E eu vou ter uma outra rota, que é uma rota de callback, que é a rota que vai... a rota que vai ser acessada quando o usuário clicar no link do e-mail. Essa rota aqui que vai validar o token do usuário e criar o JWT e redirecionar o usuário para o front-end. Então, basicamente, essa aqui vai ser a estrutura de rotas da parte de autenticação da nossa aplicação.