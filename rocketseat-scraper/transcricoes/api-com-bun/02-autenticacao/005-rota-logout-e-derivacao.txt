WEBVTT O que a gente vai fazer nessa aula é criar uma rota de logout da aplicação, porque como a gente está salvando o token em algum lugar, que é dentro dos cookies, tem um motivo da gente criar uma rota de logout, que é a gente remover isso de dentro dos cookies. Mas muitas aplicações em que esse token é enviado para o front-end e aí o front-end se vira para salvar esse token em algum lugar e depois reenvia isso nas próximas requisições. Nesse caso, a rota de logout não faz tão sentido, mas aqui faz. Então, vou criar aqui uma rota chamada signout. E ela vai ser uma rota bem simples. signout igual new lesion. Aqui eu vou ter que dar um useAuth. ponto post, sign out, async. e aqui eu vou basicamente fazer o logout do usuário e esse logout vai ser simplesmente pegar a função removeCookie e chamar ela passando o parâmetro ALF e pronto, posso voltar aqui dentro do server e dar um use sign out e pronto rota de logout já foi criada sem problemas o que eu vou fazer pra gente aproveitar essa aula que foi curtinha é o seguinte né uma das coisas que eu gosto de fazer aqui no elige até para te mostrar uma funcionalidade muito legal aqui no elige é a parte de de derivação dentro de um módulo que é o seguinte tá vendo aqui que Nos módulos, por exemplo, esse sendAuthLink, ou melhor, nesse authenticateFromLink, onde a gente faz o use do módulo de autenticação, a gente recebe algumas funções novas aqui dentro, como por exemplo, que eu mostrei lá no signCookie, removeCookie, setCookie. E no módulo que eu não uso, esse auth, eu não tenho essas funções. Ou seja, o que acontece? O Elijah, como ele tem essa forma modular da gente criar a aplicação, e aí eu vou dando use nos módulos, eu consigo estender, digamos, o que é retornado aqui dentro para que as minhas rotas possam acessar. E por que eu estou falando isso? Por exemplo, aqui dentro do módulo de autenticação, aqui no auth.ts, eu posso vir aqui embaixo e criar uma função chamada... Usar uma função chamada drive, ou se lá derive, não sei exatamente como é que se pronuncia isso. Mas a gente, simplesmente daqui de dentro, retorna qualquer coisa. Então, por exemplo, se eu retornar aqui, é name Diego, tá vendo? Agora, como eu retornei uma variável chamada name e dentro dela tem Diego, nos módulos que eu uso, esse módulo de autenticação, por exemplo, authenticateFromLink, Aqui dentro eu vou ter name, tá vendo? E dentro desse name aqui vai ter uma string que é a string Diego. Então, veja que tudo que eu retorno dentro desse deriver aqui, ele é repassado para os módulos que usam do módulo de autenticação. E por que eu estou te falando isso? Porque aqui dentro a gente pode retornar funções. Então, por exemplo, aqui nesse authenticateFromLink, se eu quiser pegar essa parte aqui que eu crio o token e vamos abstrair ela daqui de dentro e botar lá no módulo de autenticação. Então, aqui no módulo de autenticação eu vou retornar uma função chamada signUser, a função assíncrona e ela vai basicamente fazer aquele processo de criar o token e aí fazer o setCookie e no final não preciso fazer nada. E aí agora eu preciso acessar esse setCookie, esse jwt aqui dentro, eu posso acessar ele aqui do próprio deriver ou então de um control espaço, aqui dentro tem o jwt, tem o setCookie, E aí o que eu vou fazer? Esse offlinkfromcode, eu não tenho acesso a ele aqui dentro, então eu vou fazer ele ser recebido via parâmetro aqui nessa função. Então aqui nessa função eu vou receber a payload. E aí o tipo dele aqui, se eu tentar usar typeof, e aí eu vou pegar isso aqui, vou mover para uma variável, const, jwtPayload, Se eu fizer aqui, passar o JwtPayload, se eu fizer type of JwtPayload, passar o mouse aqui em cima do payload, tá vendo que ele não retorna exatamente um objeto TypeScript, ele retorna um objeto do TypeBox, tá vendo que até aqui é tObject, tString, não é String especificamente. Por isso eu preciso botar aqui por volta dele, static que vem de dentro do Elijia também e aí eu passo o type of jwd payload se eu passo o mouse no payload agora vejo que ele vem com um objeto nativo do próprio TypeScript né e aqui agora dentro desse sign aqui eu posso passar diretamente já payload dessa forma salvo, agora volto lá no meu módulo e vou importar o signUser que é a função que eu acabei de criar e aí eu vou dar um await signUser e agora eu só passo aqui o meu sub que é o authLinkFromCode userId e meu restaurantId que é o managedRestaurant.id com um pontinho de interrogação aqui no meio porque ele pode ser undefined Então, veja que agora eu só, digamos, separei a parte que lida com autenticação e salva nos cookies ali para dentro do módulo de autenticação. E eu posso fazer a mesma coisa com a parte do logout aqui, por mais que seja apenas remover o cookie. Então, aqui dentro eu vou no auth, vou retornar aqui um outro método aqui chamado signout, removeCookie, isso aqui vai vir de dentro aqui. removeCookie não é uma promise, então isso aqui não precisa nem ser async salvo, volto lá na rota de signout e agora veja que aqui dentro eu não preciso pegar o removeCookie, posso pegar direto o signout e aí eu chamo só que é o mesmo nome, acabou ficando o mesmo nome aqui em cima então não fica tão legal né deixa eu ver o que eu posso fazer aqui Nesse caso, eu acho que é melhor eu renomear aqui... InternalSignOut... Uma coisa assim, só para a gente não ficar com o mesmo nome e não acabar confundindo depois. Beleza, o resto fica tudo igual e você pode testar e a aplicação vai estar funcionando sem problemas nenhum, igualzinho. A gente só repassou essa parte dessas funcionalidades para dentro do módulo de autenticação.